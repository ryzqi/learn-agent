上一章解决了 Skill 按需加载的问题：Agent 启动时只看到技能目录，需要哪项能力，再通过 `load_skill` 把正文放进当前会话。

但目录瘦下来，只解决了初始上下文。任务一旦开始，读文件、跑命令、调用工具，历史仍会不断增长。尤其是一轮里出现多个大工具结果时，几次调用就可能把真正有用的目标、约束和最新进展淹没。

加大上下文窗口只能推迟问题。真正要做的是管理信息的生命周期：大结果放到哪里，旧内容怎样降级，哪些消息绝不能拆开，以及压缩后如何追溯原始记录。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaH86aBMHoZ6aXZCyZcvsAweBaia0icgfXUHOb6OPb8eJDMCs5Q6SsYQ2icxwjkUDwTVUeGZuiaOy1DvKRDQ31puo8IHpl2HTA6P1Kg/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「上下文膨胀」的两种代价：装不下（长度约束）和看不清（Context Rot），以及为什么后者更隐蔽。
2. 复述四层压缩的执行顺序和触发条件，并解释为什么必须「先便宜，后昂贵」。
3. 分清 canonical history、request history、transcript、artifact 这四个对象——搞混它们是压缩功能出 bug 的头号原因。
4. 解释什么是「消息原子组」，以及为什么一轮里有 3 个工具调用时，按消息下标裁剪一定会出事。
5. 算清「30,000 bytes」这类阈值到底在量什么——它不是字符数，不是 token 数，也不是窗口大小。
6. 指出压缩发生的确切时间点，以及这个时间点为什么直接决定 KV Cache 能不能复用。
7. 判断哪些能力本章**没有**做（自动 prompt-too-long 恢复、`compact` 工具、文件内容自动恢复），避免照着教程宣称不存在的行为。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的消息历史与工具配对不变量 | 第 1 章 | 压缩的所有裁剪动作都必须继续满足这条不变量 |
| workspace 硬边界与路径校验 | 第 2 章 | artifact 只能落在 workspace 内的固定目录 |
| 工具结果的 `isError` / `errorCode` 结构 | 第 2、3 章 | 落盘时必须原样保留错误元数据 |
| Hook 的 `PostToolUse` 时机 | 第 4 章 | 落盘发生在整轮 Hook 全部跑完**之后** |
| `task` 与子 Agent 的历史隔离 | 第 6 章 | 子 Agent 有自己的历史，压缩互不干扰 |
| `load_skill` 的正文会常驻历史 | 第 7 章 | 这正是本章要处理的一类大结果 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 上下文膨胀 | 历史消息随任务进行不断变长，最终挤占甚至挤掉关键信息 |
| Context Rot | 窗口没满但关键信息被大量无关内容稀释，模型「看得见却找不到」 |
| canonical history | 会话事实的唯一来源，由 `AgentRunner` 持有，压缩永不改写它 |
| request history | 每次请求模型前从 canonical 复制出的快照，压缩只作用在它身上 |
| artifact（产物） | 落盘到磁盘的完整内容文件，消息里只留路径和预览 |
| transcript | 模型摘要前保存的 canonical history 快照，JSONL 格式，用于追溯 |
| 消息原子组 | 一条 assistant 消息 + 它的全部 tool result，不可拆分的最小裁剪单位 |
| 落盘（persist） | 第一层压缩：把超大工具结果写成文件，消息里换成引用 |
| snip | 第二层压缩：整组裁掉历史中段，插一条省略标记 |
| micro | 第三层压缩：把较早工具组的**结果正文**换成占位符，保留调用结构 |
| 模型摘要 | 第四层压缩：调一次模型，把长历史换成 5 字段结构化 JSON |
| 有界预览 | 落盘后消息里保留的头尾片段，按 UTF-8 byte 截断 |
| 独占发布 | 写文件时若目标已存在就明确失败，绝不覆盖 |
| prompt-too-long | 供应商因**输入**过长拒绝请求；注意它和输出截断是两件事 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼一次真实的压缩   ← 一个大文件读进来，消息里到底剩下什么
  ↓
② 为什么加大窗口不管用     ← 两种代价，以及压缩的真正目的
  ↓
③ 四层管线与执行时机       ← 先便宜后昂贵，以及它和 KV Cache 的关系
  ↓
④ 四个对象千万别搞混       ← 本章最关键的概念区分
  ↓
⑤ 消息原子组              ← 为什么不能按消息下标裁剪
  ↓
⑥ 第一层 → 第四层          ← 逐层拆解，每层都有可复现的数字
  ↓
⑦ transcript 与缓存        ← 压缩之后怎么追溯、怎么不重复压缩
  ↓
⑧ 两个还没接线的 API       ← 主动压缩与响应式恢复的真实边界
  ↓
⑨ 离线测试 + 运行 + 实验    ← 动手验证 + 五个小实验 + 排错
```

---

## 先看一眼一次真实的压缩

抽象讲「压缩」不如直接看一次运行。下面这段全部来自 `code/chapters/ch08/tests/ch08-compaction.test.ts`，是断言过的真实数据。

工作区里有一个大文件：

```typescript
const content = "甲".repeat(10_001);          // 10001 个中文字符
await writeFile(join(workspace, "large.txt"), content, "utf8");
```

一个中文字符在 UTF-8 里占 3 bytes，所以这个文件是 30,003 bytes——刚好超过默认的单项落盘阈值 30,000。

模型第一轮调用 `read_file` 读它。工具正常返回了全部 30,003 bytes。**但写进历史之前，这批结果先过了一道处理。** 测试断言第二次请求模型时，历史里最后一条消息是这样：

```text
<persisted-tool-result>
path: .agent_tutorial/artifacts/tool-result-3f9a1c72e8b04d5f9c1e7a2b6d8f0e41.txt
original_bytes: 30003
head_preview:
甲甲甲甲甲……（最多 2,000 bytes）
tail_preview:
……甲甲甲甲甲（最多 2,000 bytes）
</persisted-tool-result>
```

对应的断言是：

```typescript
expect(persisted.content).toContain("<persisted-tool-result>");
const relativePath = /^path: (.+)$/m.exec(persisted.content)?.[1];
expect(relativePath).toMatch(/^\.agent_tutorial\/artifacts\/tool-result-[a-z0-9]+\.txt$/);
expect(await readFile(join(workspace, ...relativePath.split("/")), "utf8")).toBe(content);
```

最后那一行是重点：**完整的 30,003 bytes 一个字节都没丢**，它在磁盘上那个文件里，随时可以重新读。

三个数字变化对比一下：

| | 进历史的内容 | 大小 |
| --- | --- | ---: |
| 不压缩 | 30,003 bytes 正文 | 30,003 bytes |
| 本章第一层压缩后 | 路径 + 原始大小 + 头尾各 2,000 bytes 预览 | 约 4,100 bytes |
| 磁盘上的 artifact | 完整正文，逐字节一致 | 30,003 bytes |

历史里少了 86%，信息一点没丢——只是换了个存放位置。

### 这条消息还是合法的工具结果吗

是。测试专门钉住了这一点：

```typescript
expect(result.history[2]).toEqual(persisted);
validateToolPairing(result.history);
```

`result.history[2]` 是 canonical history 里的第 3 条消息，它和模型看到的那条完全一致；`validateToolPairing()` 确认「每个 `tool_call` 恰好配一个 `tool` 结果」这条第 1 章立下的不变量仍然成立。

换句话说，落盘替换的只是**消息的 content**，不是消息本身。`tool_call_id` 没变，配对关系没变，消息条数没变。这是本章所有压缩动作共同遵守的底线。

### 会弹审批框吗

不会。这个测试传进去的审批器是会抛异常的那个：

```typescript
class UnexpectedApproval implements ApprovalProvider {
  async decide(_request: PermissionRequest): Promise<PermissionDecision> {
    throw new Error("read_file should not request approval");
  }
}
```

测试通过，说明 `decide()` 从来没被调用。落盘是 Harness 内部的存储决策，不是一次需要用户授权的写操作——它写的是运行时自己的状态目录，不是用户的项目文件。

### 工具列表变了吗

没变。同一个测试断言：

```typescript
expect(model.requests[0]?.tools.map((tool) => tool.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write", "task", "load_skill",
]);
```

还是第 7 章那 8 个工具，**没有新增 `compact` 工具**。这一点非常重要，后面「主动压缩 API 不是 compact 工具」会专门展开：本章的压缩是 Harness 自动做的，不是模型能主动调用的能力。

---

## 为什么加大窗口解决不了问题

新手最直觉的反应是：上下文不够就换个窗口更大的模型。但这只推迟问题，因为膨胀有两种代价，而换模型只缓解第一种。

**代价一：装不下。** 这个好理解。历史超过窗口，请求直接被供应商拒绝。

**代价二：看不清。** 这个隐蔽得多。窗口还没满，但关键信息已经被稀释了。`ai-agent-book` 把这种现象叫 Context Rot：模型需要从几万 token 里反复「检索」相关片段，当无关内容占了大头，真正重要的目标、约束和最新进展就被埋掉了。

第 5 章讲过为什么会这样：注意力机制没有「越靠后权重越高」的保证。一条重要约束躺在第 3 条消息里，后面堆了 40 条工具结果，它被关注到的概率就是真的下降了。

所以压缩的目标不该理解成「省钱」，而是：

> 把「需要模型现场归纳的原始记录」换成「可以直接读取的结构化结论」。

这个区别决定了设计取向。如果只为省 token，最省的做法是把旧消息统统删掉；但那样模型会忘记自己在干什么。本章的四层压缩每一层都在回答同一个问题：**这部分信息现在还需要模型「看见」，还是只需要「能找回来」？**

- 还需要看见 → 留在消息里。
- 只需要能找回来 → 落盘，消息里留路径。
- 连找回来都未必需要，但结论要保住 → 换成占位符或结构化摘要。

---

## 核心原则：先便宜，后昂贵

第 8 章把压缩分成四层，严格按成本从低到高执行：

| 层 | 做什么 | 成本 | 触发条件 |
| ---: | --- | --- | --- |
| 1 | 大工具结果落盘，消息里留路径和预览 | 一次磁盘写 | 单项 > 30,000 bytes，或整轮合计 > 200,000 bytes |
| 2 | `snip`：整组裁掉历史中段 | 纯内存操作 | 消息组数 > 50 |
| 3 | `micro`：较早工具组的结果换占位符 | 纯内存操作 | 存在第 4 组之前的工具组 |
| 4 | 模型摘要：换成 5 字段 JSON | **一次模型请求** | 前三层做完仍 > 50,000 bytes |

关键在于第四层前面有三道闸。前三层不花模型调用，能挡住多少算多少；只有实在挡不住，才付第四层那次请求的钱和延迟。

如果顺序反过来——每轮先叫模型摘要——那么每一轮都要多一次请求、多一份延迟，还要承担摘要出错的风险。四层排序不是随便定的，是按「便宜的先上」排出来的。

![图片|809](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaGvRxd2otg2t41orry2wFymWIbict6smb3fWUrnW7b5oWwppgVSX1Ifia9Hic24ExSD6KicoHHxLLicw1DF8R7w9Xib1Gic3Hjx8WI7kE/640?from=appmsg&watermark=1#imgIndex=2)

### 压缩发生在什么时候（这决定了 KV Cache 能不能复用）

这是新手最容易忽略、但工程上影响最大的一点。压缩**不是**在一次请求内部改上下文，而是在两次模型调用**之间**预处理请求历史。

一次完整的时间线：

```text
第 N 轮
├─ ① 模型返回 tool_calls
├─ ② 依次执行工具，跑完 PreToolUse / PostToolUse
├─ ③ ★整批结果交给 compactToolResults()  ← 第一层在这里
├─ ④ 结果按原 tool_call_id 顺序写入 canonical history
│
第 N+1 轮
├─ ⑤ 从 canonical history 复制 request history 快照
├─ ⑥ ★prepare()：snip → micro → 超阈值才摘要  ← 第二、三、四层在这里
└─ ⑦ 带着压缩后的 request history 请求模型
```

为什么这个时间点重要？因为模型服务商的 KV Cache 是按**前缀**命中的：

```text
[System Prompt] [工具定义] [消息 1] [消息 2] ... [消息 N]
 ↑─────────── 静态前缀，一直可以命中 ──────────↑
                                    ↑ 从这里改动，后面全部失效
```

System Prompt 和工具定义在本章是静态的，永远可复用。request history 里从被改动的那条消息开始，后面的缓存全部失效，但**它前面的前缀仍然有效**。

由此得到两条设计选择：

1. **大结果集中在整轮结束时落盘**，而不是边执行边落盘。这样一轮里只产生一次历史改动。
2. **模型摘要推迟到超过字节阈值后**，而不是每轮都做。摘要会把一大段历史替换成一条消息，改动点非常靠前，代价最大。

`ai-agent-book` 第 2 章把这个取舍总结成一句话：**批量压缩比频繁压缩更有利于 KV Cache。**

### 代码接在哪两个位置

实际实现集中在 [`features/compaction.ts`](code/chapters/ch08/src/features/compaction.ts)，接线位于 [`bootstrap.ts`](code/chapters/ch08/src/bootstrap.ts) 和共享的 [`core/loop.ts`](code/chapters/ch08/src/core/loop.ts)。第 8 章入口 [`chapters/ch08.ts`](code/chapters/ch08/src/chapters/ch08.ts) 只选择固定的 `P08` Profile，不复制 Agent Loop。

组合根创建**同一个** `CompactionManager`，把它接到两个明确边界：

```typescript
historyProcessor: compactionManager,                    // 模型请求前准备 request history
toolResultProcessor: async (results) =>                 // 整轮工具执行完成后处理结果
  (await compactionManager.compactToolResults(results)).results,
```

一个对象，两条边界，对应上面时间线里的 ③ 和 ⑥。P08 没有复制循环，也没有注册额外的工具。

---

## 四个对象千万别搞混

压缩最容易出错的地方，是把所有 `messages` 都当成同一份可变列表。当前实现明确区分四个对象：

| 对象 | 是什么 | 会被请求级压缩改写吗 |
| --- | --- | --- |
| canonical history | `AgentRunner.#history` 保存的会话事实；`RunResult.history` 返回它的不可变快照 | **不会** |
| request history | 每次模型调用前从 canonical history 复制出的快照，交给 `prepare()` | 会，但只影响这次及后续可复用的请求快照 |
| transcript | 进入模型摘要前保存的 canonical history JSONL 快照 | 不会；文件独占发布，冲突不覆盖 |
| tool-result artifact | 大工具结果的完整 UTF-8 内容 | 不会；canonical history 里保存的是路径和预览 |

用一张图看清它们的关系：

```text
                        ┌─────────────────────────────────────┐
   工具执行完一整轮  →   │  compactToolResults()               │
                        │  超大结果 → 写 artifact 文件         │
                        └──────────────┬──────────────────────┘
                                       │ 消息里换成 <persisted-tool-result>
                                       ▼
                        ┌─────────────────────────────────────┐
                        │  canonical history（唯一事实来源）   │  ← 永远不被压缩改写
                        └──────────────┬──────────────────────┘
                                       │ 复制一份不可变快照
                                       ▼
                        ┌─────────────────────────────────────┐
                        │  request history                    │
                        │  snip → micro → （超阈值）模型摘要   │
                        └──────────────┬──────────────────────┘
                                       │        ↑
                                       │        └── 摘要前先把 canonical 存成 transcript
                                       ▼
                                   请求模型
```

### 一个看似细小、实际关键的边界

**大工具结果是在写入 canonical history 之前处理的。** 所以 canonical history 里从来没有保留过那段超大正文，它保留的是 `<persisted-tool-result>` 引用。完整正文只在独立 artifact 里。

而 `snip`、`micro` 和模型摘要都发生在**请求快照**上。它们能让模型下一次看到更短的历史，却不会倒过来删掉 `AgentRunner.#history` 里已经存在的消息。

这个区分有测试直接钉住。下面这段来自 `compaction.test.ts`：

```typescript
const first = [userMessage("start"), ...exchange("old", "large old result")];
const prepared = await manager.prepare(first);

expect(prepared[2]).toEqual(toolMessage(COMPACTED_TOOL_RESULT, "old"));  // 请求历史被压了
expect(first[2]).toEqual(toolMessage("large old result", "old"));        // 传进来的历史没变
```

同一个下标，两个不同的值。**这就是「压缩只作用于请求快照」这句话的实际含义。**

为什么必须这样设计？因为 canonical history 是会话事实。如果压缩能改写它，就会出现这些问题：

- `RunResult.history` 返回的记录和真实发生过的事情不一致，排查问题时无从下手。
- 压缩一旦有 bug，损坏是不可逆的——原始记录已经没了。
- 缓存判断失去基准：你不知道「这份历史是否已经压过」。

### 完整的请求路径

```text
一整轮 ToolResult
    → 大结果落盘，生成路径与预览
    → 作为 tool messages 追加到 canonical history
    → 复制 request history 快照
    → snip（整组裁中段）
    → micro（整组降级旧结果）
    → 规范 JSONL 的 UTF-8 bytes 是否 > 50,000？
       ├─ 否：直接请求模型
       └─ 是：先保存 canonical transcript，再摘要 request history
```

---

## 第 8 章的验收契约

先把这一章要钉住的结论列出来。后面每一节解释其中一条，「用离线测试证明行为」那一节会逐条验证。

| # | 契约 | 反面是什么 |
| ---: | --- | --- |
| 1 | 阈值按真实 UTF-8 byte 计算，恰好等于阈值仍保留在消息里 | 用字符串 `length` 冒充字节数 |
| 2 | 所有裁剪以完整消息组为单位，永不产生孤儿 `tool` 消息 | 按消息下标切片，边界落在 assistant 和它的结果之间 |
| 3 | 请求级压缩不改写 canonical history | 原地修改 `#history`，事实来源被污染 |
| 4 | 模型摘要前先写 transcript；transcript 写失败就不调用摘要模型 | 先摘要后存档，摘要失败时现场已丢 |
| 5 | artifact 独占发布，ID 冲突明确失败而不是覆盖 | 后写的悄悄盖掉前一份 |
| 6 | 落盘失败时整批结果统一替换为受控错误，配对关系不变 | 部分原文 + 部分引用混在同一轮，或结果数量对不上 |
| 7 | 相同历史复用缓存，纯追加历史复用已压缩前缀 | 每轮重复摘要，反复把旧长历史送进模型 |

---

## 消息原子组：不能只看相邻两条消息

这一节是本章的地基。理解它，后面三层压缩才不会看糊。

OpenAI 的一条 assistant 消息可以同时包含**多个** `tool_calls`。这时合法历史不是简单的「一条调用配一条结果」，而是一个不可拆分的原子组：

```text
assistant(tool_calls=[call_a, call_b])   ← 一条消息，两个调用
tool(tool_call_id=call_a)                ← 结果 1
tool(tool_call_id=call_b)                ← 结果 2
   共 3 条消息，但只算 1 个消息组
```

### 按消息下标裁剪会出什么事

假设历史有 7 条消息，你想「只保留最后 5 条」：

```text
[0] user      "开始"
[1] assistant tool_calls=[a, b]     ← 切在这里，这条被丢了
─────────────────── 保留线 ───────────────────
[2] tool      结果 a                 ← 孤儿！它的 assistant 不在了
[3] tool      结果 b                 ← 孤儿！
[4] assistant tool_calls=[c]
[5] tool      结果 c
[6] assistant "完成"
```

结果是两条孤儿 `tool` 消息。发给 OpenAI 会直接报错，因为协议要求每个 `tool` 消息必须能对应到前面某条 assistant 的 `tool_call_id`。

反过来切也一样坏：保留了 assistant 的调用却丢了部分结果，模型会看到「我发起了两个调用，只回来一个」。

### 实现怎么解决

内部的 `validatedGroups()` 先调用 `validateToolPairing()` 验证配对，再把历史划成两类组：

- **普通消息**（user / system / 无工具调用的 assistant）单独构成一组。
- **一条带工具调用的 assistant 消息 + 它的全部 tool results** 共同构成一个「工具交换组」。

分组逻辑很直接——看到带调用的 assistant，就往后多吃 `toolCalls.length` 条：

```typescript
if (message.role === "assistant" && message.toolCalls.length > 0) {
  const end = index + 1 + message.toolCalls.length;   // 1 条 assistant + N 条结果
  groups.push(createMessageGroup(snapshot.slice(index, end)));
  index = end;
  continue;
}
groups.push(createMessageGroup([message]));           // 普通消息单独成组
index += 1;
```

同一份历史按组重新看一遍：

```text
组 0: [0] user "开始"                                    ← 1 条消息
组 1: [1] assistant tool_calls=[a,b] + [2] tool a + [3] tool b   ← 3 条消息
组 2: [4] assistant tool_calls=[c] + [5] tool c          ← 2 条消息
组 3: [6] assistant "完成"                                ← 1 条消息
```

7 条消息 = 4 个组。`snip` 的保留边界、`micro` 的替换单位、响应式压缩保留的尾部，用的都是**组**，不是消息下标。所以一轮包含两个、三个甚至更多工具调用时，也不会切出孤儿。

早期示例常见的做法是「检查截断点前后各一条消息来修补边界」。这对同轮多个工具调用不可靠——你不知道该往前看几条。**先建组再切片**，边界天然落不到组内部。

这项约束不是 Prompt 建议，而是运行时契约：压缩前后都会再次验证配对，非法历史会在写 transcript 或调用摘要模型**之前**就失败。有测试直接钉住这个顺序：

```typescript
await expect(manager.compactProactively([toolMessage("orphan", "missing")]))
  .rejects.toThrow(/orphan tool result/);

expect(summarizer.histories).toEqual([]);                       // 摘要模型没被调用
await expect(readFile(join(workspace, ".agent_tutorial"), "utf8")).rejects.toThrow();  // 目录都没建
```

一条孤儿消息进来，既不会浪费一次模型请求，也不会在磁盘上留垃圾。

---

## 第一层：大工具结果按 UTF-8 bytes 落盘

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaFiad3ybIEQfzZnd92OXibH5vvEsrTWABkTQMsHqibQet6qsibyOErd2dDueaRniajYD5bhyfqpI8dfQYfjDph84ViabB5bSnSwUSEHc/640?from=appmsg&watermark=1#imgIndex=4)

### 输入是「一整轮」，不是单条结果

`CompactionManager.compactToolResults()` 接收的是一整轮已经执行完的 `ToolResult` 数组。共享 Loop 会等这一轮所有 handler 和 `PostToolUse` 都跑完，再把整批结果交给它。

为什么必须整批？因为有两条不同的规则：

- **单项规则**：这一条结果自己太大了 → 落盘。
- **批次规则**：每条都不算大，但加起来太大 → 挑最大的几条落盘，直到剩下的进预算。

批次规则需要看到全部结果才能算，逐条独立判断做不到。

### 阈值算的是真实 UTF-8 字节数

```typescript
const contentBytes = Buffer.from(result.content, "utf8");
const sizeBytes = contentBytes.byteLength;
```

不是 `result.content.length`。这个区别对中文特别重要：一个中文字符在 UTF-8 里占 3 bytes，所以 10,001 个中文字符的 `.length` 是 10,001，但 `byteLength` 是 30,003。用 `.length` 判断会让阈值实际放大三倍。

默认规则：

| 规则 | 默认值 | 说明 |
| --- | ---: | --- |
| 单项落盘阈值 | 30,000 bytes | **严格大于**才落盘；恰好等于仍保留在消息里 |
| 批次预算 | 200,000 bytes | 未落盘结果合计仍大于它，就按原始字节从大到小继续落盘 |
| 头部预览 | 2,000 bytes | 落盘后消息里保留的开头片段 |
| 尾部预览 | 2,000 bytes | 落盘后消息里保留的结尾片段 |
| 落盘位置 | `.agent_tutorial/artifacts/tool-result-<id>.txt` | 完整内容，逐字节一致 |

「严格大于」这个边界有专门的测试：

```typescript
const manager = new CompactionManager({ ..., persistThresholdBytes: 6 });
const outcome = await manager.compactToolResults([toolSuccess("abcdef")]);   // 正好 6 bytes

expect(outcome.results).toEqual([toolSuccess("abcdef")]);   // 原样保留
expect(outcome.artifacts).toEqual([]);                      // 没有产生 artifact
```

### 批次预算怎么挑

把阈值调小，用四条结果实测一次（这个脚本在「五个动手小实验」里可以自己跑）：

```text
四条结果 bytes = [ 10, 50, 30, 10 ]
合计 = 100
单项阈值 = 100（都不超）   批次预算 = 60（合计超了 40）

落盘的下标 = [ 1 ]
  [0] 留在消息里 "AAAAAAAAAA"
  [1] 落盘
  [2] 留在消息里 "CCCCCCCCCCCCCCCCCCCCCCCCCCCCCC"
  [3] 留在消息里 "DDDDDDDDDD"
```

四条都没到单项阈值，但合计 100 超了预算 60。于是挑最大的那条（下标 1，50 bytes）落盘，剩下 50 bytes 进预算，循环结束。**只落了一条，不是把所有超预算的都落掉**——够用就停。

对应的实现是先按大小排序，再一条条加进落盘集合直到进预算：

```typescript
for (const index of ranked) {           // ranked 按字节数从大到小
  if (retainedBytes <= this.#batchBudgetBytes) {
    break;                              // 已经进预算，停止
  }
  if (selected.has(index)) { continue; }
  selected.add(index);
  retainedBytes -= sizes[index];
}
```

排序时用 `rightSize - leftSize || left - right`：字节数相同就按原下标排，保证结果确定可复现。

### 预览为什么按 byte 截，以及会丢字

注意，预览的 2,000 是 **bytes**，不是字符。如果边界刚好切在一个多字节字符中间，那个不完整字符会被丢弃。

把预览限制调小，逐个值实测：

```text
previewHeadBytes=3 -> "甲"     实际 3 bytes / 1 字
previewHeadBytes=4 -> "甲"     实际 3 bytes / 1 字
previewHeadBytes=5 -> "甲"     实际 3 bytes / 1 字
previewHeadBytes=6 -> "甲乙"   实际 6 bytes / 2 字
previewHeadBytes=7 -> "甲乙"   实际 6 bytes / 2 字
```

限制 4 bytes 时，第二个字符只装得下 1 个字节，于是整个字符被丢掉，实际只输出 3 bytes。**所以中文内容的预览实际字符数通常少于「限制 ÷ 3」。**

实现用一个最多试 4 次的循环完成——UTF-8 单个 code point 最多 4 bytes，所以最多丢 3 个字节就能对齐：

```typescript
const decoder = new TextDecoder("utf-8", { fatal: true });
for (let offset = 0; offset <= Math.min(content.byteLength, 3); offset += 1) {
  const candidate = boundary === "head"
    ? content.subarray(0, content.byteLength - offset)   // 头部：从尾巴往回削
    : content.subarray(offset);                          // 尾部：从头往后削
  try {
    return decoder.decode(candidate);
  } catch {
    // 继续削，直到能解码
  }
}
```

`fatal: true` 是关键：默认的 `TextDecoder` 会把不完整字节变成 `` 替换字符，静默产生乱码。这里选择让它抛错，然后主动削掉边界。

### 写文件的三条安全规则

产物写入不是简单的 `writeFile`，有三层保护：

**1. 先写临时文件并同步，再独占发布最终路径。**

```typescript
const temporary = join(dirname(path), `.${basename(path)}.${randomUUID()}.tmp`);
handle = await open(temporary, "wx");     // wx = 独占创建，已存在就失败
await handle.writeFile(content);
await handle.sync();                      // 强制刷盘
await handle.close();
await link(temporary, path);              // 硬链接发布；目标存在会 EEXIST
```

最终路径只在成功时出现，不会看到写了一半的文件。重复 ID 会以 `EEXIST` 失败，转成 `ArtifactConflictError`，**绝不覆盖**。

**2. 目录要复查真实身份。** `ensureSafeDirectory()` 创建目录后再次 `lstat()` 和 `realpath()`，拒绝符号链接或指向 workspace 外的路径。测试用一个指向外部目录的 junction 验证过：落盘失败，外部目录里也不会出现文件。

**3. 同一批中途失败要清理。** 已经创建的本批产物会被删掉，避免留下一半引用一半原文的混乱状态：

```typescript
} catch (error) {
  await removeCreatedArtifacts(artifacts.map((a) => a.reference), error);
}
```

`removeCreatedArtifacts()` 的返回类型是 `Promise<never>`——它一定会抛异常。清理本身失败时，用 `AggregateError` 把原始错误和清理错误一起抛出，不掩盖任何一个。

### 落盘失败以后 Loop 怎么办

`compactToolResults()` 抛出的错误会被 Loop 的 `#processToolResults()` 接住，整批结果统一替换成受控错误：

```typescript
} catch {
  return Object.freeze(
    calls.map(() => toolError("tool_result_processing_error", "Tool result processing failed")),
  );
}
```

注意 `calls.map()`——替换后的结果数量仍然等于调用数量，「每个调用恰好一个结果」的契约继续成立。整批统一替换而不是部分保留，是为了避免同一轮里混着原始正文和落盘引用，让后续排查无从判断。

还有一个细节：错误结果落盘时，`isError` 和 `errorCode` 必须原样保留。

```typescript
if (original.isError) {
  const errorCode = original.errorCode;
  if (errorCode === undefined) {
    throw new CompactionError("error tool result lost its errorCode");
  }
  transformed[index] = Object.freeze({ content: rendered, isError: true, errorCode });
}
```

一个超大的错误输出（比如失败的构建日志）落盘后，它**仍然是一个错误结果**。如果这里丢了 `errorCode`，第 3 章建立的错误分类就断了。

---

## 第二层：snip 按组裁掉中段

默认消息组数**超过** 50 才触发 `snipCompactHistory()`。触发后保留：

```text
[ 最前面 3 个完整组 ]
[ 一条 system 标记：[Compacted: N message groups omitted] ]
[ 最后面 46 个完整组 ]
```

46 是算出来的，不是写死的：

```typescript
const keepTailGroups = maxGroups - keepHeadGroups - 1;   // 50 - 3 - 1 = 46
```

那个 `- 1` 是留给省略标记本身的位置。所以压缩后总组数正好等于 `maxGroups`。

### 为什么要留最前面 3 组

历史开头通常是用户的原始需求和最早的关键约束。这部分信息在整个任务里一直有效，裁掉它模型就会忘记自己在干什么。留 3 组是一个经验值：够装下「用户提问 + 最初的一两次探索」。

中段是最可能过期的部分——中间那些试错、读了又发现不相关的文件、走过的弯路。尾部是最新进展，必须留。

### 统计的是组，不是消息条数

这一点必须再强调一次。一个包含两次工具调用的工具交换组实际占 3 条消息，但在 snip 预算里只算 **1 组**，而且只能整组保留或整组省略。

实测一次（`maxGroups=3, keepHeadGroups=1`，所以 `keepTailGroups = 3-1-1 = 1`）：

```text
原始 7 条消息 / 4 个组：
  组 0: user "开始"
  组 1: assistant(calls=2) + tool(a) + tool(b)
  组 2: assistant(calls=1) + tool(c)
  组 3: assistant "完成"

snip 后 3 条消息：
  [0] user      "开始"
  [1] system    "[Compacted: 2 message groups omitted]"
  [2] assistant "完成"

bytes: 642 → 143
```

组 1 和组 2 被整组省略，标记里的数字正好是 2。**没有出现任何孤儿 `tool` 消息**——这是按组切片的直接好处。

标记里的数字算法是：

```typescript
const omitted = groups.length - keepHeadGroups - keepTailGroups;   // 4 - 1 - 1 = 2
```

它统计的也是组数，所以标记说「省略了 2 个消息组」，实际省掉的是 5 条消息。这个措辞是刻意的：告诉模型「有东西被拿掉了」，同时不给它一个会误导的消息条数。

### 配置校验

`validateSnipConfiguration()` 会拒绝三种不合理配置：

```typescript
requirePositiveInteger("maxGroups", maxGroups);
requirePositiveInteger("keepHeadGroups", keepHeadGroups);
if (maxGroups < 3) {
  throw new RangeError("maxGroups must leave room for head, marker, and tail groups");
}
if (keepHeadGroups > maxGroups - 2) {
  throw new RangeError("keepHeadGroups must leave room for marker and tail groups");
}
```

`maxGroups` 至少要装下「头 + 标记 + 尾」三部分，`keepHeadGroups` 不能把标记和尾部的位置吃掉。这些校验在构造时就跑，不会等到运行期才炸。

---

## 第三层：micro 只降级旧工具结果

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaHibxNmOfSEomAhqhzHBNm5aXSfyFSXgH6uEz3azPEHjibnRTaF1mbGkCa3aDhBYKmYvuECftNU44XXFMO1ZuW8GjNCPTmbIh2B8/640?from=appmsg&watermark=1#imgIndex=6)

`microCompactHistory()` 默认完整保留**最近 3 个工具交换组**。更早的工具组里所有结果内容都替换成：

```text
[Earlier tool result compacted. Re-run if needed.]
```

这句话里的 `Re-run if needed` 是给模型的提示：内容没了，但你可以重新调一次工具拿回来。

### 它保留什么，替换什么

| 元素 | micro 之后 |
| --- | --- |
| assistant 的 `tool_calls` | **完整保留**，包括工具名和参数 |
| `tool_call_id` | **完整保留**，配对关系不变 |
| tool 结果的 content | 替换成占位符 |
| 消息条数 | 不变 |

为什么保留调用而只换结果？因为「我调用过 `read_file('config.json')`」这个事实本身是有价值的——模型知道自己做过什么，需要时可以再调一次。而那次调用返回的 500 行 JSON，可能早就不需要了。

实测一次（`keepRecentToolGroups=1`）：

```text
  [0] user             "开始"
  [1] assistant calls=2 null
  [2] tool(a)          "[Earlier tool result compacted. Re-run if needed.]"
  [3] tool(b)          "[Earlier tool result compacted. Re-run if needed.]"
  [4] assistant calls=1 null
  [5] tool(c)          "C 的内容"        ← 最近 1 组，完整保留
  [6] assistant        "完成"
```

注意 `[2]` 和 `[3]`：同一条 assistant 发起的两个调用，它们的结果**一起**被替换。如果只替换其中一个，整个原子组就处于半压缩状态，语义变得难以解释。

### 一个诚实的观察：micro 有时会让历史变长

上面这次实测的字节数是：

```text
micro 前: 642 bytes
micro 后: 720 bytes    ← 变长了 78 bytes
```

原因很直白：原始结果是 `"A 的内容"`（约 10 bytes），占位符是 49 个字符的英文句子。**当原始结果比占位符还短时，替换反而增加字节数。**

这不是 bug，是阈值设计的必然结果。micro 的设计目标是压缩那些几 KB 到几十 KB 的工具输出，在那个量级上，49 bytes 的占位符可以忽略不计。示例里之所以变长，是因为示例的结果太短了。

理解这一点对读代码很重要：**当前实现没有「结果超过 N 字符才替换」的分支。** 只要工具组排在最近 3 组之前，它的结果就会被替换。

```typescript
const compactCount = Math.max(0, toolGroupIndices.length - keepRecentToolGroups);
if (compactCount === 0) {
  return snapshot;
}
const compactedIndices = new Set(toolGroupIndices.slice(0, compactCount));
```

这个取舍换来两个好处：规则简单到可以一句话说清；结果完全确定，同样的输入永远得到同样的输出——这对下一节讲的缓存判断是必要条件。

### snip 和 micro 的执行顺序

`prepare()` 里的顺序是固定的：

```typescript
prepared = snipCompactHistory(prepared, { ... });     // 先 snip
prepared = microCompactHistory(prepared, { ... });    // 后 micro
```

先 snip 后 micro 更划算：snip 可能整组丢掉一些工具组，那些组就不必再走 micro 的替换逻辑了。反过来先 micro，就会给马上要被丢掉的组做无用功。

两者都基于新的不可变数组快照生成结果，**不会原地修改传入的历史**。每一步结束后都会重新 `validateToolPairing()`。

---

## 第四层：超过 50,000 bytes 才调用摘要模型

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaFEOEbHS6NMSiclbbIS5PTqdhGtH29icOhmKPoeP4vw15GIXtlF7zV4X3xugZY3ZhqIniaj0iapXxkVfOlQLAQgTVMknibroicUzVC30/640?from=appmsg&watermark=1#imgIndex=8)

前三层结束后，`historyUtf8Bytes()` 把请求快照转换成规范化的 OpenAI JSONL，再计算编码后的 UTF-8 bytes。默认只有结果**大于** 50,000 bytes 时，才进入模型摘要。

### 这个阈值到底在量什么

它**不是**：

- JavaScript 字符串的 `length`；
- 工具正文长度之和；
- 模型 tokenizer 给出的 token 数；
- 模型供应商承诺的上下文窗口大小。

它是当前实现选择的一条**确定性字节预算**，包含序列化之后的 role、`tool_calls`、`tool_call_id`、JSON 标点和换行符。有测试把这个算法完整写出来：

```typescript
const history = [userMessage("甲"), ...exchange("call-1", "乙")];
const expected =
  `${JSON.stringify({ content: "甲", role: "user" })}\n` +
  `${JSON.stringify({ content: null, role: "assistant", tool_calls: [...] })}\n` +
  `${JSON.stringify({ content: "乙", role: "tool", tool_call_id: "call-1" })}\n`;

expect(historyUtf8Bytes(history)).toBe(Buffer.byteLength(expected, "utf8"));
```

也就是说，两条只有 1 个中文字符的消息，算出来的字节数远大于 6——因为 JSON 结构本身占了大头。这是刻意的：模型请求真正要传的就是这些 JSON，量它才有意义。

为什么不用精确 token 数？因为那需要引入 tokenizer 依赖，而不同模型的 tokenizer 还不一样。字节预算能稳定触发压缩，代价是**不能保证 50,000 bytes 对所有模型都对应相同的 token 数**。这是一个明确的教学取舍，不是疏漏。

### 进入第四层的五个步骤，顺序很重要

```text
1. 先把「未经 snip / micro / 摘要的 canonical 快照」写成 transcript
2. 再让 summarizer 读取「已经过便宜层处理的 request history」
3. 摘要模型请求不携带任何工具
4. 只接受 finishReason === "stop"、无 toolCalls、非空且字段精确的 JSON object
5. 成功后，请求历史变成一条 system 消息，含 transcript 路径和五类信息
```

第 1 步和第 2 步用的是**两份不同的历史**，这是最容易看漏的地方：

```typescript
const transcript = await this.#writeArtifact("transcript", ".jsonl",
  serializeTranscript(transcriptHistory));           // ← canonical，完整
const compactedSummary = await this.#summarizer.summarize(summaryHistory);   // ← 已压缩
```

为什么这样分？因为两者目的不同：transcript 是给人排查用的档案，必须完整；摘要输入是给模型读的，越短越省钱。

先写 transcript 后调摘要的顺序也是刻意的。测试钉住了两个方向：

```typescript
// 摘要失败，transcript 仍然留着
await expect(manager.compactProactively([userMessage("unchanged")])).rejects.toThrow(/summary failed/);
expect(await readFile(".../transcript-failed-summary.jsonl", "utf8")).toContain("unchanged");

// transcript 写失败，摘要模型根本不被调用
await expect(manager.compactProactively(history)).rejects.toThrow(ArtifactPathError);
expect(summarizer.histories).toEqual([]);
```

翻译成一句话：**宁可留下一份没用上的存档，也不能出现「历史被压了但现场没存」的情况。**

### 摘要的五个字段

```text
current_goal            当前目标（非空字符串）
key_findings            关键发现（字符串数组）
files_read_or_changed   读过或改过的文件（字符串数组）
remaining_work          剩余工作（字符串数组）
user_constraints        用户约束（字符串数组）
```

实测一次摘要后的请求历史（把阈值调到 800 bytes 触发）：

```text
canonical bytes = 1029 > 阈值 800

第 1 次 prepare：摘要调用次数 = 1  请求历史条数 = 1
请求历史第 0 条 role = system
{
  "current_goal": "总结当前目录的 Markdown 文件",
  "files_read_or_changed": ["README.md"],
  "key_findings": ["README 描述了运行方式"],
  "kind": "compacted_history",
  "remaining_work": ["核对命令"],
  "transcript_path": ".agent_tutorial/artifacts/transcript-probe-1.jsonl",
  "user_constraints": ["示例只用 PowerShell"]
}
```

一整段历史变成了一条 system 消息。注意多出来的两个字段：`kind` 标明这是压缩摘要，`transcript_path` 告诉模型（和你）完整记录在哪。

### 摘要输出是不可信边界

模型的输出必须严格校验。`parseCompactionSummary()` 用的是**字段集合精确相等**，不是「包含所需字段」：

```typescript
const expected = ["current_goal", "files_read_or_changed", "key_findings",
                  "remaining_work", "user_constraints"];
if (typeof payload !== "object" || payload === null || Array.isArray(payload) ||
    Object.keys(payload).sort().join("\0") !== expected.join("\0")) {
  throw new CompactionError("summary model output must contain the exact fields");
}
```

多一个字段就拒。这看起来严格得过分，但理由很实在：摘要是要替换掉一大段真实历史的，如果模型自作主张加了字段，说明它没有严格遵守约定，那么它给的其他字段也未必可信。宁可让这次摘要失败（canonical history 不变，下一轮重试），也不接受一份可能有问题的摘要。

五种拒绝情形都有测试覆盖：

| 模型返回了什么 | 拒绝理由 |
| --- | --- |
| 带 `tool_calls` | `must not call tools`——摘要请求没给工具，它也不该调 |
| `finishReason: "length"` | `finishReason must be stop`——输出被截断，JSON 不完整 |
| 多一个 `extra: true` 字段 | `exact fields`——字段集合不精确匹配 |
| `key_findings: [" "]` | `invalid values`——数组项不能是空白字符串 |
| 不是合法 JSON | `not valid JSON` |

还有一层类型保险：

```typescript
const compactedSummary: unknown = await this.#summarizer.summarize(summaryHistory);
if (!(compactedSummary instanceof CompactionSummary)) {
  throw new TypeError("summarizer must return CompactionSummary");
}
```

即使有人自己实现了 `HistorySummarizer` 接口并返回了一个形状相似的普通对象，也过不去。摘要必须是经过构造函数校验的 `CompactionSummary` 实例。

### 自动摘要后不保留尾部消息

`prepare()` 走到第四层时，传的 `keepTailGroups` 是 0：

```typescript
const outcome = await this.#compactValidated(snapshot, prepared, 0);
```

所以自动摘要后请求历史就只有那一条 system 消息，**不会额外拼接最近几条消息**。

保留最近完整组是响应式恢复 API（`compactOnPromptTooLong`）的单独行为，默认保留 5 组。两者不能混为一谈——这是读代码时容易记串的一个点。

---

## transcript 保存的到底是什么

transcript 位于 `.agent_tutorial/artifacts/transcript-<id>.jsonl`。每行是一条内部 `ChatMessage` 转成 OpenAI wire shape 后的规范 JSON，使用 UTF-8、保留中文、以换行结尾。

它保存的是**模型摘要发生前的 canonical history 快照**，不是已经 snip / micro 过的请求历史。所以可以还原「请求级压缩之前」的消息序列。

### 但它不是全量备份

这是最容易误解的地方。超过工具结果阈值的正文，早在进入 canonical history 之前就写进了单独的 tool-result artifact；transcript 记录的是那个 artifact 的**路径和预览**。

```text
transcript-abc.jsonl
  {"content":"读取大文件","role":"user"}
  {"content":null,"role":"assistant","tool_calls":[...]}
  {"content":"<persisted-tool-result>\npath: .agent_tutorial/artifacts/tool-result-xyz.txt\n...","role":"tool","tool_call_id":"call-1"}
                                              ↑
                              完整正文在这个文件里，不在 transcript 里
```

要恢复完整现场，需要**同时保留 transcript 和它引用的产物文件**。

### 稳定 JSON：一个不显眼但很关键的设计

`serializeTranscript()` 使用键排序后的稳定 JSON：

```typescript
function sortJson(value: unknown): unknown {
  if (Array.isArray(value)) { return value.map((item) => sortJson(item)); }
  if (typeof value !== "object" || value === null) { return value; }
  return Object.fromEntries(
    Object.entries(value)
      .sort(([left], [right]) => (left < right ? -1 : left > right ? 1 : 0))
      .map(([key, nested]) => [key, sortJson(nested)]),
  );
}
```

所以相同内容的历史，不管字段是按什么顺序创建的，都得到**逐字节相同**的文本。这带来两个好处：

1. `historyUtf8Bytes()` 和 transcript 用同一份序列化，阈值判断和存档不会算出两个不同的字节数。
2. `historiesEqual()` 和 `historyStartsWith()` 才能可靠识别「完全相同」和「纯追加」两类缓存场景——下一节马上用到。

如果没有键排序，`{"role":"user","content":"x"}` 和 `{"content":"x","role":"user"}` 会被判成不同的历史，缓存立刻失效。

### ID 冲突不覆盖

默认 ID 用 UUID（去掉连字符），文件通过独占发布创建。若生成器给出重复 ID，第二次写入明确失败：

```typescript
const manager = new CompactionManager({ ..., idGenerator: idGenerator("same", "same") });
const first = await manager.compactProactively([userMessage("first")]);

await expect(manager.compactProactively([userMessage("second")]))
  .rejects.toThrow(ArtifactConflictError);

expect(await readFile(first.transcript.path, "utf8")).toBe(before);   // 第一份没被动
expect(summarizer.histories).toEqual([[userMessage("first")]]);       // 摘要模型没被第二次调用
```

第一份 transcript 完好无损，摘要模型也没有被白白调用一次。

---

## 缓存：相同历史不重复压缩，纯追加复用前缀

`CompactionManager` 在当前 Agent 实例内保存最近一次的 `#preparedSource`（压缩前的 canonical 快照）和 `#preparedHistory`（压缩后的请求历史）。

三种情况分别处理：

| 情况 | 做什么 |
| --- | --- |
| canonical 快照**完全相同** | 直接返回上次准备好的请求历史，不再 snip / micro / 写 transcript / 调摘要 |
| 新历史是旧历史的**纯追加** | 复用已压缩的前缀，只把新增后缀接上，再重新做有界检查 |
| 两者**不是**追加关系 | 从当前 canonical 快照完全重新准备 |

对应实现：

```typescript
if (cachedSource !== undefined && cachedHistory !== undefined &&
    historiesEqual(snapshot, cachedSource)) {
  return cachedHistory;                                    // 情况一：直接返回
}

let prepared: readonly ChatMessage[] = snapshot;
if (cachedSource !== undefined && cachedHistory !== undefined &&
    snapshot.length > cachedSource.length &&
    historyStartsWith(snapshot, cachedSource)) {
  prepared = Object.freeze([...cachedHistory, ...snapshot.slice(cachedSource.length)]);
}                                                          // 情况二：压缩前缀 + 新后缀
```

实测三次连续调用（阈值设成 800 bytes，第一次就会触发摘要）：

```text
canonical bytes = 1029 > 阈值 800

第 1 次 prepare：                    摘要调用次数 = 1   请求历史条数 = 1
第 2 次 prepare（内容相同）：         摘要调用次数 = 1   是同一个对象吗 = true
第 3 次 prepare（纯追加一条消息）：    摘要调用次数 = 1   条数 = 2
是「压缩前缀 + 新消息」吗 = true  最新回答

canonical 有没有被改写 = 没有（仍是 1 条 1000 字符的消息）
```

三次调用，摘要模型只被调了 **1 次**。第 2 次连对象引用都是同一个（`=== true`），第 3 次是「上次那条摘要 + 新追加的消息」。

### 重新压缩时，两份历史再次分道

如果追加内容让请求再次超过阈值，会发生一件值得注意的事。看这个测试的断言：

```typescript
expect(summarizer.histories).toEqual([firstCanonical, [...firstPrepared, appended]]);
//                                     ↑第一次        ↑第二次：缓存前缀 + 新后缀

// 但 transcript 写的是完整 canonical
expect(transcript.split("\n").filter(Boolean).map((line) => JSON.parse(line))).toEqual([
  { content: "x".repeat(1_000), role: "user" },        // 完整第一段
  { content: "y".repeat(1_000), role: "assistant" },   // 完整第二段
]);
```

第二次摘要时，summarizer 看到的是「缓存后的压缩前缀 + 新增后缀」——避免反复把旧长历史送进摘要模型。但这一次写出的 transcript 仍然来自**完整的新 canonical history**。

一句话总结：**省钱省在摘要输入上，不省在存档上。**

### 缓存的边界

缓存只存在于这个 `CompactionManager` 实例中：

- 不跨进程——重启就没了。
- 不持久化到磁盘。
- 不跨 Agent 实例——子 Agent 有自己的历史，也就有自己的准备过程。

它优化的是请求准备成本，**不是另一套会话事实来源**。canonical history 永远是唯一事实。

---

## 主动压缩 API 不是 `compact` 工具

`CompactionManager.compactProactively()` 提供一个独立 API：调用者可以显式要求「先保存完整 transcript，再把给定历史变成结构化摘要」。

它和 `prepare()` 有一个明确区别：**不经过 snip 和 micro**，直接对传入的历史做摘要。

```typescript
async compactProactively(history: readonly ChatMessage[]): Promise<HistoryCompactionOutcome> {
  const { snapshot } = validatedGroups(history);
  return this.#compactValidated(snapshot, snapshot, 0);   // 两个参数都是 snapshot
}
```

聚焦测试覆盖了成功、摘要失败、写入失败和 ID 冲突四条路径。

**但当前 P08 的工具注册表里没有 `compact` 工具**，模型不能在对话里主动调用它。前面「先看一眼一次真实的压缩」那一节的工具列表断言已经证明了这一点：还是第 7 章那 8 个工具。

P08 相比 P07 新增的是运行时的 ARTIFACTS / COMPACTION 两个能力位，而不是一个新工具名：

```typescript
export const P08: ChapterProfile = Object.freeze({
  chapter: 8,
  capabilities: new CapabilitySet([
    "loop", "powershell", "tool_registry", "files", "policy",
    "hooks", "todo", "subagent", "skills",
    "artifacts", "compaction",              // ← 本章新增的两位
  ]),
});
```

实际接入 Loop 的是每轮请求前自动执行的 `prepare()`，以及整批工具结果处理器。

因此，下面这种说法**不符合**当前实现：

> ❌ 「模型觉得上下文快满时会调用 compact 工具。」

目前能保证的是：自动的请求级压缩，加上一个可由外层代码调用的主动压缩 API。

---

## prompt-too-long：恢复原语已经有了，Loop 信号还没有

`compactOnPromptTooLong(history, { retryCount: 0 })` 是一个独立的响应式恢复原语。它会：

- 保存传入历史的完整 transcript；
- 生成结构化摘要；
- 在摘要后**保留最近 5 个完整消息组**（这是它和自动摘要的关键差别）；
- 当 `retryCount > 0` 时抛出 `PromptTooLongRetryError`，阻止同一恢复窗口再次压缩。

测试同时钉住了「保留尾部」和「只许一次」：

```typescript
const outcome = await manager.compactOnPromptTooLong(history, { retryCount: 0 });
expect(outcome.history.slice(1)).toEqual([                  // 第 0 条是摘要
  systemMessage("middle"), ...exchange("recent", "data"),   // 后面是保留的最近 2 组
]);

await expect(manager.compactOnPromptTooLong(history, { retryCount: 1 }))
  .rejects.toThrow(PromptTooLongRetryError);                // 同一窗口第二次被拒
```

为什么只许压一次？因为如果压缩之后请求**还是**太长，再压一次通常也没用——真正的原因可能是单条消息本身就超过窗口，或者摘要没有实际缩短历史。这时无限重试只会烧钱。让它明确失败，把决策交回上层。

### 但 P08 的 Loop 还没接这个信号

**当前 P08 的 `AgentRunner` 尚未接入供应商无关的「输入 prompt 过长」错误信号。** 也就是说，P08 Loop 不会自动调用这个方法。

它需要未来的模型错误分类边界先确认「这次请求是因为输入过长被拒绝」，再把明确的重试计数交给恢复原语。

### 千万不要把 `finishReason === "length"` 当成 prompt-too-long

这是两个完全不同的信号：

| 信号 | 含义 | 当前 P08 行为 |
| --- | --- | --- |
| `finishReason === "length"` | 请求已被模型**接受**，但**输出**达到生成上限，响应不完整 | `AgentRunner` 抛出 `IncompleteModelReplyError`，不做响应式压缩 |
| 输入 prompt-too-long | 请求的**输入上下文**被供应商拒绝，模型根本没开始生成 | P08 尚无统一 typed signal，也没有自动恢复接线 |
| `compactOnPromptTooLong(..., { retryCount: 0 })` | 调用者完成错误分类后可用的恢复原语 | 单独可测；只允许当前恢复窗口的一次尝试 |

把这两者混起来的后果很实际：输出被截断时去压缩输入，不但没解决问题，还白白丢了上下文。

也不能靠异常字符串里是否包含 `prompt_too_long` 或 `too many tokens` 来冒充通用信号。不同 OpenAI 兼容端点的异常类型、状态码和响应体并不一致，字符串匹配今天能用明天就可能失效。**错误分类应该在模型适配器或恢复层完成**，那是第 11 章的话题。

这一边界解释了为什么第 8 章的测试能直接证明「第二次响应式压缩会被拒绝」，却不能据此宣称 P08 的真实 Agent Loop 已经会自动处理输入过长。

---

## 用离线测试证明行为

`npm run test:ch08` 会把第 1 到 8 章积累的测试全跑一遍。当前结果：

```text
 Test Files  21 passed (21)
      Tests  174 passed (174)
```

其中和本章直接相关的是两个文件，共 24 个用例：

| 文件 | 用例数 | 管什么 |
| --- | ---: | --- |
| `chapters/ch08/tests/compaction.test.ts` | 23 | 摘要契约、落盘预算、消息组裁剪、transcript 与缓存 |
| `chapters/ch08/tests/ch08-compaction.test.ts` | 1 | 装完整 Agent 跑一遍，看大结果落盘后消息里剩什么 |

23 个单元用例按四个 `describe` 分组（其中 4 个 `rejects ...` 来自同一个 `test.each`），名字本身就是一份契约清单：

```text
model-backed history summarizer
  sends a tool-free request and accepts only the five-field JSON contract
  rejects tool calls
  rejects a non-stop finish reason
  rejects an extra field
  rejects an empty array item

tool-result artifact budgeting
  persists values above the strict UTF-8 threshold and keeps bounded previews
  keeps a value exactly at the threshold inline
  persists the largest remaining value first and preserves error metadata
  never overwrites an existing artifact and removes earlier files from the failed batch
  rejects unsafe IDs and artifact directory setup failures without changing input
  rejects a state-directory junction that escapes the workspace

atomic message-group compaction
  micro compaction replaces every old result but keeps recent exchanges intact
  snip compaction removes whole groups and inserts an exact omission marker
  counts canonical OpenAI JSONL bytes including non-ASCII UTF-8 and the final newline

final history compaction
  persists the full transcript before replacing history with one structured summary
  keeps the transcript when summarization fails
  reactive compaction runs once per retry window and retains recent complete groups
  prepare caches semantic equality, reuses a compressed prefix, and leaves canonical input intact
  prepare summarizes once, caches equal history, and appends to the compressed prefix
  recompaction summarizes the cached prefix but transcribes the full canonical history
  a transcript collision never overwrites or invokes the summarizer again
  a transcript directory failure leaves history unchanged and skips summarization
  validates pairing before any transcript write or summary call
```

对着验收契约逐条核对：

| 验收契约 | 由哪个用例钉住 |
| --- | --- |
| 1 按真实 UTF-8 byte，等于阈值不落盘 | `keeps a value exactly at the threshold inline` |
| 2 按完整消息组裁剪 | `snip compaction removes whole groups...` + `micro compaction replaces every old result...` |
| 3 不改写 canonical history | `prepare caches semantic equality, reuses a compressed prefix, and leaves canonical input intact` |
| 4 先写 transcript 再摘要 | `keeps the transcript when summarization fails` + `a transcript directory failure leaves history unchanged and skips summarization` |
| 5 独占发布不覆盖 | `a transcript collision never overwrites or invokes the summarizer again` |
| 6 批内失败清理已创建产物 | `never overwrites an existing artifact and removes earlier files from the failed batch` |
| 7 相同与纯追加复用缓存 | `prepare summarizes once, caches equal history, and appends to the compressed prefix` |

`profiles.test.ts` 顺带钉住 `ch08` 固定映射到 `P08`。

### 离线测试能证明什么，不能证明什么

| 指标类型 | 离线测试覆盖 | 验证边界 |
| --- | --- | --- |
| 机制指标 | 阈值判定、组边界、配对不变量、路径安全、缓存命中、错误传播 | 确定性、可离线重复 |
| 目标指标 | 压缩后模型是否仍能正确完成任务、摘要质量是否够用 | 需要真实模型轨迹与人工验收，离线测试不承诺 |

所以「测试全部通过」只能证明压缩机制在各种边界下行为正确。它**不能**证明摘要之后模型一定还知道该干什么——那取决于摘要质量，需要用真实任务轨迹单独评估。

---

## 运行第 8 章

### 第 0 步：准备环境

```powershell
Set-Location 'F:\笔记\Agent实操\code'
node --version      # 需要 Node 20 以上
npm install         # 只在第一次或依赖变动后执行
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch08
```

期望看到 `Test Files 21 passed (21)` 和 `Tests 174 passed (174)`。

输出中间会出现一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这不是报错。** 它是 `config.test.ts` 主动断言的一条预期输出——测试在验证「缺少配置时必须明确报错而不是静默降级」。只要最后一行是 `passed`，就一切正常。

### 第 2 步：造一个够大的文件

默认阈值是 30,000 bytes，所以需要一个超过它的文件。中文按 3 bytes 算，10,001 个字就够了：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
('甲' * 10001) | Set-Content -LiteralPath '.\large-demo.txt' -Encoding utf8NoBOM

# 确认字节数
(Get-Item '.\large-demo.txt').Length
```

期望看到 `30003` 左右（如果用 `-Encoding utf8` 带了 BOM，会多 3 bytes，不影响结论）。

PowerShell 5.1 没有 `utf8NoBOM`，用 `-Encoding utf8` 即可。

### 第 3 步：让 Agent 读它，观察落盘

这一步需要真实模型，先确认 `code\.env` 里配好了 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch08 -- --prompt "读取 large-demo.txt 并告诉我它的内容特征"
```

Hook 日志会打印这样的顺序：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: read_file
[Permission] read_file: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: read_file -> ok
[Hook] Stop
```

注意 `PostToolUse` 在前，**落盘发生在它之后**，所以日志里看不到落盘这一步——它不是一次工具调用，不触发 Hook。

### 第 4 步：去磁盘上找那个 artifact

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Get-ChildItem '.\.agent_tutorial\artifacts\' | Format-Table Name, Length
```

期望看到类似：

```text
Name                                              Length
----                                              ------
tool-result-3f9a1c72e8b04d5f9c1e7a2b6d8f0e41.txt   30003
```

`Length` 应该和原文件字节数一致。验证一下内容真的完整：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
$artifact = Get-ChildItem '.\.agent_tutorial\artifacts\tool-result-*.txt' | Select-Object -First 1
(Get-Content -LiteralPath $artifact.FullName -Raw).Length
```

期望是 `10001`（字符数）。

### 第 5 步：统一入口

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 8 --prompt "读取一个大文件并总结，观察完整结果落盘后的路径与预览"
```

两个入口等价，`ch08` 是章节专用脚本，`agent-tutorial` 是统一 CLI。

### 第 6 步（可选）：完整门禁

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run lint
```

### 清理

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -LiteralPath '.\large-demo.txt' -Force
Remove-Item -Recurse -Force '.\.agent_tutorial\artifacts'
```

产物目录目前**没有自动清理**。跑得多了会一直堆积，需要手动删——这是本章明确的边界之一。

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...`（在 `npm run test:ch08` 输出中间） | 这是 `config.test.ts` 断言的预期输出 | 不用管，看最后一行是否 `passed` |
| `配置错误: Missing required settings: ...`（`npm run ch08` 后直接退出） | `code\.env` 没配 | 复制 `.env.example` 为 `.env` 并填三个值 |
| artifacts 目录里什么都没有 | 文件不够大，没触发 30,000 bytes 阈值 | 用第 2 步的命令重新造文件，确认 `Length` 超过 30000 |
| 消息里没有 `<persisted-tool-result>` | 同上，或模型没有真的去读那个文件 | 看 Hook 日志里有没有 `PreToolUse: read_file` |
| `ArtifactPathError: artifact directory could not be created` | `.agent_tutorial` 被创建成了文件，或权限不足 | 删掉那个同名文件；检查目录权限 |
| `ArtifactPathError: artifact directory escapes workspace` | `.agent_tutorial` 或 `artifacts` 被换成了符号链接 / junction | 删掉链接，让运行时自己建真实目录 |
| `ArtifactConflictError: artifact already exists` | ID 重复（自定义 `idGenerator` 时才会遇到） | 正常保护行为，检查你的 ID 生成逻辑 |
| tool result 里出现 `tool_result_processing_error` | 落盘整批失败（磁盘满、权限、路径被换） | 看上面几条排查磁盘和目录状态 |
| `CompactionError: summary model must not call tools` | 摘要模型不听话，返回了工具调用 | 换模型，或检查是否误传了工具定义 |
| `CompactionError: summary model output must contain the exact fields` | 摘要模型多给或少给了字段 | 正常拦截；本轮压缩失败，canonical 未受影响 |
| `IncompleteModelReplyError: Model output reached the token limit` | **输出**被截断，不是输入过长 | 提高 `max_tokens`，别去压缩输入 |

---

## 五个动手小实验

这五个实验都不调真实模型，跑完就能看到本章所有核心数字。先建一个临时脚本目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\scratch' | Out-Null
```

五个脚本都需要同一段样板（一个不会被调用的 summarizer），先看第一个的完整写法，后面几个只列关键部分。

**实验一：看清落盘后消息里到底剩什么**

存成 `code\scratch\probe1-persist.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { CompactionManager } from "../chapters/ch08/src/features/compaction.js";
import type { CompactionSummary, HistorySummarizer } from "../chapters/ch08/src/features/compaction.js";
import type { ChatMessage } from "../chapters/ch08/src/core/messages.js";
import { toolSuccess } from "../chapters/ch08/src/core/tools.js";

class NeverSummarizer implements HistorySummarizer {
  async summarize(_history: readonly ChatMessage[]): Promise<CompactionSummary> {
    throw new Error("summarizer should not be called in this probe");
  }
}

const workspace = mkdtempSync(join(tmpdir(), "probe1-"));
const manager = new CompactionManager({
  workspace,
  summarizer: new NeverSummarizer(),
  persistThresholdBytes: 30,     // 把阈值从 30000 调到 30，方便观察
  batchBudgetBytes: 1_000,
  previewHeadBytes: 6,
  previewTailBytes: 6,
});

const big = "甲乙丙丁戊己庚辛壬癸子丑寅卯";   // 14 个中文 = 42 bytes
console.log("原始 bytes =", Buffer.byteLength(big, "utf8"));

const outcome = await manager.compactToolResults([toolSuccess(big)]);
console.log("--- 消息里剩下什么 ---");
console.log(outcome.results[0]?.content);
console.log("--- artifact ---");
console.log("relativePath =", outcome.artifacts[0]?.reference.relativePath);
console.log("originalBytes =", outcome.artifacts[0]?.reference.originalBytes);

rmSync(workspace, { recursive: true, force: true });
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe1-persist.ts
```

实际输出：

```text
原始 bytes = 42
--- 消息里剩下什么 ---
<persisted-tool-result>
path: .agent_tutorial/artifacts/tool-result-6ffe1a8402154a04a550467a792efb41.txt
original_bytes: 42
head_preview:
甲乙
tail_preview:
寅卯
</persisted-tool-result>
--- artifact ---
relativePath = .agent_tutorial/artifacts/tool-result-6ffe1a8402154a04a550467a792efb41.txt
originalBytes = 42
```

`previewHeadBytes: 6` 换来 2 个中文字符，正好 6 bytes。文件名里那串十六进制是随机 UUID，**你跑出来的会和这里不一样**，这是正常的。

**注意 `path` 是相对路径**——绝对路径只留在内部的 `reference.path` 里，不进模型上下文，避免泄露本机目录结构。

**实验二：预览的 byte 边界会丢字**

把 `previewHeadBytes` 从 3 到 7 各试一次，`previewTailBytes` 设成 0：

```typescript
for (const limit of [3, 4, 5, 6, 7]) {
  const manager = new CompactionManager({
    workspace, summarizer: new NeverSummarizer(),
    persistThresholdBytes: 10, batchBudgetBytes: 1_000,
    previewHeadBytes: limit, previewTailBytes: 0,
  });
  const outcome = await manager.compactToolResults([toolSuccess("甲乙丙丁戊己庚辛壬癸")]);
  const head = /^head_preview:\n([\s\S]*)\ntail_preview:/m.exec(outcome.results[0]?.content ?? "")?.[1];
  console.log(`previewHeadBytes=${limit} -> ${JSON.stringify(head)}  实际 ${Buffer.byteLength(head ?? "", "utf8")} bytes`);
}
```

实际输出：

```text
previewHeadBytes=3 -> "甲"    实际 3 bytes
previewHeadBytes=4 -> "甲"    实际 3 bytes
previewHeadBytes=5 -> "甲"    实际 3 bytes
previewHeadBytes=6 -> "甲乙"  实际 6 bytes
previewHeadBytes=7 -> "甲乙"  实际 6 bytes
```

4、5、7 这三个值都「浪费」了字节——因为装不下完整的下一个字符。**给中文内容算预览容量时，要按 3 bytes/字 往下取整。**

**实验三：批次预算怎么挑落盘对象**

```typescript
const results = [
  toolSuccess("A".repeat(10)), toolSuccess("B".repeat(50)),
  toolSuccess("C".repeat(30)), toolSuccess("D".repeat(10)),
];
console.log("四条结果 bytes =", results.map((r) => Buffer.byteLength(r.content, "utf8")));
console.log("合计 =", results.reduce((sum, r) => sum + Buffer.byteLength(r.content, "utf8"), 0));

const manager = new CompactionManager({
  workspace, summarizer: new NeverSummarizer(),
  persistThresholdBytes: 100,   // 单项都不超
  batchBudgetBytes: 60,         // 但合计 100 超了
});
const outcome = await manager.compactToolResults(results);
console.log("落盘的下标 =", outcome.artifacts.map((a) => a.resultIndex));

outcome.results.forEach((r, i) => {
  const persisted = r.content.startsWith("<persisted-tool-result>");
  console.log(`  [${i}] ${persisted ? "落盘" : "留在消息里"} ${persisted ? "" : JSON.stringify(r.content)}`);
});
```

实际输出：

```text
四条结果 bytes = [ 10, 50, 30, 10 ]
合计 = 100
落盘的下标 = [ 1 ]
  [0] 留在消息里 "AAAAAAAAAA"
  [1] 落盘
  [2] 留在消息里 "CCCCCCCCCCCCCCCCCCCCCCCCCCCCCC"
  [3] 留在消息里 "DDDDDDDDDD"
```

只落了最大的那一条。**够用就停，不做多余的磁盘写。**

**实验四：一条 assistant 两个调用，snip 和 micro 分别怎么切**

```typescript
const history: ChatMessage[] = [
  userMessage("开始"),
  assistantMessage(null, [
    toolCall("a", "read_file", '{"path":"a.txt"}'),
    toolCall("b", "read_file", '{"path":"b.txt"}'),
  ]),
  toolMessage("A 的内容", "a"),
  toolMessage("B 的内容", "b"),
  assistantMessage(null, [toolCall("c", "read_file", '{"path":"c.txt"}')]),
  toolMessage("C 的内容", "c"),
  assistantMessage("完成"),
];
console.log("原始条数 =", history.length, " bytes =", historyUtf8Bytes(history));

const micro = microCompactHistory(history, { keepRecentToolGroups: 1 });
const snip = snipCompactHistory(history, { maxGroups: 3, keepHeadGroups: 1 });
```

实际输出：

```text
原始消息条数 = 7
原始 bytes  = 642

--- micro（keepRecentToolGroups=1）---
  [0] user             "开始"
  [1] assistant calls=2 null
  [2] tool(a)          "[Earlier tool result compacted. Re-run if needed.]"
  [3] tool(b)          "[Earlier tool result compacted. Re-run if needed.]"
  [4] assistant calls=1 null
  [5] tool(c)          "C 的内容"
  [6] assistant        "完成"
micro 后条数 = 7  bytes = 720

--- snip（maxGroups=3, keepHeadGroups=1）---
  [0] user      "开始"
  [1] system    "[Compacted: 2 message groups omitted]"
  [2] assistant "完成"
snip 后条数 = 3  bytes = 143
```

三个结论一次看清：

1. micro 保持消息条数不变，只换 content；两个调用的结果**一起**被替换。
2. snip 整组丢弃，7 条变 3 条，**没有孤儿 `tool` 消息**。
3. micro 这次让字节数**变多了**（642 → 720）——占位符比原始短结果还长。这印证了前面说的：micro 是为几 KB 以上的结果设计的。

**实验五：缓存到底省了几次模型调用**

```typescript
class CountingSummarizer implements HistorySummarizer {
  calls = 0;
  async summarize(_history: readonly ChatMessage[]): Promise<CompactionSummary> {
    this.calls += 1;
    return new CompactionSummary({ currentGoal: "...", keyFindings: ["..."], ... });
  }
}

const manager = new CompactionManager({ workspace, summarizer, proactiveThresholdBytes: 800 });
const canonical = [userMessage("x".repeat(1_000))];

const first = await manager.prepare(canonical);
const again = await manager.prepare([userMessage("x".repeat(1_000))]);      // 内容相同
const appended = await manager.prepare([...canonical, assistantMessage("最新回答")]);
```

实际输出：

```text
canonical bytes = 1029 > 阈值 800
第 1 次 prepare：摘要调用次数 = 1  请求历史条数 = 1
第 2 次 prepare（内容相同）：摘要调用次数 = 1  是同一个对象吗 = true
第 3 次 prepare（纯追加）：摘要调用次数 = 1  条数 = 2
canonical 有没有被改写 = 没有
```

三次 `prepare()`，一次摘要。第 2 次连对象引用都相同，第 3 次复用了压缩前缀。

做完记得删掉临时目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -Recurse -Force '.\scratch'
```

---

## 这章没有做什么

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| UTF-8 字节预算 | 精确 token 计数 | **不补**。需要引入 tokenizer 依赖，且各模型不通用 |
| 四层管线 | Claude Code 的 `contextCollapse` 层 | **不补**。见「与 Claude Code 的差异」 |
| `compactProactively()` API | `/compact` 命令或 `compact` 工具 | **不补**。暴露成工具会让上下文成本变得不可预测 |
| `compactOnPromptTooLong()` 原语 | Loop 自动识别 prompt-too-long | 第 11 章（API 韧性与错误分类） |
| 摘要保留 5 类信息 | 压缩后自动重读最近修改的文件 | **不补**。需要在 `CompactionManager` 外层叠加状态恢复 |
| artifact 落盘 | 产物目录自动清理、脱敏、加密 | **不补**。这些不该由压缩模块隐式解决 |
| 会话内压缩 | 跨会话持久记忆 | 第 9 章（文件级记忆） |
| 实例内缓存 | 跨进程持久化的压缩缓存 | **不补**。canonical history 是唯一事实来源 |
| 统一占位符替换 | 按工具类型区分保留策略 | **不补**。见「与 Claude Code 的差异」最后一段 |

需要专门强调最后一行的对偶：本章的 `microCompactHistory()` 对**所有**工具结果一视同仁地替换成占位符。Claude Code 会按工具类型区分——`read_file` 的结果可以在压缩后重新读回来，所以敢直接替换；其他工具的结果可能不可重现，需要更保守的策略。

这个差异对生产系统有实际影响：如果一个工具的结果是不可重现的（比如一次带时间戳的 API 查询），把它替换成「Re-run if needed」就是在撒谎——重跑得到的不是同一个结果。本章为了保持规则简单没做区分，但你在真实系统里加这一层时需要想清楚。

---

## 与 Claude Code 的差异

本章的上下文压缩是教学子集。Claude Code 的压缩体系同样遵循「先便宜，后昂贵」的原则，但在实现上有以下显著差异。官方文档见：https://code.claude.com/docs/en/context-window ，https://code.claude.com/docs/en/commands ，https://code.claude.com/docs/en/agent-sdk/hooks 。参考仓库：`learn-claude-code/s08_context_compact/README.md`。

- **精确度不同。** Claude Code 使用精确 token 阈值（`contextWindow - maxOutputTokens - 13,000`）。P08 使用 UTF-8 字节估算，省略了精确 tokenizer 的引入成本。
- **压缩层数不同。** Claude Code 的五级管线为 `budget → snip → micro → contextCollapse → auto`（`query.ts:379-468`）。P08 没有 `contextCollapse` 层，执行顺序为 `budget → snip → micro → auto`。
- **contextCollapse 与 sessionMemoryCompact 不同。** Claude Code 源码包含独立的 `contextCollapse` 上下文管理系统（启用时抑制 proactive autocompact）和 `sessionMemoryCompact`（compact 前先用 session memory 尝试轻量摘要）。P08 两者均不涉及。
- **摘要格式不同。** Claude Code 的摘要 prompt 包含 9 个信息部分，模型输出使用 `<analysis>` / `<summary>` 双标签，首尾双重防呆禁止调用工具。P08 使用严格 5 字段 JSON，没有防呆标签。
- **主动压缩与钩子不同。** Claude Code 内置 `/compact` 命令和 `SnipTool` 工具，并且有完整的 `PreCompact` / `PostCompact` 钩子生命周期。P08 未将 `compactProactively()` 暴露为工具，`CompactionManager` 不发射钩子事件。
- **后压缩恢复不同。** Claude Code 在 compact 后自动重新读取最近修改的文件（最多 5 个文件、每个 5K token、总预算 50K token），并维护 `readFileState` 避免重复读取未修改文件。P08 只保留摘要，不自动恢复文件内容。
- **响应式重试不同。** Claude Code 有更精细的分级重试与模型错误分类。P08 的 `compactOnPromptTooLong()` 是一个可测试的被动原语，但 P08 Loop 尚未接入供应商无关的 prompt-too-long 信号。

---

## 从 ai-agent-book 学到什么：压缩不是省 token，而是整理知识

`ai-agent-book` 第 2 章「上下文压缩策略」提出压缩有两个动机：第一是长度与成本约束，第二是**思考质量**。后者更容易被忽略，也就是本文开头讲的 Context Rot。

它的实验 2-9 用同一个研究任务对比六种策略，结果很值得对照：

| 策略 | 实测结果 | 暴露的问题 |
| --- | --- | --- |
| 无压缩 | 7 次工具调用约 367,000 字符；第 5 次迭代约 165K token 超过 128K 预算 | 只靠大窗口不可行 |
| 个体摘要 | 压缩率 10.9%，12 次迭代，276,608 token | 页面重复信息碎片化 |
| 组合摘要 | 压缩率 4.3%，10 次迭代，93,449 token | 超长输入截断可能丢尾部 |
| 上下文感知摘要 | 压缩率约 3.0%，7 次迭代，40,157 token；一次把 147,877 字符压到 1,963 字符 | 需要把当前目标和已有结论放进摘要请求 |
| 带引用摘要 | 222,992 token | 可溯源，但引用元数据有明显成本 |
| 自适应窗口化 | 174,601 token；接近 80% 阈值时批量压缩全部未标记结果 | 前几轮保留原始信息，后期集中降级 |

对照后，P08 的设计取舍更清晰：

- **先便宜后昂贵是生产原则。** `ai-agent-book` 把生产级压缩分成五层：工具结果预算控制、噪声删除、API 层微压缩、归档式摘要、全量压缩与熔断。P08 的四层是教学子集：落盘对应第一层，snip 近似删除中段，micro 用本地占位符近似 API 层微压缩，模型摘要作为最后手段。P08 没有实现服务端上下文编辑，也没有连续失败熔断器。
- **结构化摘要优于把原文留给模型自行归纳。** 实验里上下文感知摘要的 token 与迭代次数都明显低于个体摘要。P08 的 5 字段 JSON 把目标、发现、文件、剩余工作和约束变成可校验状态；transcript 和 artifact 又保留原始现场，对应书中「有损压缩 + 无损索引」的取舍。
- **当前 P08 还不是上下文感知压缩。** 书中最优策略会把当前查询意图和已有结论写进摘要 prompt。P08 的 `SUMMARY_SYSTEM_PROMPT` 是固定 JSON schema，不接收当前 goal 参数。因此**不能**拿 40,157 token 这类目标指标宣称 P08 已达到相同效果。
- **标记让压缩结果可读，但不等于持久防重复机制。** 书中自适应窗口化用 `[COMPRESSED]` 标记防止同一批结果重复压缩。P08 的 `[Compacted: ...]` 和 `[Earlier tool result compacted...]` 让 request history 可检查，但缓存判断由 `historiesEqual()` / `historyStartsWith()` 完成，**不是扫描消息正文**。
- **保留优先级和 P08 五字段一致。** 书建议保留架构决策、关键约束、已修改文件、验证状态和未解决 TODO。P08 的摘要字段虽然没有专门的「验证状态」，但 `key_findings` / `remaining_work` 可承载 pass/fail 与回滚信息，canonical transcript 保证压缩前现场不丢。

---

## 本章代码边界

| 实现 | 职责与边界 |
| --- | --- |
| `DEFAULT_*` 常量 | 集中保存 `30_000`、`200_000`、`2_000`、`3`、`5`、`50_000`、`50` 等默认预算；构造时可注入测试小值 |
| `CompactionManager.prepare()` | 模型请求前从 canonical snapshot 生成 request history：snip → micro → 超过 `50_000` bytes 才摘要；只影响本次及后续可复用的请求快照 |
| `compactToolResults()` | 整轮工具结果回填 canonical history 前，按单项阈值和批次预算落盘，保留 error metadata，返回结果与 artifact 引用 |
| `validatedGroups()` / `validateToolPairing()` | 先验证 assistant 工具调用与 tool results 的配对，再切成普通组和工具交换组；所有裁剪边界都基于组 |
| `snipCompactHistory()` | 超过 `50` 个消息组时保留前 `3` 组、省略标记和后 `46` 组，整组省略 |
| `microCompactHistory()` | 保留最近 `3` 个工具交换组，更早工具结果替换为占位符，不删除 assistant 调用和 `tool_call_id` |
| `historyUtf8Bytes()` / `serializeTranscript()` | 把历史转成稳定 JSONL 后计算 UTF-8 bytes，作为摘要阈值和 transcript 的同一份规范化输出 |
| `ModelHistorySummarizer` / `parseCompactionSummary()` | 发起无工具摘要请求，要求 `stop` 且输出精确 5 字段的非空 JSON |
| `historiesEqual()` / `historyStartsWith()` | 用稳定 JSON 识别「完全相同」和「纯追加」，复用已压缩前缀，避免重复送模型 |
| `boundedUtf8Preview()` / `decodeUtf8Boundary()` | 按 byte 截取头尾，用 `fatal: true` 的解码器丢弃边界上的不完整字符 |
| `ensureSafeDirectory()` / `writeExclusiveAtomic()` / `removeCreatedArtifacts()` | 复查真实目录、临时文件加硬链接独占发布、批内失败清理已创建产物 |
| `compactProactively()` / `compactOnPromptTooLong()` | 外层显式压缩 API 与响应式恢复原语；后者摘要后保留最近 `5` 个完整组，并用 `PromptTooLongRetryError` 限制一次重试 |
| `AgentRunner.#processToolResults()` | 结果处理器失败时把整批结果替换为 `tool_result_processing_error`，仍与原工具调用数一一对应 |

---

## 本章小结

### 三句话版本

1. 四层压缩按成本从低到高执行：落盘 → snip → micro → 模型摘要，前三层不花模型调用。
2. 压缩只作用于 request history，canonical history 永远是不被改写的会话事实。
3. 所有裁剪以完整消息组为单位，一轮多个工具调用也不会切出孤儿消息。

### 一定要记住的八条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 阈值算的是 UTF-8 **byte**，不是字符数；中文一字 3 bytes | 第一层：大工具结果按 UTF-8 bytes 落盘 |
| 2 | 恰好等于阈值**不**落盘，必须严格大于 | 同上（有专门测试） |
| 3 | 一条 assistant + 它的全部 tool result = 1 个消息组，裁剪只能整组 | 消息原子组 |
| 4 | 落盘发生在写入 canonical history **之前**，所以事实来源里从来没有那段超大正文 | 四个对象千万别搞混 |
| 5 | 先写 transcript 再调摘要；transcript 写失败就不调摘要模型 | 第四层：五个步骤 |
| 6 | 摘要字段集合必须**精确**相等，多一个字段就拒 | 第四层：不可信边界 |
| 7 | micro 对短结果可能让历史**变长**——它是为几 KB 以上结果设计的 | 第三层：一个诚实的观察 |
| 8 | P08 没有 `compact` 工具，也没有自动 prompt-too-long 恢复 | 主动压缩 API / prompt-too-long |

### 检查你是否真的读懂了

1. 一个 30,000 bytes 的工具结果会落盘吗？30,001 呢？（第一层）
2. 一轮里 5 条结果，每条 50,000 bytes，会落盘几条？（第一层：批次预算）
3. `previewHeadBytes: 10` 对纯中文内容，实际能显示几个字？（实验二）
4. 一条 assistant 发起 3 个调用，这在 snip 的预算里算几组？占几条消息？（消息原子组）
5. `snip` 之后 canonical history 变短了吗？`RunResult.history` 里能看到被裁掉的消息吗？（四个对象）
6. 摘要模型返回了正确的 5 个字段，外加一个 `confidence: 0.9`，会怎样？（第四层）
7. transcript 里能找到那个 30,003 bytes 的完整正文吗？在哪？（transcript 保存的到底是什么）
8. 连续三次用完全相同的历史调 `prepare()`，摘要模型被调用几次？（缓存）
9. 模型返回 `finishReason: "length"`，P08 会触发响应式压缩吗？为什么？（prompt-too-long）
10. 落盘时磁盘满了，这一轮的 5 条工具结果会变成什么？还满足配对契约吗？（第一层：落盘失败）

---

## 压缩解决当下，记忆解决跨会话

这一章当前能直接保证的是：工具结果按真实 UTF-8 bytes 有界落盘；所有裁剪都保持 OpenAI 工具消息原子组；请求级压缩不改写 canonical history；模型摘要前保存可追溯 transcript；相同历史和纯追加历史可以复用缓存。

它没有承诺精确 token 计数，没有把 `compactProactively()` 暴露成工具，也没有在 P08 Loop 中自动识别 prompt-too-long。产物目录目前还需要手动清理，也没有脱敏或加密能力。

上下文压缩解决的是当前会话「装不下」和「看不清」的问题。但会话一结束，摘要和 transcript 就只是磁盘上的文件，下一次运行的 Agent 并不知道它们存在。

下一章在这个边界之上讨论另一件事：哪些信息值得**跨会话**保留，以及文件级记忆如何在写入失败时仍然不丢数据。按需加载负责少拿，Context Compact 负责及时放下，文件级记忆负责下次还能记起来。
