
第 8 章解决了上下文压缩的问题：会话快满时自动生成摘要，用摘要替换历史，继续跑。但有个硬伤一直没处理。用户说的"用 tab 不用空格"，压缩之后最多变成"用户有代码风格偏好"，关键细节没了。换一个会话，连这句话也不会再出现。

LLM 没有持久状态。所有信息都在上下文窗口里，窗口满了要压缩，压缩有损，新会话从零开始。这不是压缩算法的问题，是架构问题：需要一层不参与压缩、跨会话保留的存储。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaFDCGpqNNngBPZbesAfEDgGFIMAX7qDPRAtB2PWF1CDuovepqUfUibPheVJOP4eAmj892p0NkRZHpsQhEz5u4SFEPnJEfsggpiaI/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「压缩」和「记忆」解决的是两个不同问题，以及为什么有了第 8 章的压缩仍然需要第 9 章。
2. 复述一条记忆从「用户随口一说」到「下个会话被用上」的完整路径，一共经过哪几个环节。
3. 解释为什么记忆集合的权威来源是 `manifest.json`，而不是「扫描 `.memory` 目录里所有 `.md`」。
4. 说明为什么提取记忆必须读 canonical history，读 request history 会漏掉什么。
5. 算清单条记忆的两个预算（200 行 / 4096 bytes）分别在量什么，以及正文实际还剩多少空间。
6. 看懂一次「整理」的提交顺序，并解释为什么顺序错了会导致「整理失败后记忆全空」。
7. 判断本章**没有**实现哪些能力（向量检索、记忆质量评测、Reviewer 审核），避免照着教程宣称不存在的行为。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的消息历史结构 | 第 1 章 | 记忆在回合边界读写，不进入工具调用循环 |
| workspace 硬边界与路径校验 | 第 2 章 | `.memory` 必须留在 workspace 内，链接不能绕过 |
| 工具调用与结果的配对不变量 | 第 1、2 章 | 提取器拿到的历史快照必须仍然满足配对 |
| canonical history 与 request history 的区别 | 第 8 章 | **本章最关键的前置**，提取只能读 canonical |
| 大工具结果落盘成 artifact | 第 8 章 | 中段事实可能只在 artifact 里，提取器看不到 |
| Profile 能力位与组合根 | 第 5～8 章 | P09 靠一个 `memory` 能力位决定是否接线 |

### 术语速查（本章第一次出现的词）

| 词                                   | 一句话解释                                     |
| ----------------------------------- | ----------------------------------------- |
| 文件级记忆                               | 把跨会话仍有价值的信息写成磁盘上的 `.md` 文件，不参与压缩          |
| `MemoryRecord`                      | 一条记忆的不可变值对象，构造时就完成全部校验                    |
| `MemoryStore`                       | 记忆集合的唯一事务入口，所有读改写都加锁                      |
| `MemorySession`                     | 把 `MemoryStore` 包装成 Loop 能调用的回合生命周期       |
| manifest                            | `manifest.json`，当前记忆集合的唯一权威指针             |
| 派生视图                                | 可以从 manifest 重新生成的东西（`MEMORY.md`），坏了不影响事实 |
| 有界目录                                | 只含名称和一行描述的轻量索引，供选择器读，不含正文                 |
| side-query                          | 一次独立的、不带任何工具的模型请求，只为拿一段 JSON              |
| selector / extractor / consolidator | 三个 side-query 角色：选择 / 提取 / 整理             |
| 确定性回退                               | 选择器失败时改用固定规则的关键词匹配，不靠模型                   |
| 整理（consolidation）                   | 记忆变多后合并重复与冲突，必须声明替换了哪些旧记录                 |
| 原子替换                                | 先写临时文件并 fsync，再 rename 覆盖，读方绝不会看到半写文件     |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼记忆真的跨会话生效     ← 两个独立 Agent 实例，一个写一个读
  ↓
② 为什么选文件系统               ← 和向量库、SQL 的取舍
  ↓
③ 一份集合，两份派生视图         ← 本章最关键的架构决定：manifest 是权威
  ↓
④ 加载：先选名称，再读正文       ← 省上下文的关键，以及失败怎么降级
  ↓
⑤ 写入：从 canonical history 提取 ← 为什么不能读 request history
  ↓
⑥ 整理：先声明来源，再一次提交   ← 提交顺序决定失败后是否丢数据
  ↓
⑦ 并发与失败边界                 ← 两层锁 + 原子替换
  ↓
⑧ 运行 + 实验 + 排错             ← 动手验证本章所有数字
```

---

## 先看一眼记忆真的跨会话生效

抽象讲「持久化」不如直接看一次运行。下面这段来自 `code/chapters/ch09/tests/ch09-memory.test.ts`，是断言过的真实数据。

它建了**两个完全独立的 Agent 实例**，共用同一个 workspace 目录。注意：不是同一个会话的两个回合，是两个各自持有独立历史的 `buildAgent()`。

**第一个实例：用户说了一句，Agent 把它记下来。**

```typescript
const first = buildAgent(P09, { model: firstModel, workspace, /* ... */ });
await first.run("remember the database rule");
```

这一轮实例向模型发了 2 次请求：

| # | 是什么 | 带工具吗 | 模型返回 |
| ---: | --- | --- | --- |
| 1 | 主请求（回答用户） | 带，8 个工具 | `"stored"` |
| 2 | extractor side-query | `tools: []` | 一段 JSON 数组 |

第 2 次那段 JSON 是：

```json
[
  {
    "name": "project-fact",
    "type": "project",
    "description": "Project database rule",
    "body": "Always use the integration database."
  }
]
```

回合结束，这条记忆落到磁盘上。第一个实例的生命周期就结束了，它的历史全部丢弃。

**第二个实例：换一个 Agent，问一个相关问题。**

```typescript
const second = buildAgent(P09, { model: secondModel, workspace, /* ... */ });
const result = await second.run("which database should I use?");
```

这一轮变成 3 次请求，顺序很重要：

| # | 是什么 | 带工具吗 | 干什么 |
| ---: | --- | --- | --- |
| 1 | selector side-query | `tools: []` | 从目录里挑相关记忆，返回 `["project-fact"]` |
| 2 | 主请求（回答用户） | 带，8 个工具 | 上下文里**已经**含有那条记忆的正文 |
| 3 | extractor side-query | `tools: []` | 本轮没有新东西，返回 `[]` |

关键断言是第 2 次请求：

```typescript
expect(
  secondModel.requests[1]?.messages.some((message) =>
    message.content?.includes("Always use the integration database."),
  ),
).toBe(true);
```

**第一个实例里用户说过的约束，出现在第二个实例的上下文里。**中间没有任何人手工传递，靠的就是磁盘上那几个文件。

另外三条断言钉住了边界：三次请求里，只有主请求带工具，两个 side-query 的 `tools` 都必须是空数组。

```typescript
expect(secondModel.requests[0]?.tools).toEqual([]);   // selector
expect(secondModel.requests[2]?.tools).toEqual([]);   // extractor
expect(secondModel.requests[1]?.tools.map((t) => t.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write", "task", "load_skill",
]);
```

### 这里有个成本必须先讲清楚

看上面的表就能算出来：**开了 memory 能力，一个回合最多多花 2 次模型调用**（selector + extractor），达到整理阈值后还要再多 1 次（consolidator）。

第一个实例只发了 2 次请求，因为它开始时记忆集合是空的——`beginTurn()` 读到 0 条记忆就直接返回，连 selector 都不叫。这是个有意的短路：没有记忆可选时，不浪费一次调用。

这个成本是本章设计的直接后果，不是 bug。你在真实项目里接这套机制时要自己判断值不值：偏好和约束反复出现的长期项目值得，一次性脚本不值得。

---

## 为什么选文件系统

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaHibPdk2x4Oj1EYnZLaboUkc3SVC7Rk96k5SXI5833MuNozibm7JJkq0cXzIo2jbZ6QhsxdHYmmPiaUJjtHDVVlVmxal3u8GtCiaKQ/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=2)

第一反应可能是向量数据库——把偏好和背景 embed 存起来，查询时做语义检索。但这条路有几个问题：部署依赖重，调试不直观，小规模场景完全是杀鸡用牛刀。

另一个思路是结构化数据库，用 SQL 或 JSON 存记忆。问题是查询逻辑要自己写，而且 LLM 读起来不自然，需要额外的序列化层。

三条路对比一下：

| | 文件 + Markdown | 向量数据库 | SQL / JSON |
| --- | --- | --- | --- |
| 部署依赖 | 无 | 需要服务或嵌入库 | 需要驱动或 schema 迁移 |
| 人能直接看懂吗 | 能，打开就是 | 不能，是向量 | 勉强，要格式化 |
| LLM 读写自然吗 | 自然，就是自然语言 | 需要转换层 | 需要序列化层 |
| 出问题怎么定位 | 对照 manifest 和正文 | 查嵌入和相似度 | 查表和查询语句 |
| 语义检索 | **不支持**，本章用名称匹配 | 支持 | 不支持 |

最后一行是代价，不是优点。本章的选择是：小规模场景下，可审计性比语义召回更值得。记忆到几百条以上时，这个取舍要重新算。

文件系统还有一个隐含好处：手工修改是**受支持**的操作。你可以直接打开 `.memory` 下的文件改内容。但要守住契约——保持 frontmatter 格式、不超过大小限制、不删除 manifest 正在引用的文件。改坏了会被读取端拒绝（后面「读取端不信任任何东西」一节会讲具体拒绝哪些情况）。

### 存储格式：Markdown + YAML frontmatter

```yaml
---
name: user-preference-tabs
description: User prefers tabs for indentation
type: user
---

User prefers using tabs, not spaces, for indentation.
**Why:** Consistency with existing codebase conventions.
**How to apply:** Always use tabs when writing or editing files.
```

frontmatter 存元数据，正文存完整细节。这个分层是后面「先选名称，再读正文」能省上下文的基础：

- `name` 和 `description` 进有界目录，**每轮都要给选择器看**，所以必须短。
- `body` 只在这条记忆被选中时才进上下文，所以可以写详细。

正文写法有个实践建议：像上面这样写清「是什么 / 为什么 / 怎么用」。只写"用 tab"，模型下次不知道该在什么场景应用它。

### 四种记忆类型，各有分工

| 类型 | 记什么 | 举例 |
| --- | --- | --- |
| `user` | 用户是谁、有什么偏好 | "用 tab 不用空格" |
| `feedback` | 怎么做事、踩过什么坑 | "别 mock 数据库" |
| `project` | 当前在发生什么 | "auth 重写是合规驱动" |
| `reference` | 东西在哪找 | "pipeline bug 在 Linear INGEST" |

这四个值是**封闭集合**，模型写别的类型会被直接拒绝：

```typescript
expect(() => new MemoryRecord({
  name: "valid-name", description: "description",
  kind: "invalid" as never, body: "body",
})).toThrow(/MemoryType/);
```

为什么要封闭？因为类型参与选择和整理阶段的路由。允许模型自由发明类型，等于让它每次运行都可能换一套分类法，几十条记忆之后就没法维护了。

---

## 一份集合，两份派生视图

这一节是本章最关键的架构决定，值得慢一点看。

一个很自然的想法是：记忆就是 `.memory` 目录里的所有 `.md` 文件，要读就扫目录。**本章明确不这么做。**

原因是写入和整理都可能在中途失败。想象整理写到一半崩了：目录里同时存在 3 个旧文件和 2 个新文件，扫目录会把它们全当成有效记忆，于是模型看到自相矛盾的两套内容。目录里还可能留下上一次失败遗留的孤儿文件。

所以当前实现分三个层次：

| 文件 | 职责 | 坏了会怎样 |
| --- | --- | --- |
| `.memory/manifest.json` | 当前记忆集合的**唯一权威指针** | 集合读不出来，直接报错 |
| `.memory/MEMORY.md` | 由当前集合生成的有界目录，供选择器读 | 不影响事实，可重建 |
| `.memory/<name>-<id>.md` | 单条记忆的 frontmatter 与正文 | manifest 引用它就报错，否则被忽略 |

一次成功写入后，磁盘上真实的样子是这样（这是实际跑出来的输出，不是示意）：

```text
.memory/
├── MEMORY.md
├── manifest.json
└── tabs-style-c60d6de834bb4afc98bd5f8a41ca426d.md
```

`manifest.json`：

```json
{"version":1,"files":["tabs-style-c60d6de834bb4afc98bd5f8a41ca426d.md"]}
```

`MEMORY.md`：

```text
- [tabs-style](tabs-style-c60d6de834bb4afc98bd5f8a41ca426d.md) - Tabs indentation preference
```

记录文件本身：

```text
---
name: tabs-style
description: Tabs indentation preference
type: user
---

Always indent with tabs.
```

三个细节值得注意。

**第一，文件名后面那串是 32 位十六进制**，由 `randomUUID()` 去掉连字符得到。你跑出来的一定和这里不一样，这是正常的。

为什么要加这串 ID？因为提交新集合时**不能覆盖旧文件**。整理时如果新的 `tabs-style` 直接写到旧的 `tabs-style.md` 上，写坏了就两头都没了。加了唯一 ID，新文件是新文件，旧文件在 manifest 切换成功之后才删。

**第二，`MEMORY.md` 是派生视图，不承担事务指针职责。** 它可以从 manifest 完整重建。删掉它，下次提交会重新生成；改坏它，也不影响 `records()` 读出的集合。

这里有个容易误解的点：选择器看到的目录**不是**读 `MEMORY.md` 文件得来的。`renderCatalog()` 是按 manifest 当前集合在内存里重新渲染的。`MEMORY.md` 落到磁盘上，主要是给人看和调试用。

**第三，`.memory/.lock` 不是记忆集合的一部分。** 它是 `proper-lockfile` 用来做跨进程互斥的锁文件，后面「并发」一节会讲。

对应实现集中在 `code/chapters/ch09/src/features/memory.ts` 的 `MemoryStore`，全文件没有第二套目录扫描逻辑。

### 写入入口只接受校验后的值对象

```typescript
import { MemoryRecord, MemoryStore, MemoryType } from "./features/memory.js";

const store = new MemoryStore({ workspace: process.cwd() });
await store.add(
  new MemoryRecord({
    name: "user-prefers-tabs",
    description: "User prefers tabs for indentation",
    kind: MemoryType.USER,
    body: "Use tabs in this repository.",
  }),
);
```

注意这里**没有**用 `replace("/", "-")` 之类的手法把非法输入悄悄改成另一个名字。`name` 必须本来就是安全的小写 slug，不合规就抛错。

这个选择的理由是：名称直接决定文件名。悄悄改名会产生一个用户和模型都不知道的记忆，下次按原名找不到，于是又写一条，很快就攒出一堆重复。

被拒绝的名称，每一个都有测试钉住：

```typescript
test.each(["../escape", "has space", "UPPER", "nul", "com1", "lpt9", "file:stream", "trailing."])(
  "rejects unsafe memory name %s",
  (name) => { expect(() => record(name, "description", "body")).toThrow(/slug/); },
);
```

一条条看清楚它们各自防的是什么：

| 被拒的名称 | 防什么 |
| --- | --- |
| `../escape` | 路径穿越，写到 `.memory` 外面去 |
| `has space` | 空格，让文件名在命令行里难处理 |
| `UPPER` | 大写，在大小写不敏感的文件系统上会撞车 |
| `nul` / `com1` / `lpt9` | **Windows 保留设备名**，创建即出诡异错误 |
| `file:stream` | Windows NTFS 备用数据流语法 |
| `trailing.` | Windows 会静默去掉结尾的点，导致名字对不上 |

后四行是 Windows 专属的坑。跨平台的 Agent 如果只挡了 `..` 和 `/`，在 Windows 上照样能出事。

### 两个预算：200 行 / 4096 bytes

`description` 必须是单行非空文本。序列化后的完整文件同时受两个限制，**两个都是算整个文件，不是只算正文**：

```typescript
expect(() => record("too-many-lines", "description", Array(201).fill("line").join("\n")))
  .toThrow(/200 lines/);
expect(() => record("frontmatter-counts", "description", Array(195).fill("line").join("\n")))
  .toThrow(/200 lines/);
expect(() => record("too-many-bytes", "description", "界".repeat(1_400)))
  .toThrow(/4096 UTF-8 bytes/);
```

第二行那个测试的名字（`frontmatter-counts`）就是重点：**195 行正文也会被拒**，因为 frontmatter 也占行数。

实测下来正文的真实上限是 **194 行**：

```text
正文 193 行: 通过
正文 194 行: 通过
正文 195 行: 拒绝 -> memory file must not exceed 200 lines
```

差的 6 行是这样用掉的：

```text
---                                    ← 1
name: tabs-style                       ← 2
description: Tabs indentation ...      ← 3
type: user                             ← 4
---                                    ← 5
（空行）                                ← 6
正文第 1 行                             ← 7
...
```

字节预算同理，4096 bytes 是整个文件的，frontmatter 占掉的部分要从正文里扣。纯中文一字 3 bytes，所以中文正文大约 1340 多字就到顶了。

注意这个上限**不是固定值**：`name` 和 `description` 越长，frontmatter 就越大，留给正文的字节越少。实验二会用最短的 `description: "d"` 量出精确边界（1348 字通过，1349 字被拒）；把 `description` 换成一句十几字的中文，上限会往下掉几个字。

为什么要给记忆文件设预算？因为记忆是要**注入上下文**的。一条 8000 bytes 的记忆被选中 5 条，就是 40KB 直接进请求——那等于用记忆机制把第 8 章刚压下去的上下文重新撑爆。预算是这套机制自我约束的部分。

`.memory` 解析后还必须留在 workspace 内，目录链接不能绕过边界。这一条也有测试：

```typescript
await symlink(outside, join(root, ".memory"), "junction");
await expect(new MemoryStore({ workspace: root }).records())
  .rejects.toThrow(/escapes workspace/);
```

反过来，workspace 本身是链接是允许的——它会先被 `realpath` 解析成真实路径，再在真实路径下建 `.memory`。

### 读取端不信任任何东西

读取也不是「manifest 列了哪些文件就照单全收」。`MemoryStore` 每次加载都重新做全套校验：

| 校验 | 挡住什么 |
| --- | --- |
| manifest 必须是合法 UTF-8 JSON | 文件损坏、编码错误 |
| manifest 必须精确含 `version` + `files` 两个键 | 多字段、少字段、结构漂移 |
| `version` 必须是数字 `1` | `version: true` 这类会被 JS 弱类型放过的值 |
| 文件名唯一 | 同一文件被登记两次 |
| 文件名符合安全规则 | `NUL.md`、`safe-name.md:stream` |
| `realpath` 仍在 `.memory` 内 | 用链接把文件指到工作区外 |
| 正文解析后用同一个 `MemoryRecord` 构造器校验 | 手工改坏 frontmatter、超预算 |

其中 `version: true` 那条专门有个测试，因为它是个真实的陷阱——JS 里 `true == 1` 成立，写得随意一点就会把布尔值当成版本号放过去：

```typescript
await writeFile(manifestPath, JSON.stringify({ version: true, files: [] }), "utf8");
await expect(new MemoryStore({ workspace: root }).records()).rejects.toThrow(/invalid schema/);
```

结论：手工改坏的文件、放进目录但没登记进 manifest 的文件、通过链接逃出 workspace 的文件，都不会被当作有效记忆。

---

## 加载：先选名称，再读正文

每轮开始时，`MemorySession.beginTurn(query)` 先读取当前集合。如果集合是空的，直接返回，不叫模型。

不为空时，把**查询**和**有界目录**交给选择器。给的是目录，不是全部正文——这是省上下文的关键。假设有 30 条记忆，每条正文 3KB：

| 做法 | 每轮进上下文 |
| --- | --- |
| 全部正文塞给主模型 | 约 90KB |
| 本章：目录选择 + 选中的最多 5 条正文 | 目录约 2KB + 正文最多 15KB |

模型返回的是记忆 `name` 数组，不是文件下标：

```json
["user-preference-tabs", "project-database-rule"]
```

用名称而不用下标，是因为下标会漂移。集合增删一条，下标 3 指向的东西就变了；名称是稳定标识，选错了也能立刻发现（"这个名字不存在"）。

### side-query 的三条硬要求

`ModelMemoryQueries` 通过同一个供应商无关的 `ModelClient` 发起 side-query，但请求明确设置 `tools: []`。响应必须同时满足三个条件：

| 要求 | 不满足会怎样 | 防什么 |
| --- | --- | --- |
| 没有工具调用 | 抛错 | 模型试图借 side-query 之机去读写文件 |
| `finishReason` 精确等于 `stop` | 抛错 | `length` 意味着 JSON 被截断，解析出来是残缺的 |
| 正文是非空文本 | 抛错 | 空回复当成"没有相关记忆"会静默丢信息 |

三条都有测试：

```typescript
test.each([
  { message: assistantMessage("[]"), finishReason: "length" as const },
  { message: assistantMessage(null, [{ id: "bad", name: "read_file", arguments: "{}" }]),
    finishReason: "tool_calls" as const },
  { message: assistantMessage("   "), finishReason: "stop" as const },
])("rejects invalid side-query replies", async (reply) => { /* 全部 rejects */ });
```

第一条尤其值得说：`finishReason: "length"` 时正文是 `"[]"`，**看起来是合法 JSON**，`JSON.parse()` 会成功，解析出一个空数组。如果不检查 `finishReason`，这次截断就会被当成"模型认为没有相关记忆"，静默丢掉本该记住的内容。

随后使用 `JSON.parse()` 解析**整个**响应。它不会从代码围栏或解释文字中截取一段"看起来像 JSON"的子串。所以三个 side-query 的系统提示都明确写了禁止 Markdown 代码块：

```typescript
const NO_FENCE_INSTRUCTION =
  "直接输出纯 JSON 文本，禁止使用 Markdown 代码块或反引号包裹，禁止输出任何解释文字。";
```

这是个有意的严格取舍。宽松地"从响应里找 JSON"能提高成功率，但也会在模型输出 `` ```json ... ``` `` 加一段解释时，把解释里的示例 JSON 当成真结果。本章选择严格失败 + 确定性降级。

另外，selector、extractor、consolidator 的输入 JSON 都由 `stableJson()` 按键排序生成。同一个对象在不同运行中呈现相同的字段顺序，离线测试才能稳定断言，模型也少一个无谓的变量。

### 选择失败：确定性关键词回退

选择最多保留 5 条。以下任一情况都算失败：请求本身失败、输出不是 JSON 字符串数组、名称重复、引用了不存在的记忆。

失败后 `MemorySession` 记录降级状态，再按 `name + description` 做**确定性**关键词匹配：

- 英文：切成长度至少为 3 的 token
- 中文：切成相邻 bigram（"数据库" → `数据`、`据库`）
- 得分 = 命中的 token 数；得分相同时按记忆名稳定排序

注意回退**只扫 name 和 description，不读正文**，和选择器看到的目录范围一致。

三种失败方式，同一个结果：

```typescript
test.each([new Error("offline"), "not json", '["unknown"]'])(
  "uses deterministic keyword fallback after selector failure",
  async (output) => {
    // ... 存了 tabs-style 和 database 两条
    await session.beginTurn("Please check the database integration");
    expect(session.selected).toEqual([database]);
    expect(session.lastError).toBe("Memory selection failed; deterministic fallback used");
  },
);
```

中文 bigram 也有专门的测试，因为中文没有空格，按 token 切会整句变成一个词：

```typescript
const database = record("database", "生产数据库约束", "使用真实数据库。");
const interaction = record("interaction", "前端交互约束", "保持键盘可用。");
await session.beginTurn("检查数据库集成");
expect(session.selected).toEqual([database]);
```

查询"检查数据库集成"切出 `检查`、`查数`、`数据`、`据库`、`库集`、`集成`。`生产数据库约束` 里含 `数据` 和 `据库`，得分 2；`前端交互约束` 得分 0，被过滤掉。

这里的设计意图是：**记忆机制出问题，绝不能让主任务失败。** 模型不可用、返回垃圾、网络断了，Agent 照样得回答用户，最多是记忆选得没那么准。

### 注入的真实形状

选中的正文包在 `<relevant_memories>` 里。这是实际跑出来的注入内容：

```text
<relevant_memories>

## database-rule (feedback)

生产数据库约束

测试必须连真实集成数据库，禁止 mock。

</relevant_memories>
```

第 9 章由生命周期在模型请求前增加一条 system context 消息。第 10 章会改由动态 Prompt 的 `memory` section 注入，并把这一种注入关掉（靠 `emitContextMessages: false`），确保同一份记忆不会出现两次。

一条没选中的记忆，正文绝不会出现：

```typescript
expect(context[0]?.content).toContain("Never mock the database.");
expect(context[0]?.content).not.toContain("Always indent with tabs.");
```

没有任何记忆被选中时，`beforeModel()` 返回空数组，请求里干干净净不多一条消息。

### 注入不进 canonical history

这一条单独强调，因为它是后面「提取」正确的前提。

`beforeModel()` 返回的 `<relevant_memories>` **只拼进本次模型请求**，不会追加到 canonical history。Loop 层有测试钉死这件事：

```typescript
expect(model.requests[0]?.messages).toEqual([
  { role: "system", content: "test" },
  userMessage("go"),
  userMessage("memory context"),      // ← 进了请求
]);
expect(result.history).toEqual([userMessage("go"), assistantMessage("done")]);
                                     // ↑ 没进历史
```

同一个测试还钉住了三个生命周期点的顺序：

```typescript
expect(trace).toEqual(["begin:go", "before", "complete:2"]);
```

如果注入写进了 canonical history，会发生一件很糟的事：这一轮注入的记忆，下一轮会被提取器当成"会话里出现过的新事实"重新提取一遍。几轮之后，同一条记忆就以略微不同的措辞攒出好几份。记忆注入不污染会话证据，提取阶段才能只依据完整原始会话。

---

## 写入：从 canonical history 提取

用户通常不会特意说"请写入记忆"。所以不能等指令——Agent 给出本轮最终回答后，`MemorySession.complete()` 自动调用 extractor，从会话里找跨会话仍有价值的信息。

### 必须读 canonical history，不能读 request history

这是本章最容易出错的地方，也是第 8 章那个前置知识真正派上用场的位置。

第 8 章的三层压缩（snip、micro、摘要）只生成**本次模型请求使用的** request history。`AgentRunner` 持有的 canonical history 不会被这些请求级压缩改写。

```text
canonical history   ← 会话事实的唯一来源，完整，只增不改
      │
      ├─→ request history   ← 每次请求前从 canonical 派生，压缩作用在这里
      │                        用户消息可能被 snip 整组裁掉
      │
      └─→ 记忆提取读这一份 ✅
```

如果提取器读的是 request history，会漏掉什么？被 snip 裁掉的那段历史里的用户消息。用户在第 3 轮说的"别 mock 数据库"，到第 40 轮可能已经被裁出 request history 了——提取器读不到，这条约束就永远不会被记住。

所以提取读 canonical history，用户消息和普通 canonical 内容仍保持完整。

### 但有一个例外：大 ToolResult

大工具结果是另一个边界，需要单独讲清楚，否则容易产生错误预期。

第 8 章的第一层压缩（落盘）发生在结果**进入 canonical history 之前**。也就是说，canonical history 里存的从来就不是完整正文，而是 artifact 路径加首尾预览：

```text
<persisted-tool-result>
path: .agent_tutorial/artifacts/tool-result-3f9a....txt
original_bytes: 30003
head_preview:
（最多 2,000 bytes）
tail_preview:
（最多 2,000 bytes）
</persisted-tool-result>
```

推论：**如果关键事实只在原文中段，extractor 看不到它。**

举个具体例子。Agent 读了一个 30KB 的配置文件，里面第 400 行写着"生产环境禁用 debug 端点"。这条信息既不在头 2000 bytes 也不在尾 2000 bytes 里，那么 extractor 拿到的历史里根本没有这句话，它不可能提取出来。

要让这条信息进记忆，必须先把 artifact 读回来（让它作为一段正常大小的内容进入历史），不能假设 extractor 会自动看到完整文件。

### 输入是不可变快照

提取的输入先复制为不可变消息快照，并验证工具调用配对。extractor 拿不到调用者的消息对象引用：

```typescript
const before = structuredClone(history);
await session.complete(history);
expect(history).toEqual(before);                          // 原历史没被改
expect(histories[0]).not.toBe(history);                   // 不是同一个数组
expect((histories[0] as readonly ChatMessage[])[0]).not.toBe(history[0]);  // 也不是同一个对象
```

第三行是重点：连数组里的**元素**都是新对象，不只是浅拷贝了外层数组。记忆提取是个旁路功能，它绝不该有能力改动会话事实。

### 提取器的输出契约：四个字段，多一个都不行

```json
[
  {
    "name": "windows-examples",
    "type": "project",
    "description": "Project examples target Windows",
    "body": "Use PowerShell for user-facing commands."
  }
]
```

必须**且只能**包含这四个字段。任意一项 schema、类型、slug 或大小非法，**整批都不提交**。

"整批不提交"这条容易被忽略，但它是有意的：

```typescript
// 返回两条：第一条合法，第二条名字是 "../invalid"
await session.complete([userMessage("remember both"), assistantMessage("done")]);
expect(session.lastError).toBe("Memory extraction failed");
expect(await store.records()).toEqual([]);      // ← 合法的那条也没写进去
```

为什么不保留合法的那条？因为一批提取结果往往是相关的。模型返回 3 条记忆描述同一个决策的三个方面，只写进去 1 条，得到的是一个残缺且可能误导的状态。要么整批成立，要么保持原样。

提取 side-query 失败不会让主任务失败，但 `lastError` 会明确记录 `Memory extraction failed`，旧集合保持不变。用户拿到的回答照旧，只是这一轮没记下东西。

---

## 整理：先声明替换来源，再一次提交

记忆达到默认阈值 10 条时，整理器收到「当前集合 + 本轮合法提取结果」。

这里的关键约束是：它**不能**只返回一个新数组，而必须明确指出替换了哪些旧记录。

```json
{
  "source_names": ["tabs-old", "tabs-new"],
  "records": [
    {
      "name": "tabs",
      "type": "user",
      "description": "Indentation preference",
      "body": "Use tabs in this repository."
    }
  ]
}
```

读法是：「把 `tabs-old` 和 `tabs-new` 这两条，合并替换成 `tabs` 这一条」。

如果允许模型只返回一个新集合，会发生什么？模型漏写了几条，那几条就被静默删除了。它不需要"决定删除"，只需要"忘记提及"。所以本章要求它显式声明来源：

- `source_names` 必须非空、唯一，且都属于整理候选。
- `records` 必须是非空合法集合。
- **未列入 source 的记忆必须保留**，不能因为一次局部整理被顺手删掉。

这条有测试直接钉住：

```typescript
// 存了 first、second、unrelated 三条，整理只声明替换 first 和 second
await session.complete([userMessage("done"), assistantMessage("done")]);

expect(await store.records()).toEqual([
  unrelated,                                              // ← 没参与的原样保留
  record("merged", "Merged project facts", "merged body"),
]);
expect(JSON.parse(await readFile(join(root, ".memory", "manifest.json"), "utf8"))).toEqual({
  version: 1,
  files: ["unrelated-three.md", "merged-merged.md"],
});
```

最后还检查了目录里没有留下垃圾文件：

```typescript
expect((await readdir(join(root, ".memory"))).filter((n) => n.endsWith(".md")).sort())
  .toEqual(["MEMORY.md", "merged-merged.md", "unrelated-three.md"]);
```

被替换的 `first-one.md` 和 `second-two.md` 已经删了，只在 manifest 切换成功之后。

### 提交顺序：为什么不能先删旧文件

一个直觉的实现是「先删旧文件，再逐个写新文件」。这个顺序有个致命问题：删完旧文件、新文件还没写完时进程崩了，**记忆全没了**。

`MemoryStore.applyConsolidation()` 在文件锁内重新读取 manifest，然后严格按这个顺序走：

```text
① 验证参与整理的基础记录仍未变化
      ↓
② 合并未参与记录、并发新增记录、本轮新增和替换记录
      ↓
③ 生成并 fsync 新文件            ← 此时旧文件还在，manifest 还指向旧集合
      ↓
④ 生成有界派生索引并原子写入
      ↓
⑤ 最后原子替换 manifest          ← 这一步是「提交点」，之前之后集合都自洽
      ↓
⑥ 提交成功后才清理被替换的旧文件
```

第 ⑤ 步之前的任何时刻崩溃，manifest 仍指向完整旧集合，本轮未提交的文件会被清理。**不会出现"整理失败后记忆全空"的状态。**

三个失败点各有测试：

| 什么失败了 | 结果 |
| --- | --- |
| 索引超预算 | 旧 manifest 和目录内容逐字节不变，旧集合完整可读 |
| manifest 替换失败（文件被设成只读） | 索引被还原成旧版本，本轮新文件全部删除 |
| 记录文件独占创建撞名 | 整批文件都清掉，包括已经写成功的那些 |

第三条的测试很直观：

```typescript
// 目录里预先放了一个会撞名的 second-collision.md
await expect(store.extend([record("first", ...), record("second", ...)])).rejects.toThrow(/already exists/);
expect(await readdir(memoryRoot)).toEqual(["second-collision.md"]);   // first 也没留下
```

### 整理期间的并发新增会保留

模型思考期间（可能好几秒），另一个会话可能新增了记忆。这条记录没参与本次整理，所以必须保留。

实现的做法是：在锁内**重读** manifest 拿到最新集合，而不是用整理开始时的快照。测试用一个可控的 consolidator 精确复现了这个时序：

```typescript
const consolidator = {
  async consolidate() {
    started?.();                  // 通知测试：整理开始了，模型正在"思考"
    await releasePromise;         // 卡在这里等
    return '{"source_names":["first"],"records":[{"name":"merged", ...}]}';
  },
};
const completion = session.complete([...]);
await startedPromise;

// 趁整理卡住，用另一个 MemoryStore 实例写一条新记忆
await new MemoryStore({ workspace: root, idGenerator: ids("late") }).add(late);
release?.();                      // 放行整理
await completion;

expect(await store.records()).toEqual([late, record("merged", "Merged fact", "merged body")]);
//                                     ↑ 并发新增的那条还在
```

反过来，如果**参与整理的基础记录**在等待期间被改了，整理会直接失败（`memory collection changed during consolidation`）。这是必要的严格：模型是基于旧内容做的合并决策，底下的内容变了，那个决策就不再成立。

### 原子替换怎么做的

上面反复说的"原子写入"由 `atomicReplace()` 实现：

```text
① 在 .memory 下写一个带随机名的临时文件
② sync()  ← 确保内容真的落到磁盘，不是只在 OS 缓存里
③ rename 覆盖目标
```

`rename` 在同一文件系统内是原子操作，所以读取方要么看到完整的旧文件，要么看到完整的新文件，**绝不会看到半写状态**。`MEMORY.md` 和 `manifest.json` 都走这条路径。

第 ② 步的 `sync()` 容易被省掉，但省了就有风险：`rename` 成功了，内容还在 OS 缓存里没落盘，这时候断电，重启后 manifest 可能是个空文件或半个 JSON。

### 触发条件只有一个

这版只使用「候选数达到阈值」作为整理触发条件，没有实现 24 小时间隔、会话计数或锁文件 mtime 过期等额外门控。

这个简化有个直接后果值得知道：**记忆到 10 条以后，每一轮回合结束都会调一次 consolidator**，即使这一轮什么新东西都没提取到。这是教学上的刻意简化（少一套门控逻辑，行为更容易预测），但在真实项目里会持续产生模型调用成本。后面「这章没有做什么」会说清补法。

---

## 并发：两层锁

写操作会创建 `.memory/.lock`。它不是记忆集合的一部分，而是 `proper-lockfile` 用来做跨进程互斥的锁文件。

`MemoryStore` 的每次读改写都先经过**进程内 Promise 队列**，再获取这个**文件锁**：

```text
同一个 Node 进程里的多个并发回合
      ↓
① 进程内 Promise 尾链（排队）        ← 便宜，不碰文件系统
      ↓
② proper-lockfile 文件锁（跨进程）   ← 贵，但能挡住其他进程
      ↓
读改写 manifest / 索引 / 记录文件
```

为什么要两层？只有文件锁的话，同进程内的并发操作会互相争抢同一个锁文件，在 Windows 上还会撞上 `EBUSY` / `EPERM` 之类的重命名竞争。先在进程内排队，能把大部分争抢消化掉，文件锁只用来处理真正的跨进程情况。

两种场景都有测试：

```typescript
// 同一个 workspace，两个 MemoryStore 实例并发写
await Promise.all([
  firstStore.add(record("first", "First fact", "first body")),
  secondStore.add(record("second", "Second fact", "second body")),
]);
// 两条都在，没有互相覆盖
```

还有一个更狠的：8 个 writer 同时去恢复同一个**过期锁**。

```typescript
await mkdir(lockPath);
const staleTime = new Date(Date.now() - 60_000);   // 伪造成 60 秒前的锁
await utimes(lockPath, staleTime, staleTime);

const stores = Array.from({ length: 8 }, (_, i) =>
  new MemoryStore({ workspace: root, idGenerator: ids(`id-${i}`) }));
await Promise.all(stores.map((store, i) => store.add(record(`memory-${i}`, ...))));

expect(records).toHaveLength(8);      // 8 条全部写成功，一条不丢
```

过期锁是真实场景：进程崩溃时锁文件留在磁盘上，没人释放。`proper-lockfile` 的 `stale`（30 秒）和 `update`（10 秒）参数负责识别并恢复这种锁。这个测试验证的是：8 个进程同时判断"这锁过期了，我来接管"时，仍然只有一个能真正拿到。

---

## 失败矩阵：一张表看清所有边界

把前面散落各处的失败行为汇总一下。每一行都对应至少一个测试。

| 什么失败了 | 主任务 | 记忆集合 | `lastError` |
| --- | --- | --- | --- |
| selector 请求失败 / 返回非 JSON / 引用未知名称 | 正常完成 | 不变 | `Memory selection failed; deterministic fallback used` |
| extractor 请求失败 | 正常完成 | 不变 | `Memory extraction failed` |
| extractor 返回批次里有一项非法 | 正常完成 | 不变（合法项也不写） | `Memory extraction failed` |
| extractor 成功但写盘失败 | 正常完成 | 不变 | `Memory extraction failed` |
| consolidator 请求失败 | 正常完成 | 不变（提取结果也不写） | `Memory consolidation failed` |
| consolidator 返回空 `records` | 正常完成 | 不变 | `Memory consolidation failed` |
| 整理期间基础记录被改 | 正常完成 | 不变 | `Memory consolidation failed` |
| manifest 替换失败 | 正常完成 | 不变，索引已还原 | 对应阶段的错误 |
| manifest / 记录文件被手工改坏 | **失败** | 读不出来 | — |

前八行是同一个原则：**记忆是旁路能力，它坏了不能拖垮主任务。**

最后一行是例外，也是有意的。集合读不出来时不能"当作空集合继续跑"——那等于静默丢掉用户全部的长期偏好。这种情况必须明确报错，让人去看磁盘上到底怎么了。

有两条特别值得单独看，因为它们体现了"不做一半"的原则：

**提取失败后不整理。** 提取失败意味着这一轮的输入不完整，基于不完整输入做整理会得到错误的合并决策。

```typescript
expect(session.lastError).toBe("Memory extraction failed");
expect(consolidationCalls).toBe(0);          // consolidator 根本没被调用
expect(await store.records()).toEqual([first]);
```

**整理失败时提取结果也不写。** 因为这一轮的提取结果本来是要和整理一起提交的，只写提取不整理，会留下一个"该合并但没合并"的中间状态。

```typescript
// extractor 成功返回了 second，但 consolidator 抛错
expect(session.lastError).toBe("Memory consolidation failed");
expect(await store.records()).toEqual([first]);    // second 没写进去
```

---

## Memory 和 Session Memory 的分工

第 8 章的压缩机制仍然存在，它与长期记忆解决的是不同问题。这两个东西名字像，很容易混，所以对比一下：

| | Memory（本章） | Session Memory（第 8 章） |
| --- | --- | --- |
| 持久范围 | 跨 Agent 实例、跨会话 | 当前会话 |
| 存储位置 | `.memory/*.md` + `manifest.json` | request history 摘要与 transcript |
| 注入位置 | 第 9 章 context 消息；第 10 章 Prompt section | 替换本次请求看到的早期历史 |
| 解决什么 | 长期偏好、约束和项目知识 | 压缩后继续当前任务 |
| 会不会丢 | 不会，除非手工删文件 | 会话结束即无人再读 |

一句话区分：**压缩让当前会话跑得下去，记忆让下个会话不用从零开始。**

同一轮的实际时序：

```text
beginTurn(query)
  ↓
从 manifest 当前集合选择相关记忆         ← selector side-query（集合为空时跳过）
  ↓
组装 context 与请求级压缩历史             ← 第 8 章的 snip / micro / 摘要在这里
  ↓
模型和工具循环，canonical history 持续完整追加
  ↓
最终回答                                  ← 用户在这里就拿到结果了
  ↓
complete(canonicalHistory)
  ↓
提取；达到阈值时与整理结果一次提交         ← extractor（+ consolidator）side-query
```

### 什么该记，什么不该记

Memory 不保存"曾经发生过的一切"，只保存下个会话仍会用到的内容。

| 适合写进记忆 | 留给 canonical history / 摘要 / transcript |
| --- | --- |
| 用户偏好（缩进、语言、报告风格） | 本轮的工具调用细节 |
| 反复出现的约束（别 mock 数据库） | 完整对话逐字记录 |
| 项目架构决策及其原因 | "现在读到第几个文件了" |
| 关键依赖和版本约束 | 中间推理过程 |
| 排查入口（bug 记在哪个系统） | 一次性的临时状态 |

判断标准很简单：**下个会话开始时，不知道这件事会不会犯错？** 会，就记；不会，就别记。记多了不是免费的——每条记忆都要占目录空间，都要参与选择，都可能被选中挤占上下文。

---

## 运行第 9 章

### 第 0 步：准备环境

```powershell
Set-Location 'F:\笔记\Agent实操\code'
node --version      # 需要 Node 20 以上
npm install         # 只在第一次或依赖变动后执行
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch09
```

期望看到：

```text
Test Files  23 passed (23)
     Tests  214 passed (214)
```

输出中间会出现一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这不是报错。** 它是 `config.test.ts` 主动断言的一条预期输出——测试在验证"缺少配置时必须明确报错而不是静默降级"。只要最后一行是 `passed`，就一切正常。

### 第 2 步：让 Agent 记住一件事

这一步需要真实模型，先确认 `code\.env` 里配好了 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch09 -- --prompt "记住：本项目的示例只使用 PowerShell"
```

### 第 3 步：去磁盘上看那三个文件

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Get-ChildItem '.\.memory\' -Force | Format-Table Name, Length
```

期望看到类似（文件名里那串十六进制你跑出来的会不一样）：

```text
Name                                              Length
----                                              ------
MEMORY.md                                             86
manifest.json                                         72
powershell-examples-c60d6de8...426d.md               142
```

看 manifest 指向什么：

```powershell
Get-Content -LiteralPath '.\.memory\manifest.json' -Raw
```

看目录索引（这是选择器每轮都会看到的东西）：

```powershell
Get-Content -LiteralPath '.\.memory\MEMORY.md' -Raw
```

看记忆正文：

```powershell
Get-ChildItem '.\.memory\*.md' -Exclude 'MEMORY.md' |
  ForEach-Object { Get-Content -LiteralPath $_.FullName -Raw }
```

期望看到 frontmatter 加正文的结构。如果模型没有提取出任何记忆，`.memory` 目录可能不存在——换个更明确的 prompt 再试，比如把"记住"改成"请把这条约束记下来，以后所有命令示例都遵守"。

### 第 4 步：换一个会话，验证它真的记住了

关键在于这是**一次全新的进程**，上一轮的历史完全不存在：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 9 --prompt "这个项目的命令示例有什么约束？"
```

如果记忆生效，回答里会提到 PowerShell。这就是第 8 章做不到的事——那一章的摘要和 transcript 也在磁盘上，但下一个进程根本不知道它们存在。

### 第 5 步（可选）：完整门禁

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run lint
```

### 清理

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -Recurse -Force '.\.memory'
```

两件事要注意。

第一，`.memory` **没有自动清理机制**。它是持久层，本来就该一直在。但也就意味着你跑实验攒下的记忆会一直影响后续运行，觉得行为奇怪时先看一眼里面有什么。

第二，`code\.gitignore` 目前**没有**忽略 `.memory/`。也就是说在这个仓库里跑出来的记忆文件会被 Git 看到。你把这套机制搬到自己项目时，要先想清楚：记忆里可能含用户偏好和项目内部约束，是该提交（团队共享）还是该忽略（个人本地）。两种都合理，但必须是个明确决定。

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...`（测试输出中间） | `config.test.ts` 断言的预期输出 | 不用管，看最后一行是否 `passed` |
| `配置错误: Missing required settings: ...`（`npm run ch09` 后直接退出） | `code\.env` 没配 | 复制 `.env.example` 为 `.env` 并填三个值 |
| `.memory` 目录不存在 | 模型这一轮没提取出记忆 | 用更明确的 prompt；或看是不是 extractor 请求失败了 |
| `memory name must be a safe lowercase slug` | 模型返回了含大写、空格、中文或路径符号的名字 | 正常拦截；本轮不提交，换个 prompt 重试 |
| `memory file must not exceed 200 lines` | 记忆正文太长（正文上限 194 行） | 正常拦截；让模型写更精炼的正文 |
| `memory file must not exceed 4096 UTF-8 bytes` | 同上，中文正文约 1340 多字到顶 | 同上 |
| `memory model must not call tools` | side-query 里模型试图调工具 | 正常拦截；换模型，或检查是否误传了工具定义 |
| `memory model finishReason must be stop, got length` | JSON 被输出长度截断 | 提高 `max_tokens`；或让模型少提取几条 |
| `memory model output is not valid JSON` | 模型加了代码围栏或解释文字 | 正常拦截；换个更听话的模型 |
| `memory manifest has an invalid schema` | manifest 被手工改坏 | 对照本章格式修回去；或删掉整个 `.memory` 重来 |
| `memory root escapes workspace` | `.memory` 被换成了符号链接 / junction | 删掉链接，让运行时自己建真实目录 |
| `memory names already exist: ...` | 提取出的名字和已有记忆重名 | 正常保护；说明这条已经记过了 |
| `memory collection changed during consolidation` | 整理期间基础记录被别的进程改了 | 正常保护；下一轮会重新整理 |
| 记忆写进去了但不被选中 | selector 觉得不相关，或回退匹配不到关键词 | 查询里带上记忆 `description` 里的词试试 |

---

## 五个动手小实验

这五个实验都**不调真实模型**，不需要 API Key，跑完就能看到本章所有核心数字。先建一个临时目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\scratch' | Out-Null
```

下面每个实验的输出都是实际跑出来的。文件名里的十六进制串是随机 UUID，**你跑出来的会和这里不一样**，这是正常的。

### 实验一：磁盘上到底有哪三样东西

存成 `code\scratch\probe1-layout.ts`：

```typescript
import { mkdtempSync, readFileSync, readdirSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { MemoryRecord, MemoryStore, MemoryType } from "../chapters/ch09/src/features/memory.js";

const workspace = mkdtempSync(join(tmpdir(), "probe1-"));
const store = new MemoryStore({ workspace });

await store.add(
  new MemoryRecord({
    name: "tabs-style",
    description: "Tabs indentation preference",
    kind: MemoryType.USER,
    body: "Always indent with tabs.",
  }),
);

const root = join(workspace, ".memory");
console.log("目录内容 =", readdirSync(root).sort());
console.log("manifest  =", readFileSync(join(root, "manifest.json"), "utf8"));
console.log("MEMORY.md =", JSON.stringify(readFileSync(join(root, "MEMORY.md"), "utf8")));

const recordFile = readdirSync(root).find((n) => n.startsWith("tabs-style-"));
console.log("记录文件  =", JSON.stringify(readFileSync(join(root, recordFile!), "utf8")));
console.log("记录字节数 =", readFileSync(join(root, recordFile!)).byteLength);

rmSync(workspace, { recursive: true, force: true });
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe1-layout.ts
```

实际输出：

```text
目录内容 = [
  'MEMORY.md',
  'manifest.json',
  'tabs-style-868e19a8b31d414b9d579747f90bf933.md'
]
manifest  = {"version":1,"files":["tabs-style-868e19a8b31d414b9d579747f90bf933.md"]}
MEMORY.md = "- [tabs-style](tabs-style-868e19a8b31d414b9d579747f90bf933.md) - Tabs indentation preference\n"
记录文件  = "---\nname: tabs-style\ndescription: Tabs indentation preference\ntype: user\n---\n\nAlways indent with tabs.\n"
记录字节数 = 103
```

这里用 `JSON.stringify` 打印是有意的——能看清 `\n` 的确切位置。注意记录文件里 frontmatter 结束的 `---` 之后有一个**空行**，这就是前面算行预算时占掉的第 6 行。

一条只有 24 个字符正文的记忆，落盘是 103 bytes。差的 79 bytes 全是 frontmatter，这也是为什么 4096 bytes 的预算要算整个文件。

### 实验二：两个预算和名称校验的精确边界

存成 `code\scratch\probe2-budget.ts`：

```typescript
import { MemoryRecord, MemoryType } from "../chapters/ch09/src/features/memory.js";

function tryBody(label: string, body: string): void {
  try {
    new MemoryRecord({ name: "probe", description: "d", kind: MemoryType.PROJECT, body });
    console.log(`${label}: 通过`);
  } catch (error) {
    console.log(`${label}: 拒绝 -> ${(error as Error).message}`);
  }
}

console.log("--- 行预算 ---");
for (const lines of [193, 194, 195]) {
  tryBody(`正文 ${lines} 行`, Array(lines).fill("x").join("\n"));
}

console.log("--- 字节预算（中文一字 3 bytes）---");
for (const count of [1330, 1340, 1350, 1400]) {
  tryBody(`正文 ${count} 字 = ${count * 3} bytes`, "界".repeat(count));
}

console.log("--- 精确边界扫描 ---");
for (let n = 1340; n <= 1360; n += 1) {
  try {
    new MemoryRecord({
      name: "probe",
      description: "d",
      kind: MemoryType.PROJECT,
      body: "界".repeat(n),
    });
  } catch {
    console.log(`第一个被拒的字数: ${n}（正文 ${n * 3} bytes）`);
    break;
  }
}

console.log("--- 名称校验 ---");
for (const name of ["ok-name", "../escape", "has space", "UPPER", "com1", "trailing."]) {
  try {
    new MemoryRecord({ name, description: "d", kind: MemoryType.PROJECT, body: "b" });
    console.log(`${name.padEnd(12)}: 通过`);
  } catch (error) {
    console.log(`${name.padEnd(12)}: 拒绝 -> ${(error as Error).message}`);
  }
}
```

实际输出：

```text
--- 行预算 ---
正文 193 行: 通过
正文 194 行: 通过
正文 195 行: 拒绝 -> memory file must not exceed 200 lines
--- 字节预算（中文一字 3 bytes）---
正文 1330 字 = 3990 bytes: 通过
正文 1340 字 = 4020 bytes: 通过
正文 1350 字 = 4050 bytes: 拒绝 -> memory file must not exceed 4096 UTF-8 bytes
正文 1400 字 = 4200 bytes: 拒绝 -> memory file must not exceed 4096 UTF-8 bytes
--- 精确边界扫描 ---
第一个被拒的字数: 1349（正文 4047 bytes）
```

**正文 194 行是上限，不是 200 行。**中文正文到 1348 字为止，1349 字就被拒——4047 bytes 正文加上 frontmatter 已经越过 4096。

这个 1348 是**用最短的 `description: "d"` 量出来的**。换成一句十几字的中文描述，frontmatter 变大，上限就会往下掉几个字。所以别把 1348 当成固定值记，要记的是「4096 算整个文件，frontmatter 也要占」。

名称校验部分：

```text
ok-name     : 通过
../escape   : 拒绝 -> memory name must be a safe lowercase slug
has space   : 拒绝 -> memory name must be a safe lowercase slug
UPPER       : 拒绝 -> memory name must be a safe lowercase slug
com1        : 拒绝 -> memory name must be a safe lowercase slug
trailing.   : 拒绝 -> memory name must be a safe lowercase slug
```

注意 `com1` 和 `trailing.`——这两个在 Linux 上完全合法，是专门为 Windows 挡的。

### 实验三：选择、注入和回退

存成 `code\scratch\probe3-select.ts`。这个实验用假的 selector，所以不需要模型：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import {
  MemoryRecord, MemorySession, MemoryStore, MemoryType,
} from "../chapters/ch09/src/features/memory.js";
import type { MemorySelector } from "../chapters/ch09/src/features/memory.js";

const workspace = mkdtempSync(join(tmpdir(), "probe3-"));
const store = new MemoryStore({ workspace });

await store.extend([
  new MemoryRecord({
    name: "tabs-style", description: "缩进使用 Tab", kind: MemoryType.USER,
    body: "本仓库统一用 Tab 缩进，不用空格。",
  }),
  new MemoryRecord({
    name: "database-rule", description: "生产数据库约束", kind: MemoryType.FEEDBACK,
    body: "测试必须连真实集成数据库，禁止 mock。",
  }),
]);

// A. selector 正常工作
const ok: MemorySelector = { async select() { return '["database-rule"]'; } };
const session = new MemorySession({ store, selector: ok });
await session.beginTurn("数据库测试怎么跑？");
console.log("A. 正常选择 =", session.selected.map((r) => r.name));
console.log("   注入消息条数 =", session.beforeModel().length);
console.log(session.beforeModel()[0]?.content);

// B. selector 失败，中文 bigram 回退
const dead: MemorySelector = { async select() { throw new Error("offline"); } };
const fallback = new MemorySession({ store, selector: dead });
await fallback.beginTurn("检查数据库集成");
console.log("B. 回退选择 =", fallback.selected.map((r) => r.name));
console.log("   lastError =", fallback.lastError);

// C. selector 返回不存在的名称
const bogus: MemorySelector = { async select() { return '["no-such-memory"]'; } };
const rejected = new MemorySession({ store, selector: bogus });
await rejected.beginTurn("Tab 还是空格？");
console.log("C. 未知名称 =", rejected.selected.map((r) => r.name));

// D. 完全匹配不上
const none = new MemorySession({ store, selector: dead });
await none.beginTurn("weather forecast");
console.log("D. 无匹配注入条数 =", none.beforeModel().length);

rmSync(workspace, { recursive: true, force: true });
```

实际输出：

```text
A. 正常选择 = [ 'database-rule' ]
   注入消息条数 = 1
<relevant_memories>

## database-rule (feedback)

生产数据库约束

测试必须连真实集成数据库，禁止 mock。

</relevant_memories>
B. 回退选择 = [ 'database-rule' ]
   lastError = Memory selection failed; deterministic fallback used
C. 未知名称 = [ 'database-rule', 'tabs-style' ]
D. 无匹配注入条数 = 0
```

A 和 B 得到同一个结果，但路径完全不同：A 是模型选的，B 是关键词匹配算的。B 的 `lastError` 明确记录了降级——这条信息不会丢，只是不影响主任务。

**C 的输出值得停下来看。**查询是"Tab 还是空格？"，结果 `database-rule` 排在第一个。为什么？

因为回退匹配是**子串匹配**。查询切出的 token 里有 `tab`，而 `database-rule` 这个名字里的 `database` 恰好包含 `tab`：

```text
database-rule → "data-tab-ase"？不是。是 "da-tab-ase" 里的 tab
                  ↑↑↑
```

两条记忆都得 1 分，得分相同按名称排序，`database-rule` 字母序在前。

这是确定性回退的真实局限，不是 bug——它是个字符串匹配，不理解语义。写进教程是因为你在自己项目里用这套回退时会遇到同类情况：`data` 会匹配到 `metadata`，`test` 会匹配到 `latest`。回退的定位是"模型不可用时别彻底瞎选"，不是"和模型一样准"。

### 实验四：局部整理不碰无关记忆

存成 `code\scratch\probe4-consolidate.ts`（省略 import，和实验三同源）：

```typescript
await store.extend([
  record("tabs-old", "旧的缩进偏好", "用 tab。"),
  record("tabs-new", "新的缩进偏好", "确认用 tab，不用空格。"),
  record("unrelated", "无关的参考资料", "bug 记在 Linear INGEST。"),
]);

console.log("整理前 =", (await store.records()).map((r) => r.name));
console.log("整理前文件 =", readdirSync(root).filter((n) => n.endsWith(".md")).sort());

// 只声明替换 tabs-old 和 tabs-new，unrelated 没被提及
const consolidator: MemoryConsolidator = {
  async consolidate() {
    return JSON.stringify({
      source_names: ["tabs-old", "tabs-new"],
      records: [
        { name: "tabs", type: "user", description: "缩进偏好", body: "本仓库用 tab，不用空格。" },
      ],
    });
  },
};

const session = new MemorySession({ store, consolidator, consolidateThreshold: 2 });
await session.complete([userMessage("done"), assistantMessage("done")]);

console.log("整理后 =", (await store.records()).map((r) => r.name));
console.log("整理后文件 =", readdirSync(root).filter((n) => n.endsWith(".md")).sort());
```

实际输出（为了看清，文件名截短了）：

```text
整理前 = [ 'tabs-old', 'tabs-new', 'unrelated' ]
整理前文件 = [ 'MEMORY.md',
  'tabs-new-95df2404....md',
  'tabs-old-9f16a0d1....md',
  'unrelated-deb1cbc1a8bd47a1970b38d19ec97ce3.md' ]

整理后 = [ 'unrelated', 'tabs' ]
整理后文件 = [ 'MEMORY.md',
  'tabs-0dcc0132....md',
  'unrelated-deb1cbc1a8bd47a1970b38d19ec97ce3.md' ]
lastError = undefined
```

三件事一起发生了：

1. `tabs-old` 和 `tabs-new` 合并成了 `tabs`，两个旧文件都删了。
2. `unrelated` 还在——它的**文件名一个字符都没变**（`deb1cbc1a8bd...`）。这证明它不是被重写了一遍，是原封不动留着的。
3. 新的 `tabs` 是一个全新文件名，不是覆盖某个旧文件。

脚本后半段还试了一次非法整理：

```typescript
const bad: MemoryConsolidator = {
  async consolidate() { return JSON.stringify({ source_names: ["tabs"], records: [] }); },
};
```

```text
空 records 后 = [ 'unrelated', 'tabs' ]
lastError = Memory consolidation failed
```

`records` 为空会被拒，集合保持原样。如果不拦这一条，模型返回一个空数组就等于"把 `tabs` 删掉"，而它并没有表达删除的意图。

### 实验五：manifest 才是权威

存成 `code\scratch\probe5-manifest.ts`。这个实验直接验证本章最核心的架构决定：

```typescript
await store.add(new MemoryRecord({
  name: "registered", description: "登记过的记忆",
  kind: MemoryType.PROJECT, body: "这条在 manifest 里。",
}));

// 手工放一个格式完全合法、但没登记进 manifest 的文件
writeFileSync(
  join(root, "sneaky-deadbeef.md"),
  "---\nname: sneaky\ndescription: 没登记的记忆\ntype: project\n---\n\n这条不在 manifest 里。\n",
  "utf8",
);

console.log("目录里的 .md =", readdirSync(root).filter((n) => n.endsWith(".md")).sort());
console.log("records()    =", (await store.records()).map((r) => r.name));

// 删掉派生视图，看它是否影响事实
rmSync(join(root, "MEMORY.md"));
console.log("删掉 MEMORY.md 后 records() =", (await store.records()).map((r) => r.name));

// 把 manifest 改坏
writeFileSync(join(root, "manifest.json"), JSON.stringify({ version: true, files: [] }), "utf8");
try {
  await new MemoryStore({ workspace }).records();
  console.log("坏 manifest: 竟然通过了");
} catch (error) {
  console.log("坏 manifest: 拒绝 ->", (error as Error).message);
}
```

实际输出：

```text
目录里的 .md = [ 'MEMORY.md', 'registered-f761ce9c....md', 'sneaky-deadbeef.md' ]
manifest     = {"version":1,"files":["registered-f761ce9caec64b4891591468368924c4.md"]}
records()    = [ 'registered' ]
renderCatalog():
- [registered](registered-f761ce9caec64b4891591468368924c4.md) - 登记过的记忆

删掉 MEMORY.md 后 records() = [ 'registered' ]
坏 manifest: 拒绝 -> memory manifest has an invalid schema
```

三个结论，一次全看到了：

| 做了什么 | 结果 | 说明什么 |
| --- | --- | --- |
| 放一个合法但未登记的 `.md` | `records()` 里没有它 | **扫目录的实现会读到它，本章不会** |
| 删掉 `MEMORY.md` | `records()` 照常返回 | 它是派生视图，不承担事实职责 |
| 把 `version` 改成 `true` | 明确报错 | 权威指针坏了必须报错，不能当空集合 |

第一行是这套架构的核心价值。如果实现是扫目录，那个 `sneaky-deadbeef.md` 就会被当成有效记忆注入模型上下文——而它可能是上一次失败整理留下的过期内容，或者是别的什么东西。

### 清理

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -Recurse -Force '.\scratch'
```

---

## 这章没有做什么

照着教程宣称不存在的能力，是最容易踩的坑。把边界列清楚：

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| 名称 + 描述的关键词匹配 | 向量检索、语义召回 | **不补**。见「为什么选文件系统」的取舍 |
| 候选数达到阈值触发整理 | 24 小时间隔、会话计数、锁 mtime 过期门控 | **不补**。教学上的刻意简化，真实项目建议加 |
| 单模型 side-query 提取与整理 | 独立 Reviewer 审核、证据引用 | **不补**。见「从 ai-agent-book 学到什么」 |
| 离线测试覆盖机制正确性 | 记忆质量评测、LLM-as-judge | **不补**。这是下一步该做的事，不是本章范围 |
| 第 9 章 context 消息注入 | 动态 Prompt 的 `memory` section | 第 10 章 |
| `MemoryStore` 事务与锁 | 记忆的时间衰减、重要性评分 | **不补**。需要额外的评分维度和历史版本 |
| frontmatter + 正文两层 | 嵌套槽位、person/relationship、版本化冲突字段 | **不补**。会让格式复杂度超过教学收益 |
| 提取失败记录 `lastError` | 把 `lastError` 上报给用户或监控 | **不补**。属于可观测性，不该由记忆模块决定 |
| `.memory` 持久保留 | 自动清理、脱敏、加密 | **不补**。持久层本来就该一直在 |

有两条要特别强调。

**没有语义检索。** 查询"缩进用什么"，如果记忆的 `description` 写的是"代码风格约定"，selector 可能选中（模型理解语义），但确定性回退一定选不中（没有共同字符串）。这意味着 `description` 怎么写会直接影响回退的可用性——尽量用用户可能真的说出口的词。

**没有记忆质量评测。** 本章的 214 个测试全部在验证"机制是否正确"：写入是否原子、失败是否不丢、边界是否守住。**没有一个测试在验证"记住的东西是否有用"。**这两件事完全不同，后者需要构造评测集，是下一步的工作。

---

## 与 Claude Code 的差异

Claude Code 的记忆是双轨制：`CLAUDE.md` 提供人工编写、跨会话加载的持久指令，auto memory 让 Claude 自己记录构建命令、调试经验和偏好；auto memory 默认开启，`/memory` 命令可查看与编辑（官方文档见 [How Claude remembers your project](https://code.claude.com/docs/en/memory.md)）。本教学版只实现了 auto memory 风格的单轨 `MemoryStore`，但它比参考实现更强调可审计的集合事务。

**存储与索引。** Claude Code 的 auto memory 存储在 `~/.claude/projects/<sanitized-git-root>/memory/`，按 git 仓库隔离并跨 worktree 共享；每个项目目录内以 `MEMORY.md` 作为入口索引，Claude 用标准文件工具按需读写 topic 文件。P09 把记忆放在 workspace 本地 `.memory/`，并通过 `manifest.json` 作为唯一权威指针、`MEMORY.md` 作为可重建派生视图，因此不会把目录中未登记或未提交完成的 `.md` 误当成有效记忆。参考实现直接扫描目录，教学版用 manifest 换取更明确的提交边界。

**预算与选择。** Claude Code 每次会话加载 `MEMORY.md` 的前 200 行或 25KB（frontmatter 和块级 HTML 注释不计入），topic 文件按需读取；选择器最多选 5 条，单会话记忆内容预算约 60KB。P09 的派生目录默认同样按 200 行约束，但字节预算默认只有 4096，且记录文件也同时受 200 行 / 4096 字节限制。选择器同样最多选 5 条，并在 side-query 失败时降级为确定性关键词匹配。

**提取与整理。** Claude Code 用受限子代理在 stop hook 中异步提取，整理（Dream）有四层门控：距上次合并至少 24 小时、扫描节流、自上次合并以来至少 5 个会话 transcript 有修改、并持有合并锁。P09 的提取是同一模型的无工具 side-query，失败只记录 `lastError` 不中断主任务；整理只由候选数达到阈值（默认 10）触发，没有时间、会话数或锁 mtime 门控，这是教学上的刻意简化。

**并发与失败边界。** Claude Code 的记忆目录由 Agent 自己通过文件工具写入，`MEMORY.md` 超限时返回"memory index is over its read limit"错误，整理阶段有 `.consolidate-lock` 和过期恢复。P09 把并发问题下沉到 `MemoryStore`：同进程 Promise 尾链 + 跨进程 `proper-lockfile`，每个读改写都先加锁，再以 fsync + 原子替换提交 manifest；整理时还会在锁内重读基础记录，发现并发修改立即失败。对教程读者而言，P09 的事务模型比 CC 更保守，适合作为自己实现长期记忆时的参考。

一个值得注意的架构差异：**Claude Code 让 Agent 用普通文件工具读写记忆，P09 不让模型碰文件。** P09 里模型只负责返回 JSON（选哪些、提取什么、怎么合并），所有写盘动作都由 `MemoryStore` 执行。代价是模型少了灵活性（不能自己组织文件结构），换来的是每次写入都必经同一套校验和事务。

---

## 从 ai-agent-book 学到什么：先立评估，再谈存储

本地仓库的 `ai-agent-book/book/chapter3.md`（配套实验在 `ai-agent-book/chapter3/`）是本章最接近的对照：它也把"跨会话记忆"拆成轨迹和长期记忆两层，并继续覆盖记忆表示、检索、整理和评估。P09 是教学上的最小文件实现，ai-agent-book 则展示完整生产路径。对照后能更清楚地区分"哪些设计 P09 已经做到""哪些是后续扩展才需要补的能力"。

| ai-agent-book 的关键设计 | P09 对应 | 差异与边界 |
| --- | --- | --- |
| 轨迹是当前会话只增不改的原始事件记录；长期记忆是跨会话提炼、可改写的知识 | canonical history 近似轨迹，MemoryStore 近似长期记忆 | P09 的 canonical history 只在运行期完整保留，跨运行 transcript 由第 8 章负责；记忆记录不保存证据 ID |
| 工作记忆是从长期记忆按相关性激活的动态子集 | request history + `<relevant_memories>` 只在模型请求中注入 | 记忆选择在 `beginTurn` 后只服务本次请求，不追加到 canonical history |
| 四种存储格式：Simple Notes、Enhanced Notes、JSON Cards、Advanced JSON Cards | `.md` frontmatter + 正文接近 Enhanced Notes | 没有嵌套槽位、backstory、person/relationship 或版本化冲突字段 |
| 记忆压缩：重要性评分、聚类、抽象、版本化冲突检测 | 候选数达到阈值后由 consolidator 返回 `source_names` 和新集合 | P09 不做评分、聚类、时间衰减，也不保留历史版本 |
| 文件系统范式 OpenViking：L0 摘要、L1 概览、L2 全文按需加载 | `MEMORY.md` 是有界目录，`<name>-<id>.md` 是按需正文 | P09 用 manifest 作为权威提交边界，比目录扫描严格，但没有文件间链接导航 |
| 增量更新 + 周期全量整理，Proposer/Reviewer 审核 PR | 每轮 extractor 增量提取，达到阈值后一次性整理 | P09 是单模型 side-query，没有独立 Reviewer、证据引用或 PR 审核 |
| 双层记忆：Advanced JSON Cards 常驻概览 + 上下文感知 RAG 取细节 | 目录常驻选择、正文按需注入 | P09 没有向量检索、语义召回、冲突消解或主动服务 |
| 三层次评估：基础回忆、多会话检索、主动服务 | 离线测试覆盖机制正确性 | P09 没有三层记忆评测，也未用 LLM-as-judge 验证记忆质量 |

ai-agent-book 最值得借鉴的一点，是先把"什么样的记忆系统算好"定义成三层次评估框架，再设计存储与检索。实验 3-1 为每层构造 20 个用例，共 60 条；实验 3-2 又在统一接口下切换四种存储格式，让同一组会话和查询对比 Simple Notes、Enhanced Notes、JSON Cards、Advanced JSON Cards 的提取形态与最终得分。这种"先立标尺、再做实验"的顺序，比只测"文件是否写入成功"更能暴露记忆质量问题。

另一个值得对照的是知识更新。ai-agent-book 主张把增量更新和周期整理都当成代码库 PR：Proposer Agent 依据原始证据提出 diff，异源 Reviewer Agent 独立核对证据与冲突，通过后再重建派生索引。P09 已经在存储层实现了"原始 canonical history、整理后的记录、可重建的 MEMORY.md"三者分离，并且用 manifest 原子提交避免半写状态；但提取、整理仍是同一次固定 side-query，没有审核环节，也没有在记录里保存"从哪段证据来、什么时候批准"。

因此，P09 的强项不在"记忆质量更高"，而在"边界更可审计"：manifest 唯一权威、跨进程锁、fsync 后原子替换、选择失败时确定性降级，以及 `beginTurn`/`beforeModel`/`complete` 三个明确生命周期点。ai-agent-book 的贡献则是把质量、检索、冲突和审核作为独立维度补上。若要在 P09 上继续演进，应优先加入记忆评测集、证据引用与 Reviewer 审核，而不是先堆向量数据库。

---

## 配套代码索引

第 9 章实现集中在 `code/chapters/ch09/src/`，章节入口固定选择 P09 Profile。上一章已有的 `features/compaction.ts` 继续负责请求级压缩和 artifact 落盘；本章新增 `features/memory.ts`，并通过 `core/loop.ts` 的 `TurnLifecycle` 接入 AgentRunner。

- 功能实现：`code/chapters/ch09/src/features/memory.ts`
- 组合根：`code/chapters/ch09/src/bootstrap.ts`
- 生命周期：`code/chapters/ch09/src/core/loop.ts`
- 能力与 profile：`code/chapters/ch09/src/core/profiles.ts`
- 章节入口：`code/chapters/ch09/src/chapters/ch09.ts`
- 压缩边界：`code/chapters/ch09/src/features/compaction.ts`
- 直接行为测试：`code/chapters/ch09/tests/memory.test.ts`
- 接线测试：`code/chapters/ch09/tests/ch09-memory.test.ts`
- Loop 边界测试：`code/chapters/ch09/tests/loop-processors.test.ts`

从上到下读的话，`memory.ts` 里三个类的分工是这样的：

```text
MemoryRecord    ← 一条记忆，构造即校验，不可变
     ↑
MemoryStore     ← 集合的事务入口：锁、manifest、原子提交
     ↑
MemorySession   ← 回合生命周期：选择 / 注入 / 提取 / 整理
     ↑
AgentRunner     ← 通过 TurnLifecycle 接口调用，不知道记忆的存在
```

最后一行值得注意：`AgentRunner` 只认 `TurnLifecycle` 这个接口（三个方法：`beginTurn` / `beforeModel` / `complete`），完全不知道记忆是什么。所以记忆能力可以整体拔掉，Loop 不需要改一行。

| 实现 | 职责与边界 |
| --- | --- |
| `MemoryRecord` | 不可变记忆值对象；校验安全 slug、单行 description、`MemoryType`、正文预算，序列化后再次执行 200 行 / 4096 bytes 限制 |
| `MemoryStore.records()` / `renderCatalog()` | 只认 manifest 中的集合；读取前校验 workspace、`.memory` realpath、manifest schema 与每条记录，并加锁重读 |
| `MemoryStore.add()` / `extend()` | 只追加当前集合中不存在的新记忆；生成唯一文件名、独占创建记录文件、fsync 后回读验证，再原子提交索引与 manifest |
| `MemoryStore.applyConsolidation()` | 锁内重读 manifest，验证 `baseRecords` 未变化，保留未参与记录和并发新增，按 `source_names` 替换后一次提交 |
| `ModelMemoryQueries` | selector / extractor / consolidator 共用无工具模型边界；要求 `finishReason === "stop"` 和非空全文 JSON，不从围栏或解释文字截取子串 |
| `MemorySession` | 回合前选择记忆、请求前注入 `<relevant_memories>`、回合后用完整 canonical history 提取并整理；失败只记录 `lastError`，不中断主任务 |
| `keywordSelect` | side-query 失败后的确定性回退；英文按至少 3 字符 token，中文按 bigram，得分相同按名称稳定排序 |
| `atomicReplace()` / 进程互斥 / `proper-lockfile` | 同目录临时文件 + fsync + rename，保证读方看不到半写文件；同进程 Promise 尾链和跨进程文件锁共同串行文件事务 |
| `TurnLifecycle` | `beginTurn` 在首轮模型请求前执行，`beforeModel` 只影响下一次模型请求，`complete` 使用完整 canonical history 收尾 |
| `bootstrap.createMemorySession()` | 把 `MemoryStore` 和 `ModelMemoryQueries` 组装为 `MemorySession`，并按 profile 能力位决定是否注入 |
| `P09` | 在 P08 能力上追加 `memory`，使同一 AgentRunner 同时拥有请求级压缩和跨会话文件记忆 |
| `ch09.ts` | 固定 P09 的章节入口，防止该入口意外启用其他能力 |
| `CompactionManager` | 第 8 章继承能力；压缩只改写 request history，canonical history 保持完整，因此 memory extractor 仍能读取原始会话 |

---

## 运行与验证

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch09 -- --prompt "记住：本项目的示例只使用 PowerShell"
npm run agent-tutorial -- run --chapter 9 --prompt "这个项目的命令示例有什么约束？"
```

离线行为验证覆盖跨实例选择、严格 side-query、路径边界、写入故障、局部整理和整理期间并发新增：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch09
```

当前结果：`Test Files 23 passed (23)`，`Tests 214 passed (214)`。

---

## 本章小结

### 三句话版本

1. 记忆是磁盘上一组 `.md` 文件，不参与压缩，跨会话跨进程保留。
2. `manifest.json` 是当前集合的唯一权威指针；`MEMORY.md` 只是可重建的派生视图。
3. 记忆机制的任何失败都只记 `lastError`，主任务照常完成——除非权威指针本身坏了。

### 一定要记住的九条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 读取集合只认 manifest，目录里未登记的 `.md` 一律忽略 | 一份集合，两份派生视图 / 实验五 |
| 2 | 提取必须读 canonical history；读 request history 会漏掉被 snip 裁掉的用户消息 | 写入：从 canonical history 提取 |
| 3 | 大 ToolResult 中段的事实 extractor 看不到，必须先读回 artifact | 同上 |
| 4 | 正文上限是 **194 行**，不是 200——frontmatter 占 6 行 | 两个预算 / 实验二 |
| 5 | 4096 bytes 算整个文件，frontmatter 也占；中文正文约 1340 多字到顶 | 同上 |
| 6 | side-query 必须 `finishReason === "stop"`，`length` 时 `"[]"` 是截断不是空结果 | side-query 的三条硬要求 |
| 7 | 提取批次里一项非法，整批都不提交，合法项也不写 | 提取器的输出契约 |
| 8 | 整理必须声明 `source_names`；未列入的记忆必须保留 | 整理：先声明替换来源 |
| 9 | manifest 原子替换是提交点，之前崩溃旧集合完整无损 | 提交顺序 |

### 检查你是否真的读懂了

1. 开了 memory 能力，一个回合最多多花几次模型调用？集合为空时是几次？（先看一眼记忆真的跨会话生效）
2. 手工在 `.memory` 里放一个格式完全合法的 `.md`，它会被注入模型上下文吗？（实验五）
3. 删掉 `MEMORY.md`，`records()` 还能读出记忆吗？（实验五）
4. 用户在第 3 轮说的约束，到第 40 轮还能被提取出来吗？为什么？（写入：canonical vs request）
5. 一个 30KB 配置文件第 400 行的约束，extractor 能提取到吗？（大 ToolResult 例外）
6. 正文写 195 行会怎样？194 行呢？（实验二）
7. side-query 返回 `finishReason: "length"` 且正文是 `"[]"`，会被当成"没有记忆"吗？（side-query 硬要求）
8. 提取返回 3 条，其中第 2 条名字是 `../bad`，最终写进去几条？（提取器输出契约）
9. 整理只声明替换 2 条，集合里另外 5 条会怎样？（实验四）
10. manifest 替换失败时，磁盘上是什么状态？记忆会丢吗？（提交顺序 / 失败矩阵）
11. selector 抛错之后，主 Agent 还会正常回答用户吗？（失败矩阵）
12. 查询"Tab 还是空格？"时，回退为什么会选中 `database-rule`？（实验三 C）

---

## 记忆解决跨会话，下一章解决 Prompt 本身

这一章当前能直接保证的是：记忆集合以 manifest 为唯一权威提交边界；写入经 fsync 加原子替换，任何中途失败都不会丢掉旧集合；提取读完整 canonical history，不受请求级压缩影响；选择失败降级为确定性关键词匹配，不阻断主任务；跨进程并发由两层锁串行。

它没有实现语义检索，没有整理的时间与会话门控，没有 Reviewer 审核和证据引用，也没有记忆质量评测。`.memory` 需要你自己决定是否纳入版本控制。

按需加载负责少拿，Context Compact 负责及时放下，文件级记忆负责下次还能记起来——三件事到这里齐了。

但代价是 system prompt 越来越乱。工具列表、workspace 说明、Skill 目录、已选记忆，每加一个能力就往那个字符串里塞一段，到现在已经没人敢改。下一章处理这个问题：让工具、workspace、Skill 和已选记忆都从运行态组装，不再维护一锅硬编码字符串。
