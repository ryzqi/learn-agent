项目跑起来之前，我给 Agent 准备了三份文档：TypeScript 代码规范、SQL 风格指南、API 设计约定。加起来几千行。

最直接的做法是全部塞进 System Prompt。结果是 Agent 每次请求模型，不管在改一行注释还是设计一张表，都带着全量文档；绝大多数内容和当前任务无关，但每轮都要占上下文。

更麻烦的是，规范会不断增加。每新增一份文档，System Prompt 就再膨胀一截。上一章刚用 Subagent 隔离了探索轨迹，这里又把静态知识一次性塞满上下文，显然不划算。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaEqiby9nHnFAthU3CsU1lt3JWQru7hurkR5HzZ6RhpV93s1Kvd1LM8YhIibos9yLr4aO7bqJtXtGYPiaNscJHPSfP7FWlUvycWdFY/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「知识目录」和「知识正文」为什么必须分两级进上下文，以及各自的成本在哪。
2. 写出一份能通过校验的 `SKILL.md`，并知道 `description` 该怎么写模型才不会误触发。
3. 讲清 `load_skill` 为什么只收一个 `name` 而不收 `path`——这不是省事，是安全边界。
4. 复述一次加载的完整轨迹：目录进 System Prompt、正文进 tool result，两者永不互换位置。
5. 解释为什么扫描时不解码正文，而加载时要把所有路径**重新**校验一遍。
6. 指出父 Agent 和子 Agent 在 Skill 上的那处刻意不对称（子 Agent 有工具，但没有目录）。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| `ToolRegistry` 从 Zod schema 生成参数定义，并在调用前重校验 | 第 1、2 章 | `load_skill` 不额外维护一份 JSON Schema |
| workspace 硬边界与路径组件校验 | 第 2 章 | Skill 路径复用同一套 `isWindowsReservedComponent` |
| 权限策略与审计 | 第 3 章 | `load_skill` 是 `read`，不弹审批，但仍进审计 |
| Hook 四个事件 | 第 4 章 | `load_skill` 照样触发 `PreToolUse` / `PostToolUse` |
| `task` 委派与子 Agent 隔离 | 第 6 章 | 子 Agent 能用 `load_skill`，但看不到父目录 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| Skill | 一个目录 + 一份 `SKILL.md`，装一段可按需加载的规范或流程知识 |
| frontmatter | `SKILL.md` 开头被两行 `---` 夹住的 YAML 元数据 |
| 正文（body） | 第二个 `---` 之后的全部内容，也就是真正的指导文本 |
| 目录（catalog） | 所有 Skill 的「名称 + 单行描述」清单，进 System Prompt |
| 两级加载 | 目录常驻、正文按需——本章的核心设计 |
| `load_skill` | 本章新增的工具，输入只有一个 `name`，返回正文 |
| 快照（snapshot） | 构建 Agent 那一刻扫出来的不可变元数据，运行期不再变 |
| 路径复查 | 加载时用 `realpath` 把每一层目录重新解析一次，防扫描后被换链接 |
| byte 预算 | 目录上限按 UTF-8 实际字节算，不按字符串 `length` |
| 整条截断 | 预算不够时丢掉整个条目，绝不塞半行描述 |
| 路由（routing） | 模型读 `description` 决定「该不该加载这个 Skill」 |
| `Use when / Don't use for` | 让路由更准的描述写法：既给正例也给反例 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼一次真实的加载   ← 目录里有什么、正文什么时候才出现
  ↓
② 知识管理 ≠ 知识加载      ← 两级模型和它的真实成本
  ↓
③ 验收契约                ← 这章要钉住的六条
  ↓
④ 目录与清单契约          ← SKILL.md 怎么写才合法
  ↓
⑤ 扫描只读 frontmatter    ← 启动成本为什么不随正文增长
  ↓
⑥ load_skill 的输入与复查  ← 名字为什么不能换成路径
  ↓
⑦ 接入工具注册表          ← 父子差异那处不对称
  ↓
⑧ 变化对比 + 与官方差异    ← 相比第 6 章多了什么、和真实 Claude Code 差在哪
  ↓
⑨ 离线测试 + 运行 + 排错   ← 动手 + 实验 + 自测
```

---

## 先看一眼一次真实的加载

抽象说「两级加载」不如直接看一次运行。下面这段全部来自 `code/chapters/ch07/tests/ch07-skills.test.ts`，是断言过的真实数据，不是示意。

工作区里只有一个 Skill：

```text
<workspace>/
  skills/
    python-style/
      SKILL.md
```

`SKILL.md` 的内容是：

```text
---
name: python-style
description: Python project conventions
---
# Python Style

Use pathlib for filesystem paths.
```

Agent 构建完成、第一次请求模型时，模型收到的 System Prompt 是这样：

```text
You are a coding agent. Use tools when needed, inspect their results, and answer accurately.
For complex tasks, call todo_write with the complete task snapshot and update it when the plan changes.

Available workspace Skills are listed below. Load one with load_skill only when its instructions are relevant:
- **python-style**: Python project conventions
```

请把注意力放在**没有出现**的东西上：

| 出现在第一次请求里吗 | 内容 | 测试断言 |
| --- | --- | --- |
| ✅ 出现 | `python-style` | `expect(initialPrompt).toContain("python-style")` |
| ✅ 出现 | `Python project conventions` | `expect(initialPrompt).toContain("Python project conventions")` |
| ❌ 不出现 | `Use pathlib` | `expect(initialPrompt).not.toContain("Use pathlib")` |

也就是说：**模型知道有这个 Skill、知道它管什么，但一个字正文都没看到。**

模型于是主动调用 `load_skill`，父 history 里第 3 条消息（下标 2）被断言为：

```typescript
expect(result.history[2]).toEqual({
  role: "tool",
  content: "# Python Style\n\nUse pathlib for filesystem paths.\n",
  toolCallId: "skill-1",
});
```

三个细节值得停一下：

1. 返回的是**正文**，不是完整 `SKILL.md`。frontmatter 那四行没有进上下文——它是给扫描器看的，不是给模型看的。
2. 返回的不是**文件路径**。模型拿到的是内容，不需要再发一次 `read_file`。
3. `toolCallId` 和模型那次调用配对。这仍然是第 1 章那条不变量：每个 `tool_call` 必须有配对结果。

同一个测试还断言了模型看到的工具列表，顺序固定：

```typescript
expect(model.requests[0]?.tools.map((tool) => tool.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write", "task", "load_skill",
]);
```

8 个工具，`load_skill` 排在最后——因为组合根刻意把它注册在 `task` 之后。

### 加载正文需要人点「同意」吗

不需要。这个测试传进去的审批器是会抛异常的那个：

```typescript
class UnexpectedApproval implements ApprovalProvider {
  async decide(_request: PermissionRequest): Promise<PermissionDecision> {
    throw new Error("load_skill should not request approval");
  }
}
```

测试通过，说明这个 `decide()` 从来没被调用。但审计记录了：

```typescript
expect(audit.tools).toEqual(["load_skill"]);
```

**不弹审批 ≠ 不受管。** `load_skill` 的 effect class 是 `"read"`，默认规则不拦读操作，所以不弹框；但它照样走完整权限管线，照样留审计，照样触发第 4 章的 `PreToolUse` / `PostToolUse`。

### 子 Agent 加载 Skill 时看到什么

第三个集成测试让父 Agent 调 `task`，子 Agent 在自己的历史里调 `load_skill`。断言结果里有一条特别值得注意：

```typescript
expect(model.requests[1]?.tools.map((tool) => tool.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write", "load_skill",
]);
expect(model.requests[1]?.messages[0]?.content).not.toContain("python-style");
```

子 Agent 的工具列表里**有** `load_skill`（7 个工具，没有 `task`，递归仍然关着），但它的 System Prompt 里**没有**目录。「接入现有工具注册表」那一节会解释这处不对称为什么是刻意的。

---

## 知识管理和知识加载是两件事

新手最容易把这两件事混成一件：「我要让 Agent 知道这份规范」，于是把规范贴进 System Prompt。但这句话其实包含两个独立需求：

- **知识管理**：系统里一共有哪些规范可用？（模型必须知道，否则它连选都没得选）
- **知识加载**：这一次任务需要哪份规范的正文？（只有需要时才付上下文成本）

把它们合成一件事，代价就是每轮都为所有规范付费。第 7 章采用两级加载：

| 层级       | 进入上下文的内容                     | 注入时机                                | 默认上限                   |
| -------- | ---------------------------- | ----------------------------------- | ---------------------- |
| Skill 目录 | 名称 + 单行描述                    | 构建第 7 章 Agent 时进入 System Prompt     | 100 项、8000 UTF-8 bytes |
| Skill 正文 | `SKILL.md` frontmatter 之后的正文 | 模型显式调用 `load_skill` 后进入 tool result | 单次加载一个已注册 Skill        |

用一张图看清两级加载的时间线（假设有 3 个 Skill，每份正文 2000 行）：

```text
【全塞进 System Prompt 的做法】
请求 1  system: 规范A(2000行) + 规范B(2000行) + 规范C(2000行) + ...
请求 2  system: 规范A(2000行) + 规范B(2000行) + 规范C(2000行) + ...
请求 3  system: 规范A(2000行) + 规范B(2000行) + 规范C(2000行) + ...
        ↑ 每一轮都带 6000 行，其中至少 4000 行和当前任务无关

【第 7 章的两级加载】
请求 1  system: 目录(3 行) ← 模型只知道「有 A/B/C，各管什么」
  ↓ 模型判断这次要 B，调用 load_skill("规范B")
请求 2  system: 目录(3 行) + history 里多了 规范B 的 2000 行正文
请求 3  system: 目录(3 行) + history 里仍有 规范B 的 2000 行正文
        ↑ 规范A 和 规范C 的正文从头到尾没进过上下文
```

目录让模型能做选择，正文只在真正需要时付出上下文成本。目录按名称排序，并按完整条目截断。它不会为了凑预算，把半行描述塞进 Prompt。

本章的目录是构建 Agent 时得到的快照。运行期间新增 Skill，要重建 Agent 才会进入目录；已注册 Skill 的正文则会在每次 `load_skill` 时重新读取和校验。这一点有测试直接钉住：

```typescript
const registry = SkillRegistry.scan(workspace);   // 先扫描（此时还没有 Skill）
await writeSkill(workspace, "alpha", { body: "first body" });
expect(registry.names).toEqual([]);               // ← 新增的 Skill 不会进旧快照

const refreshed = SkillRegistry.scan(workspace);  // 重新扫描才看得见
// 之后把 SKILL.md 的 description 和正文都改掉……
expect(refreshed.renderCatalog()).toBe("- **alpha**: A test skill.");  // 目录还是旧描述
await expect(refreshed.loadSkill("alpha")).resolves.toBe("second body"); // 正文却是新的
```

同一个注册表里，**目录是冻结的，正文是现读的**。这不是 bug，是刻意的取舍：目录必须稳定才能复用 KV Cache，正文必须现读才能保证你改完文件立刻生效。直到第 10 章，System Prompt 才会由动态 provider 按轮渲染。

这里需要澄清一个容易误解的点：目录是稳定前缀，因此可以复用 KV Cache。但“对 KV Cache 友好”不等于“零成本”。会话开始时，目录本身仍要完成一次 prefill；之后它固定在 System Prompt 的原位置，后续轮次可以按缓存读取。第一次加载某个 Skill 正文时，正文也要先 prefill，之后才作为会话历史的一部分复用缓存。收益来自两点：启动时不需要把全部 Skill 正文读进 Prompt；后续也不会因为加载新 Skill 而改写已经缓存的前缀。

---

## 第 7 章的验收契约

先把这一章要钉住的结论列出来。后面每一节都在解释其中一条，「用离线测试证明行为」那一节会逐条验证。

| # | 契约 | 反面是什么 |
| ---: | --- | --- |
| 1 | 目录只含名称和单行描述，**永不**含正文 | 启动就把全部规范塞进 Prompt |
| 2 | 目录按名称排序，两次扫描结果逐字节一致 | 目录顺序随文件系统返回顺序抖动，KV Cache 全废 |
| 3 | 预算按 UTF-8 byte 算，超限丢整条 | 用字符串 `length` 冒充 bytes；截出半行描述 |
| 4 | 扫描只解码到第二个 `---`，正文不解码 | 启动成本随正文总量线性增长 |
| 5 | `load_skill` 只收 `name`，多一个字段就拒 | 收 `path`，让模型指定任意文件 |
| 6 | 加载时每一层路径重新 `realpath` 复查 | 相信扫描时存下的路径，扫描后被换链接就逃逸了 |

---
## Skill 的目录和清单契约

默认目录是当前工作区下的 `skills/`。扫描只看它的一级子目录，每个 Skill 必须有一个 `SKILL.md`：

```text
skills/
  typescript-style/
    SKILL.md
  sql-style/
    SKILL.md
```

一个最小清单如下：

```yaml
---
name: typescript-style
description: Use when 编写或审查 TypeScript 类型、模块、导入和运行时校验；Don't use for SQL、部署或通用项目规划。
---
# TypeScript Style

外部输入先按 unknown 处理，通过运行时校验后再进入业务逻辑。
```

frontmatter 不是“能解析就行”，而是有明确边界：

- 第一行必须是 `---`，并且必须有结束分隔符。
- `name` 和 `description` 都必须存在且类型为字符串。
- `description` 去除首尾空白后不能为空，也不能跨行。
- `name` 最多 64 个字符，只允许小写字母、数字和单个连字符分段，例如 `typescript-style`。
- `name` 必须与目录名完全一致；`nul`、`com1`、`lpt9` 等 Windows 设备名不是合法 Skill 名。
- 重复名称直接使扫描失败，不会让后读到的条目静默覆盖前一个。
- frontmatter 和显式加载的完整文件必须是 UTF-8。

这些规则违反后会得到什么，一张表对齐（错误类型来自 `features/skills.ts`）：

| 你写成这样 | 什么时候失败 | 抛出的错误类型 |
| --- | --- | --- |
| 第一行不是 `---` | 扫描时 | `SkillManifestError`：must begin with YAML frontmatter |
| 只有开头 `---`，没有结束 `---` | 扫描时 | `SkillManifestError`：has no closing frontmatter delimiter |
| frontmatter 里 YAML 语法错 | 扫描时 | `SkillManifestError`：is not valid YAML |
| frontmatter 是数组或标量，不是键值对 | 扫描时 | `SkillManifestError`：must be a mapping |
| 缺 `name` 或缺 `description` | 扫描时 | `SkillManifestError`：requires string name and description |
| `description` 是空串或写成多行 | 扫描时 | `SkillManifestError`：must be one non-empty line |
| `name: My_Skill`（大写、下划线） | 扫描时 | `SkillManifestError`：contains an invalid name |
| `name` 和目录名不一致 | 扫描时 | `SkillManifestError`：must match its directory |
| 两个目录用了同一个 `name` | 扫描时 | `DuplicateSkillError` |
| 正文里有非 UTF-8 字节 | **加载时**（扫描能过！） | `SkillManifestError`：is not valid UTF-8 |
| Skill 目录是指向工作区外的链接 | 扫描时 | `SkillPathError`：escapes Skills root |

注意最后两行的时机差异，它直接来自「扫描只读 frontmatter」这个设计：扫描器根本没解码正文，所以正文里的坏字节它发现不了；等到 `load_skill` 真的读全文，才会失败。这不是漏洞，是「启动成本不随正文增长」的必然代价，而且失败点仍然在正文进入上下文**之前**。

还有两种情况是**静默跳过**，不算错误：

- `skills/` 下的一级子目录里没有 `SKILL.md` → 跳过，不报错（你可以在那里放草稿）。
- `skills/` 下直接放了一个文件（不是目录）→ 跳过，只扫一级子目录。

`description` 不是“功能简介”，而是模型做路由判断的条件。`ai-agent-book` 第 2 章对照了 Anthropic 的 Agent Skills 实践：最有效的写法是 `Use when / Don't use when`，并明确给出反例。缺少反例时，宽泛描述容易在不相关任务上误触发，路由准确率会明显下降。所以示例里的“编写或审查 TypeScript 类型、模块、导入和运行时校验”是正例，“SQL、部署或通用项目规划”是反例。

对照一下三种写法的差别：

| 写法 | 例子 | 模型会怎么用 |
| --- | --- | --- |
| ❌ 太笼统 | `description: 代码规范` | 几乎任何任务都可能触发；也可能因为看不出适用范围而永不触发 |
| ⚠️ 只有正例 | `description: Use when 编写 TypeScript` | 写 SQL 时也可能顺手加载，浪费上下文 |
| ✅ 正例 + 反例 | `description: Use when 编写或审查 TypeScript 类型、模块、导入和运行时校验；Don't use for SQL、部署或通用项目规划。` | 路由边界清晰，误触发明显下降 |

一句话记法：**`description` 是写给「要不要打开这本书」的判断用的，不是写给「这本书讲什么」的介绍用的。**

这条保留名检查不是为 YAML 单独写的一套宽松规则，而是复用文件系统路径组件校验。`isWindowsReservedComponent` 放在 `core/filesystem.ts`，文件 adapter 和 Skill 扫描使用同一实现，避免两套边界慢慢漂移。

这套约束的价值不在 YAML 本身，而在于让名称、目录和加载目标形成一个稳定映射。模型不能通过 `name` 参数直接指定任意文件路径。

---

## 扫描阶段只读 frontmatter

旧做法常见的一个问题是：启动扫描时直接 `readFileSync()` 读取并解码每份完整正文，再把原文存进全局 `Map`。虽然这样不会立即注入 Prompt，但启动成本仍然和正文总量绑定。

当前 `SkillRegistry.scan()` 只读取到 frontmatter 的结束分隔符，建立不可变元数据快照。正文不会在扫描时解码：

```typescript
import { SkillRegistry } from "./chapters/ch07/src/features/skills.js";

const workspace = process.cwd();
const skills = SkillRegistry.scan(workspace, {
  maxCatalogEntries: 100,
  maxCatalogBytes: 8_000,
});
const catalog = skills.renderCatalog();
```

`readFrontmatter()` 的读取过程可以这样理解——它按 4 KiB 分块读，逐行判断，**一遇到第二个 `---` 就 return**：

```text
SKILL.md 文件（假设正文 2000 行）
┌──────────────────────────────┐
│ ---                          │ ← 第 1 行分隔符
│ name: typescript-style       │ 
│ description: Use when ...    │  ← 这三行会被解码成字符串
│ ---                          │ ← 第 2 行分隔符：立刻 return，不再往下读
├──────────────────────────────┤
│ # TypeScript Style           │ ╮
│                              │ ├─ 这 2000 行既没读进内存，
│ 外部输入先按 unknown 处理…    │ │   也没解码成字符串
│ …（还有 1997 行）             │ ╯
└──────────────────────────────┘
```

所以启动成本只和「Skill 个数 × frontmatter 大小」有关，和正文总量无关。你往 `SKILL.md` 里加一万行正文，扫描时间几乎不变。

目录渲染结果类似：

```text
- **typescript-style**: Use when 编写或审查 TypeScript 类型、模块、导入和运行时校验；Don't use for SQL、部署或通用项目规划
- **sql-style**: SQL 编写规范与查询约束
```

预算按 `Buffer.byteLength(catalog, "utf8")` 的实际 byte 数计算，不拿 JavaScript 字符串的 `length` 冒充 bytes。中文通常占多个 UTF-8 bytes，因此这个区别会直接影响目录能否稳定满足模型输入预算。

这个区别有多大？`"中文说明"` 的 `.length` 是 4，但 `Buffer.byteLength(..., "utf8")` 是 12。如果拿 `length` 当预算，一份中文目录会超出你以为的 3 倍。测试直接把预算卡在「刚好装一条」的位置来验证：

```typescript
await writeSkill(workspace, "alpha", { description: "中文说明" });
await writeSkill(workspace, "beta", { description: "short" });
const firstLine = "- **alpha**: 中文说明";

const registry = SkillRegistry.scan(workspace, {
  maxCatalogEntries: 10,
  maxCatalogBytes: Buffer.byteLength(firstLine, "utf8"),  // 预算 = 恰好第一条的字节数
});

expect(registry.renderCatalog()).toBe(firstLine);   // 只出第一条
```

注意 `beta` 的描述只有 5 个字节，看起来「还塞得下」，但预算已经用光了——`boundedCatalog()` 计算下一条时会额外算上条目之间那个换行符（`separatorBytes`），一旦 `usedBytes + entryBytes > maxBytes` 就整体 `break`。它不会跳过 `alpha` 去凑一个更小的 `beta`，也不会把 `beta` 截成半行。

排序同样是钉住的。三个 Skill 按 `zeta`、`alpha`、`beta` 的顺序写入磁盘，扫描结果是：

```typescript
expect(registry.names).toEqual(["alpha", "beta", "zeta"]);           // 全部注册，按字典序
expect(registry.catalogEntries.map((e) => e.name)).toEqual(["alpha", "beta"]);  // 目录只放 2 条
expect(secondScan.renderCatalog()).toBe(registry.renderCatalog());   // 两次扫描逐字节一致
expect(registry.renderCatalog()).not.toContain("PRIVATE");           // 正文关键词不出现
```

`names`（全部已注册）和 `catalogEntries`（进 Prompt 的那些）是两个不同的东西：**超出预算的 Skill 仍然可以被 `load_skill` 加载，只是模型在目录里看不到它。** 这是刻意的——预算只约束 Prompt 大小，不缩小注册表。

如果 `skills/` 不存在，注册表返回空目录，`bootstrap.ts` 会在 Prompt 中明确写出 `(No workspace Skills are currently available.)`。这句话本身也有测试：模型第一次请求的 System Prompt 里必须包含它。为什么要专门说一句「没有」，而不是什么都不写？因为空着会让模型自己猜——猜某个 Skill 存在，然后调用 `load_skill` 得到 `skill_not_found`，白白浪费一轮。

如果 Skill 根目录、子目录或 `SKILL.md` 通过符号链接或 Windows junction 逃出受控边界，扫描直接失败。

---

## load_skill：按名称加载正文

`load_skill` 的工具输入只有一个字段：

```json
{
  "name": "typescript-style"
}
```

它不接受 `path`，也不接受额外字段。Zod 的 `.strict()` 会在 handler 运行前拒绝多余字段。`../secret` 会在参数校验阶段得到 `invalid_arguments`；格式合法但未注册的名称，得到 `skill_not_found`。

为什么不给模型一个 `path` 参数？因为那等于把「读任意文件」的能力换了个名字重新开一遍。第 2 章的 `read_file` 已经有完整的路径边界，Skill 不需要再造一个入口。**`name` 到文件的映射由 Harness 决定，不由模型决定**——这是本章最重要的一条安全设计。

输入 schema 和工具定义保持在同一个 feature 内（`features/skills.ts` 完整代码）：

```typescript
const SKILL_NAME_REGEXP = /^[a-z0-9]+(?:-[a-z0-9]+)*$/u;

const loadSkillInputSchema = z
  .object({
    name: z
      .string()
      .min(1)
      .max(MAX_SKILL_NAME_LENGTH)          // 64
      .regex(SKILL_NAME_REGEXP)            // 只允许小写字母数字和单连字符分段
      .refine((name) => !isWindowsReservedComponent(name))  // 拒绝 nul / com1 / lpt9
      .describe("The exact Skill name from the available catalog."),
  })
  .strict();                               // 多一个字段就整体拒绝

this.toolDefinition = Object.freeze({
  name: LOAD_SKILL_TOOL_NAME,
  description: "Load the full instructions for one Skill listed in the workspace catalog.",
  inputSchema: loadSkillInputSchema,
  effect: "read",
  handler: (input: LoadSkillInput, context: ToolContext) => this.#handleLoad(input, context),
});
```

这里没有额外维护一份 JSON Schema。第 1 章的 `ToolRegistry` 会从这份 Zod schema 生成发给模型的参数定义，测试连生成结果都断言了：

```typescript
expect(tools.openAITools()[0]?.function.parameters).toMatchObject({
  type: "object",
  additionalProperties: false,                                  // ← .strict() 的产物
  required: ["name"],
  properties: {
    name: { type: "string", pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$" },  // ← regex 的产物
  },
});
```

也就是说，模型在**看到工具定义的那一刻**就知道 `name` 的合法格式，而不是靠试错。真正调用 handler 前，`ToolRegistry` 还会用同一份 schema 再校验一次参数——一份 schema，两处生效。

四种越界输入分别得到什么，测试逐条钉住：

| 模型发来的参数 | 结果 | 拦在哪一层 |
| --- | --- | --- |
| `{"name":"missing"}` | `skill_not_found` | handler（格式合法，只是没注册） |
| `{"name":"../secret"}` | `invalid_arguments` | **schema**（regex 不允许 `.` 和 `/`） |
| `{"name":"known","path":"secret"}` | `invalid_arguments` | **schema**（`.strict()` 拒绝多余字段） |
| `{"name":"nul"}` | `invalid_arguments` | **schema**（`.refine()` 拒绝 Windows 设备名） |

注意后三行的共同点：**它们连 handler 都没进，一次文件访问都没发生。** 校验发生在碰磁盘之前，这是「先拒绝，再执行」而不是「先执行，再检查」。

加载时，注册表不会盲信启动阶段保存的信息。它会重新完成以下检查：

1. Skill 目录解析后仍位于已扫描的 Skill 根目录内。
2. `SKILL.md` 解析后的目标仍位于对应 Skill 目录内，而且目标是文件。
3. 完整文件可以按 UTF-8 解码，frontmatter 仍然有效。
4. 当前 `name` 仍与请求名称和目录名称一致。

为什么扫描时校验过了还要再来一遍？因为两次校验之间隔着**任意长的时间**，而文件系统是可变的：

```text
构建 Agent（扫描）          ……运行了 20 分钟……          模型调用 load_skill
      ↓                                                        ↓
skills/alpha/ 是正常目录                            skills/alpha/ 已被换成
校验通过，记下路径                                  指向 C:\Windows 的 junction
                                                             ↓
                                          如果直接用扫描时记的路径 → 逃逸成功
                                          重新 realpath 复查    → SkillPathError
```

这个场景有专门的测试：`rechecks path containment when a registered directory becomes an escape link`——先正常扫描，然后把已注册的目录替换成逃逸链接，再调用 `loadSkill()`，断言它抛 `SkillPathError`。

全部通过后，返回的是 frontmatter 后面的正文，不是完整 `SKILL.md`，也不是文件路径：

```text
# TypeScript Style

外部输入先按 unknown 处理，通过运行时校验后再进入业务逻辑。
```

这个结果由公共 Loop 写成与 `load_skill` 调用 ID 配对的 `tool` 消息。下一轮模型请求会看到正文；加载之前的请求只看到目录。

`#handleLoad()` 把领域异常映射成对模型稳定的错误码，一共五种：

| errorCode | 触发条件 | 模型看到的文本 |
| --- | --- | --- |
| `skill_not_found` | 名称合法但没注册 | Requested Skill is not registered |
| `skill_path_escape` | 加载时路径复查失败 | Registered Skill path is no longer safe |
| `invalid_skill` | 加载时 frontmatter 失效或非 UTF-8 | Registered Skill manifest is invalid |
| `skill_workspace_mismatch` | `ToolContext.workspace` 和注册表不是同一个 | Skill registry belongs to a different workspace |
| `skill_workspace_error` | `ToolContext.workspace` 根本解析不出来 | Current workspace could not be resolved |

注意错误文本里**没有任何内部文件路径**。异常里有（`SkillPathError` 的 message 带 source），但映射到工具结果时被丢掉了。原因和第 3 章一致：错误信息本身也是进上下文的内容，不该把宿主机目录结构喂给模型。

`load_skill` 的 effect class 是 `"read"`，但它仍会经过第 4 章建立的 Hook 与第 3 章建立的权限管线。Skill 正文只是给模型的任务指导。它不能放宽硬权限，也不能把工作区外写入变成允许操作。

---

## 接入现有工具注册表

完整实现位于 `code/chapters/ch07/src/features/skills.ts`。`SkillRegistry` 自己持有 `load_skill` 的 `ToolDefinition`，schema 和 handler 仍来自同一个定义，不维护第二张分发表。

`code/chapters/ch07/src/bootstrap.ts` 在 P07 构建时完成三件事：

```typescript
const skillRegistry = profile.capabilities.has("skills")
  ? SkillRegistry.scan(dependencies.workspace)
  : undefined;

if (skillRegistry !== undefined) {
  tools.register(skillRegistry.toolDefinition);
}
```

第一步扫描工作区并建立快照，第二步把 `load_skill` 注册到父工具集，第三步把有界目录追加到本章 System Prompt。注册顺序刻意放在 `task` 之后，所以 P07 的父工具顺序稳定为：

```text
shell, read_file, write_file, edit_file, glob, todo_write, task, load_skill
```

因为 P07 继承 P06，父 Agent 和一次性子 Agent 都能调用 `load_skill`。创建子工具集时，组合根把同一注册表的工具定义加进去：

```typescript
toolsFactory: () => {
  const childTools = createStandardTools(profile, commandRunner, fileSystem).tools;
  if (skillRegistry !== undefined) {
    childTools.register(skillRegistry.toolDefinition);
  }
  return childTools;
},
```

子工具列表仍然不含 `task`，所以增加知识加载不会重新打开递归委派。父子共享的是可用 Skill 的元数据快照和同一套路径边界，不共享对话历史。子 Agent 加载到的正文只进入子历史，父历史最终只得到 `task` 的结论。

但这里有一处刻意的不对称，和第 6 章 `todo_write` 那处同源：

| | 父 Agent | 子 Agent |
| --- | :---: | :---: |
| 工具列表里有 `load_skill` | ✅ | ✅ |
| System Prompt 里有 Skill 目录 | ✅ | ❌ |
| 能加载已注册的任意 Skill | ✅ | ✅ |
| 知道有哪些 Skill 可加载 | ✅ | ❌（除非父 Agent 在 description 里告诉它） |

原因在于 `SubagentTool` 用的是固定的 `DEFAULT_SUBAGENT_SYSTEM_PROMPT`，组合根只往**父** Agent 的 System Prompt 里拼目录。测试直接断言了这一点：

```typescript
expect(model.requests[1]?.messages[0]?.content).not.toContain("python-style");
```

这不是遗漏，而是第 6 章「description 必须自包含」那条规则的自然结果：**你要子 Agent 用哪个 Skill，就在 `task` 的 description 里写清它的名字。** 反过来说，如果你希望子 Agent 自己挑，就得把目录也拼进子 prompt——那是第 10 章动态 System Prompt 的活儿，本章不做。

章节入口 `code/chapters/ch07/src/chapters/ch07.ts` 仍然只有 `runProfile(P07, process.argv.slice(2))`。Skill 接入没有复制或改写公共 Agent Loop。

`features/skills.ts` 的核心实现要点可以拆成几个明确职责：

| 函数                                                       | 职责与边界                                                                                |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| `SkillRegistry.scan()`                                   | 解析 workspace 与 `skills/` 真实路径，扫描一级子目录，只读 frontmatter，建立名称、目录、manifest 的不可变快照；重复名直接失败 |
| `readFrontmatter()`                                      | 按 4 KiB 分块读文件，只在遇到第二个 `---` 时返回 frontmatter 字节，不提前解码正文                               |
| `parseSkillDocument()`                                   | 校验 frontmatter 分隔符、YAML 类型、`name` 规则、单行非空 `description`，并切出正文                        |
| `boundedCatalog()`                                       | 按条目数和 `Buffer.byteLength` 双重预算生成目录，超限即整体截断                                           |
| `resolveSkillRoot()`                                     | 只允许相对路径、拒绝父级和 Windows 保留组件，保证目录解析后仍在 workspace 内                                     |
| `checkedRealDirectoryAsync()` / `checkedRealFileAsync()` | 加载时用异步 `realpath` 重查每一层物理路径，阻止扫描后链接替换逃逸                                              |
| `loadSkill()`                                            | 重新校验请求名、目录名、manifest 与 UTF-8，返回 frontmatter 之后的正文字符串                                 |
| `#handleLoad()`                                          | 先确认工具上下文 workspace 与注册表一致，再把领域错误映射成稳定 `errorCode`                                    |

这些边界共同回答一个问题：目录让模型“知道有什么”，正文让模型“用到什么时再读”，而路径校验让“名称到文件”的映射不能成为越权入口。

---

## 相比第 6 章，变化有多大

| 组件 | 第 6 章 | 第 7 章 |
| --- | --- | --- |
| 父工具 | 7 个 | 8 个，新增 `load_skill` |
| Skill 目录 | 无 | 排序、有条目数与 UTF-8 byte 上限 |
| Skill 正文 | 无 | 显式加载后进入匹配的 tool result |
| 路径输入 | 不适用 | 模型只能给安全 Skill 名，不能给路径 |
| 子 Agent | 无 `task` | 继续无 `task`，但可使用 `load_skill` |
| 公共 Loop | `AgentRunner` | 仍是同一个 `AgentRunner` |

一句话总结这张表：**新增的是一条按需加载通道，不是一套新的执行机制。** 完整的「还没做什么 / 哪一章补」在「这章没有做什么」那一节。

## 与 Claude Code 的差异

本章的 `load_skill` 是教学子集。Claude Code 的 Skill 系统也在同样的两级加载框架下设计（官方文档：https://code.claude.com/docs/en/skills，参考外部仓库 [`shareAI-lab/learn-claude-code`](https://github.com/shareAI-lab/learn-claude-code) 的 `s07_skill_loading/README.md`）。两者的主要差异如下：

- 技能加载来源不同。Claude Code 从多个来源加载技能，包括用户技能（`~/.claude/skills/`）、项目技能（`.claude/skills/`）、`--add-dir` 目录、Legacy commands、内置技能和 MCP 技能。本章只扫描当前工作区下一个 `skills/` 目录。多来源会增加来源优先级、去重和权限管理的复杂度，这部分留给读者按需落地。
- Frontmatter 字段不同。Claude Code 的 SKILL.md 支持 `allowed-tools`（自动允许的工具列表）、`context`（`inline` 或 `fork`——fork 模式让技能作为子 Agent 运行）、`model`（模型覆盖）、`hooks`、`paths`（条件激活的 glob 模式）等字段。其中 `context: fork` 需要子 Agent 运行时（第 6 章 `task` 的基础）和上层编排逻辑，因此本章刻意省略。
- 工具输入不同。Claude Code 的 Skill 工具接收 `skill` + `args`，教学版简化为 `name`，避免了参数解析和转发的额外复杂度。
- 内容注入方式不同。Claude Code 通过 `getPromptForCommand()` 展开 SKILL.md，再经 `SkillTool` 以 `newMessages` 注入对话（tool_result 只显示 `Launching skill: {name}`）。教学版把正文直接放在 tool_result 中。最终效果等价：正文进入对话历史，但不占用 System Prompt。
- 停止条件一致。inline 技能受主 Agent 的 loop 控制。本章的 `load_skill` 只是按需注入知识，不改变工具箱或结束条件。

注意事项：本章不实现 `context: fork`，所有技能在父 Agent 历史内展开；也不实现 `allowed-tools`，技能加载不会自动调整模型可见的工具列表。如果需要，第 6 章的 `task` 和第 7 章的 `load_skill` 可以在上层组合，但这不属于本章的内容边界。

---

## 用离线测试证明行为

`npm run test:ch07` 会把第 1 到 7 章积累的测试全跑一遍。当前结果：

```text
 Test Files  18 passed (18)
      Tests  143 passed (143)
```

其中和本章直接相关的是两个文件：

| 文件 | 用例数 | 管什么 |
| --- | ---: | --- |
| `chapters/ch07/tests/skills.test.ts` | 14 | `SkillRegistry` 的扫描、预算、路径与错误码 |
| `chapters/ch07/tests/ch07-skills.test.ts` | 3 | 装完整 Agent 跑一遍，看目录和正文分别落在哪 |

14 个单元用例的名字本身就是一份契约清单：

```text
an absent Skills directory produces an empty catalog
the catalog is a snapshot while registered bodies are reread
catalog limits must be positive integers
scans a stable bounded catalog without decoding or exposing bodies
scan reads only frontmatter while explicit loading validates the full UTF-8 document
counts UTF-8 bytes and never emits a partial catalog entry
load_skill returns only a registered body after explicit tool invocation
rejects unknown, traversal, reserved, and extra load arguments before file access
rejects malformed frontmatter, unsafe names, and duplicate names
requires the manifest name to match its directory
load_skill rejects a ToolContext from another workspace
load_skill reports an unresolvable ToolContext workspace
rejects unsafe skill roots and directory links that escape the workspace
rechecks path containment when a registered directory becomes an escape link
```

对着验收契约逐条核对：

| 验收契约 | 由哪个用例钉住 |
| --- | --- |
| 1 目录永不含正文 | `scans a stable bounded catalog without decoding or exposing bodies`（断言 `not.toContain("PRIVATE")`） |
| 2 排序稳定、两次扫描一致 | 同上（断言 `secondScan.renderCatalog()` 等于第一次） |
| 3 byte 预算、整条截断 | `counts UTF-8 bytes and never emits a partial catalog entry` |
| 4 扫描不解码正文 | `scan reads only frontmatter while explicit loading validates the full UTF-8 document` |
| 5 只收 `name`，多字段就拒 | `rejects unknown, traversal, reserved, and extra load arguments before file access` |
| 6 加载时重新复查路径 | `rechecks path containment when a registered directory becomes an escape link` |

另外三个集成用例分别覆盖：空工作区的提示语、目录/正文的落点与审计记录、子 Agent 能加载但不能递归委派。`profiles.test.ts` 顺带钉住 `ch07` 固定映射到 `P07`。

第 7 章设计成两级验证：

| 指标类型 | 离线测试覆盖                       | 验证边界                  |
| ---- | ---------------------------- | --------------------- |
| 机制指标 | 目录可发现性、描述路由条件、正文进入轨迹、路径与权限边界 | 确定性、可离线重复             |
| 目标指标 | 真实模型是否主动加载 Skill、加载后是否遵循正文   | 需要真实模型轨迹与人工验收，离线测试不承诺 |

因此，“测试全部通过”只能证明 Harness 让 Skill 可以被发现、被安全加载。它不能证明某个真实模型一定会加载 Skill，也不能证明模型加载后会完整遵循正文。后两类问题应当用真实运行轨迹单独评估，例如记录 `load_skill` 的激活时机、后续动作是否按正文执行，以及任务最终是否成功。

---

## 运行第 7 章

### 第 0 步：准备环境

```powershell
Set-Location 'F:\笔记\Agent实操\code'
node --version      # 需要 Node 20 以上
npm install         # 只在第一次或依赖变动后执行
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch07
```

期望看到 `Test Files 18 passed (18)` 和 `Tests 143 passed (143)`。

输出中间会出现一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这不是报错。** 它是 `config.test.ts` 主动断言的一条预期输出——测试在验证「缺少配置时必须明确报错而不是静默降级」。只要最后一行是 `passed`，就一切正常。

### 第 2 步：确认工作区里有 Skill

仓库自带一个最小示例，先看一眼：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Get-Content -LiteralPath '.\skills\typescript-style\SKILL.md'
```

如果这个文件不存在，用下面命令创建（PowerShell 7 以上）：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\skills\typescript-style' | Out-Null
@'
---
name: typescript-style
description: Use when 编写或审查 TypeScript 类型、模块、导入和运行时校验；Don't use for SQL、部署或通用项目规划。
---
# TypeScript Style

外部输入先按 unknown 处理，通过运行时校验后再进入业务逻辑。
'@ | Set-Content -LiteralPath '.\skills\typescript-style\SKILL.md' -Encoding utf8NoBOM
```

PowerShell 5.1 没有 `utf8NoBOM`，用 `-Encoding utf8` 也能通过——BOM 会被解码器自动剥掉，不影响 frontmatter 判定。但如果用 GBK 之类的编码保存正文，`load_skill` 会在加载时报 `invalid_skill`。

### 第 3 步：观察目录与正文分别在哪一轮出现

这一步需要真实模型，先确认 `code\.env` 里配好了 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch07 -- --prompt "先调用 load_skill 加载 typescript-style，再总结其中两条约定"
```

Hook 日志会打印出这样的顺序（`[Hook]` 行来自 `cli.ts` 的 `liveHooks()`，写到 stderr）：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: load_skill
[Permission] load_skill: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: load_skill -> ok
[Hook] Stop
```

注意 `Permission` 那行：`load_skill` 走了权限管线，得到 `allow (default)`，**没有弹审批**。这和「加载正文需要人点『同意』吗」那一小节的 `UnexpectedApproval` 测试是同一件事的两种证据。

真实模型是否按你的指示调用工具具有不确定性。如果它直接开始瞎猜规范内容，说明它没有加载——这属于「目标指标」，正是「用离线测试证明行为」那一节说离线测试不承诺的部分。

### 第 4 步：让子 Agent 去加载

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 7 --prompt "调用 task，让子 Agent 加载 typescript-style 并检查当前项目是否遵守它"
```

因为子 Agent 的 System Prompt 里没有目录，prompt 里必须**把 Skill 名字写出来**。这就是「接入现有工具注册表」那一节那处不对称在实际使用中的样子。

日志里会看到两次 `UserPromptSubmit` 和两次 `Stop`（父子各一次），中间夹着子 Agent 的 `load_skill`：

```text
[Hook] UserPromptSubmit                  ← 父 Agent
[Hook] PreToolUse: task
[Hook] UserPromptSubmit                  ← ★ 子 Agent 开始
[Hook] PreToolUse: load_skill            ← ★ 子 Agent 在加载
[Hook] PostToolUse: load_skill -> ok
[Hook] Stop                              ← ★ 子 Agent 结束
[Hook] PostToolUse: task -> ok
[Hook] Stop                              ← 父 Agent 结束
```

### 第 5 步（可选）：完整门禁

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run lint
```

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...`（出现在 `npm run test:ch07` 输出中间） | 这是 `config.test.ts` 断言的预期输出 | 不用管，看最后一行是否 `passed` |
| `配置错误: Missing required settings: ...`（出现在 `npm run ch07` 之后直接退出） | `code\.env` 没配 | 复制 `.env.example` 为 `.env` 并填三个值 |
| `Skill manifest must begin with YAML frontmatter` | 文件第一行不是 `---`（常见于第一行有空行） | 删掉开头空行；BOM 不是原因 |
| `Skill manifest has no closing frontmatter delimiter` | 只写了开头的 `---` | 补上第二行 `---` |
| `Skill description must be one non-empty line` | `description` 写成了多行（比如用了 YAML 的 `\|`） | 压成一行 |
| `Skill frontmatter contains an invalid name` | `name` 用了大写、下划线或点 | 改成 `小写字母-数字` 分段，如 `my-skill` |
| `Skill name must match its directory: xxx` | `name` 和目录名不一致 | 二者改成同一个字符串 |
| `Duplicate Skill name: xxx` | 两个目录声明了同一个 `name` | 改掉一个，或删掉多余目录 |
| 模型说「没有可用的 Skill」 | 目录被 byte 预算整条截断，或 Skill 是扫描之后才加的 | 缩短 `description`；新增 Skill 后要重启进程 |
| tool result 里出现 `skill_not_found` | 名称格式合法但没注册（拼错、目录没建对） | 用第 2 步的命令确认目录结构 |
| tool result 里出现 `invalid_arguments` | 模型给了路径、多余字段或设备名 | 正常拦截，不用改代码；看模型是否被 prompt 误导 |
| tool result 里出现 `invalid_skill` | 加载时正文不是 UTF-8，或 frontmatter 已失效 | 用 UTF-8 重存 `SKILL.md` |
| tool result 里出现 `skill_path_escape` | 扫描后 Skill 目录被换成了逃逸链接 | 检查 `skills/` 下有没有符号链接或 junction |

### 四个建议动手做的小实验

这四个实验都只读不写、不调真实模型。先建一个临时脚本目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\scratch' | Out-Null
```

**实验一：证明目录里真的没有正文，并看清 byte 数**

把下面内容存成 `code\scratch\skill-probe.ts`：

```typescript
import { SkillRegistry } from "../chapters/ch07/src/features/skills.js";

const workspace = process.cwd();
const full = SkillRegistry.scan(workspace);
console.log("names   =", full.names);
console.log("catalog =", JSON.stringify(full.renderCatalog()));
console.log("bytes   =", Buffer.byteLength(full.renderCatalog(), "utf8"));
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\skill-probe.ts
```

实际输出（仓库自带那个 Skill）：

```text
names   = [ 'typescript-style' ]
catalog = "- **typescript-style**: Use when 编写或审查 TypeScript 类型、模块、导入和运行时校验；Don't use for SQL、部署或通用项目规划。"
bytes   = 155
```

一条中文描述占了 155 bytes，但这个字符串的 `.length` 只有 93——差了 62。默认预算 8000 bytes 大约能装 51 条这样的描述，如果按 `.length` 算就会误以为能装 86 条。这就是「按 byte 算」的现实意义。

**实验二：把预算压到 10 bytes，看目录和注册表的区别**

在同一个脚本末尾追加：

```typescript
const tiny = SkillRegistry.scan(workspace, { maxCatalogBytes: 10 });
console.log("tiny names   =", tiny.names);
console.log("tiny catalog =", JSON.stringify(tiny.renderCatalog()));
console.log("tiny load    =", JSON.stringify(await tiny.loadSkill("typescript-style")));
```

实际输出：

```text
tiny names   = [ 'typescript-style' ]
tiny catalog = ""
tiny load    = "# TypeScript Style\r\n\r\n外部输入先按 unknown 处理，通过运行时校验后再进入业务逻辑。"
```

三个结论一次看清：目录被**整条**丢掉（不是截半行）；注册表里 Skill 还在；`load_skill` 照样能加载。**预算只约束 System Prompt 的大小，不缩小注册表。** 顺带注意正文里的 `\r\n` 被原样保留——正文是逐字节返回的，不做换行归一化。

**实验三：制造五种坏 frontmatter，看失败时机**

存成 `code\scratch\skill-fail.ts`：

```typescript
import { mkdirSync, mkdtempSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { SkillRegistry } from "../chapters/ch07/src/features/skills.js";

function probe(label: string, manifest: string, dir = "demo"): void {
  const workspace = mkdtempSync(join(tmpdir(), "fail-"));
  mkdirSync(join(workspace, "skills", dir), { recursive: true });
  writeFileSync(join(workspace, "skills", dir, "SKILL.md"), manifest, "utf8");
  try {
    const registry = SkillRegistry.scan(workspace);
    console.log(`${label} -> OK, catalog=${JSON.stringify(registry.renderCatalog())}`);
  } catch (error) {
    console.log(`${label} -> ${(error as Error).constructor.name}`);
  }
}

probe("description 两行", "---\nname: demo\ndescription: |\n  first\n  second\n---\nbody\n");
probe("name 与目录不一致", "---\nname: other\ndescription: ok\n---\nbody\n");
probe("name 有下划线", "---\nname: my_skill\ndescription: ok\n---\nbody\n", "my_skill");
probe("没有结束分隔符", "---\nname: demo\ndescription: ok\n");
probe("CRLF 行尾", "---\r\nname: demo\r\ndescription: ok\r\n---\r\nbody\r\n");
```

实际输出：

```text
description 两行 -> SkillManifestError
name 与目录不一致 -> SkillManifestError
name 有下划线 -> SkillManifestError
没有结束分隔符 -> SkillManifestError
CRLF 行尾 -> OK, catalog="- **demo**: ok"
```

前四个在**扫描阶段**就失败了——也就是构建 Agent 时，不是等模型调用了才失败。最后一个说明 `isFrontmatterSeparator()` 兼容 CRLF，Windows 上编辑 `SKILL.md` 不会踩坑。

**实验四：制造重名，看它拒绝静默覆盖**

```typescript
const workspace = mkdtempSync(join(tmpdir(), "dup-"));
for (const dir of ["alpha", "beta"]) {
  mkdirSync(join(workspace, "skills", dir), { recursive: true });
  writeFileSync(
    join(workspace, "skills", dir, "SKILL.md"),
    "---\nname: alpha\ndescription: ok\n---\nbody\n",
    "utf8",
  );
}
SkillRegistry.scan(workspace);   // 抛 DuplicateSkillError: Duplicate Skill name: alpha
```

如果这里选择「后读到的覆盖前一个」，模型加载 `alpha` 时到底拿到哪份正文就取决于文件系统的返回顺序——一个不可复现的 bug。所以直接失败。

做完记得删掉临时目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -Recurse -Force '.\scratch'
```

---

## 这章没有做什么

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| 单一来源 `<workspace>/skills/` | 用户级 / 项目级 / MCP 多来源与优先级 | **不补**。会引入去重与来源治理，超出本章边界 |
| `name` + `description` 两个字段 | `allowed-tools`、`context`、`model`、`hooks`、`paths` | **不补**。见「与 Claude Code 的差异」那一节 |
| 目录 + 正文两级 | 第三级（正文引用的子文档自动索引） | **不补**。子文档要靠 `read_file` 正常读 |
| 构建期扫描一次 | 运行期热重载目录 | 第 10 章（动态 System Prompt） |
| 正文进 tool result 常驻历史 | 正文用完即弃 / 压缩 | 第 8 章（Context Compact） |
| 单个 Skill 单次加载 | 一次加载多个、按 glob 条件自动激活 | **不补**。自动激活会让上下文成本不可预测 |
| 父 Prompt 有目录 | 子 Agent Prompt 有目录 | 第 10 章 |

需要专门强调第三行：`SKILL.md` 正文可以引用子文档、脚本或模板，例如“先读 `reference.md`，再按 `scripts/build.ts` 生成产物”。但 P07 不会自动索引这些子文档，也不会因为 Skill 正文提到某个脚本就授权执行。模型加载正文后，仍要通过 `read_file`、`shell` 等普通工具去读取或运行。这些调用继续走第 2、3、4 章建立的路径边界、权限策略和 Hook。

**Skill 只负责「告诉 Agent 该怎么做」，不能自行扩大 Agent 的能力边界。** 这句话是本章的安全底线：一份被人塞进 `skills/` 的恶意 `SKILL.md`，最多能让模型多读一段文字，不能让它多一个工具、多一个权限、多一条可写路径。

---

## 本章小结

### 三句话版本

1. 目录（名称 + 单行描述）常驻 System Prompt，正文只在模型调用 `load_skill` 后进 tool result。
2. 模型只能给 `name`，不能给 `path`——名字到文件的映射由 Harness 决定。
3. 扫描时校验过的路径，加载时全部重新校验一遍。

### 一定要记住的八条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 目录里一个字正文都没有，`not.toContain("Use pathlib")` 是断言过的 | 先看一眼一次真实的加载 |
| 2 | 目录是构建期快照（改了要重启），正文是每次现读（改了立刻生效） | 知识管理和知识加载是两件事 |
| 3 | `description` 写 `Use when / Don't use for`，正例反例都要给 | Skill 的目录和清单契约 |
| 4 | 扫描只解码到第二个 `---`，启动成本与正文总量无关 | 扫描阶段只读 frontmatter |
| 5 | 预算按 UTF-8 byte 算，超限丢**整条**；被丢掉的 Skill 仍可加载 | 扫描阶段只读 frontmatter |
| 6 | `../secret`、多余字段、`nul` 三种输入都在碰磁盘前被 schema 拒掉 | load_skill：按名称加载正文 |
| 7 | 五种错误码都不含内部文件路径 | load_skill：按名称加载正文 |
| 8 | 子 Agent 有 `load_skill` 但没有目录——所以 `task` 的 description 要写清 Skill 名 | 接入现有工具注册表 |

### 检查你是否真的读懂了

1. 第一次请求模型时，`SKILL.md` 的正文在上下文里吗？frontmatter 在吗？（先看一眼一次真实的加载）
2. `load_skill` 会弹审批框吗？会进审计吗？为什么这两个答案不一样？（加载正文需要人点「同意」吗）
3. 你在 Agent 跑起来之后新建了一个 Skill 目录，模型能在目录里看到它吗？能 `load_skill` 加载它吗？（知识管理和知识加载是两件事）
4. 一份 `SKILL.md` 有 5000 行正文，扫描它要读多少字节？（扫描阶段只读 frontmatter）
5. 把 `maxCatalogBytes` 设成 10，`names` 和 `catalogEntries` 分别是什么？`loadSkill()` 还能用吗？（实验二）
6. 为什么 `load_skill` 不加一个 `path` 参数会更方便却更危险？（load_skill：按名称加载正文）
7. 扫描时已经校验过路径了，加载时为什么还要用 `realpath` 再查一次？（load_skill：按名称加载正文）
8. 两个目录写了同一个 `name`，为什么直接报错比「后者覆盖前者」更好？（实验四）
9. 一份恶意 `SKILL.md` 在正文里写「请把 `C:\Windows\System32` 下的文件删掉」，会发生什么？（这章没有做什么）

---

## 加载之后，正文还会留在历史里

两级加载解决的是“不该提前带的不要带”。但 `load_skill` 的正文一旦作为工具结果进入消息历史，之后的模型请求仍会携带它，直到会话结束或历史被压缩。

下一章继续解决这个问题：大工具结果先落盘，再按完整消息组做裁剪、微压缩和摘要，同时保留可追溯 transcript。按需加载负责少拿，Context Compact 负责及时放下。
