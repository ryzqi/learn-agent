给 Agent 一个十步重构任务。

它先读文件、改类型、跑测试；第三步遇到失败后，注意力全被报错占住。测试最后修好了，但最初要求处理的另一批文件没有动，验收步骤也忘了执行。

任务还在对话开头，并没有真的消失。问题是：随着工具结果、文件内容和错误日志持续进入上下文，最初计划只出现一次，却要和大量局部信息竞争模型的注意力。

本章给 Agent 增加一个很小但很关键的能力：会话级 `todo_write`。它不替模型完成任务，只让模型反复提交“当前完整计划快照”，并在连续三轮工具调用都没有更新计划时提醒一次。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaFEj3lJzTL6GbS8x2FcoCjQXZ0M6IjrA7DtFIHpfWspialgtUOQA55WKqKHhtmsq2ia4DPV9M9SDjcKE3XxU6Gljd4icqV3Xa3gD8/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清楚「系统提示写一句不要忘记计划」为什么在长任务里不管用，以及应该用什么代替。
2. 解释为什么本章只做一个 `todo_write`（整体替换），而不是 `todo_add` / `todo_update` / `todo_remove` 三件套。
3. 说出 TODO 状态为什么必须属于会话而不是模块，以及一旦做成单例会出什么错。
4. 讲清楚「陈旧提醒」的完整时序：第几轮触发计数、第几次请求才注入、注入后什么时候清零。
5. 说出为什么提醒只进入当次请求、绝不写进正式历史。
6. 解释 `todo_write` 的 `effect: "write"` 为什么不会弹出磁盘写审批。

### 你需要先具备什么

| 需要的东西 | 从哪来 | 检查方式 |
| --- | --- | --- |
| 第 1–4 章的能力累积 | 前四章 | `npm run test:ch04` 是 12 文件 / 94 测试 |
| 理解 `prepare → permission → handler` 顺序 | 第 3 章 | 能说出为什么校验必须在授权之前 |
| 理解 `tool_call_id` 必须配对 | 第 2 章 | 能说出 `missing tool results for ids` 什么时候出现 |
| 能跑起 `code/` 里的测试 | 第 1 章 | `npm ci` 已执行 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| **TODO 快照** | 「截至此刻，完整计划长什么样」的一份 JSON，不是「这次改了哪几项」 |
| **`todo_write`** | 本章唯一新增的工具，语义是**整体替换**当前计划 |
| **`TodoTracker`** | 持有快照 + 陈旧计数的会话级对象，同时充当工具 handler 和轮次观察器 |
| **`TodoStatus`** | 只有三个值：`pending` / `in_progress` / `completed` |
| **strict schema** | `.strict()` 的 Zod 对象，出现未知字段直接失败 |
| **`transform` 再 `pipe`** | 先 `trim()` 再校验非空，这样 `"   "` 会稳定失败 |
| **ASCII JSON** | 把非 ASCII 字符写成 `\uXXXX`，让同一份逻辑快照永远产生同一段文本 |
| **`effect: "write"`** | 声明「这个工具会改状态」，但**改的是什么状态由工具自己决定**，不等于磁盘写 |
| **`ToolRoundObserver`** | Loop 认识的一个小接口，只有 `beforeModel()` 和 `recordToolRound()` 两个方法 |
| **工具轮** | 一条 assistant 消息里的**全部** tool call 算一轮，不管它包含 1 个还是 3 个调用 |
| **`STALE_TOOL_ROUNDS`** | 常量 `3`，连续三个没有 `todo_write` 的工具轮就该提醒了 |
| **Nag Reminder** | 那条固定文案的 system 提醒，只注入一次 |
| **请求级消息** | 只出现在这一次 `ModelRequest` 里，不进 `#history` 的消息 |
| **`invalid_arguments`** | Zod 校验失败时的工具错误码，handler 一次都没跑 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼真实的一次运行     ← 提交快照、返回什么、提醒何时出现
  ↓
② 长上下文的真实问题        ← 为什么系统提示会失效
  ↓
③ 验收结果 + 数据流          ← 本章要证明什么
  ↓
④ 一～三：输入契约与快照      ← Zod、整体替换、稳定 JSON
  ↓
⑤ 四～五：状态归属与系统提示   ← 为什么不能做单例
  ↓
⑥ 六～七：提醒机制            ← 按轮计数、只进当次请求
  ↓
⑦ 八～九：两个容易搞错的边界   ← write effect ≠ 写审批；失败不破坏旧快照
  ↓
⑧ 十～十二：验证、运行、观察   ← 动手 + 排错 + 该看什么
  ↓
⑨ 十三～十四 + 小结           ← 与官方/参考书的差异 + 自测
```

---

## 先看一眼真实的一次运行

先不看代码，先看两件具体的事。

### 一次成功的快照提交

模型发出这个调用：

```json
{
  "todos": [
    { "content": "  编写测试  ", "status": "in_progress" },
    { "content": "ship", "status": "completed" }
  ]
}
```

写进历史的 tool result 是这一整串（注意，不是「已更新 2 项」）：

```text
{"todos":[{"content":"\u7f16\u5199\u6d4b\u8bd5","status":"in_progress"},{"content":"ship","status":"completed"}]}
```

三个细节值得先记住，它们分别对应后面三节：

| 你看到的现象 | 原因 | 在哪一节讲 |
| --- | --- | --- |
| `"  编写测试  "` 两侧的空格没了 | schema 先 `trim()` 再校验 | 一 |
| 中文变成了 `\u7f16\u5199...` | 刻意保持 ASCII，让文本可精确断言 | 三 |
| 返回的是全部两项，而不是差异 | 整体替换语义 | 二 |

这一次调用**没有弹出任何审批框**，但它确实产生了一条审计记录：

```text
PermissionDecision("allow", "No permission rule blocked the request", "default")
```

「不弹框，但过策略、留审计」——这就是第八节要解释的 `write` effect 与写审批的区别。

### 提醒到底在第几次请求出现

假设模型连着读了四次文件，一次都没更新计划。真实的时间线是这样：

```text
请求 1  ← 没有提醒            （还没有任何工具轮）
  ↓ 模型调用 read_file        计数 0 → 1
请求 2  ← 没有提醒
  ↓ 模型调用 read_file        计数 1 → 2
请求 3  ← 没有提醒
  ↓ 模型调用 read_file        计数 2 → 3   ← 达到阈值了
请求 4  ← ✅ 注入一条 system 提醒，并立刻把计数清零
  ↓ 模型调用 read_file        计数 0 → 1
请求 5  ← 没有提醒            （即使模型还是没更新计划）
```

请特别注意两点，它们是本章最容易读错的地方：

- **「计数达到 3」和「注入提醒」不在同一次请求里。** 第三个陈旧工具轮跑完，计数才变成 3；提醒因此出现在**第 4 次**请求。
- **提醒只出现一次。** 注入的同时计数就归零了，所以第 5 次请求是干净的。想再被提醒，必须再攒三个陈旧工具轮。

而这条提醒**在 `result.history` 里查不到**。它只活在第 4 次 `ModelRequest` 的消息数组里。这一点由测试直接断言，见第十节。

---

## 先明确：长上下文不是“越靠后权重越高”

Transformer 会根据内容动态计算注意力，不能简单理解为“后面的 token 一定比前面的重要”。但长任务中会出现三个很现实的问题：

- 工具输出不断增长，早期目标要与更多内容竞争注意力；
- 相似日志和局部错误会反复出现，模型更容易围绕眼前问题继续生成；
- 最初计划只描述“要做什么”，却没有持续提供“已经做到哪一步”。

因此，系统提示里写一句“不要忘记计划”并不够。它只能表达原则，不能保存任务状态。

本章的思路是把计划变成结构化状态，再通过工具结果把最新快照放回消息流。这样模型下一轮看到的不只是最初目标，还有最近一次明确提交的进度。

这里仍然要把能力边界说清楚：TODO 能降低偏航概率，不能证明任务已经完成。最终结果仍要由测试、静态检查或其他可观察验收来确认。

---

## 本章要得到什么

第 5 章建立在前四章之上，保留文件工具、权限策略和四类 Hook，只增加 `TODO` capability。

可观察的验收结果有四个：

1. `todo_write` 每次返回整个 TODO 列表的稳定 JSON 快照，而不是只返回本次变化；
2. 非法更新不会覆盖旧快照；
3. 每个 Agent 会话拥有独立状态；
4. 连续三轮调用其他工具后，下一次模型请求只注入一条临时提醒。

失败边界同样明确：`todos` 不是数组、内容为空、状态未知、超过 50 项、带未知字段或 `arguments` 不是合法 JSON，handler 都不能改变状态。

整体数据流如下：

```text
assistant tool_calls
        │
        ▼
ToolRegistry 解析 JSON + Zod 校验
        │
        ├── 失败 ──> 配对的 tool error，旧快照不变
        │
        ▼
权限 / Hook / handler
        │
        ▼
TodoTracker 原子替换完整快照
        │
        ▼
稳定 JSON 作为 tool result 进入正式历史
```

提醒走另一条路径：

```text
完成一个工具轮
    │
    ├── 本轮有 todo_write ──> 计数归零
    │
    └── 本轮没有 todo_write ──> 计数加一
                                  │
                                  └── 达到 3：下一次模型请求临时注入 system reminder
```

---

## 一、用 Zod 定义唯一输入契约

实现位于 `code/chapters/ch05/src/features/todos.ts`。先定义允许的状态和数量上限：

```typescript
export const MAX_TODOS = 50;
export const STALE_TOOL_ROUNDS = 3;
export const TODO_STATUSES = Object.freeze([
  "pending",
  "in_progress",
  "completed",
] as const);

export type TodoStatus = (typeof TODO_STATUSES)[number];
```

`as const` 保留三个字符串字面量，`TodoStatus` 因而是：

```typescript
type TodoStatus = "pending" | "in_progress" | "completed";
```

接着用 strict Zod schema 描述工具参数：

```typescript
const todoItemSchema = z
  .object({
    content: z
      .string()
      .transform((content) => content.trim())
      .pipe(z.string().min(1, "todo content must not be empty")),
    status: z.enum(TODO_STATUSES),
  })
  .strict();

const todoWriteSchema = z
  .object({
    todos: z.array(todoItemSchema).max(MAX_TODOS),
  })
  .strict();

export type TodoItem = Readonly<z.output<typeof todoItemSchema>>;
type TodoWriteInput = z.output<typeof todoWriteSchema>;
```

这段 schema 同时完成几件事：

- `content` 必须是字符串；
- 先执行 `trim()`，再检查结果不能为空；
- `status` 只能是三种已知值；
- 每次最多提交 50 项；
- 两层 `.strict()` 都拒绝未知字段。

为什么先 `transform` 再 `pipe`？如果直接对原字符串做 `min(1)`，`"   "` 会通过长度检查。现在空白被去掉后再验证，它会稳定失败。

更重要的是，`ToolDefinition` 直接持有这个 schema。`ToolRegistry` 既用它生成发给 OpenAI 的 JSON Schema，也用它在 dispatch 前验证输入，不需要再维护一套手写参数类型。模型看到的工具契约和 handler 实际接收的契约来自同一个源头。

非法 JSON 更早失败：Registry 先解析 `tool_call.arguments`。解析失败返回 `invalid_json`；能解析但不符合 Zod schema，则返回 `invalid_arguments`。这两条路径都不会调用 handler。

`ToolRegistry.openAITools()` 同样复用这份 schema：模型看到的 `parameters` 由 `z.toJSONSchema(definition.inputSchema)` 生成，运行时再传给 `prepare()` 做 `safeParse`。

开发时只维护一套类型契约。模型声明、权限策略和 handler 收到的参数不会因为手工同步而漂移。

---

## 二、一次提交必须是完整快照

本章没有设计 `todo_add`、`todo_update`、`todo_remove` 三组增量接口，只提供一个整体替换工具：

```typescript
export class TodoTracker {
  #todos: readonly TodoItem[] = Object.freeze([]);
  #nonTodoToolRounds = 0;
  readonly toolDefinition: ToolDefinition<TodoWriteInput>;

  constructor() {
    this.toolDefinition = Object.freeze({
      name: "todo_write",
      description: "Replace the current TODO list with a complete task snapshot.",
      inputSchema: todoWriteSchema,
      effect: "write",
      handler: (input: TodoWriteInput) => this.#writeTodos(input),
    });
  }

  get todos(): readonly TodoItem[] {
    return this.#todos;
  }

  #writeTodos(input: TodoWriteInput): ToolResult {
    // 先由 Zod 完整校验，再一次替换快照；失败路径不会触碰旧状态。
    this.#todos = Object.freeze(
      input.todos.map((item) =>
        Object.freeze({ content: item.content, status: item.status }),
      ),
    );
    this.#nonTodoToolRounds = 0;
    return toolSuccess(serializeSnapshot(this.#todos));
  }
}
```

完整快照比增量命令更适合模型调用，原因很实际：

- 当前状态只由最后一次成功调用决定，容易解释和重放；
- 不需要处理“更新一个已经不存在的索引”之类的额外状态机；
- tool result 本身就包含后续推理需要的全部计划；
- 校验完成后只执行一次赋值，失败不会产生半更新状态。

例如第一次提交：

```json
{
  "todos": [
    { "content": "读取现有实现", "status": "in_progress" },
    { "content": "增加失败测试", "status": "pending" },
    { "content": "实现并验证修复", "status": "pending" }
  ]
}
```

第一步完成后，模型不能只提交“第一项已完成”，而要再次提交三项完整状态：

```json
{
  "todos": [
    { "content": "读取现有实现", "status": "completed" },
    { "content": "增加失败测试", "status": "in_progress" },
    { "content": "实现并验证修复", "status": "pending" }
  ]
}
```

这就是工具描述里 `complete task snapshot` 的含义。

新手常问：「让模型每次都重复一遍全部任务，不是浪费 token 吗？」把两种方案的失败模式摆在一起，就知道这点开销买到了什么。

| 场景 | 增量接口（`todo_update` 之类） | 整体替换（本章的做法） |
| --- | --- | --- |
| 模型说「把第 2 项标为完成」，但它记错了顺序 | 改错了一项，而且**没人知道** | 不存在这个操作；它必须提交自己认为的完整列表，错就错在明面上 |
| 模型引用了一个已被删掉的索引 | 需要额外的状态机决定报错还是忽略 | 不存在索引概念 |
| 一次提交里第 1 项合法、第 3 项非法 | 可能已经写了第 1 项才发现第 3 项有问题，产生半更新 | Zod 先整体校验，一个字都不写 |
| 想知道「模型现在到底怎么理解这个任务」 | 要把历史上所有增量重放一遍 | 看最后一条 tool result 就够了 |
| 模型漏掉了一项 | 静默保留旧值，你以为它还记得 | 那一项就是被删掉了，语义明确 |

最后一行是关键，也是唯一需要模型付出的代价：**整体替换没有隐式 merge**。漏写一项等于删除一项。这个语义比「漏写就保留」更好，因为它把模型的遗忘变成了**可见**的遗忘——你在 tool result 里一眼就能看出来。

顺带一句：这也是「多花的 token」真正的用途。每次完整快照都会成为消息历史里最新的那条工具结果，模型下一轮不需要向上翻二十条消息去拼凑进度，只要看眼前这一条。**代价是重复的 token，买到的是不需要推理的进度。**

### 为什么还要冻结对象

TypeScript 的 `readonly` 只在编译期约束调用方，运行时数组仍可能被修改。因此 tracker 在保存时重新构造每一项，并冻结 item 和外层数组。

这不是为了制造复杂抽象，而是为了守住一个直接契约：成功更新后的快照只能由下一次合法 `todo_write` 替换，不能被持有引用的其他代码偷偷改掉。

---

## 三、返回稳定且完整的 JSON

handler 不返回“updated 3 todos”，而是返回完整快照：

```json
{"todos":[{"content":"ship","status":"completed"}]}
```

实现先按固定字段顺序构造数据，再序列化：

```typescript
function serializeSnapshot(todos: readonly TodoItem[]): string {
  const json = JSON.stringify({
    todos: todos.map((item) => ({
      content: item.content,
      status: item.status,
    })),
  });
  const ascii: string[] = [];

  for (const character of json) {
    const codePoint = character.codePointAt(0);
    if (codePoint === undefined) {
      throw new Error("todo snapshot contained an invalid Unicode value");
    }

    if (codePoint <= 0x7f) {
      ascii.push(character);
      continue;
    }

    if (codePoint <= 0xffff) {
      ascii.push(`\\u${codePoint.toString(16).padStart(4, "0")}`);
      continue;
    }

    const offset = codePoint - 0x10000;
    const high = 0xd800 + (offset >> 10);
    const low = 0xdc00 + (offset & 0x3ff);
    ascii.push(`\\u${high.toString(16)}\\u${low.toString(16)}`);
  }

  return ascii.join("");
}
```

这里保留 ASCII JSON，是为了让同一个逻辑快照始终产生确定文本。中文会表示成 JSON Unicode escape，例如：

```text
编写测试
```

对应：

```text
\u7f16\u5199\u6d4b\u8bd5
```

它们解码后的内容相同。稳定文本让测试可以精确断言，也避免日志、平台编码和迁移前后序列化差异干扰比较。

---

## 四、状态必须属于会话，而不是模块

`TodoTracker` 不能做成模块级单例。否则同一个 Node.js 进程里创建两个 Agent 时，第二个会话会看到第一个会话的计划。

这个错误听起来抽象，具体后果是这样的：

```text
❌ 假设写成 export const tracker = new TodoTracker();

用户 A 的会话：todo_write 提交了三项「重构 auth 模块」
用户 B 的会话（同一进程）：第一次调用 todo_write 之前，
                          #nonTodoToolRounds 已经是 A 留下的 2
  → B 才读了一个文件就被提醒「Keep the TODO list current」
  → B 的第一次 todo_write 覆盖掉 A 的计划
  → A 下一轮看到的快照，是 B 的任务
```

不只是「串台」这么简单，注意第二行：**陈旧计数也是会话状态**。它和快照一样必须跟着会话走，否则提醒时机会变成两个会话的工具调用次数之和。

组合根在每次 `buildAgent()` 时创建 tracker：

```typescript
const todoTracker = profile.capabilities.has("todo")
  ? new TodoTracker()
  : undefined;

if (todoTracker !== undefined) {
  tools.register(todoTracker.toolDefinition);
}
```

随后，同一个实例既提供工具 handler，也作为工具轮观察者传给 Loop：

```typescript
return new AgentRunner({
  model: dependencies.model,
  tools,
  systemPrompt:
    todoTracker === undefined
      ? SYSTEM_PROMPT
      : `${SYSTEM_PROMPT}${TODO_SYSTEM_PROMPT}`,
  workspace: dependencies.workspace,
  ...(todoTracker === undefined
    ? {}
    : { toolRoundObserver: todoTracker }),
});
```

这样得到的生命周期很清楚：

- 同一次 `AgentRunner` 运行中，工具和 reminder 共用一个 tracker；
- 两次 `buildAgent()` 得到两个 tracker，状态互不影响；
- 进程退出后状态消失，本章不做磁盘持久化。

第 12 章的项目 Task 会解决跨会话持久化、依赖和认领问题。它与本章的会话 TODO 生命周期不同，不应该提前揉进一个全局状态对象。

---

## 五、系统提示只负责告诉模型“什么时候用”

第 5 章在基础系统提示后追加一行：

```typescript
const TODO_SYSTEM_PROMPT =
  "\nFor complex tasks, call todo_write with the complete task snapshot and update it when the plan changes.";
```

工具 description 说明“工具做什么”，系统提示说明“复杂任务何时使用”。两者都不会强制模型调用工具，因此不能把“真实运行第一次一定调用 `todo_write`”当作确定性保证。

真正可以离线验证的是：

- P05 的工具列表包含且只新增 `todo_write`；
- 系统提示包含完整快照要求；
- 模型一旦正确调用，运行时能校验、保存并返回快照；
- 模型忘记更新时，运行时会按规则注入提醒。

---

## 六、Nag Reminder 按工具轮计数

一个 assistant 消息可以同时包含多个 tool call。如果同一轮读了三个文件，提醒计数应该增加一次，而不是三次。

`TodoTracker.recordToolRound()` 接收这一轮的全部工具名：

```typescript
recordToolRound(toolNames: readonly string[]): void {
  if (toolNames.length === 0) {
    return;
  }
  if (toolNames.includes(this.toolDefinition.name)) {
    this.#nonTodoToolRounds = 0;
    return;
  }
  this.#nonTodoToolRounds += 1;
}
```

规则只有三条：

1. 没有工具调用，不计数；
2. 本轮只要出现 `todo_write`，计数归零；
3. 本轮有工具但没有 `todo_write`，计数加一。

第一条容易被跳过，但它有明确用途：模型给出纯文本回答（没有工具调用）时，`recordToolRound([])` 会直接 `return`。**「聊了两句」不该被算成「干了两轮活还不更新计划」。**

三条规则跑起来是这个样子，注意第 2 行的一轮里有三个调用：

| 这一轮的工具 | 命中哪条规则 | 计数变化 |
| --- | --- | --- |
| `read_file` | 3 | 0 → 1 |
| `read_file` + `glob` + `read_file`（同一条 assistant 消息） | 3 | 1 → **2**（不是 4） |
| 无（模型只回文本） | 1 | 2 → 2 |
| `todo_write` | 2 | 2 → **0** |
| `shell` + `todo_write` | 2 | 0 → 0（只要**包含** `todo_write` 就归零） |

第 2 行和第 5 行是这个设计的两个要点：**按 assistant 消息计数，不按调用个数**；以及**「包含」就算更新，不要求整轮只有 `todo_write`**。后者符合直觉——模型一边跑测试一边更新计划，显然不该被当成没更新。

达到阈值后，`beforeModel()` 返回一条 system guidance，并立刻清零：

```typescript
export const TODO_STALE_REMINDER =
  "Keep the TODO list current. Call todo_write with the complete task snapshot when the plan changes.";

beforeModel(): readonly ChatMessage[] {
  if (this.#nonTodoToolRounds < STALE_TOOL_ROUNDS) {
    return [];
  }
  // 提醒是请求级上下文；立即清零可避免后续每轮重复注入。
  this.#nonTodoToolRounds = 0;
  return Object.freeze([systemMessage(TODO_STALE_REMINDER)]);
}
```

注意“达到三轮”和“注入提醒”不是同一个模型请求。第三个陈旧工具轮执行完后计数才变成 3，因此 reminder 出现在下一次，也就是第四次模型请求中：

| 模型请求 | 上一轮工具       | 请求中有 reminder |    轮后计数 |
| ---- | ----------- | ------------: | ------: |
| 1    | 无           |             否 |       0 |
| 2    | `read_file` |             否 |       1 |
| 3    | `glob`      |             否 |       2 |
| 4    | `shell`     |             是 | 重置后重新计数 |

如果第四次请求又产生一个非 TODO 工具轮，轮后计数从 0 变为 1，不会在第五次请求重复提醒。

---

## 七、提醒只进入当次请求，不污染正式历史

Loop 在调用模型前读取 guidance：

```typescript
const observerGuidance =
  this.#toolRoundObserver === undefined
    ? []
    : this.#toolRoundObserver.beforeModel();

const request: ModelRequest = Object.freeze({
  messages: Object.freeze([
    systemMessage(this.#systemPrompt),
    ...this.#history,
    ...observerGuidance,
  ]),
  tools: tools.openAITools(),
});
```

关键是 `observerGuidance` 只用于构造当前 `ModelRequest`，没有 `push` 进 `#history`。

这样做有两个直接好处：

- 提醒靠近当前生成位置，能发挥提示作用；
- 它不会永久堆积，后续请求也不会看到一串过期 reminder。

一轮中的所有工具都完成、所有 tool result 都配对写入历史后，Loop 再记录工具名：

```typescript
if (this.#toolRoundObserver !== undefined) {
  this.#toolRoundObserver.recordToolRound(
    assistant.toolCalls.map((call) => call.name),
  );
}
```

这个位置不能随便移动。先完成 tool result 配对，才能保证观察逻辑不会打断 OpenAI 的消息协议；按整条 assistant 消息记录，才能保证多个调用只算一轮。

把 P05 循环里这一段的真实顺序完整列出来，会更清楚「不能随便移动」的含义：

```text
① 执行本轮全部工具，结果先攒在 results[] 里（不写历史）
② 按 assistant.toolCalls 的顺序，把每个 result 写成 toolMessage 推入历史
③ recordToolRound(本轮全部工具名)          ← 就在这里
④ 推入 deferredContext（第 4 章 Hook 的 additionalContext）
⑤ 如果有 preventContinuation，提前结束
```

为什么必须是 ③ 这个位置，而不是往前或往后挪：

| 如果挪到 | 会发生什么 |
| --- | --- |
| ② 之前（每执行一个工具就记一次） | 三个调用的一轮会被算成三轮，提醒提前触发 |
| ② 中间（每 push 一条 tool message 就记一次） | 同上，而且观察器会看到「一半配对完成」的历史 |
| ④ 之后 | 本章看不出差别，但语义变成「等 Hook 的附加上下文也写完再算」——把两个无关机制耦合起来了 |

第一行是这个设计真正防的东西。`beforeModel()` 和 `recordToolRound()` 之所以被抽成一个只有两个方法的 `ToolRoundObserver` 接口，而不是让 Loop 直接认识 `TodoTracker`，也是同一个理由：**Loop 只需要知道「轮开始前问一句、轮结束后报一次」，不需要知道对面在数什么。**

---

## 八、`write` 副作用不等于磁盘写审批

`todo_write` 的 `effect` 是 `"write"`，因为它确实改变会话内状态：

```typescript
effect: "write"
```

但第 3 章的文件审批规则精确匹配 `write_file` 和 `edit_file`，而不是粗暴拦截所有 `write` effect：

```typescript
matches: (request) => {
  const name = request.prepared.definition?.name;
  return name === "write_file" || name === "edit_file";
}
```

因此更新 TODO 不会弹出磁盘写入审批；它仍然经过统一权限决策并写入审计。这个区分很重要：副作用类别描述行为性质，具体策略决定哪些行为需要人工确认。

第 5 章仍保留已有边界：

- `write_file`、`edit_file` 继续要求明确批准；
- `shell` 继续按已有策略处理；
- workspace 外写入不能被 Hook 或审批放宽；
- Hook 的 deny、输出改写和 Stop 语义保持不变。

---

## 九、失败为什么不会破坏旧快照

假设当前状态已经是：

```json
{
  "todos": [
    { "content": "保留这项", "status": "in_progress" }
  ]
}
```

模型随后提交非法状态：

```json
{
  "todos": [
    { "content": "错误更新", "status": "working" }
  ]
}
```

调用路径是：

```text
JSON.parse
-> todoWriteSchema.safeParse
-> 失败，生成 invalid_arguments
-> handler 未调用
-> #todos 引用和值都保持不变
```

这比“进入 handler 后逐项检查、逐项写入”简单得多，也没有回滚问题。状态赋值只有一个入口，并且只接收经过 Zod 验证的 `TodoWriteInput`。

测试不仅比较内容，还保存更新前的数组引用，确认失败后仍是同一个快照对象。这样可以证明失败路径没有偷偷创建或替换状态。

类似的信任边界也出现在 handler 输出侧。`ToolRegistry.invoke()` 把 handler 的返回值视为不可信输入，只有通过 `isToolResult()` 检查的对象才能进入正式历史；返回畸形对象时，Loop 会把它替换成 `invalid_tool_result` 错误，避免任意结构污染消息协议。

---

## 十、离线测试如何证明这些契约

`npm run test:ch05` 会执行 **14 个测试文件、109 个测试**。相比第 4 章的 12 文件 / 94 测试，本章新增两个文件：

| 测试文件 | 验证内容 |
| --- | --- |
| `todos.test.ts` | 单元层：Zod 契约、快照替换、ASCII 序列化、失败边界、会话隔离、提醒计数 |
| `ch05-todos.test.ts` | 端到端：从 `buildAgent(P05, ...)` 进入真实组合根，验证工具集、系统提示、审计、配对和提醒时机 |

其余 12 个是前四章的累计测试，继续保证「加 TODO 没改坏旧行为」。**注意本章动了公共 `loop.ts`，所以这 12 个文件的绿灯比前几章更重要。**

单元测试 `todos.test.ts` 覆盖：

- 三种状态和 `content.trim()`；
- 完整、稳定、ASCII JSON 快照；
- 恰好 50 项的上边界；
- 非数组、空内容、未知状态、未知字段、51 项和非法 JSON；
- 失败前后快照对象和值都不变；
- 两个 tracker 的会话隔离；
- 三轮提醒只出现一次；
- `todo_write` 重置陈旧轮计数。

集成测试 `ch05-todos.test.ts` 只有两个 `test`，但它们的断言值得逐条看，因为这两组断言就是本章验收标准的可执行形式。

**第一个测试**证明「新增了工具、没破坏权限」。它的四条关键断言是：

```typescript
// ① 工具集是前四章五个工具 + todo_write，顺序也固定
expect(model.requests[0]?.tools.map((tool) => tool.function.name)).toEqual([
  "shell", "read_file", "write_file", "edit_file", "glob", "todo_write",
]);
// ② TODO 系统提示确实进了第一条 system 消息
expect(model.requests[0]?.messages[0]?.content).toContain(
  "call todo_write with the complete task snapshot",
);
// ③ 审批器一次都没被调用
expect(approval.requests).toEqual([]);
// ④ 但审计里有一条 default allow
expect(audit.decisions).toEqual([
  new PermissionDecision("allow", "No permission rule blocked the request", "default"),
]);
```

③ 和 ④ 放在一起，就是第八节那句话的证明：**不弹框，不等于不过策略。** 这个测试里的审批器实现叫 `UnexpectedApproval`，它被调用时会直接 `throw`——所以只要 `todo_write` 意外触发了审批，测试不是断言失败，而是直接炸掉。

紧接着它断言 tool result 的**逐字节内容**：

```typescript
expect(result.history[2]).toEqual({
  role: "tool",
  content:
    '{"todos":[{"content":"\\u7f16\\u5199\\u6d4b\\u8bd5","status":"in_progress"},{"content":"ship","status":"completed"}]}',
  toolCallId: "todo-1",
});
```

输入里的 `"  编写测试  "` 在这里已经变成了 trim 过的 `\u7f16\u5199\u6d4b\u8bd5`，`toolCallId` 也和原调用配对。**第三节坚持 ASCII 序列化，就是为了让这一行断言写得出来。** 如果快照直接输出 UTF-8 中文，这条断言会因平台编码、文件 BOM 或 Git 换行处理而变得脆弱。

**第二个测试**证明提醒时机。它让模型连读四次文件，然后断言：

```typescript
expect(model.requests).toHaveLength(5);
// 前三次请求都没有提醒
for (const request of model.requests.slice(0, 3)) { /* ... false */ }
// 第 4 次请求里，恰好一条、且必须是 system 角色
expect(model.requests[3]?.messages.filter(
  (message) => message.role === "system" && message.content === TODO_STALE_REMINDER,
)).toEqual([{ role: "system", content: TODO_STALE_REMINDER }]);
// 第 5 次请求又干净了
expect(model.requests[4]?.messages.some(/* ... */)).toBe(false);
// 正式历史里根本查不到这条提醒
expect(result.history.some((message) => message.content === TODO_STALE_REMINDER)).toBe(false);
expect(result.history.filter((message) => message.role === "tool")).toHaveLength(4);
validateToolPairing(result.history);
```

这一组断言把「先看一眼真实的一次运行」里那张时间线图**逐格钉住了**：

| 断言 | 钉住的结论 |
| --- | --- |
| `slice(0, 3)` 全部为 false | 提醒不会提前出现 |
| `requests[3]` 用 `filter(...).toEqual([...])` | 恰好一条，不是「至少一条」 |
| 同一条断言检查 `role === "system"` | 提醒必须是 system，不能伪装成 user 或 tool |
| `requests[4]` 为 false | 注入后立即清零 |
| `history.some(...)` 为 false | 请求级消息不进正式历史 |
| `history` 里恰好 4 条 tool 消息 | 提醒没有挤掉或替换任何工具结果 |
| `validateToolPairing` | 加了这套机制，消息协议依然完整 |

**倒数第二条最容易被忽略。** 提醒是一条额外的 system 消息，插在消息数组末尾。如果实现时不小心把它 `push` 进了 `#history`，`validateToolPairing` 未必会红——system 消息不参与配对校验——但 `history.some(...)` 那条会立刻变红。两条断言配合，才既管住了协议又管住了污染。

运行第五章最小验证：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run test:ch05
```

因为本章修改了公共 Loop，完成 review 前还要跑全量回归和静态检查：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm test
npm run lint
npm run format:check
npm run build
```

测试使用 `ScriptedModelClient` 和临时 workspace，不读取 `.env`，也不访问网络。它验证的是运行时的确定性契约，而不是依赖模型随机表现做演示。

---

## 十一、运行第 5 章

### 第 0 步：准备环境

和前四章完全一样：`code/` 里 `npm ci`，`.env` 里填好三个变量。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
```

### 第 1 步：先跑离线测试

```powershell
npm run test:ch05
```

预期最后两行：

```text
 Test Files  14 passed (14)
      Tests  109 passed (109)
```

输出里那行 `配置错误: Missing required settings: ...` 仍然是被断言的预期输出（`config.test.ts` 在验证「缺配置时 CLI 返回退出码 2」），不是失败。

### 第 2 步：让模型先规划再执行

```powershell
npm run ch05 -- --prompt "先建立完整 TODO，再读取 README.md 并总结运行和验证步骤"
```

预期在 stderr 上看到（`[Hook]`、`[Permission]` 都是前几章的）：

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: todo_write
[Permission] todo_write: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: todo_write -> ok
[Hook] PreToolUse: read_file
[Permission] read_file: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: read_file -> ok
[Hook] Stop
```

请特别确认 `todo_write` 那行是 `allow (default)`，**而且没有弹出任何审批框**。它的 `effect` 是 `"write"`，但第 3 章的规则只匹配 `write_file` 和 `edit_file`。

模型是否真的先调用 `todo_write` 取决于模型本身——系统提示是引导，不是强制门控。如果它直接开始读文件，把 prompt 写得更明确一些再试。

### 第 3 步：观察快照内容

`todo_write` 的 tool result 不会出现在 stdout（stdout 只写最终答案）。想看快照，最直接的办法是让模型自己复述：

```powershell
npm run ch05 -- --prompt "先用 todo_write 建立三项计划，然后原样告诉我 todo_write 返回的完整文本"
```

预期模型复述出来的是一整份 `{"todos":[...]}`，其中中文是 `\uXXXX` 形式。

### 第 4 步：统一入口

```powershell
npm run agent-tutorial -- run --chapter 5 --prompt "先建立完整 TODO，再读取 README.md 并总结运行和验证步骤"
```

两个入口最终都使用 `P05` 和同一个 `buildAgent()`，不会复制一套第五章 Loop。固定章节文件只有三行：

```typescript
import { runProfile } from "../cli.js";
import { P05 } from "../core/profiles.js";

process.exitCode = await runProfile(P05, process.argv.slice(2));
```

`runProfile(P05, ...)` 会把固定 profile 交给统一 CLI 路由：路由仍只解析 `--prompt`，并在内部强制填入 `--chapter 5`，因此即使命令参数里出现其他章节号，也不会意外切换能力。

### 第 5 步（可选）：完整离线门禁

```powershell
npm run typecheck
npm test
npm run lint
npm run format:check
npm run build
```

本章改了公共 `loop.ts`，这一步不该跳过。

### 常见报错排查

| 现象 | 原因 | 怎么处理 |
| --- | --- | --- |
| `Error [invalid_arguments]` | `status` 不在三个值里、`content` 是空白、有未知字段、超过 50 项 | 这是**正确行为**。旧快照没有被改动，让模型重新提交合法快照即可 |
| `Error [invalid_json]` | 模型生成的 `arguments` 不是合法 JSON | 同上，handler 一次都没跑 |
| 模型压根不调用 `todo_write` | 系统提示只是引导 | 把 prompt 写明「先建立完整 TODO」；或者接受这个结果——本章不强制门控 |
| 模型每次只提交变化的那一项 | 它没理解「完整快照」语义 | 看 tool result：漏掉的项**确实被删了**。这是设计语义，不是 bug |
| 迟迟看不到 `Keep the TODO list current` | 还没攒够三个陈旧工具轮 | 数一下：只有「有工具调用且不含 `todo_write`」的轮才计数 |
| 提醒出现在第 3 次请求 | 你把「计数到 3」和「注入」当成了同一次请求 | 复查「先看一眼真实的一次运行」那张时间线：提醒在第 4 次 |
| 在 `result.history` 里找提醒找不到 | 提醒是请求级消息 | 这是设计。要观察它，得看 `model.requests[i].messages` |
| `todo_write` 弹出了审批框 | 你自己改过第 3 章的规则，把 `effect === "write"` 当成了匹配条件 | 改回精确匹配工具名。见第八节 |
| 两个会话看到同一份计划 | `TodoTracker` 被写成了模块级单例 | 每次 `buildAgent()` 必须 `new TodoTracker()` |

### 四个建议动手做的小实验

**实验一：亲手撞一次 `invalid_arguments`，确认旧快照没变**

在 `todos.test.ts` 末尾追加一个 `test`，直接复用文件顶部已有的 `invokeTodo` 辅助函数（它走 `prepare` → `invoke` 的真实路径）：

```typescript
test("实验一：非法状态不改变旧快照", async () => {
  const tracker = new TodoTracker();
  await invokeTodo(tracker, JSON.stringify({ todos: [{ content: "保留这项", status: "in_progress" }] }));
  const before = tracker.todos;                     // 存下引用，不只是值

  const failed = await invokeTodo(tracker, JSON.stringify({ todos: [{ content: "错误更新", status: "working" }] }));

  expect(failed.isError).toBe(true);
  expect(failed.content).toContain("invalid_arguments");
  expect(tracker.todos).toBe(before);               // 用 toBe：必须是同一个对象
});
```

学到：`toBe` 而不是 `toEqual`——失败路径连**新对象都没创建**。`content` 里的 `invalid_arguments` 说明 Zod 在 `prepare()` 阶段就拦下了，`#writeTodos` 一次都没跑。

**实验二：试着改掉已经保存的快照**

```typescript
test("实验二：快照冻结", async () => {
  const tracker = new TodoTracker();
  await invokeTodo(tracker, JSON.stringify({ todos: [{ content: "a", status: "pending" }] }));
  const snapshot = tracker.todos;

  expect(() => {
    // @ts-expect-error 故意违反 readonly，验证运行时也拦得住
    snapshot[0].status = "completed";
  }).toThrow(TypeError);
  expect(tracker.todos[0]?.status).toBe("pending");
});
```

预期：严格模式（ESM 模块默认严格）下抛 `TypeError`，快照值不变。
学到：第二节末尾「为什么还要冻结对象」的实际效果。TypeScript 的 `readonly` 只在编译期拦住你（所以要写 `@ts-expect-error` 才编译得过），`Object.freeze` 才在运行时拦得住。

**实验三：把 `STALE_TOOL_ROUNDS` 改成 1，观察提醒频率**

把 `features/todos.ts` 里的常量改成 `1`，然后跑 `npm run ch05` 让模型连续读几个文件。

预期：几乎每次请求都带提醒。
学到：为什么阈值不能太小——提醒本身会占注意力，变成新的噪音。**改完记得改回 `3`，否则 `todos.test.ts` 会红。**

**实验四：验证「一轮多个调用只算一轮」**

```typescript
const tracker = new TodoTracker();
tracker.recordToolRound(["read_file", "glob", "read_file"]);   // 一轮三个调用
tracker.recordToolRound(["shell"]);
expect(tracker.beforeModel()).toEqual([]);                      // 计数才 2，不提醒
tracker.recordToolRound(["read_file"]);
expect(tracker.beforeModel()).toHaveLength(1);                  // 第 3 轮之后才提醒
```

学到：第六节那张表的第 2 行。如果按调用个数计数，第一次 `beforeModel()` 就该提醒了。

---

## 十二、观察真实运行时应该看什么

第一，看模型是否在复杂任务开始时提交完整 TODO。系统提示是引导，不是强制门控；如果模型直接执行，先检查 prompt 是否真的要求规划，再评估模型行为。

第二，看状态是否形成合理流转：

```text
pending -> in_progress -> completed
```

一次把所有项目从 `pending` 改成 `completed` 虽然合法，却说明中间进度没有被记录。任务若在过程中中断，最后一份快照可能已经过时。

第三，看每次更新是否仍包含全部任务。完整替换接口没有隐式 merge；遗漏一项，就表示模型明确从当前计划中删除了它。

第四，看 reminder 的时机。它只在连续三轮其他工具之后出现一次。如果模型随后仍不更新，必须再经历三个新的陈旧工具轮才会再次提醒。

最后，不要把 TODO 的 `completed` 当作验收证据。它是模型提交的计划状态，不是测试结果。代码是否正确，仍以本章列出的直接验证为准。

---

## 十三、与 Claude Code 的差异

本章的 `todo_write` 是教学子集。Claude Code 内部并存两套任务系统（差异整理自外部仓库 [`shareAI-lab/learn-claude-code`](https://github.com/shareAI-lab/learn-claude-code) 的 `s05_todo_write/README.md`）：

- TodoWrite（V1）：一个 `todo_write` 工具，列表保存在会话内存中，退出后清空；
- Task System（V2）：文件持久化、`blockedBy` 依赖图、并发锁、任务所有权，以及 Create/Get/Update/List 四个独立工具。

P05 的固定“连续三轮未更新就提醒”是教学机制。Claude Code 没有同样的固定轮数，更接近的行为是在已完成 3 项以上 TODO 却没有 verification 项时追加验证提醒；第 5 章刻意选择固定轮数，是为了让离线测试能确定性地证明“提醒只出现一次、不污染正式历史”。

Anthropic 在《Building Effective Agents》中把“显式展示 Agent 的规划步骤”列为实现透明性的核心原则（https://www.anthropic.com/engineering/building-effective-agents）。

P05 的完整快照工具结果就是这个原则的可测试落点：模型每次提交的 TODO 都进入正式消息历史，读者和运行时都能看到它当前认为的完整计划。

---

## 十四、与《ai-agent-book》的对照：注意力检索、状态栏与轨迹边界

本章的实现与《ai-agent-book》第二章、第三章和第五章的几个关键概念直接呼应。把它们放在一起看，更容易理解 `todo_write` 为什么值得这样设计。

**状态栏：把算好的结论直接加进上下文。**《ai-agent-book》第二章介绍 Agent 状态栏时提出：不要让模型在海量原始信息中被动检索，而是把经过提炼的结构化状态主动放到模型面前。P05 的完整 TODO 快照正是这个原则的可测试落点。

每次 `todo_write` 返回的不是“已更新 3 项”，而是“截至此刻，完整计划是什么”；模型下一轮不需要从头翻阅系统提示或早期对话，只需要查看最近一次工具结果。

**注意力擅长检索，不擅长统计与推理。**《ai-agent-book》第二章还指出，Transformer 的注意力机制本质上像软检索：它擅长在已有内容中定位与当前查询相关的片段，却不擅长在大量 token 之间做统计归纳。

这解释了系统提示词为什么会在长上下文中逐渐失效：初始计划只出现一次，随着工具输出增长，它要与越来越多的局部信息竞争注意力。TODO 快照把“当前进度”从需要模型自己推理的状态，变成了可以直接检索的显式 JSON。

**轨迹保持 append-only，临时提醒不走历史。**《ai-agent-book》第三章把轨迹定义为按时间顺序追加、只增不改的完整事件记录。P05 的 reminder 没有写入 `#history`，只在 `beforeModel()` 构造当次请求时临时加入，因此不会在轨迹中累积过期提醒。

这个设计和“完整快照作为最近一次工具结果”共同保证：正式历史始终是真实工具调用与结果的配对记录，临时提示不会污染审计和重放。

**显式状态优于隐式期望。** 把系统提示写成“不要忘记计划”，是给模型一条原则，但无法保存“当前进度”。P05 用结构化工具把状态变成可校验、可断言、可替换的数据契约：模型提交完整 JSON，运行时校验并替换，非法输入不改变旧状态。这正是《ai-agent-book》第二章“主动提供经过提炼的结构化知识”的工程化表达。

这段对照不是要说明 P05 已经实现书中全部能力，而是说明它选择了最基础、最可验证的一条路径：把容易丢失的计划状态从长上下文中抽出来，变成模型每次都能稳定看到的工具结果。

---

## 本章完整改动

相较第 4 章，第 5 章只增加一条实现路径：

- `code/chapters/ch05/src/features/todos.ts`：Zod 契约、会话快照和 reminder 计数；
- `code/chapters/ch05/src/core/loop.ts`：模型调用前读取 guidance，工具轮后记录工具名；
- `code/chapters/ch05/src/bootstrap.ts`：每个 P05 会话创建并注册一个 tracker；
- `code/chapters/ch05/src/core/profiles.ts`：新增固定 `P05` 和 `todo` capability；
- `code/chapters/ch05/src/chapters/ch05.ts`：第五章固定入口；
- `code/chapters/ch05/tests/todos.test.ts`：TODO 领域行为和失败边界；
- `code/chapters/ch05/tests/ch05-todos.test.ts`：从组合根验证权限、消息和提醒接缝。

没有增加数据库、全局状态、增量更新协议或第二套 Loop。这个实现只解决当前问题：让单次 Agent 会话拥有一个可验证、可重复提交、不会被非法输入破坏的任务快照。

---

## 本章小结

### 三句话版本

1. 长任务里丢的不是「目标」，是「进度」——目标在开头出现过一次，进度从来没被显式记录。
2. 所以本章把进度做成结构化状态：模型每次提交**完整快照**，快照作为最新的 tool result 回到消息流。
3. 模型忘记更新时，运行时按「连续三个陈旧工具轮」注入一条**只进当次请求**的提醒。

### 一定要记住的七条

| # | 结论 | 出现在哪一节 |
| ---: | --- | --- |
| 1 | 整体替换没有隐式 merge：漏写一项 = 删除一项 | 二 |
| 2 | 校验全部通过后才做一次赋值，所以不存在半更新，也不需要回滚 | 二、九 |
| 3 | `readonly` 只管编译期，运行时要靠 `Object.freeze` | 二 |
| 4 | 快照和陈旧计数**都是**会话状态，做成单例会串台 | 四 |
| 5 | 按 assistant 消息计数，一轮三个调用只算一轮 | 六 |
| 6 | 「计数到 3」和「注入提醒」不在同一次请求；注入后立刻清零 | 六 |
| 7 | 提醒进 `ModelRequest`，不进 `#history` | 七 |

### 本章代码边界（明确「还没做什么」）

| 已经有 | 还没有 | 说明 |
| --- | --- | --- |
| 会话内 TODO 快照 | 磁盘持久化、跨会话恢复 | 进程退出即消失。第 9 章做文件记忆 |
| 单一整体替换工具 | 依赖关系、`blockedBy`、并发认领 | 第 12、17 章做任务 DAG |
| 固定「三轮」阈值 | 官方那种「完成 3 项却没有 verification 项就提醒」 | 刻意选固定轮数，让离线测试能确定性断言 |
| 一条固定文案的提醒 | 按任务内容动态生成的提醒 | **不做**。动态文案无法精确断言 |
| 提醒只注入当次请求 | 提醒进入正式历史 | **不做**。会在轨迹里累积过期提示 |
| `effect: "write"` 参与权限决策 | 用 effect 粗暴匹配审批规则 | 规则精确匹配工具名，见第八节 |
| 单一会话的 tracker | 全局共享的 tracker | **不做**。见第四节的串台示例 |

### 检查你是否真的读懂了

1. 模型提交 3 项计划，下一次只提交 1 项，另外 2 项会怎样？（二）
2. `content` 是 `"   "`（三个空格）时，为什么会失败？如果 schema 写成 `z.string().min(1).transform(trim)` 呢？（一）
3. 为什么快照要输出 `\u7f16\u5199` 而不是直接输出中文？（三、十）
4. `TodoTracker` 做成 `export const tracker = new TodoTracker()` 会出什么问题？至少说两个。（四）
5. 一条 assistant 消息里有 `read_file`、`glob`、`shell` 三个调用，计数加几？（六）
6. 提醒在第几次模型请求出现？为什么不是第 3 次？（六）
7. 在 `result.history` 里能搜到 `Keep the TODO list current` 吗？为什么？（七）
8. `todo_write` 的 effect 是 `write`，为什么不弹审批框？但审计里有东西吗？（八、十）
9. 模型提交 `{"todos":[{"content":"a","status":"working"}]}`，handler 跑了吗？旧快照变了吗？（九）

---

下一章会处理更大的任务：把一个目标交给独立子 Agent，在隔离历史中执行，再只把最终结果带回父 Agent。那解决的是上下文隔离，不是本章的会话内计划追踪。
