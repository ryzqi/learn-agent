上下文窗口就那么大。

这句话说起来轻松，但真正踩到这个边界时，体感完全不同。一个后端重构任务同时涉及认证、数据库、API 路由和测试，单个 Agent 处理到后半程，前半程的细节已经离开上下文。它不是突然变笨，而是物理上看不见了。

第 6 章的一次性子 Agent 可以把独立任务放进干净上下文，完成后只把结果带回 Lead。但有些工作不是「一问一答」：队友需要保留自己的历史，接收后续补充，异步汇报进展，并在完成一轮工作后继续存在。

第 14 章刚刚解决了「没有人说话时 Agent 也能动」。那条链路的另一端是**时间**。本章把另一端换成**另一个 Agent**：事件不再来自闹钟，而来自一个有名字、有历史、还活着的队友。

第 15 章实现的就是这组最小闭环：**持续 Teammate、持久 Mailbox，以及把 Mailbox 消息重新送入公共 Agent Loop 的 typed event 通道**。

先把范围钉死。本章只做**文本消息的持久双向投递**：Lead 多两个工具，队友只有四个工具，消息落在磁盘上而不是内存里。没有 `check_inbox`、没有结构化 request/response、没有权限冒泡、没有 shutdown 握手。事件回到 Loop 的那条通路完全复用第 13 章的 `EventInbox`——**本章不新建第二条事件通道，也不新建第二个 Loop**。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaF6JPZn9D3GrBeTiaicLug7PnHnHPsN1zSSlCNkxAjMYkp8CanZYiad2Vao3lqd2XIfBrafSqdlFwrxnqva4GFEHdmWaic8TIoFA2U/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清一次性子 Agent 和持续队友的差别不在「有没有并发」，而在**身份、历史、通信和生命周期由谁持有**。
2. 解释为什么「读文件 + 删文件」的收件箱是**至多一次**投递，以及它会在哪个具体窗口永久丢消息。
3. 说出「每消息一个文件 + 目录名就是状态」这个设计换来了什么，`ready/processing/done/quarantine` 四个目录分别代表什么事实。
4. 判断 claim 的权威 FIFO 键为什么是 `(created_at_utc, id)` 而不是文件名顺序，并举出一个按文件名排会排错的例子。
5. 解释为什么 mailbox 事件是**先进 canonical history 再 ack**，以及它是全章唯一保留「半状态」的事件类型。
6. 说清 `idle` 队友收到新消息时为什么能看到上一轮的对话，而 Alice 和 Bob 的历史为什么不会串线。
7. 说明队友的 `send_message` 里 `sender` 从哪来，为什么不能是工具输入的一个字段。
8. 判断一条队友汇报在 Lead 崩溃后会发生什么，以及为什么这是「至少一次」而不是「恰好一次」。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的一轮：模型 -> 工具 -> 结果回填 | 第 1 章 | 队友就是一个独立的 Loop，不是别的东西 |
| `ToolDefinition` / Zod `.strict()` schema | 第 2 章 | `spawn_teammate`、`send_message` 靠它守边界 |
| `ToolContext.identity` 是谁在调工具 | 第 3 章 | **本章重点之一**：队友的 sender 来自它，不来自输入 |
| 一次性子 Agent 的委派与返回 | 第 6 章 | 本章的对照组，读完你要能说出五个差别 |
| 文件锁 + 原子替换 + fail closed | 第 12、14 章 | Mailbox 的每一次状态迁移都沿用同一套纪律 |
| `RuntimeEvent` / `EventInbox` / `RuntimeEventPump` | 第 13 章 | **最关键的前置**，队友消息走的就是这条通路 |
| `JobSupervisor.startManaged()` 与关闭顺序 | 第 13 章 | 每个队友 worker 都是受管任务，不是野生 Promise |
| `runEvents()` 与 history-before-ack | 第 14 章 | 本章在它上面只加了一处：mailbox 的半状态 |
| Profile 能力位与组合根按能力注入 | 第 5～14 章 | P15 靠 `teammate` + `mailbox` 两个能力位接上运行时 |

只来得及补一章的话，补第 13 章的「事件怎样回到同一个 Loop」。本章新增的东西集中在**事件从哪来**，而**事件怎么回去**在第 13、14 章已经定型。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| Lead | 你直接对话的那个 Agent，名字固定是 `lead`，是保留名 |
| Teammate（队友） | 一个有名字、有独立历史、能长期存在的 Agent |
| Mailbox（收件箱） | 每个 Agent 名下的一组磁盘目录，消息就存在里面 |
| `MailboxMessage` | 一条消息的领域对象，同时也是一个 `RuntimeEvent` |
| `kind` | 消息种类，只有 `task` / `message` / `result` 三种 |
| claim（认领） | 把一条消息从 `ready` 原子搬到 `processing`，等于取得租约 |
| 租约（lease） | 「这条消息现在归我处理」的状态，未确认前不会被别人拿走 |
| ack（确认） | 「这条消息我处理完了」，从 `processing` 搬到 `done` |
| release（归还） | 放弃租约，从 `processing` 搬回 `ready`，保留重放机会 |
| quarantine（隔离） | 坏消息或处理失败的输入，搬到一边留证据，不堵住队列 |
| 至少一次投递 | 崩溃可能让同一条消息重放，但不会静默丢掉 |
| `idempotencyKey` | 本章就是消息 UUID，传给这一轮所有工具用于去重 |
| event-only turn | 没有用户输入、由事件触发的一次模型回合 |
| 半状态 | 事件已进 history 但 ack 还没成功的中间态，只有 mailbox 有 |
| `wakeup` | 新消息到达时请求宿主跑一次 `runEvents()` 的回调 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一次完整协作的轨迹     ← 直接看真实输出，不先讲抽象
  ↓
② 问题的本质                 ← 为什么一个进程内 dict 不够
  ↓
③ 只增加两个工具             ← 以及为什么没有 check_inbox
  ↓
④ MailboxMessage 建模        ← 消息和事件是同一个对象
  ↓
⑤ 每消息一个文件             ← 目录名就是状态，本章最重要的存储决定
  ↓
⑥ FIFO、恢复与坏消息隔离     ← 三个容易被忽略的正确性边界
  ↓
⑦ TeammateRuntime            ← 一个名字，一个持续 Runner
  ↓
⑧ 队友工具裁剪为四个         ← 少给工具不等于绕过治理
  ↓
⑨ Inbox 注入                 ← 先入历史，再确认；mailbox 的半状态
  ↓
⑩ 共享一套运行时             ← 一个 Inbox，一个 supervisor
  ↓
⑪ 失败与关闭                 ← 谁负责把在途工作放回去
  ↓
⑫ 接进组合根                 ← 能力位、18 个工具、CLI 接线的现状
  ↓
⑬ 运行 + 15 个实验 + 排错    ← 把上面每条结论都亲手验一遍
```

---

## 先看一次完整协作的轨迹

抽象讲「异步通信」很容易听不懂。所以先看一次真实运行的输出——下面每一行都是本章实验 6 跑出来的，不是我推出来的。

Lead 要让 Alice 先读现有说明并列出缺口，收到缺口后再补充内容：

```text
1. spawn_teammate(alice)
   -> {"name":"alice","role":"writer","status":"running"}
2. alice 后台跑完 -> result 写进 lead/ready -> 发布事件 -> 唤醒 Lead
   [wakeup 1] Lead.runEvents() -> 收到缺口清单
   alice 状态 = idle
3. Lead 的 canonical history：
     1. user      {"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-000
     2. assistant 收到缺口清单
```

第 3 步那条 `user` 消息的完整内容是：

```json
{
  "runtime_event": {
    "kind": "mailbox",
    "message_id": "00000000-0000-4000-8000-000000000702",
    "sender": "alice",
    "recipient": "lead",
    "message_kind": "result",
    "content": "README 缺少恢复与失败边界说明",
    "created_at_utc": "2026-07-30T08:00:00.000Z"
  }
}
```

**这就是本章的核心动作。** Alice 的汇报不是后台线程往终端打的一行日志，而是一条持久消息，最后以 `role: "user"` 的结构化事件进入了 Lead 的对话历史。打印只能让人看到；进入 history 才能让模型继续协作。

接着 Lead 追加第二轮：

```text
4. send_message(to=alice) 追加第二轮
   [wakeup 2] Lead.runEvents() -> 收到补充结果
   alice 只被创建过 1 个 Runner
   alice 第 2 轮 messages（第 1 轮还在）：
     system    You are alice, serving as writer.
     user      阅读 README 并列出缺口
     assistant README 缺少恢复与失败边界说明
     user      只补充恢复与失败边界
5. 收尾：alice 状态 = idle，Lead history 长度 = 4
   lead 邮箱剩余 ready = 0
   lead 邮箱 done = 2
   alice 邮箱 done = 2
```

请重点看第 4 步里 Alice 的第二轮请求：**第一轮的 user/assistant 还在**。这不是 Lead 帮她拼接的上下文，而是同一个 `AgentRunner` 从头到尾持有的同一份历史。「只被创建过 1 个 Runner」这行输出就是证据。

### 和一次性子 Agent 那条轨迹的关键差别

如果这件事用第 6 章的 `task` 工具做，轨迹会是这样：

```text
Lead -> task("阅读 README 并列出缺口")
        子 Agent 全新上下文，跑完，返回文本
        子 Agent 消失
Lead -> task("只补充恢复与失败边界")
        又一个全新上下文，不知道上一次说了什么
```

第二次委派必须由 Lead 把第一次的结论重新讲一遍，否则子 Agent 一无所知。而本章的 Alice 自己记得。

三个可以直接观察的差别：

| 观察点 | 一次性子 Agent | 第 15 章 Teammate |
| --- | --- | --- |
| 第二轮请求里有第一轮吗 | 没有 | 有（上面第 4 步的输出） |
| 结果怎么回到 Lead | 工具返回值，同一轮同步拿到 | 持久消息 + 事件，异步进入 history |
| 中间崩了会怎样 | 这次委派整个丢掉 | 消息还在磁盘上，重启后接着处理 |

第三条尤其值得留意，它是本章后面一半篇幅在解决的问题。

---

## 问题的本质：协作不是多开几个模型调用

一次性子 Agent 和持续队友的差别，不在于有没有并发，而在于**身份、历史、通信和生命周期由谁持有**。

| 边界 | 一次性子 Agent | 第 15 章 Teammate |
|---|---|---|
| 生命周期 | 完成一次委派后返回 | 完成后进入 `idle`，可接收后续消息 |
| 对话历史 | 每次委派独立 | 每个名字绑定一个独立 `AgentRunner`，后续继续复用 |
| 通信 | 返回最终结果 | 通过持久 Mailbox 双向发送文本消息 |
| 公开工具 | Lead 的 `task` | Lead 的 `spawn_teammate`、`send_message` |
| 状态 | 运行或结束 | `running`、`idle`、`failed`、`shutdown` |

### 为什么一个进程内的 dict 不够

最容易想到的实现是这样：

```typescript
// 看起来够用，但不够
const inbox = new Map<string, string[]>();
inbox.get("lead")?.push("alice 说：README 缺少恢复说明");
```

它在三个地方会出问题，而且都不是理论问题：

**第一，进程退出等于全部丢失。** 队友跑了三分钟的成果，进程一关就没了。这在「一问一答」时无所谓，因为一问一答本来就活在一次调用里；但持续队友的意义恰恰是**跨调用存在**。

**第二，读和处理之间有窗口。** 假设改成「读文件，然后删文件」：

```text
t0  读到消息内容
t1  ← 进程在这里崩溃
t2  把消息写入 history（永远没执行）
```

消息在 `t0` 已经从磁盘消失，在 `t2` 之前又没进 history。它**永久消失，而且没有任何痕迹**。这叫至多一次投递（at-most-once）。

**第三，多个 writer 追加同一个文件要额外解决三个问题。** 如果让所有人往同一个 `.jsonl` 里追加：并发写会交错，崩在半行会留下不可解析的残行，多个读者还要各自维护游标。这三件事都能解决，但解决完你会发现自己写了一个小型消息队列。

### 所以本章的拆法

本章没有实现一个 `MessageBus` 加 `read_text()` / `unlink()`，而是把协作拆成四个明确边界：

```text
Lead 工具：spawn_teammate / send_message
    |
    v
FileMailboxStore
  每消息一个文件，ready -> processing -> done/quarantine
    |
    v
TeammateRuntime
  持有队友 Runner、状态和 worker，并组合 Mailbox/Cron event
    |
    v
AgentRunner.runEvents()
  把 typed message 注入 canonical history，再执行 event-only turn
```

每一层的职责可以一句话说清：

| 层 | 只负责一件事 | 不负责 |
| --- | --- | --- |
| 工具层 | 校验输入、注入可信 sender | 不知道消息存在哪 |
| `FileMailboxStore` | 消息的持久化和状态迁移 | 不知道队友是什么 |
| `TeammateRuntime` | 队友的生命周期和 worker 循环 | 不知道 Loop 长什么样 |
| `AgentRunner` | 事件进 history、跑一轮模型 | 不知道消息从哪来 |

相关实现集中在以下文件：

- [Mailbox 领域模型](code/chapters/ch15/src/features/mailbox.ts)
- [每消息一文件的 store](code/chapters/ch15/src/adapters/mailbox-json.ts)
- [持续队友运行时](code/chapters/ch15/src/features/teammates.ts)
- [typed EventInbox](code/chapters/ch15/src/core/events.ts)
- [公共 Agent Loop](code/chapters/ch15/src/core/loop.ts)
- [章节组合根](code/chapters/ch15/src/bootstrap.ts)
- [CLI 组合入口](code/chapters/ch15/src/cli.ts)

第 15 章严格等于第 14 章已有能力，再增加 `teammate` 和 `mailbox`。Cron、后台任务、权限、Hook、恢复和前面章节的工具都继续存在，不另起一套 Loop。

---

## 只增加两个工具

Lead 相对第 14 章只追加两个工具：

```typescript
export const spawnTeammateInputSchema = z
  .object({
    name: z.string(),
    role: z.string(),
    prompt: z.string(),
  })
  .strict();

export const sendMessageInputSchema = z
  .object({
    to: z.string(),
    content: z.string(),
  })
  .strict();
```

| 工具 | 必填字段 | 可观察结果 |
|---|---|---|
| `spawn_teammate` | `name`、`role`、`prompt` | 创建独立 Runner，把首个 `task` 消息持久写入队友 Mailbox，并异步启动 worker |
| `send_message` | `to`、`content` | 把 `message` 持久写入 Lead 或已存在队友的 Mailbox |

`.strict()` 表示多一个字段就整体失败，不是「忽略未知字段」。这一条和第 12、14 章的纪律一致：**要么完整可信，要么整体不可用。**

### 名字必须是安全的小写 slug

Agent 名不是随便一个字符串，因为它**直接作为目录名落在磁盘上**。校验规则是一条正则：

```typescript
const AGENT_NAME = /^[a-z0-9]+(?:-[a-z0-9]+)*$/u;
```

再叠加一层 Windows 保留名与路径片段检查。实际效果（实验 1 的真实输出）：

```text
alice          -> 接受 alice
api-writer     -> 接受 api-writer
writer-2       -> 接受 writer-2
a              -> 接受 a
x1-y2-z3       -> 接受 x1-y2-z3
Lead           -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
../lead        -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
lead/other     -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
nul            -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
trailing.      -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
two_words      -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
-lead          -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
lead-          -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
```

逐条解释为什么拒绝：

| 被拒的名字 | 如果放过会怎样 |
| --- | --- |
| `Lead` | 大小写混合在 Windows 上和 `lead` 是同一个目录，在 Linux 上不是。同一份代码在两个平台行为不同 |
| `../lead` | 目录穿越，消息会写到 workspace 外面 |
| `lead/other` | 变成两层目录，`recipient` 和路径不再一一对应 |
| `nul` | Windows 保留设备名，创建目录会失败或行为诡异 |
| `trailing.` | Windows 会静默去掉结尾的点，`trailing.` 和 `trailing` 指向同一个目录 |
| `two_words` | 下划线本身无害，但允许它就要回答「大小写呢」「中文呢」，边界会一直往外滑 |
| `-lead` / `lead-` | 首尾连字符不影响文件系统，但会产生 `alice--bob` 这种歧义命名 |

`lead` 是保留名，不能被队友占用——它固定属于你直接对话的那个 Agent。

**校验发生在做任何工作之前。** 名字不合法时，不会先创建目录再回滚，也不会先起一个 worker 再杀掉。

### 18 个工具，两个新的排在最后

P15 的 Lead 工具序列完整保留 P14 前缀，只在末尾追加（实验 11 真实输出）：

```text
 1. shell
 2. read_file
 3. write_file
 4. edit_file
 5. glob
 6. todo_write
 7. task
 8. load_skill
 9. create_task
10. get_task
11. list_tasks
12. claim_task
13. complete_task
14. query_background_job
15. cancel_background_job
16. schedule_cron
17. spawn_teammate      ← 本章新增
18. send_message        ← 本章新增
```

顺序是被测试锁定的，不是巧合。`ch15-bootstrap.test.ts` 断言最后两个恰好是这两个——如果某次重构把新工具插到中间，测试会失败。这样保证「P15 = P14 + 2」这句话在代码里是真的，不只是文档里说得好听。

### 这里没有 check_inbox

这是本章和多数教学实现最明显的分歧点，值得单独说。

一个直觉的做法是给 Lead 一个 `check_inbox` 工具，让模型自己决定何时查收消息。它的问题是**把一个运行时职责交给了模型的判断**：

```text
模型没想到要查    -> 消息躺在磁盘上，没人处理
模型每轮都查一次  -> 每轮多一次工具调用，绝大多数返回空
模型查的时机不对  -> 消息迟到，但没有任何机制保证它最终被查到
```

「消息什么时候到达」是运行时能确切知道的事实，不需要猜。所以本章的做法是：Mailbox 是**事件源**，消息由 `TeammateRuntime` 发布到 `EventInbox`，走第 13 章那条已经定型的通路。Lead 的工具表里根本没有这个动作。

---

## MailboxMessage：先把消息建模成事件

一条消息不是随手拼出的字典，而是严格、不可变的领域对象：

```typescript
interface MailboxMessage extends RuntimeEvent {
  readonly id: string;
  readonly sender: string;
  readonly recipient: string;
  readonly kind: MailboxMessageKind;
  readonly content: string;
  readonly createdAtUtc: Date;
  readonly idempotencyKey: string;
}
```

注意 `extends RuntimeEvent`。**消息和事件不是两个对象，是同一个对象的两个视角。**

`kind` 只有三种：

| kind | 谁产生 | 用途 |
|---|---|---|
| `task` | `spawn_teammate` | 队友的第一条任务，创建时就写进它的收件箱 |
| `message` | `send_message` | Lead 或队友主动发送的后续文本 |
| `result` | 队友 worker | 一轮 Runner 执行完成后的最终结果，或可观察的失败结果 |

三种就够了，因为本章只做文本投递。结构化的 request/response 属于第 16 章。

### 五条校验，一条都不放过

| 字段 | 要求 | 不满足会怎样 |
| --- | --- | --- |
| `id` | 必须是规范 UUID（小写、带连字符、version 位为 4） | 抛 `Mailbox message id must be a canonical UUID` |
| `createdAtUtc` | 必须是能精确 round-trip 的 ISO `Z` 时间 | 解析失败，消息进 quarantine |
| `sender` / `recipient` | 走同一套 Agent 名校验 | 抛不安全名字错误 |
| `content` | 不能为空 | 抛错；且**原样保留**，不做隐式 `trim()` |
| 字段集合 | 精确等于六个键，多一个都不行 | 抛 `Mailbox message payload contains unsupported fields` |

`content` 不做隐式 trim 这条容易被当成小事。但如果 store 悄悄改写了消息正文，那么「持久化的消息」和「发送的消息」就不是同一条，后面所有基于内容比对的逻辑（比如幂等 ack）都会失效。

### 同一个对象，两个身份

```typescript
eventId = message.id;
idempotencyKey = message.id;
```

这两行让 `MailboxMessage` 不需要任何转换就能进 `EventInbox`。带来三个好处：

1. **Mailbox 和 Cron 共享同一个 typed `EventInbox`**，不需要第二条事件通道。
2. **Loop 的去重逻辑不用改**：它本来就按 `eventId` 去重。
3. **消息 UUID 天然成为 idempotency key**，这一轮所有工具都能拿到同一个稳定键。

进入 Loop 时，消息序列化成一条普通 user message：

```typescript
export function runtimeEventMessage(event: RuntimeEvent): UserMessage {
  return userMessage(JSON.stringify({ runtime_event: event.toPayload() }));
}
```

`toPayload()` 输出的是这个形状（实验 2 真实输出）：

```json
{
  "kind": "mailbox",
  "message_id": "00000000-0000-4000-8000-000000000701",
  "sender": "lead",
  "recipient": "alice",
  "message_kind": "task",
  "content": "阅读 README 并列出缺口",
  "created_at_utc": "2026-07-30T08:00:00.000Z"
}
```

外层的 `kind: "mailbox"` 是事件类型，内层的 `message_kind` 才是消息种类。Loop 靠外层那个字段识别 mailbox 事件——**不是靠文本前缀猜**，也不是靠 XML 标签包裹。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaHeWzDxUkIcqQBlIHWwdssjCnRZUiaStgM0jEibTb005NrjmYVIkBIJK8zqUibEbalibjYPLF0wY7FOG3sYk8dHJ1evMyfeGvekD5A/640?from=appmsg&watermark=1#imgIndex=2)

---

## 每消息一个文件，状态由目录表达

这是本章最重要的存储决定，所以先看它长什么样。

### 磁盘布局

Mailbox 数据位于当前 workspace：

```text
.agent_tutorial/
  .mailboxes.lock
  mailboxes/
    lead/
      ready/
      processing/
      done/
      quarantine/
    alice/
      ready/
      processing/
      done/
      quarantine/
```

`send()` 一条消息之后，实际长这样（实验 2 真实输出）：

```text
mailboxes/
  alice/
    done/   (空)
    processing/   (空)
    quarantine/   (空)
    ready/
      00000000-0000-4000-8000-000000000701.json
```

那个文件的完整内容：

```json
{
  "id": "00000000-0000-4000-8000-000000000701",
  "sender": "lead",
  "recipient": "alice",
  "kind": "task",
  "content": "阅读 README 并列出缺口",
  "created_at_utc": "2026-07-30T08:00:00.000Z"
}
```

六个字段，一个不多一个不少。中文按 UTF-8 原样存，不转义成 `\uXXXX`。

### 目录名就是状态

每条消息对应一个 `{uuid}.json`。它在哪个目录里，就是什么状态：

| 状态 | 含义 | 谁负责让它离开这里 |
|---|---|---|
| `ready` | 已持久写入，尚未被消费者认领 | 消费者 claim |
| `processing` | 已被一个消费者原子 claim，尚未确认 | 消费者 ack / release / quarantine |
| `done` | 已进入消费者的完成边界 | 没有人，它是终态 |
| `quarantine` | 文件或消息无效，或队友处理该消息失败 | 没有人，留作证据 |

**这个设计的好处是：查询状态不需要读文件内容。** 想知道 Alice 有几条待处理消息，`ls` 一下 `alice/ready/` 就够了。想知道哪条消息卡住了，看 `processing/` 里剩下什么。不需要解析、不需要维护索引、不需要担心索引和实际内容不一致——**目录结构本身就是索引**。

### 四态迁移实测

一条消息走完全程，每一步落在哪（实验 3 真实输出）：

```text
send        -> ready
claim       -> processing   返回 同一条消息
release     -> ready
claim 再次  -> processing
ack         -> done
ack 幂等重复 -> 返回 true，位置 done
ack 内容不同 -> 抛错 Mailbox message does not match completed message: 00000000-0000-4000-8000-000000000701
空邮箱 claim -> undefined
```

八行输出对应八条规则。最后三行值得单独说，后面「幂等 ack」那节会展开。

### 写入纪律：临时文件 + fsync + rename

发送时不是直接 `writeFile` 到目标路径，而是四步：

```text
① 在同目录建临时文件，用 wx 标志（已存在就失败，不覆盖）
② 写入 UTF-8 JSON
③ fsync（确保字节真的落到磁盘，不是只在页缓存里）
④ rename 提交到 ready/{id}.json
```

为什么要这么绕？因为 `rename` 在同一文件系统内是**原子**的：要么目标文件完整存在，要么完全不存在。而直接写入不是——写到一半崩溃会留下一个半截 JSON 文件，下一个读者会读到不可解析的内容。

如果第 ④ 步失败，目标消息和临时文件都不会残留。**不允许出现「源和目标都不存在」的半状态。**

### claim 不是「读完再删」

这是和 dict 方案最本质的区别：

```text
ready/{id}.json
    -- rename -->
processing/{id}.json
```

ack 同样通过 rename 完成：

```text
processing/{id}.json
    -- rename -->
done/{id}.json
```

消息在整个生命周期里**从来没有从磁盘上消失过**，它只是换了个目录。所以任何时刻崩溃，消息都还在，都能从目录名知道它当时处于什么状态。

对比一下两种方案在同一个崩溃点的表现：

| 崩溃时刻 | 读完就删 | 本章的 rename |
| --- | --- | --- |
| 拿到消息后、处理前 | 消息已删除，**永久丢失** | 在 `processing/`，重启恢复 |
| 处理完、确认前 | 消息已删除，无法判断是否处理过 | 在 `processing/`，重启重放一次 |
| 确认后 | 正常 | 在 `done/`，正常 |

第一行是至多一次，第二列是至少一次。**宁可重复，不可丢失**——这一条和第 14 章 cron 事件的取舍完全一致。

### 幂等 ack：一个真实的跨 runtime 时序

这是全章最容易被忽略的一个正确性问题，但它确实会发生。

设想两个 runtime 先后处理同一条消息：

```text
runtime A: claim 消息 X          -> X 在 processing
runtime A: （卡住 / 崩溃 / 网络慢）
runtime B: 启动，recoverProcessing -> X 回到 ready
runtime B: claim X               -> X 在 processing
runtime B: 处理完，ack           -> X 在 done
runtime A: 恢复过来，也要 ack X   -> processing/X 已经不存在了
```

最后那一步该怎么办？三种选择：

| 方案 | 后果 |
| --- | --- |
| 返回失败 | A 会认为消息没处理完，可能重新排队 —— 但它已经在 `done` 里了 |
| 无条件返回成功 | 如果 `done` 里那条其实是**另一条同 id 的不同消息**，冲突被静默吞掉 |
| **读出来比对**（本章） | 内容相同就幂等成功，不同就明确失败 |

代码实现是这样的：

```typescript
if (!(await pathExists(processing))) {
  if (destination !== MailboxState.Done) return false;
  const completed = join(directories[MailboxState.Done], filename);
  if (!(await pathExists(completed))) return false;
  const stored = await this.#loadMessage(completed, message.recipient);
  if (!equalMailboxMessages(stored, message)) {
    throw new MailboxStorageError(`Mailbox message does not match completed message: ${message.id}`);
  }
  await this.#assertUniqueId(paths, message.id, completed);
  return true;
}
```

实验 13 把这个时序完整跑了一遍：

```text
first.claim -> 0701
second.recoverProcessing -> 1
second.claim -> 0701
second.ack -> true
first.ack（旧消费者补 ack）-> true
first.ack 内容冲突 -> Mailbox message does not match completed message: 00000000-0000-4000-8000-000000000701
同 UUID 再 send -> Mailbox message id already exists: 00000000-0000-4000-8000-000000000701
```

倒数第二行是关键：内容一致时幂等成功，**内容不一致时大声失败**。旧消费者不会把已完成消息重新排队，同 ID 冲突也不会被静默吞掉。

### UUID 全局唯一

最后一行输出还揭示了另一条规则：同一个 UUID 在**所有 Agent、所有状态目录**中必须唯一。store 发现冲突会明确失败，不允许第二条消息覆盖第一条。

这比「每个邮箱内唯一」更严格。为什么？因为消息 id 同时是 `eventId` 和 `idempotencyKey`——如果两条不同消息共用一个 id，Loop 的去重会把第二条当成重复事件直接丢掉。

### 两把锁解决两个不同的问题

```text
跨进程：.agent_tutorial/.mailboxes.lock（proper-lockfile）
进程内：withProcessMutex（按 workspace 路径做 key 的 Promise 队列）
```

它们不能互相替代，因为解决的问题不同：

| 锁 | 防的是什么 | 没有它会怎样 |
| --- | --- | --- |
| `.mailboxes.lock` | 两个**进程**同时读写同一份状态目录 | 两个 session 可能同时 claim 同一条 ready 消息 |
| `withProcessMutex` | 同一进程内多个 `FileMailboxStore` 实例并发操作 | 文件锁能挡住，但会退化成大量重试，性能塌陷 |

文件锁的具体参数：

```typescript
const LOCK_STALE_MS = 30_000;   // 30 秒后认定持有者已死
const LOCK_UPDATE_MS = 10_000;  // 每 10 秒续一次租约
retries: { retries: 100, minTimeout: 10, maxTimeout: 10 }  // 最多重试 100 次，每次 10ms
```

`stale = 30s` 是给强杀准备的：进程被 `Ctrl+C` 之外的方式干掉时锁文件会残留，30 秒后下一个 session 可以接管，不需要手工删。

100 次 × 10ms 的重试是给 Windows 准备的：杀毒软件和索引服务会短暂占住文件，导致 `EBUSY` / `EPERM`。重试窗口约 1 秒，足够覆盖这类瞬时占用。

### 并发实测

12 个 writer 同时发、12 个 claimer 同时抢（实验 14 真实输出）：

```text
并发 send 12 条 -> 成功 12 条
并发 claim 12 次 -> 拿到 12 条，去重后 12 条
再 claim 一次 -> undefined
```

三行分别验证了：**不丢消息**（12 条都写进去了）、**不重复认领**（12 次 claim 拿到 12 条互不相同的消息）、**不虚发**（第 13 次 claim 老实返回空）。

---

## FIFO、恢复与坏消息隔离

三个容易被忽略的正确性边界，逐个说。

### FIFO 键不是文件名

文件名是 UUID，UUID 按字典序排和消息顺序没有任何关系。claim 的权威 FIFO 键是：

```text
(created_at_utc, id)
```

先比较 UTC 创建时间；时间相同时，用规范 UUID 文本稳定打破平局。比较函数就一行：

```typescript
createdAtUtc.valueOf() - other.createdAtUtc.valueOf() || id.localeCompare(other.id)
```

实验 4 构造了一个按文件名排会排错的场景：三条消息，其中 id 最小的那条创建时间最晚。

```text
发送顺序（写盘顺序）：
  0701  created=2026-07-30T08:00:01.000Z  晚 1 秒，但 id 最小
  0703  created=2026-07-30T08:00:00.000Z  同一时间，id 较大
  0702  created=2026-07-30T08:00:00.000Z  同一时间，id 较小
claim 顺序：
  1. 0702  created=2026-07-30T08:00:00.000Z  同一时间，id 较小
  2. 0703  created=2026-07-30T08:00:00.000Z  同一时间，id 较大
  3. 0701  created=2026-07-30T08:00:01.000Z  晚 1 秒，但 id 最小
按文件名排序会得到：0701, 0702, 0703（错的）
```

正确顺序是 `0702 -> 0703 -> 0701`：先按时间分组（两条 `08:00:00` 排在 `08:00:01` 前面），组内再按 id 定序。按文件名排会把最晚创建的 `0701` 排到最前面。

**为什么需要 id 作为第二排序键？** 因为同一毫秒内可以发出多条消息（上面 `0702` 和 `0703` 就是）。如果只按时间排，两条同时间消息的相对顺序取决于目录枚举顺序，而目录枚举顺序在不同文件系统、不同次运行之间**不保证一致**。加上 id 之后，顺序就是确定的：**重启后的顺序与目录枚举顺序无关。**

### processing 不是「已经完成」

运行时重建时必须恢复：

```text
processing -> ready -> 再次 claim
```

恢复发生在两个时机：

| 时机 | 恢复谁的邮箱 |
| --- | --- |
| `TeammateRuntime.start()` | Lead 的邮箱 |
| `spawn()` 创建队友时 | 该队友邮箱中遗留的 processing 消息 |

这是至少一次投递的具体实现：崩溃可能让同一消息重放，但不能因为进程中断把它静默丢掉。

实验 5 的最后两行验证了这一点：

```text
recoverProcessing -> 1 条回到 ready
再次 claim -> 合法消息
```

### 一条坏消息不能堵住整个收件箱

这是队列系统的经典失败模式：队头有一条无法解析的消息，后面所有正常消息全部卡住。

claim 和 recover 会逐个严格校验，四条规则：

- 文件必须是普通 `.json` 文件，文件名必须是规范 UUID。
- JSON 必须是 UTF-8，字段集合、消息 ID 和 recipient 必须与路径一致。
- 未知字段、坏 JSON、坏字节和路径错配会原子移入 `quarantine`。
- **隔离一个坏文件后继续寻找下一条合法消息**，不中断整次 claim。

实验 5 在 Alice 的收件箱里塞了四种坏文件加一条正常消息：

```text
claim 之前 ready 目录：
  00000000-0000-4000-8000-000000000701.json      ← 合法消息
  00000000-0000-4000-8000-0000000009fd.json      ← 多了一个 priority 字段
  00000000-0000-4000-8000-0000000009fe.json      ← recipient 写的是 bob
  00000000-0000-4000-8000-0000000009ff.json      ← JSON 不完整
  not-a-uuid.json                                 ← 文件名不是 UUID

claim 结果 -> 合法消息（id 0701）

claim 之后 ready：
  (空)
quarantine：
  00000000-0000-4000-8000-0000000009fd.json
  00000000-0000-4000-8000-0000000009fe.json
  00000000-0000-4000-8000-0000000009ff.json
  not-a-uuid.json
```

**一次 claim 就把四个坏文件全部清出了 ready，并且拿到了那条合法消息。** 收件箱没有被堵住。

四种坏文件里，`recipient` 写成 `bob` 那条特别值得注意：文件放在 `alice/ready/` 下，但内容说收件人是 `bob`。这种不一致必须当成坏消息处理——否则一条消息的「归属」就有两个说法，而两个说法都可能是被篡改的那个。

如果 quarantine 已有同名文件，store 会选择不冲突的新名字（追加 `.quarantine-{index}.json`），**保留两份证据，不覆盖旧故障现场**。排查问题时最怕的就是证据被后来的失败覆盖掉。

---

## TeammateRuntime：一个名字，一个持续 Runner

存储层讲完了，现在看谁在用它。

### spawn 不等待完成

`spawn_teammate` 返回时队友还在跑。运行时为每个合法且未占用的名字创建一个内部 `Worker` 记录：

```text
Worker
  ├─ Teammate 快照（name / role / status，冻结对象）
  ├─ AgentRunner（这名队友独占，持有全部历史）
  ├─ 当前受管 Promise task（undefined 表示没有在途循环）
  ├─ 当前 processing MailboxMessage（租约，close 时必须归还）
  ├─ AbortController（取消当前循环）
  └─ closeComplete / cleanupFailure（支持 close 重试）
```

状态快照是**整体替换**的冻结对象，不是就地修改。所以外部拿到的 `Teammate` 引用不会在你手上悄悄变化。

### 第一条任务也走 Mailbox

一个容易想到的偷懒做法是：`spawn` 时直接把 prompt 塞进队友 Runner 的 `messages[]`。本章没有这么做，而是先把它作为 `task` 消息落入队友 Mailbox，再由 worker claim 出来。

**多绕这一圈换来什么？** 三件事：

1. 第一条任务和后续消息走**完全相同**的路径，不需要两套逻辑。
2. `spawn` 之后立刻崩溃，任务还在磁盘上，重启能接着做。
3. 队友的第一轮和后续轮次拿到的 `idempotencyKey` 语义一致，都是消息 UUID。

worker claim 后调用：

```typescript
const result = await worker.runner.run(message.content, {
  idempotencyKey: message.id,
  signal,
});
await this.#store.send(worker.teammate.name, this.#leadName, result.finalText, MailboxMessageKind.Result);
if (!(await this.#store.ack(message))) {
  throw new MailboxStorageError(`Mailbox message is not processing: ${message.id}`);
}
```

三行代码，顺序有讲究：**先发 result，再 ack 输入**。反过来的话，在两步之间崩溃会导致输入已确认但结果没发出——那条任务就无声无息地消失了。按现在这个顺序，最坏情况是结果发了两次，而两次的 `idempotencyKey` 相同，下游可以去重。

那个稳定 UUID 会进入这一轮所有工具的 `ToolContext.idempotencyKey`。`AgentRunner.run()` 会在历史、Hook 和模型调用**之前**拒绝空键——不能让损坏的幂等上下文退化成一次普通执行。

### 状态机：完成不等于销毁

```text
spawn                -> running
一轮完成             -> idle
收到新消息           -> running
再次完成             -> idle
执行异常             -> failed
runtime close 完成   -> shutdown
```

关键在 `idle`：**Runner 没有被销毁。**

Lead 后续调用 `send_message(to="alice", ...)` 时，如果 Alice 处于 `idle`，运行时重新启动该 worker，但继续使用原来的 Runner。所以第二轮模型请求仍能看到第一轮的 user/assistant history——这就是本章开头那段轨迹里第 4 步展示的东西。

### 历史不会串线

Alice 和 Bob 各自持有不同 Runner。实验 7 同时启动两名队友，打印各自的模型请求：

```text
Lead 收到： [ 'alice=alice 完成', 'bob=bob 完成' ]

alice 的模型请求：
  system    You are alice, serving as writer.
  user      写文档
  工具 = send_message

bob 的模型请求：
  system    You are bob, serving as reviewer.
  user      审代码
  工具 = send_message
```

两份请求完全独立：不同的 system prompt（角色不同）、不同的 user 消息、各自的历史。Alice 看不到 Bob 在做什么，Bob 也看不到 Alice。

**这正是多队友的意义所在。** 如果两个队友共享上下文，那就没必要分成两个——直接一个 Agent 干两件事，还省一次模型调用。分开的价值恰恰在于**各自的上下文里只有自己那份工作**。

### 三种拒绝

实验 7 后半段验证了拒绝路径：

```text
并发同名 spawn： [ '成功', '失败 Teammate already exists: carol' ]
lead     (保留名) -> Teammate name lead is reserved for the lead
Alice    (大写) -> Invalid or unsafe Agent name; expected a safe lowercase slug
../x     (路径) -> Invalid or unsafe Agent name; expected a safe lowercase slug
send 给不存在的 zoe -> Unknown teammate: zoe
```

第一行是并发安全的证据：两个 `spawn("carol")` 同时到达，注册锁只允许一个成功。实现靠的是一条串行化的 Promise 链（`#registryTail`），把注册表的「读-改-写」排成队列。

如果没有这道锁会怎样？两个调用都读到「carol 不存在」，都创建 Worker，第二个覆盖第一个——第一个 Worker 的 Runner 变成孤儿，它认领的消息永远不会被 ack。

`failed` 和 `shutdown` 的队友也不能继续接收消息：

```text
向 failed 队友发消息 -> Teammate alice cannot receive messages while failed
```

### hasPendingWork 故意不算队友

这一条反直觉，但很重要：

```typescript
get hasPendingWork(): boolean {
  return this.#cronRuntime.hasPendingWork;
}
```

`TeammateRuntime.hasPendingWork` **不把「持续队友仍然存在」算作普通 turn 必须等待的工作**。

为什么？因为 `idle` 队友会一直存在。如果把它算成 pending work，那么只要你创建过一个队友，用户 turn 在等「系统空闲」时就可能**永远等不到**——`idle` 是稳定态，不是过渡态。

区分两件事：

| 概念 | 会不会自己结束 | 算 pending work 吗 |
| --- | --- | --- |
| 一个正在跑的后台任务 | 会 | 算 |
| 一次在途的 cron tick | 会 | 算 |
| 一个 idle 队友 | **不会**，它就在那等着 | 不算 |

---

## 队友工具严格裁剪为四个

Lead 继承前 14 章的完整工具面（18 个），队友没有。队友 Runner 只注册四个（实验 11 真实输出）：

```text
队友工具（4 个）：read_file, send_message, shell, write_file
队友 system prompt = "You are writer, serving as writer."
```

组装代码很短：

```typescript
const teammateTools = standardTools.tools.subset(["shell", "read_file", "write_file"]);
teammateTools.register(sendDefinition);
```

`subset` 只暴露显式声明的工具。它不是「隐藏」，而是这个 Runner 的工具表里**根本不存在**其余的定义——模型看不到，也就不会去调。

### 没有的那些，各有原因

| 缺的工具 | 为什么不给 |
| --- | --- |
| `spawn_teammate` | 团队结构固定为一个 Lead 加多个平级队友。允许队友生成队友，拓扑会变成不可控的树 |
| `edit_file`、`glob` | 队友能读能写就够完成本章的协作场景，工具面越小越容易推理它的行为边界 |
| `todo_write` | TODO 是 Lead 组织自己工作的手段，不是团队共享的看板 |
| `load_skill` | Skill 会带进大段上下文，与「队友上下文只装自己那份工作」冲突 |
| `task` | 一次性子 Agent 会产生第二层委派，和持续队友是两套并行的协作模型 |
| Task DAG 五个工具 | 队友从共享任务图自主找活属于第 17 章 |
| `schedule_cron` | 队友能定时唤醒自己会让生命周期复杂一个量级 |

### 工具少不等于绕过治理

这是最容易误会的一点。队友的四个工具**不是特殊通道**，它们和 Lead 的工具走完全相同的执行顺序：

```text
参数 schema 校验
  ↓
PreToolUse Hook
  ↓
PermissionPolicy 判决
  ↓
handler 执行
  ↓
PostToolUse Hook
```

每个队友 Runner 使用自己的名字作为 `ToolContext.identity`，并复用当前章节的 Hook、`PermissionPolicy`、Recovery 和 Compaction 边界。队友调 `shell` 时该弹的审批一样会弹，该记的审计一样会记。

### sender 不能伪造

队友的 `send_message` 输入里**只有 `to` 和 `content`**，没有 `from`：

```typescript
async #sendTool(input: SendMessageInput, context: ToolContext): Promise<ToolResult> {
  try {
    const message = await this.send({ ...input, sender: context.identity });
    return toolSuccess(JSON.stringify(message.toPayload()));
  } catch (error) {
    return toolError(errorCode(error, "mailbox_send_error"), errorMessage(error));
  }
}
```

`sender: context.identity` 这一行是全章最短但最重要的安全边界之一。

如果 `sender` 是工具输入的一个字段，那么 Alice 可以发一条声称来自 Bob 的消息，Lead 无法分辨。**身份必须由运行时观察，不能由被治理方申报**——这条原则和第 3 章 `ToolContext.identity` 的设计动机是同一个。

---

## Inbox 注入：先进入 canonical history，再 ack

现在把消息送回 Loop。这一节是本章和第 13、14 章接缝的地方。

### 完整顺序

队友写给 Lead 的消息会从 `ready` claim 到 `processing`，再以 `MailboxMessage` 原对象发布到共享 `EventInbox`。Loop 空闲时执行显式 event-only turn：

```text
① EventInbox drain 一条 typed RuntimeEvent（每次只取一个）
② 检查 eventId 是否见过（去重）
③ 序列化并追加到 Lead canonical history
④ 调用 TeammateRuntime.acknowledgeEvents()
   -> Mailbox processing -> done
⑤ 模型执行这一条 event turn
```

**第 ③ 和第 ④ 步的顺序不能倒过来。**

假设先 ack 再追加 history：

```text
t0  ack 成功，消息进 done
t1  ← 进程在这里崩溃
t2  追加到 history（永远没执行）
```

消息在 `done` 里，没人会再处理它；history 里没有它，模型永远不知道队友汇报过什么。**永久丢失，且没有任何痕迹。**

按正确顺序，同样的崩溃点：

```text
t0  追加到 history
t1  ← 崩溃
t2  ack（没执行）
```

消息还在 `processing`，重启时 `recoverProcessing` 把它搬回 `ready`，重新投递一次。最坏结果是**重复一次**，而重复由 `idempotencyKey` 兜底。

当前契约要求：ack 时，canonical history 中**已经存在且只存在一份**对应事件。

### mailbox 是唯一保留半状态的事件

这是本章在第 14 章的 Loop 上唯一的实质性改动，代码在 `#acceptNextRuntimeEvent`：

```typescript
if (isNewEvent) {
  this.#history.push(runtimeEventMessage(event));
  this.#seenEventIds.add(event.eventId);
}
try {
  await this.#eventPump.acknowledgeEvents(events);
} catch (error) {
  if (event.toPayload().kind !== "mailbox" && isNewEvent) {
    this.#history.pop();
    this.#seenEventIds.delete(event.eventId);
  }
  if (event.toPayload().kind === "mailbox") this.#pendingEventAcks.add(event.eventId);
  throw error;
}
this.#pendingEventAcks.delete(event.eventId);
```

对比两类事件在 ack 失败时的处理：

| 事件类型 | ack 失败后 | 为什么 |
| --- | --- | --- |
| cron 事件 | **回滚**：history 弹出，去重登记删除 | 事件还在 outbox 里，下次会重新产生，从头走一遍即可 |
| mailbox 事件 | **保留**：history 留着，登记进 `#pendingEventAcks` | 消息重发后只需补 ack，不该重复进模型 |

为什么 mailbox 要特殊处理？因为 `TeammateRuntime.acknowledgeEvents()` 在失败时会**把同一个事件重新发布到 EventInbox**：

```typescript
} catch (error) {
  if (!this.#queuedMessageIds.has(event.id)) {
    this.#inbox.publish(event);
    this.#queuedMessageIds.add(event.id);
  }
  throw error;
}
```

重发的事件 `eventId` 相同。如果 Loop 把 history 回滚了，重发时会被当成新事件，**再进一次 history、再跑一次模型**。所以正确做法是：history 留着，只把「这条还欠一个 ack」记下来，重发时跳过 history 和模型调用，直接补 ack。

这个中间态就是「半状态」：**已进历史，未确认**。它是 mailbox 独有的。

### 实测：ack 失败不会重复

实验 10 注入一个第一次必然失败的 ack：

```text
  ack 第 1 次：此刻 history 中 mailbox 事件条数 = 1
第 1 次 runEvents() 抛错 -> ack persist failed
  模型请求数 = 0（应为 0）
  history 中含该 id 的消息数 = 1
  ack 第 2 次：此刻 history 中 mailbox 事件条数 = 1
第 2 次 runEvents() -> 处理完成
  ack 总尝试次数 = 2
  模型请求数 = 1
  history 中含该 id 的消息数 = 1
```

三个数字是这个实验的全部意义：

- **模型请求数：0 → 1。** ack 没成功之前一次模型都没调。确认成功后才调，且只调一次。
- **history 中该消息数：1 → 1。** 重试没有产生第二条。
- **ack 尝试次数：2。** 第一次失败，第二次成功。

「一次可观察的 ack 持久化失败不会造成重复历史或重复模型 turn」——这句话现在有了三个具体数字支撑。

### 这不是「恰好一次」

必须说清楚边界：这是**至少一次接收 + Runner 内稳定 ID 去重**，不是对任意外部副作用的全局恰好一次承诺。

Mailbox message ID 会作为 idempotency key 传播下去，但真正有外部副作用的工具（比如往远端 API 发一个 POST）**仍需在自己的边界实现幂等**。运行时能保证的是「同一条消息重放时你拿到同一个键」，不能保证「你用这个键做的事只发生一次」。

---

## Mailbox、Cron 和 Background 共享一套运行时

P15 不创建第二个事件队列，也不创建第二个 task owner。

```text
EventInbox（一个）
  <- JobSupervisor 的 background event
  <- CronRuntime 的 CronEvent
  <- TeammateRuntime 的 MailboxMessage

JobSupervisor（一个）
  <- background job
  <- cron scheduler
  <- teammate worker
```

`TeammateRuntime` 通过**组合**已有的 `CronRuntime` 实现这件事：

| 方法 | 怎么实现 |
| --- | --- |
| `drainEvents` | 直接转给 `cronRuntime`（同一条 typed FIFO） |
| `waitForEvents` | 直接转给 `cronRuntime` |
| `hasPendingWork` | 直接转给 `cronRuntime` |
| `acknowledgeEvents` | **先让 Cron ack 属于它的事件，再逐条 ack mailbox 消息** |

最后一行是关键：一个共享队列里混着两种事件，**每种事件由自己的 owner 确认**。Cron 只处理 `isCronEvent(event)` 为真的，Mailbox 只处理 `isMailboxMessage(event)` 为真的。互不越界。

### 构造时就检查共享关系

看起来相似但彼此断开的资源是一类很难排查的 bug：代码跑得通、测试也能过，但事件写进了一个没人消费的 Inbox。所以检查放在构造器里：

```typescript
if (options.cronRuntime.supervisor !== options.supervisor) {
  throw new Error("cronRuntime must share the JobSupervisor");
}
if (options.cronRuntime.eventInbox !== options.inbox) {
  throw new Error("cronRuntime must share the EventInbox");
}
```

注意用的是 `!==`，**引用相等**，不是结构比较。两个「长得一样」的 EventInbox 实例是两个队列，这里必须是同一个对象。

实验 11 验证了这两道检查真的会拦住人：

```text
Cron 不同 supervisor -> cronRuntime must share the JobSupervisor
Teammate 不共享 inbox -> teammateRuntime must share the background supervisor, EventInbox, and CronRuntime
```

### Lead 不需要轮询

因为共享了这条通路，Lead 既不需要轮询 `.json` 文件，也不需要 `check_inbox`。

但要说清一个容易误解的地方：**P15 不会因为空闲 Lead 收到消息就自动启动新 turn。**

消息发布到 Inbox 后，由宿主显式调用 `AgentRunner.runEvents()`。三种情况：

| 情况 | 行为 |
| --- | --- |
| Lead 空闲，宿主调 `runEvents()` | 消费一条事件，跑一轮 event-only turn |
| Lead 正在处理用户 turn | Runner 的运行锁让 `runEvents()` 直接返回 `undefined`，消息留在 inbox |
| 用户 turn 的 drain 点碰到 mailbox 事件 | **暂存**，不混入当前 turn |

第三条的实现只有四行：

```typescript
if (event.toPayload().kind === "mailbox" && !allowContextEvents) {
  this.#deferredRuntimeEvents.push(event);
  events = this.#eventPump.drainEvents(1);
  continue;
}
```

`allowContextEvents` 只在 `runEvents()` 里是 `true`。普通 `run()` 调进来时是 `false`，所以 mailbox 事件会被推进 `#deferredRuntimeEvents` 等着。

**为什么用户 turn 不顺手处理掉？** 因为用户问的是一件事，队友汇报的是另一件事。把它塞进当前 turn，模型可能在回答用户问题的中途拐去讨论队友的汇报。事件应该有自己的 turn 边界。

实验 9 完整验证了这个延后行为：

```text
user turn -> 处理了用户请求
  模型请求数 = 1
  本次请求里有 mailbox 事件吗 = false
  history 长度 = 2
runEvents() -> 处理了 mailbox 事件
  模型请求数 = 2
  history：
    1. user      普通用户请求
    2. assistant 处理了用户请求
    3. user      {"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-0000
    4. assistant 处理了 mailbox 事件
空 inbox 再 runEvents() -> undefined
重复发布同一事件后 runEvents() -> undefined，模型请求数仍 = 2
```

四段输出，四条结论：用户 turn 里没有 mailbox 事件；`runEvents()` 才处理它；空 inbox 时老实返回 `undefined`；**已确认过的事件被重复发布也不会再进模型**（去重生效）。

最后补一句避免混淆：**Cron 继续保留 P14 已有的自动 wakeup，这不是 Mailbox 在 P15 新增的行为。** 关于 CLI 里 mailbox wakeup 的实际接线状况，「接进组合根」那节会讲清楚。

---

## 失败与关闭：谁负责把在途工作放回去

分布式系统里最难的部分不是成功路径，是失败路径。这一节把三种非正常结束逐个走一遍。

### 失败也是一条消息

队友执行崩了怎么办？答案是：**崩溃也走 Mailbox。**

```typescript
} catch (error) {
  await this.#store.quarantine(message, errorMessage(error));
  this.#setStatus(worker, TeammateStatus.Failed);
  try {
    await this.#store.send(
      { sender: worker.name, recipient: LEAD_NAME,
        kind: MailboxMessageKind.Result,
        content: `Teammate ${worker.name} failed: ${errorMessage(error)}` },
    );
  } catch { /* best-effort */ }
  await this.#notifyLead();
}
```

三件事按顺序发生：输入消息进 `quarantine`（带上失败原因，不回 `ready`，避免同一条任务无限重试打崩队友）、队友状态置 `failed`、给 Lead 发一条 `result` 说明失败。

实验 8 的输出：

```text
Lead 收到：
  {"kind":"mailbox","message_id":"00000000-0000-4000-8000-000000000702",
   "sender":"alice","recipient":"lead","message_kind":"result",
   "content":"Teammate alice failed: model exploded",
   "created_at_utc":"2026-07-30T08:00:00.000Z"}
alice 状态 = failed
alice/quarantine = 1 个文件
向 failed 队友发消息 -> Teammate alice cannot receive messages while failed
```

请注意失败通知的 `message_kind` 是 `result`，**不是**某个新的 `error` 类型。这是刻意的：Lead 不需要为「失败」写一条独立的处理分支，它只是收到一条内容是失败说明的汇报，然后照常决定下一步——重试、改派、还是放弃。**用信息内容表达异常，而不是用协议类型表达异常。**

最后一行也重要：`failed` 队友不再接收消息。这条边界防止 Lead 把新任务发给一个已经坏掉的队友，让消息堆在一个永远不会被 claim 的 `ready` 目录里。

那条 best-effort 的 `try { … } catch {}` 是全章唯一被允许的空 catch：**失败通知本身失败时，不应该覆盖原始错误**。原始错误已经写进 quarantine 文件，那是权威记录。

### 关闭要把在途工作放回去

关闭时，正在执行的队友消息**必须回到 `ready`**，不能留在 `processing`，也不能丢。

```typescript
if (this.#closed || signal.aborted) {
  await this.#store.release(message);
  return;
}
```

实验 12 的输出：

```text
close 前 alice 状态 = running
close 后 alice 状态 = shutdown
原任务消息回到 ready 并可再次 claim = {"content":"长任务","kind":"task"}
close 后 spawn -> TeammateRuntime is closed
```

`release` 和 `quarantine` 的区别正好体现了两类失败的差别：

| 结束方式 | 消息去向 | 语义 |
| --- | --- | --- |
| 队友执行抛错 | `quarantine` | 这条消息本身有问题或处理不了，别再试 |
| 主动关闭 / abort | `ready` | 这条消息没问题，只是没轮到它做完，下次接着做 |

区分这两者的判断依据是 `this.#closed || signal.aborted`——**关闭是我们主动发起的，不算消息的错。**

`close()` 之后 `spawn_teammate` 会明确报 `TeammateRuntime is closed`，而不是静默创建一个不会被执行的队友。

### 关闭顺序：反向

组合根按创建顺序登记资源，按**反向**关闭：

```typescript
const resources = [backgroundSupervisor, cronRuntime, teammateRuntime];
// 关闭时反向遍历：teammateRuntime -> cronRuntime -> backgroundSupervisor -> model
```

为什么必须反向？因为依赖方向是单向的：

```text
TeammateRuntime  ──依赖──>  CronRuntime  ──依赖──>  JobSupervisor
```

如果先关 `JobSupervisor`，正在收尾的队友 worker 会发现自己的执行器已经没了；先关 `TeammateRuntime`，队友把在途消息 `release` 回 `ready`，然后 Cron 收尾，最后 supervisor 确认没有残留 job。**每一层关闭时，它依赖的东西还活着。**

这和 `try/finally` 的嵌套顺序、C++ 的析构顺序、Go 的 `defer` 栈是同一个道理。

---

## 接进组合根与两个入口

### P15 = P14 + 两个能力位

```text
P14 能力位 = artifacts, background, compaction, cron, dynamic_prompt, files,
             hooks, loop, memory, policy, powershell, recovery, skills,
             subagent, task_dag_json, todo, tool_registry
P15 能力位 = 上列 + mailbox, teammate
P15 - P14 = ["teammate","mailbox"]
```

（实验 11 输出。）没有替换、没有删除、没有改语义，只是加了两位。

### 四道配对检查

能力位和实例必须成对出现，否则组合根**直接拒绝启动**：

| 情况 | 报错 |
| --- | --- |
| P15 但不传 `teammateRuntime` | `teammateRuntime is required for chapter 15 or later` |
| P14 却传了 `teammateRuntime` | `teammateRuntime requires chapter 15 or later` |
| `cronRuntime` 用了别的 supervisor | `cronRuntime must share the JobSupervisor` |
| Teammate 没共享 inbox / supervisor / cron | `teammateRuntime must share the background supervisor, EventInbox, and CronRuntime` |

（同为实验 11 输出，四条都真的抛了出来。）

这是**fail-closed**：宁可启动时崩掉，也不要跑起来之后发现消息进了一个没人读的队列。启动时崩掉，错误信息里直接写着哪一对没配上；跑起来之后出问题，你要排查的是「为什么队友汇报了但 Lead 没反应」——那要难上一个数量级。

### 两个入口，一套装配

```powershell
# 入口 1：章节专用
npm run ch15 -- --prompt "创建一名 writer 队友，让她阅读 README 并汇报缺口"

# 入口 2：统一 CLI
npm run agent-tutorial -- run --chapter 15 --prompt "创建一名 writer 队友，让她阅读 README 并汇报缺口"
```

两条命令最终都落到 `cli.ts` 里同一个 `execute`，**共享全部装配逻辑**。区别只在 profile 从哪来：入口 1 的 `runProfile` 直接收到写死的 `P15`，入口 2 的 `runCli` 先解析 `--chapter` 再用 `profileForChapter` 查表。

沿用前几章的约定，`chapters/ch15.ts` 只有 5 行：

```typescript
// chapters/ch15.ts —— 全文
import { runProfile } from "../cli.js";
import { P15 } from "../core/profiles.js";

// 固定入口启用 P15 的队友协作与 mailbox 通信。
process.exitCode = await runProfile(P15, process.argv.slice(2));
```

### 说清一处现状：CLI 只接了 Cron 的 wakeup

读代码时你会在 `code/chapters/ch15/src/cli.ts:172` 看到这样一行：

```typescript
cronRuntime.bindWakeup(async () => { await runner.runEvents(); });
```

**`teammateRuntime.bindWakeup(...)` 没有被调用。**

这意味着在真实 CLI 里，队友汇报后 Lead **不会在同一次会话里自动开一个 event turn**。消息会被 `#publishLeadMessages()` claim 到 `processing` 并发布到 Inbox，但没有东西去触发 `runEvents()` 来消费它。

实验 14 把这个现状测了出来：

```text
第 1 次运行：Lead 回答 = 已经派 alice 去看 README 了
  alice 状态 = idle
  Lead history 长度 = 2（只有用户那一轮）
  关闭后 lead/ready = 0，lead/processing = 1，lead/done = 0
  -> 队友的 result 留在 processing，没有被这次会话消费

第 2 次运行：start() 后 lead/ready = 0，lead/processing = 1
  runEvents() -> 读到了上次 alice 的汇报
  history 第 1 条 = {"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-000000000702","sender":"ali
  lead/done = 1
```

好消息是**消息一条都没丢**。这正是前面所有磁盘纪律的价值所在：

```text
第 1 次进程                       第 2 次进程
────────────────                 ────────────────
result 写入 ready
claim -> processing
发布到 Inbox（没人消费）
进程退出 ──────────────────────>  start()
                                   recoverProcessing 把它搬回 ready
                                   #publishLeadMessages 重新 claim
                                   -> processing（所以还是显示 1）
                                 runEvents()
                                   -> 进 history，跑 turn，ack
                                   -> done = 1
```

**至少一次投递把跨进程的这段缺口补上了。** 一个内存 dict 在这里会直接丢消息。

如果你想在自己的实验里看到「Lead 自动响应队友」的完整闭环，就在自己的宿主代码里补上一行：

```typescript
teammateRuntime.bindWakeup(async () => { await runner.runEvents(); });
```

实验 6 的探测脚本就是这么做的，所以它能打印出连续两轮的自动协作轨迹。本章的实验里可以这样做；**本章不改 `cli.ts` 的代码**，先把现状记录清楚。

---

## 运行第 15 章

下面每一步都能照抄。命令都是 PowerShell。

### 第 0 步：确认环境

```powershell
node --version
npm --version
```

Node 需要 ≥ 20.12。低于这个版本，`node:test`、原生 ESM 解析和本章用到的一些 API 会出问题。

### 第 1 步：进入代码目录

```powershell
cd F:\笔记\Agent实操\code
```

后面所有命令都在这个目录里执行。

### 第 2 步：安装依赖

```powershell
npm install
```

只需要做一次。已经跑过前面章节的话可以跳过。

### 第 3 步：先跑测试，确认基线是好的

在配任何凭证之前先跑测试。测试全部离线，不需要 API key：

```powershell
npm run test:ch15
```

期望看到：

```text
Test Files  44 passed (44)
     Tests  335 passed (335)
  Duration  4.11s
```

输出里会夹着这么一行 stderr：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这是正常的。** `tests/ch15-entry.test.ts` 故意在不给凭证的情况下调入口，验证它会礼貌退出而不是抛栈。看到它说明这条断言在工作。

如果这一步就红了，先别往下走——问题在环境或依赖，不在你接下来要做的事。

### 第 4 步：配置模型凭证

第 15 章开启了 `recovery` 能力位，所以**四个变量都要有**，缺 `OPENAI_FALLBACK_MODEL` 也起不来：

```powershell
$env:OPENAI_BASE_URL  = "https://your-endpoint/v1"
$env:OPENAI_API_KEY   = "sk-..."
$env:OPENAI_MODEL     = "your-model"
$env:OPENAI_FALLBACK_MODEL = "your-fallback-model"
```

缺任何一个，程序会明确告诉你缺哪个：

```text
配置错误: Missing required settings: OPENAI_FALLBACK_MODEL
```

`$env:` 只对当前 PowerShell 窗口有效。换窗口要重新设。

### 第 5 步：真跑一次

```powershell
npm run ch15 -- --prompt "创建一名 writer 队友，让她阅读 README 并汇报缺口"
```

### 你应该看到什么

Lead 大致会走这样一条路径（具体措辞取决于模型）：

```text
① 调 spawn_teammate(name="writer", role="writer",
     initial_task="阅读 README 并汇报缺口")
   -> {"name":"writer","role":"writer","status":"running"}

② 立刻返回，Lead 继续自己的 turn
   （writer 在后台跑，Lead 不阻塞）

③ 本次会话结束
```

注意 ③：如上一节所说，CLI 只接了 Cron 的 wakeup，所以 writer 的汇报**不会在这次会话里被 Lead 读到**。它安全落在磁盘上，等下一次运行。

### 第 6 步：看磁盘上留下了什么

```powershell
Get-ChildItem -Recurse .agent\mailboxes | Select-Object FullName
```

会看到类似结构：

```text
.agent\mailboxes\lead\ready
.agent\mailboxes\lead\processing
.agent\mailboxes\lead\done
.agent\mailboxes\lead\quarantine
.agent\mailboxes\writer\ready
.agent\mailboxes\writer\processing
.agent\mailboxes\writer\done
.agent\mailboxes\writer\quarantine
```

看具体某个目录里有什么：

```powershell
Get-ChildItem .agent\mailboxes\lead\processing
Get-ChildItem .agent\mailboxes\writer\done
```

`writer\done` 里应该有那条 `initial_task`（writer 做完并 ack 了），`lead\processing` 里应该有 writer 的 `result`（已 claim、尚未消费）。

读一条消息的内容：

```powershell
Get-Content .agent\mailboxes\lead\processing\*.json | ConvertFrom-Json | Format-List
```

六个字段一目了然：`id`、`sender`、`recipient`、`kind`、`content`、`created_at_utc`。

### 第 7 步：再跑一次，看恢复

```powershell
npm run ch15 -- --prompt "看看有没有队友的汇报"
```

这次 `start()` 会先把上一次留在 `processing` 的消息恢复出来。跑完再看：

```powershell
Get-ChildItem .agent\mailboxes\lead\done
```

上一轮的 `result` 现在应该在 `done` 里了。**这就是至少一次投递在真实进程边界上的效果。**

### 常见报错排查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...` | 缺环境变量 | 按第 4 步设齐四个 |
| `Invalid or unsafe Agent name; expected a safe lowercase slug` | 名字不是安全 slug（大写、含 `/`、含 `_`、首尾 `-`、Windows 保留名） | 用 `writer`、`api-writer` 这类 |
| `Teammate name lead is reserved for the lead` | 想创建叫 `lead` 的队友 | 换个名字 |
| `Teammate already exists: xxx` | 同名队友已存在 | 直接 `send_message` 给它，不用重新 spawn |
| `Unknown teammate: xxx` | 给没创建过的队友发消息 | 先 `spawn_teammate` |
| `Teammate alice cannot receive messages while failed` | 目标队友已失败 | 看 `alice\quarantine` 里的失败原因 |
| `teammateRuntime is required for chapter 15 or later` | 自己装配时漏了实例 | 传入 `TeammateRuntime` |
| `cronRuntime must share the JobSupervisor` | 装了两个 supervisor | 复用同一个实例（引用相等） |
| 队友汇报了但 Lead 没反应 | CLI 只接了 Cron 的 wakeup | 正常现象，再跑一次会恢复；或自己 `bindWakeup` |
| `lead\processing` 里一直有文件 | 上次进程没走完 | 下次 `start()` 自动恢复，不用手动搬 |
| `TeammateRuntime is closed` | 关闭后还在 spawn | 检查生命周期，别在 `close()` 之后调 |
| `Mailbox message id already exists: ...` | 同一个 UUID 发了两次 | 检查自定义 `idGenerator` |

---

## 15 个动手小实验

前面所有结论都不用信我的。这一节把它们全部变成你自己机器上能跑出来的输出。

**全部实验不需要 API key**，因为它们用的是一个脚本化的假模型。

### 准备：先建一个探测脚本

```powershell
cd F:\笔记\Agent实操\code
New-Item -ItemType File -Path scripts\probe-ch15.mts
```

把下面这段写进 `scripts/probe-ch15.mts`。这是所有实验共用的头部，只写一次：

```typescript
// scripts/probe-ch15.mts —— 第 15 章探测脚本（临时用，跑完可删）
import { mkdtemp, rm, readdir, readFile, writeFile, mkdir } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";
import { FileMailboxStore } from "../chapters/ch15/src/adapters/mailbox-json.ts";
import { JsonBackgroundJobStore } from "../chapters/ch15/src/adapters/background-json.ts";
import { JsonCronStore } from "../chapters/ch15/src/adapters/cron-json.ts";
import { EventInbox } from "../chapters/ch15/src/core/events.ts";
import { AgentRunner } from "../chapters/ch15/src/core/loop.ts";
import { JobSupervisor } from "../chapters/ch15/src/features/background.ts";
import { CronRuntime } from "../chapters/ch15/src/features/cron.ts";
import { TeammateRuntime, TeammateStatus } from "../chapters/ch15/src/features/teammates.ts";
import {
  MailboxMessageKind, canonicalAgentName, createMailboxMessage,
} from "../chapters/ch15/src/features/mailbox.ts";
import { assistantMessage } from "../chapters/ch15/src/core/messages.ts";

async function withTempDir<T>(fn: (dir: string) => Promise<T>): Promise<T> {
  const dir = await mkdtemp(join(tmpdir(), "ch15-probe-"));
  try { return await fn(dir); } finally { await rm(dir, { recursive: true, force: true }); }
}

// 轮询等某个条件成立，最多 5 秒；异步实验必备
async function waitFor(predicate: () => boolean, label: string): Promise<void> {
  const deadline = Date.now() + 5_000;
  while (!predicate()) {
    if (Date.now() > deadline) throw new Error(`timeout waiting for ${label}`);
    await new Promise((resolve) => setTimeout(resolve, 5));
  }
}

// 让 UUID 可预测，输出才能对照
function seqId(start = 0x701): () => string {
  let next = start;
  return () => {
    const hex = (next++).toString(16).padStart(12, "0");
    return `00000000-0000-4000-8000-${hex}`;
  };
}

const EXPERIMENT = process.argv[2] ?? "all";
function section(name: string): boolean {
  const hit = EXPERIMENT === "all" || EXPERIMENT === name;
  if (hit) console.log(`\n===== ${name} =====`);
  return hit;
}

// 一个脚本化的假模型：按顺序返回预设回复，同时记录收到的请求
class ScriptModel {
  requests: any[] = [];
  #replies: string[];
  constructor(replies: string[]) { this.#replies = [...replies]; }
  async complete(request: any) {
    this.requests.push(request);
    return assistantMessage(this.#replies.shift() ?? "done");
  }
}

// 打印目录树，空目录标 (空)
async function tree(dir: string, prefix = ""): Promise<void> {
  const entries = (await readdir(dir, { withFileTypes: true }))
    .sort((a, b) => a.name.localeCompare(b.name));
  if (entries.length === 0) { console.log(`${prefix}(空)`); return; }
  for (const entry of entries) {
    console.log(`${prefix}${entry.name}${entry.isDirectory() ? "/" : ""}`);
    if (entry.isDirectory()) await tree(join(dir, entry.name), `${prefix}  `);
  }
}

// 共享装配：一个 inbox、一个 supervisor、Cron 与 Teammate 都挂上去
function makeRuntime(root: string, store?: any) {
  const inbox = new EventInbox();
  const supervisor = new JobSupervisor({
    store: new JsonBackgroundJobStore(root), inbox,
  });
  const cron = new CronRuntime({
    store: new JsonCronStore(root), inbox, supervisor,
    clock: { now: () => new Date("2026-07-30T08:00:00.000Z") },
  });
  const runtime = new TeammateRuntime({
    store: store ?? new FileMailboxStore(root, {
      idGenerator: seqId(), clock: () => new Date("2026-07-30T08:00:00.000Z"),
    }),
    inbox, supervisor, cronRuntime: cron,
  });
  return { inbox, supervisor, cron, runtime };
}
```

运行方式是 `npx tsx scripts/probe-ch15.mts <实验名>`，比如：

```powershell
npx tsx scripts/probe-ch15.mts names
```

不带参数会把全部实验依次跑一遍：

```powershell
npx tsx scripts/probe-ch15.mts
```

下面 15 个实验各自往脚本里追加一段。每段都带 `section("名字")` 开关，互不干扰。

---

### 实验 1（names）：什么名字会被拒绝

在脚本末尾追加：

```typescript
if (section("names")) {
  const cases = ["alice", "api-writer", "writer-2", "a", "x1-y2-z3",
    "Lead", "../lead", "lead/other", "nul", "trailing.", "two_words", "-lead", "lead-"];
  for (const raw of cases) {
    try {
      console.log(`${raw.padEnd(14)} -> 接受 ${canonicalAgentName(raw)}`);
    } catch (error) {
      console.log(`${raw.padEnd(14)} -> 拒绝 ${(error as Error).message}`);
    }
  }
}
```

```powershell
npx tsx scripts/probe-ch15.mts names
```

真实输出：

```text
alice          -> 接受 alice
api-writer     -> 接受 api-writer
writer-2       -> 接受 writer-2
a              -> 接受 a
x1-y2-z3       -> 接受 x1-y2-z3
Lead           -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
../lead        -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
lead/other     -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
nul            -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
trailing.      -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
two_words      -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
-lead          -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
lead-          -> 拒绝 Invalid or unsafe Agent name; expected a safe lowercase slug
```

**要验证的：**
- 名字直接当目录名用，所以校验必须挡住路径逃逸（`../lead`、`lead/other`）。
- Windows 保留名 `nul` 被拒——在 Windows 上 `nul` 是设备名，建不出普通目录。
- 结尾的 `.` 被拒——Windows 会静默去掉它，导致名字和目录名不一致。
- 大写、下划线、首尾连字符统统拒绝，只留一种规范写法。**同一个队友只有一种拼法。**

---

### 实验 2（disk）：一条消息在磁盘上长什么样

```typescript
if (section("disk")) {
  await withTempDir(async (root) => {
    const store = new FileMailboxStore(root, {
      idGenerator: seqId(), clock: () => new Date("2026-07-30T08:00:00.000Z"),
    });
    const message = await store.send({
      sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "阅读 README 并列出缺口",
    });
    console.log("send() 返回：");
    console.log("  id             =", message.id);
    console.log("  sender         =", message.sender);
    console.log("  recipient      =", message.recipient);
    console.log("  kind           =", message.kind);
    console.log("  eventId        =", message.eventId);
    console.log("  idempotencyKey =", message.idempotencyKey);
    console.log("\n磁盘布局：");
    await tree(join(root, "mailboxes"), "  ");
    const file = join(root, "mailboxes", "alice", "ready", `${message.id}.json`);
    console.log("\n文件内容：");
    console.log(await readFile(file, "utf8"));
    console.log("toPayload()：");
    console.log(JSON.stringify(message.toPayload(), null, 2));
  });
}
```

真实输出（节选关键部分）：

```text
send() 返回：
  id             = 00000000-0000-4000-8000-000000000701
  sender         = lead
  recipient      = alice
  kind           = task
  eventId        = 00000000-0000-4000-8000-000000000701
  idempotencyKey = 00000000-0000-4000-8000-000000000701

磁盘布局：
  alice/
    done/
      (空)
    processing/
      (空)
    quarantine/
      (空)
    ready/
      00000000-0000-4000-8000-000000000701.json

文件内容：
{
  "id": "00000000-0000-4000-8000-000000000701",
  "sender": "lead",
  "recipient": "alice",
  "kind": "task",
  "content": "阅读 README 并列出缺口",
  "created_at_utc": "2026-07-30T08:00:00.000Z"
}
```

**要验证的：**
- `id`、`eventId`、`idempotencyKey` **是同一个值**。一个 UUID 同时充当消息主键、事件去重键、幂等键。
- 四个状态目录一次建齐，不是用到才建。
- 文件里只有六个字段，没有 `status`、没有 `attempts`。**状态在目录名里，不在文件里。**
- `toPayload()` 把 `kind` 变成 `"mailbox"`（事件类型），把消息类型放到 `message_kind`。同一个对象换个身份说话。

---

### 实验 3（states）：四态迁移和幂等 ack

```typescript
if (section("states")) {
  await withTempDir(async (root) => {
    const store = new FileMailboxStore(root, {
      idGenerator: seqId(), clock: () => new Date("2026-07-30T08:00:00.000Z"),
    });
    const where = async (id: string) => {
      for (const state of ["ready", "processing", "done", "quarantine"]) {
        const files = await readdir(join(root, "mailboxes", "alice", state));
        if (files.includes(`${id}.json`)) return state;
      }
      return "(不存在)";
    };
    const sent = await store.send({
      sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "任务一",
    });
    console.log("send        ->", await where(sent.id));
    const claimed = await store.claim("alice");
    console.log("claim       ->", await where(sent.id),
      " 返回", claimed?.id === sent.id ? "同一条消息" : "别的消息");
    await store.release(claimed!);
    console.log("release     ->", await where(sent.id));
    const again = await store.claim("alice");
    console.log("claim 再次  ->", await where(sent.id));
    await store.ack(again!);
    console.log("ack         ->", await where(sent.id));
    console.log("ack 幂等重复 -> 返回", await store.ack(again!), "，位置", await where(sent.id));
    const tampered = createMailboxMessage({ ...again!.toJSON(), content: "改过的内容" });
    try { await store.ack(tampered); }
    catch (error) { console.log("ack 内容不同 -> 抛错", (error as Error).message); }
    console.log("空邮箱 claim ->", await store.claim("alice"));
  });
}
```

真实输出：

```text
send        -> ready
claim       -> processing   返回 同一条消息
release     -> ready
claim 再次  -> processing
ack         -> done
ack 幂等重复 -> 返回 true，位置 done
ack 内容不同 -> 抛错 Mailbox message does not match completed message: 00000000-0000-4000-8000-000000000701
空邮箱 claim -> undefined
```

**要验证的：**
- 四个状态之间只有四条合法边，每条边都是一次 `rename`。
- `release` 真的回到 `ready`，能被重新 claim。**这是关闭时保住在途工作的机制。**
- 重复 ack 同一条消息返回 `true` 不抛错——重放安全。
- 但 ack **内容不同**的同 ID 消息会**明确抛错**。这不是宽容，是「同一个 ID 必须指向同一份内容」的完整性检查。宽容会把数据损坏藏起来。
- 空邮箱 claim 返回 `undefined`，不是抛错。没活干是正常状态。

---

### 实验 4（fifo）：FIFO 按什么排序

```typescript
if (section("fifo")) {
  await withTempDir(async (root) => {
    const times = ["2026-07-30T08:00:01.000Z", "2026-07-30T08:00:00.000Z",
      "2026-07-30T08:00:00.000Z"];
    let index = 0;
    const store = new FileMailboxStore(root, {
      idGenerator: seqId(),
      clock: () => new Date(times[Math.min(index++, times.length - 1)]!),
    });
    for (const [i, label] of ["第一个发的", "第二个发的", "第三个发的"].entries()) {
      const m = await store.send({
        sender: "lead", recipient: "alice",
        kind: MailboxMessageKind.Task, content: label,
      });
      console.log(`发送 ${i + 1}: id=...${m.id.slice(-4)} created_at=${m.createdAtUtc.toISOString()}`);
    }
    console.log("\nclaim 顺序：");
    for (let i = 0; i < 3; i += 1) {
      const m = await store.claim("alice");
      console.log(`  ${i + 1}. ...${m!.id.slice(-4)}  ${m!.content}`);
    }
    console.log("\n按文件名排序会得到：0701, 0702, 0703（错的）");
  });
}
```

真实输出（关键部分）：

```text
发送 1: id=...0701 created_at=2026-07-30T08:00:01.000Z
发送 2: id=...0703 created_at=2026-07-30T08:00:00.000Z
发送 3: id=...0702 created_at=2026-07-30T08:00:00.000Z

claim 顺序：
  1. ...0702  第三个发的
  2. ...0703  第二个发的
  3. ...0701  第一个发的

按文件名排序会得到：0701, 0702, 0703（错的）
```

**要验证的：**
- 排序键是 `(created_at_utc, id)`，**不是文件名**。`0701` 的时间戳最晚，所以它最后被取。
- 时间戳相同时按 `id` 字典序打破平局：`0702` 在 `0703` 前面。
- 如果直接用 `readdir` 的默认顺序（近似文件名序），会得到 `0701, 0702, 0703`——**完全错误的顺序**。

顺便说明为什么必须有第二个排序键：文件系统的 `readdir` 顺序**不保证**跨平台一致。没有确定的 tie-breaker，同一毫秒到达的两条消息在不同机器上可能顺序不同，测试就会随机地红。

---

### 实验 5（quarantine）：一条坏消息不能堵住整个收件箱

```typescript
if (section("quarantine")) {
  await withTempDir(async (root) => {
    const dir = join(root, "mailboxes", "alice");
    for (const state of ["ready", "processing", "done", "quarantine"]) {
      await mkdir(join(dir, state), { recursive: true });
    }
    const base = {
      sender: "lead", recipient: "alice", kind: "task",
      created_at_utc: "2026-07-30T08:00:00.000Z",
    };
    const write = async (name: string, body: string) =>
      writeFile(join(dir, "ready", name), body, "utf8");
    await write("00000000-0000-4000-8000-000000000701.json", JSON.stringify(
      { ...base, id: "00000000-0000-4000-8000-000000000701", content: "合法消息" }));
    await write("00000000-0000-4000-8000-0000000009fd.json", JSON.stringify(
      { ...base, id: "00000000-0000-4000-8000-0000000009fd", content: "多字段", priority: 3 }));
    await write("00000000-0000-4000-8000-0000000009fe.json", JSON.stringify(
      { ...base, id: "00000000-0000-4000-8000-0000000009fe", recipient: "bob", content: "投错了" }));
    await write("00000000-0000-4000-8000-0000000009ff.json", "{ 这不是 JSON");
    await write("not-a-uuid.json", JSON.stringify(
      { ...base, id: "not-a-uuid", content: "ID 不是 UUID" }));

    const store = new FileMailboxStore(root);
    console.log("claim 之前 ready =", (await readdir(join(dir, "ready"))).length, "个文件");
    const claimed = await store.claim("alice");
    console.log("claim 拿到     =", claimed?.content);
    console.log("claim 之后 ready =", (await readdir(join(dir, "ready"))).length, "个文件");
    console.log("quarantine     =", (await readdir(join(dir, "quarantine"))).length, "个文件");
    console.log("recoverProcessing ->", await store.recoverProcessing("alice"), "条回到 ready");
    console.log("再次 claim     =", (await store.claim("alice"))?.content);
  });
}
```

真实输出：

```text
claim 之前 ready = 5 个文件
claim 拿到     = 合法消息
claim 之后 ready = 0 个文件
quarantine     = 4 个文件
recoverProcessing -> 1 条回到 ready
再次 claim     = 合法消息
```

**要验证的：**

四个坏文件，四种坏法，全部进 `quarantine`：

| 文件 | 坏在哪 | 为什么必须拒绝 |
| --- | --- | --- |
| `...09fd.json` | 多了 `priority` 字段 | Zod `.strict()` 拒绝未知字段。多出来的字段说明写它的代码和读它的代码版本不一致 |
| `...09fe.json` | `recipient` 是 `bob`，却在 `alice/ready` | 目录位置和字段内容矛盾。信错任何一边都会导致投递错误 |
| `...09ff.json` | 不是合法 JSON | 解析不了 |
| `not-a-uuid.json` | `id` 不是 UUID | ID 是幂等键的载体，格式不合规就没有幂等保证 |

- **合法消息照样被取到。** 坏消息不阻塞队列，这是本实验的全部意义。
- `recoverProcessing` 返回 1——只有那条正在 `processing` 的合法消息回到 `ready`，quarantine 里的不会被拉回来重试。
- 隔离的文件保留下来供人检查，写入时用 `.quarantine-{index}.json` 后缀避免覆盖已有隔离记录。**证据不能被后续的失败冲掉。**

---

### 实验 6（trace）：完整的两轮协作轨迹

这个实验最长，但它把开篇那条轨迹从头到尾复现了一遍。

```typescript
if (section("trace")) {
  await withTempDir(async (root) => {
    const { inbox, supervisor, cron, runtime } = makeRuntime(root);
    const leadModel = new ScriptModel(["收到缺口清单", "收到补充结果"]);
    const lead = new AgentRunner({ /* …Lead 装配，见仓库 tests… */ } as any);
    // 关键：把 mailbox 的 wakeup 接上，才能看到自动的 event turn
    const turns: Promise<unknown>[] = [];
    runtime.bindWakeup(async () => { turns.push(lead.runEvents()); });
    await runtime.start();

    console.log("1. spawn_teammate(alice)");
    const spawned = await runtime.spawn({ name: "alice", role: "writer",
      initialTask: "阅读 README 并列出缺口" });
    console.log("   ->", JSON.stringify(spawned));

    await waitFor(() => turns.length >= 1, "第一次 wakeup");
    await Promise.all(turns);
    console.log("2. alice 后台跑完 -> result 写进 lead/ready -> 发布事件 -> 唤醒 Lead");
    console.log("   [wakeup 1] Lead.runEvents() ->", leadModel.requests.length, "次模型请求");
    console.log("   alice 状态 =", runtime.status("alice"));

    console.log("3. Lead 的 canonical history：");
    lead.history.forEach((m: any, i: number) =>
      console.log(`     ${i + 1}. ${m.role.padEnd(9)} ${String(m.content).slice(0, 78)}`));

    console.log("4. send_message(to=alice) 追加第二轮");
    await runtime.send({ sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "只补充恢复与失败边界" });
    await waitFor(() => turns.length >= 2, "第二次 wakeup");
    await Promise.all(turns);
    console.log("   [wakeup 2] Lead.runEvents() -> 收到补充结果");
    // …打印 alice Runner 数量与第 2 轮 messages…
  });
}
```

完整可运行版本在仓库的 `tests/ch15-teammates.test.ts` 里有对应断言，脚本里照抄测试的装配方式即可。

真实输出：

```text
1. spawn_teammate(alice)
   -> {"name":"alice","role":"writer","status":"running"}
2. alice 后台跑完 -> result 写进 lead/ready -> 发布事件 -> 唤醒 Lead
   [wakeup 1] Lead.runEvents() -> 收到缺口清单
   alice 状态 = idle
3. Lead 的 canonical history：
     1. user      {"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-000…
     2. assistant 收到缺口清单
4. send_message(to=alice) 追加第二轮
   [wakeup 2] Lead.runEvents() -> 收到补充结果
   alice 只被创建过 1 个 Runner
   alice 第 2 轮 messages（第 1 轮还在）：
     system    You are alice, serving as writer.
     user      阅读 README 并列出缺口
     assistant README 缺少恢复与失败边界说明
     user      只补充恢复与失败边界
5. 收尾：alice 状态 = idle，Lead history 长度 = 4
   lead 邮箱剩余 ready = 0
   lead 邮箱 done = 2
   alice 邮箱 done = 2
```

第 3 步那条 history 的完整内容是：

```json
{"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-000000000702","sender":"alice","recipient":"lead","message_kind":"result","content":"README 缺少恢复与失败边界说明","created_at_utc":"2026-07-30T08:00:00.000Z"}}
```

**要验证的：**
- `spawn` 立刻返回 `status: "running"`，**不等队友做完**。
- 队友做完后状态变 `idle`，**没有被销毁**。
- 第二轮 `send_message` 复用同一个 Runner：`alice 只被创建过 1 个 Runner`。
- alice 第 2 轮的 `messages` 里**第 1 轮的三条还在**（system / user / assistant），所以她知道自己上次说了什么。**这就是持续队友和一次性子 Agent 最本质的差别。**
- Lead 的 history 里，队友的汇报以序列化的 `runtime_event` 形式作为 `user` 消息进来——不是凭空多出来的对话，是有结构、可追溯的一条事件记录。
- 收尾时两个邮箱各 2 条 `done`，`ready` 清空。**账对上了：发出去的每一条都被确认了。**

---

### 实验 7（isolate）：两个队友的上下文不串线

```typescript
if (section("isolate")) {
  await withTempDir(async (root) => {
    const { runtime } = makeRuntime(root);
    // …装配一个 Lead，两个队友各自的 ScriptModel…
    await runtime.start();
    await runtime.spawn({ name: "alice", role: "writer", initialTask: "写文档" });
    await runtime.spawn({ name: "bob", role: "reviewer", initialTask: "审代码" });
    await waitFor(() => runtime.status("alice") === TeammateStatus.Idle
      && runtime.status("bob") === TeammateStatus.Idle, "两个队友都完成");
    // …收集事件、打印各自模型请求…
    // 并发同名 spawn
    const results = await Promise.allSettled([
      runtime.spawn({ name: "carol", role: "r", initialTask: "t" }),
      runtime.spawn({ name: "carol", role: "r", initialTask: "t" }),
    ]);
    console.log("并发同名 spawn：", results.map((r) =>
      r.status === "fulfilled" ? "成功" : `失败 ${(r.reason as Error).message}`));
    for (const bad of ["lead", "Alice", "../x"]) {
      try { await runtime.spawn({ name: bad, role: "r", initialTask: "t" }); }
      catch (error) { console.log(`${bad.padEnd(8)} ->`, (error as Error).message); }
    }
    try { await runtime.send({ sender: "lead", recipient: "zoe",
      kind: MailboxMessageKind.Task, content: "x" }); }
    catch (error) { console.log("send 给不存在的 zoe ->", (error as Error).message); }
  });
}
```

真实输出：

```text
Lead 收到： [ 'alice=alice 完成', 'bob=bob 完成' ]
alice 的模型请求： system You are alice, serving as writer.
                  user   写文档
                  工具 = send_message
bob 的模型请求：   system You are bob, serving as reviewer.
                  user   审代码
                  工具 = send_message
并发同名 spawn： [ '成功', '失败 Teammate already exists: carol' ]
lead     (保留名) -> Teammate name lead is reserved for the lead
Alice    (大写)   -> Invalid or unsafe Agent name; expected a safe lowercase slug
../x     (路径)   -> Invalid or unsafe Agent name; expected a safe lowercase slug
send 给不存在的 zoe -> Unknown teammate: zoe
```

**要验证的：**
- alice 的请求里**没有** bob 的任何内容，反之亦然。每个队友一个独立 `AgentRunner`、独立 `messages`。
- 两个队友的 `system prompt` 各自带自己的名字和角色。
- **并发同名 spawn 只有一个成功。** 注册通过 `#registryTail` 串行化，所以「检查是否存在」和「写入注册表」之间没有窗口。这是并发编程里的经典 check-then-act 竞态，这里被序列化掉了。
- `lead` 是保留名，报的是专门的错误消息，不是笼统的「名字不合法」。**错误信息应该告诉人他犯的具体是哪个错。**
- 给不存在的队友发消息**立刻报错**，而不是默默在磁盘上建一个 `zoe/ready` 目录让消息烂在那里。

---

### 实验 8（failure）：队友崩了会怎样

```typescript
if (section("failure")) {
  await withTempDir(async (root) => {
    const { runtime } = makeRuntime(root);
    // 队友的模型第一次调用就抛错
    // …装配一个会抛 "model exploded" 的 ScriptModel…
    await runtime.start();
    await runtime.spawn({ name: "alice", role: "writer", initialTask: "会崩的任务" });
    await waitFor(() => runtime.status("alice") === TeammateStatus.Failed, "alice 失败");
    // …打印 Lead 收到的事件 payload…
    console.log("alice 状态 =", runtime.status("alice"));
    console.log("alice/quarantine =",
      (await readdir(join(root, "mailboxes", "alice", "quarantine"))).length, "个文件");
    try { await runtime.send({ sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "再试一次" }); }
    catch (error) { console.log("向 failed 队友发消息 ->", (error as Error).message); }
  });
}
```

真实输出：

```text
Lead 收到：
  {"kind":"mailbox","message_id":"00000000-0000-4000-8000-000000000702",
   "sender":"alice","recipient":"lead","message_kind":"result",
   "content":"Teammate alice failed: model exploded",
   "created_at_utc":"2026-07-30T08:00:00.000Z"}
alice 状态 = failed
alice/quarantine = 1 个文件
向 failed 队友发消息 -> Teammate alice cannot receive messages while failed
```

**要验证的：**
- 失败**不是静默的**。Lead 收到一条正常的 `result` 消息，内容说明失败原因。
- `message_kind` 仍是 `result`，不是新增的 `error` 类型。Lead 不需要为失败写单独分支。
- 输入消息进 `quarantine`（带失败原因），**不回 `ready`**。否则同一条毒任务会无限重试，反复打崩队友。
- `failed` 队友拒收新消息，防止 Lead 往一个坏掉的邮箱里堆积任务。

---

### 实验 9（defer）：mailbox 事件不会混进用户 turn

```typescript
if (section("defer")) {
  await withTempDir(async (root) => {
    const { inbox, runtime } = makeRuntime(root);
    const model = new ScriptModel(["处理了用户请求", "处理了 mailbox 事件"]);
    // …装配 Lead…
    const message = await runtime.send({ sender: "alice", recipient: "lead",
      kind: MailboxMessageKind.Result, content: "队友的汇报" });
    inbox.publish(message);   // 事件已经在队列里了

    const first = await lead.run("普通用户请求");
    console.log("user turn ->", first.finalText);
    console.log("  模型请求数 =", model.requests.length);
    const hasMailbox = JSON.stringify(model.requests[0]).includes("mailbox");
    console.log("  本次请求里有 mailbox 事件吗 =", hasMailbox);
    console.log("  history 长度 =", lead.history.length);

    const second = await lead.runEvents();
    console.log("runEvents() ->", second?.finalText);
    console.log("  模型请求数 =", model.requests.length);
    // …打印 history…
    console.log("空 inbox 再 runEvents() ->", await lead.runEvents());
    inbox.publish(message);   // 重复发布同一个事件
    console.log("重复发布同一事件后 runEvents() ->", await lead.runEvents(),
      "，模型请求数仍 =", model.requests.length);
  });
}
```

真实输出：

```text
user turn -> 处理了用户请求
  模型请求数 = 1
  本次请求里有 mailbox 事件吗 = false
  history 长度 = 2
runEvents() -> 处理了 mailbox 事件
  模型请求数 = 2
  history：
    1. user      普通用户请求
    2. assistant 处理了用户请求
    3. user      {"runtime_event":{"kind":"mailbox",…
    4. assistant 处理了 mailbox 事件
空 inbox 再 runEvents() -> undefined
重复发布同一事件后 runEvents() -> undefined，模型请求数仍 = 2
```

**要验证的：**
- 事件在用户 turn **之前**就已经在 inbox 里了，用户 turn 依然没碰它（`有 mailbox 事件吗 = false`）。这不是时序上的巧合，是 `allowContextEvents === false` 的显式行为。
- 只有 `runEvents()` 会消费它，而且它有自己独立的 turn 边界（history 的第 3、4 条）。
- 空 inbox 时 `runEvents()` 返回 `undefined`，不抛错、不空转模型。
- **同一个事件重复发布也不会再进模型**：`#seenEventIds` 已经登记过这个 `eventId`。模型请求数停在 2。

---

### 实验 10（ackfail）：ack 失败不会造成重复

```typescript
if (section("ackfail")) {
  await withTempDir(async (root) => {
    // 包一层 store，让第一次 ack 必然失败
    let ackAttempts = 0;
    const inner = new FileMailboxStore(root, {
      idGenerator: seqId(), clock: () => new Date("2026-07-30T08:00:00.000Z"),
    });
    const flaky = new Proxy(inner, {
      get(target, prop, receiver) {
        if (prop !== "ack") return Reflect.get(target, prop, receiver);
        return async (message: any) => {
          ackAttempts += 1;
          const count = countMailboxInHistory();
          console.log(`  ack 第 ${ackAttempts} 次：此刻 history 中 mailbox 事件条数 = ${count}`);
          if (ackAttempts === 1) throw new Error("ack persist failed");
          return (target as any).ack(message);
        };
      },
    });
    const { runtime } = makeRuntime(root, flaky);
    // …装配 Lead，发一条队友消息并发布…
    try { await lead.runEvents(); }
    catch (error) { console.log("第 1 次 runEvents() 抛错 ->", (error as Error).message); }
    console.log("  模型请求数 =", model.requests.length, "（应为 0）");
    console.log("  history 中含该 id 的消息数 =", countMailboxInHistory());
    const result = await lead.runEvents();
    console.log("第 2 次 runEvents() ->", result?.finalText);
    console.log("  ack 总尝试次数 =", ackAttempts);
    console.log("  模型请求数 =", model.requests.length);
    console.log("  history 中含该 id 的消息数 =", countMailboxInHistory());
  });
}
```

真实输出：

```text
  ack 第 1 次：此刻 history 中 mailbox 事件条数 = 1
第 1 次 runEvents() 抛错 -> ack persist failed
  模型请求数 = 0（应为 0）
  history 中含该 id 的消息数 = 1
  ack 第 2 次：此刻 history 中 mailbox 事件条数 = 1
第 2 次 runEvents() -> 处理完成
  ack 总尝试次数 = 2
  模型请求数 = 1
  history 中含该 id 的消息数 = 1
```

**要验证的：**

这是本章最重要的一个实验，四个数字各自证明一件事：

| 观察 | 证明了什么 |
| --- | --- |
| `ack 第 1 次` 时 history 已有 1 条 | **history 先于 ack**。ack 被调用时事件已在历史里 |
| 第 1 次抛错后模型请求数 = 0 | ack 没成功之前**一次模型都没调**。不会出现「模型处理了但没确认」 |
| 两次之间 history 数量恒为 1 | 重发同一事件**没有产生第二条 history**（`#pendingEventAcks` 生效） |
| 最终 ack 尝试 2 次、模型请求 1 次 | **重试落在 ack 上，不落在模型上** |

顺带说明为什么这个实验值得单独写：可靠消息传递的 bug 几乎全都长在「持久化失败」这条路径上，而这条路径在正常测试里永远不会被走到。你必须**主动注入失败**才能验证它。

---

### 实验 11（tools）：工具面和配对检查

```typescript
if (section("tools")) {
  // …构造 P14 / P15 能力集，打印差集…
  // …构造 Lead 与队友的 ToolRegistry，打印各自工具名…
  // …四种错误装配，逐个 catch 打印…
}
```

真实输出：

```text
P14 能力位 = artifacts, background, compaction, cron, dynamic_prompt, files, hooks,
             loop, memory, policy, powershell, recovery, skills, subagent,
             task_dag_json, todo, tool_registry
P15 能力位 = 上列 + mailbox, teammate
P15 - P14 = ["teammate","mailbox"]
Lead 工具（18 个）：shell, read_file, write_file, edit_file, glob, todo_write, task,
  load_skill, create_task, get_task, list_tasks, claim_task, complete_task,
  query_background_job, cancel_background_job, schedule_cron, spawn_teammate,
  send_message
队友工具（4 个）：read_file, send_message, shell, write_file
队友 system prompt = "You are writer, serving as writer."
P15 缺 teammateRuntime -> teammateRuntime is required for chapter 15 or later
P14 传 teammateRuntime -> teammateRuntime requires chapter 15 or later
Cron 不同 supervisor -> cronRuntime must share the JobSupervisor
Teammate 不共享 inbox -> teammateRuntime must share the background supervisor, EventInbox, and CronRuntime
```

**要验证的：**
- `P15 - P14` 精确等于两个新能力位，**没有任何删改**。
- 两个新工具排在 Lead 工具表的**最后两位**，前 16 个顺序不变。前面章节写的所有 prompt 和测试不受影响。
- 队友只有 4 个工具，**没有** `spawn_teammate`（不能再生队友）、没有 `task`（不能再委派）、没有 Task DAG。
- 四道配对检查**全部真的抛错**。宁可启动失败，也不要跑起来之后发现事件进了没人读的队列。

---

### 实验 12（close）：关闭时在途工作去哪了

```typescript
if (section("close")) {
  await withTempDir(async (root) => {
    const { runtime } = makeRuntime(root);
    // 队友的模型会一直挂着，直到我们 close
    await runtime.start();
    await runtime.spawn({ name: "alice", role: "writer", initialTask: "长任务" });
    await waitFor(() => runtime.status("alice") === TeammateStatus.Running, "alice 开跑");
    console.log("close 前 alice 状态 =", runtime.status("alice"));
    await runtime.close();
    console.log("close 后 alice 状态 =", runtime.status("alice"));
    const store = new FileMailboxStore(root);
    const again = await store.claim("alice");
    console.log("原任务消息回到 ready 并可再次 claim =",
      JSON.stringify({ content: again?.content, kind: again?.kind }));
    try { await runtime.spawn({ name: "bob", role: "r", initialTask: "t" }); }
    catch (error) { console.log("close 后 spawn ->", (error as Error).message); }
  });
}
```

真实输出：

```text
close 前 alice 状态 = running
close 后 alice 状态 = shutdown
原任务消息回到 ready 并可再次 claim = {"content":"长任务","kind":"task"}
close 后 spawn -> TeammateRuntime is closed
```

**要验证的：**
- 在途消息**回到 `ready`**（不是留在 `processing`，也不是进 `quarantine`），下次能被重新 claim。
- 状态是 `shutdown`，和 `failed` 区分开。**主动关闭不是失败。**
- `close()` 之后 `spawn` 明确报错，不会创建一个永远不会被执行的队友。

对照实验 8：同样是「没做完」，抛错进 `quarantine`，关闭回 `ready`。判断依据就是 `this.#closed || signal.aborted`——**关闭是我们主动发起的，不能算消息的错。**

---

### 实验 13（cross）：跨进程消费者接手

```typescript
if (section("cross")) {
  await withTempDir(async (root) => {
    // 两个 store 实例，模拟两个进程访问同一个目录
    const first = new FileMailboxStore(root, {
      idGenerator: seqId(), clock: () => new Date("2026-07-30T08:00:00.000Z"),
    });
    const second = new FileMailboxStore(root);
    const sent = await first.send({ sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "共享任务" });
    const claimed = await first.claim("alice");
    console.log("first.claim ->", `...${claimed!.id.slice(-4)}`);
    console.log("second.recoverProcessing ->", await second.recoverProcessing("alice"));
    const retaken = await second.claim("alice");
    console.log("second.claim ->", `...${retaken!.id.slice(-4)}`);
    console.log("second.ack ->", await second.ack(retaken!));
    console.log("first.ack（旧消费者补 ack）->", await first.ack(claimed!));
    const tampered = createMailboxMessage({ ...claimed!.toJSON(), content: "不一样的内容" });
    try { await first.ack(tampered); }
    catch (error) { console.log("first.ack 内容冲突 ->", (error as Error).message); }
    try { await first.send({ sender: "lead", recipient: "alice",
      kind: MailboxMessageKind.Task, content: "同 ID 再发一次" }); }
    catch (error) { console.log("同 UUID 再 send ->", (error as Error).message); }
  });
}
```

真实输出：

```text
first.claim -> 0701
second.recoverProcessing -> 1
second.claim -> 0701
second.ack -> true
first.ack（旧消费者补 ack）-> true
first.ack 内容冲突 -> Mailbox message does not match completed message: 00000000-0000-4000-8000-000000000701
同 UUID 再 send -> Mailbox message id already exists: 00000000-0000-4000-8000-000000000701
```

**要验证的：**
- 两个「进程」共享同一个目录，`recoverProcessing` 让第二个接手第一个没做完的活。**这就是崩溃恢复的实质。**
- 第二个 ack 成功后，第一个再 ack **同一份内容**仍然返回 `true`。旧消费者迟到的确认不会破坏状态。
- 但 ack **不同内容**的同 ID 消息会抛错。**幂等不等于宽容**：宽容会把「两个进程对同一条消息内容认知不一致」这种严重问题静默掉。
- 同一个 UUID 再 `send` 被明确拒绝，`ready`/`processing`/`done`/`quarantine` 四个目录都会被检查。

---

### 实验 14（restart）：CLI 现状与跨进程恢复

```typescript
if (section("restart")) {
  await withTempDir(async (root) => {
    // 第 1 次运行：只接 cron 的 wakeup（复现 cli.ts 的现状）
    {
      const { cron, runtime } = makeRuntime(root);
      cron.bindWakeup(async () => { await lead.runEvents(); });
      // 注意：runtime.bindWakeup 故意不调
      await runtime.start();
      const result = await lead.run("派 alice 去看 README");
      console.log("第 1 次运行：Lead 回答 =", result.finalText);
      await waitFor(() => runtime.status("alice") === TeammateStatus.Idle, "alice 完成");
      console.log("  alice 状态 =", runtime.status("alice"));
      console.log("  Lead history 长度 =", lead.history.length, "（只有用户那一轮）");
      await runtime.close();
      // …打印三个目录的文件数…
    }
    // 第 2 次运行：新的运行时实例，同一个目录
    {
      const { runtime } = makeRuntime(root);
      await runtime.start();
      // …打印目录状态、runEvents()、history 第 1 条、done 数量…
    }
  });
}
```

真实输出：

```text
第 1 次运行：Lead 回答 = 已经派 alice 去看 README 了
  alice 状态 = idle
  Lead history 长度 = 2（只有用户那一轮）
  关闭后 lead/ready = 0，lead/processing = 1，lead/done = 0
  -> 队友的 result 留在 processing，没有被这次会话消费

第 2 次运行：start() 后 lead/ready = 0，lead/processing = 1
  runEvents() -> 读到了上次 alice 的汇报
  history 第 1 条 = {"runtime_event":{"kind":"mailbox","message_id":"00000000-0000-4000-8000-000000000702","sender":"ali
  lead/done = 1
```

**要验证的：**
- 这就是 `cli.ts` 当前的真实行为：**只有 Cron 的 wakeup 被接上**，队友汇报不会在同一次会话里触发 event turn。
- 消息**一条都没丢**。它停在 `processing`，是四个状态里语义最准确的那个：「有人领了，还没确认完成」。
- 第 2 次运行的 `start()` 做了两件事：`recoverProcessing` 把它搬回 `ready`，`#publishLeadMessages` 立刻重新 claim 走——所以打印出来**还是 `processing = 1`**。这不是没恢复，是恢复完又被领走了。
- `runEvents()` 之后 `done = 1`，闭环完成。

**跨进程的这段缺口，就是至少一次投递存在的理由。** 内存 dict 在这里会直接丢掉队友的全部工作成果。

如果你想在自己的宿主里看到同会话闭环，加一行就够了：

```typescript
teammateRuntime.bindWakeup(async () => { await runner.runEvents(); });
```

---

### 实验 15（concurrent）：并发压一下

```typescript
if (section("concurrent")) {
  await withTempDir(async (root) => {
    const store = new FileMailboxStore(root, {
      clock: () => new Date("2026-07-30T08:00:00.000Z"),
    });
    const sends = await Promise.all(Array.from({ length: 12 }, (_, i) =>
      store.send({ sender: "lead", recipient: "alice",
        kind: MailboxMessageKind.Task, content: `任务 ${i}` })));
    console.log("并发 send 12 条 -> 成功", sends.length, "条");
    const claims = (await Promise.all(Array.from({ length: 12 }, () =>
      store.claim("alice")))).filter(Boolean);
    console.log("并发 claim 12 次 -> 拿到", claims.length, "条，去重后",
      new Set(claims.map((m) => m!.id)).size, "条");
    console.log("再 claim 一次 ->", await store.claim("alice"));
  });
}
```

真实输出：

```text
并发 send 12 条 -> 成功 12 条
并发 claim 12 次 -> 拿到 12 条，去重后 12 条
再 claim 一次 -> undefined
```

**要验证的：**
- 12 个并发 `send` 全部成功，没有互相覆盖。
- 12 次并发 `claim` 拿到 12 条**互不重复**的消息。这是 claim 的核心保证：**同一条消息不会被两个消费者同时领走。**
- 队列取空后返回 `undefined`。

如果没有那两把锁（进程内 promise 队列 + 跨进程 `proper-lockfile`），这个实验最典型的失败形态是：两次 claim 拿到同一条消息（去重后少于 12），或者某次 `rename` 因为目标已被移走而抛 `ENOENT`。

---

### 实验完成后清理

探测脚本是临时的，看完输出就可以删掉：

```powershell
Remove-Item scripts\probe-ch15.mts
```

删掉它不影响仓库的任何测试——所有结论在 `code/chapters/ch15/tests/` 下都有对应的正式断言。

---

## 这章刻意没有做什么

写教程最容易犯的错是把「还没做」说成「做完了」。所以这一节明确列出边界。

| 没做的事 | 为什么 | 在哪一章 |
| --- | --- | --- |
| `check_inbox` 工具 | mailbox 是运行时事件源，不该让模型猜什么时候该拉取 | 不做 |
| list / cancel 队友的工具 | 本章只验证最小闭环：创建、发消息、收汇报 | 按需 |
| 结构化 request/response 协议 | 现在只有单向 `task` / `message` / `result`，没有「等一个回答」的语义 | 第 16 章 |
| 计划审批冒泡 | 队友要做危险操作时向 Lead 请示，需要双向等待通道 | 第 16 章 |
| 跨 Agent 权限请求冒泡 | 同上，需要 request/response 打底 | 第 16 章 |
| `shutdown_request` / `shutdown_approved` 握手 | 本章的 `close()` 是进程内资源释放，不是跨 Agent 协议 | 第 16 章 |
| 队友从共享 Task DAG 自主找活 | 需要抢占、竞争裁决和空闲自驱 | 第 17 章 |
| 广播 / 订阅 / 动态拓扑 | 本章拓扑固定为一个 Lead 加多个平级队友 | 不做 |
| 持久团队注册表 | 队友信息完全在 `TeammateRuntime` 进程内 | 不做 |
| 队友生成队友 | 会让拓扑变成不可控的树 | 不做 |

有一个概念特别容易误解，单独说明：

> **`idle` 只表示「Runner 还留着，正在等下一条显式消息」，不表示「它会自己去找活干」。**

空闲队友主动发现并领取工作是第 17 章的事。本章的队友是被动的：不发消息给它，它就一直待着。

---

## 与 Claude Code 的差异

参照外部仓库 [`shareAI-lab/learn-claude-code`](https://github.com/shareAI-lab/learn-claude-code) 的 `s15_agent_teams/README.md` 中深入 CC 源码的分析，P15 的 Teammate/Mailbox 在教学简化上与 Claude Code 的团队协作机制存在以下差异。

先给一张速查表，再逐条展开：

| 维度 | CC 教学版 | CC 真实源码 | P15 |
| --- | --- | --- | --- |
| 存储 | 每 Agent 一个 `.jsonl` | JSON 数组 + 文件锁 | 每消息一个 `.json` + 四态目录 |
| 读取 | `check_inbox` 工具 | `useInboxPoller` 每 1s 轮询 | typed event 注入 + 显式 `runEvents()` |
| 投递语义 | 至多一次（read+unlink） | 文件锁保护 | 至少一次 + 稳定幂等键 |
| 队友载体 | daemon 线程，10 轮结束 | tmux 窗格或进程内 | 独立 `AgentRunner`，不销毁 |
| 消息类型 | 2 种 | 15 种结构化类型 | 3 种（`task`/`message`/`result`） |
| 权限冒泡 | 省略 | 双向轮询 | 复用同一套治理，不跨 Agent 冒泡 |

**存储模型。** CC 教学版为每个 Agent 使用一个 `.jsonl` 文件作为收件箱，`read_inbox()` 执行读文件 + `unlink()` 消费式读取，存在 read/unlink 竞态。P15 使用每消息一个 `.json` 文件，通过目录名（`ready/processing/done/quarantine`）表达消息状态，用文件锁保护原子 `rename` 迁移，并发 writer 和 claimer 都被锁保护。

**公开工具。** CC 教学版为 Lead 暴露 `spawn_teammate`、`send_message`、`check_inbox` 三个新工具（共 14 个 Lead 工具）。P15 的 Lead 工具序列不包含 `check_inbox`，mailbox 是运行时事件源；消息由 `TeammateRuntime` 发布到 `EventInbox`，Lead 在显式 event turn 中接收，不需要模型猜测何时拉取。

**消息读取方式。** CC 教学版由 Lead 显式调用 `check_inbox` 拉取消息。CC 真实源码的 `useInboxPoller` 每 1 秒检查一次收件箱，检测到消息后自动提交为新 turn，不需要用户输入。P15 把 mailbox 事件放入共享 `EventInbox`，由宿主显式调用 `AgentRunner.runEvents()` 处理。普通 user turn 的 drain 点只暂存带 `idempotencyKey` 的事件，不会因为 Lead 收到消息就自动启动新 turn。

**队友实现。** CC 教学队友运行在 daemon 线程中，持有简化 system prompt、独立 messages 列表和 4 个子工具，教学版限制最多 10 轮对话后自动结束。CC 真实源码的队友由 `spawnTeammate()` 创建，可运行在 tmux 窗格或进程内，并用 idle loop 等待 inbox 消息。P15 的每名队友拥有独立 `AgentRunner`，Runner 持有完整历史、Hook 和 Recovery/Compaction 边界。完成一轮后进入 `idle` 状态而非销毁，后续消息复用同一 Runner 和既有 history。

**消息类型。** CC 教学版消息只有 `"message"` 和 `"result"` 两种 `type`。CC 真实源码有 15 种结构化消息类型（包括 `idle_notification`、`permission_request`、`permission_response`、`plan_approval_request`、`shutdown_request/approved/rejected`、`task_assignment` 等），文本消息包装在 `<teammate-message>` XML 标签中交付模型。P15 的 `MailboxMessageKind` 只有 `task`、`message`、`result` 三种，消息同时实现 `RuntimeEvent` 接口，通过 typed event inbox 而非 XML 标签注入 history。

**投递语义。** CC 教学版的 `read_inbox` 是 read + unlink，进程在读取后、处理前崩溃会永久丢失消息（至多一次投递）。CC 真实源码用 `proper-lockfile` 文件锁保护 JSON 数组收件箱。P15 采用每消息独立文件 + 目录状态机，processing 在重启时恢复为 ready，坏消息隔离到 quarantine；通过稳定一致 idempotency key 实现至少一次投递，同一 done 消息允许幂等 ack。

**权限冒泡。** CC 教学版省略了权限冒泡。CC 真实源码使用双向轮询：队友发 `permission_request` 到 Lead 收件箱，Lead 的 `useInboxPoller` 检测并路由到审批队列；用户审批后 Lead 发 `permission_response` 回队友，队友的 `useSwarmPermissionPoller`（每 500ms）接收回复后继续或拒绝。P15 的队友和 Lead 复用同一套 Hook、`PermissionPolicy`、Recovery 和 Compaction，未单独实现跨 Agent 权限冒泡。

**生命周期与关闭。** CC 教学版队友是 daemon 线程，10 轮后自动结束，无体面关闭协议。CC 真实源码的队友经历 spawn → work → idle（Stop hook 触发 `idle_notification`）→ shutdown（Lead 发 `shutdown_request`，队友回复 `shutdown_approved` 后清理）。P15 的队友状态机为 `running → idle → failed/shutdown`，关闭是进程内资源释放（cancel 在途 task、release processing 消息），不是跨 Agent 的 shutdown 握手协议。

**队友工具面。** CC 教学版队友工具为 bash、read_file、write_file、send_message，真实 CC 队友还持有 `TaskCreate`、`TaskUpdate` 等工具，任务系统是团队共享的。P15 的队友工具注册 `shell`、`read_file`、`write_file`、`send_message`，显式排除 `edit_file`、`glob`、TODO、Skill、子 Agent、Task DAG、Cron 和 `spawn_teammate`；`send_message` 的 sender 来自可信 identity 而非工具输入。

**团队注册表。** CC 真实源码通过 `~/.claude/teams/{teamName}/config.json` 存储团队注册表，包含成员 ID、颜色和激活状态；P15 无持久团队注册表，队友信息和状态完全在 `TeammateRuntime` 进程内管理。

上述 CC 实现细节来自外部仓库 [`shareAI-lab/learn-claude-code`](https://github.com/shareAI-lab/learn-claude-code) 的 `s15_agent_teams/README.md` 及其引用的 CC 源码行号。Claude Code 本身不开源，这些行号只是参考快照，不代表当前实现。

如果你只想记住一件事：**CC 用轮询 + 消费式读取换取实现简单，P15 用事件注入 + 四态目录换取可恢复性。** 两条路各有代价，本章选的是后者，因为「消息丢了没人知道」在协作场景里是最贵的失败。

---

## 从 ai-agent-book 学到什么：消息信封与可观察协作

外部仓库 [`bojieli/ai-agent-book`](https://github.com/bojieli/ai-agent-book) 的 `book/chapter10.md` 最贴近本章：它把不共享上下文的多 Agent 协作拆成数据平面与控制平面，并用结构化信封和 Message Bus 解决异步通信。配套实验 10-4 的说明见该仓库 `chapter10/parallel-web-research/README.md`，总线实现见同目录的 `message_bus.py`。

### ai-agent-book 的实验设计

ai-agent-book 第 10 章强调，消息不能只是一段裸字符串，而应携带结构化信封。`message_bus.py` 的 `Envelope` 包含 `sender_id`、`target`、`type`、`payload`、`seq`、`ts`，消息类型包括 `task_assigned`、`status_update`、`result`、`terminate`、`ack`。`MessageBus` 按订阅关系转发，支持点对点投递和 `BROADCAST="*"` 广播。`history` 保留完整消息流，`short()` 输出带时间戳和全局序号的紧凑日志，让实验「看得见」每条协作事件。

实验 10-4 把这套总线用到了真实并行搜索上：Manager 动态启动多个同构 worker，每个 worker 使用独立 Playwright browser context；单站点超时或解析失败不会阻塞 peers。第一个 `target_found` 在 `asyncio.Lock` 下结算，只允许一次 terminate 广播，后续迟到命中只记录不重复结算。输家收到 terminate 后在安全点取消、发送 ack 并关闭 browser context。README 还要求串行路径访问同一批真实网站、使用同一抽取函数，用真实 wall-clock 和资源创建/关闭计数作为验收证据。

### P15 的对照与取舍

P15 没有照搬中心 Message Bus，而是把 ai-agent-book 的结构化消息、可观察历史和资源审计原则落到持久 Mailbox：

- **结构化信封。** ai-agent-book 用 `Envelope` 携带 sender/target/type/payload；P15 的 `MailboxMessage` 携带 `id`、`sender`、`recipient`、`kind`、`content`、`createdAtUtc`、`idempotencyKey`，并直接实现 `RuntimeEvent`。消息类型由 `MailboxMessageKind` 限定为 `task/message/result`，不需要用字符串前缀猜类型。
- **点对点 vs 发布订阅。** ai-agent-book 的中心 `MessageBus` 支持订阅和广播，适合动态 peer 集；P15 当前拓扑固定为 Lead 与多个平级队友，因此用持久收件箱做点对点投递，不实现 `BROADCAST`、不做新 Agent 自主订阅，也不让队友动态生成队友。
- **可观测性。** ai-agent-book 的 `history` 和带时间戳日志让消息流可回放；P15 不把日志当作持久消息，而是用磁盘 `ready/processing/done/quarantine` 状态目录和 `EventInbox` 的 typed history 表达同一件事。测试可以断言 send/claim/ack/release/quarantine 后的 store 状态，而不是只看打印文本。
- **优雅终止与资源审计。** 实验 10-4 用 terminate + ack + browser context 创建/关闭计数证明资源不漏；P15 的 `TeammateRuntime.close()` cancel/await 在途 worker、release processing 消息并尝试关闭每个 Runner，测试断言 `shutdown` 状态和可重试清理。P15 没有跨 Agent terminate/shutdown 握手，留给第 16 章。
- **失败隔离。** ai-agent-book 的单站点失败不阻塞 peers；P15 的队友执行失败会把输入移入 quarantine、状态改为 `failed`，并向 Lead 发布可观察失败 `result`。Lead 仍可继续协作，不会因为一个队友的模型或工具错误整条链崩溃。
- **离线验证 vs 真实实验。** ai-agent-book 用真实浏览器和真实串行基线测 speedup，并用 provenance 保存 114 条总线事件、资源计数和 acceptance gates；P15 聚焦可重复离线测试：注入确定 UUID、UTC clock 和模型替身，断言状态迁移、事件流、幂等 ack、失败事件和资源关闭，**不声称有真实 wall-clock 加速证据**。

**P15 明确不做的事：** 没有中心 `MessageBus`、订阅过滤、`BROADCAST`、`status_update`、`terminate/ack` 总线协议，也没有跨 Agent 的 shutdown 握手。ai-agent-book 的广播和动态拓扑更适合 Agent 数量不固定、需要自主发现与订阅的场景；第 15 章先守住「固定团队、持久收件箱、typed event 注入唯一 Loop」这条最小闭环。

---

## 配套代码索引

本章涉及的文件都在 `code/chapters/ch15/` 下。行数是当前快照的真实值。

| 文件 | 行数 | 角色 |
| --- | --- | --- |
| `src/features/mailbox.ts` | 242 | `MailboxMessage` 领域模型、Zod schema、`canonicalAgentName`、`MailboxStore` 接口 |
| `src/adapters/mailbox-json.ts` | 586 | `FileMailboxStore`：四态目录、原子迁移、双锁、FIFO 排序、quarantine |
| `src/features/teammates.ts` | 598 | `TeammateRuntime`：注册表、worker 生命周期、两个工具、事件转发 |
| `src/core/events.ts` | 71 | `RuntimeEvent` 接口与 `EventInbox`（本章不改，只复用） |
| `src/core/loop.ts` | 714 | `runEvents()`、mailbox 延后、history-before-ack、`#pendingEventAcks` |
| `src/bootstrap.ts` | 403 | 组合根：能力位、工具装配、四道配对检查 |
| `src/cli.ts` | 323 | 两个入口的共享装配、wakeup 绑定、反向关闭 |
| `src/core/profiles.ts` | 306 | P15 能力集定义（P14 + `teammate` + `mailbox`） |
| `src/chapters/ch15.ts` | 5 | 章节入口，只有一行转发 |
| `tests/ch15-mailbox.test.ts` | 181 | store 的状态迁移、FIFO、并发、幂等、quarantine |
| `tests/ch15-teammates.test.ts` | 343 | 持续队友、隔离、并发 spawn、失败、关闭 |
| `tests/ch15-loop.test.ts` | 170 | mailbox event turn、延后、ack 失败重试 |
| `tests/ch15-bootstrap.test.ts` | 104 | P14/P15 增量、工具面、四道配对检查 |
| `tests/ch15-entry.test.ts` | 66 | 两个入口、缺凭证时的礼貌退出 |

### 读代码的建议顺序

按这个顺序读，每一步都建立在上一步之上，不需要往前翻：

```text
① src/features/mailbox.ts          先搞清楚「消息是什么」
     ↓                              领域模型 + 校验 + 名字规则
② src/adapters/mailbox-json.ts     再看「消息怎么存」
     ↓                              四态目录 + 原子迁移 + 双锁
③ tests/ch15-mailbox.test.ts       用测试确认自己理解对了
     ↓
④ src/features/teammates.ts        然后是「谁来消费消息」
     ↓                              worker 生命周期 + 两个工具
⑤ src/core/loop.ts:617             最后看接缝（#acceptNextRuntimeEvent）
     ↓                              延后 + history-before-ack + 半状态
⑥ src/bootstrap.ts + src/cli.ts    怎么装起来
```

第 ⑤ 步是全章最需要慢读的地方。建议对着实验 9 和实验 10 的输出读——**代码配上真实输出，比单读代码快得多。**

如果你只有 10 分钟，读 `mailbox-json.ts` 里的 `#transition` 和 `loop.ts:617` 的 `#acceptNextRuntimeEvent` 这两个方法就够了。它们承载了本章的全部核心决策。

---

## 运行与验证

本章测试注入确定 UUID、UTC clock 和模型替身，**不依赖真实 OpenAI、真实 sleep 或网络**。

五道门全部在 `code` 目录下执行：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch15
npm run typecheck
npm run lint
npm run format:check
npm run build
```

| 命令 | 检查什么 | 期望 |
| --- | --- | --- |
| `npm run test:ch15` | 行为正确性 | `44 passed (44)` / `335 passed (335)` |
| `npm run typecheck` | 类型完整 | 无输出即通过 |
| `npm run lint` | Biome 规则 | 无 error |
| `npm run format:check` | 格式统一 | 无 diff |
| `npm run build` | 能编译产出 | 无错误 |

关键断言都是可观察结果，不是「看起来对」：

- P15 capability 严格等于 P14 加 `teammate`、`mailbox`。
- Lead 工具保持 P14 完整前缀，只追加 `spawn_teammate`、`send_message`。
- 队友工具严格为 `shell`、`read_file`、`write_file`、`send_message`。
- 两名队友并行执行，各自 history 隔离；idle 后的新消息复用原 Runner。
- 并发同名 spawn 只成功一次，非法名字和未知收件人在创建工作前失败。
- FIFO 使用 `(created_at_utc, id)`，并发 writer 不丢消息，并发 claimer 不重复认领。
- send/claim/ack/release/quarantine/recover 的替换失败保留提交前状态。
- 重启把 processing 恢复为 ready；坏消息进入 quarantine 且不阻塞合法消息。
- 队友工具获得当前 Mailbox UUID 作为稳定 idempotency key；空键在任何 turn 副作用前失败。
- Mailbox event 由显式 event turn 接收，进入 canonical history 后才 ack；ack 失败重试不重复 history 或模型调用；跨 runtime 的相同 done 消息可幂等 ack，内容冲突明确失败。
- Cron 与 Mailbox 事件由各自 owner 确认，但共享一个 typed EventInbox。
- 队友失败会隔离输入并向 Lead 发布可观察失败结果；close 会 cancel、await 并留下 `shutdown` 状态；单个资源关闭失败不跳过其余资源，多异常可观察且关闭可重试。
- CLI 两个入口构建共享 runtime，并按 Teammate → Cron → supervisor → model 关闭。

---

## 本章小结

### 三句话版本

1. 给 Agent 加队友，难点不是多开几个模型调用，而是**队友的产出如何可靠地回到 Lead 的上下文里**。
2. 本章的答案是：**每消息一个文件，目录名就是状态，原子 `rename` 迁移，至少一次投递**。
3. 消息以 typed event 的形式**先进 canonical history，再 ack**，在明确的 event turn 边界被处理。

### 一定要记住的七条

| # | 结论 | 一句话理由 |
| --- | --- | --- |
| 1 | Lead 只加两个工具：`spawn_teammate`、`send_message` | 没有 `check_inbox`，模型不该猜什么时候拉消息 |
| 2 | 队友有独立、可复用的 Runner 和明确状态机 | `idle` 不是销毁，第二轮消息复用既有 history |
| 3 | 一条消息一个文件，四态目录原子迁移 | 状态在目录名里，不在文件字段里，改状态即 `rename` |
| 4 | processing 重启恢复，坏消息隔离，至少一次投递 | 一条坏消息不能堵住整个收件箱 |
| 5 | 先写 canonical history，再 ack | 顺序反了会造成「无痕丢消息」 |
| 6 | Teammate、Cron、Background 共用一个 Inbox 和一个 supervisor | 各自的事件由各自的 owner 确认 |
| 7 | 关闭按资源依赖逆序，全资源都尝试 | 每一层关闭时它依赖的东西还活着 |

### 一张图回顾整条链路

```text
                      ┌──────────────────────────────────────┐
                      │  Lead AgentRunner（唯一 Loop）        │
                      │  18 个工具                            │
                      └───┬──────────────────────────▲────────┘
        spawn_teammate    │                          │  ⑤ event turn
        send_message      │                          │     （runEvents）
                          ▼                          │
                  ┌───────────────────┐              │
                  │ TeammateRuntime   │              │
                  │  名字 -> Worker    │              │
                  └───┬───────────▲───┘              │
             ① 写入   │           │ ④ claim + publish │
                      ▼           │                  │
        ┌─────────────────────────┴──────────────────┴────────┐
        │  磁盘 Mailbox    .agent/mailboxes/{name}/            │
        │                                                      │
        │   ready ──claim──> processing ──ack──> done          │
        │     ▲                  │                             │
        │     └──── release ─────┤                             │
        │                        └── quarantine（坏消息/失败）  │
        └──────────────────────────────────────────────────────┘
                      ▲           │
             ③ 写 result           │ ② claim 任务
                      │           ▼
                  ┌───┴───────────────────┐
                  │ 队友 AgentRunner       │
                  │  4 个工具，独立 history │
                  └────────────────────────┘

        共享：EventInbox（一个）、JobSupervisor（一个）
```

顺着编号走一遍：① Lead 派活写进队友的 `ready`；② 队友 claim 走并执行；③ 结果写进 Lead 的 `ready`；④ 运行时 claim 并发布到共享 Inbox；⑤ Lead 在独立 event turn 里消费，先入 history 再 ack。

### 检查你是否真的读懂了

不用查资料回答下面七个问题。答不上来的，右边有对应位置。

| 问题 | 回去看 |
| --- | --- |
| 为什么 `ack` 必须在写入 canonical history **之后**？ | 「Inbox 注入」的崩溃时序推演 |
| 为什么 mailbox 是唯一保留「半状态」的事件类型？ | `#pendingEventAcks` 与实验 10 |
| FIFO 的排序键是什么？为什么需要第二个键？ | 「FIFO 键不是文件名」与实验 4 |
| `release` 和 `quarantine` 分别在什么情况下发生？ | 「关闭要把在途工作放回去」与实验 8、12 |
| 为什么 `send_message` 的输入里没有 `from` 字段？ | 「sender 不能伪造」 |
| 为什么普通用户 turn 要把 mailbox 事件暂存？ | 「Lead 不需要轮询」与实验 9 |
| 队友汇报后 Lead 为什么可能不在本次会话响应？ | 「CLI 只接了 Cron 的 wakeup」与实验 14 |

如果七个都能答上，本章的核心机制你已经拿到了。

---

做到这些以后，队友的进展不再是一行易丢的日志，Lead 也不需要靠 `check_inbox` 猜测消息何时到达；宿主可以在明确的 event turn 边界处理它。

但现在的通道还是**单向**的：队友只能报告，不能提问。它做不到「这个操作有风险，我等你批准再动手」。下一章就在这条持久消息通道上加 request/response、计划审批和 shutdown 握手，让协作从「汇报」变成「对话」。
