队友能持续运行、能收发消息、能提交计划之后，Lead 仍然可能卡在一个很朴素的瓶颈上：分活。

10 个任务手动发 10 次，勉强还能接受。任务之间一旦有依赖，Lead 还得盯着谁完成了、谁解锁了、下一项该交给谁。队友越多，中心协调者反而越忙。

真正的自治不是"收到命令后会执行"，而是"空闲时能从共享任务板找到当前可做的工作，并且只让一个 worker 获得它"。

这一章要落地的机制，一句话说清：

```text
任务不再由 Lead 逐个派发，而是写进一张共享的 SQLite 任务板；
空闲队友自己轮询这张板子，按创建顺序找第一个「依赖已完成」的任务，
用一次带条件的 UPDATE 把它从 pending 改成 in_progress —— 谁改成功谁拿到。
认领成功会拿到一张 claim token（认领凭证）和一个 lease（租约截止时间）；
完成任务必须出示这张 token，token 不对、租约过期，都完不成。
```

第 17 章在第 16 章的持续 Teammate、typed Protocol 和硬计划门控之上，只增加两项能力：

- `TASK_DAG_SQLITE`：把本章任务图切换到独立 SQLite store；
- `WORK_STEALING`：让 idle Teammate 按稳定顺序原子认领已解锁任务。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaEsibIxNKicV8v8bQOOYo1QvuRbQt26BPeHysflic1oBvhICiasagAgb96HpxFUPX7MZjQrpk7YqHIHWLOt4IF4l1LnJUhwgibk1xsU/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

- 说清"先扫描再写 owner"为什么在并发下必然重复派活，以及 TOCTOU 竞态到底发生在哪一行；
- 用 SQLite 的 `BEGIN IMMEDIATE` 加一次条件 `UPDATE`，把"选任务 + 改状态"压成一个原子步骤；
- 解释三层互斥（进程内串行、跨进程文件锁、数据库写事务）各自挡住了什么，少一层会漏什么；
- 说清 claim token 为什么必须是"全生命周期唯一"，而不只是"当前唯一"；
- 读懂 lease 为什么是半开区间 `[claimed_at, lease_expires_at)`，边界那一毫秒归谁；
- 说清 idle loop 里四件事的固定优先级，以及为什么 shutdown 永远不会被 ready task 饿死；
- 亲手跑通第 17 章，并用 11 个不需要 API key 的实验，逐条验证上面每个结论。

### 你需要先具备什么

| 前置 | 具体要求 | 不满足会怎样 |
| --- | --- | --- |
| 第 15、16 章的机制 | 知道 Teammate 是常驻 worker、Mailbox 是 claim/ack 队列、计划门禁会拦住有副作用的工具 | 本章的 idle loop 优先级会看不懂 |
| 第 12 章的任务 DAG | 知道任务有 `blocked_by` 依赖、完成一个会解锁下游 | 分不清本章新增的是「存储后端」还是「任务模型」 |
| Node.js 22+ | 本文实测用的是 v25.7.0 | 依赖装不上，或 `tsx` 跑不起来 |
| Windows PowerShell | 本文所有命令都是 PowerShell 写法 | 命令要自己翻译成 bash |
| 一点 SQL 常识 | 看得懂 `UPDATE ... WHERE ...` 和事务 | 原子认领那节需要多读两遍 |
| 模型凭证（可选） | 只有"真跑一次"那步需要 | 前 4 步和全部 11 个实验都不需要 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| **work stealing** | 空闲 worker 自己去共享任务板取活，而不是等中心协调者派活 |
| **共享任务板** | 本章就是那个 SQLite 数据库文件 `tasks.sqlite3`，多个进程都能读写 |
| **ready task** | 状态是 `pending`、且所有 `blocked_by` 依赖都已 `completed` 的任务 |
| **TOCTOU** | Time-Of-Check To Time-Of-Use，"检查完"到"真正用"之间状态被别人改了 |
| **原子认领** | 选任务和改状态在同一个事务里完成，中间不给别人插队的机会 |
| **`BEGIN IMMEDIATE`** | SQLite 写事务，一开始就拿保留锁，而不是等到第一次写才拿 |
| **条件 UPDATE** | `UPDATE ... WHERE status='pending' AND owner IS NULL`，只有状态还没被改的那一方能改成功 |
| **`getRowsModified()`** | 上一条语句真正改了几行；认领时必须等于 1，否则说明被人抢先了 |
| **claim token** | 认领凭证，一个 UUID；完成任务时必须原样出示 |
| **lease（租约）** | 认领的有效期截止时间；过期后任务自动回到 `pending`，可被别人认领 |
| **半开区间** | `[claimed_at, lease_expires_at)`，右端点那一刻算过期，不算有效 |
| **三层互斥** | 进程内 tail-chain → 跨进程文件锁 → `BEGIN IMMEDIATE`，层层收窄 |
| **有界 polling** | 空转次数有上限（默认 12 次），到顶就退出循环，而不是无限转 |
| **可中断等待** | 轮询间隔可以被新消息提前打断，不用干等满 5 秒 |
| **幂等键（idempotencyKey）** | 一次运行的去重标识；本章直接复用 claim token |

### 建议的阅读路线

```text
[1] 先看一次完整认领的轨迹      | 有画面感，知道最终跑出来是什么样
      ↓
[2] 为什么不能「先扫描，再写 owner」 | 本章存在的理由，三种错误写法
      ↓
[3] 独立 SQLite 后端与表结构     | 数据长什么样，约束挡住了什么
      ↓
[4] 原子认领：三层互斥          | 本章最核心的一节
      ↓
[5] claim token 与 lease        | 凭证和有效期，完成任务的两道校验
      ↓
[6] 五个工具与角色裁剪          | Lead 和队友拿到的工具集不一样
      ↓
[7] idle loop 的真实优先级       | 自驱轮询在代码里的确切位置
      ↓
[8] 有界 polling，不伪造 shutdown | 空转怎么收敛，怎么被观测
      ↓
[9] 运行第 17 章                | 从零跑起来，第 0~3 步不需要 API key
      ↓
[10] 11 个动手小实验            | 全部不需要 API key，逐条验证上面结论
```

如果你只有 10 分钟，读 `[1]`、`[4]`、`[7]` 三节，再跑实验 5 和实验 10。

---
## 先看一次完整认领的轨迹

下面这段输出来自本章实验 1（脚本 `scripts/probe-ch17.mts`，稍后会带你一步步建起来）。三条任务、三个 worker，全程没有 Lead 派活：

```text
===== trace =====
[1] Lead 建了三条任务：A=…1701 B=…1702 C=…1703（C 依赖 A、B）
[2] 任务板现状：抓取数据(pending) 清洗数据(pending) 出报表(pending)
[3] alice 轮询到活：抓取数据 token=…1711 租约到 2026-07-27T14:01:00.000Z
[4] bob 同时轮询，拿到的是另一条：清洗数据 token=…1712
[5] carol 也来轮询：claimNext → undefined（C 的依赖还没完成）
[6] alice 出示 token 完成 A → unblocked=[]（C 还差 B）
[7] bob 完成 B → unblocked=[出报表]（C 解锁了）
[8] carol 下一轮轮询就拿到了：出报表 token=…1713
[9] carol 完成 C，任务板清空：抓取数据(completed) 清洗数据(completed) 出报表(completed)
[10] token 历史表里累计 3 条，全生命周期不可复用
```

逐条看这十行里藏了什么：

`[1]` `[2]` Lead 唯一的动作是把任务写进任务板。它不需要知道有几个 worker、谁空着。三条任务全是 `pending`，`owner` 是 `null`。

`[3]` `[4]` **这是本章的核心画面**：alice 和 bob 各自调用 `claimNext`，一个拿到 A、一个拿到 B。注意它们拿的不是同一条——不是靠"Lead 事先分配"，而是靠数据库那一次条件 `UPDATE`：谁先把某行从 `pending` 改成 `in_progress`，那行就归谁，另一个进来发现改不动，自动往下找下一条。

`[3]` 里还有两样东西是第 12 章的任务模型没有的：`token=…1711` 是认领凭证，`租约到 14:01:00` 是这次认领的有效期（默认 60 秒，实验里显式设成了 60000ms）。

`[5]` carol 也在轮询，但拿到 `undefined`。C 依赖 A 和 B，两者都没完成，C 不是 ready task。`undefined` 不是错误，是"现在没活"的正常信号，worker 收到它就去睡一会儿再来。

`[6]` `[7]` 完成任务要出示 token。`unblocked` 是这次完成解锁了哪些下游：完成 A 时 C 还差 B，所以是空的；完成 B 时 C 的依赖齐了，`unblocked=[出报表]`。**注意解锁不等于派发**——系统没有把 C 推给任何人，只是把它变成了 ready task。

`[8]` carol 下一轮轮询自然就看到了 C。整个依赖推进过程里，没有任何一方在"盯着谁完成了、下一项该给谁"。

`[10]` 三次认领产生了三个 token，都留在历史表 `task_claim_tokens` 里。这张表的作用不是查账，是**保证同一个 token 在整个数据库生命周期内不可能出现第二次**——后面会讲为什么"当前唯一"不够。

用一张图看这条链路：

```text
        Lead                      共享任务板 (tasks.sqlite3)              空闲队友
         │                                                                  │
  create_task ────────────►  ┌──────────────────────────┐                    │
    A B C(依赖A,B)           │ A pending  owner=null    │                    │
                             │ B pending  owner=null    │  ◄──── claimNext ──┤ alice
                             │ C pending  owner=null    │                    │
                             └──────────────────────────┘  ──── A + token ──►│
                                                                             │
                             ┌──────────────────────────┐  ◄──── claimNext ──┤ bob
                             │ A in_progress owner=alice│                    │
                             │ B in_progress owner=bob  │  ──── B + token ──►│
                             │ C pending                │                    │
                             └──────────────────────────┘  ◄──── claimNext ──┤ carol
                                                            ──── undefined ─►│ (睡 5s 再来)
```

---

## 为什么不能「先扫描，再写 owner」

第 12 章的任务图已经能表达依赖了。很自然的想法是：给 worker 加个循环，扫一遍任务列表，找到 ready task，把 `owner` 写成自己。

这个写法在单个 worker 时完全正常，在两个 worker 时会**重复派活**。问题出在"扫"和"写"之间。

### 三种做不对的写法

**❌ 写法一：先读列表，再写 owner**

```typescript
// 错误示范
const tasks = await store.listTasks();                    // ①
const ready = tasks.find((t) => t.status === "pending" && depsDone(t));
if (ready !== undefined) {
  await store.assign(ready.id, myName);                   // ②
}
```

`①` 和 `②` 是两次独立的存储访问。alice 在 `①` 读到"A 是 pending"，还没走到 `②`，bob 也执行了 `①`，也读到"A 是 pending"。两个人都走到 `②`，两个人都认为自己拿到了 A。

这就是 **TOCTOU**（Time-Of-Check To Time-Of-Use）：检查的那一刻状态是真的，用的那一刻状态已经变了。间隔多短都不行——异步 I/O 之间随时可能切换。

**❌ 写法二：加个内存锁**

```typescript
// 错误示范
await mutex.runExclusive(async () => {
  const ready = (await store.listTasks()).find(isReady);
  if (ready !== undefined) await store.assign(ready.id, myName);
});
```

同一个进程内确实不会重复了。但队友可能跑在不同进程里（第 13 章起就有后台作业和独立入口），内存锁跨不出进程边界。实验 5 会用两个真实子进程验证这一点。

**❌ 写法三：写完再检查，发现冲突就退回**

```typescript
// 错误示范
await store.assign(taskId, myName);
const current = await store.getTask(taskId);
if (current.owner !== myName) {
  await store.unassign(taskId);   // 退回
}
```

"退回"本身也是一次非原子写。两个 worker 可能同时退回，任务变成没人做；也可能退回时覆盖掉对方刚写进去的合法 owner。用补偿逻辑修竞态，通常是引入第二个竞态。

### 正确的形状：让"选择"和"状态迁移"在同一个事务里

结论很短：**不能有"先看一眼再决定"的间隙**。判断"这条是不是 pending"和"把它改成 in_progress"必须是同一条语句的一部分，由存储层保证原子性：

```sql
UPDATE tasks
   SET status = 'in_progress', owner = ?, claim_token = ?, lease_expires_at_utc = ?
 WHERE id = ? AND status = 'pending' AND owner IS NULL
```

`WHERE` 里的 `status = 'pending' AND owner IS NULL` 就是那道"检查"，它和"写"在同一条 SQL 里。谁执行成功，`getRowsModified()` 返回 1；谁慢一步，`WHERE` 匹配不到行，返回 0——它就知道自己没抢到，继续找下一条。

这也是为什么本章要换存储后端：JSON 文件做不到"带条件的原子更新"，SQLite 可以。

---
## 独立 SQLite 后端：不迁移，也不双写

本章新增 `SqliteTaskStore`，但**没有**把第 12 章的 `JsonTaskStore` 数据搬过来，也没有让两者同步。这是有意的：

| 做法 | 为什么不选 |
| --- | --- |
| 写迁移脚本，把 JSON 图导入 SQLite | 迁移逻辑要处理各种历史脏数据，教学上是噪音，且本章要讲的是并发不是迁移 |
| 两边都写，读时以 SQLite 为准 | 双写就有一致性窗口，一旦不一致要靠额外规则裁决，等于把复杂度搬进业务代码 |
| 加一层适配器，运行时二选一 | 那就要维护两套并发语义（JSON 没有原子认领），测试面翻倍 |
| **本章：两个独立后端，按能力位选一个** | 一个问题一个真相来源。P17 只用 SQLite，P12~P16 只用 JSON，互不知情 |

实验 2 的实测输出直接证实了它们互不可见：

```text
===== backend =====
SQLite 数据库路径: <root>\.agent_tutorial\tasks.sqlite3
SQLite listTasks(): 1 条
JSON  listTasks(): 0 条
SQLite 里的任务: 1701 / 只写进 SQLite
--- JSON 也写一条之后 ---
SQLite listTasks(): 1 条
JSON  listTasks(): 1 条
```

组合根层面靠能力位强制：P17 要求 `workStealingRuntime`，同时**拒绝**传入 JSON 版 `taskStore`。传错了会在启动时报错，而不是运行到一半才发现认领不原子（`src/bootstrap.ts`）：

```typescript
const usesJsonTaskStore =
  profile.capabilities.has("task_dag_json") && !profile.capabilities.has("task_dag_sqlite");
```

---

## 表结构：稳定顺序、依赖、token 历史

数据库文件在 `<workspace>/.agent_tutorial/tasks.sqlite3`。四张表，每次打开都执行 `CREATE TABLE IF NOT EXISTS`（`src/adapters/task-sqlite.ts:456`）。

实验 3 直接把表结构和真实行打出来了：

```text
===== schema =====
表清单:
  sqlite_sequence
  task_claim_tokens
  task_dependencies
  task_metadata
  tasks
schema_version: 1
tasks 行（按 sequence）:
  seq=1 id=…1701 status=in_progress owner=alice token=…1711 lease=2026-07-27T14:00:30.000Z
  seq=2 id=…1702 status=pending owner=null token=null lease=null
task_dependencies 行:
  task=…1702 dep=…1701 position=0
task_claim_tokens 行:
  token=…1711 task=…1701
```

四张表各管一件事：

| 表 | 作用 | 关键设计 |
| --- | --- | --- |
| `task_metadata` | 只存 `schema_version = '1'` | 打开时版本不是 `'1'` 就直接报错，不做隐式升级 |
| `tasks` | 任务本体 | `sequence INTEGER PRIMARY KEY AUTOINCREMENT` 提供稳定创建顺序 |
| `task_dependencies` | 依赖边 | 带 `position`，保证 `blocked_by` 数组顺序可复现 |
| `task_claim_tokens` | token 历史 | `token TEXT PRIMARY KEY`，同一 token 全生命周期只能出现一次 |

### 为什么要 `sequence` 而不是靠 `id` 排序

`claimNext` 必须按一个**确定的顺序**扫描候选，否则两个 worker 可能因为顺序不同而反复撞在同一条上。任务 ID 是 UUID，字典序和创建顺序无关。`sequence` 是自增整数，`ORDER BY sequence ASC` 就是创建顺序，跨进程、跨重启都一样。

### 三态一致性写进了 CHECK 约束

`tasks` 表上有一条三选一的 `CHECK`，把"状态和字段必须匹配"钉死在数据库层：

```sql
CHECK(
  (status = 'pending'     AND owner IS NULL     AND claim_token IS NULL     AND lease_expires_at_utc IS NULL)
  OR (status = 'in_progress' AND owner IS NOT NULL AND claim_token IS NOT NULL AND lease_expires_at_utc IS NOT NULL)
  OR (status = 'completed'   AND owner IS NOT NULL AND claim_token IS NULL     AND lease_expires_at_utc IS NULL)
)
```

对照上面实验 3 的输出：`seq=1` 是 `in_progress`，owner/token/lease 三样都有；`seq=2` 是 `pending`，三样都是 `null`。这条约束的价值是：**即使代码写错，也写不进一个自相矛盾的状态**。比如"有 owner 但状态还是 pending"会被数据库直接拒绝。

`completed` 那一行值得单独看：完成后 owner 保留（要知道是谁做的），但 token 和 lease 清空（凭证用过就废、租约无意义了）。

---

## 原子认领：三层互斥

上面说清了"一条条件 UPDATE 就够"。但真正跑起来还要处理三种并发场景，所以代码里是三层互斥，一层挡一种情况。

```text
  同一进程内两个队友同时认领
        │
        ▼
  ┌─────────────────────────────────────────┐
  │ 第 1 层：withProcessMutex(数据库路径)     │  同进程串行排队，不去争文件锁
  └─────────────────────────────────────────┘
        │
        ▼   不同进程同时认领
  ┌─────────────────────────────────────────┐
  │ 第 2 层：proper-lockfile 锁 .lock 文件    │  跨进程互斥，Windows 竞态会重试
  └─────────────────────────────────────────┘
        │
        ▼   同一进程内、锁持有期间仍要保证事务原子
  ┌─────────────────────────────────────────┐
  │ 第 3 层：BEGIN IMMEDIATE 写事务           │  扫描 + UPDATE 在一个事务里
  └─────────────────────────────────────────┘
        │
        ▼
  ┌─────────────────────────────────────────┐
  │ 最终互斥点：UPDATE ... WHERE status=     │  getRowsModified() 必须等于 1
  │ 'pending' AND owner IS NULL              │
  └─────────────────────────────────────────┘
```

每一层少了会漏什么：

| 层 | 挡住什么 | 去掉会怎样 |
| --- | --- | --- |
| `withProcessMutex` | 同进程多个队友同时打文件锁 | 不会出错，但同进程内部先自己抢一遍文件锁，白白重试 |
| 文件锁 | 不同进程同时进入写事务 | 两个进程各自打开 sql.js 内存库，各自 export 落盘，**后写的覆盖先写的**，一次认领凭空消失 |
| `BEGIN IMMEDIATE` | 事务内读到的图和写回的状态不一致 | 读到的 DAG 可能是旧的，依赖判断基于过期数据 |
| 条件 `UPDATE` | 两个事务改同一行 | 即使前三层都在，逻辑判断和写入之间仍有 TOCTOU |

### 为什么是 `BEGIN IMMEDIATE` 而不是 `BEGIN`

SQLite 默认的 `BEGIN`（deferred）在**第一次真正写**的时候才去拿写锁。也就是说事务开头的那些 `SELECT` 是在没有写锁的情况下读的，读完准备写时才发现拿不到锁。`BEGIN IMMEDIATE` 一开始就拿保留锁，让"扫描 + 更新"整段处于写锁保护下。

### `claimNext` 的完整逻辑

`src/adapters/task-sqlite.ts:198`：

```typescript
async claimNext(owner: string): Promise<TaskClaim | undefined> {
  // 自动认领按稳定创建顺序选择首个 ready task；没有候选时返回 undefined 进入空闲轮询。
  const normalizedOwner = normalizeOwner(owner);
  return await this.#transaction(true, (database) => {
    // 先释放过期租约，再加载完整 DAG，按创建顺序找第一个没有未完成依赖的 pending task。
    const now = this.#now();
    releaseExpired(database, now);
    const graph = loadGraph(database);
    for (const id of graph.order) {
      const task = graph.tasks.get(id);
      if (task === undefined || task.status !== TaskStatus.PENDING) continue;
      if (
        task.blockedBy.some(
          (dependency) => graph.tasks.get(dependency)?.status !== TaskStatus.COMPLETED,
        )
      ) {
        continue;
      }
      return this.#claim(database, graph, task, normalizedOwner, now);
    }
    return undefined;
  });
}
```

四件事的顺序很重要：

1. **`releaseExpired`**：先把过期租约的任务放回 `pending`，否则一个崩掉的 worker 会永久占住任务；
2. **`loadGraph`**：一次性把整张 DAG 读进内存，依赖判断基于同一快照；
3. **按 `graph.order` 遍历**：即创建顺序，跳过非 `pending` 和依赖未完成的；
4. **`#claim`**：命中就在同一事务里完成认领，然后立刻 `return`——一次调用只认领一条。

整个函数体在 `#transaction(true, ...)` 的回调里，也就是在三层互斥内部。`true` 表示这是写操作，允许创建存储目录。

### `#claim`：token 先登记，再条件更新

`src/adapters/task-sqlite.ts:278`，这是认领的最后一步：

```typescript
const token = this.#nextClaimToken();
const expiresAt = new Date(now.getTime() + this.#leaseDurationMs);
// 历史 token 表保证同一 token 全生命周期不可复用；冲突会让整个认领事务回滚。
try {
  database.run("INSERT INTO task_claim_tokens(token, task_id) VALUES (?, ?)", [token, task.id]);
} catch (error) {
  throw new TaskStorageError("Generated claim token already exists", { cause: error });
}
// 条件 UPDATE 是最终互斥点：并发扫描即使看到同一 pending task，也只有一个事务能改到 in_progress。
const changed = database.run(
  "UPDATE tasks SET status = 'in_progress', owner = ?, claim_token = ?, lease_expires_at_utc = ? WHERE id = ? AND status = 'pending' AND owner IS NULL",
  [owner, token, encodeDate(expiresAt), task.id],
);
if (changed.getRowsModified() !== 1) {
  throw new TaskStateError(`Task ${task.id} could not be atomically claimed`);
}
```

两个顺序细节：

**token 的 INSERT 在 UPDATE 之前。** 如果反过来，先把任务改成 `in_progress` 再登记 token，那么 token 冲突时任务已经被改了，得再写补偿逻辑退回去。放在前面，冲突直接抛错，整个事务 `ROLLBACK`，任务纹丝不动。实验 7 会验证这一点。

**`getRowsModified() !== 1` 抛错而不是返回 undefined。** 走到这一行说明代码刚刚判断过这条是 `pending`，结果改不动——这意味着有别的路径绕过了互斥，是真正的异常，应该炸出来而不是静默继续。

### 事务外壳：`#transaction`

`src/adapters/task-sqlite.ts:310` 是所有公开操作的共同入口，三层互斥就在这里编排：

```typescript
return await withProcessMutex(paths.database, async () => {          // 第 1 层
  let release: (() => Promise<void>) | undefined;
  let raceRetries = 0;
  try {
    while (release === undefined) {
      try {
        release = await acquireFileLock(paths.root, {                // 第 2 层
          lockfilePath: paths.lock, realpath: true,
          stale: LOCK_STALE_MS, update: LOCK_UPDATE_MS, retries: 0,
        });
      } catch (error) {
        if (isWindowsLockRace(error) && raceRetries < MAX_WINDOWS_LOCK_RACE_RETRIES) {
          raceRetries += 1;
          await delay(LOCK_RETRY_MS);
          continue;
        }
        if (hasErrorCode(error, "EEXIST") || hasErrorCode(error, "ELOCKED")) {
          await delay(LOCK_RETRY_MS);
          continue;
        }
        throw error;
      }
    }
    await this.#validatePaths(paths);
    const database = await openDatabase(paths);
    try {
      database.run("BEGIN IMMEDIATE");                              // 第 3 层
      try {
        const result = operation(database);
        database.run("COMMIT");
        // sql.js 是内存数据库，提交后必须 export 并通过临时文件原子替换落盘。
        await persistDatabase(paths.database, database.export());
        return result;
      } catch (error) { /* ROLLBACK 后包装错误 */ }
    } finally {
      database.close();
    }
  } finally {
    if (release !== undefined) await release();
  }
});
```

三处值得注意：

**`retries: 0` 加手写重试循环。** `proper-lockfile` 自带重试，但这里关掉了，改成自己判断错误类型。原因是 Windows 上锁文件在删除/重命名过程中会短暂返回 `EBUSY` / `ENOTEMPTY` / `EPERM`，这些是竞态不是失败，值得重试（上限 100 次，每次等 10ms）；其他错误应该立刻上抛，不能被通用重试掩盖。

**`COMMIT` 之后还要落盘。** `sql.js` 是编译成 WASM 的 SQLite，数据库整个在内存里。`COMMIT` 只让内存中的事务生效，必须再 `export()` 拿到字节，写回文件。

**落盘走临时文件 + rename。** `persistDatabase` 的动作是：在同目录建 `.{uuid}.tmp`，用 `open(temporary, "wx")` 独占创建，`writeFile`，`sync()` 刷盘，`close()`，最后 `rename()` 覆盖。`rename` 在同一文件系统内是原子的，所以任何时刻其他进程读到的都是完整的旧版本或完整的新版本，不会是半写文件。

---
## claim token 与 lease：完成任务的两道校验

### 认领结果不是一句字符串

`claimNext` 成功后返回的是一个三字段结构（`src/features/work-stealing.ts`）：

```typescript
export interface TaskClaim {
  readonly task: Task;
  readonly claimToken: string;
  readonly leaseExpiresAtUtc: Date;
}
```

如果只返回 `Task`，worker 完成时就只能说"我做完了 task X"。系统凭什么相信它？它可能是租约已过期的旧持有者，也可能是根本没认领过的另一个 worker。

所以 `completeTask` 的签名是三个参数：

```typescript
async completeTask(taskId: string, owner: string, claimToken: string): Promise<TaskCompletion>
```

owner 和 token **必须同时匹配**当前行。对比第 12 章的 `complete_task` 只需要 `task_id`，这是本章工具契约上最直接的变化。

### 为什么 token 要"全生命周期唯一"

`tasks.claim_token` 上已经有 `UNIQUE` 约束了，为什么还要一张 `task_claim_tokens` 历史表？

因为 `tasks.claim_token` 在任务完成时会被清成 `NULL`。一旦清空，同一个 token 值就可以再次插入 `tasks`——`UNIQUE` 约束只管"当前同时存在的值"。假设 UUID 生成器出了问题（被注入了固定值、随机源有缺陷），一个已完成任务用过的 token 可能被下一次认领复用，那么旧的完成请求就能凭旧 token 完成新的认领。

历史表用 `token TEXT PRIMARY KEY` 记录**所有出现过的** token，并且从不删除。冲突时 `INSERT` 失败，整个认领事务回滚。

实验 7 用一个"永远返回同一个 token"的生成器验证：

```text
===== token-history =====
第一次认领拿到 token: …1711
第二次认领（同一 token）→ TaskStorageError: Generated claim token already exists
第二条任务仍然是: status=pending owner=null
说明：token 冲突让整个认领事务回滚，没有留下半个状态。
```

第二条任务保持 `pending` / `owner=null`，这就是"token INSERT 放在 UPDATE 之前"的收益。

### lease 是半开区间

租约在认领时固化成一个**绝对时间**：`lease_expires_at_utc = 认领时刻 + leaseDurationMs`（默认 `60_000`）。存的是截止时刻而不是时长，所以后续改配置不会追溯影响已发出的租约。

有效期是半开区间 `[claimed_at_utc, lease_expires_at_utc)`：右端点那一刻**已经过期**。判定代码就一个 `>=`（`src/adapters/task-sqlite.ts:237`）：

```typescript
if (current.status === TaskStatus.IN_PROGRESS && lease !== null && now >= lease) {
  releaseExpired(database, now);
  expired = true;
  return undefined;
}
```

`releaseExpired` 的 `WHERE` 用的也是 `<=`，两处口径一致：

```sql
UPDATE tasks SET ... WHERE status = 'in_progress'
  AND lease_expires_at_utc IS NOT NULL
  AND lease_expires_at_utc <= ?
```

为什么要选半开而不是闭区间？因为边界必须归属唯一。如果 `now == lease` 既算"还有效"又算"可释放"，那么在这一毫秒里，原持有者能完成任务，同时另一个 worker 也能认领它——两个人都拿到了同一条任务的合法所有权。半开区间让这一刻只有一个答案：过期。

实验 6 卡在这个边界上测：

```text
===== lease =====
认领时刻:     2026-07-27T14:00:00.000Z
租约截止:     2026-07-27T14:00:30.000Z
claim token:  …1711

--- 时间推到 +29999ms（租约内） ---
当前时刻: 2026-07-27T14:00:29.999Z
claimNext("bob") → undefined（拿不到）

--- 时间推到 +30000ms（正好等于截止时间） ---
当前时刻: 2026-07-27T14:00:30.000Z
alice 用原 token 完成 → TaskLeaseExpiredError: Task 00000000-0000-4000-8000-000000001701 claim lease expired
任务已回到: status=pending owner=null
bob 重新认领 → token=…1712 截止=2026-07-27T14:01:00.000Z
alice 再用旧 token 完成 → TaskClaimError: Task 00000000-0000-4000-8000-000000001701 owner or claim token does not match the active claim
bob 用新 token 完成 → status=completed
```

`+29999ms` 拿不到、`+30000ms` 过期，边界行为和 `>=` 完全对上。

最后三行还展示了两种不同的错误：

| 场景 | 错误类型 | 含义 |
| --- | --- | --- |
| 租约刚过期，原持有者来完成 | `TaskLeaseExpiredError` | 你的租约到点了，任务已放回 pending |
| 任务已被别人重新认领，你拿旧 token 来完成 | `TaskClaimError` | 你的凭证不是当前凭证 |

`TaskLeaseExpiredError extends TaskClaimError`，所以只想统一处理"认领相关失败"时 catch 父类就够；要区分"过期"和"凭证不对"时再判子类。注意 `TaskLeaseExpiredError` 是在事务**外面**抛的——事务内先把过期租约释放掉并正常提交，然后才根据 `expired` 标记抛错，这样"释放"这个副作用不会被 `ROLLBACK` 一起撤掉。

### owner 不能被工具参数伪造

worker 是模型驱动的，模型能编造任何工具参数。如果 `claim_task` 的参数里有 `owner` 字段，模型就能以别人的身份认领。

所以 `claim_task` 和 `complete_task` 的 schema 里**根本没有 owner 字段**，owner 来自 `ToolContext.identity`——由组合根在创建每个队友 Runner 时固定写入（`identity: name`），模型无法触及。`DirectTaskClaimService` 负责这层转换。

实验 9 直接验证：

```text
===== identity =====
claim_task 返回（identity=alice）:
  {"claim_token":"00000000-0000-4000-8000-000000001711","lease_expires_at_utc":"...","task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001701","owner":"alice","status":"in_progress","subject":"看 owner 从哪来"}}

工具参数里没有 owner 字段，owner 由 identity 决定 → alice

bob 拿 alice 的 token 完成 → Error [task_claim_mismatch]: Task ...1701 owner or claim token does not match the active claim
alice 拿自己的 token 完成 → {"task":{...,"owner":"alice","status":"completed",...},"unblocked":[]}

试着在参数里塞 owner → Error [invalid_arguments]: Tool arguments failed schema validation
```

最后一行是 `z.strictObject` 的作用：schema 之外的字段一律拒绝，而不是忽略。**忽略未知字段是危险的默认行为**——模型传了 `owner` 却被静默丢掉，它会以为自己成功指定了 owner。

注意 bob 那一行：即使 bob 拿到了 alice 的 token（token 是对的），owner 不匹配也完不成。两个条件是"与"的关系。

---

## 五个工具，Lead 和队友拿到的集合不同

`leasedTaskToolDefinitions` 返回一个固定顺序的五元组。实验 8 的实测：

```text
===== tools =====
Lead   工具集: create_task, get_task, list_tasks, claim_task, complete_task
队友   工具集: get_task, list_tasks, claim_task, complete_task
队友少了: create_task

每个工具的 effect 与参数:
  create_task   effect=write 参数=[subject, description, blocked_by] 必填=[subject, description, blocked_by]
  get_task      effect=read  参数=[task_id] 必填=[task_id]
  list_tasks    effect=read  参数=[] 必填=[]
  claim_task    effect=write 参数=[task_id] 必填=[task_id]
  complete_task effect=write 参数=[task_id, claim_token] 必填=[task_id, claim_token]

maxIdlePolls 默认 = 12
只传 subject 调 create_task → isError=false
  {"blocked_by":[],"description":"","id":"...1701","owner":null,"status":"pending","subject":"只传 subject"}
```

裁剪的实现就是数组切片（`src/features/work-stealing.ts`）：

```typescript
const definitions = leasedTaskToolDefinitions(this.#store, this.#claimService);
this.#leadToolDefinitions = definitions;
this.#teammateToolDefinitions = Object.freeze(definitions.slice(1));
```

`slice(1)` 去掉的正好是第一个 `create_task`。队友不能建任务，只能做已有任务——**这是防止任务自我繁殖**：如果队友能建任务，一个循环里的模型可以不停造活给自己做，任务板永不收敛。要拆解任务，走 Lead 或走计划审批。

上面输出里有个容易误读的地方：`create_task` 的"必填"里出现了 `description` 和 `blocked_by`。这是 `z.toJSONSchema` 对带 `.default()` 的字段的表示方式，**运行时并不真的必填**——最后两行验证了只传 `subject` 也能成功，`description` 补成 `""`，`blocked_by` 补成 `[]`。

### wire format：camelCase 与 snake_case 的边界

内部领域模型用 camelCase（`blockedBy`、`claimToken`、`leaseExpiresAtUtc`），暴露给模型的 JSON 用 snake_case（`blocked_by`、`claim_token`、`lease_expires_at_utc`）。转换只发生在两个函数里：

```typescript
// 出站：Task → 模型看到的 JSON
function taskPayload(task: Task) {
  return { blocked_by: ..., description: ..., id: ..., owner: ..., status: ..., subject: ... };
}
// 出站：TaskClaim → 模型看到的 JSON
function claimPayload(claim: TaskClaim) {
  return { claim_token: ..., lease_expires_at_utc: ..., task: taskPayload(claim.task) };
}
```

字段按字母序排列，不是随手写的：**模型看到的 JSON 顺序稳定，prompt 前缀才能命中缓存**。

入站方向由 zod schema 负责，`create_task` 的 `blocked_by` 会映射成内部的 `blockedBy`——`tests/ch17-work-stealing.test.ts` 有一条测试专门断言这次映射。

---

## idle loop 的真实优先级

前面都是存储层。现在看队友什么时候调用 `claimNext`。

核心在 `src/features/teammates.ts:573` 的 `#runWorker`。每一轮循环开头是一次 `scan`，优先级固定为四级：

```text
每轮扫描（在 #withRegistry 锁内完成）：

  ① Mailbox 有消息？ ──── 有 ──► 处理消息（shutdown / plan 回复 / 普通任务），本轮结束
        │ 没有
        ▼
  ② work stealing 配好了吗？ ── 没配 ──► 状态设为 Idle，退出 worker
        │ 配了
        ▼
  ③ planAllowsEffectful？ ──── false ──► 跳过认领，直接进第 4 步的等待
        │ true
        ▼
  ④ claimNext(自己的名字) ─── 拿到 ──► 跑任务，报告 Lead，continue
        │ undefined
        ▼
     waitForPoll(可被中断) ──► 被唤醒？continue（不计空转）
                            └► 正常睡醒？空转 +1，到上限就退出
```

代码原文：

```typescript
const scan = await this.#withRegistry(async () => {
  this.#ensureAvailable();
  const message = await this.#store.claim(worker.teammate.name);
  if (message !== undefined) {
    return { message, workStealing: undefined, claim: undefined };
  }
  const workStealing = this.#workStealingRuntime;
  if (workStealing === undefined) {
    return { message: undefined, workStealing: undefined, claim: undefined };
  }
  const protocol = this.#protocolRuntime;
  if (protocol === undefined) {
    throw new TeammateStateError("Work-stealing runtime requires Protocol runtime");
  }
  if (!(await protocol.planAllowsEffectful(worker.teammate.name))) {
    return { message: undefined, workStealing, claim: undefined };
  }
  return {
    message: undefined,
    workStealing,
    claim: await workStealing.claimNext(worker.teammate.name),
  };
});
```

### 为什么 Mailbox 必须排第一

如果先认领任务再看 Mailbox，那么任务板上只要还有活，队友就一直在做任务，`shutdown` 请求排在队列里永远等不到处理——**ready task 会把控制消息饿死**。Lead 想优雅关掉一个队友，得等任务板空了才行，这显然不对。

`tests/ch17-runtime.test.ts` 有一条测试锁住这个顺序：任务板上有 ready task，同时投递一条 shutdown 协议消息，断言 `response[0].toPayload().kind === "protocol"`、任务保持 `PENDING`、`model.requests` 长度为 1（模型只被调用了一次，是处理协议消息那次，不是跑任务）。

### 为什么计划门禁排在认领之前

第 16 章的规则是：计划没批准，队友不能执行有副作用的工具。认领任务本身就是写操作（改数据库状态），而且认领之后紧接着就要真做事。所以门禁必须在 `claimNext` **之前**检查，而不是等模型调工具时才拦。

注意 `planAllowsEffectful` 的语义：没有提交过计划时返回 `true`（保留第 15 章的既有协作行为），只有最新计划处于 `Pending` 或 `Rejected` 时才返回 `false`。

实验 11 验证 `Pending` 的拦截效果：

```text
===== plan-gate =====
任务状态（计划未批准）: pending owner=null
sleeper.calls = [5,5]  ← 每个元素是一次轮询等待的秒数
idleTimeoutCount("alice") = 1
自动认领 prompt 条数 = 0
任务从未离开 pending: true
```

`sleeper.calls = [5,5]` 说明它老老实实空转了两轮（`maxIdlePolls` 设成 2），每轮等 5 秒；`自动认领 prompt 条数 = 0` 说明 `claimNext` 一次都没被调用——任务不是"认领后被拒绝"，是**根本没进入认领**。

### 自动认领后，模型仍须显式完成

拿到 claim 后，队友并不是跑完就自动标记完成。它收到的是一段结构化 prompt（`renderClaimPrompt`）：

```typescript
renderClaimPrompt(claim: TaskClaim): string {
  const payload = {
    claim_token: claim.claimToken,
    lease_expires_at_utc: claim.leaseExpiresAtUtc.toISOString(),
    task: taskPayload(claim.task),
  };
  return `<auto-claimed-task>\n${JSON.stringify(payload)}\n</auto-claimed-task>`;
}
```

实验 10 打出了队友真实收到的内容：

```text
===== idle-loop =====
收到事件数: 2

队友收到的自动认领 prompt（共 1 条）:
  <auto-claimed-task>
  {"claim_token":"00000000-0000-4000-8000-000000001731","lease_expires_at_utc":"...","task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001721","owner":"alice","status":"in_progress","subject":"autonomous"}}
  </auto-claimed-task>

任务终态: autonomous=completed
idleTimeoutCount("alice") = 1
drainEvents() = []
```

**为什么不自动完成？** 因为"Runner 跑完了"和"任务做成了"是两件事。模型可能中途发现做不了、可能只做了一半、可能需要先问 Lead。让它自己调 `complete_task` 并出示 token，是让"完成"成为一个有据可查的显式决定。prompt 里附上 token 和租约截止，正是为了让模型有据可依。

### claim token 复用为幂等键

启动这一轮 Runner 时，claim token 被当成 `idempotencyKey`：

```typescript
const finalText = (
  await worker.runner.run(workStealing.renderClaimPrompt(claim), {
    idempotencyKey: claim.claimToken,
    signal,
  })
).finalText;
```

一次认领对应一个 token，token 又是天然唯一的，正好满足幂等键的要求。如果 Runner 因为网络重试重复提交，恢复层能靠这个键识别出是同一次运行，不会重复执行。这里没有另造一个 ID——**token 已经是这一轮工作的身份**。

---
## 有界 polling，不伪造 shutdown

"空闲时轮询"听起来简单，但一个无限轮询的循环在测试里就是永不结束的进程。本章的做法是给空转次数设上限，并把"到上限了"变成一个可以被观测的事件。

`WorkStealingRuntime` 两个默认值：

| 参数 | 默认值 | 作用 |
| --- | --- | --- |
| `pollIntervalSeconds` | `5` | 每次没活时等多久再来 |
| `maxIdlePolls` | `12` | 连续空转多少次就退出 worker 循环 |

两个都在构造时校验，非正数或非整数直接 `RangeError`。

计数逻辑（`src/features/teammates.ts:632`）：

```typescript
this.#setStatus(worker, TeammateStatus.Idle);
const wakeup = new AbortController();
worker.pollWakeup = wakeup;
// waitForPoll 可被 #wakeWorker 提前中止，让空闲队友在收到新消息后立即重启扫描。
await workStealing.waitForPoll(wakeup.signal);
if (worker.pollWakeup === wakeup) worker.pollWakeup = undefined;
if (wakeup.signal.aborted) continue;
worker.idlePolls += 1;
if (worker.idlePolls >= workStealing.maxIdlePolls) {
  worker.idleTimeoutCount += 1;
  worker.idleTimeout.resolve();
  return;
}
```

三个设计点：

**`if (wakeup.signal.aborted) continue;` 在计数之前。** 被新消息唤醒不算一次空转。这很重要：一个繁忙的队友可能被反复唤醒，如果每次唤醒都消耗预算，它会因为"太忙"而提前退出循环，这完全是反的。`tests/ch17-runtime.test.ts` 有一条测试专门覆盖：用 `TwoWakeSleeper(1)` 配 `maxIdlePolls: 1`，中途唤醒一次，断言 `idleTimeoutCount === 1`——如果唤醒消耗了预算，这个数会不对。

**空转到顶是 `return`，不是伪造一个 shutdown。** 退出时只做两件事：`idleTimeoutCount += 1` 和 `idleTimeout.resolve()`。它**不会**发一个 shutdown 事件、不会改成 `Shutdown` 状态、不会往 Lead 的 Mailbox 里塞消息。因为"暂时没活"和"被要求关闭"是两件完全不同的事，混在一起会让 Lead 收到假的关机信号。

**`idlePolls` 在拿到活或收到消息时清零。** 拿到 claim 时 `worker.idlePolls = 0`，处理 Mailbox 消息时也是 `worker.idlePolls = 0`。所以"12 次"的含义是**连续**空转 12 次，不是累计。`tests/ch17-runtime.test.ts` 用 `maxIdlePolls: 2` 配合三次投递验证：最终 `sleeper.calls === 4`——如果不清零，第三次投递之前就该退出了。

### 外部怎么知道 worker 退出了

两个观测接口：

```typescript
teammates.idleTimeoutCount(name)      // 该队友因空转退出过几次
await teammates.waitForIdleTimeout(name)  // 等到该队友这一次空转退出
```

这两个是本章测试和实验能稳定收敛的关键——不需要 `sleep(60000)` 然后猜进程是不是退了，直接 `await` 一个明确的信号。`send()` 时如果队友处于 Idle，会调 `#wakeWorker`（内部只做 `worker.pollWakeup?.abort()`）中断等待，再重启 worker。

### 唤醒和睡眠的实现

默认的 `AsyncioWorkStealingSleeper.sleep()` 逻辑：如果 `wakeup` 已经 aborted 就立刻返回，否则让 `setTimeout` 和 `wakeup` 的 abort 事件赛跑，谁先到就结束等待。这样 5 秒的间隔既能正常睡满，也能被新消息立刻打断。

---

## 组合根：必须共享同一个 runtime

`WorkStealingRuntime` 在构造时会检查 `claimService.store` 是不是自己的 store：

```typescript
if (claimService.store !== options.store) {
  throw new Error("claimService must use the WorkStealingRuntime store");
}
```

为什么这么严？因为如果 Lead 的工具指向 store A、队友的自动认领指向 store B，那么 Lead 建的任务队友永远看不到，队友完成的任务 Lead 也不知道。这种错配不会报错，只会表现为"系统好像不工作"——极难排查。宁可在启动时炸掉。

`src/bootstrap.ts` 里，Lead 工具、子 Agent 工具、队友工具三处都从同一个 `deps.workStealingRuntime` 取：

```typescript
registerLeasedTaskTools(tools, deps.workStealingRuntime.store, deps.workStealingRuntime.claimService);
```

配置顺序也是固定的：

```text
configureProtocol(protocolRuntime)      ← 必须先配协议
        ↓
configureWorkStealing(workStealingRuntime)  ← 依赖协议做计划门禁
        ↓
configureRunnerFactory(...)             ← 最后配 Runner 工厂
```

`configureWorkStealing` 上有两道显式检查（`src/features/teammates.ts:231`）：

| 检查 | 抛出 |
| --- | --- |
| 已经配过，或 runtime 已经 start | `TeammateStateError("Work-stealing runtime must be configured once before start")` |
| 协议 runtime 还没配 | `TeammateStateError("Work-stealing runtime requires Protocol runtime")` |

第二条对应 idle loop 里那个 `if (protocol === undefined) throw` 分支——门禁检查不能被跳过，所以协议必须先在位。`tests/ch17-runtime.test.ts` 第一条测试就是验证这个：不配协议直接 `configureWorkStealing`，期待 `TeammateStateError`。

### 两个运行入口

| 入口 | 命令 | 说明 |
| --- | --- | --- |
| 章节专属 | `npm run ch17 -- --prompt "..."` | 固定跑 P17 |
| 通用 CLI | `npm run agent-tutorial -- run --chapter 17 --prompt "..."` | `--chapter` 决定能力位 |

通用 CLI 里 SQLite store 是按能力位条件构造的（`src/cli.ts`）：

```typescript
const workStealingRuntime = profile.capabilities.has("task_dag_sqlite")
  ? new WorkStealingRuntime({ store: new SqliteTaskStore(workspace) })
  : undefined;
```

---

## 运行第 17 章

这一节从零开始，一步一步跑起来。**第 0 到第 3 步不需要 API key**。

### 第 0 步：确认环境

```powershell
node --version
npm --version
```

Node 需要 22 或更高。本文实测：

```text
v25.7.0
11.19.0
```

### 第 1 步：进入代码目录

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Test-Path package.json
```

应该输出：

```text
True
```

如果是 `False`，说明路径不对。本仓库的代码目录就是 `code`，所有 npm 命令都在这里执行。

### 第 2 步：安装依赖

```powershell
npm install
```

本章新增两个运行时依赖：`sql.js`（WASM 版 SQLite）和 `proper-lockfile`（跨进程文件锁）。它们随 `npm install` 一起装好，不需要单独装 SQLite。

### 第 3 步：先跑测试，确认基线是好的

```powershell
npm run test:ch17
```

实测输出：

```text
> agent-tutorial@0.1.0 test:ch17
> vitest run chapters/ch17/tests

 RUN  v4.1.10 F:/笔记/Agent实操/code

运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL

 Test Files  54 passed (54)
      Tests  368 passed (368)
   Start at  22:33:02
   Duration  6.61s
```

输出里会混进这么两行，**这是正常的**：

```text
运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

这是入口测试在验证"没有配置凭证时应该干净地失败并返回退出码 2"，它故意触发了这两条错误输出。只要最后 `Test Files` 和 `Tests` 全绿就没问题。

54 个测试文件里，第 17 章自己的是 5 个（共 20 个测试）。想只跑本章的：

```powershell
npx vitest run chapters/ch17/tests/ch17-task-sqlite.test.ts chapters/ch17/tests/ch17-work-stealing.test.ts chapters/ch17/tests/ch17-runtime.test.ts chapters/ch17/tests/ch17-bootstrap.test.ts chapters/ch17/tests/ch17-entry.test.ts
```

实测：

```text
 Test Files  5 passed (5)
      Tests  20 passed (20)
   Duration  2.33s
```

### 第 4 步：配置模型凭证

```powershell
New-Item -ItemType File -Path .env -Force
```

然后在 `.env` 里写三行：

```text
OPENAI_BASE_URL=https://your-endpoint/v1
OPENAI_API_KEY=sk-your-key
OPENAI_MODEL=your-model-name
```

三个都是必填。缺任何一个，启动时会以退出码 2 失败，并打印缺了哪些——就是第 3 步看到的那条 `配置错误:`。

### 第 5 步：真跑一次

```powershell
npm run ch17 -- --prompt "创建三个任务：抓取数据、清洗数据、出报表（依赖前两个），然后派一个队友去做"
```

#### 你应该看到什么

Lead 先调 `create_task` 建任务，然后 `spawn_teammate` 起队友。队友起来后，如果它有已批准的计划（或从未提交过计划），空闲时就会自己调 `claimNext` 认领任务，你会在它的上下文里看到 `<auto-claimed-task>` 开头的 prompt。

具体轨迹取决于模型，但下面这几件事是机制保证的：

- 同一条任务不会被两个队友同时拿到；
- `出报表` 在前两个完成前不会被认领；
- 每次完成都要出示 token。

### 第 6 步：看磁盘上留下了什么

```powershell
Get-ChildItem -Recurse .agent_tutorial | Select-Object FullName
```

本章会多出一个 `tasks.sqlite3`。它是二进制文件，`Get-Content` 看不了。想看内容，用第 3 步已经装好的 `sql.js`，或者直接用后面实验 3 的探测脚本。

跑的时候还会短暂出现 `tasks.sqlite3.lock` 和 `.{uuid}.tmp`：前者是跨进程写锁，后者是落盘用的临时文件。正常结束后两者都不会留下。**如果看到 `.lock` 残留**，说明有进程异常退出了；`stale` 设成 30 秒，30 秒后下一个进程会判定它失效并接管。

### 第 7 步：清干净再来一次

```powershell
Remove-Item -Recurse -Force .agent_tutorial
```

⚠️ 这条命令会删掉全部本地状态：任务板、Mailbox、协议请求、后台作业、Cron 记录。只在你确认不需要现场时执行。

删完再跑第 5 步，会得到一个全新的空任务板。

### 通用入口

```powershell
npm run agent-tutorial -- run --chapter 17 --prompt "创建两个任务并让队友自己认领"
```

`--chapter` 和 `--prompt` 两个都必填。少一个会报 `Both --chapter and --prompt are required`，写错参数名会报 `Unknown argument: xxx`。

### 说清一处现状：CLI 只接了 Cron 的 wakeup

`src/cli.ts` 里只有 `cronRuntime.bindWakeup(...)`，**没有** `teammateRuntime.bindWakeup(...)`。

这意味着通过 CLI 跑的时候，队友的轮询等待不会被外部唤醒信号提前打断，只能等满 `pollIntervalSeconds`（5 秒）。这不影响正确性——空转预算和唤醒机制在库里是完整的，测试也全都覆盖到了——只是 CLI 这个入口没接上那根线。想验证唤醒行为，用实验里的方式直接驱动 `TeammateRuntime`。

### 常见报错排查

| 报错 | 原因 | 怎么解决 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...` | `.env` 缺字段 | 补齐 `OPENAI_BASE_URL` / `OPENAI_API_KEY` / `OPENAI_MODEL` |
| `Both --chapter and --prompt are required` | 通用 CLI 少传参数 | 两个都要传 |
| `Unknown argument: --promt` | 参数名拼错 | 改成 `--prompt` |
| `workStealingRuntime is required for chapter 17 or later` | 自己搭组合根时漏了 | 传入 `workStealingRuntime` |
| `taskStore requires chapter 12 or later` / `taskStore is required...` | P17 传了 JSON 版 `taskStore` | P17 只用 `workStealingRuntime`，别传 `taskStore` |
| `claimService must use the WorkStealingRuntime store` | `claimService` 和 runtime 用了不同 store | 让两者共享同一个 `SqliteTaskStore` 实例 |
| `Work-stealing runtime requires Protocol runtime` | 没先 `configureProtocol` | 按 protocol → workStealing → runnerFactory 的顺序配 |
| `Work-stealing runtime must be configured once before start` | 配了两次，或 start 之后才配 | 只在 start 前配一次 |
| `TaskStorageError: SQLite Task database escapes workspace or is not a regular file` | 数据库路径是链接/非普通文件 | 删掉那个路径上的异常文件，让它重新生成 |
| `TaskStorageError: Unsupported SQLite Task schema version` | 数据库是别的 schema 版本 | 本章不做迁移，删掉 `tasks.sqlite3` 重建 |
| `TaskLeaseExpiredError: ... claim lease expired` | 任务跑太久，租约到点了 | 正常行为；任务已回 pending，重新认领即可 |
| `Error [invalid_arguments]: Tool arguments failed schema validation` | 传了 schema 之外的字段 | `strictObject` 不接受未知字段，检查参数名 |
| `.lock` 文件残留 | 进程异常退出 | 等 30 秒自动失效，或手动删掉 |

---
## 11 个动手小实验

前面所有结论都不用信我的。这一节带你建一个探测脚本，逐条跑出来看。

**全部实验不需要 API key**，因为它们直接调库，不经过真实模型。

### 准备：先建一个探测脚本

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType File -Path scripts\probe-ch17.mts -Force
```

先写公共头部。这段是所有实验共用的工具函数，一次贴进去：

```typescript
// scripts/probe-ch17.mts —— 第 17 章探测脚本（临时用，跑完可删）
import { spawn } from "node:child_process";
import { mkdtemp, readFile, rm } from "node:fs/promises";
import { createRequire } from "node:module";
import { tmpdir } from "node:os";
import { dirname, join } from "node:path";

import initSqlJs from "sql.js";

import { JsonBackgroundJobStore } from "../chapters/ch17/src/adapters/background-json.js";
import { JsonCronStore } from "../chapters/ch17/src/adapters/cron-json.js";
import { FileMailboxStore } from "../chapters/ch17/src/adapters/mailbox-json.js";
import { JsonProtocolStore } from "../chapters/ch17/src/adapters/protocol-json.js";
import { JsonTaskStore } from "../chapters/ch17/src/adapters/task-json.js";
import { SqliteTaskStore } from "../chapters/ch17/src/adapters/task-sqlite.js";
import { EventInbox } from "../chapters/ch17/src/core/events.js";
import { AgentRunner } from "../chapters/ch17/src/core/loop.js";
import { assistantMessage, toolCall } from "../chapters/ch17/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch17/src/core/model.js";
import { ToolRegistry } from "../chapters/ch17/src/core/tools.js";
import { JobSupervisor } from "../chapters/ch17/src/features/background.js";
import { CronRuntime } from "../chapters/ch17/src/features/cron.js";
import {
  createProtocolMailboxMessage,
  ProtocolMessageKind,
} from "../chapters/ch17/src/features/mailbox.js";
import { ProtocolRequestKind, ProtocolRuntime } from "../chapters/ch17/src/features/protocol.js";
import { TaskStatus } from "../chapters/ch17/src/features/tasks.js";
import { TeammateRuntime } from "../chapters/ch17/src/features/teammates.js";
import {
  leasedTaskToolDefinitions,
  registerTeammateLeasedTaskTools,
  WorkStealingRuntime,
  type WorkStealingSleeper,
} from "../chapters/ch17/src/features/work-stealing.js";

const require = createRequire(import.meta.url);
const EXPERIMENT = process.argv[2] ?? "all";
const CLOCK_BASE = new Date("2026-07-27T14:00:00.000Z");

function section(name: string): boolean {
  const selected = EXPERIMENT === "all" || EXPERIMENT === name;
  if (selected) console.log(`\n===== ${name} =====`);
  return selected;
}

async function withTempDir<T>(operation: (root: string) => Promise<T>): Promise<T> {
  const root = await mkdtemp(join(tmpdir(), "probe-ch17-"));
  try {
    return await operation(root);
  } finally {
    await rm(root, { recursive: true, force: true });
  }
}

function seqId(start = 0x1701): () => string {
  let next = start;
  return () => {
    const hex = next.toString(16).padStart(12, "0");
    next += 1;
    return `00000000-0000-4000-8000-${hex}`;
  };
}

class Clock {
  value = new Date(CLOCK_BASE);
  now(): Date {
    return new Date(this.value);
  }
}

function errorLine(error: unknown): string {
  if (error instanceof Error) return `${error.constructor.name}: ${error.message}`;
  return `未知错误: ${String(error)}`;
}

function short(id: string): string {
  return id.slice(-4);
}

async function readSqlite(
  path: string,
): Promise<(sql: string) => readonly Record<string, unknown>[]> {
  const SQL = await initSqlJs({
    locateFile: (file: string) => join(dirname(require.resolve("sql.js")), file),
  });
  const database = new SQL.Database(new Uint8Array(await readFile(path)));
  return (sql: string) => {
    const statement = database.prepare(sql);
    const rows: Record<string, unknown>[] = [];
    while (statement.step()) rows.push(statement.getAsObject() as Record<string, unknown>);
    statement.free();
    return rows;
  };
}

class ImmediateSleeper implements WorkStealingSleeper {
  readonly calls: number[] = [];
  async sleep(seconds: number, _wakeup: AbortSignal): Promise<void> {
    this.calls.push(seconds);
  }
}

class PromptRecordingModel implements ModelClient {
  readonly prompts: string[] = [];
  async complete(request: ModelRequest): Promise<ModelReply> {
    const last = request.messages.at(-1);
    if (last?.role === "user" && last.content.startsWith("<auto-claimed-task>")) {
      this.prompts.push(last.content);
      const payload = JSON.parse(last.content.split("\n")[1] ?? "{}") as {
        readonly claim_token?: string;
        readonly task?: { readonly id?: string };
      };
      return {
        message: assistantMessage(null, [
          toolCall(
            "auto-complete",
            "complete_task",
            JSON.stringify({ task_id: payload.task?.id, claim_token: payload.claim_token }),
          ),
        ]),
        finishReason: "tool_calls",
      };
    }
    if (last?.role === "tool") {
      return { message: assistantMessage("auto task completed"), finishReason: "stop" };
    }
    return { message: assistantMessage("ready"), finishReason: "stop" };
  }
}

interface TeamHarness {
  readonly teammates: TeammateRuntime;
  readonly protocol: ProtocolRuntime;
  readonly tasks: SqliteTaskStore;
  readonly workStealing: WorkStealingRuntime;
  readonly model: PromptRecordingModel;
  close(): Promise<void>;
}

async function makeTeam(
  root: string,
  options: { readonly maxIdlePolls?: number; readonly sleeper?: WorkStealingSleeper } = {},
): Promise<TeamHarness> {
  const inbox = new EventInbox();
  const supervisor = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox });
  const cron = new CronRuntime({
    store: new JsonCronStore(root),
    inbox,
    supervisor,
    clock: { now: () => new Date(CLOCK_BASE) },
  });
  const teammates = new TeammateRuntime({
    store: new FileMailboxStore(root),
    inbox,
    supervisor,
    cronRuntime: cron,
  });
  const tasks = new SqliteTaskStore(root, {
    idGenerator: seqId(0x1721),
    claimTokenGenerator: seqId(0x1731),
  });
  const workStealing = new WorkStealingRuntime({
    store: tasks,
    sleeper: options.sleeper ?? new ImmediateSleeper(),
    maxIdlePolls: options.maxIdlePolls ?? 2,
  });
  const model = new PromptRecordingModel();
  const protocol = new ProtocolRuntime({ store: new JsonProtocolStore(root), team: teammates });
  teammates.configureProtocol(protocol);
  teammates.configureWorkStealing(workStealing);
  teammates.configureRunnerFactory((name, role, sendDefinition) => {
    const tools = new ToolRegistry();
    tools.register(sendDefinition);
    registerTeammateLeasedTaskTools(tools, workStealing.store, workStealing.claimService);
    return new AgentRunner({
      model,
      tools,
      systemPrompt: `You are ${name}, serving as ${role}.`,
      workspace: root,
      identity: name,
    });
  });
  return {
    teammates,
    protocol,
    tasks,
    workStealing,
    model,
    async close() {
      await teammates.close();
      await cron.close();
      await supervisor.close();
    },
  };
}

async function approvePlan(protocol: ProtocolRuntime, teammate: string): Promise<void> {
  const plan = await protocol.store.createRequest({
    kind: ProtocolRequestKind.PlanApproval,
    sender: teammate,
    target: "lead",
    content: "Complete the SQLite task.",
  });
  await protocol.store.consumeResponse(
    createProtocolMailboxMessage({
      id: "00000000-0000-4000-8000-000000001741",
      sender: "lead",
      recipient: teammate,
      kind: ProtocolMessageKind.PlanApprovalResponse,
      requestId: plan.id,
      content: "Approved",
      approved: true,
      createdAtUtc: new Date(CLOCK_BASE),
    }),
  );
}

function claimInSpawnedProcess(workspace: string, owner: string): Promise<string | undefined> {
  const tsxCli = join(dirname(require.resolve("tsx")), "cli.mjs");
  const program = join(
    process.cwd(),
    "chapters",
    "ch17",
    "tests",
    "fixtures",
    "ch17-claim-child.ts",
  );
  return new Promise((resolveResult, rejectResult) => {
    const child = spawn(process.execPath, [tsxCli, program, workspace, owner], {
      cwd: join(process.cwd(), "chapters", "ch17"),
      stdio: ["ignore", "pipe", "pipe"],
    });
    let stdout = "";
    let stderr = "";
    child.stdout.setEncoding("utf8");
    child.stderr.setEncoding("utf8");
    child.stdout.on("data", (chunk: string) => {
      stdout += chunk;
    });
    child.stderr.on("data", (chunk: string) => {
      stderr += chunk;
    });
    child.once("error", rejectResult);
    child.once("close", (code) => {
      if (code !== 0) {
        rejectResult(new Error(`子进程失败 (${String(code)}): ${stderr}`));
        return;
      }
      const claim = stdout.trim();
      resolveResult(claim.length === 0 ? undefined : claim);
    });
  });
}
```

头部里有六件事值得先说明：

**`section(name)`** 让一个脚本装下所有实验。`npx tsx scripts/probe-ch17.mts lease` 只跑 `lease` 那一段，不传参数就全跑。

**`withTempDir`** 每个实验在系统临时目录里开一个全新工作区，跑完删掉。所以实验之间互不影响，也不会污染你的仓库。

**`seqId(start)`** 生成可预测的 UUID：`seqId(0x1701)` 依次产出 `...1701`、`...1702`……真实代码用 `randomUUID`，但实验里需要能对上号的 ID，所以注入这个生成器。输出里的 `…1701` 就是它的尾四位。

**`Clock`** 可手动推进的时钟。`SqliteTaskStore` 的 `clock` 参数就是为这个留的——测租约过期不需要真的等 30 秒，把 `clock.value` 往后拨就行。

**`readSqlite`** 用 `sql.js` 直接读数据库文件。因为 `tasks.sqlite3` 是二进制的，要看里面的行只能这样。

**`makeTeam`** 把第 15、16、17 章的运行时拼成一个最小可跑的团队：Mailbox、协议、后台监工、Cron、SQLite 任务板、work stealing runtime。`PromptRecordingModel` 是个假模型，它认出 `<auto-claimed-task>` prompt 就调 `complete_task`，同时把收到的 prompt 记下来。

**运行方式**是 `npx tsx scripts/probe-ch17.mts <实验名>`。下面每个实验都会告诉你实验名。

---

### 实验 1（trace）：一次完整的自驱认领

追加：

```typescript
if (section("trace")) {
  await withTempDir(async (root) => {
    const clock = new Clock();
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      claimTokenGenerator: seqId(0x1711),
      clock,
      leaseDurationMs: 60_000,
    });
    const step = (index: number, text: string): void => console.log(`[${index}] ${text}`);
    const a = await store.createTask({ subject: "抓取数据" });
    const b = await store.createTask({ subject: "清洗数据" });
    const c = await store.createTask({ subject: "出报表", blockedBy: [a.id, b.id] });
    step(1, `Lead 建了三条任务：A=…${short(a.id)} B=…${short(b.id)} C=…${short(c.id)}（C 依赖 A、B）`);
    step(2, `任务板现状：${(await store.listTasks()).map((task) => `${task.subject}(${task.status})`).join(" ")}`);
    const alice = await store.claimNext("alice");
    if (alice === undefined) throw new Error("alice 应当认领成功");
    step(
      3,
      `alice 轮询到活：${alice.task.subject} token=…${short(alice.claimToken)} 租约到 ${alice.leaseExpiresAtUtc.toISOString()}`,
    );
    const bob = await store.claimNext("bob");
    if (bob === undefined) throw new Error("bob 应当认领成功");
    step(4, `bob 同时轮询，拿到的是另一条：${bob.task.subject} token=…${short(bob.claimToken)}`);
    step(5, `carol 也来轮询：claimNext → ${String(await store.claimNext("carol"))}（C 的依赖还没完成）`);
    clock.value = new Date(CLOCK_BASE.getTime() + 5_000);
    const doneA = await store.completeTask(a.id, "alice", alice.claimToken);
    step(
      6,
      `alice 出示 token 完成 A → unblocked=[${doneA.unblocked.map((task) => task.subject).join(", ")}]（C 还差 B）`,
    );
    const doneB = await store.completeTask(b.id, "bob", bob.claimToken);
    step(
      7,
      `bob 完成 B → unblocked=[${doneB.unblocked.map((task) => task.subject).join(", ")}]（C 解锁了）`,
    );
    const carol = await store.claimNext("carol");
    if (carol === undefined) throw new Error("carol 应当认领成功");
    step(8, `carol 下一轮轮询就拿到了：${carol.task.subject} token=…${short(carol.claimToken)}`);
    await store.completeTask(c.id, "carol", carol.claimToken);
    step(9, `carol 完成 C，任务板清空：${(await store.listTasks()).map((task) => `${task.subject}(${task.status})`).join(" ")}`);
    const query = await readSqlite(store.databasePath);
    step(10, `token 历史表里累计 ${query("SELECT token FROM task_claim_tokens").length} 条，全生命周期不可复用`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts trace
```

真实输出：

```text
===== trace =====
[1] Lead 建了三条任务：A=…1701 B=…1702 C=…1703（C 依赖 A、B）
[2] 任务板现状：抓取数据(pending) 清洗数据(pending) 出报表(pending)
[3] alice 轮询到活：抓取数据 token=…1711 租约到 2026-07-27T14:01:00.000Z
[4] bob 同时轮询，拿到的是另一条：清洗数据 token=…1712
[5] carol 也来轮询：claimNext → undefined（C 的依赖还没完成）
[6] alice 出示 token 完成 A → unblocked=[]（C 还差 B）
[7] bob 完成 B → unblocked=[出报表]（C 解锁了）
[8] carol 下一轮轮询就拿到了：出报表 token=…1713
[9] carol 完成 C，任务板清空：抓取数据(completed) 清洗数据(completed) 出报表(completed)
[10] token 历史表里累计 3 条，全生命周期不可复用
```

**要验证的：** alice 和 bob 拿到的是不同任务；carol 在依赖没齐时拿到 `undefined`；完成 A 时 `unblocked` 是空的，完成 B 时才解锁 C。

**顺手改个实验：** 把 `blockedBy: [a.id, b.id]` 改成 `blockedBy: [a.id]`，看 `[6]` 那行的 `unblocked` 是否立刻变成 `[出报表]`。

---

### 实验 2（backend）：SQLite 和 JSON 两个后端互不可见

追加：

```typescript
if (section("backend")) {
  await withTempDir(async (root) => {
    const sqlite = new SqliteTaskStore(root, { idGenerator: seqId(0x1701) });
    const json = new JsonTaskStore(root);
    const created = await sqlite.createTask({ subject: "只写进 SQLite" });
    console.log(`SQLite 数据库路径: ${sqlite.databasePath.replace(root, "<root>")}`);
    console.log(`SQLite listTasks(): ${(await sqlite.listTasks()).length} 条`);
    console.log(`JSON  listTasks(): ${(await json.listTasks()).length} 条`);
    console.log(`SQLite 里的任务: ${short(created.id)} / ${created.subject}`);
    await json.createTask({ subject: "只写进 JSON" });
    console.log(`--- JSON 也写一条之后 ---`);
    console.log(`SQLite listTasks(): ${(await sqlite.listTasks()).length} 条`);
    console.log(`JSON  listTasks(): ${(await json.listTasks()).length} 条`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts backend
```

真实输出：

```text
===== backend =====
SQLite 数据库路径: <root>\.agent_tutorial\tasks.sqlite3
SQLite listTasks(): 1 条
JSON  listTasks(): 0 条
SQLite 里的任务: 1701 / 只写进 SQLite
--- JSON 也写一条之后 ---
SQLite listTasks(): 1 条
JSON  listTasks(): 1 条
```

**要验证的：** 两个 store 用的是同一个工作区目录，但各写各的文件，任何一方都看不见对方的任务。这就是"不迁移、不双写"的实际含义。

---

### 实验 3（schema）：表结构与真实行

追加：

```typescript
if (section("schema")) {
  await withTempDir(async (root) => {
    const clock = new Clock();
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      claimTokenGenerator: seqId(0x1711),
      clock,
      leaseDurationMs: 30_000,
    });
    const a = await store.createTask({ subject: "任务 A" });
    await store.createTask({ subject: "任务 B", blockedBy: [a.id] });
    await store.claimTask(a.id, "alice");
    const query = await readSqlite(store.databasePath);
    console.log("表清单:");
    for (const row of query("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")) {
      console.log(`  ${String(row.name)}`);
    }
    console.log(`schema_version: ${String(query("SELECT value FROM task_metadata")[0]?.value)}`);
    console.log("tasks 行（按 sequence）:");
    for (const row of query(
      "SELECT sequence, id, status, owner, claim_token, lease_expires_at_utc FROM tasks ORDER BY sequence",
    )) {
      console.log(
        `  seq=${String(row.sequence)} id=…${short(String(row.id))} status=${String(row.status)}` +
          ` owner=${String(row.owner)} token=${row.claim_token === null ? "null" : `…${short(String(row.claim_token))}`}` +
          ` lease=${String(row.lease_expires_at_utc)}`,
      );
    }
    console.log("task_dependencies 行:");
    for (const row of query("SELECT task_id, dependency_id, position FROM task_dependencies")) {
      console.log(
        `  task=…${short(String(row.task_id))} dep=…${short(String(row.dependency_id))} position=${String(row.position)}`,
      );
    }
    console.log("task_claim_tokens 行:");
    for (const row of query("SELECT token, task_id FROM task_claim_tokens")) {
      console.log(`  token=…${short(String(row.token))} task=…${short(String(row.task_id))}`);
    }
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts schema
```

真实输出：

```text
===== schema =====
表清单:
  sqlite_sequence
  task_claim_tokens
  task_dependencies
  task_metadata
  tasks
schema_version: 1
tasks 行（按 sequence）:
  seq=1 id=…1701 status=in_progress owner=alice token=…1711 lease=2026-07-27T14:00:30.000Z
  seq=2 id=…1702 status=pending owner=null token=null lease=null
task_dependencies 行:
  task=…1702 dep=…1701 position=0
task_claim_tokens 行:
  token=…1711 task=…1701
```

**要验证的：** `sqlite_sequence` 是 SQLite 为 `AUTOINCREMENT` 自动建的；`seq` 就是创建顺序；`in_progress` 那行 owner/token/lease 三样齐全，`pending` 那行三样都是 `null`（CHECK 约束的效果）。

**顺手改个实验：** 把 `claimTask` 那行删掉再跑，看两行是不是都变成 `pending`，`task_claim_tokens` 是不是空的。

---

### 实验 4（order）：创建顺序、依赖门控与三方争抢

追加：

```typescript
if (section("order")) {
  await withTempDir(async (root) => {
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      claimTokenGenerator: seqId(0x1711),
    });
    const a = await store.createTask({ subject: "A（无依赖）" });
    const b = await store.createTask({ subject: "B（无依赖）" });
    const c = await store.createTask({ subject: "C（依赖 A、B）", blockedBy: [a.id, b.id] });
    console.log(`创建顺序: ${(await store.listTasks()).map((task) => task.subject).join(" → ")}`);
    const first = await store.claimNext("alice");
    const second = await store.claimNext("bob");
    const third = await store.claimNext("charlie");
    console.log(`alice   claimNext → ${first === undefined ? "undefined" : first.task.subject}`);
    console.log(`bob     claimNext → ${second === undefined ? "undefined" : second.task.subject}`);
    console.log(`charlie claimNext → ${third === undefined ? "undefined" : String(third)}`);
    if (first === undefined || second === undefined) throw new Error("前两次认领应当成功");
    await store.completeTask(a.id, "alice", first.claimToken);
    const afterA = await store.claimNext("charlie");
    console.log(`只完成 A 后 charlie claimNext → ${afterA === undefined ? "undefined" : "拿到了"}`);
    const completion = await store.completeTask(b.id, "bob", second.claimToken);
    console.log(`完成 B 后 unblocked: ${completion.unblocked.map((task) => task.subject).join(", ")}`);
    const afterBoth = await store.claimNext("charlie");
    console.log(
      `A、B 都完成后 charlie claimNext → ${afterBoth === undefined ? "undefined" : afterBoth.task.subject}`,
    );
    console.log(`C 现在的状态: ${(await store.getTask(c.id)).status}`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts order
```

真实输出：

```text
===== order =====
创建顺序: A（无依赖） → B（无依赖） → C（依赖 A、B）
alice   claimNext → A（无依赖）
bob     claimNext → B（无依赖）
charlie claimNext → undefined
只完成 A 后 charlie claimNext → undefined
完成 B 后 unblocked: C（依赖 A、B）
A、B 都完成后 charlie claimNext → C（依赖 A、B）
C 现在的状态: in_progress
```

**要验证的：** alice 拿 A、bob 拿 B，顺序严格按创建顺序；只完成一个依赖时 charlie 仍然拿不到 C；`unblocked` 只在最后一个依赖完成时才包含 C。

---

### 实验 5（cross-process）：两个真实进程，只有一个能认领

这个实验会 spawn 两个独立的 Node 进程，各自跑一次 `claimNext`。它用的是仓库里已有的测试 fixture `chapters/ch17/tests/fixtures/ch17-claim-child.ts`（10 行，只做一件事：认领成功就把任务 ID 打到 stdout）。

追加：

```typescript
if (section("cross-process")) {
  await withTempDir(async (root) => {
    const store = new SqliteTaskStore(root, { idGenerator: seqId(0x1701) });
    const task = await store.createTask({ subject: "只有一个进程能拿到" });
    const [left, right] = await Promise.all([
      claimInSpawnedProcess(root, "alice"),
      claimInSpawnedProcess(root, "bob"),
    ]);
    console.log(`子进程 1 stdout: ${left === undefined ? "(空)" : `…${short(left)}`}`);
    console.log(`子进程 2 stdout: ${right === undefined ? "(空)" : `…${short(right)}`}`);
    const winners = [left, right].filter((value) => value !== undefined);
    console.log(`拿到任务的进程数: ${winners.length}`);
    console.log(`拿到的就是那条任务: ${winners[0] === task.id}`);
    const current = await store.getTask(task.id);
    console.log(`任务终态: status=${current.status} owner=${String(current.owner)}`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts cross-process
```

真实输出：

```text
===== cross-process =====
子进程 1 stdout: …1701
子进程 2 stdout: (空)
拿到任务的进程数: 1
拿到的就是那条任务: true
任务终态: status=in_progress owner=alice
```

**要验证的：** 这是内存锁做不到的那件事。两个真实进程并发认领，只有一个拿到；另一个 stdout 是空的（它的 `claimNext` 返回了 `undefined`），而不是报错。跨进程互斥靠的是 `.lock` 文件 + `BEGIN IMMEDIATE`。

哪个进程赢是不确定的（这一次是 alice），但"只有一个赢"是确定的。多跑几次看看。

---

### 实验 6（lease）：半开区间的边界

追加：

```typescript
if (section("lease")) {
  await withTempDir(async (root) => {
    const clock = new Clock();
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      claimTokenGenerator: seqId(0x1711),
      clock,
      leaseDurationMs: 30_000,
    });
    const task = await store.createTask({ subject: "会过期的任务" });
    const claim = await store.claimTask(task.id, "alice");
    console.log(`认领时刻:     ${clock.now().toISOString()}`);
    console.log(`租约截止:     ${claim.leaseExpiresAtUtc.toISOString()}`);
    console.log(`claim token:  …${short(claim.claimToken)}`);

    clock.value = new Date(CLOCK_BASE.getTime() + 29_999);
    console.log(`\n--- 时间推到 +29999ms（租约内） ---`);
    console.log(`当前时刻: ${clock.now().toISOString()}`);
    console.log(`claimNext("bob") → ${(await store.claimNext("bob")) === undefined ? "undefined（拿不到）" : "拿到了"}`);

    clock.value = new Date(CLOCK_BASE.getTime() + 30_000);
    console.log(`\n--- 时间推到 +30000ms（正好等于截止时间） ---`);
    console.log(`当前时刻: ${clock.now().toISOString()}`);
    try {
      await store.completeTask(task.id, "alice", claim.claimToken);
      console.log("完成成功（不应该出现）");
    } catch (error) {
      console.log(`alice 用原 token 完成 → ${errorLine(error)}`);
    }
    const back = await store.getTask(task.id);
    console.log(`任务已回到: status=${back.status} owner=${String(back.owner)}`);
    const replacement = await store.claimNext("bob");
    if (replacement === undefined) throw new Error("过期后应可重新认领");
    console.log(`bob 重新认领 → token=…${short(replacement.claimToken)} 截止=${replacement.leaseExpiresAtUtc.toISOString()}`);
    try {
      await store.completeTask(task.id, "alice", claim.claimToken);
      console.log("旧 token 完成成功（不应该出现）");
    } catch (error) {
      console.log(`alice 再用旧 token 完成 → ${errorLine(error)}`);
    }
    const done = await store.completeTask(task.id, "bob", replacement.claimToken);
    console.log(`bob 用新 token 完成 → status=${done.task.status}`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts lease
```

真实输出：

```text
===== lease =====
认领时刻:     2026-07-27T14:00:00.000Z
租约截止:     2026-07-27T14:00:30.000Z
claim token:  …1711

--- 时间推到 +29999ms（租约内） ---
当前时刻: 2026-07-27T14:00:29.999Z
claimNext("bob") → undefined（拿不到）

--- 时间推到 +30000ms（正好等于截止时间） ---
当前时刻: 2026-07-27T14:00:30.000Z
alice 用原 token 完成 → TaskLeaseExpiredError: Task 00000000-0000-4000-8000-000000001701 claim lease expired
任务已回到: status=pending owner=null
bob 重新认领 → token=…1712 截止=2026-07-27T14:01:00.000Z
alice 再用旧 token 完成 → TaskClaimError: Task 00000000-0000-4000-8000-000000001701 owner or claim token does not match the active claim
bob 用新 token 完成 → status=completed
```

**要验证的：** `+29999ms` 时 bob 拿不到（租约还有效），`+30000ms` 时 alice 完不成（租约已过期）——半开区间的边界正好在这一毫秒。过期后任务自动回到 `pending`/`owner=null`，bob 重新认领拿到**新** token，alice 的旧 token 从此无效。

**顺手改个实验：** 把 `29_999` 改成 `30_000`，看 bob 是不是就能在那一刻抢到任务了。

---

### 实验 7（token-history）：token 全生命周期唯一

追加：

```typescript
if (section("token-history")) {
  await withTempDir(async (root) => {
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      // 故意让 token 生成器每次都返回同一个值，模拟碰撞。
      claimTokenGenerator: () => "00000000-0000-4000-8000-000000001711",
    });
    const first = await store.createTask({ subject: "第一条" });
    const second = await store.createTask({ subject: "第二条" });
    const claim = await store.claimTask(first.id, "alice");
    console.log(`第一次认领拿到 token: …${short(claim.claimToken)}`);
    try {
      await store.claimTask(second.id, "bob");
      console.log("第二次认领成功（不应该出现）");
    } catch (error) {
      console.log(`第二次认领（同一 token）→ ${errorLine(error)}`);
    }
    const untouched = await store.getTask(second.id);
    console.log(`第二条任务仍然是: status=${untouched.status} owner=${String(untouched.owner)}`);
    console.log("说明：token 冲突让整个认领事务回滚，没有留下半个状态。");
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts token-history
```

真实输出：

```text
===== token-history =====
第一次认领拿到 token: …1711
第二次认领（同一 token）→ TaskStorageError: Generated claim token already exists
第二条任务仍然是: status=pending owner=null
说明：token 冲突让整个认领事务回滚，没有留下半个状态。
```

**要验证的：** 第二条任务**完全没变**。这就是把 `INSERT INTO task_claim_tokens` 放在条件 `UPDATE` 之前的收益——冲突在任何状态被修改之前就发生了，`ROLLBACK` 不需要补偿任何东西。

**顺手改个实验：** 先 `completeTask` 完成第一条任务（这会把 `tasks.claim_token` 清成 `NULL`），再认领第二条。你会看到**依然**报 `Generated claim token already exists`——因为历史表里那条记录不会被删。这正是历史表存在的理由：`tasks.claim_token` 上的 `UNIQUE` 在清空后就管不住了。

---

### 实验 8（tools）：五个工具与角色裁剪

追加：

```typescript
if (section("tools")) {
  await withTempDir(async (root) => {
    const store = new SqliteTaskStore(root, { idGenerator: seqId(0x1701) });
    const runtime = new WorkStealingRuntime({ store });
    const tools = new ToolRegistry();
    for (const tool of runtime.leadToolDefinitions) tools.register(tool);
    console.log(`Lead   工具集: ${runtime.leadToolDefinitions.map((tool) => tool.name).join(", ")}`);
    console.log(`队友   工具集: ${runtime.teammateToolDefinitions.map((tool) => tool.name).join(", ")}`);
    console.log(`队友少了: ${
      runtime.leadToolDefinitions
        .map((tool) => tool.name)
        .filter((name) => !runtime.teammateToolDefinitions.some((tool) => tool.name === name))
        .join(", ")
    }`);
    console.log("\n每个工具的 effect 与参数:");
    const effects = new Map(
      leasedTaskToolDefinitions(store).map((tool) => [tool.name, tool.effect]),
    );
    for (const schema of tools.openAITools()) {
      const parameters = schema.function.parameters as {
        readonly required?: readonly string[];
        readonly properties?: Readonly<Record<string, unknown>>;
      };
      console.log(
        `  ${schema.function.name.padEnd(13)} effect=${String(effects.get(schema.function.name)).padEnd(5)}` +
          ` 参数=[${Object.keys(parameters.properties ?? {}).join(", ")}]` +
          ` 必填=[${(parameters.required ?? []).join(", ")}]`,
      );
    }
    console.log(`\nmaxIdlePolls 默认 = ${runtime.maxIdlePolls}`);
    // required 里出现 description / blocked_by 是因为它们在 schema 里带 .default()；
    // 运行时只传 subject 依然可以成功，下面这行验证这一点。
    const onlySubject = tools.prepare({
      id: "t1",
      name: "create_task",
      arguments: JSON.stringify({ subject: "只传 subject" }),
    });
    const created = await tools.invoke(onlySubject, { workspace: root, identity: "lead" });
    console.log(`只传 subject 调 create_task → isError=${created.isError}`);
    console.log(`  ${created.content}`);
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts tools
```

真实输出：

```text
===== tools =====
Lead   工具集: create_task, get_task, list_tasks, claim_task, complete_task
队友   工具集: get_task, list_tasks, claim_task, complete_task
队友少了: create_task

每个工具的 effect 与参数:
  create_task   effect=write 参数=[subject, description, blocked_by] 必填=[subject, description, blocked_by]
  get_task      effect=read  参数=[task_id] 必填=[task_id]
  list_tasks    effect=read  参数=[] 必填=[]
  claim_task    effect=write 参数=[task_id] 必填=[task_id]
  complete_task effect=write 参数=[task_id, claim_token] 必填=[task_id, claim_token]

maxIdlePolls 默认 = 12
只传 subject 调 create_task → isError=false
  {"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001701","owner":null,"status":"pending","subject":"只传 subject"}
```

**要验证的：** 队友的工具集比 Lead 少一个 `create_task`；`complete_task` 的必填参数是 `task_id` + `claim_token` 两个（第 12 章只有 `task_id`）；`claim_task` 的参数里**没有** owner。

最后两行说明 `create_task` 的 `description` / `blocked_by` 虽然出现在 JSON Schema 的 `required` 里（因为带 `.default()`），运行时并不真的必填。

---

### 实验 9（identity）：owner 来自 identity，不能伪造

追加：

```typescript
if (section("identity")) {
  await withTempDir(async (root) => {
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1701),
      claimTokenGenerator: seqId(0x1711),
    });
    const runtime = new WorkStealingRuntime({ store });
    const tools = new ToolRegistry();
    registerTeammateLeasedTaskTools(tools, runtime.store, runtime.claimService);
    const call = async (
      id: string,
      name: string,
      args: Readonly<Record<string, unknown>>,
      identity: string,
    ): Promise<string> => {
      const prepared = tools.prepare({ id, name, arguments: JSON.stringify(args) });
      const result = await tools.invoke(prepared, { workspace: root, identity });
      return result.content;
    };
    const task = await store.createTask({ subject: "看 owner 从哪来" });
    const claimed = await call("c1", "claim_task", { task_id: task.id }, "alice");
    console.log("claim_task 返回（identity=alice）:");
    console.log(`  ${claimed}`);
    const parsed = JSON.parse(claimed) as {
      readonly claim_token: string;
      readonly task: { readonly owner: string };
    };
    console.log(`\n工具参数里没有 owner 字段，owner 由 identity 决定 → ${parsed.task.owner}`);
    console.log(
      `\nbob 拿 alice 的 token 完成 → ${await call("c2", "complete_task", { task_id: task.id, claim_token: parsed.claim_token }, "bob")}`,
    );
    console.log(
      `alice 拿自己的 token 完成 → ${await call("c3", "complete_task", { task_id: task.id, claim_token: parsed.claim_token }, "alice")}`,
    );
    console.log(
      `\n试着在参数里塞 owner → ${await call("c4", "claim_task", { task_id: task.id, owner: "carol" }, "bob")}`,
    );
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts identity
```

真实输出（`lease_expires_at_utc` 用真实时间，每次跑都不同）：

```text
===== identity =====
claim_task 返回（identity=alice）:
  {"claim_token":"00000000-0000-4000-8000-000000001711","lease_expires_at_utc":"2026-08-30T14:49:20.362Z","task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001701","owner":"alice","status":"in_progress","subject":"看 owner 从哪来"}}

工具参数里没有 owner 字段，owner 由 identity 决定 → alice

bob 拿 alice 的 token 完成 → Error [task_claim_mismatch]: Task 00000000-0000-4000-8000-000000001701 owner or claim token does not match the active claim
alice 拿自己的 token 完成 → {"task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001701","owner":"alice","status":"completed","subject":"看 owner 从哪来"},"unblocked":[]}

试着在参数里塞 owner → Error [invalid_arguments]: Tool arguments failed schema validation
```

**要验证的：** 四件事。`claim_task` 的参数只有 `task_id`，owner 是 `alice` 因为 `identity: "alice"`；bob 即使拿到了正确的 token 也完不成（owner 不匹配）；alice 用自己的 token 正常完成；试图在参数里塞 `owner` 会被 `strictObject` 直接拒绝，而不是静默忽略。

**顺手改个实验：** 把 `c2` 那次调用的 `identity` 从 `"bob"` 改成 `"alice"`，看它是不是就成功了——这说明拦住 bob 的确实是 identity，不是 token。

---

### 实验 10（idle-loop）：完整 idle loop 自动认领并完成

这个实验起一个真实的 `TeammateRuntime`，配好协议、批准计划，然后让队友自己去认领。

追加：

```typescript
if (section("idle-loop")) {
  await withTempDir(async (root) => {
    const team = await makeTeam(root, { maxIdlePolls: 2 });
    try {
      await team.tasks.createTask({ subject: "autonomous" });
      await approvePlan(team.protocol, "alice");
      await team.teammates.start();
      await team.teammates.spawn({
        name: "alice",
        role: "worker",
        prompt: "be ready",
        sender: "lead",
      });
      const events = [
        ...(await team.teammates.waitForEvents(1)),
        ...(await team.teammates.waitForEvents(1)),
      ];
      console.log(`收到事件数: ${events.length}`);
      console.log(`\n队友收到的自动认领 prompt（共 ${team.model.prompts.length} 条）:`);
      for (const prompt of team.model.prompts) {
        for (const line of prompt.split("\n")) console.log(`  ${line.replace(root, "<root>")}`);
      }
      const tasks = await team.tasks.listTasks();
      console.log(`\n任务终态: ${tasks.map((task) => `${task.subject}=${task.status}`).join(", ")}`);
      await team.teammates.acknowledgeEvents(events);
      await team.teammates.waitForIdleTimeout("alice");
      console.log(`idleTimeoutCount("alice") = ${team.teammates.idleTimeoutCount("alice")}`);
      console.log(`drainEvents() = ${JSON.stringify(team.teammates.drainEvents())}`);
    } finally {
      await team.close();
    }
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts idle-loop
```

真实输出：

```text
===== idle-loop =====
收到事件数: 2

队友收到的自动认领 prompt（共 1 条）:
  <auto-claimed-task>
  {"claim_token":"00000000-0000-4000-8000-000000001731","lease_expires_at_utc":"2026-08-30T14:51:51.694Z","task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001721","owner":"alice","status":"in_progress","subject":"autonomous"}}
  </auto-claimed-task>

任务终态: autonomous=completed
idleTimeoutCount("alice") = 1
drainEvents() = []
```

**要验证的：** 五件事。队友没有收到任何"去做这个任务"的消息，是自己认领的；prompt 是 `<auto-claimed-task>` 三行格式，中间一行是 JSON；JSON 里带着 token 和租约截止，模型才能调 `complete_task`；任务最终是 `completed`（模型显式完成的）；空转到上限后 `idleTimeoutCount` 变成 1，而 `drainEvents()` 是空的——**没有伪造任何 shutdown 事件**。

两个事件分别是：spawn 时那次 `prompt: "be ready"` 的结果，和自动认领任务完成后回报 Lead 的结果。

---

### 实验 11（plan-gate）：计划未批准就不进入认领

追加：

```typescript
if (section("plan-gate")) {
  await withTempDir(async (root) => {
    const sleeper = new ImmediateSleeper();
    const team = await makeTeam(root, { maxIdlePolls: 2, sleeper });
    try {
      const task = await team.tasks.createTask({ subject: "计划没批就不许认领" });
      // 只创建计划请求、不消费响应：最新计划停留在 Pending。
      await team.protocol.store.createRequest({
        kind: ProtocolRequestKind.PlanApproval,
        sender: "alice",
        target: "lead",
        content: "等 Lead 批准",
      });
      await team.teammates.start();
      await team.teammates.spawn({
        name: "alice",
        role: "worker",
        prompt: "be ready",
        sender: "lead",
      });
      const events = await team.teammates.waitForEvents(1);
      await team.teammates.acknowledgeEvents(events);
      await team.teammates.waitForIdleTimeout("alice");
      const current = await team.tasks.getTask(task.id);
      console.log(`任务状态（计划未批准）: ${current.status} owner=${String(current.owner)}`);
      console.log(`sleeper.calls = ${JSON.stringify(sleeper.calls)}  ← 每个元素是一次轮询等待的秒数`);
      console.log(`idleTimeoutCount("alice") = ${team.teammates.idleTimeoutCount("alice")}`);
      console.log(`自动认领 prompt 条数 = ${team.model.prompts.length}`);
      console.log(`任务从未离开 pending: ${current.status === TaskStatus.PENDING}`);
    } finally {
      await team.close();
    }
  });
}
```

跑：

```powershell
npx tsx scripts/probe-ch17.mts plan-gate
```

真实输出：

```text
===== plan-gate =====
任务状态（计划未批准）: pending owner=null
sleeper.calls = [5,5]  ← 每个元素是一次轮询等待的秒数
idleTimeoutCount("alice") = 1
自动认领 prompt 条数 = 0
任务从未离开 pending: true
```

**要验证的：** 任务板上明明有一条 ready task，队友却一次都没认领。`自动认领 prompt 条数 = 0` 说明 `claimNext` 根本没被调用——门禁挡在认领**之前**，不是认领后再回滚。`sleeper.calls = [5,5]` 说明它规规矩矩空转了两轮（`maxIdlePolls: 2`），每轮 5 秒，就是 `pollIntervalSeconds` 的默认值。

**顺手改个实验：** 把创建计划请求那段换成 `await approvePlan(team.protocol, "alice");`，你会看到任务立刻被认领并完成——这就是实验 10。

---

### 实验完成后清理

```powershell
Remove-Item scripts\probe-ch17.mts
git status --short
```

`git status --short` 应该没有输出（或者只剩你自己本来就有的改动）。探测脚本是临时的，不要留在仓库里。

---
## 这章刻意没有做什么

写到这里你大概能猜到接下来"顺手"能加什么。列清楚，你读代码时就不会到处找这些东西。

**没有 JSON 到 SQLite 的自动迁移、读取回退或双写。** 这是实验 2 验证过的：两个 store 在同一个工作区里各写各的文件，互相看不见。想在 P16 和 P17 之间切换，就换 profile 重新建任务，不是"平滑升级"。

**没有 lease 续租工具，也没有 heartbeat。** 五个工具里没有 `renew_lease`。租约到期就是到期，任务回到 `pending` 让别人接手。如果你的任务真的需要跑 10 分钟，正确做法是把 `leaseDurationMs` 配大，或者把任务拆小——不是加一个"我还活着"的心跳接口。

**没有承诺外部副作用的 exactly-once。** SQLite 事务保证的是"任务状态只迁移一次"。claim token 作为 idempotencyKey 传给 `AgentRunner.run()`，能让**同一轮**模型重试不重复执行。但如果队友在 lease 过期前已经发出了一封邮件，任务回到 `pending` 被别人重新认领后，那封邮件不会被撤回。副作用去重是业务层的事。

**没有超时后伪造 graceful shutdown。** 实验 10 和 11 都验证过：空转到上限后 `idleTimeoutCount` 加一，`drainEvents()` 是空的。worker 安静退出，不会替 Lead 编一条 `shutdown_response`。

**没有 worktree 隔离。** 队友之间已经不会认领同一个任务了，但他们仍然在同一个工作目录里改文件。**任务所有权不等于文件系统隔离。** 这是第 18 章的内容。

**没有动态 MCP 工具发现。** 工具面在组合根里写死，Lead 五个、队友四个。

**没有 daemon thread、进程级全局 BUS、或者没人持有的 async task。** polling 的 sleep 由 `WorkStealingRuntime` 持有，`close()` 时能停干净。

**符号链接防御有代码，但本章没有测试覆盖。** 这一条要说清楚，因为它是本章唯一一处"实现比测试严"的地方。`src/adapters/task-sqlite.ts` 里有四层路径逃逸防御：`realpath` 归一化、拒绝符号链接（`:410`、`:420`、`:551`）、`nlink !== 1` 拒绝硬链接（`:565`）、`pathIsInside` 边界检查。但 `tests/ch17-*.test.ts` 里**只有硬链接测试**（`ch17-task-sqlite.test.ts:223`，"rejects a database hardlink before changing the outside file"），没有任何符号链接测试。原因是普通 Windows 用户创建文件符号链接需要额外权限，写了会在多数机器上 skip。所以：符号链接防御的代码是真的，但你不能说"它被验证过"。

这些不做项不是"以后补上"的 TODO，而是本章边界的一部分。第 17 章要守住的最小闭环只有四个词：**共享任务板、原子认领、租约回收、有界轮询。**

---

## 与 Claude Code 的差异

分清"我们简化了"和"我们比教学版更严"这两类差异，对理解设计取舍很有帮助。

### 我们简化了的地方

**空闲机制只有一种。** 我们是 5 秒轮询 + `AbortSignal` 唤醒，默认 12 次后进入可观察的 idle timeout。CC 不是单一的 `idle_poll()`，而是四样东西的组合：`sendIdleNotification()` 主动告诉 Lead"我闲着"、`waitForNextPromptOrShutdown()` 的 500ms mailbox 轮询、`useTaskListWatcher` 的文件系统监听、`tryClaimNextTask()` 的主动认领。我们的队友进入 idle 是静默的，Lead 不知道。

**没有任务级细粒度锁。** CC 真实源码用 `proper-lockfile` 同时管任务级锁和 task-list 锁，在锁内完成读-检查-改-写。我们只有一把数据库级锁（`tasks.sqlite3.lock`），粒度更粗——同一时刻整个任务板只能有一个写者。这在几十个队友的规模下够用，几百个就会成为瓶颈。

**队友不能创建任务。** CC 真实源码的队友还能用任务创建和更新类工具。我们刻意不给队友 `create_task`，避免它在自治循环里无限扩张任务图（"我需要先建一个子任务"→ 认领 → "这个也需要拆" → …）。

**没有 `<identity>` 重注入的特殊处理。** CC 教学版在 `len(messages) <= 3` 时手动重注入身份。我们每次 `run()` 由 Runner 注入稳定的 system prompt，自动认领时额外把 claim payload 作为本轮输入。

### 我们比教学版更严的地方

这几条是反过来的——CC 的**教学版 s17** 没做，我们做了：

**三层互斥，而不是零层。** CC 教学版的 scan + claim 用 JSON 文件，**没有文件锁**。它的正确性依赖于"实际上不会真的并发"。我们用进程内 tail-chain + `proper-lockfile` + `BEGIN IMMEDIATE`，实验 5 用两个真实进程验证过。

**claim 语义是结构化的。** CC 教学版 `claim_task` 返回 `"Claimed {id} ({subject})"` 一句文本。我们返回 `TaskClaim`，带 `task`、`claimToken`、`leaseExpiresAtUtc` 三样。文本形式的问题不是不好看——是模型没有 token 就没法证明"这个任务是我的"。

**有 lease。** CC 的 claim 没有租约，任务卡住需要人工介入。我们的半开区间 `[claimed_at, lease_expires_at)` 到期自动回到 `pending` 并清空 owner/token（实验 6）。

**token 全生命周期唯一。** `task_claim_tokens` 历史表让 token 在数据库整个生命周期内不可复用——任务完成后、租约过期后，旧 token 都失效（实验 7）。CC 的 owner 字符串没有 token 历史，只能靠文件锁和状态检查避免误完成。

**plan gate 和 claim 绑成同一个确定性边界。** 我们在 `claimNext()` **之前**检查 `planAllowsEffectful()`，pending/rejected 让自动认领保持零写入（实验 11）。CC 教学版靠命令语义和工具检查，没有把这两件事绑在一起。

一句话总结这张对照：**CC 真实源码比我们完整，CC 教学版比我们松。** 我们挑的是"能在离线测试里被断言住的最小严格集合"。

---

## 从 ai-agent-book 学到什么：共享任务板与原子认领

ai-agent-book 第 10 章和本章正对：它区分了共享上下文/不共享上下文，以及 Orchestration（编排）/ Choreography（编舞）/ Decentralized（去中心化）三种协作形态。我们这一章做的正是从第一种走向第三种。配套的[实验 10-4](https://github.com/bojieli/ai-agent-book/blob/main/chapter10/parallel-web-research/README.md) 展示了一个真实的并行搜索系统，实现见 [agents.py](https://github.com/bojieli/ai-agent-book/blob/main/chapter10/parallel-web-research/agents.py) 与 [message_bus.py](https://github.com/bojieli/ai-agent-book/blob/main/chapter10/parallel-web-research/message_bus.py)。

### 它是怎么设计的

Manager 分配页面，多个同构 worker 各自持有独立的浏览器 context。第一个 `target_found` 在 `asyncio.Lock` 下经过幂等的 `settle_once` 结算，**只广播一次 `terminate`**；后续迟到的命中只记录、不重复结算。输家收到 terminate 后在安全点取消、发 ack、关掉自己的 browser context。

这里面有三个原则直接决定了本章的形状：

第一，**共享文件系统是并发冲突的高发区**，需要乐观锁或者 worktree 隔离。它明确指出了这一点，我们本章解决了任务层的冲突，文件层留给第 18 章。

第二，**`settle_once` 必须是幂等的，并且由锁或事务保护。** 不是"检查一下有没有人结算过"，而是让结算这个动作本身在临界区内。这正是 `BEGIN IMMEDIATE` + 条件 UPDATE 的形状。

第三，**首个"已验证成功"才结算，而不是首个"声称成功"。** 模型可以提出完成，但不能自己批准完成。

### 我们的对照与取舍

本章没有照搬中心化的 MessageBus，而是把它的原则翻译到任务板上：

| ai-agent-book 的设计 | 本章的做法 | 为什么这么选 |
| --- | --- | --- |
| 共享文件系统需要乐观锁或 worktree 隔离 | `tasks.sqlite3` 是单一任务状态源，三层互斥保证只有一个 worker 能 claim | 先解决任务层冲突；同一 workspace 的文件隔离留给第 18 章 |
| 数据平面与控制平面分开，消息总线承载控制事件 | Mailbox 与 typed Protocol 仍是控制平面，SQLite 只存任务状态 | 任务板不扮演 broadcast bus；idle loop 里 Mailbox 优先于任务板 |
| `settle_once` 幂等且由锁或事务保护 | `BEGIN IMMEDIATE` + 条件 UPDATE + `task_claim_tokens` 历史表 | 让"选择"和"迁移"在同一个事务里，而不是先选再改 |
| 首个"已验证成功"才结算 | 队友必须显式调 `complete_task` 并带当前 `claim_token` | 但要诚实说：我们没有独立验证器，验证仍来自工具调用本身 |
| terminate → 安全点清理 → ack → 退出 | 第 16 章的 typed shutdown 优先于 ready task；idle timeout 不伪造 shutdown | 关机是控制平面的事，不能被任务认领抢在前面 |
| 循环失控需要预算与取消机制 | `maxIdlePolls` 是有界轮询预算，lease 防止 worker 卡死任务 | 本章不做全局限额或 heartbeat |
| 并行协调适合用消息总线做广播 | 刻意不实现 `BROADCAST` | 一对多通知和 A2A 状态同步留给后续章节 |

**它做了而我们明确不做的：** 中心 MessageBus、`BROADCAST` 广播、`status_update` 主动上报、动态 worker 池、真实浏览器 context 计数作为证据。

有一句话值得单独摘出来：work stealing 把调度从 Lead 的判断移到了共享任务板上，但它**仍然不是一个没有控制平面的自组织网络**。任务怎么认领可以是去中心化的，但消息、计划审批和关机仍然需要确定性的 typed 状态机。这就是 idle loop 里那四级优先级的全部理由。

---

## 配套代码索引

本章代码全部在 `code/chapters/ch17/` 下。行数是给你估工作量用的——看到 768 行不用慌，`task-sqlite.ts` 里一大半是路径安全校验和错误分类。

**新增或本章重点改动的文件：**

| 文件 | 行数 | 本章负责什么 |
| --- | --- | --- |
| [src/features/work-stealing.ts](code/chapters/ch17/src/features/work-stealing.ts) | 417 | `WorkStealingRuntime`、`DirectTaskClaimService`、五个 leased 工具定义、`taskPayload` / `claimPayload` wire format、`WorkStealingSleeper` |
| [src/adapters/task-sqlite.ts](code/chapters/ch17/src/adapters/task-sqlite.ts) | 768 | `SqliteTaskStore`：schema DDL、`claimNext` 的原子认领、`#transaction` 的三层互斥、lease 回收、路径逃逸防御、临时文件 + rename 落盘 |
| [src/features/teammates.ts](code/chapters/ch17/src/features/teammates.ts) | 884 | `#runWorker` 的四级优先级、自动认领 prompt、claim token 复用为 idempotencyKey、有界 polling 与 `idleTimeoutCount` |
| [src/features/tasks.ts](code/chapters/ch17/src/features/tasks.ts) | 336 | `TaskStore` 接口、`TaskStatus`、`TaskClaim`、`TaskClaimError` / `TaskLeaseExpiredError` 等错误类型 |
| [src/features/protocol.ts](code/chapters/ch17/src/features/protocol.ts) | 463 | `planAllowsEffectful`（第 16 章来的，本章被认领门禁复用） |
| [src/bootstrap.ts](code/chapters/ch17/src/bootstrap.ts) | 445 | 组合根：能力位分派、store 同一性校验、Lead / 队友工具面裁剪 |
| [src/config.ts](code/chapters/ch17/src/config.ts) | 69 | `taskStore` / `workStealingRuntime` 配置项 |
| [src/cli.ts](code/chapters/ch17/src/cli.ts) | 333 | 两个入口的参数解析和资源关闭 |
| [src/chapters/ch17.ts](code/chapters/ch17/src/chapters/ch17.ts) | 5 | 固定章节入口，只有 5 行 |

**测试（本章 5 个文件，共 20 个测试）：**

| 文件 | 行数 | 断言什么 |
| --- | --- | --- |
| [tests/ch17-task-sqlite.test.ts](code/chapters/ch17/tests/ch17-task-sqlite.test.ts) | 240 | 后端独立、按 sequence 排序、依赖门控、跨进程恰好一个成功、lease 边界、token 碰撞回滚、硬链接拒绝 |
| [tests/ch17-work-stealing.test.ts](code/chapters/ch17/tests/ch17-work-stealing.test.ts) | 103 | 工具面契约、owner 来自 identity、wire format 字段名、`complete_task` 的 required schema |
| [tests/ch17-runtime.test.ts](code/chapters/ch17/tests/ch17-runtime.test.ts) | 806 | 四级优先级、协议不进模型、plan gate 零写入、自动认领后显式完成、idle timeout 可观察且不伪造 shutdown |
| [tests/ch17-bootstrap.test.ts](code/chapters/ch17/tests/ch17-bootstrap.test.ts) | 101 | P16/P17 能力增量、拒绝 JSON taskStore、store 同一性、三侧工具面精确 |
| [tests/ch17-entry.test.ts](code/chapters/ch17/tests/ch17-entry.test.ts) | 28 | 两个入口共享同一 runtime，配置失败前不创建状态目录 |
| [tests/fixtures/ch17-claim-child.ts](code/chapters/ch17/tests/fixtures/ch17-claim-child.ts) | 10 | 跨进程认领用的子进程程序（实验 5 也用它） |

### 读代码的建议顺序

不要从 768 行的 `task-sqlite.ts` 开头读。按这个顺序，每一步都能接着上一步的疑问：

1. **`src/core/profiles.ts` 的 `P17`** —— 先确认差量真的只有两个能力位：`task_dag_sqlite` 和 `work_stealing`。一分钟。
2. **`src/features/tasks.ts` 的 `TaskClaim` 和错误类型** —— 三个字段、四个错误类。这是整章的词汇表。
3. **`src/adapters/task-sqlite.ts` 的 `claimNext`（第 198 行起）** —— 只有二十几行，但把"释放过期 → 找 ready → 原子认领"这条链完整演示了一遍。
4. **`src/adapters/task-sqlite.ts` 的 `#claim`（第 278 行起）** —— 现在看条件 UPDATE 和 `getRowsModified() === 1`，你已经知道它在防什么了。注意 `INSERT INTO task_claim_tokens` 在 UPDATE **之前**。
5. **`src/adapters/task-sqlite.ts` 的 `#transaction`（第 310 行起）** —— 三层互斥的外壳。重点看三件事：`retries: 0`、COMMIT 之后才落盘、临时文件 + rename。
6. **`src/features/work-stealing.ts` 的 `DirectTaskClaimService` 和 `claimPayload`** —— owner 从哪来、wire format 在哪转换。
7. **`src/features/teammates.ts` 的 `#runWorker`（第 573 行起）** —— 四级优先级的实际代码。看清楚 Mailbox 和协议为什么在认领之前。
8. **`src/features/teammates.ts` 的第 632-645 行** —— 有界 polling 的计数逻辑。重点是唤醒**不**消耗空转预算。
9. **`src/bootstrap.ts` 的 store 同一性校验和工具面裁剪** —— 最后读这段。它解释的是"为什么两处必须是同一个对象"，属于收尾细节。

跳过 `task-sqlite.ts` 的路径校验部分（第 375-570 行）和 `cli.ts` 的大部分——前者是安全边界，逻辑独立；后者是参数解析。

---

## 运行与验证

五道门，按顺序过。全部在 `F:\笔记\Agent实操\code` 下执行：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch17
npm run typecheck
npm run lint
npm run format:check
npm run build
```

我这边的实测结果：

```text
Test Files  54 passed (54)
     Tests  368 passed (368)
```

`test:ch17` 会连带跑前面所有章节的测试，所以是 54 个文件、368 个测试，不是只有 ch17 那 5 个文件。这是有意的——第 17 章的一个要求就是不能破坏 P16 及以前的行为，特别是"旧的 JSON `TaskStore` 行为完全不变"这一条。

只想看本章那 20 个测试：

```powershell
npx vitest run --root chapters/ch17 tests/ch17-task-sqlite.test.ts tests/ch17-work-stealing.test.ts tests/ch17-runtime.test.ts tests/ch17-bootstrap.test.ts tests/ch17-entry.test.ts
```

```text
Test Files  5 passed (5)
     Tests  20 passed (20)
```

`typecheck`、`lint`（1367 个文件）、`format:check`（1367 个文件）、`build` 我这边都是干净通过。

本章测试全部注入了确定 UUID、UTC clock、可控 sleeper 和模型替身，**不依赖真实 OpenAI、不依赖网络、没有真实 sleep**。唯一的例外是实验 5 那个跨进程测试——它必须 spawn 真实进程，因为跨进程互斥没法在单进程里模拟。

关键断言都是具体的业务结果，不是"没抛异常"这种填充式断言：

- SQLite 与旧 JSON backend 相互独立，顺序按 `sequence` 而不是 UUID 大小；
- A、B 被两个队友并发认领，C 只在二者**都**完成后才可认领；
- Windows `spawn` 两个进程竞争同一任务时**恰好一个**成功，另一个拿到 `undefined` 而不是报错；
- 租约在 `lease_expires_at_utc` 那一刻已过期，旧 owner/token 不能完成新租约；
- active token 和历史 token 的碰撞都会回滚整个事务，不产生半认领状态；
- 数据库硬链接不能把状态写到 workspace 外，且**在修改外部文件之前**就被拒绝；
- shutdown 已送达时任务保持 `pending`，协议请求不进入模型；
- `pending`／`rejected` 的最新计划让自动认领**零写入**，approved 响应先更新门禁再放行；
- idle timeout 可观察（`idleTimeoutCount`），但不生成虚假协议消息或 summary；
- Lead、Subagent、Teammate 的工具集合和 `complete_task` 的 required schema 精确匹配；
- CLI 只构造**一个** SQLite store，配置失败前不创建状态目录。

---

## 本章小结

### 三句话版本

第一，work stealing 新增的不是一个 `while True`，而是**让"选择任务"和"改变状态"变成同一个原子动作**。分成两步，无论中间加多少检查，都有 TOCTOU 窗口。

第二，认领必须留下**可出示的凭据**。owner 字符串只能说明"曾经是谁的"，claim token 才能证明"现在还是我的"。

第三，自治不等于放任。队友可以自己找活，但 Mailbox、协议、计划门禁都排在认领之前，空转有预算，租约有期限。

### 一定要记住的七条

1. **选择和迁移必须同事务。** `BEGIN IMMEDIATE` 起写事务，条件 `UPDATE ... WHERE status='pending' AND owner IS NULL` 是最终互斥点，只认 `getRowsModified() === 1`（《原子认领》、实验 4、5）。
2. **三层互斥各管一段。** 进程内 tail-chain 管同进程并发，`proper-lockfile` 管跨进程，`BEGIN IMMEDIATE` 管数据库内部。去掉任何一层都有对应的失败场景（《三层互斥》、实验 5）。
3. **token 全生命周期唯一。** 历史表 `task_claim_tokens` 让 token 在任务完成后、租约过期后都不可复用。冲突发生在 UPDATE 之前，回滚不需要补偿（实验 7）。
4. **lease 是半开区间。** `[claimed_at, lease_expires_at)`。释放用 `<=`，完成检查用 `>=`，边界正好差一毫秒（实验 6）。
5. **owner 来自 identity，不来自参数。** `claim_task` 的 schema 里根本没有 owner 字段，`strictObject` 让伪造尝试直接失败（实验 9）。
6. **Mailbox 和协议优先于任务板。** 计划门禁排在 `claimNext` 之前，pending/rejected 时零写入——不是认领后再回滚（《idle loop》、实验 11）。
7. **有界轮询，不伪造 shutdown。** 空转到 `maxIdlePolls` 就退出，`idleTimeoutCount` 可观察，`drainEvents()` 是空的。唤醒不消耗空转预算（实验 10、11）。

### 一张图回顾整条链路

```text
  ① 建图                     Lead 的工具                  队友的工具
  ────────                   create_task                  （没有 create_task）
                             get_task / list_tasks        get_task / list_tasks
                             claim_task / complete_task   claim_task / complete_task
                                     │
                                     ▼
                        ┌────────────────────────┐
                        │   WorkStealingRuntime  │
                        │   （Lead / Subagent /   │
                        │    Teammate 共享一个）  │
                        └────────────┬───────────┘
                                     │
  ② 落盘                             ▼
  ────────      ┌──────────────────────────────────────────┐
                │  .agent_tutorial/tasks.sqlite3            │
                │                                          │
                │  tasks（sequence / status / owner /       │
                │         claim_token / lease_expires）     │
                │  task_dependencies（DAG 边 + position）   │
                │  task_claim_tokens（全生命周期唯一）      │
                │  task_metadata（schema_version = 1）      │
                │                                          │
                │  CHECK 约束：owner / token / lease        │
                │  三样必须同时有或同时无                   │
                └──────────────────────────────────────────┘
                                     │
  ③ 互斥                             ▼
  ────────      ┌──────────────────────────────────────────┐
                │  第 1 层  进程内 tail-chain（按 db 路径）  │
                │  第 2 层  proper-lockfile（.lock 文件）    │
                │  第 3 层  BEGIN IMMEDIATE + 条件 UPDATE    │
                │                                          │
                │  最终互斥点只有一个：                     │
                │  UPDATE ... WHERE status='pending'        │
                │              AND owner IS NULL           │
                │  → getRowsModified() === 1 才算认领成功   │
                └──────────────────────────────────────────┘
                                     │
  ④ 优先级                           ▼
  ────────      ┌──────────────────────────────────────────┐
                │  队友 idle loop 的四级顺序                │
                │                                          │
                │  1. Mailbox 有消息    ──▶ 先处理消息      │
                │  2. typed 协议请求    ──▶ 不进模型，回响应 │
                │  3. planAllowsEffectful() 为假 ──▶ 只睡觉 │
                │  4. claimNext() ──▶ 有活就干，没活就睡    │
                └──────────────────────────────────────────┘
                                     │
  ⑤ 完成                             ▼
  ────────      ┌──────────────────────────────────────────┐
                │  complete_task(task_id, claim_token)     │
                │                                          │
                │  两道校验：owner+token 匹配、租约未过期   │
                │  返回 unblocked（这次解锁了哪些下游）     │
                │  claim token 同时是 Runner 的幂等键       │
                └──────────────────────────────────────────┘
```

五个编号就是本章五条硬规则的位置：① 只有 Lead 建图 ② 状态落在独立 SQLite ③ 三层互斥保证单一认领者 ④ 控制平面优先于任务板 ⑤ 完成需要出示凭据。

### 检查你是否真的读懂了

不看前文，试着回答。答不上来的，括号里是对应小节：

1. "先扫描找 ready task，再写 owner"这个写法的 TOCTOU 窗口具体在哪一行之间？（《为什么不能先扫描再写 owner》）
2. 为什么内存锁不够？它具体在什么场景下失效？（《三层互斥》、实验 5）
3. `BEGIN` 和 `BEGIN IMMEDIATE` 的区别是什么？用前者会在什么时候出错？（《为什么是 BEGIN IMMEDIATE》）
4. 条件 `UPDATE` 里为什么要同时写 `status='pending'` 和 `owner IS NULL` 两个条件？只写一个行不行？（《claimNext 的完整逻辑》）
5. `getRowsModified() === 1` 返回 0 时，代码是抛错还是返回 `undefined`？为什么这么选？（《claimNext 的完整逻辑》、实验 5）
6. `task_claim_tokens` 历史表存在的理由是什么？`tasks.claim_token` 上的 `UNIQUE` 为什么不够？（《为什么 token 要全生命周期唯一》、实验 7）
7. 为什么 `INSERT INTO task_claim_tokens` 要放在条件 `UPDATE` **之前**？（实验 7）
8. 租约释放用 `lease_expires_at_utc <= now`，完成检查用 `now >= lease`——这两个方向为什么是一致的？（《lease 是半开区间》、实验 6）
9. `claim_task` 工具的参数里为什么没有 owner？如果模型硬塞一个会怎样？（《owner 不能被工具参数伪造》、实验 9）
10. 队友的工具集比 Lead 少哪一个？为什么少这一个？（《五个工具》、实验 8）
11. idle loop 的四级优先级是什么？为什么计划门禁排在认领**之前**而不是之后？（《idle loop 的真实优先级》、实验 11）
12. 队友自动认领到任务后，任务会自己变成 `completed` 吗？如果模型不调 `complete_task` 会发生什么？（《自动认领后，模型仍须显式完成》、实验 10）
13. 空转到 `maxIdlePolls` 上限后，`drainEvents()` 里有什么？（《有界 polling》、实验 10）
14. 本章承诺 exactly-once 吗？"任务状态只迁移一次"和"外部副作用只发生一次"差在哪？（《这章刻意没有做什么》）

做到这些之后，Lead 可以只建立 DAG 和启动队友，依赖解锁、稳定选择和并发认领由队友自主完成。人不再是调度器。

不过还有一个问题没解决：Alice 和 Bob 已经不会认领同一个任务了，但他们仍然在同一个工作目录里改文件。**任务所有权不等于文件系统隔离。** 下一章我们用 Git Worktree 给每个队友划出各自的地盘。
