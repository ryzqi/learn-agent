凌晨两点，手机响了。

一个跑了三个小时的 Agent 任务挂了。日志最后一行：

```text
openai.APIStatusError: Error code: 529 - {'type': 'error', 'error': {'type': 'overloaded_error', 'message': 'Overloaded'}}
```

没有重试，没有等待，没有任何恢复动作。直接崩。

三个小时的工具调用、文件读写、中间结论全部作废，而供应商侧那次过载可能只持续了两秒。

这不是罕见 bug，而是四类日常情况的必然结果：

- 429 是标准限流信号，配额一紧就出现；
- 一些 OpenAI 兼容端点用 529 表示临时过载；
- 输入超过 context window 时，端点返回结构化的「prompt 太长」错误；
- 输出撞上 `max_tokens` 时，你拿到的是一个**成功**的 HTTP 200，正文却是半句话。

问题从来不是 API 会不会出错，而是 Agent 能否在明确边界内恢复。

第 10 章的 Agent 已经有记忆、上下文压缩、规划工具和动态 Prompt，但模型请求仍然裸着穿过唯一 Loop：`model.complete(request)` 一抛错，整个 `run()` 就结束。第 11 章只增加一层能力——让**同一个逻辑模型请求**自己处理输出截断、输入过长和瞬态 API 故障，同时保证不污染正式历史、不重复执行 Hook、不隐藏失败。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaGsTNZSXRlG4fL5wHvcaIrLwardHsUpxcyrxic5ZEAWlaiaByrxZCvvezTicJhNcCXCx0yrsQoxYtAvEiaLIxXz7FTMqDLAm0rsZeI/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 分清三种「模型没给出可用答案」的故障，说出各自的供应商无关信号和恢复动作，而不是笼统地「重试一下」。
2. 解释为什么判断故障类型不能用 `"429" in str(exc)`，以及正确做法只读哪几个字段。
3. 说清 `finishReason === "length"` 为什么不是异常，以及第一次遇到它时为什么要把整段回复丢掉。
4. 手算默认配置下 10 次瞬态重试的每一次等待时长，并说出总等待为什么正好解释了 300 秒的默认时限。
5. 判断一个 `Retry-After` 头会被接受还是被拒绝，并说出被拒绝时抛的是哪个错误。
6. 说明「连续 3 次 529 才切 fallback」里的**连续**是什么意思，哪些情况会把计数清零。
7. 说出一次续写恢复之后，canonical history 里应该剩几条消息、外层 turn 应该是几。
8. 解释本章为什么只包装主 Loop 请求，而记忆抽取、压缩摘要、子 Agent 仍然走裸模型。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 每轮如何组装模型请求 | 第 1 章 | 恢复层就插在「组装完请求」和「拿到回复」之间 |
| `ModelClient` / `ModelRequest` / `ModelReply` 契约 | 第 1 章 | 恢复层只认这三个类型，不认供应商对象 |
| canonical history 与请求历史的区别 | 第 8 章 | **本章最关键的前置**，恢复只改请求快照，不改正式历史 |
| `CompactionManager` 与协议组不可拆分 | 第 8 章 | 输入过长时直接复用它做响应式压缩 |
| 动态系统提示每轮重新渲染 | 第 10 章 | 首条 system 消息在压缩时必须留在原位 |
| Profile 能力位与组合根 | 第 5～10 章 | P11 靠一个 `recovery` 能力位切换到恢复路径 |

如果只来得及补一章，补第 8 章的「canonical history 和请求历史不是一回事」。本章一半的设计都是围绕这条边界展开的。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 逻辑模型请求 | 外层 Loop 眼里的「一次模型调用」，内部可能真实发出多次 HTTP 请求 |
| 恢复层 | `RecoveryManager`，把一次逻辑请求内的重试、续写、压缩、切换全部收在里面 |
| 归一化错误 | 在适配器边界把供应商异常转成本项目自己的类型，之后只按类型分派 |
| 输出截断 | 模型撞到 `max_tokens` 停下，HTTP 200 但 `finish_reason` 是 `length` |
| 输出预算升级 | 第一次截断时把 `maxTokens` 从 8000 提到 64000，重发同一请求 |
| 续写 | 预算提到上限仍截断时，把已有片段和续写提示追加进**请求快照**再问一次 |
| 请求快照 | `complete()` 内部那个局部 `requestMessages`，冻结数组，每次重试重建 |
| 响应式压缩 | 因为「输入过长」被动触发的压缩，区别于第 8 章按阈值主动触发的压缩 |
| 瞬态故障 | 429 限流和 529 过载，重试有意义的那一类失败 |
| 指数退避 | 等待时长按 `base × 2^attempt` 增长，封顶 32 秒 |
| 抖动（jitter） | 在退避时长上叠加一小段随机量，避免同批请求同时醒来 |
| `Retry-After` | 服务端明确告诉你「等多久再来」的响应头，优先级高于本地退避 |
| fallback model | 主模型连续过载时切换到的备用模型 |
| 总时限（deadline） | 一个 turn 从 `beginTurn()` 起算的墙钟上限，默认 300 秒 |
| typed failure | 每种耗尽/超时/取消都有专属错误类，调用方按类型判断而不是读文案 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看三条路径真实跑起来什么样   ← 直接看输出，不先讲抽象
  ↓
② 三种故障，三条路              ← 为什么必须分开处理
  ↓
③ 先归一化错误                  ← 只读结构化字段；200 也要校验
  ↓
④ 路径一：输出截断              ← 升预算 → 续写 → 只有合并结果出门
  ↓
⑤ 路径二：输入过长              ← 只压缩请求快照，只压一次
  ↓
⑥ 路径三：429 / 529             ← 退避曲线、Retry-After、fallback 计数
  ↓
⑦ 取消与总时限                  ← 次数不是唯一边界
  ↓
⑧ 接入唯一 Loop                 ← 内部重试为什么不算新 turn
  ↓
⑨ 运行 + 七个实验 + 排错        ← 动手把上面每条结论都验一遍
```

---

## 先看三条路径真实跑起来什么样

抽象讲「重试」不如直接看输出。下面三段是用**脚本化的假模型**跑出来的真实结果：假模型按预定顺序返回回复或抛错，时钟和 sleep 都被注入成可观测的假实现，所以每次请求的参数、每次等待的秒数都能打印出来。完整脚本在后面「实验一」。

**第一段：输出被截断。**

```text
=== 路径一：输出被截断 ===
第 1 次请求  model=primary-model maxTokens=8000 消息数=2 末条=user: "写一份长报告"
第 2 次请求  model=primary-model maxTokens=64000 消息数=2 末条=user: "写一份长报告"
第 3 次请求  model=primary-model maxTokens=64000 消息数=4 末条=user: "Continue exactly where you left off. Do not r
最终 content = "前半段。后半段。"
最终 finishReason = stop
调用方原请求消息数 = 2（未被改写）
state.recoveryCount = 1
state.hasEscalated = true
```

三行请求日志说明了三件事：

- 第 2 次请求的 `maxTokens` 从 8000 变成 64000，但**消息数还是 2**——第一次那段残缺回复被整段丢掉了，没有进请求。
- 第 3 次请求消息数变成 4，多出来的两条是「上一次的半截回复 + 续写提示」，它们只存在于请求快照里。
- 最后一行 `调用方原请求消息数 = 2`：调用方手里那个 `ModelRequest` 从头到尾没被改写。

**第二段：输入过长。**

```text
=== 路径二：输入过长 ===
第 1 次请求  model=primary-model maxTokens=8000 消息数=2 末条=user: "写一份长报告"
第 2 次请求  model=primary-model maxTokens=8000 消息数=3 末条=user: "写一份长报告"
第 2 次请求首条 role = system
第 2 次请求第二条是否含 compacted_history = true
最终 content = "压缩后成功"
state.hasAttemptedReactiveCompact = false
```

注意 `maxTokens` 没变：输入过长和输出截断是两件事，不能互相顶替。首条仍然是 system，第二条换成了压缩摘要。

**第三段：连续过载。**

```text
=== 路径三：连续 529 切 fallback ===
每次请求用的模型 = ["primary-model","primary-model","primary-model","fallback-model"]
实际 sleep 秒数（jitter 注入为 0）= [0.5,1,2]
```

三次 529、三次退避，第 4 次请求才换模型。这行 `["primary-model","primary-model","primary-model","fallback-model"]` 就是「连续 3 次 529 后切 fallback」的字面含义。

三条路径的外部表现完全不同，但对外层 Loop 来说，它们都只是「`complete()` 返回了一个可用的 `ModelReply`」。

---

## 三种故障，三条路

三种故障看起来都像「模型没给出可用答案」，但触发信号和恢复动作完全不同：

| 故障      | 供应商无关信号                                        | 恢复动作                                        |
| ------- | ---------------------------------------------- | ------------------------------------------- |
| 输出被截断   | `ModelReply.finishReason === "length"`         | 首次提升输出预算；之后在请求窗口内续写                         |
| 输入上下文过长 | `ModelPromptTooLongError`                      | 保留当前 system prompt，响应式压缩请求快照一次              |
| 临时故障    | `ModelRateLimitError` / `ModelOverloadedError` | 遵守 Retry-After 或指数退避；连续 3 次 529 后切 fallback |

**为什么不能用一条通用重试路径解决全部三种。** 挨个看反例：

- 输出截断时重试原请求毫无意义——预算没变，第二次还是会截在同一个位置。它需要的是**改参数**（提预算）或**改请求内容**（追加续写提示），不是重发。
- 输入过长时重试更糟——请求一个字节都没少，重试一百次都是同一个 400。它需要的是**缩小输入**。
- 429/529 恰恰相反：请求本身完全正确，唯一该做的就是**什么都别改，等一会儿再发**。改内容反而可能引入新问题。

一条通用路径必然在这三者中至少两个上做错事。所以这三条路径统一放在 `RecoveryManager.complete()` 里，但**内部是三个独立分支**。

公共 `AgentRunner` 的职责完全不变：组装当前请求、执行工具、追加最终回复、运行 Hook。恢复层不会重新进入外层 Loop，所以一次 API 重试不会被计成新的 Agent turn。

还有第四种情况需要单独说：**不认识的错误**。500、503、连接重置、DNS 失败、TLS 握手失败、程序自己的 `TypeError`——这些统统原样抛出，一次都不重试。

```text
=== 未知错误：原样抛出，不重试 ===
抛出 TypeError: undefined is not a function
是否同一个对象 = true
实际请求次数 = 1
sleep 次数 = 0
```

`是否同一个对象 = true` 是有意断言的：错误对象被原样传递，没有被包装、没有丢栈。这条设计后面「这章没有做什么」会再解释为什么保守是对的。

---

## 先归一化错误，禁止匹配异常文本

很多示例代码这样判断故障：

```python
# 反面教材，不要这么写
if "429" in str(exc):
    retry()
elif "prompt" in str(exc):
    compact()
```

这会在三种情况下判错：

1. 业务错误文案里恰好带 `429`（比如用户输入了一个订单号 `INV-429`）；
2. 反向代理返回的 HTML 错误页里带 `prompt` 字样；
3. 程序自己的异常消息里带这些词——本该立刻暴露的 bug 变成了静默重试。

当前实现把供应商对象严格限制在一个文件里：[openai-chat.ts](code/chapters/ch11/src/adapters/openai-chat.ts)。整个项目只有它 `import` OpenAI SDK。适配器只读取 SDK 的稳定结构：

- `APIError.status`
- `APIError.error`
- `APIError.headers`
- `APIError.requestID`

分类规则是显式的：

```typescript
const PROMPT_TOO_LONG_CODES = new Set([
  "context_length_exceeded",
  "max_context_window",
  "prompt_is_too_long",
  "prompt_too_long",
]);

if (status === 429) {
  const retryAfter = headers.get("retry-after");
  throw new ModelRateLimitError("OpenAI request was rate limited", {
    ...(retryAfter === null ? {} : { retryAfter }),
  });
}

if (status === 529) {
  throw new ModelOverloadedError("OpenAI model was overloaded");
}

if (status === 400 && errorCode !== undefined && PROMPT_TOO_LONG_CODES.has(errorCode)) {
  throw new ModelPromptTooLongError("OpenAI prompt exceeded the model context window");
}
```

`errorCode` 只允许来自平坦的 `error.code` / `error.type`，或最多一层嵌套的 `error.error.code` / `error.error.type`。实现不读 `message`，不递归搜索任意 JSON，不根据异常字符串猜测。

### 分类结果实测

下面 14 行是把各种错误对象喂给适配器后的真实分类结果（脚本见「实验五」）：

```text
=== 错误分类：只认结构化字段 ===
APIError 429                                 -> 归一为 ModelRateLimitError
APIError 429 + retry-after 头                 -> 归一为 ModelRateLimitError
APIError 529                                 -> 归一为 ModelOverloadedError
APIError 400 code=context_length_exceeded    -> 归一为 ModelPromptTooLongError
APIError 400 code=prompt_too_long            -> 归一为 ModelPromptTooLongError
APIError 400 嵌套 error.code=prompt_too_long   -> 归一为 ModelPromptTooLongError
APIError 400 code=PROMPT_TOO_LONG（大写）        -> 归一为 ModelPromptTooLongError
APIError 400 code=unknown_bad_request        -> 原样抛出 Error
APIError 400 message 写了 prompt is too long   -> 原样抛出 Error
APIError 500                                 -> 原样抛出 Error
APIError 503                                 -> 原样抛出 Error
普通 Error，字符串里带 429                           -> 原样抛出 Error
伪造对象：有 status=429 但不是 APIError               -> 原样抛出 Error
连接层错误 ECONNRESET                             -> 原样抛出 Error
```

四行值得单独指出：

- `code=PROMPT_TOO_LONG（大写）` **被识别**——识别前会统一转小写，因为不同端点大小写不一致是常见现象。
- `message 写了 prompt is too long` **不被识别**——文案不是契约，改一版措辞就失效。
- `普通 Error，字符串里带 429` **不被识别**——这正是文本匹配会踩的坑。
- `伪造对象：有 status=429 但不是 APIError` **不被识别**——判断用的是 `instanceof APIError`，而不是「有没有 `status` 字段」。鸭子类型在这里是漏洞，不是灵活性。

### 类型和 status 是不变量

三种内部异常定义在 [model.ts](code/chapters/ch11/src/core/model.ts)，构造期就锁死了 status：

| 错误类 | 允许的 status | 携带的额外信息 |
| --- | --- | --- |
| `ModelRateLimitError` | 只能是 429 | 可选 `retryAfter` 原始字符串 |
| `ModelOverloadedError` | 只能是 529 | 无 |
| `ModelPromptTooLongError` | 只能是 400 | 无 |

这样恢复层可以按 TypeScript 类型分派，不会收到「类型是限流但 status 是 500」这种语义矛盾的对象。构造一个矛盾对象会在构造期就失败，而不是在恢复逻辑里表现成诡异行为。

### HTTP 200 同样是不可信边界

上面全在讲「请求失败」。但供应商返回 200 不代表结构可信——字段缺失、类型不对、出现未见过的枚举值，都可能发生。`normalizeResponse()` 在把响应交给恢复层之前逐字段验证：

- 必须恰好一个 `choice`；
- `message.role` 只能是 `assistant`；
- `finish_reason` 只能落在白名单内；
- `content` 必须是 `string | null`；`refusal` 是字符串时可以作为最终内容回退；
- `tool_calls` 必须逐项带完整的 `id`、`name`、`arguments`；
- `usage` 三个计数必须是非负整数。

实测的 13 种响应（脚本见「实验五」）：

```text
=== 成功响应：200 也要逐字段校验 ===
标准 stop 回复                     -> 通过：content="done" finishReason=stop
finish_reason=content_filter   -> 通过：content="部分内容" finishReason=content_filter
content=null 但有 refusal        -> 通过：content="I cannot help." finishReason=stop
choices 为空数组                   -> OpenAIResponseError: Chat completion must return exactly one choice
choices 有两个                    -> OpenAIResponseError: Chat completion must return exactly one choice
finish_reason 是未知新值            -> OpenAIResponseError: Unsupported finish_reason: max_output_reached
role 不是 assistant              -> OpenAIResponseError: Chat completion message role must be assistant
tool_calls 缺 id                -> OpenAIResponseError: Invalid function tool call: tool call id must be a string
旧式 function_call               -> OpenAIResponseError: Legacy function_call responses are unsupported
usage.prompt_tokens 为负数        -> OpenAIResponseError: Chat completion usage field prompt_tokens must be non-negative integer
usage 缺 total_tokens           -> OpenAIResponseError: Chat completion usage field total_tokens must be non-negative integer
usage 三项齐全                     -> 通过：content="done" finishReason=stop
响应根不是对象                        -> OpenAIResponseError: Chat completion response must be an object
```

三条容易被忽略的边界：

**`finish_reason` 未知值直接失败，不当成 `stop`。** 供应商加了一个新枚举值（比如 `max_output_reached`），宽容处理会让恢复层把一个截断回复当成完整回复接受，答案就少了一截而且没人知道。明确失败比静默错误好。

**`content_filter` 能通过适配器。** 它是 OpenAI 合法的 `finish_reason`，适配器的职责是忠实传递供应商语义。最终要不要接受它由 Loop 决定——后面「接入唯一 Loop」会讲它在哪一层被拒。

**`refusal` 可以当内容。** 模型明确拒答时 `content` 是 `null`，但 `refusal` 里有文本。把它当最终内容，比让 Loop 抛「模型没给文本也没调工具」更贴近真实语义。

请求侧同样有 wire format 映射，方向相反：`toOpenAIMessage()` 把内部 `ChatMessage` 转成 OpenAI 消息参数（`system`/`user` 直接映射，`tool` 必须带 `tool_call_id`，`assistant` 按需附加 `tool_calls`）；`toOpenAITool()` 把内部工具 schema 固定映射为 `type: "function"` 加 `name`/`description`/`parameters`。

一句话总结这一层的价值：**core 不 import OpenAI SDK，所有供应商细节收在 adapter 内。** 恢复层面对的永远是 model.ts 里那套稳定契约，换供应商时改 adapter 就够了。

---

## 路径一：输出截断只存在于请求窗口

`finishReason === "length"` 是一个**正常响应**，HTTP 200，不是异常。它表示模型撞到了 `max_tokens` 上限，话没说完就被截断了。

第一次遇到它时，恢复层做两件事：把输出预算从 8000 提到 64000，然后把那段残缺回复**完全丢弃**，重发同一个请求：

```typescript
if (reply.finishReason === "length" && !state.hasEscalated) {
  state.currentMaxTokens = config.escalatedMaxTokens;
  state.hasEscalated = true;
  continue;
}
```

为什么第一次要整段丢掉，而不是直接从半截处续写？因为提预算之后模型可能一次就写完，一份连贯的完整回复比「半截 + 拼接」质量更好。丢掉的那点 token 换来的是更好的输出结构，值得。

`RecoveryConfig` 在构造时就验证 `escalatedMaxTokens <= modelMaxTokens`：

```text
escalatedMaxTokens > modelMaxTokens      -> RangeError: escalatedMaxTokens must not exceed modelMaxTokens
initialMaxTokens >= escalatedMaxTokens   -> RangeError: escalatedMaxTokens must exceed initialMaxTokens
```

预算不能靠「先发请求再看供应商是否接受」来试错——那样每次配置错误都要浪费一次真实请求和一次真实等待。

### 提预算之后还截断，才进入续写

这时才追加片段和续写提示，而且只追加到 `requestMessages` 这个**局部快照**上：

```typescript
const fragment = reply.message.content;
if (reply.message.toolCalls.length > 0 || fragment === null || fragment.length === 0) {
  throw new RecoveryRetriesExhausted(
    "length recovery requires a non-empty text-only assistant fragment",
  );
}
if (state.recoveryCount >= this.#config.maxContinuations) {
  throw new RecoveryRetriesExhausted("continuation recovery attempts exhausted");
}

fragments.push(fragment);
requestMessages = Object.freeze([
  ...requestMessages,
  reply.message,
  userMessage(CONTINUATION_PROMPT),
]);
```

续写提示是固定的一句：

```typescript
export const CONTINUATION_PROMPT =
  "Continue exactly where you left off. Do not repeat any text, no apology, no recap. Pick up mid-thought.";
```

三个要求各有原因：`Do not repeat any text` 防止重复正文，`no apology` 防止模型写「抱歉刚才被截断了」这类对最终读者无意义的话，`Pick up mid-thought` 明确允许从句子中间接上，不必凑成一个新段落。

### 请求次数实测

默认 `maxContinuations = 3`。如果模型一直截断，实际会发多少次请求？

```text
=== 默认 maxContinuations=3，一直截断 ===
抛出 RecoveryRetriesExhausted: continuation recovery attempts exhausted
实际请求次数 = 5
每次请求消息数 = [2,2,4,6,8]
state.recoveryCount = 3
```

5 次 = 1 次初始 + 1 次提预算 + 3 次续写。消息数序列 `[2,2,4,6,8]` 很直观：提预算那次消息数不变（残缺回复被丢了），之后每次续写多两条（半截回复 + 续写提示）。

把 `maxContinuations` 设成 0 就等于关掉续写：

```text
=== maxContinuations=0：直接关掉续写 ===
抛出 RecoveryRetriesExhausted: continuation recovery attempts exhausted
实际请求次数 = 2
```

只剩「初始 + 提预算」两次，第二次仍截断就直接失败。这是合法配置，构造期不会报错。

### 截断的回复带 tool_calls 时立刻失败

```text
=== 截断片段带 tool_calls：立刻失败，不管还剩几次续写 ===
抛出 RecoveryRetriesExhausted: length recovery requires a non-empty text-only assistant fragment
实际请求次数 = 2
state.recoveryCount = 0（一次续写都没发生）
```

注意 `recoveryCount = 0`：这不是「续写次数用完了」，而是**根本不该续写**。一个被截断的 `tool_calls` 参数可能只有半个 JSON，把它当片段拼接会产出无法解析的工具调用；把它追加进历史更糟，会留下一个永远等不到 `tool` 结果的孤儿调用。所以这里选择立刻失败。

空字符串片段同理：

```text
=== 空字符串片段：同样拒绝 ===
抛出 RecoveryRetriesExhausted: length recovery requires a non-empty text-only assistant fragment
实际请求次数 = 2
```

如果允许空片段，一个反复返回空串加 `length` 的端点会让恢复层白跑三轮续写，最后拼出一个空答案。

### 最终成功时才合并

合并只发生在成功返回的那一刻，规则是「按原样拼接，不自动补空格」：

```typescript
function mergeFragments(reply: ModelReply, fragments: readonly string[]): ModelReply {
  if (fragments.length === 0) {
    return reply;
  }
  return Object.freeze({
    ...reply,
    message: {
      ...reply.message,
      content: `${fragments.join("")}${reply.message.content ?? ""}`,
    },
  });
}
```

不补空格是刻意的：中文、代码、URL 中间插一个空格都是错的，而模型被要求「接着上一句写」，本来就不该有额外空白。

最终回复携带的工具调用会完整保留：

```text
=== 最终回复带 tool_calls：片段照常合并，工具调用完整保留 ===
最终 content = "我先说一半，再说另一半，然后调工具。"
最终 finishReason = tool_calls
最终 toolCalls = [{"id":"call-final","name":"read_file","arguments":"{\"path\":\"README.md\"}"}]
```

只有这一个合并后的消息能返回公共 Loop 并进入 canonical history。这条边界同时解决两个问题：

- 首次残缺回复、续写提示、中间片段都不会污染会话历史；
- 带未完成 `tool_calls` 的截断响应不会被追加，从源头避免孤儿工具调用。

---

## 路径二：输入过长只压缩请求快照一次

输入过长来自适配器抛出的 `ModelPromptTooLongError`。它和 `finishReason === "length"` 是两件完全不同的事，不能互相代替：

| | 输入过长 | 输出截断 |
| --- | --- | --- |
| HTTP 状态 | 400 | 200 |
| 表现形式 | 抛异常 | 正常返回，`finish_reason` 是 `length` |
| 根因 | 请求太大，模型根本没开始生成 | 请求没问题，但答案写不完 |
| 正确动作 | 缩小输入 | 提高输出预算或续写 |
| 拿错了会怎样 | 一直提预算，请求还是超窗口，白跑 | 一直压缩历史，答案还是被截断，而且丢了上下文 |

恢复层直接复用第 8 章的 `CompactionManager.compactOnPromptTooLong()`：

```typescript
const [leadingSystem, compactableHistory] = splitLeadingSystem(requestMessages);
const outcome = await runBounded((signal) =>
  compaction.compactOnPromptTooLong(
    compactableHistory,
    { retryCount: promptTooLongRetries },
    signal,
  ),
);
requestMessages = Object.freeze([...leadingSystem, ...outcome.history]);
```

`splitLeadingSystem` 只做一件小事：如果首条是 system 就把它单独摘出来，压缩只作用于其后的消息，压完再把 system 放回第一位。

为什么必须这样？因为第 10 章的系统提示是**每轮动态渲染**的，里面有当前工具列表、workspace 路径、选中的记忆。如果它被压进摘要，模型下一次就不知道自己有哪些工具、能在哪个目录操作。实测确认了它留在原位：

```text
第 2 次请求首条 role = system
第 2 次请求第二条是否含 compacted_history = true
```

其后的请求快照按**完整消息组**压缩：assistant 的全部 `tool_calls` 和对应的 `tool` 结果仍然是不可拆分单元。这条规则来自第 8 章，本章直接继承。

### 没有修改任何原数组

canonical history、调用方传进来的 `ModelRequest`、以及那份只读 messages 快照，全都没有被修改。`requestMessages` 是 `complete()` 内部的局部变量，每次重新 `Object.freeze` 出一个新数组。

响应式 transcript 记录的是「被压缩前的**请求快照**」，不是 canonical history 的别名。完整会话追溯仍由第 8 章的主动压缩 transcript 负责。这两份 transcript 用途不同，不要混。

### 同一个逻辑请求只压缩一次

```text
=== 路径二：第二次输入过长直接失败 ===
抛出 PromptTooLongRetryError: prompt-too-long compaction was already attempted
实际请求次数 = 2
```

为什么不允许压第二次？因为已经压过一次还是超窗口，说明问题不在「历史太长」，而在别的地方——可能是单条 user 消息本身就超窗口、可能是某个工具返回了巨型结果、也可能是模型的真实窗口比配置里小。这些情况反复压缩只会把有用上下文一点点磨掉，最后拿一个既没上下文又还是失败的请求去撞墙。

需要澄清一个容易误读的状态位。`state.hasAttemptedReactiveCompact` 在成功返回时会被重置成 `false`：

```text
最终 content = "压缩后成功"
state.hasAttemptedReactiveCompact = false
```

它不是「本回合压过一次」的持久记录。真正拦住第二次压缩的是 `complete()` 内部的局部计数 `promptTooLongRetries`——`hasAttemptedReactiveCompact` 只是给外部观测用的瞬时标记。想在测试里断言「压缩发生过」，要在失败路径上看，或者观测 transcript 文件。

压缩及其模型摘要也处在同一个取消和总时限边界内，不能借摘要调用绕开 deadline。摘要本身也可能失败，这时会连它落盘的 transcript 一起回滚：

```text
=== 场景三：摘要失败回滚 transcript ===
抛出 Error: 摘要模型自己挂了
是否原样抛出摘要失败 = true
artifacts 目录内容 = []
模型调用次数 = 1（没有再重试）
```

`artifacts 目录内容 = []` 是关键断言：摘要失败没有留下半份 transcript。

---

## 路径三：429、529、Retry-After 与 fallback

429 和 529 共用同一条瞬态重试路径，区别只有两点：529 会累计连续计数，429 会读 `Retry-After`。

### 退避曲线

没有有效 `Retry-After` 时，默认退避基线是 `0.5 × 2^attempt`，封顶 32 秒。把抖动注入成恒定 0，能看到干净的曲线：

```text
=== 完整退避曲线（jitter 注入为 0，10 次尝试耗尽）===
sleep 序列 = [0.5,1,2,4,8,16,32,32,32]
sleep 总秒数 = 127.5
实际请求次数 = 10
抛出 RecoveryRetriesExhausted: rate limit recovery attempts exhausted
```

**注意：10 次请求，只有 9 次 sleep。** 这不是 off-by-one，而是正确行为——第 10 次失败时次数已经用尽，直接抛错，没有理由再睡一觉。

抖动取满（`base × 25%`）时：

```text
=== 抖动取满（jitter 返回上界 = base × 25%）===
sleep 序列 = [0.625,1.25,2.5,5,10,20,40,40,40]
sleep 总秒数 = 159.375
```

每一项都是无抖动版本的 1.25 倍，总等待 159.375 秒。注意封顶后的三项变成了 40 秒——`maxDelaySeconds = 32` 封的是**基线**，抖动叠在封顶之后。这是有意的：抖动的目的就是打散同时醒来的请求，如果它也被硬封在 32 秒，一批同时被限流的客户端仍会在同一秒重试。

整理成表：

| 第几次失败 | 基线（jitter=0） | 抖动取满 | 累计（抖动取满） |
| --- | --- | --- | --- |
| 1 | 0.5 | 0.625 | 0.625 |
| 2 | 1 | 1.25 | 1.875 |
| 3 | 2 | 2.5 | 4.375 |
| 4 | 4 | 5 | 9.375 |
| 5 | 8 | 10 | 19.375 |
| 6 | 16 | 20 | 39.375 |
| 7 | 32（封顶） | 40 | 79.375 |
| 8 | 32 | 40 | 119.375 |
| 9 | 32 | 40 | 159.375 |
| 10 | 不 sleep，直接耗尽 | — | — |

最后一列解释了默认 `totalTimeoutSeconds = 300` 是怎么来的：最坏情况下光等待就要 159 秒，加上 10 次真实请求本身的耗时，300 秒是一个「能跑完全部重试，又不会把用户挂到天亮」的量级。这个值不是拍出来的。

### Retry-After 优先，但必须能被严格解析

429 带 `Retry-After` 时，服务端的指示优先于本地退避：

```text
=== 路径三：429 遵守 Retry-After ===
实际 sleep 秒数 = [3.25]（不是退避基线 0.5）
每次请求用的模型 = ["primary-model","primary-model"]
```

解析规则很严格。14 种输入的实测结果（注入的当前时间是 `2026-08-28T12:00:00.000Z`）：

```text
=== Retry-After 解析规则 ===
"0"                                  -> sleep 0 秒
"3"                                  -> sleep 3 秒
"3.25"                               -> sleep 3.25 秒
" 7 "                                -> sleep 7 秒
"-1"                                 -> InvalidRetryAfterError
""                                   -> InvalidRetryAfterError
"NaN"                                -> InvalidRetryAfterError
"Infinity"                           -> InvalidRetryAfterError
"1e2"                                -> InvalidRetryAfterError
"Fri, 28 Aug 2026 12:00:05 GMT"      -> sleep 5 秒
"Fri, 28 Aug 2026 11:59:50 GMT"      -> sleep 0 秒
"2026-08-28T12:00:30.000Z"           -> InvalidRetryAfterError
"2026-08-28T12:00:30"                -> InvalidRetryAfterError
"not-a-date"                         -> InvalidRetryAfterError
```

整理成规则表：

| 输入形式 | 结果 | 原因 |
| --- | --- | --- |
| 非负十进制秒数（含小数） | 接受 | HTTP 规范的 delta-seconds 形式 |
| 两端有空格 | 接受 | 解析前先 `trim()` |
| `0` | 接受，等 0 秒 | 服务端说立刻可以重试，那就立刻 |
| 负数 | 拒绝 | 没有「往回等」这种语义 |
| 空串 / `NaN` / `Infinity` | 拒绝 | 不是有效秒数 |
| `1e2` | 拒绝 | 科学计数法不在 delta-seconds 语法里，容忍它等于容忍任意 `Number()` 可解析的垃圾 |
| 带 `GMT`/`UTC`/`±HHMM` 的 HTTP-date | 接受，算差值 | 有明确时区，能唯一确定时刻 |
| 已经过去的 HTTP-date | 接受，等 0 秒 | 差值取 `max(0, ...)` |
| ISO-8601 带 `Z`，如 `2026-08-28T12:00:30.000Z` | **拒绝** | 见下方说明 |
| 无时区的日期串 | 拒绝 | 会被按本地时区解释，同一个头在不同机器上算出不同结果 |
| 任意非日期文本 | 拒绝 | 无法解析 |

ISO-8601 带 `Z` 被拒绝这一条最容易踩。它在语义上明明是 UTC，但当前实现用的时区检查是这个正则：

```typescript
/(?:\b(?:GMT|UTC)\b|[+-]\d{4})$/i
```

只认结尾的 `GMT`、`UTC` 或 `±HHMM`，不认单个 `Z`。HTTP 规范里 `Retry-After` 的日期形式就是 HTTP-date（IMF-fixdate），不包含 ISO-8601，所以严格来说这是**符合规范的严格**，不是 bug。但如果你的端点确实返回 ISO 时间，就会看到 `InvalidRetryAfterError`——这时该改的是正则，而不是把整个解析放宽。

`Retry-After` 非法时的行为很明确：**不 sleep，不重试，直接抛 `InvalidRetryAfterError`**。因为端点返回了一个不符合契约的头，说明它的限流语义不可信；这时按自己的退避猜一个时长，很可能撞在同一个限流窗口里。

### 连续 529 才切 fallback

```typescript
state.consecutive529 += 1;
if (state.consecutive529 >= config.overloadFallbackThreshold) {
  state.currentModel = config.fallbackModel;
  state.consecutive529 = 0;
}
```

阈值默认 3。第三次 529 时更新状态、完成本次退避，**下一次请求**（也就是第 4 个）才显式携带 fallback model。回看开头那段输出：

```text
每次请求用的模型 = ["primary-model","primary-model","primary-model","fallback-model"]
实际 sleep 秒数（jitter 注入为 0）= [0.5,1,2]
```

「连续」这个词是字面意思。429、输入过长、成功响应都会把计数清零：

```text
=== 429 打断连续 529 计数 ===
每次请求用的模型 = ["primary-model","primary-model","primary-model","primary-model","primary-model"]
sleep 序列 = [0.5,1,2,4]
说明：529 -> 529 -> 429 -> 529，连续计数被 429 清零，所以从未切 fallback
```

三次 529 加起来，但中间夹了一次 429，所以从头到尾都是主模型。为什么要这么严？因为「连续过载」是判断主模型真的不可用的证据，而「偶尔过载」只是正常波动。一次 429 夹在中间，说明请求确实到达了主模型并被正常处理（只是限流），不构成切换理由。

注意 `sleep 序列 = [0.5,1,2,4]`：退避的 `attempt` 用的是**总瞬态失败次数**，不区分 429 还是 529。所以第 4 次失败等 4 秒，而不是因为「529 计数被清零」就重新从 0.5 秒开始。这一点符合直觉：退避对付的是「整体上正在失败」，不该被错误类型切换重置。

---

## 取消与总时限是恢复的一部分

重试次数不是唯一边界。上一节算过：10 次退避在最大抖动下，仅等待就是 159 秒；如果每次真实请求还各花十几秒，一个 turn 就能拖到五分钟以上。所以默认 turn 总时限设为 300 秒，并允许测试或调用方显式缩短：

```typescript
const config = new RecoveryConfig({
  primaryModel: "primary-model",
  fallbackModel: "fallback-model",
  initialMaxTokens: 8_000,
  escalatedMaxTokens: 64_000,
  modelMaxTokens: 64_000,
  maxContinuations: 3,
  maxTransientAttempts: 10,
  totalTimeoutSeconds: 300,
});
```

### 五个可注入边界

`RecoveryManager` 把所有「与真实世界打交道」的东西都做成可注入依赖，这是本章离线测试能精确到小数点的原因：

| 注入点 | 默认实现 | 测试里换成什么 |
| --- | --- | --- |
| `monotonic` | `performance.now() / 1000` | 手动推进的假时钟 |
| `utcNow` | `() => new Date()` | 固定时刻，让 `Retry-After` 日期差值可预测 |
| `sleeper` | 真实 `setTimeout` | 记录秒数并推进假时钟，不真等 |
| `jitter` | `Math.random() * upperBound` | 恒定 0 或恒定上界，拿到确定曲线 |
| `cancellation` | 新建一个 `CancellationToken` | 外部持有，测试里手动 `cancel()` |

单调时钟和 UTC 时钟分开是有原因的：算 deadline 必须用单调时钟（系统时间被 NTP 回调也不会算错），算 `Retry-After` 日期差值必须用真实 UTC 时钟（要和服务端说的时刻对齐）。用一个时钟兼顾两件事，其中一件必然出错。

### 该失败的时候立刻失败

如果等待会达到或越过当前 turn 的剩余时限，恢复层直接抛 `RecoveryDeadlineExceeded`——不会先睡到超时再报告失败：

```text
=== 场景一：sleep 会跨过 deadline ===
Retry-After = 30 秒，剩余 deadline = 10 秒
抛出 RecoveryDeadlineExceeded: retry delay would reach or cross the turn deadline
是否真的 sleep 过 = false
模型调用次数 = 1
```

`是否真的 sleep 过 = false` 是这个设计的全部意义。反面做法是「先睡 30 秒，醒来发现超时了，再报错」——用户白等 30 秒，拿到的还是同一个失败。

### 取消要等操作真正收束

```typescript
async #runBounded<T>(operation: (signal: AbortSignal) => Promise<T>): Promise<T> {
  this.#raiseIfCancelled();
  const remaining = this.#remainingSeconds();
  const controller = new AbortController();
  // ...
  const result = await Promise.race([operationPromise, deadlinePromise, cancellationPromise]);
```

`#runBounded()` 在模型调用、退避 sleep、响应式压缩三处统一包裹，每次都做同样的事：先检查取消与 deadline，再让「操作 / 超时 / 取消」三者竞争，最后把同一个 `AbortSignal` 传给底层。

取消或超时时，它先 `abort()` 再 `await` 操作收束：

```typescript
} catch (error) {
  if (error instanceof RecoveryCancelledError || error instanceof RecoveryDeadlineExceeded) {
    controller.abort();
    await operationPromise.catch(() => undefined);
  }
  throw error;
}
```

那句 `await operationPromise.catch(...)` 看起来多余，其实是关键。没有它，`complete()` 会在被中止的请求还在飞的时候就返回，进程里留着一个仍可能写文件、仍可能回调的后台操作。实测：

```text
=== 场景二：取消进行中的模型调用 ===
模型请求已发出，此时触发 cancel()
抛出 RecoveryCancelledError: recovery turn was cancelled
模型侧 signal.aborted = true
模型侧操作已收束 settled = true
```

最后两行分别验证了「信号真的传下去了」和「被中止的操作真的结束了」。

`CancellationToken` 的契约也是明确的：`cancel()` 幂等，只触发一次，触发后清空监听器；`subscribe()` 拒绝非函数参数；如果令牌已经取消，新订阅会**立即执行**并返回一个空操作退订函数。最后一条防的是竞态——订阅晚了一步就永远等不到通知。

### 五种 typed failure

所有耗尽路径都有专属错误类，调用方按类型判断，不读文案：

| 错误类 | 什么时候抛 | 该怎么处理 |
| --- | --- | --- |
| `RecoveryRetriesExhausted` | 续写次数用完、瞬态尝试用完、片段不合法 | 真的失败了，向用户报告 |
| `RecoveryDeadlineExceeded` | 剩余时间不够，或操作跑过了 deadline | 缩小任务或调大 `totalTimeoutSeconds` |
| `RecoveryCancelledError` | 外部 `cancel()` 被调用 | 正常路径，不必当错误上报 |
| `InvalidRetryAfterError` | 服务端返回的 `Retry-After` 不合规范 | 查端点实现，或按需放宽解析 |
| `PromptTooLongRetryError` | 压缩过一次仍然输入过长 | 检查是否有单条巨型消息或工具结果 |

它们都继承 `RecoveryError`，所以想一把接住全部恢复失败可以 `catch (e) { if (e instanceof RecoveryError) ... }`，想精确区分就用具体类。

---

## 接入唯一 Loop，而不是复制 Loop

最容易走错的一步是「为了加恢复能力，复制一份 Loop 出来改」。那样会立刻分裂成两套 Hook 顺序、两套历史维护规则，之后每加一个能力都要改两处。

本章的做法是给公共运行时新增**一个窄协议**，只有两个方法：

```typescript
export interface ModelRequestExecutor {
  beginTurn(): void;
  complete(request: ModelRequest): Promise<ModelReply>;
}
```

`AgentRunner.run()` 在现有 memory turn lifecycle 之后调用一次 `beginTurn()`：

```typescript
// 执行器与 memory lifecycle 对齐回合边界；P01-P10 不注入时继续直接调用 ModelClient。
this.#modelRequestExecutor?.beginTurn();
```

然后在模型边界二选一：

```typescript
const reply = await (this.#modelRequestExecutor === undefined
  ? this.#model.complete(request)
  : this.#modelRequestExecutor.complete(request));
```

### 为什么必须分成 beginTurn 和 complete 两个方法

`beginTurn()` 重置的是**跨请求的恢复状态**：当前模型（可能已经切到 fallback）、当前输出预算（可能已经升级过）、连续 529 计数、deadline 起点。

```typescript
beginTurn(): void {
  const startedAt = this.#monotonicNow();
  this.#state = {
    currentModel: this.#config.primaryModel,
    currentMaxTokens: this.#config.initialMaxTokens,
    hasEscalated: false,
    recoveryCount: 0,
    consecutive529: 0,
    hasAttemptedReactiveCompact: false,
  };
  this.#deadline = startedAt + this.#config.totalTimeoutSeconds;
}
```

这些状态的生命周期是「一个 Agent turn」，而 `complete()` 在一个 turn 内会被调用多次（每一轮工具调用之后都要再问一次模型）。如果把重置放进 `complete()`，上一轮切到 fallback、下一轮又回到主模型，deadline 也会每轮重新计时——总时限就形同虚设。

反过来，忘记调 `beginTurn()` 会立刻失败，不会静默用默认值跑：

```text
=== 场景五：忘记 beginTurn ===
抛出 RecoveryError: Recovery turn has not been started
```

### 内部重试不重跑任何 Hook

这是「一个逻辑请求」这个说法的实际含义。`complete()` 内部无论重试多少次，都不会重新运行：

- UserPrompt Hook
- 请求历史准备（`historyProcessor.prepare()`）
- 动态系统提示渲染
- 工具 schema 快照
- Stop Hook

外层看到的仍然是一次模型调用。这一点可以直接测出来：

```text
=== 截断恢复不污染 canonical history ===
finalText = "完整答案"
turns = 1
canonical history:
  user: "干活"
  assistant: "完整答案"
主 Loop 请求次数 = 2（外层只算 1 个 turn）
主 Loop 请求 maxTokens = [8000,64000]
主 Loop 请求 model = ["primary-model","primary-model"]
旁路请求次数（走 raw model）= 1
旁路请求 model = ["undefined"]
```

这段输出是本章最该记住的一屏（完整脚本见「实验六」）。逐行看：

- **canonical history 只有两条消息。** 那段被丢弃的残缺回复没有留下任何痕迹。
- **`turns = 1`。** 内部发了 2 次 HTTP 请求，外层仍然只算一个 turn。
- **`maxTokens = [8000,64000]`。** 预算升级确实发生了。
- **旁路请求 `model = ["undefined"]`。** 记忆抽取那类调用根本没带 `model` 字段。这条是下一节的话题。

### `content_filter` 在 Loop 层显式失败

适配器能解析 `content_filter`，因为它是 OpenAI 合法的 `finish_reason`。但被内容过滤的回复既不是最终答案，也不是可重试的瞬态错误。所以 loop.ts 在写入 history 前就拒掉它：

```typescript
// content_filter 是供应商明确说"回复被过滤"的结束状态；不能当作最终文本或继续重试。
if (reply.finishReason === "content_filter") {
  throw new AgentRunError("Model response was blocked by the content filter");
}
```

```text
=== content_filter 在 Loop 层显式失败 ===
抛出 AgentRunError: Model response was blocked by the content filter
模型请求次数 = 1（没有重试）
```

放在 Loop 层而不是恢复层，是因为这是**语义判断**（这个回复能不能当答案），不是**可靠性判断**（这次调用要不要重来）。恢复层只管后者。

### 拒绝调用方覆盖恢复状态

`complete()` 会检查调用方有没有试图自己指定 `model` 或 `maxTokens`：

```typescript
if (request.model !== undefined && request.model !== this.#config.primaryModel) {
  throw new RangeError("request.model must match RecoveryConfig.primaryModel");
}
if (
  request.maxTokens !== undefined &&
  (!Number.isInteger(request.maxTokens) ||
    request.maxTokens <= 0 ||
    request.maxTokens !== this.#config.initialMaxTokens)
) {
  throw new RangeError("request.maxTokens must match RecoveryConfig.initialMaxTokens");
}
```

四种输入的实测：

```text
=== 场景四：拒绝调用方覆盖恢复状态 ===
model=another-model maxTokens=undefined    -> RangeError: request.model must match RecoveryConfig.primaryModel
model=undefined maxTokens=4000             -> RangeError: request.maxTokens must match RecoveryConfig.initialMaxTokens
model=undefined maxTokens=8000             -> 接受，模型调用 1 次
model=primary-model maxTokens=undefined    -> 接受，模型调用 1 次
```

规则是「不填，或者填得和恢复层的起点一致」。为什么这么严？因为这两个字段是恢复状态机自己要改的：`model` 会因 fallback 变化，`maxTokens` 会因预算升级变化。允许调用方每轮传入自己的值，就等于允许它随时把状态机重置到一个不确定的位置。与其在运行时表现成「fallback 莫名失效」，不如在接线阶段就报错。

### 组合根：能力位与配置必须成对

P01-P10 不注入执行器，仍直接调用 raw `ModelClient`，原有的 `length` guard 继续作为明确失败（`IncompleteModelReplyError`）。只有 P11 的 Bootstrap 才创建 `RecoveryManager`，并且要求显式 `RecoveryConfig`：

```text
=== 能力位与配置必须成对 ===
P10 + recoveryConfig -> recoveryConfig requires chapter 11 or later
P11 无 recoveryConfig -> recoveryConfig is required for chapter 11 or later
```

两个方向都拦：给 P10 传恢复配置是配置错误（能力位没开，配置不会生效，静默忽略最坏）；P11 不传恢复配置更危险——如果允许，P11 会**静默退化成 raw ModelClient**，你以为有恢复能力其实没有，直到线上凌晨两点才发现。

第三个检查是依赖关系：

```typescript
if (dependencies.recoveryConfig !== undefined) {
  if (compactionManager === undefined) {
    throw new Error("recovery capability requires compaction");
  }
```

响应式压缩要用 `CompactionManager`，所以 `recovery` 能力必须建立在 `compaction` 之上。

### 摘要器必须绑定裸模型

Bootstrap 复用同一个 `CompactionManager` 处理主动和响应式压缩（保证协议组拆分规则一致），但 `ModelHistorySummarizer` 继续绑定 raw model：

```typescript
new CompactionManager({
  workspace: dependencies.workspace,
  summarizer: new ModelHistorySummarizer(dependencies.model),  // 注意是 model，不是 recovery
})
```

如果把 summarizer 绑到 `RecoveryManager`，路径会变成：

```text
输入过长 → 响应式压缩 → 摘要请求 → 恢复层 → 摘要请求本身输入过长 → 再压缩 → ...
```

每一层都还持有上一层的 deadline 和状态，问题会以「莫名超时」的形式表现出来，而不是一个清晰的栈溢出。

同理，本章只包装**主 Loop 请求**。memory selector/extractor、压缩摘要器、一次性 subagent 仍使用 raw model——这就是上面那行 `旁路请求 model = ["undefined"]` 的来源：旁路请求没有携带 `model` 字段，因为它没经过恢复层。

给这些旁路调用加恢复能力需要新的会话所有权契约（谁的 deadline？谁的 fallback 状态？），不能共享主 Agent 的 `RecoveryState`。这是本章明确划出的边界，不是遗漏。

---

## 运行第 11 章

### 第 0 步：准备环境

```powershell
Set-Location 'F:\笔记\Agent实操\code'
node --version      # 需要 Node 20 以上
npm install         # 只在第一次或依赖变动后执行
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch11
```

期望看到：

```text
Test Files  28 passed (28)
     Tests  258 passed (258)
```

输出中间会出现一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这不是报错。** 它是 `config.test.ts` 主动断言的一条预期输出——测试在验证「缺少配置时必须明确报错而不是静默降级」。只要最后一行是 `passed`，就一切正常。

（这行只列了三个字段，因为那条测试跑的是 `--chapter 1`，第 1 章不需要 fallback model。）

### 第 2 步：配置四个环境变量

第 11 章起 `.env` 需要**四个**字段，比前十章多一个 `OPENAI_FALLBACK_MODEL`：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Copy-Item '.env.example' '.env'
notepad '.env'
```

`.env.example` 的内容就是这四行：

```text
OPENAI_BASE_URL=
OPENAI_API_KEY=
OPENAI_MODEL=
OPENAI_FALLBACK_MODEL=
```

如果漏了 fallback，启动时会立刻失败，而不是等到真的过载时才发现：

```text
配置错误: Missing required settings: OPENAI_FALLBACK_MODEL
```

四个都没配的话，四个字段会**一次性全部列出**，不用一个一个试：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL, OPENAI_FALLBACK_MODEL
```

`OPENAI_FALLBACK_MODEL` 填什么？最有意义的选法是**和主模型不同容量或不同部署的模型**——主模型连续过载时，切到一个同样过载的模型没有任何收益。填成和主模型同名是允许的（配置校验不拦），但那样 fallback 就只是空转。

### 第 3 步：跑一次真实请求

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch11 -- --prompt "只回答一句话：你现在用的是哪个模型名？不要调用任何工具。"
```

实际输出（模型答什么取决于你配的端点）：

```text
[Hook] UserPromptSubmit
[Hook] Stop
我是 Claude，由 Anthropic 开发。
```

两行 `[Hook]` 各出现**一次**，这是本章一个隐含的正确性信号：恢复层在 Hook 之内，内部重试不会让它们重复打印。

也可以用统一入口，两种形式共用同一个 P11 Profile 和同一个 Loop：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 11 --prompt "检查当前工作区并给出结论"
```

`--prompt` 是**具名参数，不能省**。直接写位置参数会立刻失败：

```text
运行失败: Unknown argument: 检查当前工作区
```

固定入口 `ch11` 已经自动补上了 `--chapter 11`，不需要自己再传。

### 第 4 步（可选）：完整门禁

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run lint
npm run format:check
```

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...`（测试输出中间） | `config.test.ts` 断言的预期输出 | 不用管，看最后一行是否 `passed` |
| `配置错误: Missing required settings: OPENAI_FALLBACK_MODEL` | 沿用了前十章的三字段 `.env` | 补上第四个字段 |
| `运行失败: Unknown argument: ...` | 忘了写 `--prompt` | 参数必须具名 |
| `Error: recoveryConfig is required for chapter 11 or later` | 手工调 `buildAgent(P11, ...)` 没传配置 | 传一个 `RecoveryConfig`；这条拦的是「静默退化成裸模型」 |
| `Error: recoveryConfig requires chapter 11 or later` | 给 P10 或更早的 profile 传了恢复配置 | 恢复配置只在 P11+ 生效，去掉或换 profile |
| `Error: recovery capability requires compaction` | 自己拼了个只有 `recovery` 没有 `compaction` 的能力集 | 用导出的 `P11` 单例 |
| `RecoveryError: Recovery turn has not been started` | 直接调 `complete()` 没先 `beginTurn()` | 每个 turn 开始时调一次 `beginTurn()` |
| `RangeError: request.model must match RecoveryConfig.primaryModel` | 请求里自己指定了别的模型名 | 不要在请求里覆盖 `model`，fallback 由恢复层决定 |
| `RangeError: request.maxTokens must match RecoveryConfig.initialMaxTokens` | 请求里自己指定了输出预算 | 预算由恢复层升级，不要在请求里写 |
| `RangeError: escalatedMaxTokens must not exceed modelMaxTokens` | 升级后预算超过模型上限 | 调小 `escalatedMaxTokens` 或调大 `modelMaxTokens` |
| `RangeError: escalatedMaxTokens must exceed initialMaxTokens` | 两档预算写反或写成相等 | 升级档必须严格大于初始档 |
| `RangeError: baseDelaySeconds must not exceed maxDelaySeconds` | 退避基线比封顶还大 | 检查这两个值 |
| `InvalidRetryAfterError: Retry-After is not seconds or an HTTP date` | 端点返回了 ISO 时间或非法值 | 看「Retry-After 解析规则」那张表；确实需要支持 ISO 就改正则 |
| `PromptTooLongRetryError: prompt-too-long compaction was already attempted` | 压缩一次后仍超窗口 | 查是否有单条巨型 user 消息或巨型工具结果 |
| `RecoveryRetriesExhausted: continuation recovery attempts exhausted` | 续写次数用完还在截断 | 拆小任务，或调大 `maxContinuations` |
| `RecoveryRetriesExhausted: length recovery requires a non-empty text-only assistant fragment` | 截断的回复带 `tool_calls` 或正文为空 | 这是有意的硬失败，见「路径一」 |
| `RecoveryRetriesExhausted: rate limit recovery attempts exhausted` | 10 次都是 429 | 配额确实不够，降并发或换 key |
| `RecoveryDeadlineExceeded: retry delay would reach or cross the turn deadline` | 要等的时间超过剩余时限 | 调大 `totalTimeoutSeconds`，或接受这次失败 |
| `AgentRunError: Model response was blocked by the content filter` | 供应商内容过滤命中 | 改 prompt；这不是可重试错误 |
| `OpenAIResponseError: Unsupported finish_reason: ...` | 端点返回了未知结束原因 | 先确认端点兼容性；宽容处理会引入静默截断 |
| 500 / 503 / `ECONNRESET` 直接抛出 | 本章有意不重试这些 | 见「这章没有做什么」 |

---

## 七个动手小实验

这七个实验**都不调真实模型**，不需要 API Key，也不会访问网络。跑完就能把本章所有核心结论亲手验证一遍。

先建一个临时目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\scratch' | Out-Null
```

前四个实验共用一个小工具文件，把「脚本化假模型 + 全可观测的 RecoveryManager」抽出来，省掉重复代码。存成 `code\scratch\kit.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { CompactionManager, CompactionSummary } from "../chapters/ch11/src/features/compaction.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch11/src/core/model.js";
import { RecoveryConfig, RecoveryManager } from "../chapters/ch11/src/features/recovery.js";
import type { CancellationToken } from "../chapters/ch11/src/features/recovery.js";

// 按脚本顺序返回回复或抛出错误，并记录每次收到的请求
export class ScriptModel implements ModelClient {
  readonly requests: ModelRequest[] = [];
  readonly #actions: (ModelReply | Error)[];

  constructor(actions: readonly (ModelReply | Error)[]) {
    this.#actions = [...actions];
  }

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.requests.push(request);
    const action = this.#actions.shift();
    if (action === undefined) {
      throw new Error("脚本用尽：不该再发请求");
    }
    if (action instanceof Error) {
      throw action;
    }
    return action;
  }
}

export interface KitOptions {
  readonly config?: RecoveryConfig;
  readonly jitter?: (upperBound: number) => number;
  readonly utcNow?: () => Date;
  readonly cancellation?: CancellationToken;
  readonly summarizerError?: Error;
  readonly realSleep?: boolean;
}

export interface Kit {
  readonly recovery: RecoveryManager;
  readonly workspace: string;
  readonly delays: number[];
  cleanup(): void;
}

// 组装一个全可观测的 RecoveryManager：假时钟、记账 sleeper、可注入抖动
export function buildKit(model: ModelClient, options: KitOptions = {}): Kit {
  const workspace = mkdtempSync(join(tmpdir(), "kit-"));
  const delays: number[] = [];
  let clock = 1_000;
  const summarizer = {
    async summarize() {
      if (options.summarizerError !== undefined) {
        throw options.summarizerError;
      }
      return new CompactionSummary({
        currentGoal: "把请求压回窗口内",
        keyFindings: [],
        filesReadOrChanged: [],
        remainingWork: [],
        userConstraints: [],
      });
    },
  };
  const recovery = new RecoveryManager({
    model,
    compaction: new CompactionManager({
      workspace,
      summarizer,
      idGenerator: () => "kit-transcript",
    }),
    config:
      options.config ??
      new RecoveryConfig({ primaryModel: "primary-model", fallbackModel: "fallback-model" }),
    jitter: options.jitter ?? (() => 0),
    ...(options.utcNow === undefined ? {} : { utcNow: options.utcNow }),
    ...(options.cancellation === undefined ? {} : { cancellation: options.cancellation }),
    ...(options.realSleep === true
      ? {}
      : {
          monotonic: () => clock,
          sleeper: async (seconds: number) => {
            delays.push(seconds);
            clock += seconds;
          },
        }),
  });
  return {
    recovery,
    workspace,
    delays,
    cleanup: () => rmSync(workspace, { recursive: true, force: true }),
  };
}
```

这个文件里有三处值得注意的设计，它们也是本章测试策略的缩影：

- `jitter` 默认注入成 `() => 0`，所以退避曲线是**确定的**，可以逐项断言。
- `sleeper` 不真的等待，只记录秒数并推进假时钟。10 次退避的 127.5 秒，实测跑完不到一毫秒。
- `monotonic` 是手动推进的假时钟，deadline 判断因此完全可控，不受机器负载影响。

下面每个实验的输出都是实际跑出来的。临时目录名里的随机串**你跑出来的会不一样**，这是正常的。

### 实验一：三条恢复路径各自的完整轨迹

存成 `code\scratch\exp1-paths.ts`：

```typescript
import { assistantMessage, systemMessage, userMessage } from "../chapters/ch11/src/core/messages.js";
import {
  ModelOverloadedError,
  ModelPromptTooLongError,
  ModelRateLimitError,
} from "../chapters/ch11/src/core/model.js";
import type { ModelRequest } from "../chapters/ch11/src/core/model.js";
import { ScriptModel, buildKit } from "./kit.js";

const original: ModelRequest = Object.freeze({
  messages: Object.freeze([systemMessage("system prompt"), userMessage("写一份长报告")]),
  tools: Object.freeze([]),
});

function describe(request: ModelRequest): string {
  const tail = request.messages.at(-1);
  const preview =
    tail === undefined ? "(空)" : `${tail.role}: ${JSON.stringify(tail.content).slice(0, 46)}`;
  return `model=${request.model} maxTokens=${request.maxTokens} 消息数=${request.messages.length} 末条=${preview}`;
}

console.log("=== 路径一：输出被截断 ===");
{
  const model = new ScriptModel([
    { message: assistantMessage("这段会被整段丢弃"), finishReason: "length" },
    { message: assistantMessage("前半段。"), finishReason: "length" },
    { message: assistantMessage("后半段。"), finishReason: "stop" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    const reply = await kit.recovery.complete(original);
    model.requests.forEach((request, index) => {
      console.log(`第 ${index + 1} 次请求  ${describe(request)}`);
    });
    console.log(`最终 content = ${JSON.stringify(reply.message.content)}`);
    console.log(`最终 finishReason = ${reply.finishReason}`);
    console.log(`调用方原请求消息数 = ${original.messages.length}（未被改写）`);
    console.log(`state.recoveryCount = ${kit.recovery.state.recoveryCount}`);
    console.log(`state.hasEscalated = ${kit.recovery.state.hasEscalated}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 路径二：输入过长 ===");
{
  const model = new ScriptModel([
    new ModelPromptTooLongError("too long"),
    { message: assistantMessage("压缩后成功"), finishReason: "stop" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    const reply = await kit.recovery.complete(original);
    model.requests.forEach((request, index) => {
      console.log(`第 ${index + 1} 次请求  ${describe(request)}`);
    });
    console.log(`第 2 次请求首条 role = ${model.requests[1]?.messages[0]?.role}`);
    console.log(
      `第 2 次请求第二条是否含 compacted_history = ${String(
        model.requests[1]?.messages[1]?.content.includes("compacted_history"),
      )}`,
    );
    console.log(`最终 content = ${JSON.stringify(reply.message.content)}`);
    console.log(
      `state.hasAttemptedReactiveCompact = ${kit.recovery.state.hasAttemptedReactiveCompact}`,
    );
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 路径二：第二次输入过长直接失败 ===");
{
  const model = new ScriptModel([
    new ModelPromptTooLongError("too long"),
    new ModelPromptTooLongError("still too long"),
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(original);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 路径三：连续 529 切 fallback ===");
{
  const model = new ScriptModel([
    new ModelOverloadedError("overloaded"),
    new ModelOverloadedError("overloaded"),
    new ModelOverloadedError("overloaded"),
    { message: assistantMessage("备用模型答完"), finishReason: "stop" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(original);
    console.log(`每次请求用的模型 = ${JSON.stringify(model.requests.map((item) => item.model))}`);
    console.log(`实际 sleep 秒数（jitter 注入为 0）= ${JSON.stringify(kit.delays)}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 路径三：429 遵守 Retry-After ===");
{
  const model = new ScriptModel([
    new ModelRateLimitError("rate limited", { retryAfter: "3.25" }),
    { message: assistantMessage("限流后成功"), finishReason: "stop" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(original);
    console.log(`实际 sleep 秒数 = ${JSON.stringify(kit.delays)}（不是退避基线 0.5）`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 未知错误：原样抛出，不重试 ===");
{
  const defect = new TypeError("undefined is not a function");
  const model = new ScriptModel([defect]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(original);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`是否同一个对象 = ${String(error === defect)}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
    console.log(`sleep 次数 = ${kit.delays.length}`);
  } finally {
    kit.cleanup();
  }
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp1-paths.ts
```

输出就是本章开头「先看三条路径真实跑起来什么样」那三段，加上后面三段：

```text
=== 路径二：第二次输入过长直接失败 ===
抛出 PromptTooLongRetryError: prompt-too-long compaction was already attempted
实际请求次数 = 2

=== 路径三：429 遵守 Retry-After ===
实际 sleep 秒数 = [3.25]（不是退避基线 0.5）

=== 未知错误：原样抛出，不重试 ===
抛出 TypeError: undefined is not a function
是否同一个对象 = true
实际请求次数 = 1
sleep 次数 = 0
```

**改一个地方再跑一次：** 把路径三的三个 `ModelOverloadedError` 删掉一个，只留两个：

```text
=== 路径三：连续 529 切 fallback ===
每次请求用的模型 = ["primary-model","primary-model","primary-model"]
实际 sleep 秒数（jitter 注入为 0）= [0.5,1]
```

两次 529 不够，第三次请求仍然打在主模型上。阈值是 3，不是「过载了就切」。

顺便说一句关于 `ScriptModel` 的用法：它的动作列表必须**刚好够用**。如果你想试「一直截断」，得往路径一里塞足够多的 `length` 回复，否则会先撞上 `Error: 脚本用尽：不该再发请求`——那是脚本本身的断言，不是恢复层的行为。下一个实验就是专门为「一直截断」准备的。

### 实验二：续写的四条边界

存成 `code\scratch\exp2-continuation.ts`：

```typescript
import {
  assistantMessage,
  systemMessage,
  toolCall,
  userMessage,
} from "../chapters/ch11/src/core/messages.js";
import type { ModelRequest } from "../chapters/ch11/src/core/model.js";
import { RecoveryConfig } from "../chapters/ch11/src/features/recovery.js";
import { ScriptModel, buildKit } from "./kit.js";

const req: ModelRequest = Object.freeze({
  messages: Object.freeze([systemMessage("s"), userMessage("写长文")]),
  tools: Object.freeze([]),
});

function config(maxContinuations: number): RecoveryConfig {
  return new RecoveryConfig({
    primaryModel: "primary-model",
    fallbackModel: "fallback-model",
    maxContinuations,
  });
}

console.log("=== 默认 maxContinuations=3，一直截断 ===");
{
  const model = new ScriptModel(
    Array.from({ length: 8 }, (_item, index) => ({
      message: assistantMessage(`片段${index}`),
      finishReason: "length" as const,
    })),
  );
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
    console.log(
      `每次请求消息数 = ${JSON.stringify(model.requests.map((item) => item.messages.length))}`,
    );
    console.log(`state.recoveryCount = ${kit.recovery.state.recoveryCount}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== maxContinuations=0：直接关掉续写 ===");
{
  const model = new ScriptModel([
    { message: assistantMessage("丢弃"), finishReason: "length" },
    { message: assistantMessage("还是截断"), finishReason: "length" },
  ]);
  const kit = buildKit(model, { config: config(0) });
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 截断片段带 tool_calls：立刻失败，不管还剩几次续写 ===");
{
  const model = new ScriptModel([
    { message: assistantMessage("丢弃"), finishReason: "length" },
    {
      message: assistantMessage(null, [toolCall("call-1", "read_file", "{}")]),
      finishReason: "length",
    },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
    console.log(`state.recoveryCount = ${kit.recovery.state.recoveryCount}（一次续写都没发生）`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 空字符串片段：同样拒绝 ===");
{
  const model = new ScriptModel([
    { message: assistantMessage("丢弃"), finishReason: "length" },
    { message: assistantMessage(""), finishReason: "length" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 最终回复带 tool_calls：片段照常合并，工具调用完整保留 ===");
{
  const call = toolCall("call-final", "read_file", '{"path":"README.md"}');
  const model = new ScriptModel([
    { message: assistantMessage("丢弃"), finishReason: "length" },
    { message: assistantMessage("我先说一半，"), finishReason: "length" },
    { message: assistantMessage("再说另一半，然后调工具。", [call]), finishReason: "tool_calls" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    const reply = await kit.recovery.complete(req);
    console.log(`最终 content = ${JSON.stringify(reply.message.content)}`);
    console.log(`最终 finishReason = ${reply.finishReason}`);
    console.log(`最终 toolCalls = ${JSON.stringify(reply.message.toolCalls)}`);
  } finally {
    kit.cleanup();
  }
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp2-continuation.ts
```

实际输出：

```text
=== 默认 maxContinuations=3，一直截断 ===
抛出 RecoveryRetriesExhausted: continuation recovery attempts exhausted
实际请求次数 = 5
每次请求消息数 = [2,2,4,6,8]
state.recoveryCount = 3

=== maxContinuations=0：直接关掉续写 ===
抛出 RecoveryRetriesExhausted: continuation recovery attempts exhausted
实际请求次数 = 2

=== 截断片段带 tool_calls：立刻失败，不管还剩几次续写 ===
抛出 RecoveryRetriesExhausted: length recovery requires a non-empty text-only assistant fragment
实际请求次数 = 2
state.recoveryCount = 0（一次续写都没发生）

=== 空字符串片段：同样拒绝 ===
抛出 RecoveryRetriesExhausted: length recovery requires a non-empty text-only assistant fragment
实际请求次数 = 2

=== 最终回复带 tool_calls：片段照常合并，工具调用完整保留 ===
最终 content = "我先说一半，再说另一半，然后调工具。"
最终 finishReason = tool_calls
最终 toolCalls = [{"id":"call-final","name":"read_file","arguments":"{\"path\":\"README.md\"}"}]
```

第三段和第四段抛的是同一条消息，但含义不同：第三段的 `recoveryCount = 0` 说明「不该续写」，第一段的 `recoveryCount = 3` 说明「续写机会用完」。看错误消息分不出来，看状态就能分出来。

### 实验三：退避曲线与 Retry-After 解析

存成 `code\scratch\exp3-backoff.ts`：

```typescript
import { assistantMessage, systemMessage, userMessage } from "../chapters/ch11/src/core/messages.js";
import { ModelOverloadedError, ModelRateLimitError } from "../chapters/ch11/src/core/model.js";
import type { ModelRequest } from "../chapters/ch11/src/core/model.js";
import { RecoveryConfig } from "../chapters/ch11/src/features/recovery.js";
import { ScriptModel, buildKit } from "./kit.js";

const req: ModelRequest = Object.freeze({
  messages: Object.freeze([systemMessage("s"), userMessage("u")]),
  tools: Object.freeze([]),
});

// 时限放大到 100000 秒，让 10 次尝试全部跑完而不被 deadline 截断
const longDeadline = new RecoveryConfig({
  primaryModel: "primary-model",
  fallbackModel: "fallback-model",
  totalTimeoutSeconds: 100_000,
});

function tenRateLimits(): ScriptModel {
  return new ScriptModel(Array.from({ length: 10 }, () => new ModelRateLimitError("rate limited")));
}

console.log("=== 完整退避曲线（jitter 注入为 0，10 次尝试耗尽）===");
{
  const model = tenRateLimits();
  const kit = buildKit(model, { config: longDeadline });
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`sleep 序列 = ${JSON.stringify(kit.delays)}`);
    console.log(`sleep 总秒数 = ${kit.delays.reduce((sum, item) => sum + item, 0)}`);
    console.log(`实际请求次数 = ${model.requests.length}`);
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 抖动取满（jitter 返回上界 = base × 25%）===");
{
  const kit = buildKit(tenRateLimits(), {
    config: longDeadline,
    jitter: (upperBound) => upperBound,
  });
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch {
    console.log(`sleep 序列 = ${JSON.stringify(kit.delays)}`);
    console.log(`sleep 总秒数 = ${kit.delays.reduce((sum, item) => sum + item, 0)}`);
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== 429 打断连续 529 计数 ===");
{
  const model = new ScriptModel([
    new ModelOverloadedError("overloaded"),
    new ModelOverloadedError("overloaded"),
    new ModelRateLimitError("rate limited"),
    new ModelOverloadedError("overloaded"),
    { message: assistantMessage("ok"), finishReason: "stop" },
  ]);
  const kit = buildKit(model);
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
    console.log(`每次请求用的模型 = ${JSON.stringify(model.requests.map((item) => item.model))}`);
    console.log(`sleep 序列 = ${JSON.stringify(kit.delays)}`);
    console.log("说明：529 -> 529 -> 429 -> 529，连续计数被 429 清零，所以从未切 fallback");
  } finally {
    kit.cleanup();
  }
}

console.log("\n=== Retry-After 解析规则 ===");
{
  const utcNow = new Date("2026-08-28T12:00:00.000Z");
  const cases: readonly string[] = [
    "0",
    "3",
    "3.25",
    " 7 ",
    "-1",
    "",
    "NaN",
    "Infinity",
    "1e2",
    new Date(utcNow.getTime() + 5_000).toUTCString(),
    "Fri, 28 Aug 2026 11:59:50 GMT",
    "2026-08-28T12:00:30.000Z",
    "2026-08-28T12:00:30",
    "not-a-date",
  ];
  for (const retryAfter of cases) {
    const model = new ScriptModel([
      new ModelRateLimitError("rate limited", { retryAfter }),
      { message: assistantMessage("ok"), finishReason: "stop" },
    ]);
    const kit = buildKit(model, { config: longDeadline, utcNow: () => utcNow });
    try {
      kit.recovery.beginTurn();
      await kit.recovery.complete(req);
      console.log(`${JSON.stringify(retryAfter).padEnd(36)} -> sleep ${kit.delays[0]} 秒`);
    } catch (error) {
      console.log(`${JSON.stringify(retryAfter).padEnd(36)} -> ${(error as Error).name}`);
    } finally {
      kit.cleanup();
    }
  }
  console.log(`（注入的 utcNow = ${utcNow.toISOString()}）`);
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp3-backoff.ts
```

输出就是「路径三」那一节里的四段（退避曲线、抖动取满、429 打断计数、14 行 Retry-After 矩阵）。

**这个实验最值得动手改的地方是 `jitterRatio`。** 把 `longDeadline` 里加一行 `jitterRatio: 0`，退避序列会变成完全确定的 `[0.5,1,2,4,8,16,32,32,32]`；改成 `jitterRatio: 1`，抖动上界就等于基线本身，最坏情况下每次等待翻倍。生产环境该用多少取决于你的并发规模——并发越高，抖动越该大。

### 实验四：时限、取消、回滚、覆盖、构造校验

这个实验一次性覆盖六个「不走正常路」的场景。存成 `code\scratch\exp4-deadline.ts`，分六段读：

```typescript
import { readdirSync } from "node:fs";
import { join } from "node:path";

import { assistantMessage, systemMessage, userMessage } from "../chapters/ch11/src/core/messages.js";
import { ModelPromptTooLongError, ModelRateLimitError } from "../chapters/ch11/src/core/model.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch11/src/core/model.js";
import { CancellationToken, RecoveryConfig } from "../chapters/ch11/src/features/recovery.js";
import { ScriptModel, buildKit } from "./kit.js";

const req: ModelRequest = Object.freeze({
  messages: Object.freeze([systemMessage("s"), userMessage("u")]),
  tools: Object.freeze([]),
});

console.log("=== 场景一：sleep 会跨过 deadline ===");
{
  const model = new ScriptModel(
    Array.from({ length: 3 }, () => new ModelRateLimitError("rate limited", { retryAfter: "30" })),
  );
  const kit = buildKit(model, {
    config: new RecoveryConfig({
      primaryModel: "primary-model",
      fallbackModel: "fallback-model",
      totalTimeoutSeconds: 10,
    }),
  });
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log("Retry-After = 30 秒，剩余 deadline = 10 秒");
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`是否真的 sleep 过 = ${String(kit.delays.length > 0)}`);
    console.log(`模型调用次数 = ${model.requests.length}`);
  } finally {
    kit.cleanup();
  }
}
```

第二段构造一个「永远不返回」的模型，用来验证取消是真的把信号传下去了：

```typescript
console.log("\n=== 场景二：取消进行中的模型调用 ===");
{
  class HangingModel implements ModelClient {
    signal: AbortSignal | undefined;
    settled = false;
    readonly started: Promise<void>;
    #resolveStarted: (() => void) | undefined;

    constructor() {
      this.started = new Promise<void>((resolve) => {
        this.#resolveStarted = resolve;
      });
    }

    async complete(_request: ModelRequest, signal?: AbortSignal): Promise<ModelReply> {
      this.#resolveStarted?.();
      if (signal === undefined) {
        throw new Error("恢复层必须传 AbortSignal");
      }
      this.signal = signal;
      return new Promise<ModelReply>((_resolve, reject) => {
        signal.addEventListener(
          "abort",
          () => {
            queueMicrotask(() => {
              this.settled = true;
              reject(new Error("模型调用已中止"));
            });
          },
          { once: true },
        );
      });
    }
  }
  const model = new HangingModel();
  const cancellation = new CancellationToken();
  const kit = buildKit(model, { cancellation, realSleep: true });
  try {
    kit.recovery.beginTurn();
    const pending = kit.recovery.complete(req);
    await model.started;
    console.log("模型请求已发出，此时触发 cancel()");
    cancellation.cancel();
    await pending;
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`模型侧 signal.aborted = ${String(model.signal?.aborted)}`);
    console.log(`模型侧操作已收束 settled = ${String(model.settled)}`);
  } finally {
    kit.cleanup();
  }
}
```

第三段让摘要器自己失败，看压缩失败会不会留下半成品：

```typescript
console.log("\n=== 场景三：摘要失败回滚 transcript ===");
{
  const failure = new Error("摘要模型自己挂了");
  const model = new ScriptModel(
    Array.from({ length: 3 }, () => new ModelPromptTooLongError("too long")),
  );
  const kit = buildKit(model, { summarizerError: failure });
  try {
    kit.recovery.beginTurn();
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`是否原样抛出摘要失败 = ${String(error === failure)}`);
    const artifacts = join(kit.workspace, ".agent_tutorial", "artifacts");
    console.log(`artifacts 目录内容 = ${JSON.stringify(readdirSync(artifacts))}`);
    console.log(`模型调用次数 = ${model.requests.length}（没有再重试）`);
  } finally {
    kit.cleanup();
  }
}
```

第四段和第五段是「调用方犯错」的两种形式：

```typescript
console.log("\n=== 场景四：拒绝调用方覆盖恢复状态 ===");
{
  const overrides: readonly ModelRequest[] = [
    { ...req, model: "another-model" },
    { ...req, maxTokens: 4_000 },
    { ...req, maxTokens: 8_000 },
    { ...req, model: "primary-model" },
  ];
  for (const overridden of overrides) {
    const model = new ScriptModel([{ message: assistantMessage("ok"), finishReason: "stop" }]);
    const kit = buildKit(model);
    const label = `model=${String(overridden.model)} maxTokens=${String(overridden.maxTokens)}`;
    try {
      kit.recovery.beginTurn();
      await kit.recovery.complete(overridden);
      console.log(`${label.padEnd(42)} -> 接受，模型调用 ${model.requests.length} 次`);
    } catch (error) {
      console.log(`${label.padEnd(42)} -> ${(error as Error).name}: ${(error as Error).message}`);
    } finally {
      kit.cleanup();
    }
  }
}

console.log("\n=== 场景五：忘记 beginTurn ===");
{
  const kit = buildKit(new ScriptModel([{ message: assistantMessage("ok"), finishReason: "stop" }]));
  try {
    await kit.recovery.complete(req);
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
  } finally {
    kit.cleanup();
  }
}
```

第六段把配置的自检规则全过一遍——包括两条「看着像错、其实合法」的：

```typescript
console.log("\n=== 场景六：RecoveryConfig 构造期校验 ===");
{
  const cases: readonly [string, () => RecoveryConfig][] = [
    [
      "escalatedMaxTokens > modelMaxTokens",
      () =>
        new RecoveryConfig({
          primaryModel: "p",
          fallbackModel: "f",
          escalatedMaxTokens: 64_000,
          modelMaxTokens: 32_000,
        }),
    ],
    [
      "initialMaxTokens >= escalatedMaxTokens",
      () =>
        new RecoveryConfig({
          primaryModel: "p",
          fallbackModel: "f",
          initialMaxTokens: 64_000,
          escalatedMaxTokens: 64_000,
        }),
    ],
    [
      "baseDelaySeconds > maxDelaySeconds",
      () =>
        new RecoveryConfig({
          primaryModel: "p",
          fallbackModel: "f",
          baseDelaySeconds: 60,
          maxDelaySeconds: 32,
        }),
    ],
    ["primaryModel 为空白", () => new RecoveryConfig({ primaryModel: "   ", fallbackModel: "f" })],
    [
      "maxTransientAttempts = 0",
      () => new RecoveryConfig({ primaryModel: "p", fallbackModel: "f", maxTransientAttempts: 0 }),
    ],
    [
      "maxContinuations = 0（合法：直接关掉续写）",
      () => new RecoveryConfig({ primaryModel: "p", fallbackModel: "f", maxContinuations: 0 }),
    ],
    [
      "jitterRatio = 0（合法：完全确定的退避）",
      () => new RecoveryConfig({ primaryModel: "p", fallbackModel: "f", jitterRatio: 0 }),
    ],
  ];
  for (const [label, build] of cases) {
    try {
      build();
      console.log(`${label.padEnd(40)} -> 构造成功`);
    } catch (error) {
      console.log(`${label.padEnd(40)} -> ${(error as Error).name}: ${(error as Error).message}`);
    }
  }
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp4-deadline.ts
```

六段的实际输出：

```text
=== 场景一：sleep 会跨过 deadline ===
Retry-After = 30 秒，剩余 deadline = 10 秒
抛出 RecoveryDeadlineExceeded: retry delay would reach or cross the turn deadline
是否真的 sleep 过 = false
模型调用次数 = 1

=== 场景二：取消进行中的模型调用 ===
模型请求已发出，此时触发 cancel()
抛出 RecoveryCancelledError: recovery turn was cancelled
模型侧 signal.aborted = true
模型侧操作已收束 settled = true

=== 场景三：摘要失败回滚 transcript ===
抛出 Error: 摘要模型自己挂了
是否原样抛出摘要失败 = true
artifacts 目录内容 = []
模型调用次数 = 1（没有再重试）

=== 场景四：拒绝调用方覆盖恢复状态 ===
model=another-model maxTokens=undefined    -> RangeError: request.model must match RecoveryConfig.primaryModel
model=undefined maxTokens=4000             -> RangeError: request.maxTokens must match RecoveryConfig.initialMaxTokens
model=undefined maxTokens=8000             -> 接受，模型调用 1 次
model=primary-model maxTokens=undefined    -> 接受，模型调用 1 次

=== 场景五：忘记 beginTurn ===
抛出 RecoveryError: Recovery turn has not been started

=== 场景六：RecoveryConfig 构造期校验 ===
escalatedMaxTokens > modelMaxTokens      -> RangeError: escalatedMaxTokens must not exceed modelMaxTokens
initialMaxTokens >= escalatedMaxTokens   -> RangeError: escalatedMaxTokens must exceed initialMaxTokens
baseDelaySeconds > maxDelaySeconds       -> RangeError: baseDelaySeconds must not exceed maxDelaySeconds
primaryModel 为空白                         -> TypeError: primaryModel must be a non-empty string
maxTransientAttempts = 0                 -> RangeError: maxTransientAttempts must be a positive integer
maxContinuations = 0（合法：直接关掉续写）          -> 构造成功
jitterRatio = 0（合法：完全确定的退避）              -> 构造成功
```

六段各自说明一件事：

| 场景 | 结论 |
| --- | --- |
| 一 | 时限不是「等完再检查」，是「等之前先算」。要等 30 秒但只剩 10 秒，就一秒都不等，直接抛 `RecoveryDeadlineExceeded` |
| 二 | 取消不只是让 `complete()` 提前返回，`AbortSignal` 真的传到了模型客户端，而且恢复层等到底层操作收束（`settled = true`）才抛错，不留悬空请求 |
| 三 | 压缩失败原样抛出摘要器的错误（`error === failure` 为 `true`），不包装成恢复层错误；`artifacts` 目录为空说明没写下任何半成品 |
| 四 | 覆盖 `model` 或 `maxTokens` 会被拒绝，除非它和恢复层自己的值完全一致——这是「同一个字段两个主人」的防线 |
| 五 | 忘记 `beginTurn()` 直接抛 `RecoveryError`，而不是拿一份陈旧状态硬跑 |
| 六 | 不合理的配置在**构造时**就炸，而不是等到线上第 5 次重试才暴露 |

**改一个地方再跑一次：** 把场景一的 `totalTimeoutSeconds` 从 10 改成 100，同一段脚本的结果变成：

```text
sleep 序列 = [30,30,30]
模型调用次数 = 4
抛出 Error: 脚本用尽：不该再发请求
```

它老老实实等了三次 30 秒（累计 90 秒，都在 100 秒时限内），第 4 次请求时脚本里只准备了 3 个错误，于是 `ScriptModel` 自己抛了「脚本用尽」。同样的输入、同样的 `Retry-After`，只改一个时限，就从「一秒都不等」变成「等满 90 秒」。这就是为什么时限该属于恢复层而不属于调用方——调用方每次传一个数字，行为就会飘。

### 实验五：适配器的错误分类与 200 校验

这就是前面「分类结果实测」和「HTTP 200 同样是不可信边界」两节输出的来源。它不需要真实网络——`OpenAIChatModel` 的第二个构造参数是一个可注入的 `OpenAIClientBoundary`，喂一个假客户端就行。存成 `code\scratch\exp5-adapter.ts`：

```typescript
import { APIError } from "openai";

import { OpenAIChatModel } from "../chapters/ch11/src/adapters/openai-chat.js";
import type { OpenAIClientBoundary } from "../chapters/ch11/src/adapters/openai-chat.js";
import type { OpenAISettings } from "../chapters/ch11/src/config.js";
import { userMessage } from "../chapters/ch11/src/core/messages.js";
import type { ModelRequest } from "../chapters/ch11/src/core/model.js";

const settings: OpenAISettings = Object.freeze({
  baseUrl: "https://example.test/v1",
  apiKey: "test-key",
  model: "primary-model",
});

const req: ModelRequest = Object.freeze({
  messages: Object.freeze([userMessage("hi")]),
  tools: Object.freeze([]),
});

// 一个最小的假 SDK 客户端：要么抛出给定的东西，要么原样返回给定的响应
function client(result: unknown, mode: "throw" | "return"): OpenAIClientBoundary {
  return {
    chat: {
      completions: {
        async create() {
          if (mode === "throw") {
            throw result;
          }
          return result;
        },
      },
    },
  };
}

const api = (status: number, body: unknown, headers?: Record<string, string>): APIError =>
  new APIError(status, body, undefined, new Headers(headers));
```

上半段把 14 种错误对象逐个喂进去，只看「归一了还是原样抛出」：

```typescript
console.log("=== 错误分类：只认结构化字段 ===");
const errorCases: readonly [string, unknown][] = [
  ["APIError 429", api(429, { code: "rate_limited" })],
  ["APIError 429 + retry-after 头", api(429, { code: "rate_limited" }, { "retry-after": "5" })],
  ["APIError 529", api(529, { type: "overloaded_error" })],
  ["APIError 400 code=context_length_exceeded", api(400, { code: "context_length_exceeded" })],
  ["APIError 400 code=prompt_too_long", api(400, { code: "prompt_too_long" })],
  ["APIError 400 嵌套 error.code=prompt_too_long", api(400, { error: { code: "prompt_too_long" } })],
  ["APIError 400 code=PROMPT_TOO_LONG（大写）", api(400, { code: "PROMPT_TOO_LONG" })],
  ["APIError 400 code=unknown_bad_request", api(400, { code: "unknown_bad_request" })],
  ["APIError 400 message 写了 prompt is too long", api(400, { message: "prompt is too long" })],
  ["APIError 500", api(500, { code: "server_error" })],
  ["APIError 503", api(503, { code: "unavailable" })],
  ["普通 Error，字符串里带 429", new Error("Error code: 429 - rate limited by upstream proxy")],
  [
    "伪造对象：有 status=429 但不是 APIError",
    Object.assign(new Error("looks like rate limit"), {
      status: 429,
      headers: new Headers({ "retry-after": "2" }),
      error: { code: "rate_limited" },
    }),
  ],
  ["连接层错误 ECONNRESET", Object.assign(new Error("socket hang up"), { code: "ECONNRESET" })],
];

for (const [label, thrown] of errorCases) {
  try {
    await new OpenAIChatModel(settings, client(thrown, "throw")).complete(req);
    console.log(`${label.padEnd(44)} -> 没有抛错（不应发生）`);
  } catch (mapped) {
    const name = (mapped as Error).name;
    console.log(`${label.padEnd(44)} -> ${mapped === thrown ? `原样抛出 ${name}` : `归一为 ${name}`}`);
  }
}
```

下半段构造 13 种 HTTP 200 响应。`choice()` 和 `ok()` 两个小工厂让每个用例只占一两行：

```typescript
console.log("\n=== 成功响应：200 也要逐字段校验 ===");

type Fields = Record<string, unknown>;

const choice = (overrides: Fields = {}, message: Fields = {}): Fields => ({
  index: 0,
  logprobs: null,
  finish_reason: "stop",
  message: { role: "assistant", content: "done", refusal: null, ...message },
  ...overrides,
});

const ok = (overrides: Fields = {}): Fields => ({
  id: "completion-1",
  created: 0,
  model: "primary-model",
  object: "chat.completion",
  choices: [choice()],
  ...overrides,
});

const responseCases: readonly [string, unknown][] = [
  ["标准 stop 回复", ok()],
  [
    "finish_reason=content_filter",
    ok({ choices: [choice({ finish_reason: "content_filter" }, { content: "部分内容" })] }),
  ],
  ["content=null 但有 refusal", ok({ choices: [choice({}, { content: null, refusal: "I cannot help." })] })],
  ["choices 为空数组", ok({ choices: [] })],
  ["choices 有两个", ok({ choices: [choice({}, { content: "a" }), choice({ index: 1 }, { content: "b" })] })],
  ["finish_reason 是未知新值", ok({ choices: [choice({ finish_reason: "max_output_reached" })] })],
  ["role 不是 assistant", ok({ choices: [choice({}, { role: "user" })] })],
  [
    "tool_calls 缺 id",
    ok({
      choices: [
        choice(
          { finish_reason: "tool_calls" },
          { content: null, tool_calls: [{ type: "function", function: { name: "shell", arguments: "{}" } }] },
        ),
      ],
    }),
  ],
  [
    "旧式 function_call",
    ok({ choices: [choice({}, { content: null, function_call: { name: "shell", arguments: "{}" } })] }),
  ],
  ["usage.prompt_tokens 为负数", ok({ usage: { prompt_tokens: -1, completion_tokens: 1, total_tokens: 0 } })],
  ["usage 缺 total_tokens", ok({ usage: { prompt_tokens: 10, completion_tokens: 5 } })],
  ["usage 三项齐全", ok({ usage: { prompt_tokens: 10, completion_tokens: 5, total_tokens: 15 } })],
  ["响应根不是对象", "OK"],
];

for (const [label, response] of responseCases) {
  try {
    const reply = await new OpenAIChatModel(settings, client(response, "return")).complete(req);
    console.log(
      `${label.padEnd(30)} -> 通过：content=${JSON.stringify(reply.message.content)} finishReason=${reply.finishReason}`,
    );
  } catch (error) {
    console.log(`${label.padEnd(30)} -> ${(error as Error).name}: ${(error as Error).message}`);
  }
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp5-adapter.ts
```

输出就是本文前面「分类结果实测」的 14 行加上「HTTP 200 同样是不可信边界」的 13 行。

**改一个地方再跑一次：** 把 `["APIError 429", api(429, { code: "rate_limited" })]` 的 status 从 429 改成 529，看它从 `ModelRateLimitError` 变成 `ModelOverloadedError`——**只改数字，不改文案**，分类就变了。再反过来：把文案从 `rate_limited` 改成 `totally_unrelated`，分类**不变**。这两次改动合起来就是「只认结构化字段」的完整含义。

### 实验六：从完整 Agent 观察恢复层

前面五个实验都在直接操作 `RecoveryManager`。这个实验换个视角：通过 `buildAgent` 组装一个完整 Agent，从**外面**看恢复到底留下了什么痕迹。存成 `code\scratch\exp6-loop.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { buildAgent } from "../chapters/ch11/src/bootstrap.js";
import { assistantMessage } from "../chapters/ch11/src/core/messages.js";
import { PermissionDecision } from "../chapters/ch11/src/core/permissions.js";
import type {
  ApprovalProvider,
  AuditSink,
  PermissionRequest,
} from "../chapters/ch11/src/core/permissions.js";
import { P10, P11 } from "../chapters/ch11/src/core/profiles.js";
import { RecoveryConfig } from "../chapters/ch11/src/features/recovery.js";
import { ScriptModel } from "./kit.js";

class AllowAll implements ApprovalProvider {
  async decide(_request: PermissionRequest): Promise<PermissionDecision> {
    return new PermissionDecision("allow", "probe", "probe");
  }
}

class NoAudit implements AuditSink {
  async record(): Promise<void> {}
}

const config = new RecoveryConfig({
  primaryModel: "primary-model",
  fallbackModel: "fallback-model",
});

// 每个场景用一个独立的临时工作区，跑完就删
function withWorkspace<T>(prefix: string, body: (workspace: string) => Promise<T>): Promise<T> {
  const workspace = mkdtempSync(join(tmpdir(), prefix));
  return body(workspace).finally(() => rmSync(workspace, { recursive: true, force: true }));
}

console.log("=== 能力位与配置必须成对 ===");
await withWorkspace("exp6-pair-", async (workspace) => {
  try {
    buildAgent(P10, { model: new ScriptModel([]), workspace, recoveryConfig: config });
  } catch (error) {
    console.log(`P10 + recoveryConfig -> ${(error as Error).message}`);
  }
  try {
    buildAgent(P11, { model: new ScriptModel([]), workspace });
  } catch (error) {
    console.log(`P11 无 recoveryConfig -> ${(error as Error).message}`);
  }
});
```

第二段是这个实验的重点：脚本里第一次回复是截断的，第二次才完整。跑完之后去数 canonical history 剩几条：

```typescript
console.log("\n=== 截断恢复不污染 canonical history ===");
await withWorkspace("exp6-history-", async (workspace) => {
  const model = new ScriptModel([
    { message: assistantMessage("这段残缺回复会被整段丢弃"), finishReason: "length" },
    { message: assistantMessage("完整答案"), finishReason: "stop" },
    { message: assistantMessage("[]"), finishReason: "stop" },
  ]);
  const runner = buildAgent(P11, {
    model,
    workspace,
    recoveryConfig: config,
    approvalProvider: new AllowAll(),
    auditSink: new NoAudit(),
  });
  const result = await runner.run("干活");
  console.log(`finalText = ${JSON.stringify(result.finalText)}`);
  console.log(`turns = ${result.turns}`);
  console.log("canonical history:");
  for (const message of result.history) {
    console.log(`  ${message.role}: ${JSON.stringify(message.content)}`);
  }
  // 主 Loop 请求带工具，旁路请求（记忆抽取、摘要等）不带
  const main = model.requests.filter((request) => request.tools.length > 0);
  console.log(`主 Loop 请求次数 = ${main.length}（外层只算 1 个 turn）`);
  console.log(`主 Loop 请求 maxTokens = ${JSON.stringify(main.map((item) => item.maxTokens))}`);
  console.log(`主 Loop 请求 model = ${JSON.stringify(main.map((item) => item.model))}`);
  const side = model.requests.filter((request) => request.tools.length === 0);
  console.log(`旁路请求次数（走 raw model）= ${side.length}`);
  console.log(`旁路请求 model = ${JSON.stringify(side.map((item) => String(item.model)))}`);
});

console.log("\n=== content_filter 在 Loop 层显式失败 ===");
await withWorkspace("exp6-filter-", async (workspace) => {
  const model = new ScriptModel([
    { message: assistantMessage("被过滤的片段"), finishReason: "content_filter" },
  ]);
  const runner = buildAgent(P11, {
    model,
    workspace,
    recoveryConfig: config,
    approvalProvider: new AllowAll(),
    auditSink: new NoAudit(),
  });
  try {
    await runner.run("干活");
  } catch (error) {
    console.log(`抛出 ${(error as Error).name}: ${(error as Error).message}`);
    console.log(`模型请求次数 = ${model.requests.length}（没有重试）`);
  }
});
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp6-loop.ts
```

实际输出：

```text
=== 能力位与配置必须成对 ===
P10 + recoveryConfig -> recoveryConfig requires chapter 11 or later
P11 无 recoveryConfig -> recoveryConfig is required for chapter 11 or later
=== 截断恢复不污染 canonical history ===
finalText = "完整答案"
turns = 1
canonical history:
  user: "干活"
  assistant: "完整答案"
主 Loop 请求次数 = 2（外层只算 1 个 turn）
主 Loop 请求 maxTokens = [8000,64000]
主 Loop 请求 model = ["primary-model","primary-model"]
旁路请求次数（走 raw model）= 1
旁路请求 model = ["undefined"]
=== content_filter 在 Loop 层显式失败 ===
抛出 AgentRunError: Model response was blocked by the content filter
模型请求次数 = 1（没有重试）
```

第三段脚本里塞的第三个回复 `assistantMessage("[]")` 是给记忆抽取器准备的——那次调用不带工具，所以被 `side` 过滤出来，`model` 是 `undefined`。这就是「只包装主 Loop 请求」的直接证据：同一个 Agent 内，一部分请求经过恢复层（带 `model` 和 `maxTokens`），另一部分不经过（两个字段都空）。

**改一个地方再跑一次：** 把第二段的第一条回复从 `finishReason: "length"` 改成 `"stop"`：

```text
finalText = "这段残缺回复会被整段丢弃"
turns = 1
canonical history:
  user: "干活"
  assistant: "这段残缺回复会被整段丢弃"
主 Loop 请求次数 = 1（外层只算 1 个 turn）
主 Loop 请求 maxTokens = [8000]
```

请求次数 2 → 1，`maxTokens` `[8000,64000]` → `[8000]`，而 canonical history 的**形状**没变：永远是「一条 user、一条 assistant」，`turns` 永远是 1。变的只有那条 assistant 的内容——因为这次是模型自己说完的。历史结构对「路上重试了几次」完全无感，这正是恢复层想要的效果。

### 实验七：四字段配置校验

最后一个实验最短，但它对应你真实会踩的第一个坑：`.env` 从三个字段变成四个。`settingsFromMapping(mapping, requireFallback)` 就是 CLI 内部用的那个函数，直接喂一个假的环境变量表就能测。存成 `code\scratch\exp7-config.ts`：

```typescript
import { settingsFromMapping } from "../chapters/ch11/src/config.js";

// 第 11 章起，CLI 要求四个字段齐全（requireFallback = true）
const cases: readonly [string, Record<string, string | undefined>][] = [
  ["四个字段齐全", {
    OPENAI_BASE_URL: "https://api.example.test/v1",
    OPENAI_API_KEY: "sk-test",
    OPENAI_MODEL: "primary-model",
    OPENAI_FALLBACK_MODEL: "fallback-model",
  }],
  ["缺 OPENAI_FALLBACK_MODEL", {
    OPENAI_BASE_URL: "https://api.example.test/v1",
    OPENAI_API_KEY: "sk-test",
    OPENAI_MODEL: "primary-model",
  }],
  ["OPENAI_FALLBACK_MODEL 只有空格", {
    OPENAI_BASE_URL: "https://api.example.test/v1",
    OPENAI_API_KEY: "sk-test",
    OPENAI_MODEL: "primary-model",
    OPENAI_FALLBACK_MODEL: "   ",
  }],
  ["什么都没配", {}],
  ["baseUrl 用了 file 协议", {
    OPENAI_BASE_URL: "file:///etc/passwd",
    OPENAI_API_KEY: "sk-test",
    OPENAI_MODEL: "primary-model",
    OPENAI_FALLBACK_MODEL: "fallback-model",
  }],
  ["fallback 与主模型同名（允许，但没有隔离收益）", {
    OPENAI_BASE_URL: "https://api.example.test/v1",
    OPENAI_API_KEY: "sk-test",
    OPENAI_MODEL: "same-model",
    OPENAI_FALLBACK_MODEL: "same-model",
  }],
];

for (const [label, mapping] of cases) {
  try {
    const settings = settingsFromMapping(mapping, true);
    console.log(
      `${label.padEnd(38)} -> 通过：model=${settings.model} fallbackModel=${String(settings.fallbackModel)}`,
    );
  } catch (error) {
    console.log(`${label.padEnd(38)} -> ${(error as Error).name}: ${(error as Error).message}`);
  }
}

console.log("\n=== 同一份配置在 P10（requireFallback = false）下 ===");
try {
  const settings = settingsFromMapping(
    {
      OPENAI_BASE_URL: "https://api.example.test/v1",
      OPENAI_API_KEY: "sk-test",
      OPENAI_MODEL: "primary-model",
    },
    false,
  );
  console.log(`通过：model=${settings.model} fallbackModel=${String(settings.fallbackModel)}`);
} catch (error) {
  console.log(`${(error as Error).name}: ${(error as Error).message}`);
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\exp7-config.ts
```

实际输出：

```text
四个字段齐全                                 -> 通过：model=primary-model fallbackModel=fallback-model
缺 OPENAI_FALLBACK_MODEL                -> ConfigurationError: Missing required settings: OPENAI_FALLBACK_MODEL
OPENAI_FALLBACK_MODEL 只有空格             -> ConfigurationError: Missing required settings: OPENAI_FALLBACK_MODEL
什么都没配                                  -> ConfigurationError: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL, OPENAI_FALLBACK_MODEL
baseUrl 用了 file 协议                     -> ConfigurationError: Missing required settings: OPENAI_BASE_URL
fallback 与主模型同名（允许，但没有隔离收益）            -> 通过：model=same-model fallbackModel=same-model

=== 同一份配置在 P10（requireFallback = false）下 ===
通过：model=primary-model fallbackModel=undefined
```

四点值得注意：

- **只有空格等于没填。** 校验会 trim，所以 `OPENAI_FALLBACK_MODEL=   ` 和完全不写是一个结果。
- **缺的字段一次性全列出。** 不用「填一个、跑一次、再发现下一个缺」。
- **`file://` 协议被当成「没填」。** 校验不只看有没有值，还看协议是不是 `http`/`https`。这是一条不显眼但重要的安全边界。
- **同一份三字段配置，P10 通过、P11 失败。** `requireFallback` 就是 `profile.capabilities.has("recovery")` 的直接后果——能力位决定配置要求，不是两套独立的规则。

**改一个地方再跑一次：** 把最后那段的 `false` 改成 `true`，同一份配置立刻变成 `ConfigurationError`。这一行就是「前十章的 `.env` 到第 11 章为什么不够用」的完整答案。

---

## 这章没有做什么

写清楚边界，比列举功能更能说明这一层的定位。

**不重试 500 / 503 / 连接错误。** 本章只对 429 和 529 做退避重试。500、503、`ECONNRESET`、`ETIMEDOUT` 全部原样抛出。原因是这些状态的语义不明确：500 可能是幂等的临时故障，也可能是「请求已经被部分处理」。在不知道服务端是否已产生副作用的情况下重试，风险大于收益。真要加，需要先明确幂等性契约。

**不做流式恢复。** 本章的所有恢复都建立在「拿到一个完整的 `ModelReply` 之后再判断」这个前提上。流式响应在中途断开时，已经吐出的 token 该不该保留、要不要拼进续写，是另一套设计。

**不做主动 token 预算控制。** 恢复层是**被动**的：等到供应商说「太长了」才压缩。真实生产系统通常还会在发请求**之前**估算 token 数并提前裁剪。那属于成本控制，不属于故障恢复。

**不给旁路请求恢复能力。** memory selector/extractor、压缩摘要器、subagent 都还是裸模型。这不是遗漏，是因为它们需要各自的会话所有权契约：谁的 deadline？谁的 fallback 状态？共享主 Agent 的 `RecoveryState` 会让状态互相污染。

**不做跨 turn 的熔断。** `consecutive529` 每次 `beginTurn()` 都清零。也就是说，如果连续三个 turn 各遇到两次 529，每个 turn 都会从主模型重新开始，不会记住「这个模型最近一直不行」。跨 turn 的健康度追踪需要一个 turn 之上的生命周期。

**不做多 fallback 链。** 只有一个 `fallbackModel`。fallback 自己也过载时，就是重试到耗尽为止，不会再往下切。

---

## 与 Claude Code 的差异

Claude Code 的恢复系统比教学版 P11 覆盖更广的生产场景。CC 的查询引擎定义了 17+ 个 reason/transition，每轮 LLM 调用后根据 stop_reason 和错误类型路由到专门恢复路径；P11 只实现长度升级→续写→耗尽、prompt_too_long→响应式压缩→耗尽、429/529→退避→fallback→耗尽三条路径，其余错误原样失败。上述 CC 实现细节来自 `learn-claude-code/s11_error_recovery/README.md`。

**退避公式。** CC 使用 `min(500 × 2^(attempt-1), 32000) + random(0~25%)`（毫秒），attempt 表示第几次重试。P11 应用 `baseDelaySeconds × 2^attempt`（默认 0.5s），封顶 `maxDelaySeconds`（默认 32s），再叠加可配置的 `jitterRatio`（默认 0.25）。CC 从 attempt=1 开始（attempt-1 让首次等待 500–625ms），P11 从 attempt=0 开始（`2^0 = 1`），同样得到 0.5s + 抖动。两条曲线实质相同，参数都可配置。

**Fallback 切换。** CC 在切换到备用模型时清空当前轮所有 pending messages，并显示 "Switched to {model} due to high demand"。P11 只将 `currentModel` 从 primary 改为 fallback，messages 和 histories 原封不动。CC 的清空策略能避免不同模型 prompt 格式不兼容导致的 tokenization 错误；P11 保守地保留上下文，假设两模型 prompt 结构兼容。

**续写收益递减。** CC 的 `tokenBudget.ts` 追踪每次 continuation 的 token 增量，连续 3 次每次不足 500 token 时主动停止续写，避免无限追加无实质进展的片段。P11 只检查固定的 `maxContinuations` 计数（默认 3 次），不评估每次 output 的实际质量变化。

**流式恢复。** CC 在流式模式下会将 429/529 等错误缓冲，不中断已有的流式输出，等待流结束再决策重试；用户对后端恢复几乎无感知。P11 当前的 `ModelClient` 和 `RecoveryManager` 都走 request/response 模式，一次 LLM 调用要么返回要么抛错，不支持流式恢复路径。

---

## 配套代码索引

想按文件读代码，从这张表进：

| 文件 | 行数 | 你能在里面找到什么 |
| --- | --- | --- |
| [features/recovery.ts](code/chapters/ch11/src/features/recovery.ts) | 603 | 本章主体。`RecoveryConfig`、`RecoveryState`、`CancellationToken`、五个 typed 失败、`RecoveryManager.complete()` 的三条恢复路径、`#runBounded`、`#parseRetryAfter`、`mergeFragments`、`splitLeadingSystem` |
| [core/model.ts](code/chapters/ch11/src/core/model.ts) | 158 | 稳定契约层。`FinishReason` 白名单、`ModelAPIError` 及三个子类的 status 不变量、`ModelRequest`／`ModelReply`／`TokenUsage`／`ModelClient` |
| [adapters/openai-chat.ts](code/chapters/ch11/src/adapters/openai-chat.ts) | 344 | 唯一 import OpenAI SDK 的文件。`maxRetries: 0`、`mapApiStatusError`、`structuredErrorIdentifier`、`retryAfterValue`、`normalizeResponse`、`toOpenAIMessage`、`toOpenAITool` |
| [core/loop.ts](code/chapters/ch11/src/core/loop.ts) | 444 | `ModelRequestExecutor` 协议定义、`beginTurn()` 调用点、执行器与裸模型的分派、`length` guard、`content_filter` 显式失败 |
| [bootstrap.ts](code/chapters/ch11/src/bootstrap.ts) | 263 | 组合根。能力位与配置的双向成对检查、`recovery requires compaction`、`RecoveryManager` 构造、摘要器绑定裸模型 |
| [core/profiles.ts](code/chapters/ch11/src/core/profiles.ts) | 261 | `P11 = P10 + "recovery"` 的能力位定义 |
| [config.ts](code/chapters/ch11/src/config.ts) | 75 | `settingsFromMapping(mapping, requireFallback)`、四字段校验、`http`/`https` 协议检查 |
| [features/compaction.ts](code/chapters/ch11/src/features/compaction.ts) | 919 | 第 8 章的压缩器，本章通过 `compactOnPromptTooLong()` 复用 |

测试按路径分文件，想看某条路径的全部边界条件，直接读对应测试：

| 测试文件 | 覆盖什么 |
| --- | --- |
| `chapters/ch11/tests/recovery.test.ts` | 三条恢复路径、退避、fallback、deadline、取消、快照隔离（666 行，全仓最大的测试文件） |
| `chapters/ch11/tests/ch11-recovery.test.ts` | 从 `buildAgent(P11, ...)` 端到端观察恢复行为 |
| `chapters/ch11/tests/openai-chat.test.ts` | 错误归一化、200 响应逐字段校验、wire format 映射 |
| `chapters/ch11/tests/config.test.ts` | 四字段校验、`requireFallback` 开关 |
| `chapters/ch11/tests/ch11-entry.test.ts` | 固定入口缺配置时的 stderr 输出 |

---

## 运行与验证

两种入口共用同一个 P11 Bootstrap：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch11 -- --prompt "检查当前工作区并给出结论"
npm run agent-tutorial -- run --chapter 11 --prompt "检查当前工作区并给出结论"
```

离线测试使用确定性模型、FakeClock、RecordingSleeper 和模拟 OpenAI SDK 状态对象，不需要 API Key，也不会访问网络：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch11
npm exec vitest run chapters/ch11/tests/openai-chat.test.ts chapters/ch11/tests/config.test.ts
npm run typecheck
npm run lint
npm run format:check
```

`npm run test:ch11` 的实测结果：

```text
Test Files  28 passed (28)
     Tests  258 passed (258)
```

当前实现有意不重试未知 5xx、连接重置、DNS、TLS、认证和流式中断。没有明确 typed signal、幂等语义和测试前，这些错误原样失败比「看起来更坚强」的广泛重试更安全。

---

## 本章小结

### 三句话版本

API 韧性不是把每个异常都吞掉，而是让每条恢复路径有可验证的输入、状态和终止条件。三种故障各走各的路：输出截断在请求窗口内升级预算并续写，输入过长只压缩请求快照一次，429/529 按结构化信号退避重试并在连续过载时切 fallback。恢复过程对 canonical history 完全不可见——正式历史只接收最终那条合并后的完整回复。

### 一定要记住的五条

**一、恢复只改请求快照，不改正式历史。** 这是本章一半设计的来源。丢弃的截断片段、压缩掉的中间消息、失败的重试，都只发生在 `complete()` 内部的局部变量上。外层看到的永远是「一次模型调用，一条最终回复」。

**二、只认结构化字段，不匹配异常文本。** `instanceof APIError` 加 `status` 加 `error.code`，就这三样。文案会变，`status` 不会。一个字符串里带 429 的普通 `Error` 不该被当成限流——鸭子类型在这里是漏洞，不是灵活性。

**三、HTTP 200 也是不可信边界。** `finish_reason` 出现未知新值时明确失败，而不是宽容地当成 `stop`。宽容处理会让一个截断回复以「完整答案」的身份进入历史，而且没有任何人会知道。

**四、耗尽必须是 typed 失败，不是静默降级。** 五个 `RecoveryError` 子类各自对应一种耗尽原因。看到 `RecoveryDeadlineExceeded` 就知道是时限，看到 `RecoveryRetriesExhausted` 就知道是次数，看到 `PromptTooLongRetryError` 就知道压缩过一次还是不够。含糊的失败让线上问题不可诊断。

**五、能力位与配置必须成对。** 给 P10 传恢复配置报错，P11 不传恢复配置也报错。后者更重要——如果允许，P11 会静默退化成裸模型，你以为有恢复能力其实没有，直到凌晨两点才发现。

### 检查你是否真的读懂了

答不上来就回去看对应章节：

1. 一次续写恢复之后，canonical history 里有几条消息？外层 `turns` 是几？（「接入唯一 Loop」）
2. 为什么第一次遇到 `finishReason === "length"` 时要把那段回复**整段丢弃**，而不是留着接续写？（「路径一」）
3. `Retry-After: 2026-08-28T12:00:30.000Z` 会被接受吗？为什么？（「路径三」的解析表）
4. 抖动为什么加在 32 秒封顶**之后**，而不是之前？（「路径三」）
5. 连续两次 529、然后一次 429、再一次 529，会切 fallback 吗？（实验三第三段）
6. 一个字符串里写着 `Error code: 429` 的普通 `Error`，会被归一成 `ModelRateLimitError` 吗？（「先归一化错误」）
7. 为什么 `ModelHistorySummarizer` 必须绑定裸模型而不是 `RecoveryManager`？（「摘要器必须绑定裸模型」）
8. `content_filter` 为什么在 Loop 层拒绝，而不是在恢复层？（「`content_filter` 在 Loop 层显式失败」）
9. 10 次瞬态尝试会产生几次 sleep？为什么默认时限是 300 秒？（实验三第一段）
10. 同一份三字段 `.env`，为什么 P10 能跑、P11 不能？（实验七）

---

下一篇进入任务系统。Agent 已经能从 API 故障中恢复，但项目任务仍需要跨进程持久化、依赖解锁和原子认领。第 12 章会把会话 TODO 与真正的 JSON Task DAG 分开建模——前者是一次对话内的待办清单，后者是能被多个进程并发认领的持久化依赖图。








