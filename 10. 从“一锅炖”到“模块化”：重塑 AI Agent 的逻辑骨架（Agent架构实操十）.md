
第 9 章做完之后，Agent 已经有了相当完整的能力栈：记忆系统、上下文压缩、技能懒加载、权限管道、子 Agent 调度。每加一个能力，system prompt 就多一段。到第 9 章结束时，那个字符串大概长这样：

```typescript
const systemPrompt = [
  "You are a coding agent.",
  "Use tools when needed and inspect their results.",
  "For complex tasks, keep the TODO snapshot current.",
  // 工具、Skill、workspace 和记忆不在这里硬编码。
].join(" ");
```

没人敢改它。不知道哪段是必须的，哪段是某次调试留下的，改一处会不会影响其他地方。项目切换时要整个重写，但又不确定哪些该留、哪些该扔。

这不是 prompt 写得好不好的问题，是结构问题。一大段硬编码字符串本质上是把配置藏在代码里，和把数据库连接字符串写死在业务逻辑里是同一类错误。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaHHeHpRlBuAT1pPoQZBztdcFmicsZuFkuEhAn6ibm1BmChZ2DPYQhicl37o0eZibicdhp5zJe2RQsOkEhqu7fVLLdjaU3SwXBaGLvKM/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「硬编码 system prompt」具体会带来哪三个可复现的工程问题，而不是笼统地说"不优雅"。
2. 复述一次系统提示从「运行态对象」到「发给模型的字符串」经过的三个角色，并说明每个角色为什么不能省。
3. 背出五个 section 的固定顺序，并解释为什么"顺序"本身就是一份契约。
4. 判断一个 context 值会被接受还是被拒绝，并说出被拒绝时抛的是哪个错误。
5. 解释为什么缓存必须挂在实例上而不是模块全局，以及缓存键少覆盖一个输入会导致什么后果。
6. 说明为什么本章的缓存**不等于**供应商 API 的 prompt cache，不要把两件事混着宣称。
7. 分清哪些运行态变化会在下一轮生效、哪些必须重建 Agent 才生效。
8. 说清第 9 章的记忆注入路径为什么必须在第 10 章关掉，不关会看到什么现象。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 每轮如何组装模型请求 | 第 1 章 | 系统提示在每轮组装请求前渲染 |
| `ToolRegistry` 与工具快照 | 第 2 章 | `tools` section 直接读注册表当前名称 |
| workspace 绝对路径与硬边界 | 第 2 章 | `workspace` section 输出规范化后的绝对路径 |
| Profile 能力位与组合根 | 第 5～9 章 | P10 靠一个 `dynamic_prompt` 能力位切换渲染路径 |
| Skill 目录与正文分离 | 第 7 章 | `skills` section 只放目录，正文仍靠 `load_skill` |
| `MemorySession.selected` 与 `TurnLifecycle` | 第 9 章 | **本章最关键的前置**，记忆注入要从旧路径搬到新路径 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| 动态系统提示 | 每轮模型请求前按当前运行态重新渲染出来的 system prompt |
| section | 系统提示里一个带 `## 标题` 的独立段落，本章固定五个 |
| `DynamicPromptRenderer` | 纯渲染器：拿到运行态对象，按固定顺序拼出字符串 |
| `DynamicPromptProvider` | 适配器：把渲染器和运行态对象绑在一起，对外只暴露零参数 `render()` |
| `SystemPromptProvider` | Loop 侧的接口，只有一个零参数 `render(): string` |
| 运行态取值 | 渲染时才去读数据源当前值，不在构建期把值拷成字符串 |
| 规范 JSON（canonical JSON） | 按键排序、紧凑输出的唯一序列化形式，语义相同必然文本相同 |
| 缓存键 | 覆盖全部模型可见输入的规范 JSON 字符串，用来判断能否复用上次结果 |
| 会话隔离 | 缓存挂在 renderer 实例上，两个 Agent 实例互不影响 |
| API prompt cache | 供应商侧按请求前缀命中的缓存，和本章的进程内缓存**不是一回事** |
| 有界目录 | 只含名称和一行描述的轻量索引，不含正文 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一眼渲染结果长什么样     ← 三个阶段，看 section 怎么长出来
  ↓
② 三个真实的痛点               ← 为什么必须拆，不是审美问题
  ↓
③ 三个角色，一条渲染链         ← 本章的架构骨架：Renderer / Provider / Loop
  ↓
④ 五个 section 与固定顺序      ← 顺序是契约，空值也有明确形状
  ↓
⑤ Context 必须是严格 JSON      ← 为什么不能用 default=str 糊过去
  ↓
⑥ 缓存必须属于会话             ← 挂错位置会串会话；缓存键少一项会发过期指令
  ↓
⑦ 接入唯一 Loop                ← 零参数契约，以及失败为什么不回退
  ↓
⑧ 记忆只注入一次               ← 第 9 章的旧路径必须关掉
  ↓
⑨ 运行 + 六个实验 + 排错       ← 动手验证本章所有结论
```

---

## 先看一眼渲染结果长什么样

抽象讲"模块化"不如直接看输出。下面三段是同一个 renderer 在三个阶段渲染出来的真实结果，用一个临时 workspace 跑出来的（完整脚本在后面「实验一」）。

**阶段 1：空注册表，没有 Skill，没有记忆。**

```text
## identity
agent
context: {"chapter":10}

## tools
(none)

## workspace
C:\Users\你的用户名\AppData\Local\Temp\probe1-OTdZiy
```

三个 section。注意 `tools` 下面那个 `(none)`——没有工具时不是省略这个 section，而是明确写出"当前没有工具"。后面会讲为什么。

**阶段 2：注册两个工具，其他不变。**

```text
## identity
agent
context: {"chapter":10}

## tools
- read_file
- todo_write

## workspace
C:\Users\你的用户名\AppData\Local\Temp\probe1-OTdZiy
```

只有 `tools` section 变了。这里没有任何人手工去改字符串——`ToolRegistry` 里多了两个工具，渲染时读到的就是新列表。

**阶段 3：加上一个 Skill 和一条被选中的记忆。**

```text
## identity
agent
context: {"chapter":10}

## tools
- read_file
- todo_write

## workspace
C:\Users\你的用户名\AppData\Local\Temp\probe1-OTdZiy

## skills
- **sql-style**: SQL 编写规范

## memory
<relevant_memories>

## database (project)

生产数据库约束

始终使用真实数据库。

</relevant_memories>
```

五个 section 全部出现。两件事值得先记住：

- `skills` section 里只有 `sql-style` 的**名称和一行描述**，Skill 文件里的正文一个字都没进来。正文仍然要靠 `load_skill` 显式加载。
- `memory` section 里只有被选中的那条记忆。同一个 store 里还有一条 `keyboard` 记忆没被选中，它的正文也一个字都没进来。

这两条在实验一里有断言：

```text
含 Skill 正文吗 = false
含未选中记忆正文吗 = false
```

### 真实运行时它长什么样

上面用的是最小输入。跑真实的 P10 Agent 时，`identity` section 是这样的（实验六能跑出来）：

```text
## identity
You are a coding agent. Use tools when needed, inspect their results, and answer accurately.
For complex tasks, call todo_write with the complete task snapshot and update it when the plan changes.
context: {"chapter":10,"identity":"demo-user"}
```

`tools` section 是完整的 8 个工具：

```text
## tools
- shell
- read_file
- write_file
- edit_file
- glob
- todo_write
- task
- load_skill
```

这里有个容易看漏的点：**`identity` 参数收到的不是"你是谁"这一句，而是组合根拼好的整段基线指令**。`bootstrap.ts` 把 `BASE_SYSTEM_PROMPT` 加上 TODO 指令一起当作 `identity` 传进去，所以 identity section 有三行——两行行为基线，一行 context。这是当前实现的真实形状，不是笔误。

---

## 三个真实的痛点

在拆解解法之前，先把问题说清楚——不是泛泛的"不够优雅"，是三个具体会出问题的地方。

### 痛点一：换项目要重写整个 prompt，但不知道哪些该改

当前这个 Agent 是 coding agent，prompt 里有工作目录、工具列表、行为规范。换一个项目，工作目录变了，工具可能变了，但身份定义和行为规范也许还能复用。

硬编码成一段字符串之后，这些边界完全不可见。你面前只有一坨文本，唯一能做的是全部重写，或者小心翼翼地做字符串替换——而字符串替换在 prompt 上格外危险，因为改错了不会报错，只会让模型行为变得微妙地不对。

拆成 section 之后，边界变成可见的：`identity` 可复用，`workspace` 必须换，`tools` 自己会跟着注册表变。

### 痛点二：改一处可能悄悄影响全局

加一段工具描述，可能和前面某条行为规范在语义上冲突。LLM 会怎么理解这个冲突，完全不可预测。

这类 bug 最难缠的地方在于：**它不报错。** 类型检查过，测试过（如果测试没断言 prompt 内容），CI 全绿，只是 Agent 偶尔做出奇怪的选择。排查时你甚至不会怀疑到 prompt，因为"prompt 又没改过"——但它上周确实被加了一段。

按 section 拆开、并且用测试断言 section 顺序和内容之后，这类改动至少变成可见的：加的东西落在哪个 section、有没有挤到别人前面，都是能断言的。

### 痛点三：每次请求带全部内容，但并不是每次都需要

记忆内容只在本轮真的选中了相关记录时才有意义。如果这一轮什么都没选中，还留一段"当前没有相关记忆"的占位说明，就是纯噪音。

System prompt 每轮都会发送。无关指令不只是浪费 token，更重要的是**稀释注意力**——这是第 5 章讲过的问题，上下文里无关内容越多，关键指令的相对权重越低。

所以本章的规则是：`skills` 和 `memory` 两个 section 在没内容时**整段省略**，不留占位。

### 三个痛点对应三个设计决定

| 痛点 | 本章的对策 | 在哪一节展开 |
| --- | --- | --- |
| 边界不可见 | 拆成五个具名 section，各有唯一数据源 | 五个 section 与固定顺序 |
| 改一处影响全局 | 顺序固定成契约，用测试断言 | 同上 |
| 无关内容占位 | 可选 section 无内容时整段省略 | 同上 |
| 顺带解决：值会过期 | 运行态取值，每轮重新读 | 三个角色，一条渲染链 |

---

## 三个角色，一条渲染链

这一节是本章的架构骨架，值得慢一点看。

系统提示从"运行态对象"变成"发给模型的字符串"，中间经过三个角色。它们的职责严格不重叠：

```text
运行态数据源                     三个角色                        模型
─────────────                 ──────────                     ────

ToolRegistry ─┐
SkillRegistry ─┤
MemorySession ─┼──> DynamicPromptProvider ──> DynamicPromptRenderer ──> system 消息
workspace 路径 ─┤    （绑定 + 零参数适配）      （校验 + 规范化 + 拼装）        ↑
identity/context ┘              ↑                                        │
                                │                                        │
                          AgentRunner 每轮请求前调用 render() ─────────────┘
```

| 角色 | 唯一职责 | 明确不做的事 |
| --- | --- | --- |
| `DynamicPromptRenderer` | 校验输入、规范化 context、按固定顺序拼字符串、维护缓存 | 不发现依赖，不读磁盘，不选记忆 |
| `DynamicPromptProvider` | 在构建期绑定 renderer 和运行态对象，对外暴露零参数 `render()` | 不改渲染逻辑，不加自己的缓存 |
| `AgentRunner` | 每轮模型请求前调一次 `render()`，校验返回值 | 不知道有几个 section，不知道数据从哪来 |

### 为什么要有 Provider 这一层

第一次看这个设计，很自然会问：既然 renderer 已经能渲染了，为什么不让 `AgentRunner` 直接调 `renderer.render({...})`？

因为那样 Loop 就必须知道每一个数据源。它得持有 `ToolRegistry`、`SkillRegistry`、`MemorySession`、workspace、context……然后每加一个 section，Loop 的构造参数就要多一个。这正是本章想消除的耦合，只是从字符串搬到了参数列表。

Provider 的作用就是把这堆绑定关系收在**组合根**里。对 Loop 来说，系统提示只是一个接口：

```typescript
export interface SystemPromptProvider {
  // 每轮模型调用临时读取动态系统提示，不把渲染结果写入对话历史。
  render(): string;
}
```

一个方法，零参数。Loop 不知道背后有五个 section，也不知道记忆是什么。

Provider 的实现只做转发，不做决策：

```typescript
render(): string {
  return this.#renderer.render({
    identity: this.#identity,
    tools: this.#tools,
    workspace: this.#workspace,
    context: this.#context,
    ...(this.#skills === undefined ? {} : { skills: this.#skills }),
    ...(this.#memory === undefined ? {} : { memory: this.#memory }),
  });
}
```

注意最后两行的展开写法。`skills` 和 `memory` 是可选能力，**不存在时连这个键都不传**，而不是传 `undefined`。这样 renderer 侧的"没有这个能力"和"这个能力当前是空的"是两种不同状态，不会混在一起。

### 组合根怎么接线

`bootstrap.ts` 里 Provider 的构建是这样的：

```typescript
const systemPromptProvider = new DynamicPromptProvider({
  renderer: new DynamicPromptRenderer(),
  identity: systemPrompt,
  tools,
  workspace: dependencies.workspace,
  context: Object.freeze({ chapter: profile.chapter, identity }),
  ...(skillRegistry === undefined ? {} : { skills: skillRegistry }),
  ...(memorySession === undefined ? {} : { memory: memorySession }),
});
```

三个细节：

第一，`renderer` 是**在这里 new 出来的**。每次 `buildAgent()` 都得到一个新 renderer，这是后面「缓存必须属于会话」能成立的前提。

第二，传进去的 `tools`、`skillRegistry`、`memorySession` 都是**对象引用**，不是它们当前的字符串形态。所以后续这些对象内部状态变了，renderer 下次读到的就是新值。

第三，整个 Provider 只在 `dynamic_prompt` 能力位打开时才创建：

```typescript
const dynamicPrompt = profile.capabilities.has("dynamic_prompt");
```

P09 及之前没这个能力位，`systemPromptProvider` 是 `undefined`，Loop 退回用构建期的固定字符串。旧章节行为一行都没改。

---

## 五个 section 与固定顺序

当前实现没有维护一份 `PROMPT_SECTIONS` 字符串字典，也没有手写 `enabledTools` 列表。`DynamicPromptRenderer` 直接读取已经参与运行的对象，输出五个 section：

| Section | 策略 | 数据来源 | 没内容时 |
| --- | --- | --- | --- |
| `identity` | 始终 | 基础行为指令 + 规范化 JSON context | 不可能为空（空 identity 直接抛错） |
| `tools` | 始终 | `ToolRegistry.names` 当前快照 | 输出 `(none)` |
| `workspace` | 始终 | 已解析的 workspace 绝对路径 | 不可能为空（空字符串直接抛错） |
| `skills` | 按需 | `SkillRegistry.renderCatalog()`，不含正文 | **整段省略** |
| `memory` | 按需 | `MemorySession.renderSelected()`，本轮选中的记忆 | **整段省略** |

顺序是契约：`identity -> tools -> workspace -> skills -> memory`。

### 为什么"顺序"要当成契约

一个常见的实现是把 section 放进对象，再遍历键输出。那样顺序就依赖对象键的插入顺序——一个能被重构悄悄改掉的东西。

本章用数组，顺序写死在代码里：

```typescript
const sections = [
  // 固定段落顺序让提示词可预测，后续可选段落只追加不插队。
  `## identity\n${identity}\ncontext: ${contextJson}`,
  `## tools\n${toolCatalog}`,
  `## workspace\n${workspace}`,
];
if (skillCatalog.length > 0) {
  sections.push(`## skills\n${skillCatalog}`);
}
if (memoryBody.length > 0) {
  sections.push(`## memory\n${memoryBody}`);
}
// 固定 section 顺序是契约；可选 section 只追加到尾部，不改变前面内容的相对位置。
const prompt = sections.join("\n\n");
```

两条规则从这段代码直接读得出来：

1. **前三个 section 无条件存在**，位置永远是 0、1、2。
2. **可选 section 只能 push 到尾部**，永远不会插到前面三个中间。

测试断言的不是"包含某段文本"，而是相对位置：

```typescript
expect(prompt?.indexOf("## identity")).toBeLessThan(prompt?.indexOf("## tools") ?? -1);
expect(prompt?.indexOf("## tools")).toBeLessThan(prompt?.indexOf("## workspace") ?? -1);
expect(prompt?.indexOf("## workspace")).toBeLessThan(prompt?.indexOf("## skills") ?? -1);
expect(prompt?.indexOf("## skills")).toBeLessThan(prompt?.indexOf("## memory") ?? -1);
```

这样任何人把 `push` 改成 `unshift`，或者把某个 section 挪个位置，测试立刻红。

### 为什么没有工具时要写 `(none)`，而 Skill 为空就省略

这两个决定看起来矛盾，其实针对的是两种不同情况。

```typescript
// 工具列表为空也输出 "(none)"，让模型明确知道当前没有可用工具。
const toolCatalog = tools.length === 0 ? "(none)" : tools.map((name) => `- ${name}`).join("\n");
```

**工具是核心能力，"没有工具"是一个需要模型知道的事实。** 如果整段省略，模型看到的是"这里本该有工具列表但什么都没说"，它可能仍然尝试调用工具，然后拿到一堆错误。明确写 `(none)`，模型就知道这一轮只能靠纯文本回答。

**Skill 和记忆是补充信息，"没有"就等于"这件事不存在"。** 写一段"当前没有可用 Skill"没有任何指导价值，只是占位噪音——正好是痛点三要消除的东西。

判断标准可以这样记：**这个 section 为空时，模型的行为应该改变吗？** 工具为空，行为要变（别调工具）；Skill 为空，行为不变（本来就没这个选项）。

### section 之间是空行分隔

`sections.join("\n\n")` 意味着 section 之间隔一个空行。最小情况下渲染结果的原始字符是这样的（`JSON.stringify` 打出来的，能看清 `\n` 的确切位置）：

```text
"## identity\nagent\ncontext: {\"chapter\":10}\n\n## tools\n(none)\n\n## workspace\nC:\\Users\\你的用户名\\AppData\\Local\\Temp\\probe1b-cLL8zY"
```

拆开看是 9 行：3 行 identity + 空行 + 2 行 tools + 空行 + 2 行 workspace。这个数字在实验一里可以直接验证。

---

## Context 必须是严格 JSON

`context` 用来放章节号、identity、运行模式这类小型结构化状态。它的特殊之处在于：**同一份 context 既要渲染成给模型看的文本，又要参与缓存键计算。**

这就是为什么不能用 `JSON.stringify(context, null, 0)` 配 `default=str` 之类的手法把未知对象糊成字符串。一旦允许"糊过去"，两个语义不同的 context 可能序列化出同一个字符串，缓存就会错误命中；或者同一个 context 两次序列化出不同字符串，缓存永远不命中。

### 允许的类型

```typescript
type JsonScalar = boolean | number | string | null;
interface JsonObject {
  // 仅允许字符串键；值递归受 JsonValue 约束，保证可稳定序列化。
  readonly [key: string]: JsonValue;
}
type JsonValue = JsonScalar | readonly JsonValue[] | JsonObject;
```

类型只是第一道关。运行时还有一道递归校验，因为 TypeScript 的类型在边界上是可以被绕过的（`as never`、`unknown` 强转、外部 JSON 输入）。

### 完整的接受 / 拒绝清单

这张表是实验二实际跑出来的结果，不是推测：

| 输入 | 结果 | 渲染出的 context / 错误信息 |
| --- | --- | --- |
| `{}` | 通过 | `context: {}` |
| `{ n: 1, s: "文本", b: true, nil: null }` | 通过 | `context: {"b":true,"n":1,"nil":null,"s":"文本"}` |
| `{ a: { y: 2, x: 1 }, list: [1, "二", false, null] }` | 通过 | `context: {"a":{"x":1,"y":2},"list":[1,"二",false,null]}` |
| `{ z: 1, a: 2 }` | 通过 | `context: {"a":2,"z":1}` |
| `JSON.parse('{"__proto__":"safe"}')` | 通过 | `context: {"__proto__":"safe"}` |
| `["不能是数组"]` | 拒绝 | `PromptContextError: context must be a JSON object` |
| `"不能是字符串"` | 拒绝 | 同上 |
| `{ when: new Date() }` | 拒绝 | `context contains an unsupported JSON value` |
| `{ m: new Map() }` | 拒绝 | 同上 |
| `{ fn: () => 1 }` | 拒绝 | 同上 |
| `{ maybe: undefined }` | 拒绝 | 同上 |
| `{ n: Number.NaN }` | 拒绝 | `context contains a non-finite JSON number` |
| `{ n: Number.POSITIVE_INFINITY }` | 拒绝 | 同上 |
| `{ [Symbol("secret")]: "hidden" }` | 拒绝 | `context JSON object keys must be strings` |
| 自引用对象 | 拒绝 | `context contains a cyclic JSON object` |
| 自引用数组 | 拒绝 | `context contains a cyclic JSON array` |

有几行值得单独说。

**键顺序不影响结果。** `{ z: 1, a: 2 }` 渲染出 `{"a":2,"z":1}`——按键排序。嵌套对象也一样，`{ y: 2, x: 1 }` 变成 `{"x":1,"y":2}`。所以两份"写的顺序不同、内容相同"的 context 会得到同一个缓存键。

**`undefined` 会被拒绝，不是被忽略。** 这一点和 `JSON.stringify` 的行为不同——后者会静默丢掉值为 `undefined` 的键。这里明确拒绝，因为"我传了一个键但它消失了"是个应该报错的情况。

**`__proto__` 是普通字符串键。** `JSON.parse` 产生的 `__proto__` 是一个真正的自有属性（不是原型设置器），所以它被当作普通键保留，并且参与缓存键。测试专门钉住了这条：

```typescript
expect(first).toContain('context: {"__proto__":"first"}');
expect(second).toContain('context: {"__proto__":"second"}');
expect(second).not.toBe(first);
```

如果这里用了 `Object.assign` 之类会触发原型设置的写法，两次渲染就会得到同一个结果——一个静默的缓存错误命中。

### 实现上的两个关键点

`normalizeContext` 做递归克隆并冻结，递归路径上维护一个 `active` Set 来发现循环引用：

```typescript
if (Array.isArray(value)) {
  // active 集合保存当前递归路径上的对象，用于检测循环引用。
  if (active.has(value)) {
    throw new PromptContextError("context contains a cyclic JSON array");
  }
  active.add(value);
  try {
    return Object.freeze(value.map((item) => normalizeJsonValue(item, active)));
  } finally {
    active.delete(value);
  }
}
```

注意 `finally` 里的 `active.delete(value)`。如果不删，同一个对象在**兄弟位置**出现两次（合法的 DAG，不是循环）也会被误判成循环。删掉之后，只有真正在**当前递归路径上**的对象才算循环。

`isPlainObject` 的判断是严格的原型相等，不是 `typeof === "object"`：

```typescript
function isPlainObject(value: unknown): value is Record<string, unknown> {
  return (
    typeof value === "object" &&
    value !== null &&
    !Array.isArray(value) &&
    Object.getPrototypeOf(value) === Object.prototype
  );
}
```

所以 `Date`、`Map`、`Set`、任何类实例都进不来。这是有意的：这些对象的序列化形式要么依赖实现细节（`Date` 受时区影响），要么根本没有标准 JSON 形式（`Map`）。

### identity 也有校验

顺带说一下 `identity`，它的规则更简单但同样严格：

```typescript
function normalizeIdentity(identity: string): string {
  // identity 是 Prompt 可见输入，空字符串会在进入缓存前失败。
  if (typeof identity !== "string") {
    throw new TypeError("identity must be a string");
  }
  const normalized = identity.trim();
  if (normalized.length === 0) {
    throw new Error("identity must not be empty");
  }
  return normalized;
}
```

实测行为：

| identity 输入 | 结果 |
| --- | --- |
| `"  前后空格  "` | 通过，渲染成 `前后空格`（trim 掉了） |
| `""` | 拒绝：`identity must not be empty` |
| `"   "` | 拒绝：同上 |

trim 是为了让"多打了个空格"不会产生一个新的缓存键。

---

## 缓存必须属于会话

### 挂在模块全局会串会话

一个很容易写出来的版本是这样：

```typescript
// 反面例子：不要这样写
let lastContextKey: string | undefined;
let lastPrompt: string | undefined;
```

单个 Agent 跑起来毫无问题。但同一个进程里跑两个 Agent 实例时，它们共享这两个变量——第一个 Agent 渲染完，第二个 Agent 输入不同却可能读到第一个的结果。表现出来就是"B 会话的 Agent 拿到了 A 会话的 workspace 路径"，而且极难复现，因为它依赖两个会话的渲染时序。

本章把缓存放进实例字段：

```typescript
export class DynamicPromptRenderer {
  // 上一次完整输入的稳定 JSON 键；不同 section 数据不会误命中缓存。
  #lastKey: string | undefined;
  // 与 #lastKey 对应的最终 prompt 文本。
  #lastPrompt: string | undefined;
  // 仅用于观测缓存复用次数，不影响渲染结果。
  #cacheHits = 0;
```

每次 `buildAgent()` 都 `new DynamicPromptRenderer()`，所以缓存天然按会话隔离。实验三里能直接看到：换一个 renderer 实例，`cacheHits` 从 0 开始，即使输入完全一样。

### 缓存键必须覆盖全部可见输入

这是本节最要紧的一条。缓存键少覆盖一个输入，后果不是"缓存效率低"，而是**给模型发过期指令**。

```typescript
// 缓存键覆盖所有模型可见输入，任一运行态变化都会失效。
const key = stableJson({
  context,
  identity,
  memory: memoryBody,
  skills: skillCatalog,
  tools: [...tools],
  workspace,
});
```

六个输入，正好对应五个 section 里所有会变的东西：

| 缓存键里的项 | 对应 section | 少了它会怎样 |
| --- | --- | --- |
| `identity` | identity | 换了行为基线仍发旧指令 |
| `context` | identity | 章节号或运行模式变了没反映 |
| `tools` | tools | **新注册的工具模型不知道，或已移除的工具模型还在调** |
| `workspace` | workspace | 换了工作目录仍报旧路径 |
| `skills` | skills | 新 Skill 不出现在目录里，模型不知道能加载 |
| `memory` | memory | 本轮选中的记忆是上一轮的 |

注意 `tools: [...tools]` 这个展开。`tools` 是 `readonly string[]`，展开成新数组是为了让 `stableJson` 能正常序列化它——而且这里用的是**名称列表**，不是注册表对象。工具的描述或 schema 变化不会触发失效，因为那些内容不进 system prompt（它们走 `tools` 参数，是另一条边界）。

### 缓存的实际行为

实验三跑出来的完整序列：

```text
第 1 次渲染后 cacheHits = 0
第 2 次（完全相同）      cacheHits = 1 | 同一字符串 = true
第 3 次（键顺序颠倒）    cacheHits = 2 | 同一字符串 = true
第 4 次（新增工具后）    cacheHits = 2 | 同一字符串 = false
另一个 renderer          cacheHits = 0 | 文本与上一次相同 = true
非法 context 被拒绝 = context contains an unsupported JSON value
失败后再渲染             cacheHits = 3 | 仍复用失败前结果 = true
```

逐行读一遍：

- 第 1 次必然 miss，没有可比的东西。
- 第 2 次完全相同，命中。
- 第 3 次把 context 的键顺序颠倒，**仍然命中**——这就是规范 JSON 的作用。
- 第 4 次注册了一个新工具，`cacheHits` 停在 2（没涨），返回的也是不同字符串。失效生效了。
- 换一个 renderer 实例，`cacheHits` 从 0 开始，但渲染出的**文本相同**。这两件事同时成立才说明设计对：缓存隔离，结果一致。
- 最后一段最有意思，下面单独说。

### 失败不污染缓存

看这三行：

```text
非法 context 被拒绝 = context contains an unsupported JSON value
失败后再渲染             cacheHits = 3 | 仍复用失败前结果 = true
```

一次非法渲染抛错之后，再用之前那份合法输入渲染，**仍然命中缓存**，拿到的还是失败前那个结果。

这是因为所有校验都在更新 `#lastKey` / `#lastPrompt` **之前**完成：

```typescript
const context = normalizeContext(options.context);   // 非法在这里就抛了
const contextJson = stableJson(context);
// ...
const key = stableJson({ /* ... */ });
if (key === this.#lastKey && this.#lastPrompt !== undefined) {
  this.#cacheHits += 1;
  return this.#lastPrompt;
}
// ...拼装...
this.#lastKey = key;      // 只有全部成功才走到这里
this.#lastPrompt = prompt;
```

如果顺序反了——先写缓存再校验——一次失败调用就会把缓存置成半成品状态，之后所有渲染都拿到脏数据。测试专门覆盖了这条，四种非法 context 各跑一遍：

```typescript
test.each([
  ["root array", ["root must be an object"]],
  ["unsupported object", { value: new Date() }],
  ["non-finite number", { value: Number.NaN }],
  ["symbol key", { [Symbol("secret")]: "hidden" }],
])("rejects %s context without poisoning the cache", (_label, context) => {
```

### 这个缓存不是 API prompt cache

必须说清楚，否则很容易误宣称性能收益。

| 对比项 | 本章的缓存 | 供应商 API 的 prompt cache |
| --- | --- | --- |
| 在哪 | 进程内，renderer 实例字段 | 供应商服务端 |
| 省什么 | 重复拼装字符串的 CPU | 重复处理相同 token 前缀的费用和延迟 |
| 命中条件 | 六个输入的规范快照全部相等 | 断点前的完整请求前缀一致 |
| 谁能观测 | `renderer.cacheHits` | 响应里的 usage 字段 |

当前 OpenAI 适配器**没有**把静态和动态 section 分成不同的 cache-control block。所以本章不能宣称"动态内容变化时仍会命中 API 级静态前缀缓存"——那需要额外的分块设计，本章没做。

`cacheHits` 这个 getter 存在的意义是**可测试性**，不是性能指标：

```typescript
// 缓存命中次数用于测试和观测，证明相同输入不会重复组装字符串。
get cacheHits(): number {
  return this.#cacheHits;
}
```

---

## 接入唯一 Loop，而不是复制 Loop

本章新增一个能力，但 `core/loop.ts` 里的工具执行循环一行都没有复制。动态提示的接入点只有一处：每轮组装模型请求前。

```typescript
// 系统提示在每轮组装请求前渲染，使工具列表和选中记忆等运行态变化下一轮生效。
const systemPrompt = this.#renderSystemPrompt();
const requestMessages = Object.freeze([
  systemMessage(systemPrompt),
  ...requestHistory,
  ...turnGuidance,
  ...observerGuidance,
]);
```

工具调用、权限、Hook、整批结果处理、压缩、Stop 行为全部还是原来那套实现。

### 两道校验，两个时机

**构造时**校验 provider 有没有 `render()`：

```typescript
if (
  options.systemPromptProvider !== undefined &&
  typeof options.systemPromptProvider.render !== "function"
) {
  // provider 契约在构造时校验，避免运行到模型调用阶段才发现缺 render()。
  throw new TypeError("systemPromptProvider must implement render()");
}
```

**每轮渲染时**校验返回值：

```typescript
// 每轮模型请求前渲染系统提示；provider 缺失时使用构建期固定字符串。
#renderSystemPrompt(): string {
  // Provider 输出是模型边界输入，空值立即失败而不是回退到旧缓存。
  const rendered =
    this.#systemPromptProvider === undefined
      ? this.#systemPrompt
      : this.#systemPromptProvider.render();
  if (typeof rendered !== "string" || rendered.trim().length === 0) {
    throw new AgentRunError("System prompt provider returned an empty prompt");
  }
  return rendered;
}
```

实验六把所有分支跑了一遍：

| provider | 结果 |
| --- | --- |
| `{ render: () => "## identity\nagent" }` | 通过，正常完成 |
| `{ render: () => "" }` | `AgentRunError: System prompt provider returned an empty prompt` |
| `{ render: () => "   \n  " }` | 同上（只有空白也算空） |
| `{ render: () => null }` | 同上 |
| `{ render: () => 42 }` | 同上（类型不对也走这条） |
| `{ render: "不是函数" }` | `TypeError: systemPromptProvider must implement render()` |
| `{}` | 同上 |
| `{ render: () => { throw new Error("渲染器自己炸了") } }` | 原样抛出 `Error: 渲染器自己炸了` |

最后一行说明 Loop **不捕获** renderer 内部的异常。这是有意的：渲染失败意味着系统提示的某个数据源坏了，继续跑只会用错误的指令去操作真实环境。让它炸，比静默降级安全。

### 为什么失败不回退到构建期的字符串

`AgentRunner` 手里确实有一个构建期的 `#systemPrompt`（P10 里它是 `BASE_SYSTEM_PROMPT` 加 TODO 指令那段）。既然有 fallback，为什么 provider 返回空时不用它？

因为那段字符串**不包含 workspace、工具列表、Skill 目录和记忆**。用它去跑，模型会以为自己没有工具、不知道在哪个目录工作。这不是降级，是换了个 Agent。

实验六最后一段证明 provider 存在时构建期字符串确实不参与：

```text
实际发给模型的 system 消息 = "运行态渲染结果"
```

传进去的 `systemPrompt: "构建期固定 fallback"` 完全没出现。

### "下一轮生效"的准确边界

"运行态变化下一轮生效"这句话要按真实实现理解，不能笼统地当成"什么都会自动更新"。

| 变化 | 下一轮生效吗 | 为什么 |
| --- | --- | --- |
| `ToolRegistry` 注册 / 移除工具 | 生效 | 每次渲染重读 `tools.names` |
| `MemorySession` 本轮选中的记忆变了 | 生效 | 每次渲染重读 `memory.selected` |
| workspace 路径 | 不变 | Provider 绑定的是构建期传入的路径 |
| 磁盘上新增 / 删除 Skill 目录 | **不生效** | `SkillRegistry` 是构建期扫描的快照 |

最后一行是最容易误解的。实验四直接验证：

```text
=== 同一 provider：磁盘再加 gamma ===
两次渲染是否相同 = true
是否出现 gamma  = false
```

同一个 provider，磁盘上多了一个 Skill，渲染结果一个字都没变。要让它出现，必须重建 Agent：

```text
=== 磁盘新增 beta 后，重建 Agent 才看到 ===
## skills
- **alpha**: Alpha 规范
- **beta**: Beta 规范
```

组合根里那行注释写明了这个边界：

```typescript
// 章节组装阶段扫描一次；运行时 load_skill 仍会复查真实路径。
const skillRegistry = profile.capabilities.has("skills")
  ? SkillRegistry.scan(dependencies.workspace)
  : undefined;
```

**当前实现不会悄悄监视目录。** 这是刻意的选择：每轮都去扫一遍磁盘，会给每次模型请求加上文件系统 I/O，而 Skill 目录在一次会话里几乎不变。要做热更新，应该显式地加一个失效通知机制，而不是靠轮询。

---

## 记忆只注入一次

这一节是本章唯一需要动到前一章行为的地方，也是最容易出 bug 的地方。

### 问题：两条路径同时开着

第 9 章的记忆是通过 `TurnLifecycle.beforeModel()` 注入的——它返回一条额外的 system 消息，追加在请求历史后面：

```typescript
// 只把选中记忆作为 system context 附加到下一次模型请求，不写入 canonical history。
beforeModel(): readonly ChatMessage[] {
  if (!this.#emitContextMessages || this.#selected.length === 0) {
    return Object.freeze([]);
  }
  return Object.freeze([systemMessage(this.renderSelected())]);
}
```

第 10 章之后，选中的记忆已经进了动态 Prompt 的 `memory` section。如果旧路径还开着，同一段正文会在一次请求里出现**两次**：一次在 system prompt 里，一次作为独立 system 消息。

这不是理论推演。实验五把两种配置各跑一遍：

```text
emitContextMessages=true  -> 主请求里记忆正文出现 2 次（消息总数 3）
emitContextMessages=false -> 主请求里记忆正文出现 1 次（消息总数 2）
```

重复注入的后果：白烧一份 token，而且给模型一个错误信号——同一件事说两遍，通常意味着它更重要。

### 解法：组合根显式切换所有权

```typescript
// 启用动态 Prompt 时关闭 MemorySession.beforeModel() 的独立消息注入，由 Provider 统一输出记忆正文。
const memorySession = profile.capabilities.has("memory")
  ? createMemorySession(dependencies, !dynamicPrompt)
  : undefined;
```

`!dynamicPrompt` 这个取反是整段逻辑的核心：

| Profile | `dynamicPrompt` | `emitContextMessages` | 记忆从哪注入 |
| --- | --- | --- | --- |
| P09 | `false` | `true` | `TurnLifecycle.beforeModel()` 的独立 system 消息 |
| P10 | `true` | `false` | 动态 Prompt 的 `memory` section |

这样 P09 行为完全不变，P10 换成新路径，两者都只注入一次。

### 生命周期没有被关掉，只关了它的注入

容易误解的一点：`emitContextMessages: false` **不是**把记忆功能关掉。`MemorySession` 仍然完整地干这些事：

- `beginTurn()` 每轮读取集合、调用 selector 选记忆（失败时降级为关键词匹配）
- `complete()` 回合结束从 canonical history 提取新记忆、达到阈值时整理
- `selected` getter 暴露本轮选中的记录

只有 `beforeModel()` 的那条独立消息被关掉了。renderer 通过 `selected` 读同一份数据：

```typescript
const memoryBody =
  options.memory === undefined || options.memory.selected.length === 0
    ? ""
    : options.memory.renderSelected();
```

注意这里读的是 `selected` 和 `renderSelected()`，不是重新去 store 里选一遍。**选择权仍然属于 `MemorySession`，renderer 只负责渲染。** 这是本章反复出现的分工原则：prompt 层不重新实现工具发现、记忆选择或路径校验。

### 注入位置的变化

实验五之外，实验五的对照脚本还打出了位置信息：

```text
P09（旧路径：TurnLifecycle 注入）: 主请求消息数 = 3，含 <relevant_memories> 的消息数 = 1
  第 1 处：role=system，位置 index=2
  system prompt 是否含 "## memory" = false
P10（新路径：动态 Prompt 注入）: 主请求消息数 = 2，含 <relevant_memories> 的消息数 = 1
  第 1 处：role=system，位置 index=0
  system prompt 是否含 "## memory" = true
```

差异看得很清楚：

```text
P09 的请求：                      P10 的请求：
┌─────────────────────────┐      ┌─────────────────────────┐
│ [0] system: 固定 prompt  │      │ [0] system: 动态 prompt  │
│                         │      │      ├─ ## identity     │
│                         │      │      ├─ ## tools        │
│                         │      │      ├─ ## workspace    │
│                         │      │      ├─ ## skills       │
│                         │      │      └─ ## memory  ←注入 │
├─────────────────────────┤      ├─────────────────────────┤
│ [1] user: 用户问题       │      │ [1] user: 用户问题       │
├─────────────────────────┤      └─────────────────────────┘
│ [2] system: 记忆 ←注入   │
└─────────────────────────┘
```

P10 少了一条消息，记忆的位置从"用户问题之后"挪到了"系统提示内部"。

### 测试怎么钉住这条

关键是**统计出现次数**，而不是断言"包含某段文本"。后者在重复注入时同样会通过：

```typescript
expect(
  firstMain?.messages.filter((message) =>
    (message.content ?? "").includes("<relevant_memories>"),
  ),
).toHaveLength(1);
```

同一个测试还断言了跨工具轮的稳定性：

```typescript
expect(secondMain?.messages[0]).toEqual(firstMain?.messages[0]);
```

一次工具调用之后，第二次主请求的 system 消息和第一次**完全相同**。工具集合没变、选中记忆没变、Skill 目录没变，所以缓存命中，渲染结果一致。

---

## 运行第 10 章

### 第 0 步：准备环境

```powershell
Set-Location 'F:\笔记\Agent实操\code'
node --version      # 需要 Node 20 以上
npm install         # 只在第一次或依赖变动后执行
```

### 第 1 步：先跑离线测试（不需要 API Key）

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch10
```

期望看到：

```text
Test Files  25 passed (25)
     Tests  226 passed (226)
```

输出中间会出现一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这不是报错。** 它是 `config.test.ts` 主动断言的一条预期输出——测试在验证"缺少配置时必须明确报错而不是静默降级"。只要最后一行是 `passed`，就一切正常。

### 第 2 步：让 Agent 报出自己的工具列表

这一步需要真实模型，先确认 `code\.env` 里配好了 `OPENAI_BASE_URL`、`OPENAI_API_KEY`、`OPENAI_MODEL`。

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch10 -- --prompt "列出当前 Agent 的可用工具"
```

这个 prompt 是有讲究的：模型能准确报出 8 个工具名，说明 `tools` section 真的到了它面前。如果它回答"我没有工具"或者报出一堆不存在的工具名，说明渲染链某处断了。

也可以用统一入口，两种形式共用同一套 Bootstrap 和同一个 Loop：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 10 --prompt "列出当前 Agent 的可用工具"
```

### 第 3 步：换个 prompt 看 workspace 和记忆

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch10 -- --prompt "你当前的工作目录是哪里？有哪些可以加载的 Skill？"
```

模型应该报出 `code` 目录的绝对路径，以及 `code\skills\` 下扫描到的 Skill 名称。

如果之前跑过第 9 章、`.memory` 里有内容，还可以试：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch10 -- --prompt "关于这个项目，你记得什么约束？"
```

### 第 4 步（可选）：完整门禁

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run typecheck
npm run lint
```

### 常见报错排查

| 你看到的 | 原因 | 怎么办 |
| --- | --- | --- |
| `配置错误: Missing required settings: ...`（测试输出中间） | `config.test.ts` 断言的预期输出 | 不用管，看最后一行是否 `passed` |
| `配置错误: Missing required settings: ...`（`npm run ch10` 后直接退出） | `code\.env` 没配 | 复制 `.env.example` 为 `.env` 并填三个值 |
| `TypeError: systemPromptProvider must implement render()` | 传了个没有 `render()` 的对象 | 检查是否误传了 renderer 而不是 provider |
| `AgentRunError: System prompt provider returned an empty prompt` | `render()` 返回空串、空白、`null` 或非字符串 | 检查 renderer 的数据源，不要试图加 fallback |
| `PromptContextError: context must be a JSON object` | context 传了数组或标量 | 根必须是普通对象 |
| `PromptContextError: context contains an unsupported JSON value` | context 里有 `Date`、`Map`、函数或 `undefined` | 先转成 JSON 标量，比如 `date.toISOString()` |
| `PromptContextError: context contains a non-finite JSON number` | 有 `NaN` 或 `Infinity` | 通常是某个计算没做除零保护 |
| `PromptContextError: context JSON object keys must be strings` | 用了 symbol 做键 | 换成字符串键 |
| `PromptContextError: context contains a cyclic JSON object` | context 里有循环引用 | 传之前手工挑出需要的字段，别整个对象丢进来 |
| `TypeError: tools must be ToolRegistry` | 传了普通数组或字符串列表 | renderer 要的是注册表对象，不是名称列表 |
| `Error: identity must not be empty` | identity 是空串或纯空白 | 至少给一句身份描述 |
| `Error: profile must be a fixed chapter profile` | 自己构造了 profile 对象 | 用导出的 `P10` 单例 |
| 模型说"我没有可用工具" | `tools` section 是 `(none)` | 检查是否用了空的 `ToolRegistry` |
| 磁盘上新增的 Skill 不出现 | `SkillRegistry` 是构建期快照 | 重建 Agent；这是设计边界，不是 bug |
| 记忆正文出现两次 | `emitContextMessages` 没关 | 检查是否绕过 `bootstrap` 手工组装了 session |

---

## 六个动手小实验

这六个实验都**不调真实模型**，不需要 API Key，跑完就能验证本章所有核心结论。先建一个临时目录：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType Directory -Force '.\scratch' | Out-Null
```

下面每个实验的输出都是实际跑出来的。临时目录名里的随机串**你跑出来的会和这里不一样**，这是正常的。

### 实验一：五个 section 怎么长出来，正文会不会泄漏

存成 `code\scratch\probe1-sections.ts`：

```typescript
import { mkdirSync, mkdtempSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { z } from "zod";

import { ToolRegistry, toolSuccess } from "../chapters/ch10/src/core/tools.js";
import {
  MemoryRecord,
  MemorySession,
  MemoryStore,
  MemoryType,
} from "../chapters/ch10/src/features/memory.js";
import { DynamicPromptRenderer } from "../chapters/ch10/src/features/prompting.js";
import { SkillRegistry } from "../chapters/ch10/src/features/skills.js";

const workspace = mkdtempSync(join(tmpdir(), "probe1-"));

function addTool(tools: ToolRegistry, name: string): void {
  tools.register({
    name,
    description: `Run ${name}.`,
    inputSchema: z.object({}).strict(),
    effect: "read",
    handler: () => toolSuccess(name),
  });
}

// 阶段 1：空注册表、无 Skill、无记忆
const tools = new ToolRegistry();
const renderer = new DynamicPromptRenderer();
console.log("=== 阶段 1：什么都没有 ===");
console.log(renderer.render({ identity: "agent", tools, workspace, context: { chapter: 10 } }));

// 阶段 2：注册两个工具
addTool(tools, "read_file");
addTool(tools, "todo_write");
console.log("\n=== 阶段 2：注册两个工具后 ===");
console.log(renderer.render({ identity: "agent", tools, workspace, context: { chapter: 10 } }));

// 阶段 3：加上 Skill 目录和选中记忆
mkdirSync(join(workspace, "skills", "sql-style"), { recursive: true });
writeFileSync(
  join(workspace, "skills", "sql-style", "SKILL.md"),
  "---\nname: sql-style\ndescription: SQL 编写规范\n---\n# 这是 Skill 正文，不应出现在 Prompt 里\n",
  "utf8",
);
const store = new MemoryStore({ workspace, idGenerator: () => "one" });
await store.extend([
  new MemoryRecord({
    name: "database",
    description: "生产数据库约束",
    kind: MemoryType.PROJECT,
    body: "始终使用真实数据库。",
  }),
  new MemoryRecord({
    name: "keyboard",
    description: "未选中的键盘说明",
    kind: MemoryType.PROJECT,
    body: "这段正文不应出现在 Prompt 里",
  }),
]);
const memory = new MemorySession({
  store,
  selector: {
    async select() {
      return '["database"]';
    },
  },
});
await memory.beginTurn("检查数据库");

const full = renderer.render({
  identity: "agent",
  tools,
  workspace,
  context: { chapter: 10 },
  skills: SkillRegistry.scan(workspace),
  memory,
});
console.log("\n=== 阶段 3：五个 section 全部出现 ===");
console.log(full);

console.log("\n=== 泄漏检查 ===");
console.log("含 Skill 正文吗 =", full.includes("这是 Skill 正文"));
console.log("含未选中记忆正文吗 =", full.includes("这段正文不应出现"));
console.log("cacheHits =", renderer.cacheHits);

rmSync(workspace, { recursive: true, force: true });
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe1-sections.ts
```

三个阶段的输出就是本章开头「先看一眼渲染结果长什么样」那三段。最后三行是关键断言：

```text
=== 泄漏检查 ===
含 Skill 正文吗 = false
含未选中记忆正文吗 = false
cacheHits = 0
```

前两个 `false` 证明了两条边界：Skill 正文和未选中记忆的正文都没进 prompt。`cacheHits = 0` 说明三次渲染输入各不相同，全部 miss——符合预期，因为每次都改了工具或 section。

想看清空行的确切位置，可以加一个最小脚本 `code\scratch\probe1b-raw.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { ToolRegistry } from "../chapters/ch10/src/core/tools.js";
import { DynamicPromptRenderer } from "../chapters/ch10/src/features/prompting.js";

const workspace = mkdtempSync(join(tmpdir(), "probe1b-"));
const prompt = new DynamicPromptRenderer().render({
  identity: "agent",
  tools: new ToolRegistry(),
  workspace,
  context: { chapter: 10 },
});

console.log("原始字符 =", JSON.stringify(prompt));
console.log("行数 =", prompt.split("\n").length);
rmSync(workspace, { recursive: true, force: true });
```

实际输出：

```text
原始字符 = "## identity\nagent\ncontext: {\"chapter\":10}\n\n## tools\n(none)\n\n## workspace\nC:\\Users\\你的用户名\\AppData\\Local\\Temp\\probe1b-cLL8zY"
行数 = 9
```

用 `JSON.stringify` 打印是有意的——终端会把连续空行折叠，只有这样才能确认 section 之间真的是 `\n\n`。

### 实验二：context 的精确边界

存成 `code\scratch\probe2-context.ts`：

```typescript
import { ToolRegistry } from "../chapters/ch10/src/core/tools.js";
import { DynamicPromptRenderer } from "../chapters/ch10/src/features/prompting.js";

const tools = new ToolRegistry();
const workspace = process.cwd();

function probe(label: string, context: unknown): void {
  const renderer = new DynamicPromptRenderer();
  try {
    const prompt = renderer.render({
      identity: "agent",
      tools,
      workspace,
      context: context as never,
    });
    const line = prompt.split("\n").find((text) => text.startsWith("context: "));
    console.log(`${label.padEnd(28)} 通过 -> ${line}`);
  } catch (error) {
    console.log(`${label.padEnd(28)} 拒绝 -> ${(error as Error).name}: ${(error as Error).message}`);
  }
}

console.log("--- 合法 ---");
probe("空对象", {});
probe("标量混合", { n: 1, s: "文本", b: true, nil: null });
probe("嵌套对象与数组", { a: { y: 2, x: 1 }, list: [1, "二", false, null] });
probe("键顺序颠倒", { z: 1, a: 2 });
probe("JSON 里的 __proto__", JSON.parse('{"__proto__":"safe"}'));

console.log("\n--- 拒绝 ---");
probe("根是数组", ["不能是数组"]);
probe("根是字符串", "不能是字符串");
probe("Date 对象", { when: new Date() });
probe("Map 对象", { m: new Map() });
probe("函数值", { fn: () => 1 });
probe("undefined 值", { maybe: undefined });
probe("NaN", { n: Number.NaN });
probe("Infinity", { n: Number.POSITIVE_INFINITY });
probe("symbol 键", { [Symbol("secret")]: "hidden" });

const cyclic: Record<string, unknown> = {};
cyclic.self = cyclic;
probe("循环引用对象", cyclic);

const cyclicArray: unknown[] = [];
cyclicArray.push(cyclicArray);
probe("循环引用数组", { list: cyclicArray });

console.log("\n--- identity 校验 ---");
for (const identity of ["  前后空格  ", "", "   "]) {
  try {
    const prompt = new DynamicPromptRenderer().render({
      identity,
      tools,
      workspace,
      context: {},
    });
    console.log(`identity=${JSON.stringify(identity)} 通过 -> 第 2 行 = ${JSON.stringify(prompt.split("\n")[1])}`);
  } catch (error) {
    console.log(`identity=${JSON.stringify(identity)} 拒绝 -> ${(error as Error).message}`);
  }
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe2-context.ts
```

实际输出：

```text
--- 合法 ---
空对象                          通过 -> context: {}
标量混合                         通过 -> context: {"b":true,"n":1,"nil":null,"s":"文本"}
嵌套对象与数组                      通过 -> context: {"a":{"x":1,"y":2},"list":[1,"二",false,null]}
键顺序颠倒                        通过 -> context: {"a":2,"z":1}
JSON 里的 __proto__            通过 -> context: {"__proto__":"safe"}

--- 拒绝 ---
根是数组                         拒绝 -> PromptContextError: context must be a JSON object
根是字符串                        拒绝 -> PromptContextError: context must be a JSON object
Date 对象                      拒绝 -> PromptContextError: context contains an unsupported JSON value
Map 对象                       拒绝 -> PromptContextError: context contains an unsupported JSON value
函数值                          拒绝 -> PromptContextError: context contains an unsupported JSON value
undefined 值                  拒绝 -> PromptContextError: context contains an unsupported JSON value
NaN                          拒绝 -> PromptContextError: context contains a non-finite JSON number
Infinity                     拒绝 -> PromptContextError: context contains a non-finite JSON number
symbol 键                     拒绝 -> PromptContextError: context JSON object keys must be strings
循环引用对象                       拒绝 -> PromptContextError: context contains a cyclic JSON object
循环引用数组                       拒绝 -> PromptContextError: context contains a cyclic JSON array

--- identity 校验 ---
identity="  前后空格  " 通过 -> 第 2 行 = "前后空格"
identity="" 拒绝 -> identity must not be empty
identity="   " 拒绝 -> identity must not be empty
```

三行值得对着看：

- `{ z: 1, a: 2 }` 输出 `{"a":2,"z":1}`，嵌套的 `{ y: 2, x: 1 }` 输出 `{"x":1,"y":2}`——**排序是递归的**。
- `{ maybe: undefined }` 被**拒绝**而不是被丢掉。`JSON.stringify` 会静默删掉这个键，这里明确报错。
- `identity: "  前后空格  "` 通过但被 trim 成 `前后空格`。多打的空格不会变成一个新缓存键。

### 实验三：缓存的六种情况

存成 `code\scratch\probe3-cache.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { z } from "zod";

import { ToolRegistry, toolSuccess } from "../chapters/ch10/src/core/tools.js";
import { DynamicPromptRenderer } from "../chapters/ch10/src/features/prompting.js";

const workspace = mkdtempSync(join(tmpdir(), "probe3-"));
const tools = new ToolRegistry();
const renderer = new DynamicPromptRenderer();

function render(context: Record<string, unknown>): string {
  return renderer.render({
    identity: "agent",
    tools,
    workspace,
    context: context as never,
  });
}

// 1. 首次渲染：必然是 miss
const first = render({ a: 1, b: 2 });
console.log("第 1 次渲染后 cacheHits =", renderer.cacheHits);

// 2. 同样的 context：命中
const same = render({ a: 1, b: 2 });
console.log("第 2 次（完全相同）      cacheHits =", renderer.cacheHits, "| 同一字符串 =", same === first);

// 3. 键顺序不同但语义相同：仍然命中
const reordered = render({ b: 2, a: 1 });
console.log("第 3 次（键顺序颠倒）    cacheHits =", renderer.cacheHits, "| 同一字符串 =", reordered === first);

// 4. 注册一个新工具：失效
tools.register({
  name: "write_file",
  description: "Write a file.",
  inputSchema: z.object({}).strict(),
  effect: "write",
  handler: () => toolSuccess("ok"),
});
const afterTool = render({ b: 2, a: 1 });
console.log("第 4 次（新增工具后）    cacheHits =", renderer.cacheHits, "| 同一字符串 =", afterTool === first);

// 5. 另一个 renderer 实例：缓存不共享
const other = new DynamicPromptRenderer();
const otherPrompt = other.render({ identity: "agent", tools, workspace, context: { b: 2, a: 1 } });
console.log("另一个 renderer          cacheHits =", other.cacheHits, "| 文本与上一次相同 =", otherPrompt === afterTool);

// 6. 非法 context 不污染缓存
try {
  render({ when: new Date() });
} catch (error) {
  console.log("\n非法 context 被拒绝 =", (error as Error).message);
}
const afterFailure = render({ b: 2, a: 1 });
console.log("失败后再渲染             cacheHits =", renderer.cacheHits, "| 仍复用失败前结果 =", afterFailure === afterTool);

rmSync(workspace, { recursive: true, force: true });
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe3-cache.ts
```

实际输出：

```text
第 1 次渲染后 cacheHits = 0
第 2 次（完全相同）      cacheHits = 1 | 同一字符串 = true
第 3 次（键顺序颠倒）    cacheHits = 2 | 同一字符串 = true
第 4 次（新增工具后）    cacheHits = 2 | 同一字符串 = false
另一个 renderer          cacheHits = 0 | 文本与上一次相同 = true
非法 context 被拒绝 = context contains an unsupported JSON value
失败后再渲染             cacheHits = 3 | 仍复用失败前结果 = true
```

最值得琢磨的是倒数第三行：`cacheHits = 0` 但 `文本与上一次相同 = true`。**缓存是隔离的，渲染是确定的**——这两件事必须同时成立。只做到前者，会有两个 Agent 拿到不同 prompt 的风险；只做到后者，就是模块全局缓存那套问题。

### 实验四：哪些运行态变化会生效，哪些不会

存成 `code\scratch\probe4-live.ts`：

```typescript
import { mkdirSync, mkdtempSync, rmSync, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { buildAgent } from "../chapters/ch10/src/bootstrap.js";
import { assistantMessage } from "../chapters/ch10/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch10/src/core/model.js";
import { PermissionDecision } from "../chapters/ch10/src/core/permissions.js";
import { P10 } from "../chapters/ch10/src/core/profiles.js";
import { ToolRegistry } from "../chapters/ch10/src/core/tools.js";
import {
  DynamicPromptProvider,
  DynamicPromptRenderer,
} from "../chapters/ch10/src/features/prompting.js";
import { SkillRegistry } from "../chapters/ch10/src/features/skills.js";

class Recorder implements ModelClient {
  readonly requests: ModelRequest[] = [];

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.requests.push(request);
    return { message: assistantMessage("done"), finishReason: "stop" };
  }
}

function writeSkill(workspace: string, name: string, description: string): void {
  const directory = join(workspace, "skills", name);
  mkdirSync(directory, { recursive: true });
  writeFileSync(
    join(directory, "SKILL.md"),
    `---\nname: ${name}\ndescription: ${description}\n---\n# body\n`,
    "utf8",
  );
}

const workspace = mkdtempSync(join(tmpdir(), "probe4-"));
try {
  writeSkill(workspace, "alpha", "Alpha 规范");

  // 1. P10 完整工具列表
  const model = new Recorder();
  const runner = buildAgent(P10, {
    model,
    workspace,
    approvalProvider: { async decide() { return new PermissionDecision("allow", "t", "t"); } },
    auditSink: { async record() {} },
  });
  await runner.run("hi");
  const prompt = model.requests[0]?.messages[0]?.content ?? "";
  console.log("=== P10 的 tools section ===");
  console.log(prompt.split("\n\n").find((s) => s.startsWith("## tools")));
  console.log("\n=== P10 的 skills section ===");
  console.log(prompt.split("\n\n").find((s) => s.startsWith("## skills")));

  // 2. 磁盘新增 Skill，但 registry 是构建期扫描的快照
  writeSkill(workspace, "beta", "Beta 规范");
  const model2 = new Recorder();
  const runner2 = buildAgent(P10, {
    model: model2,
    workspace,
    approvalProvider: { async decide() { return new PermissionDecision("allow", "t", "t"); } },
    auditSink: { async record() {} },
  });
  await runner2.run("hi");
  const prompt2 = model2.requests[0]?.messages[0]?.content ?? "";
  console.log("\n=== 磁盘新增 beta 后，重建 Agent 才看到 ===");
  console.log(prompt2.split("\n\n").find((s) => s.startsWith("## skills")));

  // 3. 同一个 provider 内：Skill 目录不会自己重扫
  const tools = new ToolRegistry();
  const skills = SkillRegistry.scan(workspace);
  const provider = new DynamicPromptProvider({
    renderer: new DynamicPromptRenderer(),
    identity: "agent",
    tools,
    workspace,
    context: {},
    skills,
  });
  const before = provider.render();
  writeSkill(workspace, "gamma", "Gamma 规范");
  const after = provider.render();
  console.log("\n=== 同一 provider：磁盘再加 gamma ===");
  console.log("两次渲染是否相同 =", before === after);
  console.log("是否出现 gamma  =", after.includes("gamma"));
} finally {
  rmSync(workspace, { recursive: true, force: true });
}
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe4-live.ts
```

实际输出：

```text
=== P10 的 tools section ===
## tools
- shell
- read_file
- write_file
- edit_file
- glob
- todo_write
- task
- load_skill

=== P10 的 skills section ===
## skills
- **alpha**: Alpha 规范

=== 磁盘新增 beta 后，重建 Agent 才看到 ===
## skills
- **alpha**: Alpha 规范
- **beta**: Beta 规范

=== 同一 provider：磁盘再加 gamma ===
两次渲染是否相同 = true
是否出现 gamma  = false
```

最后两行是本实验的重点：**同一个 provider 不会重新扫描磁盘。** 这是设计边界，不是 bug——理由在「"下一轮生效"的准确边界」那一节。

工具列表的顺序也值得记一下：`shell` 到 `glob` 是第 2 章的基础工具，`todo_write` 来自第 5 章，`task` 来自第 6 章，`load_skill` 来自第 7 章。这个顺序由组合根的注册顺序决定，`tests/ch07-skills.test.ts` 里对这八个名字有精确断言。`bootstrap.ts` 那行注释写明了为什么 `load_skill` 排在最后：

```typescript
// load_skill 注册在 task 之后，使父工具顺序稳定且子工具仍可复用同一注册表。
tools.register(skillRegistry.toolDefinition);
```

### 实验五：记忆到底注入几次

存成 `code\scratch\probe5-once.ts`：

```typescript
import { mkdtempSync, rmSync } from "node:fs";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { AgentRunner } from "../chapters/ch10/src/core/loop.js";
import { assistantMessage } from "../chapters/ch10/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch10/src/core/model.js";
import { ToolRegistry } from "../chapters/ch10/src/core/tools.js";
import {
  MemoryRecord,
  MemorySession,
  MemoryStore,
  MemoryType,
} from "../chapters/ch10/src/features/memory.js";
import {
  DynamicPromptProvider,
  DynamicPromptRenderer,
} from "../chapters/ch10/src/features/prompting.js";

class Recorder implements ModelClient {
  readonly requests: ModelRequest[] = [];

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.requests.push(request);
    return { message: assistantMessage("done"), finishReason: "stop" };
  }
}

// 故意保留 emitContextMessages 默认值 true，同时又把 memory 接进动态 Prompt。
async function run(emitContextMessages: boolean): Promise<void> {
  const workspace = mkdtempSync(join(tmpdir(), "probe5-"));
  try {
    const store = new MemoryStore({ workspace, idGenerator: () => "one" });
    await store.add(
      new MemoryRecord({
        name: "database",
        description: "生产数据库约束",
        kind: MemoryType.PROJECT,
        body: "始终使用真实数据库。",
      }),
    );
    const memory = new MemorySession({
      store,
      selector: { async select() { return '["database"]'; } },
      emitContextMessages,
    });
    const tools = new ToolRegistry();
    const model = new Recorder();
    const runner = new AgentRunner({
      model,
      tools,
      systemPrompt: "fallback",
      systemPromptProvider: new DynamicPromptProvider({
        renderer: new DynamicPromptRenderer(),
        identity: "agent",
        tools,
        workspace,
        context: { chapter: 10 },
        memory,
      }),
      workspace,
      turnLifecycle: memory,
    });

    await runner.run("检查数据库");

    const request = model.requests[0];
    const hits = (request?.messages ?? []).filter((message) =>
      (message.content ?? "").includes("始终使用真实数据库。"),
    );
    console.log(
      `emitContextMessages=${String(emitContextMessages).padEnd(5)} -> 主请求里记忆正文出现 ${hits.length} 次` +
        `（消息总数 ${request?.messages.length}）`,
    );
  } finally {
    rmSync(workspace, { recursive: true, force: true });
  }
}

await run(true);
await run(false);
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe5-once.ts
```

实际输出：

```text
emitContextMessages=true  -> 主请求里记忆正文出现 2 次（消息总数 3）
emitContextMessages=false -> 主请求里记忆正文出现 1 次（消息总数 2）
```

这个实验故意绕过了 `bootstrap`，直接手工组装 `AgentRunner`——目的就是复现"忘记关旧路径"的后果。走正常的 `buildAgent(P10, ...)` 不会出现这个问题，因为组合根用 `createMemorySession(dependencies, !dynamicPrompt)` 强制关掉了它。

想同时看 P09 和 P10 的注入位置差异，可以把 profile 换成 `buildAgent`：本章「注入位置的变化」那一节的输出就是这么跑出来的。

### 实验六：provider 契约的所有失败分支

存成 `code\scratch\probe6-provider.ts`：

```typescript
import { AgentRunner } from "../chapters/ch10/src/core/loop.js";
import { assistantMessage } from "../chapters/ch10/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch10/src/core/model.js";
import { ToolRegistry } from "../chapters/ch10/src/core/tools.js";

class Recorder implements ModelClient {
  readonly requests: ModelRequest[] = [];

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.requests.push(request);
    return { message: assistantMessage("done"), finishReason: "stop" };
  }
}

async function attempt(label: string, provider: unknown): Promise<void> {
  try {
    const runner = new AgentRunner({
      model: new Recorder(),
      tools: new ToolRegistry(),
      systemPrompt: "构建期固定 fallback",
      systemPromptProvider: provider as never,
      workspace: process.cwd(),
    });
    const result = await runner.run("hi");
    console.log(`${label.padEnd(26)} 通过 -> finalText=${JSON.stringify(result.finalText)}`);
  } catch (error) {
    console.log(`${label.padEnd(26)} 失败 -> ${(error as Error).name}: ${(error as Error).message}`);
  }
}

await attempt("正常字符串", { render: () => "## identity\nagent" });
await attempt("空字符串", { render: () => "" });
await attempt("只有空白", { render: () => "   \n  " });
await attempt("返回 null", { render: () => null });
await attempt("返回数字", { render: () => 42 });
await attempt("render 不是函数", { render: "不是函数" });
await attempt("没有 render", {});
await attempt("render 内部抛错", {
  render: () => {
    throw new Error("渲染器自己炸了");
  },
});

// 证明 provider 存在时构建期的 systemPrompt 不再被使用
const model = new Recorder();
const runner = new AgentRunner({
  model,
  tools: new ToolRegistry(),
  systemPrompt: "构建期固定 fallback",
  systemPromptProvider: { render: () => "运行态渲染结果" },
  workspace: process.cwd(),
});
await runner.run("hi");
console.log("\n实际发给模型的 system 消息 =", JSON.stringify(model.requests[0]?.messages[0]?.content));
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx tsx scratch\probe6-provider.ts
```

实际输出：

```text
正常字符串                      通过 -> finalText="done"
空字符串                       失败 -> AgentRunError: System prompt provider returned an empty prompt
只有空白                       失败 -> AgentRunError: System prompt provider returned an empty prompt
返回 null                    失败 -> AgentRunError: System prompt provider returned an empty prompt
返回数字                       失败 -> AgentRunError: System prompt provider returned an empty prompt
render 不是函数                失败 -> TypeError: systemPromptProvider must implement render()
没有 render                  失败 -> TypeError: systemPromptProvider must implement render()
render 内部抛错                失败 -> Error: 渲染器自己炸了

实际发给模型的 system 消息 = "运行态渲染结果"
```

三个观察点：

- 前两个 `TypeError` 是**构造时**抛的，`run()` 根本没开始。契约问题越早暴露越好。
- 四种"返回值不对"都归到同一条 `AgentRunError`，因为对 Loop 来说它们是同一类问题：拿不到可用的系统提示。
- 最后一行证明 `systemPrompt: "构建期固定 fallback"` 完全没被用。provider 存在时它就是死代码，只在 P09 及之前的路径上有用。

想看真实 P10 的 identity section 长什么样，把 provider 换成 `buildAgent(P10, { ..., identity: "demo-user" })` 跑一次，就能得到本章「真实运行时它长什么样」那段输出。不传 `identity` 时组合根会回退成 `"user"`，context 里就是 `"identity":"user"`。

### 清理

```powershell
Set-Location 'F:\笔记\Agent实操\code'
Remove-Item -Recurse -Force '.\scratch'
```

这六个实验都用 `mkdtempSync` 建临时目录、`rmSync` 收尾，不会在仓库里留下 `.memory` 或 `skills` 目录。唯一需要手工删的是 `scratch` 本身。

---

## 这章没有做什么

照着教程宣称不存在的能力，是最容易踩的坑。把边界列清楚：

| 已经有 | 还没有 | 什么时候补 |
| --- | --- | --- |
| 五个固定 section | 静态 / 动态缓存边界分块 | **不补**。需要适配器层配合 cache-control，见「这个缓存不是 API prompt cache」 |
| renderer 实例缓存 | API 级 prompt cache 命中保证 | **不补**。当前 OpenAI 适配器没有分块 |
| 全部内容渲染进 system prompt | 系统上下文 / 用户上下文分离 | **不补**。见「与 Claude Code 的差异」 |
| 构建期扫描 Skill 目录 | 目录变化的运行时热更新 | **不补**。需要显式失效通知，不该靠轮询 |
| Profile 控制能力集合 | 按运行模式整体替换 prompt | **不补**。教学版只需要一套 section |
| `cacheHits` 计数 | 真实 API cache ratio / TTFT 实验 | **不补**。需要真实凭据和压测环境 |
| 严格 JSON context | context 的 schema 声明与版本化 | **不补**。当前 context 只放章节号和 identity |
| 顺序与内容的离线断言 | prompt 质量评测（A/B、行为回归） | 这是下一步该做的事，不是本章范围 |

有两条要特别强调。

**没有静态 / 动态缓存边界。** 本章把 identity、工具、workspace、Skill 目录和选中记忆**全部**渲染进 system prompt。这意味着记忆一变，整个 system prompt 的文本就变了。如果供应商按前缀缓存，这会让前面那些其实没变的 section 也一起失效。真实产品需要把静态部分和动态部分分开成不同的缓存块——本章没做，所以也不宣称有这个收益。

**没有 prompt 质量评测。** 本章的 226 个测试全部在验证"结构是否正确"：顺序对不对、缓存会不会串、正文有没有泄漏、记忆注入几次。**没有一个测试在验证"这套 prompt 是否让模型表现更好"。** 后者需要构造评测集做行为对比，是完全不同的工作。

---

## 与 Claude Code 的差异

Claude Code 的系统提示同样按 section 组装，但规模比本教学版大得多，而且会显式区分静态与动态两类内容。

**静态 / 动态边界。** 标准交互模式下，静态 section（identity、doing_tasks、using_tools、tone_style 等）和动态 section（session_guidance、memory、env_info_simple、output_style 等）由 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY` 分隔。`mcp_instructions` 是唯一的易失 section，用 `DANGEROUS_uncachedSystemPromptSection()` 标记为不参与常规 section 缓存——因为 MCP server 可以在轮次之间连接或断开。

P10 的 `identity -> tools -> workspace -> skills -> memory` 只把可选 section 追加到尾部，没有静态 / 动态边界，也没有必须绕过缓存的 section。对教学版来说这是合理简化，但它意味着 P10 的 system prompt 是"全都会变"的一整块。

**缓存层级。** 官方 API 的 prompt caching 会缓存 `tools`、`system`、`messages` 前缀，只有断点前的完整前缀一致时才命中（[Prompt caching](https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching)）。CC 在 API 级缓存之外还有会话级 `getSystemContext` / `getUserContext` memoize 和动态 section 注册缓存。

P10 只有 renderer 实例缓存这一层。而且当前 OpenAI 适配器没有把静态和动态 section 分成不同缓存块，所以即使同一会话内组装结果被复用，也无法保证供应商侧会命中静态前缀缓存。

**上下文归属。** CC 把 `gitStatus`、`cacheBreaker` 等**系统上下文**追加到 system prompt，把 `CLAUDE.md`、当前日期等**用户上下文**前置为 `<system-reminder>` 用户消息。P10 把所有可见上下文都渲染进 system prompt，不区分归属。

这个差异直接影响静态前缀的稳定性：用户消息变化不应该破坏 system prompt 的缓存前缀，而 P10 当前没有利用这一层。想在自己项目里做前缀缓存，这是第一个该改的地方。

**模式替换。** CC 支持 `CLAUDE_CODE_SIMPLE`、Proactive/KAIROS、Coordinator、Agent 等运行模式，部分模式会用紧凑 prompt 或专用 prompt 整体替换标准 section——`CLAUDE_CODE_SIMPLE` 下核心 prompt 只有约 150 字符。

P10 的 Profile 只控制能力集合，渲染器始终输出同一套 section。真实产品通常要按运行模式切换整套指令，这是教学版和产品的一条明显分界。

四个差异汇总：

| 维度 | Claude Code | P10 |
| --- | --- | --- |
| section 规模 | 大量，显式分静态 / 动态两组 | 五个，全部视为可变 |
| 缓存层级 | API 前缀缓存 + 会话 memoize + section 注册缓存 | 只有 renderer 实例缓存 |
| 易失内容 | `mcp_instructions` 显式标记绕过缓存 | 无此概念 |
| 上下文归属 | 系统上下文进 system，用户上下文进 `<system-reminder>` | 全部进 system prompt |
| 运行模式 | 多模式，可整体替换 prompt | 单一套 section |

---

## 从 ai-agent-book 学到什么：先定缓存边界，再做动态提示

`ai-agent-book/book/chapter2.md` 是本章最接近的对照：它用 KV Cache 与 Agent 状态栏说明了"什么能放进 system prompt、什么应该追加到末尾"。

P10 把身份、工具、workspace、Skill 目录和选中记忆全部渲染进 system prompt。这是教学上的简化，代价就是不能宣称 API prompt cache 命中。

| ai-agent-book 的关键设计 | P10 对应 | 差异与边界 |
| --- | --- | --- |
| 静态前缀 + 动态轨迹分开 | P10 固定五个 section，运行态只读数据源 | P10 没有 static/dynamic 缓存边界 |
| 动态信息追加末尾 | P10 可选 skills/memory section 追加到 prompt 尾部 | P10 仍全在 system prompt；ai-agent-book 建议动态状态作为末尾 user 消息 |
| Agent 状态栏由代码确定性维护 | P10 的 provider/renderer 每轮重新读取运行时对象 | P10 没有独立状态栏；workspace/skills/memory 都在 system prompt |
| Skills 目录常驻、正文按需加载 | `SkillRegistry.renderCatalog()` + `loadSkill()` | P10 已与 ai-agent-book 主张一致 |
| 实验先定义指标再实现 | 离线测试 `cacheHits`、顺序断言、context 规范化、记忆单次注入 | P10 没有真实 API cache ratio / TTFT 实验 |

最值得借鉴的一点是**顺序**：先确定缓存边界在哪，再决定动态内容放哪。P10 反过来了——先把 section 拆干净，缓存边界留到以后。这在教学上可以接受（拆分本身就是本章要讲的事），但在真实项目里，如果一开始不想清楚"哪些内容会变、变了会让谁失效"，后面很可能要把整个 prompt 结构再翻一遍。

如果要在 P10 上继续演进，优先级应该是：先把上下文按归属拆到 system / user 两侧，再引入静态 / 动态缓存块，最后才是 prompt 质量评测。反过来做，评测结果会被缓存结构的变化反复推翻。

---

## 配套代码索引

第 10 章实现集中在 `code/chapters/ch10/src/`，章节入口固定选择 P10 Profile。本章新增 `features/prompting.ts`，并通过 `core/loop.ts` 的 `SystemPromptProvider` 接口接入 AgentRunner。

- 功能实现：`code/chapters/ch10/src/features/prompting.ts`
- 组合根：`code/chapters/ch10/src/bootstrap.ts`
- Loop 接入点：`code/chapters/ch10/src/core/loop.ts`
- 能力与 profile：`code/chapters/ch10/src/core/profiles.ts`
- 章节入口：`code/chapters/ch10/src/chapters/ch10.ts`
- 数据源：`code/chapters/ch10/src/core/tools.ts`、`features/skills.ts`、`features/memory.ts`
- 直接行为测试：`code/chapters/ch10/tests/prompting.test.ts`
- 接线测试：`code/chapters/ch10/tests/ch10-prompt.test.ts`

从上到下读的话，三个角色的分工是这样的：

```text
ToolRegistry / SkillRegistry / MemorySession   ← 各自持有数据，各自负责校验
     ↑ 只读当前值
DynamicPromptRenderer   ← 校验 + 规范化 + 固定顺序拼装 + 实例缓存
     ↑ 零参数适配
DynamicPromptProvider   ← 构建期绑定，运行期只转发
     ↑ SystemPromptProvider 接口
AgentRunner             ← 每轮请求前调 render()，不知道有几个 section
```

最后一行值得注意：`AgentRunner` 只认 `SystemPromptProvider` 这个接口（一个零参数方法），完全不知道 prompt 里有什么。所以动态提示能力可以整体拔掉，Loop 不需要改一行——P09 就是这么跑的。

| 实现 | 职责与边界 |
| --- | --- |
| `DynamicPromptRenderer.render()` | 唯一渲染入口；校验四类输入类型、规范化 context、生成缓存键、按固定顺序输出五个 section |
| `normalizeIdentity()` | 拒绝非字符串和空白 identity；trim 后参与缓存键 |
| `normalizeContext()` / `normalizeJsonValue()` | 递归克隆并冻结合法 JSON；拒绝非普通对象、symbol 键、非有限数和循环引用 |
| `isPlainObject()` | 用原型严格相等判断普通对象；`Date`、`Map`、类实例全部拒绝 |
| `stableJson()` | 递归按键排序序列化，使语义相同的 context 得到同一文本和同一缓存键 |
| `DynamicPromptRenderer` 的 `#lastKey` / `#lastPrompt` | 实例级缓存；只有六个输入的规范快照全部相等才复用，校验失败不写入 |
| `cacheHits` | 只用于测试与观测，证明相同输入不重复组装；不是性能指标 |
| `DynamicPromptProvider` | 构建期绑定 renderer 与运行态对象，对外暴露零参数 `render()`；可选能力缺失时不传对应键 |
| `SystemPromptProvider` | Loop 侧接口，只有 `render(): string`，使 Loop 与数据源解耦 |
| `AgentRunner.#renderSystemPrompt()` | 每轮请求前渲染；空值或非字符串立即抛 `AgentRunError`，不回退到构建期字符串 |
| `bootstrap.createMemorySession()` | 用 `!dynamicPrompt` 决定 `emitContextMessages`，保证记忆只注入一次 |
| `P10` | 在 P09 能力上追加 `dynamic_prompt`，使组合根切换到运行时渲染路径 |
| `ch10.ts` | 固定 P10 的章节入口，防止该入口意外启用其他能力 |

---

## 运行与验证

第 10 章有两种等价的启动方式，任选一种：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch10 -- --prompt "列出当前目录并总结"
```

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 10 --prompt "列出当前目录并总结"
```

两者都会走 P10 Profile，也就是启用动态提示的那条路径。

`--prompt` 是**具名参数，不能省**。直接写 `npm run ch10 -- "列出当前目录并总结"` 会立刻失败：

```text
运行失败: Unknown argument: 列出当前目录并总结
```

固定入口 `ch10` 已经自动补上了 `--chapter 10`，不需要自己再传。

跑测试：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch10
```

本章写作时的实际结果：

```text
Test Files  25 passed (25)
     Tests  226 passed (226)
```

226 个测试里，与本章直接相关的是两个文件：

| 文件 | 验证什么 |
| --- | --- |
| `tests/prompting.test.ts` | renderer 与 provider 的直接行为：section 顺序、可选段落省略、context 规范化与拒绝、缓存命中与隔离、`__proto__` 处理、provider 的失败分支 |
| `tests/ch10-prompt.test.ts` | 接线是否正确：真实 bootstrap 出来的 Agent 每轮拿到的 system prompt 顺序对不对、记忆是否只注入一次 |

如果你改了 `prompting.ts` 想快速验证，可以只跑这两个文件：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx vitest run chapters/ch10/tests/prompting.test.ts chapters/ch10/tests/ch10-prompt.test.ts
```

提交前建议再跑一次完整检查：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run lint
npm run typecheck
```

---

## 本章小结

### 三句话版本

1. 硬编码的 prompt 字符串在构建期就定死了，工具、Skill、记忆一变它就说谎；本章把 prompt 变成每轮请求前重新渲染的**函数**。
2. 渲染分三个角色：`DynamicPromptRenderer` 只负责校验和拼装，`DynamicPromptProvider` 负责绑定数据源并适配成零参数接口，`AgentRunner` 每轮调一次 `render()`。
3. 固定的五段顺序（`identity -> tools -> workspace -> skills -> memory`）、canonical JSON 缓存键和实例级缓存，共同保证"同样的上下文 → 同样的文本"，这是后续做缓存边界的地基。

### 一定要记住的五条

**第一，section 顺序是契约，不是排版。** 可选段落只能追加到尾部，不能插队。一旦顺序随输入变化，缓存键就失去意义，模型看到的结构也不稳定。测试里的顺序断言就是在守这条线。

**第二，缓存键必须覆盖所有模型可见的输入。** P10 的键包含六项：`context`、`identity`、`memory`、`skills`、`tools`、`workspace`。漏掉任何一项，那一项变了却复用旧 prompt，模型就会拿到过期信息——这类 bug 不会报错，只会让 Agent 行为莫名其妙。

**第三，缓存必须是实例级的。** 模块级全局缓存会让两个会话互相污染：A 会话的记忆可能出现在 B 会话的 prompt 里。这不是性能问题，是数据泄漏。

**第四，校验要在写缓存之前。** 非法 context 应该抛错并且**不留下任何痕迹**。如果先写缓存再校验，一次失败就会污染后续所有渲染。

**第五，记忆只能注入一次。** 动态提示启用后，记忆已经作为 `## memory` section 进了 system prompt，`MemorySession` 就必须停止再发自己的 system 消息。`createMemorySession(dependencies, !dynamicPrompt)` 那个取反就是干这件事的，看着不起眼，去掉就是双份注入。

### 检查你是否真的读懂了

试着回答下面五个问题。答不上来的，回到对应实验重跑一遍。

1. 为什么 `render()` 是零参数的？如果它接受参数，谁会变成 Loop 的依赖？（实验六 / `SystemPromptProvider` 那一节）
2. `{ b: 1, a: 2 }` 和 `{ a: 2, b: 1 }` 会不会命中同一个缓存？为什么需要 canonical JSON？（实验二、实验三）
3. `new Date()` 放进 context 会发生什么？为什么不直接接受它？（实验二）
4. 工具列表变了但 identity 没变，缓存会命中吗？说出判断依据。（实验三、实验五）
5. 磁盘上新增一个 Skill 目录，正在运行的 Agent 会看到吗？为什么？（实验四、"这章没有做什么"）

第五问最容易答错：不会看到。`SkillRegistry.scan()` 是构建期的一次性快照，不监听目录变化。动态提示的"动态"指的是**每轮重新读取运行时对象**，不是**运行时重新扫描磁盘**。这两件事很容易混。

---

第 10 章把提示词从"构建期的一个字符串"变成了"运行期的一个函数"。到这里，Agent 每轮看到的上下文终于是它真实拥有的能力了。

但真实运行远不止"上下文对不对"。模型会输出到一半被截断，API 会限流，会返回 503，输入会超出上下文窗口，你还可能想在中途换一个模型。这些都不是逻辑错误，而是运行时必然会遇到的状况——第 11 章会把它们逐一处理掉。
