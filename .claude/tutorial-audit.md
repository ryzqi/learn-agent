# 教程/代码一致性审校台账

目标：形成完整、由浅入深的 Agent 开发教程；逐章核对教程与 `code/chapters/chNN` 对应，并保证讲解深入浅出。

## 工具

- `code/scripts/chapter-delta.mjs [from] [to]`：相邻章节快照差异，确认"每章只加一个能力"。
- `code/scripts/verify-tutorial-refs.mjs [ch...]`：教程引用的路径 / npm script / 文件名是否真实存在。
- `code/scripts/verify-tutorial-snippets.mjs [ch...]`：教程 TS 代码块能否在本章快照中找到对应源码。

## 全局核对结果

| 项 | 状态 |
| --- | --- |
| 路径/脚本/文件名引用 | 已核对，无失效引用（`main.ts`/`query.ts`/`peek.ts`/`full-harness.ts` 为正当引用） |
| 章节递增结构 | 已核对，ch02–ch20 每章新增 1 个 feature + 1 个入口 + 对应测试 |

## 逐章进度

状态含义：`待审` / `已定位`（发现问题未改） / `已修改未验证` / `已验证通过`

| 章 | 主题 | 状态 | 备注 |
| ---: | --- | --- | --- |
| 1 | Agent Loop | 已验证通过 | 数值声明（120s / 50000 / 20 轮 / 13 源文件 / 6 文件 30 测试）、配置错误原文、`validateToolPairing` 三条错误文案均与源码及实测一致 |
| 2 | 工具与文件 | 已验证通过 | 实测 8 文件 47 测试一致；测试文件表逐一对上；`safePath` 两关、五工具错误码、`P01`/`P02`、bootstrap 分支与源码一致。偏差：教程引用的 `.describe()` 为源码缩写版（语义一致，属正当简化） |
| 3 | 权限系统 | 已验证通过 | 实测 10 文件 61 测试一致；四态优先级 `deny>ask>allow>passthrough`、七种 fail-closed（5 在 `#resolveApproval` + 2 在 CLI）、`[Permission]` 审计格式、审批提示语、stderr/stdout 分流、P01/P02 无 policy 时仍保 shell 审批（累加原则）均与源码逐字一致 |
| 4 | Hook | 已验证通过 | 实测 12 文件 94 测试一致；四事件字段归属、`validateFor` 越权列名、`normalizeUpdatedInput` 引用相等 + Zod 重解析 + 冻结、合并规则（后覆盖前 / OR / `strongerPermission`）、`hook_stopped_continuation` 补配对、`deferredContext` 后置、`stopHookActive` 单次续跑、五种故障错误码、「只传 hooks 自动补空 PermissionPolicy」均与源码一致 |
| 5 | TODO 计划 | 已验证通过（改） | 实测 14 文件 109 测试一致。`MAX_TODOS=50`、`STALE_TOOL_ROUNDS=3`、`TODO_STALE_REMINDER` 文案、三状态枚举、`transform(trim)→pipe(min(1))` 顺序、整体替换 + 冻结、`serializeSnapshot` ASCII 转义（含代理对分支）、`capabilities.has("todo")`（能力名单数，文件名 `todos.ts`，非漂移）、`ToolRoundObserver` 两方法接线、第 611-617 行五步顺序对应 `loop.ts:159-194`、`effect:"write"` 不触发写审批但留 default allow 审计、集成测试全部断言逐字一致。**修 3 处转义丢失**：原第 98 行 tool result、第 106 行表格、第 1084 行自测题都把 `\uXXXX` 写成了原样中文，与源码及 `ch05-todos.test.ts:88` 矛盾，已按 handler 实际输出改回 |
| 6 | 子 Agent | 已验证通过 | 实测 16 文件 118 测试一致。实跑 `buildAgent(P06)` 探针确认：父 7 工具顺序、子 6 工具无 `task`、`task` 的 JSON Schema（`minLength:1` + `additionalProperties:false`，教程省 `$schema` 属无害简化）、父 history 恰好 `user/assistant/tool/assistant`。四个错误码原文、`maxTurns>30` 构造期拒绝、三道防线、`modelFactoryCalls===0` 的「模型零调用」、`not.toContain("last-tool-result")`、脱敏两断言、`decide(request)` 接口名、审批文案 `User approved this tool call`/`terminal-approval` 均与源码一致。第 668-687 行主动写明「子有 `todo_write` 但无 observer」的不对称，非遗漏。八个单元用例与第 711-718 行逐一对应 |
| 7 | Skill | 已验证通过 | 实测 18 文件 143 测试一致。探针实跑 `buildAgent(P07)`：8 工具顺序、`load_skill` 的 `pattern`/`additionalProperties:false`、初始 prompt 有名称与描述但无正文（`Use pathlib` 为 false）、`history[2]` 与第 123-127 行逐字相同、抛异常的审批器从未被调用。两段提示词原文（`Available workspace Skills are listed below...` / `(No workspace Skills are currently available.)`）一致。错误表 11 行消息原文、五种 errorCode 与文案、四种越界输入的拦截层次全部命中。实验一/二/三输出逐字复现：155 bytes、`.length` 93、51 对 86 条、`maxCatalogBytes:10` 时 catalog 为空串但 `names` 仍在且能加载（正文保留 `\r\n`）、五种坏 frontmatter 均扫描期 `SkillManifestError`、CRLF 兼容通过 |
| 8 | 上下文压缩 | 已验证通过 | 实测 21 files/174 tests。五个实验数字全部复现（42 bytes、预览 3/3/3/6/6、落盘下标 [1]、642→720/143、1029 bytes 一次摘要）。修 3 处：①测试文件表漏 `loop-processors.test.ts`，24→31 用例；②验收契约 6 映射错到 artifact 清理用例，改指 loop-processors 的受控错误用例；③「五种拒绝情形都有测试覆盖」不实，`not valid JSON` 分支无用例，改为标注缺口 |
| 9 | 文件记忆 | 已验证通过 | 实测 23 files/214 tests。五个实验全部复现（103 bytes、194/195 行、1348/1349 字=4047 bytes、C 项子串匹配 database-rule 在前、unrelated 文件名不变、坏 manifest 报错）。TurnLifecycle 三点顺序、两个预算、maxSelected 5/threshold 10、manifest 权威+MEMORY.md 派生、两层锁、三个 NO_FENCE 提示、三个 lastError 串、applyConsolidation 六步提交顺序、ch10 的 emitContextMessages:false 全部对得上。修 2 处：①失败矩阵「每一行都对应至少一个测试」不实，写盘失败→lastError 与 `memory collection changed during consolidation`（memory.ts:347）两行无用例，加「有测试吗」列并写明缺口；②实验五代码片段缺 import 与两行 console.log，输出里的 `manifest =`/`renderCatalog()` 无法复现，补齐并标注同源 |
| 10 | 动态 Prompt | 已验证通过（改） | 实测 25 files/226 tests。六个实验全部逐字复现：9 行/`\n\n` 位置、三阶段 section 生长、两个泄漏检查 false、16 行 context 接受拒绝清单 + identity 三行、7 行缓存序列（含另一 renderer `cacheHits=0` 但文本相同、失败后仍复用）、构建期 Skill 快照（gamma 不出现）、`emitContextMessages` 2 次 vs 1 次、8 个 provider 分支。另实跑核实：`identity` 传 `demo-user`/缺省 → `context:{"chapter":10,"identity":"demo-user"}` / `"user"`，`npm run ch10 -- "…"` 报 `Unknown argument`，六道类型守卫消息逐字一致。五 section 顺序契约、六项缓存键、`!dynamicPrompt` 取反、`SkillRegistry` 构建期快照边界、「不是 API prompt cache」的免责均与源码一致；ch20 追加第 6 个 `runtime_status` section 到尾部，与本章「可选 section 只追加不插队」自洽。修 2 处：①第 1905 行自测题 4（工具变了缓存是否命中）指向实验五，但实验五只讲记忆注入次数、完全不涉及缓存，改指实验一、实验三；②配套代码索引称 `prompting.test.ts` 验证「provider 的失败分支」不实——该文件 7 个 test 声明里 `toThrow` 只有 `PromptContextError` 和 `/cyclic/` 两处 context 校验，九道类型守卫在二十章测试里零覆盖，已加用例数列并补一张缺口表标明各条由哪个实验覆盖。偏差（未改）：开头第 4-11 行的 `.join(" ")` 示意块与 ch09 真实源码（`SYSTEM_PROMPT` + 以 `\n` 开头的 `TODO_SYSTEM_PROMPT`）不同，但原文写了「大概长这样」且第 190 行明确讲了真实三行形状，属正当示意 |
| 11 | API 恢复 | 已验证通过（改） | 实测 28 files/258 tests。**七个实验 + 五处「改一个地方再跑一次」全部逐字复现**：三条路径轨迹（含 `hasAttemptedReactiveCompact = false` 这个反直觉值，第 516-523 行已主动解释）、`[2,2,4,6,8]`、退避 `[0.5,1,2,4,8,16,32,32,32]`/127.5s 与抖动满载 `[0.625,…,40]`/159.375s、14 行 Retry-After 矩阵（含 ISO `Z` 被拒）、14 行错误分类 + 13 行 200 校验、六个 deadline/取消场景、四字段配置表；变体侧两个 529 不切 fallback、`jitterRatio:0`、时限 10→100 得 `[30,30,30]` + 脚本用尽、`length`→`stop` 得 `maxTokens=[8000]`、只改 status 429→529 分类跟着变而只改文案不变。代码侧逐一核对：9 个声明行数（603/158/344/444/263/261/75/919 + 测试 666）全中、`maxRetries: 0`、`src/` 内只有 `openai-chat.ts` import OpenAI SDK、五个可注入边界的默认实现（`performance.now()/1000`、`new Date()`、`sleep`、`Math.random()*upperBound`、`new CancellationToken()`）、`#runBounded` 两段（含 `await operationPromise.catch()` 收束）、`beginTurn()` 六个字段、`complete()` 两道 RangeError 守卫、`mergeFragments` 逐字、`ModelRequestExecutor` 恰两方法、`beginTurn()` 确在 turnLifecycle 之后（loop.ts:172 vs 175）、bootstrap 三道成对检查、`consecutive529` 三处清零点（429/输入过长/成功各一）全部对上。修 2 处：①`recovery.test.ts` 称「666 行，全仓最大的测试文件」不实——666 行只是本章最大，全仓最大是 `ch20-full-harness.test.ts` 1210 行，且该文件自身到 ch20 已长到 683 行，改为「本章最大」；②测试表加用例数列（26/2/6/8/1，合计 43）并补可复制的 `npm exec vitest` 命令，已实跑验证。偏差（未改）：教程的 `scratch` 探针脚本在严格模式下有 2 处类型错误（`exp1` 第 62 行 `content.includes()` 对 `string \| null` 取值、`exp5` 的 `new APIError(status, body: unknown, …)`），但教程给的运行命令是 `npx tsx`（不做类型检查）且 `tsconfig.json` 的 `include` 本就不含 `scratch`，七个实验实跑全过，属「按教程照做能成功」；另路径一与路径二的两段 typescript 片段是源码的语义等价缩写（合并条件分支、省 `this.#` 前缀、省 `validateToolPairing` 一行），与前几章已签收的简化同类。**受限未执行**：第 2421 行「与 Claude Code 的差异」整节引 `learn-claude-code/s11_error_recovery/README.md`，该目录在本仓不存在，CC 侧的 17+ reason/transition、`tokenBudget.ts`、退避公式等声明无法核实 |
| 12 | 任务 DAG | 已验证通过（改） | 实测 31 files/271 tests。章节增量干净：只加 `features/tasks.ts`+`adapters/task-json.ts`+`chapters/ch12.ts`+3 测试，漂移只有 `bootstrap.ts`/`cli.ts`/`profiles.ts`/`profiles.test.ts` 四个接线文件，无 42c0e6f 式非传导。**实测输出几乎全部逐字复现**：11 行 UUID 变形表（1 通过 10 拒绝）、4 行版本位不检查、owner trim、5 条被拒迁移 + 跳过 claim、4 行 ready 派生（含 `未完成依赖=2222,1111` 的顺序）、`TaskBlockedError` 带 blockedBy 且文件字节未变、锁目录三态（进程间不存在/临界区内是目录/结束后消失）、只读不建目录、两种 junction 逃逸、3 行 workspace 非法（空串是 `TypeError` 而非 `TaskStorageError`）、菱形图四步 unblocked（`[endpoints, docs]`/`[tests]`/`[]`/`[release]` 及 lint/tests/release/docs 四个否定断言）、6 行 ID 序排序、原子写失败 + ID 碰撞不覆盖旧字节、4 条创建期拒绝且任务数 0、7 种坏磁盘状态 + 2 种图层损坏、坏文件毒化整张图（`get_task` 读好任务也失败）、13 工具完整顺序、bootstrap 三态、五工具注册顺序、owner 伪造两次均 `invalid_arguments`、正常流程 8 行、schema 层 4 种坏 task_id + `list_tasks` 多余字段、`create_task` 默认值、无锁竞态（两个都「成功」/文件数 1）、临界区成对不交错。代码侧核对：7 行构造校验表的错误**类型**分裂正确（ID 与依赖走 `TaskGraphError`，subject/status/owner 走 `TaskStorageError`）、`CANONICAL_UUID` 正则、四个锁常量 30_000/10_000/10/100、`UTF8_DECODER` 的 `fatal: true`、五个工具 effect（write/read/read/write/write）、`atomicReplace` 四步、`serializeTask` 字母序 + 结尾换行、`taskPayload` 蛇形命名。修 1 处：并发认领两段实测把「谁赢」写成了确定输出（`worker-a 成功`、`最终 owner = worker-0`），实测连跑六次两方赢家在 worker-a/worker-b 之间变、十方在 worker-0/1/2 之间变——根因是 `#preparePaths` 的 `mkdir`/`stat` 异步，到达 `withProcessMutex` 的先后由事件循环决定；已加段说明「变的是赢家、不变的是成功数 1 与错误码集合 `{task_invalid_state}`」，并顺带说明下一节跨进程版实测 4/4 稳定是 `proc-a` 但那只是 spawn 顺序 + Node 启动耗时放大了先后差，不是有序性保证。偏差（未改）：路径一类的若干 typescript 片段省略 `this.#` 前缀或合并分支，与前几章同类简化一致；`task-json.ts` 的 `atomicReplace` 同样是 `rename` 无 Windows 竞态重试，属 P1 已记录的同族潜在暴露（该文件内已有 `isWindowsLockRace`，但只用在取锁路径） |
| 13 | 后台任务 | 已验证通过（改） | 实测 34 files/285 tests。章节增量干净：只加 `features/background.ts`+`adapters/background-json.ts`+`core/events.ts`+`chapters/ch13.ts`+3 测试；`core/loop.ts` 的 ch12→ch13 改动全部合法（`ToolDispatcher`/`RuntimeEventPump`/`AsyncResource` 三个接口、`#seenEventIds`、`#running`/`#closed`/`#closing` 状态位、`close()` 逆序释放 + `AggregateError`、`#acceptNextRuntimeEvent`、两个注入点 273/340、`#executeTool` 的 dispatcher try/catch 兜 `tool_dispatch_error`）。**实测输出几乎全部逐字复现**：开篇 7 条消息轨迹（含 `turns = 3`）、实验 1–7 + 附加实验、两份 shell JSON Schema、schema 隔离 3 种拒绝 + 归一化后参数原文、`concurrency` 的 `background_eligible`/`inline` 分裂、8 条构造不变量、`EventInbox.publish` 3 种拒绝、batch 位置守卫、`BackgroundClosedError \| JobSupervisor is closed`、job_id 三层校验、坏文件毒化整个目录、P13 的 15 工具表、代码索引 11 个行数声明（786/520/100/629/231/317/251/282/599/84/25）全部命中。代码侧核对：`BACKGROUND_MARKERS` 七条、7 个错误类、三方 `Promise.race`、`finishRunning` 返回 `undefined` 做条件迁移、`interruptRunning` 的 `localeCompare` 定序、`ready()` 时序、双向能力位检查（90/94）、子 Agent 硬编码 `false`（155）、Supervisor 同时是 Dispatcher 引用 + `eventPump` + `resources[0]`。修 5 处：①「构造时就校验的五条不变量」实为八条，连带改「用表格看这五条约束」与阅读路线 ② 两处下游引用；②删掉 1475/1477 重复段落；③「这是本章唯一一处 `??`」是错的，实际三处（`background.ts:250`/`background.ts:693`/`background-json.ts:341`），已改为三行表格；④两处失效行号 `background.ts:31`→`19`、`background.ts:733`→`700`；⑤自测映射表 row 4「为什么后台 job 不能做成 Task 的第四个状态」指向「一活五终」，但该节只讲 job 自己六个状态，正面回答在「三种「等待」要分清」（188–192 行），已改指向。补 5 处实测：①不变量表加第三列 8 条构造器原文错误消息；②附加实验补「进程还要再挂 119 秒」的成因 ASCII 轨迹（`control.cancel()`→catch→`abort()`→operation 不理 signal→`await execution.catch` 卡住→`finally` 到不了→`timeoutHandle.cancel()` 没执行→120s 定时器挂在事件循环）+ 实测 `timeoutMs: 3_000` 变 3.0 秒退出；③补全被省略的毒化报错 `Error [background_storage_error]: Background job file is invalid: 00000000-0000-4000-8000-0000000000ff.json` 并注明 `listJobs` 同样行为；④代码索引测试行补用例数（12/1/1=14）与实跑命令；⑤开篇「三次模型请求」补注：那是主回合数即 `RunResult.turns`，在 `ModelClient.complete()` 上挂计数器会数到 4，多出的一次是第 9 章记忆提取 side query（`tools` 空、system 是「从会话中提取值得跨会话保留的新记忆」），与后台无关。五道验证门实跑全绿（`test:ch13` 34/285、全量 65/428、`typecheck`/`lint`/`format:check`/`build` exit 0，`dist/` 已被 `.gitignore` 覆盖）。偏差（未改）：`background-json.ts` 的 `atomicReplace` 仍是裸 `rename` 无 Windows 竞态重试，同 P1 已记录的同族潜在暴露。`受限未执行`：`ai-agent-book/`、`learn-claude-code/` 本地不存在，「与 Claude Code 的差异」表与整节「从 ai-agent-book 的 Flux 实验中学到什么」（含 INTERRUPT/IMMEDIATE/DEFERRED 分类、suspended vs interrupted 对照）无法核对，同 ch11 |
| 14 | Cron | 已验证通过（改） | 实测 39 files/318 tests（与基线及教程第 3 步声称一致，含 `配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL` 那行预期 stderr）。章节增量干净：只加 `features/cron.ts`+`adapters/cron-json.ts`+`chapters/ch14.ts`+5 测试。`core/loop.ts` 的 ch13→ch14 改动全部合法且都是本章主题：`acknowledgeEvents` 返回类型放宽为 `void | Promise<void>`、新增 `#deferredRuntimeEvents`、`runEvents()` 事件专用回合、`#runUserTurn` 收 `runtimeEvent` 参数并据此构造 `identity`/`idempotencyKey`、`#acceptNextRuntimeEvent` 改签名加 `allowContextEvents` 并从「整批 drain」改「`drainEvents(1)` 单条」。**实测输出几乎全部逐字复现**：开篇轨迹＝实验 9（4 条消息、2 次请求、`identity=cron-owner` 而 Runner 自身是 `interactive-user`、`idempotencyKey`＝event_id、outbox 0，注入 JSON 的 8 个字段顺序一致）、实验 1 的 7 条拒绝消息（含本章自加的「恰好五段」与 `Invalid descending Cron range`）、实验 2 的 4 个时区 + DST gap 推进到 `2026-03-08T07:00:00.000Z` + fold 两个 instant `05:30Z`/`06:30Z` + `Unknown Cron timezone: Mars/Olympus`、实验 3 两份 `state.json` 全文（含字段顺序）及「`recurring: false` 后 `jobs: []` 而 outbox 事件仍在」的变体、实验 4 七行（`ack 第二次 -> false`）、实验 5「停机三天只补 1 个、slot 是最早那个」及三十天仍 1 个的变体、实验 7「容量 1 时 second 的 next `原地不动: true`，ack 后再 tick 才产生」、实验 8 leader 交接（A 拿 0501、B 空、A 关闭后 B 拿到同一个 0501）及 `durable: false` 三行全空的变体、实验 10 七行（用户回合结束时 cron 事件未进历史、identity 未被污染、再调 `runEvents()` 返回 `undefined`）、实验 11（`P14 能力数 = 17`、`P14 - P13 = ["cron"]`）、实验 12 五行锁生命周期及注释掉 `releaseLeader()` 的变体。代码侧核对：`scheduleCronSchema` 五字段全必填 + `.strict()`、工具 description 与 `effect: "write"` 逐字一致、四个错误类与错误码（`cron_expression_error`/`cron_storage_error`/`cron_job_not_found`/`cron_closed`）、`cron-parser` 锁定 `5.6.2`、三层锁参数 30_000/10_000/10/100、ack 失败回滚就是 `loop.ts:635-636` 的 `#history.pop()` + `#seenEventIds.delete()`、`CronEvent.contextIdentity` 只由 cron 设（故后台事件不走延后分支）。自测映射表 8 行目标小节全部存在且对题。修 6 处：①②③工具数 14→16 共 4 处 + 16 工具表重排 + 代码索引三个行数（`background.ts` 632→726、`bootstrap.ts` 330→338、`ch14-bootstrap.test.ts` 132→134），均属 P4 补回后台工具的连带更新；④1009 行「后台事件可以批量注入，带 `batch: {index, total}`」是错的——ch14 已整体删掉 `RuntimeEventBatchPosition`，两类事件同走 `drainEvents(1)`，已改成正面交代这次反向变化；⑤自测第 8 题沿用了同一个错误前提（「cron 不能批量而第 13 章后台可以」），已改问「批量为什么被整体退掉、退掉后后台是否也一次一条」；⑥实验 6 标称「实测输出」5 行，但贴出的脚本只有 3 个 `console.log` 且丢弃了第二个 `scheduleCron` 的返回值，照抄得不到前两行，已补 `const session =` 与两行打印（5 行值本身实测全对）。⑦代码索引里 `core/events.ts` 的角色写「本章不改，只复用」，实际退掉了批量注入，已改为写明。`受限未执行`：「与 Claude Code 的差异」表与「从 ai-agent-book 的 Flux 实验中学到什么」整节（含 Flux 批量追加、checkpoint/suspended 对照）无法核对——`ai-agent-book/` 本地不存在，同 ch11/ch13 |
| 15 | Teammate/Mailbox | 已验证通过 | 实测 44 files/335 tests，与基线一致。教程 2784 行。章节增量干净：新增 `mailbox.ts`/`mailbox-json.ts`/`teammates.ts`/`ch15.ts` + 5 测试，7 个老文件漂移全部合法（`profiles.ts` +2 能力位、`bootstrap.ts` 双向检查 + 工厂接线、`loop.ts` 新增 `RunOptions`/`#pendingEventAcks`/mailbox 延后逻辑、`tools.ts` 新增 `subset`、`cli.ts` 关闭改 AggregateError、`recovery.ts`/`recovery.test.ts` 加 signal 取消支持）。代码索引 9 个行数声明全中（242/586/598/714/403/323/306/5，测试 181/343/170/104/66）。核心机制：每消息一文件 + 目录名即状态（`ready/processing/done/quarantine`）、FIFO 排序键 `(created_at_utc, id)`、至少一次投递（processing 重启恢复、坏消息隔离）、history-before-ack（mailbox 是唯一保留半状态的事件类型，`#pendingEventAcks` 防重复）、四道配对检查（teammate 必须共享 inbox/supervisor/cron）、关闭按逆序释放资源。Lead 18 工具（P14 前 16 + `spawn_teammate`/`send_message`），队友 4 工具（`shell`/`read_file`/`write_file`/`send_message`，无 `spawn_teammate`/Task DAG/Cron）。配套测试覆盖：状态迁移、FIFO、并发、幂等 ack、quarantine、跨 runtime 恢复、ack 失败重试、mailbox 延后、工具面裁剪、配对检查。偏差：无（教程探针脚本 `probe-ch15.mts` 是临时说明文件，非源码一部分）。**未验收**：15 个实验输出（教程给出预期输出，但未实跑探针脚本）、与 Claude Code / ai-agent-book 的对比章节（引用外部仓库 `learn-claude-code/s15_agent_teams/README.md` 与 `ai-agent-book/chapter10.md`，本地不存在） |
| 16 | 协议与计划门控 | 已验证通过（改） | 实测 49 files/348 tests，与基线及教程第 3 步声称一致（含 `配置错误: Missing required settings: ...` 与 `运行失败: CLI execution or cleanup failed` 两行预期 stderr）。教程 3569 行，全仓最长。章节增量干净：新增 `features/protocol.ts`/`adapters/protocol-json.ts`/`chapters/ch16.ts` + 5 测试（`ch16-{protocol,plan-gate,runtime,bootstrap,entry}.test.ts`）。老文件漂移全部合法且都是本章主题：`profiles.ts` 新增 P16（246-249 行，`[...P15.capabilities, "protocol", "plan_gate"]`）+ `profileForChapter` 分支、`bootstrap.ts` 四道配对检查（123-137）+ `basePermissionPolicy`/`teammatePermissionPolicy` 拆分（167-173）+ 队友 factory 追加 `submitPlanToolDefinition`、`cli.ts` 构造 `JsonProtocolStore`/`ProtocolRuntime`、`mailbox.ts` 加 typed 协议信封、`mailbox-json.ts` 加 `sendProtocol`、`teammates.ts` 加 `deliverProtocol` + 协议路由 + 失败分类、`permissions.ts` 加 `withRules()`。`core/loop.ts` ch15=ch16 逐字相同（714 行），教程对它只做解释性引用，行号 640/646/668/673 全部对得上实际代码。代码索引 12 个 src 行数 + 5 个测试行数全中（463/715/461/635/768/365/410/714/304/71/322/5，测试 162/63/264/137/28）。核心机制核对：`ProtocolRequest` 9 个持久字段与教程列表逐字一致；默认 TTL `300_000`（`protocol-json.ts:96`）；单快照 `version: 1` 且最外层只允许 `version`/`requests` 两键（645-650）；六个错误码 `protocol_{storage_error,mismatch,not_found,state_error,expired,delivery_error}` 与全部错误文案逐字对上；确定性 prompt `Plan approved (id). Proceed with the approved plan.` / `Plan rejected (id). Feedback: ...`、固定文案 `Ready to shut down.` / `Graceful shutdown requested.` 全部在 `protocol.ts:233,286,300,301`；`planAllowsEffectful` 判定就是 `latest === undefined || latest.status === Approved`（222-226）；`#requiresPlanApproval` 三条豁免 `effect==="read"`/`send_message`/`submit_plan`（381-393）；plan gate 只挂队友侧（`bootstrap.ts:169-173` 注释明写「Lead 仍使用原策略」）；`idle` 唤醒在 `teammates.ts:344-348`（store 层不唤醒）；协议信封三条构造期校验（`mailbox.ts:157,213,229`）。修 1 处：3447 行「P16 Lead 与队友的工具集合精确相等（18 / 5）」中 Lead 数错，18 是 ch15 的值（从 ch15 复制未更新）——实跑 `buildAgent(P16, ...)` 得 Lead 20 个、最后四个 `["spawn_teammate","send_message","request_shutdown","review_plan"]`、`Lead 有 submit_plan = false`，且教程 329/334/3243 三处本来就写 20，已改为 20 / 5。另实测 `P15 能力数=19 P16=21`，与「P16 = P15 + 两个能力位」相符。**未验收**：14 个实验输出（教程给出预期输出，但未实跑 `probe-ch16.mts`；仅实验 14 的工具面部分被我用等价探针复核）。`受限未执行`：「与 Claude Code 的差异」（引 `learn-claude-code/s16_team_protocols/README.md`）与「从 ai-agent-book 学到什么」整节（引 `ai-agent-book/chapter10/parallel-web-research/README.md`、`message_bus.py`）无法核对——两个外部仓库本地不存在，同 ch11/ch13/ch14/ch15 |
| 17 | SQLite 认领 | 已验证通过（改） | 实测 54 files/368 tests，与基线一致；单跑本章 5 个测试文件得 `5 passed (5)` / `20 passed (20)`，与教程声称一致（task-sqlite 8、runtime 8、work-stealing 2、bootstrap 1、entry 1）。教程 2392 行。章节增量干净：新增 `adapters/task-sqlite.ts`/`features/work-stealing.ts`/`chapters/ch17.ts` + 5 测试 + `tests/fixtures/`。老文件非注释漂移：`teammates.ts` +126（idle loop 四级优先级 + `configureWorkStealing` + `idleTimeoutCount`/`waitForIdleTimeout`）、`bootstrap.ts` +50（`usesJsonTaskStore` + 双向能力位检查 + 三侧工具注册）、`cli.ts` +11、`profiles.ts` +11（P17 = P16 + `task_dag_sqlite`/`work_stealing`）、`mailbox-json.ts` +8（锁符号链接检查改 `try{lstat}catch(ENOENT)`），其余为 0。src 总行数 14664→16027。核心机制核对：四张表 DDL 与教程表格逐条一致（`task_metadata` 仅 `schema_version='1'` 无隐式升级、`tasks` 用 `sequence INTEGER PRIMARY KEY AUTOINCREMENT`、`task_dependencies` 带 `position` + 双外键、`task_claim_tokens` `token TEXT PRIMARY KEY` 全生命周期唯一）；三态 `CHECK` 约束三个分支逐字对上（`completed` 保留 owner 但清 token/lease）；`#claim` 里 `INSERT INTO task_claim_tokens` 在条件 UPDATE **之前**，UPDATE 带 `WHERE id=? AND status='pending' AND owner IS NULL` + `getRowsModified() !== 1` 判定；三层互斥 `withProcessMutex` → `acquireFileLock(retries: 0, stale: LOCK_STALE_MS=30_000)` → `BEGIN IMMEDIATE`，COMMIT 后才 `persistDatabase` 原子落盘；租约半开区间双口径一致（`:236` 的 `now >= lease` + `releaseExpired` 的 `lease_expires_at_utc <= ?`），测试证实 `+30_000ms` 时原 owner 得 `TaskLeaseExpiredError`、任务自动回 pending 让 bob 拿到新 token、旧 token 转 `TaskClaimError`；五工具 effect 分布正确（`get_task`/`list_tasks` read，其余 write），`definitions.slice(1)` 砍掉 `create_task` 得队友四工具；wire format snake_case（`claim_token`/`lease_expires_at_utc`）；`renderClaimPrompt` 三行 `<auto-claimed-task>` 与实验 10 输出逐字符对上，claim token 复用为 `idempotencyKey`；idle loop 四级顺序实测为 ① `store.claim` Mailbox ② `#workStealingRuntime === undefined` 直接 Idle ③ `planAllowsEffectful` 为 false 零写入跳过 ④ `claimNext`；有界 polling 的唤醒 `continue` 在 `idlePolls += 1` **之前**（`teammates.ts:632-645`）；默认 `pollIntervalSeconds: 5` / `maxIdlePolls: 12` 与 `claimService.store !== options.store` 构造期检查在位；配置顺序 `configureProtocol` → `configureWorkStealing` → `configureRunnerFactory`；三侧注册齐全（Lead `:221`、subagent childTools `:242`、teammate 工厂 `:282` 用 `registerTeammateLeasedTaskTools`）。教程引用 `teammates.ts:231/573/632` 三处全部正确。修 5 处：①②`task-sqlite.ts` 行数 761→776（正文两处 + 代码索引表）；③`tasks.ts` 行数 328→336；④符号链接拒绝行号 `:548`→`:563`、`nlink !== 1` 行号 `:562`→`:577`（偏移 +15 与文件增长量一致，说明教程定稿后文件加了 15 行导致靠后引用整体失效）；⑤路径校验区间 `第 400-570 行`→`第 374-581 行`（实际 `#preparePaths` 起 374、`validateDatabaseFile` 止 581）。`:409`/`:417`/`:236`/`:453` 原本正确未动。typecheck 通过。**发现真实代码缺陷（非教程问题，未修）**：`task-sqlite.ts:417` 先 `pathExists(paths.lock)` 再 `lstat(paths.lock)`，跨进程测试中另一进程的 `proper-lockfile` 可在两调用间删掉 `.lock`，`lstat` 抛 `ENOENT` 被 `:401` 包成 `TaskStorageError`——同仓 `mailbox-json.ts:327-333` 已是正确写法 `try{lstat}catch(ENOENT){}`；ch18/ch19/ch20 的对应位置继承同一 TOCTOU 写法。该失败偶发，8 次单文件 + 2 次全量重跑未复现。导读/路线/正文三处「11 个实验」与实验区实际 11 条一致（先前疑似的 9 vs 11 冲突不成立）。**未验收**：11 个实验输出（未实跑探针脚本；仅实验 3/6/8/10 的关键断言用测试与源码等价复核） |
| 18 | Worktree | 已验证通过（改） | 实测 59 files/394 tests，与基线及教程 3035-3036 行声称一致（三行预期 stderr：`workspace is not a Git repository root`、`CLI execution or cleanup failed`、`Missing required settings`）；单跑本章 5 个测试文件得 `5 passed (5)` / `26 passed (26)`，与教程 3048-3056 行一致。教程 3070 行。章节增量干净：新增 `features/worktrees.ts`(989)/`adapters/git.ts`(74)/`chapters/ch18.ts`(5) + 5 测试（`ch18-{worktrees,bootstrap,loop-workspace,claim-routing,entry}.test.ts`）。增量表 11 个行数声明**全中**——口径是 `git diff --numstat` 的「加+删」而非 GNU diff（`task-sqlite.ts` 526、`loop.ts` 129、`teammates.ts` 109、`bootstrap.ts` 57、`cli.ts` 46、`tools.ts` 39、`profiles.ts` 31、`subagents.ts` 27 逐个复核吻合；GNU diff 会少算 CRLF 相关的 17/4/1 行，故用 git 口径核对）。`task-sqlite.ts` 776→1190（+414）属本章主题内的合法增长（两张新表 + DDL + 两个 append-only trigger + 五个迁移方法 + `claimNextBound`），非另起一套实现。代码索引 16 个行数声明 15 中。核心机制核对：`ToolContextProvider.resolve()` 每次工具调用都走，位置为 `tools.prepare`(schema) → `#resolveToolContext`(cwd, `loop.ts:532-548`) → `runPreTool` → 权限 → handler，解析失败返回 `tool_context_error` 且 handler **零调用**、带配对 tool_result；Loop 不信任 provider 的三道检查（`identity`/`idempotencyKey`/`executionScope` 不可变、`realpath` + `isWithin(workspaceRoot)`、重新 `Object.freeze`）逐字对上 `loop.ts:621-644`——Loop 管「出不去」，provider 管「去哪里」；`executionScope = Object.freeze({})` 每轮一个、作为 `WeakMap`(`#scopeClaims`) 键使得同一条回复里 `claim_task` → `write_file` 共享路由，用 WeakMap 而非 Map 避免只增不减的泄漏，显式 `claimToken` 优先级高于 scope（跨回复恢复 vs 同回复关联），`complete_task` 清 scope；`#explicitContextToken` 返回可辨识联合（`{required:true,value}` / `{required:false}`）；fail-closed 语义正确——无 claim 走主 workspace（正常），claim 无效抛 `WorktreeContextError` 绝不回退，`lookupClaim` 区分「从未存在」(`undefined`) 与「存在但已失效」(`TaskClaimError("claim token is known but no longer active")`)；`claimNextBound` 对无 active binding 的任务是**跳过而非报错**（`task-sqlite.ts:347-373` 逐字核对），未绑定的 ready 任务因此永远停在 pending，属刻意设计；八步移除证明与任一步失败转 `needs_review` + 固定英文原因（绝不透出 Git stderr）齐全，末三步为 `git switch --detach` → `git branch -d`（非 `-D`）→ `git worktree remove`（非 `--force`），包含性用 `git merge-base --is-ancestor`、干净性用 `git status --porcelain=v1 --untracked-files=all`；`#transitionWorktree` 是唯一迁移入口，条件 `UPDATE ... WHERE task_id=? AND status=?` + `getRowsModified() !== 1` + 同事务 `appendWorktreeEvent`；`validateRepository()` 要求恰好是仓库根（`repositoryRoot !== root` 即抛），`#repositoryValidated` 缓存，CLI 在构造任何 runtime 状态前调用。实测确认全 `ch18/src` 零 `--force` / 零 `-D` / 零 `discard_changes`；三个 Worktree 工具仅 Lead 可见（`bootstrap.ts:279` 是唯一注册点），effect 分布 external/write/external；`bootstrap.ts:115-125` 引用相等校验错误文案 `worktreeRuntime must be the WorkStealingRuntime claim service and share its store`。修 3 处：①1286 行 `encodeBinding` 片段 `branch: binding.branchTip` → `branch: binding.branch`（源码 `worktrees.ts:967` 为后者，且教程自己的示例输出 `"branch": "wt/alice"` 与错误片段自相矛盾）；②1516 行「八条 review reason」→「十条」并补回漏掉的 `keep_worktree` 那条 `"managed worktree path or branch tip could not be resolved"`（源码实有 10 条，位于 `worktrees.ts:418/446/453/461/468/481/492/500/508/516`）；③代码索引 `task-sqlite.ts` 1175 → 1190。**我自己的错误推断已排除**：曾疑 1730/1731 行把非 Git 目录的报错写成 `workspace is not a Git repository root` 有误，本地探针（`SqliteTaskStore` + `SubprocessGitRunner` + `WorktreeRuntime`，无 model 无 `--prompt`）证明教程正确——Windows 上 `%TEMP%` 的某个祖先是 Git 仓库，故 `--is-inside-work-tree` 返回 true、`--show-toplevel` 指向该祖先，执行落到 `repositoryRoot !== root` 分支。未改教程，探针已删。行号引用 `loop.ts:355`(`toolContextScope`)、`bootstrap.ts:106-123`、`#executeTool` 528-570、`#resolveToolContext` 621-644 全部正确；worktree DDL/trigger 教程写 735-780、实际 736-779，两端各差 1 行但完整覆盖真实范围，未改。**继承 ch17 的真实代码缺陷（未修）**：`task-sqlite.ts:658` 同样是先 `pathExists(paths.lock)` 再 `lstat` 的 TOCTOU 写法。**未验收**：教程实验输出（未实跑探针脚本；关键断言用测试与源码等价复核） |
| 19 | MCP | 已验证通过（改） | 实测 63 files/414 tests，与基线及教程 1370-1371、2769-2770 两处声称一致（四行预期 stderr：两次 `workspace is not a Git repository root`、`CLI execution or cleanup failed`、`Missing required settings`）。教程 2916 行。章节增量：新增 `features/mcp-tools.ts`/`adapters/mcp-client.ts`/`adapters/mcp-schema.ts`/`mcp-servers/demo.ts`/`chapters/ch19.ts` + 4 测试（`ch19-{registry,mcp,bootstrap,entry}.test.ts`）。按教程 1378/2779 行给的命令单跑这 4 个文件得 `4 passed (4)` / `20 passed (20)`，与教程「共 20 个测试」一致。代码索引 14 个行数声明 13 中。核心机制核对：六个暴露名前缀规则 `mcp__<alias>__<tool>`；四步归一化（转小写 → 非法字符转 `_` → 折叠连续 `_` → 去首尾 `_`）+ 64 字符上限逐字对上源码，归一化后空串报 `mcp_invalid_tool_name`、归一化后撞名报 `mcp_name_collision` 且**拒绝整个 connection**（不加数字后缀、不覆盖旧定义）；Ajv 四道检查（`strict: true` 能编译、顶层必须 `type: "object"`、三个 dialect 白名单、`SCHEMA_REFERENCE_KEYS` 三个关键字禁外部 `$ref`）与教程表述逐字一致；`registerMany` 原子性「先全验再全写」+ 对象身份比对 `current !== stored`。三个实验用等价探针**实测复现**，输出与教程逐字符吻合：实验 3（atomic）8 行、实验 6（schema）10 行、实验 7（name）6 行——其中 schema 步骤 [8] 证实三次坏参调用后远程 `callTool` 计数为 0（校验在本地 `prepare()` 阶段，坏参不出网），[10] 合法参数后才变 1。教程贴出的探针代码字段名与构造器签名全部可直接复制运行（`WorktreeRuntime({workspace, store, gitRunner})`、`toolCall` 来自 `core/messages.js` 而非 `core/tools.js`，均正确）。「读代码建议顺序」11 处行号全中（值对象 49/82/148/181、`#connect` 412、`#publishDefinitions` 516、`#invokeRemote` 591、`#dropFailedConnection` 642、`createMcpRuntime` 256、`loop.ts:406`）。修 6 处（`git diff` 复核）：①代码索引 `ch19-mcp.test.ts` 行数 363 → 365；②1152 行代码注释 `// bootstrap.ts:345-349` → `353-357`；③1205 行 `// bootstrap.ts:373-379` → `381-387`；④2747 行「读代码建议」第 11 条同两组行号一并更新；⑤⑥**两处 P4 遗留漏改**——1184/1190 行实验输出与叙述里 Lead 工具数 23 → 25（P4 补回 `query_background_job`/`cancel_background_job` 后实际为 25，`ch20-full-harness` 的 `LEAD_TOOLS` 断言可证），代码索引 `bootstrap.ts` 行数 493 → 501 并同步其中四组行号（111-116/345-349/373-379/473-486 → 115-120/353-357/381-387/481-494）。**这与 P4 记录里「ch19（493→501，四处行号区间 +4/+8）」矛盾**：那一轮声称已改，实际未落盘，本次才真正补上——P4 条目中 ch14–ch18/ch20 的同类声明需复核是否也有未落盘项。另修：动态注册五步顺序的 `text` 块把 ① ② 倒了——源码是**先** `new Set(names)` 批内唯一（151-154）**再**循环 `validateDefinition` + `has()`（155-160），教程紧接的代码片段顺序本来正确，仅上方描述块需改，已改并顺延 ④ ⑤ 编号。**发现跨章语义冲突（已修，记为 P5，详见 P5 节）**：ch18 的 `#scopeClaims` 在 `complete_task` 后清除 scope，ch19/ch20 删掉了该清除，两版语义相反。已采纳方案①把 ch19 写法回移 ch18，三章 `worktrees.ts` 逐字节相同（979 行），ch18 教程 948 行已改正面叙述。**继承 ch17 的真实代码缺陷（未修）**：`task-sqlite.ts:658` 同为先 `pathExists(paths.lock)` 再 `lstat` 的 TOCTOU 写法。`受限未执行`：`npm run ch19` 走 `runProfile` 需真实 config，因安全约束（`code/.env` 有真实凭据）未跑；教程另 7 个实验输出未实跑探针 |
| 20 | 完整 Harness | 已验证通过（改） | 实测 65 files/428 tests，与基线及教程声称一致（五行预期 stderr：三次 `workspace is not a Git repository root`、`CLI execution or cleanup failed`、`Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL`）；单跑本章 2 个测试文件得 `2 passed (2)` / `11 passed (11)`，与教程一致。教程 3878 行。**本章是唯一的净减行章节**，需专门排查：`profiles.ts` 326→152（−174）、`mcp-tools.ts` −50、`mcp-client.ts` −12、`mcp-schema.ts` −3、`demo.ts` −2、`tools.ts` −1。逐一定性：`profiles.ts` 是把 20 个手写 profile 常量换成 `PROFILE_DELTAS` 增量表 + `PROFILES.map` 推导，**机械地强制了「每章是前一章严格超集」这条不变量**，属本章主题内的合法重构而非能力删除；其余 5 个文件用「剥注释后逐字比对」证明**纯注释删除、逻辑字节一致**。真实增量落在四处：①`full_harness` 标记能力（`Capability` 联合加一项，不注册任何运行时、不加任何工具）；②`prompting.ts` 252→274 新增 `DynamicPromptStatusProvider` + `status` 折进 `stableJson` 缓存键 + 渲染出 `## runtime_status` 段——这是本章唯一实质新功能；③`loop.ts` 781→789 把 ch19 内联的 36 行请求组装（369-409）抽成 `#prepareModelRequest()`；④`bootstrap.ts` 501→525、`cli.ts` 403→447 装配 + 新增 3 文件（`chapters/ch20.ts` 5 行、`ch20-full-harness.test.ts` 1212 行、`ch20-entry.test.ts` 65 行）。核心机制核对：`profile.capabilities.has("full_harness")` 是全代码库唯一读点（`bootstrap.ts:287-293`），P19 及之前走 `{}` 分支故旧章节提示词零字节变化、旧测试全部继续有效；`statusProvider` 是**函数**而非值，`render()` 每次重调，故 MCP 连接与后台状态按请求刷新；`fullHarnessRuntimeStatus()`（81-93）只读三个同步 getter（`backgroundSupervisor`/`cronRuntime`/`teammateRuntime` 的 `hasPendingWork` + `mcpRuntime.connectedAliases`），零副作用，且教程明确「状态栏不替代 EventInbox」；`#prepareModelRequest` 内 505 渲染提示与 511 拍工具快照仅隔六行，保证同一请求的状态面与工具面同源。**教程第 11 节自陈的行为回退经实测确认为真**：ch19 `bootstrap.ts:481-494` 有 `mcp-external-approval` 审批规则，ch20 `509-525` 只剩一条，三条证据（ch19 三个 `asks before` 测试在 ch20 归零、`ch19-bootstrap.test.ts` 测试数 6→3、两章 `bootstrap.ts` 规则块逐字对比）全部成立，教程两个代码块与源码逐字吻合。实验实测复现：实验 1（能力集 26 项）6 行、实验 3（`runtime_status`）8 行、实验 7（close）6 行，均与教程逐字符吻合。25 个工具名与注册顺序逐字对上 7.1 节表格（`LEAD_TOOLS` 断言可证），4.1 节顺序图 32 处行号全对。修 17 处：①②③三处文件行数（`ch20-full-harness.test.ts` 1210 → 1212，出现在正文/增量表/代码索引三处）；④⑤整章统计 18573 → 18706 源码行、15541 → 15547 测试行（教程写成后代码又增长，与 1210/1212 同源）；⑥`bootstrap.ts` 493 → 517 改为 501 → 525（**教程内部原本自相矛盾**：3508 行写 493→517，3605 行写 501→525）；⑦⑧⑨⑩⑪⑫⑬⑭六组 `bootstrap.ts` +8 行号偏移（`fullHarnessRuntimeStatus` 78-96 → 78-93、装配段 279-296 → 280-297、协议两组工具 197-202 → 205-210、队友显式列举 210-224 → 218-232、队友多一条规则 115-118 → 119-122、`resources` 293-299 → 302-308、`validateBuildDependencies` 330-438 → 338-446）；⑮`#prepareModelRequest` 区间末行 475-532 → 475-516（532 实为 `#executeTool` 起始行，索引表与阅读顺序两处同错）；⑯测试文件内部结构 735/736 → 737/738、第 6 个测试 1035-1135 → 1037-1137；⑰4.1 节顺序图把 511 拍快照画在 510 配对校验之前，与源码顺序相反，已调正。**P4 遗留漏改已在本轮补齐并复核**：ch14–ch18 的 `bootstrap.ts` 行数声明（338/403/410/445/476）与 ch14 的 `background.ts` 726、测试 134、ch18 三校验行号 106-123 全部已正确落盘，P4 的未落盘项仅限 ch19 与 ch20 两章，均已修完。**注释密度回退（已修，记为 P6，详见 P6 节）**：本章删掉 ch19 约 68 行解释性注释、跨 5 个文件、逻辑零变化。已整文件回填，5 个文件与 ch19 逐字节相同；ch20 教程 11 节「第一类」块与全部行号引用已同步重对（2026-09-02 五道门全绿）。**继承 ch17 的真实代码缺陷（已修）**：`task-sqlite.ts` 的锁目录 `pathExists`+`lstat` TOCTOU 已换为 `lstatIfExists` 单调用写法。`受限未执行`：`npm run ch20` 需真实 config，因安全约束（`code/.env` 有真实凭据）未跑 |

## 各章测试基线（孤立运行，已实测）

| 章 | 测试文件 | 测试数 |
| ---: | ---: | ---: |
| 1 | 6 | 30 |
| 2 | 8 | 47 |
| 3 | 10 | 61 |
| 4 | 12 | 94 |
| 5 | 14 | 109 |
| 6 | 16 | 118 |
| 7 | 18 | 143 |
| 8 | 21 | 174 |
| 9 | 23 | 214 |
| 10 | 25 | 226 |
| 11 | 28 | 258 |
| 12 | 31 | 271 |
| 13 | 34 | 285 |
| 14 | 39 | 318 |
| 15 | 44 | 335 |
| 16 | 49 | 348 |
| 17 | 54 | 368 |
| 18 | 59 | 394 |
| 19 | 63 | 414 |
| 20 | 65 | 428 |

## 发现的问题与处理

### P1 全量测试不稳定（已修，12/12 通过）

排查时逐层剥出**四个互不相关**的原因，不是同一个问题的四种表现。修完连跑 12 次全量 `428 passed`，二十章孤立基线与上表逐行一致。

| # | 位置 | 现象 | 根因 | 处理 |
| --- | --- | --- | --- | --- |
| 1 | 无 `vitest.config.ts` | `ch15-mailbox` / `ch17-runtime` / `ch18-bootstrap` 报 `Test timed out in 5000ms` | 仓库从未配置 vitest，用默认 5s；测试大量使用真实文件系统、`proper-lockfile`（100×10ms）、sql.js 落盘、git 子进程，65 文件并行时单用例墙钟远超 5s | 新增 `code/vitest.config.ts`，`testTimeout`/`hookTimeout` = 30s |
| 2 | `ch{17,18,19,20}/tests/ch17-runtime.test.ts:255` | `coordinates two claims...` 间歇失败 | `waitForEventWithin` 的挂起守卫硬编码 1s，`collectEvents` 每等一个事件都受它限制 | 放宽到 15s（低于框架 30s，超时仍先命中自定义诊断信息） |
| 3 | `ch{17,18,19,20}/src/adapters/task-sqlite.ts` 的 `persistDatabase` | `ch20-full-harness` 的 `gates teammate effects...` 报 `expected 'Error [tool_context_error]...' to contain 'Wrote'` | **真实代码 bug**：Windows 上 `rename(temp → tasks.sqlite3)` 返回 `EPERM`，事务已 `COMMIT` 但落盘失败，后续 `lookupClaim` 变成 `claim token is known but no longer active` → `tool_context_error`。同文件取锁路径早有 `isWindowsLockRace`（EBUSY/ENOTEMPTY/EPERM）重试，落盘路径漏了 | 用同文件已有的 `isWindowsLockRace` + `LOCK_RETRY_MS` + `MAX_WINDOWS_LOCK_RACE_RETRIES` 给 `rename` 补重试，不新增机制 |
| 4 | `ch{19,20}/tests/ch19-mcp.test.ts:247` | `expected 'mcp_timeout' to be 'mcp_connection_lost'` | 三个错误映射用例共用 `demoSpec(..., 0.02)` 的 20ms 工具超时。`terminate` 靠子进程 `process.exit(17)` 触发连接断开，负载下检测慢于 20ms，`mcp_timeout` 抢先命中；`fail` 同样有 20ms 内往返的风险 | 改成按用例带超时：只有 `delay`（100ms 延迟）保留 0.02s，`fail`/`terminate` 用 2s |

定位 #3 的方法：在 `ch20/src/core/loop.ts` 的 `catch` 里临时加 `console.error` 打印被吞掉的 `error`，循环跑全量直到复现，拿到完整 cause 链后撤除探针（`git diff` 已确认无残留）。

**同类暴露，未改（超出本次范围，留给人决定）**：

- `memory.ts` 的 `atomicReplace`（ch09–ch20 共 12 份）、`mailbox-json.ts` 的 `atomicWrite` / `move`（ch15–ch20）、`task-json.ts` 的 `atomicReplace`（ch12–ch20 共 9 份）形状与 `persistDatabase` 相同，`rename` 同样无 Windows 竞态重试。`task-json.ts` 尤其明显：同文件已定义 `isWindowsLockRace`，但只用在取锁路径，落盘路径没用。三者在 30+ 次全量里从未复现（harness 对 SQLite 文件的写入频次远高于它们），属潜在暴露而非实测缺陷。

辅助脚本（未跟踪，投放于 `code/scripts/`，按约定保留待人决定清理）：`backfill-no-fence.mjs`、`widen-runtime-guard.mjs`、`fix-persist-rename-race.mjs`、`fix-mcp-case-timeouts.mjs`。

### P2 ch09 的提示词修复未传递到后续章节（已修）

- commit `42c0e6f`「修复Bug」给 ch09 的 `memory.ts` 加了 `NO_FENCE_INSTRUCTION`（三个 side-query 系统提示都追加「直接输出纯 JSON 文本，禁止使用 Markdown 代码块或反引号包裹」）和 extractor 的 slug 规则行，但只改了 ch09。
- 累积快照的不变量是「第 N 章 = 第 N-1 章 + 恰好一个新能力」，ch10–ch20 全部缺这 11 行，导致 ch09 教程第 448–449 行讲的常量在后 11 章代码里根本不存在。
- 处理：`scripts/backfill-no-fence.mjs` 回填 ch10–ch20，每章 +11/-3，与 `42c0e6f` 完全同形；回填后各章该区块与 ch09 逐字一致。

### P3 ch13–ch20 的任务工具描述比 ch12 短（已修）

- ch12 的 `features/tasks.ts` 给五个任务工具写的是长描述，其中两条含关键约束：`claim_task` 的 `Owner is set by the runtime identity; do not pass an owner argument.`、`create_task` 的 `blocked_by must contain canonical task UUIDs returned by list_tasks or get_task.`。ch13–ch20 全部是一版更短的描述，把这些约束丢了。
- 违反累积快照不变量（第 N 章 = 第 N-1 章 + 恰好一个新能力），且 **ch12 教程第 1270 行逐字引用了 `Owner is set by the runtime identity; do not pass an owner argument.`**，读者翻到 ch13 的代码会搜不到这句。
- 与 P2 不同：这不是「某章修了没往后传」，两版描述在初始提交 `f9794e6` 里就已分叉，属原始撰写不一致。
- 改动前确认安全：全仓无任何测试断言这五条描述，也没有别的教程引用短版。
- 处理：`scripts/forward-port-task-descriptions.mjs` 把 ch12 的五条前移到 ch13–ch20，每条断言恰好命中 1 次，漂移则抛错。改后 ch12–ch20 的 `tasks.ts` **非注释差异为 0 行**（仅注释密度不同，属另一条待报告的全仓现象）。
- 验证：`npm run test:ch13` = 34 files / 285 tests（与基线一致）、全量 65 files / 428 tests、`typecheck` exit 0、`biome lint`/`format` 1367 文件无改动。

### P4 ch14–ch20 静默删掉了 ch13 的两个后台工具（已修，用户确认「补回代码」）

- ch13 注册 `query_background_job` / `cancel_background_job`（`features/background.ts` 的 `registerBackgroundJobTools`），并在教程里用整节「查询与取消：两个工具」（1481–1678 行）讲它们的 schema、effect 分裂、`job_id` 三层校验、取消后回读。ch14 起这两个工具、两个 zod schema、`backgroundJobPayload`、`backgroundJobToolError`、以及 `JobSupervisor.getJob()` 全部消失，ch14–ch20 七章都没有。
- 证据链：① ch13 的 `ch13-bootstrap.test.ts` 断言 P13 **含**这两个工具；② ch14 的 `ch14-bootstrap.test.ts` 断言精确 14 个工具**不含**它们，用例名却叫 `appends schedule_cron after P13 tools`；③ ch14 教程 1284 行明写「本章不反向改变上一章的能力——这也是一条测试断言」；④ ch14「刻意没有做什么」10 项里没有这两个工具，全章无交代；⑤ `cancel_background_job` / `background_job_not_found` 的测试覆盖在 ch14–ch20 归零，而 `JobSupervisor.cancel()` 仍在，即活代码零覆盖。
- 分叉同样来自初始提交 `f9794e6`，属原始撰写不一致，非后期回退。
- 处理（三个脚本，全部幂等、锚点漂移即抛错）：`scripts/restore-background-tools.mjs` 把两个工具定义、两个 schema、payload/error 辅助、`getJob()`、`zod` 与 `ToolDefinition` 导入前移到 ch14–ch20；`scripts/fix-background-tool-order.mjs` 把 ch17–ch20 的注册块从 `taskStore` 之后挪到 `registerLeasedTaskTools` 之后（P17+ 的五个任务工具由 leased 版注册，插在前面会排到 `create_task` 之前）；`scripts/patch-background-tool-asserts.mjs` 同步 8 处精确工具表断言。位置统一为「紧跟 `complete_task`」，与 ch13 同序，ch13 前缀因此逐字不变。
- 工具数随之 +2，全部实测而非推算：P14 = 16（7 章的 `ch14-bootstrap` 断言）、P15 = 18（`scratch/ch15tools.ts` 实跑）、P16 Lead = 20 且 `[20,0,5]`、队友仍 5（`scratch/ch16tools.ts` 实跑）、P20 Lead = 25（`ch20-full-harness` 的 `LEAD_TOOLS` 断言）。子 Agent 12 / 队友 9 不变——注册只在主工具路径一处，子执行者 factory 不接 `backgroundSupervisor`。
- 教程同步：ch14（工具数 4 处 + 16 工具表重排 + `background.ts` 632→726、`bootstrap.ts` 330→338、测试 132→134）、ch15（3 处 + 18 工具表 + 实验 11 输出 + `bootstrap.ts` 395→403）、ch16（5 处含实验 14 的 `[20,0,5]` + `bootstrap.ts` 402→410）、ch17（437→445）、ch18（468→476，三校验行号 102-119→106-123）、ch19（493→501，四处行号区间 +4/+8）、ch20（13 处含两份 25 工具清单、按章节归组表加一行 P13、开篇轨迹 23→25 与 24→26、`bootstrap.ts` 493→517 改 501→525、四处行号区间）。行号位移规律：bootstrap 的 import 从 1 行变 5 行（+4），注册块 +4，故插入点之前的引用 +4、之后 +8。
- 顺带修正 ch14 教程 1009 行的错误对比：原文说「后台事件可以批量注入，带 `batch: {index, total}`」，但 ch14 已整体删掉 `RuntimeEventBatchPosition` 与 `batch` 字段，两类事件同走 `drainEvents(1)`。已改成正面交代这次反向变化，并在 ch13 对应小节加了前向提示（批量注入只活到第 13 章，第 14 章为身份隔离退掉）。**batch 的移除本身不是缺陷**——ch14「刻意没做」表第 2247 行已记录「事件批量注入 / 会破坏身份隔离 / 每轮最多一个事件」。
- 验证：全量 619 files / 4635 tests 全绿（改动后首跑）、`typecheck` exit 0、`biome lint`/`format` 1367 文件无改动、`build` exit 0、`scripts/verify-tutorial-refs.mjs` 全 20 章行数与路径声明零问题（余 4 条是教程让读者自建的探针文件名 `main.ts`/`query.ts`/`peek.ts`/`full-harness.ts`，非缺陷）。
- 观察（非本次改动引入）：满负载跑 619 个文件时 `chapters/ch09/tests/config.test.ts` 的 CLI 子进程用例偶发 30s 超时，单跑 547ms / 7 tests 通过，属并行饥饿抖动。
- **教程同步的落盘复核（ch19/ch20 审计时补做）**：上面「教程同步」一条声称的改动，ch19 与 ch20 两章当时**并未真正落盘**，是在审 ch19、ch20 时才补上的（ch19 的 Lead 工具数 23→25 与 `bootstrap.ts` 493→501 加四组行号；ch20 的 `bootstrap.ts` 493→517 改 501→525 加六组行号）。ch14–ch18 逐个复核**确认已正确落盘**：`bootstrap.ts` 行数 338/403/410/445/476 与实际逐个吻合，ch14 的 `background.ts` 726、`ch14-bootstrap.test.ts` 134、ch18 的三校验行号 106-123 亦均正确。至此 P4 的教程同步全部闭合。

### P5 ch18 与 ch19 对「complete_task 之后的 scope」语义相反（已修：采纳方案①）

- ch18 的 `WorktreeRuntime` 在 `complete_task` 成功后从 `#scopeClaims` 里删掉当前 `executionScope`；ch19/ch20 删掉了这次清除，并加注释「后续调用仍由 `resolve()` 按租约重新校验」。
- 行为差异（探针实测，非推算）——`complete_task` 之后、**同一条回复里**再调一个工具：

  | 版本 | scope 状态 | 结果 |
  | --- | --- | --- |
  | ch18 | 已清 | 查不到 claim → 视作「无 claim」→ **回落主 workspace，正常执行** |
  | ch19/ch20 | 保留 | 旧 token 已失效 → `lookupClaim` 抛 `TaskError` → **`WorktreeContextError("Execution claim is no longer active")`** |

- 两章测试**同名** `completed claim tokens cannot fall back to the main workspace`，但各自绕开了自己实现的薄弱处：ch18 版用 `claimNext` 取 token（`claimNext` 不登记 scope，只有 `claimTask` 登记）再显式传 `claimToken`，scope 清理路径根本没被触达；ch19 版改用 `claimTask` + 不传 `claimToken`，纯靠 scope 解析——正因为删了 `delete`，才可能抛错。判定：ch19/ch20 语义更严且与 fail-closed 一致，是修正。
- **修复（方案①：把 ch19 写法回移 ch18）**：删掉 ch18 `worktrees.ts` 的 scope 清除块（原 566-573 行，逻辑 2 行 + 注释空行 6 行）；ch18/ch19/ch20 三章 `worktrees.ts` 现已逐字节相同（979 行 / 逻辑 835）。ch18 教程 948 行改为正面叙述（「乍看反直觉——租约都完成了，为什么还留着映射？因为清掉才是危险的那一边」），889/904/924 行引用不变。测试与五道门全绿。

### P6 累计快照之间的注释密度漂移（记录在案的实例已修；全书系统性存量见 P7）

- 现象：后一章的快照会删掉前一章文件里的解释性注释，逻辑字节不变。已确认的发生点：ch09、ch10、ch12、ch13、ch16→ch17，以及**规模最大的 ch19→ch20**（约 68 行注释、跨 5 个文件：`mcp-tools.ts` −50、`mcp-client.ts` −12、`mcp-schema.ts` −3、`demo.ts` −2、`tools.ts` −1，含两个文件头注释）。
- **修复完成（2026-09-02 验证）**：ch19→ch20 的 5 个文件现已与 ch19 **逐字节相同**（`mcp-tools.ts` 806、`mcp-client.ts` 347、`mcp-schema.ts` 76、`tools.ts` 380、`demo.ts` 63，`diff -rq` 零差异）；ch18/ch19/ch20 的 `loop.ts`（789）与 `worktrees.ts`（979）三章逐字节相同。ch18→ch19、ch19→ch20 相邻对按「剥注释后逻辑行 + 总行数」扫描：**零注释缺口**，剩余差异全部是本章新功能的合法增量（bootstrap/cli/loop `#prepareModelRequest`/profiles 增量表/prompting `statusProvider`/tools `registerMany`）。
- 教程行号同步（回填插入的注释使行号位移，已逐一重对并实测源码）：ch20 教程 4.1 顺序图 9 处锚点（475→478 等，`#prepareModelRequest` 478-521、渲染 508、快照 516、「隔六行」改「隔八行」）、4.2/4.3 三处 L 引用、11 节 delta 表（`781→789` 改 `789→800`，13→8 个 diff 文件，「第一类：只改了注释」整块改写为「零改动（修复后逐字节相同）」并保留 PowerShell 验证命令）、代码索引（loop 800、close 672-699、`validateToolPairing` 124-159/36 行、tools 380、permissions 286/333-346、events 34、prompting 缓存键 89）、整章统计 18706→19364 源码行 / 15547→15546 测试行、自测第 4 条（508/516 隔八行）；ch19 教程 loop 781→789、config 68→78；ch18 教程 worktrees 981→979（两处）。
- 验证：全量 65 files / 428 tests 全绿（五行预期 stderr 与基线逐字一致）、`typecheck`/`lint`/`format:check`/`build` exit 0。
- ch16→ch17 只加文件头注释（+1，正向，非删除）；ch9→ch10、ch8→ch9 无注释缺口。

### P7 全书注释密度系统性存量（五族大额缺口已修，2026-09-02；其余保留现状）

- P6 的定性方法（「剥注释后逐字比对」）只能抓到**纯注释删除**的文件；对「逻辑和注释同时变化」的混合文件不敏感。改用**注释计数递减**扫描（`grep -c '^\s*//'`，相邻章同名文件计数下降即记）后，全书共发现 **53 处注释损失**。
- 对照原始提交 `f9794e6` 分类，根因分三类：
  - **E-型 21 处**：历次审校把 ch(N) 的文件富化了注释（如 ch13 `background.ts` 原 15→现 104、ch3 `permissions.ts` 原 16→现 55、ch10 `compaction.ts` 原 26→现 82），**但没有传导到 ch(N+1)**，人为制造了「读者从 N 读到 N+1 看到解释凭空消失」。原始提交里很多相邻对本来没有损失（如 ch10→ch11 compaction 原为 26→27）。
  - **M-型 24 处**：两侧都相对原始提交变过（如 ch1→ch2 `loop.ts` 原 36→10、现 56→37，部分均衡但仍差 19），需逐文件判断哪些是合法重写。
  - **O-型 1 处**：`chapters/ch01.ts` 8→1（入口文件注释，琐细）。
- **用户定夺（2026-09-02）：修大额缺口五族**。已修（工具 `scripts/propagate-comments.mjs`：LCS 对齐剥 `export ` 前缀的逻辑行锚点，整行注释插入，写前断言逻辑行序列不变，按注释文本幂等去重）：
  | 族 | 源 | 目标段（传导后注释数/逻辑行，逻辑行与传导前逐一相等） |
  | --- | --- | --- |
  | `core/permissions.ts` | ch3（55c/288 逻辑） | ch4-9（58c/288）、ch10-15（64c/288）、ch16-20（86c/300） |
  | `features/background.ts` | ch13（104c/620 逻辑） | ch14-20（107c/643） |
  | `features/compaction.ts` | ch10（82c/801 逻辑） | ch11-16（85c/825）、ch17-20（86c/825） |
  | `features/subagents.ts` + `teammates.ts` | ch17 | ch18-20（subagents 22c/150、teammates 122c/775） |
  | `features/recovery.ts` | ch14（50c/512 逻辑） | ch15-16（53c/543）、ch17-20（54c/543） |
  - 传导后类型签名演化处的少量「无锚点」注释经人工判别：ch16 的 `PermissionMatcher`（async 联合型）补 1 行；`evaluate`/`#evaluateRules` 两处 ch16 已有异步版等价注释，不重复；ch14 `CANONICAL_UUID`、ch11/ch17 `HistorySummarizer` 各补 1 行。
  - 复扫：五族相邻对注释计数零递减；剩余 47 处（ch1→ch2、ch2→ch3、ch6→ch7、ch15→ch16 bootstrap 等非五族项）按用户决定保留现状。
  - 验证：全量 65 files / 428 tests 全绿、`typecheck`/`lint`/`format:check`/`build` exit 0、`verify-tutorial-refs` 20 章零新增问题（余 4 条为教程让读者自建的探针文件名，历史已知）。
- 教程行号同步（传导插入的注释使行号位移，逐一实测源码后改）：ch11 索引 compaction 919→977；ch14 索引 background 726→813；ch16 索引 permissions 365→415；ch18 增量表 teammates 109→62、subagents 27→25（叙述「那 27 行」同改 25）、loop 129→136、索引表 teammates 871→928；ch20 permissions 286/333→330/378、阅读顺序 333-346→378-392、recovery 四错区 299-340→324-366、429/529 区 301-317→326-342、`evaluate()` 候选顺序 185-195→257-265、整章统计 19364→19655 源码行。ch8/ch13/ch15/ch17/ch19 的相关引用经核对均指向本章未改动文件，零漂移。逐章进度表各行的历史行数声明如与 P7 本节冲突，以本节当前值为准。
- 附带发现：`core/messages.ts` 头部 5 行注释中两对互为同义重复（「与 Chat Completions 对齐…」×2、「validateToolPairing 强制保证…」×2），ch12-ch20 九章一致——前会话统一回填的产物，非本章新增漂移，未动。

（逐条记录：章节、位置、问题、处理）
