闹钟不需要你盯着它才会响。

你设好 7:00，去睡觉，去洗澡，去做饭，到点它自己响。这件事理所当然到你从来不会去想：闹钟是怎么在你不在的时候知道时间到了的。

但当你开始构建 Agent 系统的时候，这个问题就不理所当然了。

第 13 章的 Agent 已经支持后台执行：`npm install` 跑两分钟，主循环照样能去读 README。但你回头看看那条链路的**起点**——它仍然是模型的一次工具调用，而模型之所以会调工具，是因为**你先说了一句话**。

你说一句，Agent 动一下。「每天早上 9 点跑一次测试」「每 30 分钟检查一次 CI」，这类任务没有人去推，Agent 就永远不会动。

第 13 章解决的是「慢操作不要占住 Loop」，第 14 章解决的是**「没有人说话的时候，Agent 也能开始干活」**。

本章只增加一个能力，它由四部分组成：**一个新工具、一个 cron 解析边界、一份带 outbox 的原子快照、一个受管的 scheduler**。

先把范围钉死。本章只做**时间驱动**：一个 `schedule_cron` 工具，没有 list、没有 cancel、没有文件监听、没有 Webhook、没有抖动和自动过期。事件回到 Loop 的那条通路，完全复用第 13 章的 `EventInbox`——**本章不新建第二条事件通道**。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaGgUlESgPjKmOwymibl8OkM0NTmiaianiaJ7FQiad5jblyQOeoUlPHDLkp8j9Aqy1nIwXFF5EZs0iaJwlr8ViamSd6w5nbyPD6a1Z4eXM/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

1. 说清「触发权从人手里交给调度器」之后，多出来的四个必答问题分别是什么。
2. 解释为什么 `CronJob`（时间规则）和 `CronEvent`（一次到期的工作意图）必须是两个对象，合成一个会在哪个具体崩溃点丢工作。
3. 说出五段 cron 里 DOM 和 DOW 同时被约束时是 **OR** 而不是 AND，并举出一个被它坑到的表达式。
4. 描述夏令时的 gap 和 fold 各是什么，以及「本地日历计算 + UTC instant 持久化」为什么能同时处理这两种情况。
5. 说出 `.cron.lock` 和 `leader.lock` 各自解决什么问题，为什么一把锁不够。
6. 解释为什么事件是**先进 history 再 ack**，反过来会在哪个窗口丢工作。
7. 判断 Agent 停机三天后重启，一个「每分钟」的 recurring job 会补发多少个事件。
8. 说明一个定时任务在触发时还要不要过权限，以及 job 创建之后权限规则改了以哪一版为准。

### 你需要先具备什么

| 前置 | 出自哪一章 | 本章怎么用它 |
| --- | --- | --- |
| Agent Loop 的一轮：模型 -> 工具 -> 结果回填 | 第 1 章 | 定时触发不改这条链路，只换一个起点 |
| `ToolDefinition` / Zod `.strict()` schema | 第 2 章 | `schedule_cron` 五个必填字段全靠它守边界 |
| `PermissionPolicy` 的判决位置 | 第 3 章 | **本章重点之一**：定时任务触发时仍然过这道门 |
| Pre/PostToolUse Hook 顺序 | 第 4 章 | 事件驱动的 turn 一步都不跳 |
| 文件锁 + 原子替换 + fail closed | 第 12 章 | `state.json` 直接沿用同一套纪律 |
| `RuntimeEvent` / `EventInbox` / `RuntimeEventPump` | 第 13 章 | **最关键的前置**，cron 事件走的就是这条通路 |
| `JobSupervisor.startManaged()` 与关闭顺序 | 第 13 章 | scheduler 是一个受管 worker，不是野生 `setInterval` |
| Profile 能力位与组合根按能力注入 | 第 5～13 章 | P14 靠一个 `cron` 能力位接上整套调度运行时 |

只来得及补一章的话，补第 13 章的「事件怎样回到同一个 Loop」。本章新增的东西集中在**事件从哪来**，而**事件怎么回去**完全是上一章的结论。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| slot（槽位） | 一个具体的到期时刻，本章一律以 UTC instant 表示 |
| `CronJob` | **未来的时间规则**，可以触发无数次，是「定义」 |
| `CronEvent` | **一次已经到期的工作意图**，只对应一个 slot，是「事实」 |
| `next_run_at_utc` | 这个 job 下一次该触发的 UTC 时刻 |
| `last_slot_at_utc` | 上一次已经产生过事件的 slot，防重复触发靠它 |
| outbox | 持久事件队列。事件在被确认之前不会消失 |
| ack（确认） | 「这条事件已经进了 history」，随后从 outbox 删除 |
| DOM / DOW | day of month（几号）/ day of week（星期几） |
| DST gap | 春季跳时，某个本地时间**不存在**（如纽约 02:30） |
| DST fold | 秋季回拨，某个本地时间**出现两次**（如纽约 01:30） |
| `durable` | `true` 写进 `state.json`，跨进程重启恢复；`false` 只活在内存 |
| leader | 同一个 workspace 里唯一有权为 durable job 产生事件的那个 session |
| event-only turn | 没有用户输入、由事件触发的一次模型回合 |
| misfire（漏触发） | 停机期间错过的 slot，重启后怎么处理是个策略决定 |

### 建议的阅读路线

```text
本章导读（你在这）
  ↓
① 先看一次定时触发的完整轨迹   ← 直接看四条消息，不先讲抽象
  ↓
② 三个「时间」要分清            ← slot / now / next_run，混了后面全乱
  ↓
③ 只增加一个工具               ← 五个必填字段，以及为什么没有默认值
  ↓
④ 五段表达式：别重造五十年边界  ← DOM/DOW 的 OR 陷阱
  ↓
⑤ 时区：本地日历算，UTC 存      ← gap 和 fold 两种反常情况
  ↓
⑥ Job 和 Event 必须分开        ← 本章最重要的建模决定
  ↓
⑦ 单一原子快照 + outbox        ← 状态长什么样，怎么写进去的
  ↓
⑧ durable 和 session-only      ← 两套生命周期
  ↓
⑨ 两把锁各管什么               ← 快照锁 vs leader 锁
  ↓
⑩ 从 outbox 到 Agent           ← 先入历史，再确认
  ↓
⑪ 触发时重新授权               ← 定时任务不是权限票据
  ↓
⑫ 受管 scheduler 与关闭顺序    ← 谁负责停下来
  ↓
⑬ 接进组合根                   ← 能力位、14 个工具、两个入口
  ↓
⑭ 运行 + 12 个实验 + 排错      ← 把上面每条结论都亲手验一遍
```

---

## 先看一次定时触发的完整轨迹

先不讲设计，直接看结果。

下面这段轨迹来自一次真实运行（实验 9 的输出，你后面可以自己跑出来）。场景是：一个 one-shot job，prompt 是「检查 CI 状态」，创建者 identity 是 `cron-owner`，时区 `Asia/Shanghai`。时间一到，没有任何人说话，Agent 自己动了：

```text
模型请求次数 = 2

 1. user      {"runtime_event":{"event_id":"...0501","job_id":"...0401","kind":"cron",
              "identity":"cron-owner","prompt":"检查 CI 状态","timezone":"Asia/Shanghai",
              "durable":true,"slot_at_utc":"2026-06-01T12:01:00.000Z"}}
 2. assistant content=(null) toolCalls=[action({"value":"green"})#cron-action]
 3. tool      toolCallId=cron-action CI: green
 4. assistant content="CI 是 green，无需处理。" toolCalls=[]

工具看到的上下文 = [{"identity":"cron-owner","idempotencyKey":"...0501"}]
Runner 自身 identity = interactive-user
outbox 剩余 = 0
```

（第 1 条实际是一整行紧凑 JSON，这里为了看清结构做了换行。id 尾号做了省略。）

四条消息，两次模型请求。逐条看，每一条都在回答一个设计问题。

**第 1 条是本章的全部新意。** 注意三件事：

- 它的 `role` 是 **`user`**，不是 `tool`。这条消息不是任何一次 `tool_call` 的结果——它根本没有对应的 `tool_call`，因为**没有人调用过任何工具**。它是时间到了以后凭空注入进来的。
- 它是一条**结构化 JSON**，不是裸字符串 `"检查 CI 状态"`。`kind`、`identity`、`slot_at_utc` 都在里面，模型能看出这是一次定时触发以及是哪个 slot。
- `identity` 是 `cron-owner`，也就是**当初创建这个 job 的人**。

**第 2、3 条说明这就是普通的一轮工具调用。** 模型看到事件后决定调 `action` 工具，工具正常执行、正常回填。**参数校验、Hook、权限判决一步都没跳。**

**第 4 条是最终回答。** 模型基于真实工具结果给出结论。

真正值得盯的是最后三行：

```text
工具看到的上下文 = [{"identity":"cron-owner","idempotencyKey":"...0501"}]
Runner 自身 identity = interactive-user
```

`AgentRunner` 自己的 identity 是 `interactive-user`，但工具执行时看到的 `identity` 是 **`cron-owner`**。这不是 bug，是本章一条明确的设计：**定时任务以创建者的身份执行，不以当前交互者的身份执行。** 顺便，`idempotencyKey` 被填成了这次事件的 `event_id`——有外部副作用的工具可以拿它做幂等边界。

### 和第 13 章那条轨迹的关键差别

第 13 章的后台事件轨迹有七条消息，本章只有四条。少掉的三条恰好说明了区别：

| | 第 13 章（后台完成事件） | 第 14 章（定时触发事件） |
| --- | --- | --- |
| 起点 | 用户说了一句话 | **没有人说话** |
| 事件之前 | 已经有 `assistant` + `tool` 占位结果 | history 是**空的**，事件就是第一条消息 |
| 事件对应什么 | 一次真实发生过的 `tool_call` 的迟到结果 | 一个**未来时间规则**的兑现 |
| 事件里的 identity | 就是当前交互者 | **创建 job 时的那个人** |
| 用的 `EventInbox` | 同一个 | 同一个 |

最后一行是重点：**两章共用一个 `EventInbox`**。本章没有为 cron 单独造一条队列。事件从哪来变了，事件怎么回去一个字没改。

### 那么「谁在看表」

上面那条轨迹里，第 1 条消息凭空出现之前，实际发生了这些事：

```text
scheduler（受管 worker）每秒醒一次
  ↓
store.tick(now)  ← 拿当前 UTC 时间，问「有没有 job 到期了」
  ↓
有 → 在同一个锁里：造 CronEvent、进 outbox、推进 job
  ↓
把 pending event 发布到 EventInbox
  ↓
调 AgentRunner.runEvents()  ← 唤醒 Agent
  ↓
Runner 空闲 → drain 一条事件 → 进 history → ack → 跑 model turn
```

这七步就是本章的骨架。后面每一节都在拆其中一步。

---

## 三个「时间」要分清

进入实现之前必须先分开三个时间概念。它们在日常说话时都叫「时间」，但在代码里是三个不同的量，混起来会让后面每一节都打结。

```text
now          ← 现在几点。scheduler 每次 tick 的输入
slot         ← 这次「应该在什么时候执行」。事件的属性
next_run     ← 这个 job「下一次该在什么时候执行」。job 的属性
```

用一个具体例子把三者钉死。假设一个「每分钟」的 job，创建于 `12:00:30`：

| 时刻 | now | job.next_run | 发生了什么 |
| --- | --- | --- | --- |
| 创建时 | `12:00:30` | `12:01:00` | 算出第一个未来 slot |
| 第一次 tick | `12:00:45` | `12:01:00` | `now < next_run`，什么也不做 |
| 第二次 tick | `12:01:02` | `12:01:00` | **到期了**，产生 slot = `12:01:00` 的事件 |
| 同一次迁移里 | `12:01:02` | `12:02:00` | job 的 `next_run` 被推进 |

注意第三行：事件的 `slot` 是 **`12:01:00`**，不是 `12:01:02`。**slot 是应该执行的时刻，不是实际发现它到期的时刻。** 这个区别在两个地方会救你：

- 一个「每天 9:00」的 job，因为进程忙了 8 秒才发现，事件的 slot 仍然是 `09:00:00`。模型看到的是「这是 9 点那一次」，不是「这是 9 点 08 秒那一次」。
- 判断「这个 slot 是不是已经发过了」，靠的是比对 `last_slot_at_utc`，而它记的就是 slot。如果记的是发现时刻，同一个 slot 就没法可靠去重了。

另外注意：创建时算出来的第一个 slot 是 `12:01:00`，**不是 `12:00:30` 本身**，也不是 `12:00:00`。`next_run` 永远是**严格大于当前时刻**的第一个匹配点。「创建即触发一次」不是本章的语义。

---

## 问题的本质：触发权变了

手动触发的链路是：**人 -> Agent**。人不在，链路就断了。

定时触发的链路是：**Scheduler -> Agent**。人退出了这条链路，调度器成了新的触发者。

这一个箭头的改变，会逼出四个必答问题。它们不是「可以优化的地方」，是**不回答就一定出错的地方**：

| 问题 | 不回答会怎样 | 本章在哪里回答 |
| --- | --- | --- |
| 谁解释 cron 表达式和时区？ | 「每天 9 点」到底几点没有确定答案 | 五段表达式 + 时区两节 |
| 时间到了以后，怎么持久记录「这次该执行」？ | 进程一崩，这次工作永久消失 | Job/Event 分离 + outbox |
| Agent 正忙时事件怎么办？ | 要么丢事件，要么两个 turn 同时写 history | 从 outbox 到 Agent |
| 定时任务调工具时还过权限吗？ | 一个旧 job 变成绕过当前策略的后门 | 触发时重新授权 |

第 14 章没有另起一套 Agent Loop。它严格等于第 13 章已有能力，再加一个 `cron` 能力位，并把 scheduler 纳入已有的异步资源生命周期。最终落成四个边界：

```text
schedule_cron 工具
    |  只负责校验参数、取当前 identity、创建 job
    v
JsonCronStore
    |  保存 job、next slot 和 durable outbox；一把锁包住读改写
    v
CronRuntime
    |  受管 scheduler 定期 tick，把 pending event 发布到 EventInbox
    v
AgentRunner.runEvents()
       空闲时执行 event-only turn，工具仍过 Hook 与权限
```

每一层的职责边界都值得说一句：

- **Scheduler 只判断时间并产生事件**，它不执行业务工具，也不绕过 Agent Loop。
- **持久 outbox 只负责「工作意图不能凭空消失」**，它不负责「恰好执行一次」。
- **`EventInbox` 只负责当前进程内的 typed 交接**，它不持久化任何东西。
- **`AgentRunner` 是模型、Hook、权限和工具执行的唯一入口**，本章一个字都没改这条规则。

---

## 只增加一个工具

第 14 章相对第 13 章只追加一个工具：`schedule_cron`。

```typescript
{
  name: "schedule_cron",
  description: "Schedule a recurring or one-shot prompt with a five-field Cron expression.",
  effect: "write",
}
```

没有 `schedule_job` 别名，没有 `list_cron_jobs`，也没有 `cancel_cron_job`。为什么不顺手加上，本章末尾「刻意没做什么」会讲。

输入 schema 有五个字段，**全部必填**：

```typescript
const scheduleCronSchema = z
  .object({
    cron: z.string(),
    prompt: z.string(),
    timezone: z.string(),
    recurring: z.boolean(),
    durable: z.boolean(),
  })
  .strict();
```

| 字段 | 含义 | 为什么不能有默认值 |
| --- | --- | --- |
| `cron` | 严格五段 cron 表达式 | 没有「合理的默认周期」这种东西 |
| `prompt` | 到期后交给 Agent 的工作描述 | 空 prompt 的定时任务毫无意义 |
| `timezone` | IANA 时区名，如 `UTC`、`Asia/Shanghai` | **默认成宿主机时区是本章最想避免的坑** |
| `recurring` | `true` 周期，`false` one-shot | 猜错任一个方向都是严重后果 |
| `durable` | `true` 跨进程重启恢复 | 「以为存下来了其实没存」比明确报错危险得多 |

`.strict()` 的作用是**多传字段直接失败**。模型如果自作聪明加一个 `enabled: true`，不会被静默忽略，而是当场报参数错误。

### 三个字段不由模型填

`schedule_cron` 的输入里没有 `identity`、`id`、`next_run_at_utc`。这三样都由运行时决定：

```text
identity        ← 从当前 ToolContext.identity 取，模型无法伪造
id              ← 运行时生成 UUID
next_run_at_utc ← 由 cron 表达式 + 时区 + 当前时间算出
last_slot_at_utc ← 新建时固定为 null
```

`identity` 这一条尤其重要。如果它是输入字段，模型就能创建一个「以管理员身份运行」的定时任务——那等于把权限系统整个绕过去了。**创建者身份是观察到的事实，不是可申报的参数。**

所以 `CronJob` 的字段比工具输入多四个：

```typescript
interface CronJob {
  readonly id: string;               // 运行时生成
  readonly cron: string;             // 来自输入（已归一化）
  readonly prompt: string;           // 来自输入
  readonly timezone: string;         // 来自输入（已校验）
  readonly recurring: boolean;       // 来自输入
  readonly durable: boolean;         // 来自输入
  readonly identity: string;         // 从 ToolContext 观察
  readonly nextRunAtUtc: Date;       // 运行时计算
  readonly lastSlotAtUtc: Date | null; // 运行时推进
}
```

这组字段让「定义是什么」「谁创建的」「下一次何时触发」「上一个 slot 是什么」都有明确归属，不再依赖进程内的零散字典。

### 四种 typed 错误

工具边界返回的错误码是稳定的，模型可以据此决策：

| 错误类 | `errorCode` | 什么时候出现 |
| --- | --- | --- |
| `CronExpressionError` | `cron_expression_error` | 表达式不是五段、字段越界、反向范围、未知时区 |
| `CronStorageError` | `cron_storage_error` | 快照损坏、版本不符、原子写失败、锁异常 |
| `CronJobNotFoundError` | `cron_job_not_found` | 引用了不存在的 job 或 event |
| `CronClosedError` | `cron_closed` | 运行时已关闭还调 schedule/start/tick |

注意 `CronClosedError` 的存在意义：关闭之后继续调度**必须明确失败**，不能静默成功也不能静默忽略。否则关闭返回后还有活跃 worker，第 13 章那套关闭纪律就破了。

---

## 五段表达式：别重造五十年的边界

五段 cron 的顺序是固定的：

```text
分钟  小时  日  月  星期
 │     │    │   │   └── 0-7（0 和 7 都是周日）
 │     │    │   └────── 1-12
 │     │    └────────── 1-31
 │     └─────────────── 0-23
 └───────────────────── 0-59
```

本章严格限制为五段，语法和「下一次发生时间」都交给锁定版本的 `cron-parser`（`5.6.2`）。列表、范围、步进、星期全部由成熟解析器处理：

```text
* * * * *              每分钟
0 9 * * *              每天本地时间 09:00
*/15 9-10 * * 1,3,5    周一、周三、周五的 09:00-10:59，每 15 分钟
0 9 * * 1-5            周一到周五的本地时间 09:00
```

自己手写 cron 解析器是个经典陷阱。它看起来只是五个数字，但边界条件五十年来被踩过一遍又一遍：`*/0` 该怎么办、`8-2` 是不是跨月、`0` 和 `7` 都是周日、二月三十号存在吗。**这些不是本章要教的东西**，交给依赖。

### 但校验要自己收口

依赖负责语法，本章负责**在写状态之前就失败**。实测所有拒绝路径：

```text
* * * *          -> Cron expression must contain exactly five fields
* * * * * *      -> Cron expression must contain exactly five fields
61 * * * *       -> Invalid Cron expression: 61 * * * *
*/0 * * * *      -> Invalid Cron expression: */0 * * * *
0 9 8-2 * *      -> Invalid descending Cron range
0 9 * 13 *       -> Invalid Cron expression: 0 9 * 13 *
0 9 * * 8        -> Invalid Cron expression: 0 9 * * 8
```

注意两条是本章自己加的，不是 `cron-parser` 给的：

- **恰好五段。** 六段的 `* * * * * *`（带秒）在有些 cron 实现里合法，本章明确拒绝。允许两种段数会让「第一个字段是分钟还是秒」变成猜谜。
- **反向范围。** `8-2` 在某些实现里被解释成「8 到月末 + 月初到 2」，这种隐式跨界很容易写错却不报错。本章直接拒绝。

**坏输入不会先写状态再等 scheduler 出错。** 这是一条通用纪律：校验失败必须发生在任何持久化之前，否则磁盘上会留下一个永远触发不了的僵尸 job。

### DOM 和 DOW 是 OR，不是 AND

这是五段 cron 最著名的坑，值得单独一节。

- DOM 是 day of month，也就是「几号」。
- DOW 是 day of week，也就是「星期几」。

标准 cron 在**两者都被约束**时使用 **OR**。看这个表达式：

```text
0 9 1 * 1
     ↑   ↑
    1号  周一
```

直觉会读成「每月第一个周一的 9 点」。**它不是。** 它的实际含义是：

```text
每月 1 号的 09:00  ✅
        或者
每个周一的 09:00   ✅
```

实测验证（2026-06-08 是周一）：

```text
0 9 1 * 1  after 2026-06-02T00:00:00Z -> 2026-06-08T09:00:00.000Z
```

从 6 月 2 日往后找，下一次是 **6 月 8 日**（周一），不是 7 月 1 日。如果是 AND 语义，答案会是「7 月里第一个既是 1 号又是周一的日子」——那得等很久。

规则是这样的：

| DOM | DOW | 语义 |
| --- | --- | --- |
| `*` | `1-5` | 只看 DOW |
| `15` | `*` | 只看 DOM |
| `1` | `1` | **两个都算，取并集** |

只有**两个字段都不是通配符**时，OR 才会生效。这也是实现里 `matchesCronFields` 的判断条件。

### 一个推论：五段 cron 表达不了「每月第一个工作日」

既然 DOM/DOW 是 OR，它只能**扩大**匹配集合，永远无法表达「同时满足两个条件」。所以这些需求五段 cron 都做不到：

```text
每月第一个工作日      ✗
每月最后一个周五      ✗
每季度第一天          ✗
```

真实业务日历还涉及节假日、调休、补班，这些本来就应该由**独立的日历规则**处理，不该塞进 cron 表达式。硬要用 cron 逼近，只会得到一个「大部分时候对」的调度——而调度错一次的代价通常不小。

正确的做法是：cron 负责「够频繁的粗筛」，业务逻辑负责「今天到底算不算」。比如用 `0 9 1-5 * *` 触发，然后在 prompt 里让 Agent 自己判断今天是不是本月第一个工作日。**把日历判断放进 prompt，而不是放进表达式。**

---

## 时区：本地日历计算，UTC 持久化

「每天 9 点」如果不说时区，就没有完整含义。上海的 9 点、伦敦的 9 点、纽约的 9 点是三个不同的 instant。

所以 `timezone` 是必填的 IANA 时区名。实测同一个 `0 9 * * *` 在四个时区下的结果：

```text
UTC                0 9 * * * -> 2026-06-01T09:00:00.000Z
Asia/Shanghai      0 9 * * * -> 2026-06-01T01:00:00.000Z
Europe/London      0 9 * * * -> 2026-06-01T08:00:00.000Z
America/New_York   0 9 * * * -> 2026-06-01T13:00:00.000Z
```

四个不同的 UTC 时刻，全都是「当地早上 9 点」。上海比 UTC 早 8 小时，所以上海的 9 点是 UTC 的 1 点；纽约（6 月是夏令时，UTC-4）的 9 点是 UTC 的 13 点。

未知时区当场失败：

```text
Mars/Olympus -> Unknown Cron timezone: Mars/Olympus
```

### 两步走：本地算，UTC 存

核心策略只有一句话：**在本地日历里计算，把结果作为 UTC instant 持久化。**

```typescript
function nextCronOccurrence(expression: string, timezone: string, afterUtc: Date): Date {
  const normalized = validateCronExpression(expression);
  const iterator = CronExpressionParser.parse(normalized, {
    currentDate: afterUtc,                        // 基准是 UTC instant
    tz: validateCronTimezone(timezone),           // 但在这个时区的日历里推进
  });
  return adjustDstOccurrence(normalized, timezone, afterUtc, iterator.next().toDate());
}
```

为什么不能反过来（先转成本地时间字符串再算）？因为**本地时间字符串会丢信息**。`"2026-11-01 01:30"` 在纽约对应两个不同的 UTC 时刻，一个字符串装不下两个答案。

所有持久化字段——`next_run_at_utc`、`last_slot_at_utc`、`slot_at_utc`——都必须是 UTC ISO 字符串。运行时拒绝无效 `Date`（`new Date("bad")` 会直接报错），也绝不保存没有时区含义的本地分钟字符串。

### DST gap：不存在的那一小时

春季跳时，时钟从 02:00 直接跳到 03:00。**02:30 这个本地时间在那一天根本不存在。**

纽约 2026 年 3 月 8 日就是这样一天。一个 `30 2 * * *`（每天 02:30）的 job 会怎样？

```text
30 2 * * * @America/New_York
  after 2026-03-07T08:00:00Z -> 2026-03-08T07:00:00.000Z
```

`2026-03-08T07:00:00Z` 换成纽约本地时间是 **03:00**（当天已经是 UTC-4）。本章的策略是：**推进到第一个有效时刻**。

三种可选策略对比：

| 策略 | 结果 | 问题 |
| --- | --- | --- |
| 跳过这一天 | 3 月 8 日不执行 | 「每天」变成「除了那天每天」，静默漏一次 |
| 报错 | 抛异常 | 一年两次的日期让一个正常 job 崩掉 |
| **推进到有效时刻** | 03:00 执行 | 那天晚了 30 分钟——**代价最小** |

选第三个。理由是：定时任务的语义通常是「大约那个时候做一次」，晚半小时远比「那天没做」或「整个 job 崩了」要好。

### DST fold：出现两次的那一小时

秋季回拨，时钟从 02:00 退回 01:00。**01:30 这个本地时间在那一天出现两次。**

纽约 2026 年 11 月 1 日就是这样一天。一个 `30 1 * * *` 的 job 实测：

```text
第一次 -> 2026-11-01T05:30:00.000Z   ← 本地 01:30 EDT（UTC-4）
第二次 -> 2026-11-01T06:30:00.000Z   ← 本地 01:30 EST（UTC-5）
```

**同一个本地时钟面，两个不同的 UTC instant，都被保留了。**

这正是「UTC 持久化」的价值所在。如果状态里存的是本地时间字符串 `"01:30"`，第二次 tick 时你会发现「`last_slot` 是 01:30，现在也是 01:30，看起来重复了，跳过吧」——于是**静默漏掉一次执行**。存 UTC instant 就不会有这个问题：`05:30Z` 和 `06:30Z` 是两个明确不同的值。

不过这里也要说清楚：本章**没有**为 fold 做「只执行一次」的特殊处理。一个「每天 01:30」的 job 在秋季回拨那天会执行两次。这是把「UTC instant 是唯一真相」这条规则一贯执行到底的结果，不是遗漏。如果业务上不能容忍，正确的做法是让 job 自己幂等（拿 `idempotencyKey` 去重），而不是让调度器猜。

测试固定了这几组结果，所以**宿主机的本地时区不会影响断言**。这一点在 CI 上尤其重要：开发机在上海、CI 在 UTC，测试结果必须一样。

---

## Job 和 Event 必须分开

这是本章最重要的建模决定。

`CronJob` 是**未来的时间规则**：它可以触发无数次，它描述「什么时候该做」。
`CronEvent` 是**某一次已经到期的工作意图**：它只对应一个 slot，它是一个已经发生的事实。

两者不能共用一个状态。

```typescript
interface CronEvent extends RuntimeEvent {
  readonly kind: "cron";
  readonly eventId: string;      // 这次触发的唯一 id
  readonly jobId: string;        // 来自哪个规则
  readonly identity: string;     // 创建者
  readonly prompt: string;       // 要做什么
  readonly timezone: string;     // 当初设定的时区
  readonly durable: boolean;
  readonly slotAtUtc: Date;      // 应该执行的那个时刻
}
```

### 为什么不能合成一个

假设你偷懒，只用 `CronJob` 一个对象，加个 `pending: true` 字段表示「到期了还没执行」。看看 one-shot 任务会发生什么：

```text
时间到 → 标记 pending=true
       → 交给 Agent 执行
       → 执行完了，job 是 one-shot，删掉定义
```

问题出在「删掉定义」这一步该放在哪。

**方案 A：先删定义，再执行。**

```text
时间到 → 删掉 job → 💥 进程崩溃 → 重启
                                 ↓
                        磁盘上什么都没有了
                        这次工作永久消失
```

**方案 B：先执行，再删定义。**

```text
时间到 → 执行 → 💥 崩在删除之前 → 重启
                                  ↓
                         job 还在，看起来还没执行
                         这次工作被执行两次
```

两个方案都错，而且**无论怎么排顺序都错**，因为「定义」和「这一次的工作意图」被绑在同一个记录上，你只有一次原子写的机会，却要同时表达两件事。

拆开就解决了：

```text
时间到 → 在同一个原子写里：{ 创建 event 进 outbox } + { 删除 job 定义 }
       → 崩溃 → 重启 → 定义没了（对，one-shot 不该再触发）
                      event 还在 outbox（对，这次工作还没做）
       → 继续执行 → 执行完 ack → event 才从 outbox 删除
```

**定义的生命周期和工作意图的生命周期本来就不一样，所以它们必须是两个对象。**

### 到期时的一次原子迁移

`store.tick(now)` 在**同一个锁、同一次原子写**里完成这些动作：

```text
1. 找出所有 next_run_at_utc <= now 的 job
2. 按 next_run_at_utc 排序（相同则按 id），保证顺序确定
3. 为最早到期的 slot 创建一个带稳定 UUID 的 CronEvent
4. 把 event 放进 outbox
5a. recurring → 更新 last_slot_at_utc，把 next_run_at_utc 推进到 now 之后
5b. one-shot  → 删除 job 定义（但 event 留在 outbox）
6. 只有真的产生了 durable event 才落盘
```

第 2 步的排序不是可选的。多个 job 同时到期时，如果顺序不确定，outbox 容量满的时候「谁被挤掉」就成了随机的，测试也没法写。

第 6 步「只有真的产生了 durable event 才落盘」是个小优化，也是个正确性要求：没有变化就不写文件，避免每秒一次无意义的原子替换。

### 重复 tick 不靠字符串标记

一种常见的错误实现是用 `"%H:%M"` 之类的字符串记「今天这个点发过了」。本章不需要，因为**第一次迁移之后 job 的状态已经变了**：

- recurring 的 `next_run_at_utc` 已经推进到了 `now` 之后，再 tick 时 `next_run > now`，不到期。
- one-shot 的定义已经删除，再 tick 时根本找不到它。

实测：

```text
one-shot 创建: next=2026-06-01T12:01:00.000Z
tick -> 产生 1 个 event, slot=2026-06-01T12:01:00.000Z
定义还在吗: 已删除
outbox: 00000000-0000-4000-8000-000000000501
再 tick 一次 -> 0 个 event
```

**防重复触发是状态迁移的自然结果，不是额外加的一层标记。** 少一层标记，就少一个「标记和状态不一致」的失败模式。

### 停机三天：只补一个

如果 Agent 停机三天后重启，一个「每分钟」的 recurring job 会补发多少个事件？

朴素实现会算出「三天 = 4320 分钟」，然后**一口气产生 4320 个事件**，把 outbox 和上下文全部撑爆。

本章的答案是 **1 个**。实测：

```text
创建时 next = 2026-06-01T12:01:00.000Z
三天后开机, now = 2026-06-04T12:01:00.000Z
补发了 1 个 event, slot = 2026-06-01T12:01:00.000Z
lastSlotAtUtc = 2026-06-01T12:01:00.000Z
nextRunAtUtc  = 2026-06-04T12:02:00.000Z  (> now: true)
```

注意三个细节：

- 事件的 slot 是 **`06-01T12:01`**，也就是**最早被漏掉的那个 slot**，不是「现在」。模型能看出这是一次迟到的触发。
- `last_slot_at_utc` 记的也是那个最早的 slot。
- `next_run_at_utc` 被直接推进到 **`06-04T12:02`**，也就是 `now` 之后的第一个匹配点。中间 4319 个 slot 被**明确放弃**。

这是一个 misfire 策略选择，叫「只补最早一个，然后追平」。它的理由是：定时任务的价值通常在「现在的状态如何」，不在「补齐历史每一分钟」。一个「每分钟检查 CI」的任务，补 4320 次毫无意义——你只需要知道现在 CI 是什么状态，再加上一条「刚才漏了一段时间」的提示。

保留最早那一个而不是「只发一个当前的」，是为了让模型知道**漏了多久**：`slot_at_utc` 和当前时间的差值就是停机时长。

---

## 单一原子快照：state.json 加 outbox

durable 状态只有一个文件：

```text
workspace/.agent_tutorial/cron/state.json
```

它是 durable job 和 durable outbox 的**单一权威快照**。注意这里和第 13 章的差别：第 13 章是「每个 job 一个文件」，本章是「所有状态一个文件」。原因后面讲。

### 长什么样

下面两段是实测输出（不是示意）。一个「每天 UTC 09:00」的 recurring job，刚创建时：

```json
{
  "version": 1,
  "jobs": [
    {
      "id": "00000000-0000-4000-8000-000000000401",
      "cron": "0 9 * * *",
      "prompt": "check CI",
      "timezone": "UTC",
      "recurring": true,
      "durable": true,
      "identity": "user",
      "next_run_at_utc": "2026-06-02T09:00:00.000Z",
      "last_slot_at_utc": null
    }
  ],
  "outbox": []
}
```

`outbox` 是空的（还没到期），`last_slot_at_utc` 是 `null`（还没触发过）。

时间推到 `2026-06-02T09:00:00Z`，tick 一次之后：

```json
{
  "version": 1,
  "jobs": [
    {
      "id": "00000000-0000-4000-8000-000000000401",
      "cron": "0 9 * * *",
      "prompt": "check CI",
      "timezone": "UTC",
      "recurring": true,
      "durable": true,
      "identity": "user",
      "next_run_at_utc": "2026-06-03T09:00:00.000Z",
      "last_slot_at_utc": "2026-06-02T09:00:00.000Z"
    }
  ],
  "outbox": [
    {
      "event_id": "00000000-0000-4000-8000-000000000501",
      "job_id": "00000000-0000-4000-8000-000000000401",
      "identity": "user",
      "prompt": "check CI",
      "timezone": "UTC",
      "durable": true,
      "slot_at_utc": "2026-06-02T09:00:00.000Z"
    }
  ]
}
```

三处变化，一次原子写完成：

```text
next_run_at_utc:  06-02 → 06-03    ← 推进到下一天
last_slot_at_utc: null  → 06-02    ← 记住这个 slot 发过了
outbox:           []    → [event]  ← 工作意图落盘
```

**这三处必须同时生效。** 如果 `next_run` 推进了但 event 没落盘，这次工作就丢了；如果 event 落盘了但 `next_run` 没推进，下次 tick 会再发一遍。这正是「为什么是一个文件而不是多个文件」的答案：**它们必须在一次原子替换里一起提交。**

顺便注意持久化格式用的是 snake_case（`next_run_at_utc`），而 TypeScript 接口用 camelCase（`nextRunAtUtc`）。两层各有自己的命名约定，中间由 store 转换。

### 怎么写进去的

`JsonCronStore` 用 workspace 内的 `proper-lockfile` 目录锁包住整个「读取 → 判断到期 → 创建 event → 推进 job → 持久化」过程。写入不是直接覆盖目标文件：

```typescript
const temporary = join(dirname(path), `.${randomUUID()}.tmp`);
handle = await open(temporary, "wx");   // wx: 文件已存在就失败
await handle.writeFile(content);
await handle.sync();                    // 强制刷到存储
await handle.close();
await rename(temporary, path);          // 同目录 rename，原子操作
```

四个细节都有意义：

| 步骤 | 为什么 |
| --- | --- |
| 随机名临时文件 | 两个进程同时写不会撞同一个临时文件 |
| `"wx"` 标志 | 临时文件已存在就失败，不覆盖别人的中间态 |
| `handle.sync()` | 内容真的落到存储，而不是停在 page cache |
| 同目录 `rename` | 同一文件系统内的 rename 是原子的，不会出现「半个文件」 |

在当前进程可观察的替换失败和普通进程重启边界内，一次写入要么提交完整新快照，要么保留旧字节。测试分别让 schedule、tick、ack 三条路径的原子替换失败，并验证 job、outbox 与**旧文件字节**都没有漂移。

### durable 承诺到哪一层

这里必须说清楚，避免过度承诺。

`durable` **不承诺**主机断电或内核崩溃级持久性。实现同步了临时文件，但**没有额外同步父目录**——极端情况下 rename 本身可能还没落盘。

它承诺的是：**Agent 进程正常退出、异常退出（`Ctrl+C`、被 kill）或重新启动后，可以从已经提交的快照恢复。**

这个边界对本章的场景够用。要做到断电级持久，需要 `fsync` 父目录，代价是每次写入多一次同步；而定时任务丢一次的后果，通常远小于「每秒一次额外 fsync」的代价。**这是一个明确的取舍，不是疏漏。**

### 坏状态不会被静默忽略

损坏的 `state.json` 一律抛 `CronStorageError`。触发条件包括：

```text
未知字段        ← requireExactKeys，多一个键就失败
错误版本号      ← version 不是 1
重复 id         ← 两个 job 或 event 同 id
非法 job        ← next_run 不晚于 last_slot、时间不是 UTC 格式
非 UTF-8        ← TextDecoder 用 fatal 模式
坏 JSON         ← 解析失败
非 durable 记录  ← durable:false 的记录出现在快照里
```

「静默跳过坏状态」看起来更健壮，实际更危险：调用者会**误以为任务已经恢复**，而实际上少了几条。这就是第 12 章定下的 **fail closed**：状态要么完整可信，要么整体不可用。

`version` 这一条尤其值得说。如果将来快照格式要改，读到 `version: 2` 的旧代码会**明确报错**，而不是按 v1 的规则去解释 v2 的数据。测试专门覆盖了这条：手工把 `version` 改成 `2`，读取时抛 `CronStorageError`。

### outbox 有容量上限

默认 50 条。容量满时的行为是关键：

```text
后续 due job 保持原来的 next_run_at_utc 和 last_slot_at_utc
等 event 被确认后再重试
```

**不能先推进时间再丢工作。** 实测（把容量调成 1，两个 job 同时到期）：

```text
容量 1, 两个 job 同时到期 -> 产生 1 个 event (first)
first  next: 2026-06-01T12:02:00.000Z (推进了)
second next: 2026-06-01T12:01:00.000Z (原地不动: true)
ack 之后再 tick -> 产生 1 个 event (second)
```

`second` 的 `next_run_at_utc` 一动不动。等 `first` 的事件被 ack、outbox 腾出位置，下一次 tick 就把 `second` 的事件补上了。

如果实现成「容量满就跳过这个 job，但照样推进 next_run」，那这次工作就静默消失了——而且**看起来一切正常**，因为 job 的状态是「已经处理到 12:02 了」。这类 bug 极难发现。

---

## durable 和 session-only 是两套生命周期

`durable` 这个布尔值决定了 job 和它的事件活在哪里。

`durable=true`：job、下一 slot 和 pending event 写入 `state.json`，新建 `JsonCronStore` 后可以恢复。

`durable=false`：job 和 outbox **只存在当前 store 实例的内存**里。进程或 session 结束后消失，也不会混入 durable 快照。

实测对照（两个 job 同时创建、同时到期，然后重建 store）：

```text
durable job = 00000000-0000-4000-8000-000000000401
session job = 00000000-0000-4000-8000-000000000402
同一实例 outbox = 0501:durable, 0502:session
重建后 jobs   = 0 个
重建后 outbox = 0501:durable
```

第三行：同一个 store 实例里，两个事件都在。第五行：重建之后，**只有 durable 那个还在**。session-only 的事件干净地消失了，没有残留、没有报错。

（`jobs` 变成 0 个是因为这两个都是 one-shot，到期时定义已被删除。durable 的**事件**留在了 outbox 里，这正是上一节讲的 Job/Event 分离。）

### durable 不等于「关掉应用还在跑」

这是最容易误解的一点，必须说清楚。

```text
❌ durable = 我关掉 Agent，到点它还会自己启动执行
✅ durable = 我下次启动第 14 章 Agent 时，规则和未确认事件还在
```

scheduler 属于 Agent 进程。**进程停止时，没有任何代码在看表。** 一个「每天 9 点」的 durable job，如果你从周一晚上关到周五早上，它不会在周二、周三、周四的 9 点执行任何东西。周五启动时，它会按上一节的 misfire 策略补发**一个**事件。

要做到真正的「关机也能触发」，需要操作系统级调度（Windows 任务计划、systemd timer、cron daemon），那是另一个层次的东西，本章明确不做。

### 什么时候该用哪个

| 场景 | 选哪个 | 理由 |
| --- | --- | --- |
| 「每天早上检查依赖更新」 | `durable=true` | 是长期规则，重启后该继续 |
| 「20 分钟后提醒我看一下这个 PR」 | `durable=false` | 只对当前会话有意义 |
| 「每 5 分钟轮询一次这个部署」 | `durable=false` | 会话结束了就不用轮询了 |
| 「每周一生成周报」 | `durable=true` | 跨会话的长期承诺 |

一个实用判据：**这个任务的意义是否依赖于当前对话上下文？** 依赖就用 session-only，不依赖就用 durable。session-only 的好处是不会在磁盘上堆积一堆你早就忘了的 job。

---

## 两把锁各管什么

同一个 workspace 可能同时打开多个 Agent session。这会带来两个**完全不同**的问题，所以需要两把锁。

| 锁 | 磁盘上的东西 | 负责什么 |
| --- | --- | --- |
| 快照锁 | `.agent_tutorial/.cron.lock` | 保护 durable 快照的原子读改写 |
| leader 锁 | `.agent_tutorial/cron/leader.lock` | 限制同一 workspace 只有一个 durable scheduler |

代码里传给 `proper-lockfile` 的路径是 `cron/leader`（一个普通目录），库会在紧挨着的位置创建 `leader.lock` 作为锁本体。所以「leader 锁」和 `leader.lock` 说的是同一件事，后面两个名字会混着用。

### 为什么一把锁不够

先看它们各自防的是什么。

**快照锁防的是「同时写坏文件」。** 两个 session 都要改 `state.json`，如果不串行化，一个的写入会覆盖另一个的。这把锁的持有时间**极短**——只覆盖一次读改写。

**leader 锁防的是「同一个 slot 被投递两次」。** 假设两个 session 都在跑 scheduler，9 点到了，两个都发现 job 到期，两个都创建事件——**同一件工作被执行两次**。这把锁的持有时间**很长**——整个 session 的生命周期。

如果只用快照锁会怎样？两个 session 的 tick 被串行化了，第一个先执行，把 `next_run` 推进到明天；第二个拿到锁后发现 `next_run > now`，不到期，于是什么也不做。

**咦，那不是也对吗？**

大部分情况下是对的。但它依赖一个假设：两个 session 看到的是同一份状态。而 session-only job 打破了这个假设——它们在内存里，不在快照里。更重要的是，**只靠快照锁的正确性来自「碰巧顺序对了」，而不是来自一条明确的规则。** 一旦以后加上「查询 job 列表」「取消 job」这类操作，靠状态推断的去重就会开始出现缝隙。

leader 锁把这件事变成显式规则：**只有 leader 为 durable job 产生事件。** 实测：

```text
session A drain: 0501
session B drain: (空)
A 关闭, leader 释放
session B 再 tick: 0501
ack 后 outbox: 0 条
```

A 拿到了 leader，产生并拿到事件；B 什么也没拿到。**A 关闭之后 leader 释放**，B 再 tick 就接管了——而且拿到的是**同一个事件 `0501`**，因为 A 关闭前没有 ack，工作意图还在 outbox 里。

最后一行是整个持久化设计的兑现：**leader 换人，工作不丢。**

注意 B 每个 session 仍然可以处理自己的 session-only job——leader 锁只管 durable。这是合理的：session-only job 本来就只属于创建它的那个 session，不存在重复投递问题。

### 还有第三层：进程内互斥

同一个进程里也可能有多个 `JsonCronStore` 实例（比如测试里，或者一个进程开了两个 workspace）。它们会先用 `withMutex` 串行化：

```text
withMutex（进程内）  ← 同进程多个 store 实例竞争同一份 session Map 和快照
    ↓
.cron.lock（跨进程） ← 不同进程竞争同一个文件
```

为什么需要这一层？因为 `proper-lockfile` 是**跨进程**锁，同进程内两个实例可能都认为自己拿到了锁。加一层进程内互斥把这个缝补上。

### 锁的具体参数

```text
stale:  30_000 ms   ← 超过 30 秒没更新的锁视为失效（持有者崩了）
update: 10_000 ms   ← 每 10 秒刷新一次「我还活着」
retry:  10 ms × 100 ← 拿不到锁最多重试 100 次，每次间隔 10 毫秒
```

`stale` 的存在是必须的：如果一个进程被 `kill -9`，它的锁文件不会被清理。没有 stale 机制，这个 workspace 就永久锁死了。30 秒配 10 秒刷新，意味着「连续三次刷新失败」才判定失效——足够容忍一次 GC 停顿或磁盘抖动。

100 次 × 10 毫秒 = 最多等 1 秒。超过就抛 `CronStorageError`。这个上限也是有意的：**一秒还拿不到锁，说明有更严重的问题**，继续等下去只会让错误更晚暴露。

Windows 上偶发的 `EBUSY` / `EPERM`（杀毒软件或索引服务占着文件）正好被这 100 次重试吸收掉，这是重试机制的实际收益。

---

## 从 outbox 到 Agent：先入历史，再确认

`CronRuntime.tick()` 的职责很窄，只有四步：

```text
① 确认自己是 leader（durable job 需要）
② store.tick(clock.now())        ← 判断到期、创建事件、推进 job
③ 把当前可投递的 pending event 发布到共享 EventInbox
④ 若绑定了 wakeup，调 AgentRunner.runEvents()
```

第 ③ 步有个进程内去重：`#queued` 是一个 `Set`，已经发布过的 `eventId` 不会重复进 inbox。这解决的是「事件已经在 inbox 里排队，但还没被 ack，下一次 tick 又看到它还在 outbox」的情况。

`EventInbox` 是第 13 章引入的 session-local typed FIFO。**P14 不创建第二套 cron queue**，background event 和 cron event 共享同一条窄通道，各自的状态模型保持独立。

### 事件进入 Loop 的顺序

```text
从 EventInbox drain 一个 RuntimeEvent
  ↓
校验 event_id、identity 和 idempotency key
  ↓
序列化为普通 user message，追加到 canonical history   ← 先做这个
  ↓
通知 event pump acknowledge                          ← 再做这个
  ↓
使用事件上下文执行 event-only model turn
```

**「先入历史，再确认」这个顺序是本节的核心。** 反过来会怎样：

```text
先 ack（从 outbox 删除）
  ↓
💥 进程在这里崩溃
  ↓
重启：outbox 是空的，history 里也没有这条消息
      这次工作永久消失，而且没有任何痕迹
```

按正确顺序，同样的崩溃点：

```text
先入 history
  ↓
💥 进程在这里崩溃
  ↓
重启：outbox 里事件还在 → 下次 tick 重新发布 → 重新执行
```

代价是**可能重复执行一次**。这是一个明确的取舍：**宁可重复，不可丢失。** 而重复的那部分风险由 `idempotencyKey` 兜底——同一个事件重投时 key 是一样的，有外部副作用的工具可以据此去重。

### ack 到底确认了什么

这个词容易被过度解读，说准确一点：

```text
ack 的含义 = 「这条事件已经进了 history，可以从 durable outbox 移除了」
ack 不含   = 「后续所有外部副作用已经恰好一次完成」
```

第二条做不到。事件进了 history 之后，模型还要调工具、工具还要产生副作用，这中间任何一步都可能崩。**运行时能承诺的只是「工作意图不丢」，不是「工作恰好做一次」。**

真正需要「恰好一次」的工具（转账、发邮件、创建资源）必须自己用 `idempotencyKey` 设计幂等边界。运行时把 key 递给你，剩下的是工具的责任。

### ack 失败要回滚

如果 ack 调用返回失败（比如原子写失败），Runner 会**回滚这次 history 接收**：

```typescript
this.#history.pop();                      // 撤掉刚追加的那条 user 消息
this.#seenEventIds.delete(event.eventId);  // 撤掉去重记录
```

为什么必须回滚？因为这条消息**还没有交给模型**。如果留在 history 里：

- 事件还在 outbox（ack 失败了），下次会重新投递。
- 但 history 里已经有一条了，重新投递时会被 `#seenEventIds` 去重挡掉。
- 结果：**事件在 outbox 里永远出不来，history 里那条永远不会被处理。** 死锁。

回滚之后，后续 tick 可以干净地重新发布同一个事件。测试专门覆盖了这条：ack 失败时抛 `Cron event is no longer pending`，事件仍然 pending，**零条** `'"kind":"cron"'` 的 user 消息进入 history，模型请求次数为 0；重试后成功，且**恰好一条**这样的消息。

### 每次只取一个事件

`drainEvents()` 每次最多取一个 cron 事件。这和第 13 章的后台事件不一样——后台事件可以批量注入，带 `batch: {index, total}` 位置信息。

为什么 cron 事件不批量？因为**不同事件可能有不同的 identity**：

```text
事件 A: identity=alice,  idempotencyKey=...0501
事件 B: identity=bob,    idempotencyKey=...0502
```

一个 model turn 只有一个 `ToolContext`，装不下两个 identity。如果批量注入，要么用错身份（安全问题），要么只用最后一条的（前面的事件被静默降权）。**逐条处理是身份隔离的必然结果。**

顺便这也避免了「批量事件里模型只关注最后一条」的老问题——一次给模型五个任务，它大概率只认真做最后一个。

### Agent 正忙的时候

`runEvents()` 使用 Runner 自己的运行锁。如果 Agent 正在处理用户 turn，它**立即返回 `undefined`**，不会并发进入同一个 history。

此时事件仍在 inbox，durable event 也仍在 outbox，后续空闲 turn 可以继续接收。

更微妙的情况是：**用户 turn 进行中，恰好轮询到了 cron 事件。** 这时候不能直接注入——那会把一条定时任务的消息插进用户对话中间，而且身份是错的。实现的做法是把它暂存在 Runner 内部的 `#deferredRuntimeEvents` 里：

```text
用户 turn 进行中 → 轮询到带 identity 的 cron 事件
                 → 暂存，不追加 history，不 ack
                 → 用户 turn 正常结束
                 → 后续 runEvents() 按保存的事件上下文执行它
```

实测（工具 handler 里手工触发一次 tick，模拟「用户回合进行中定时任务到期」）：

```text
用户回合结束: 模型调用 2 次, 工具看到 [{"identity":"interactive-user"}]
历史条数 = 4
cron 事件是否已进历史 = false
显式 runEvents() -> 有结果, 模型调用累计 3 次
现在进历史了吗 = true
工具上下文 = [{"identity":"interactive-user"}]
再调一次 runEvents() -> undefined
```

逐行读：

- 用户回合结束时，cron 事件**没有**进历史（第 3 行 `false`），用户看到的对话是干净的。
- 工具在用户回合里看到的 identity 是 `interactive-user`——**没有被 cron 事件的身份污染**。
- 显式调 `runEvents()` 之后事件才进历史，模型多跑了一轮。
- **最后一行是关键**：再调一次返回 `undefined`，说明那个被延后的事件**没有被重复发布**。暂存机制没有制造重复。

### 事件上下文怎么构造

确认成功后，事件携带的身份和幂等键进入工具上下文：

```typescript
const context: ToolContext = Object.freeze({
  workspace: this.#workspace,
  identity: runtimeEvent?.contextIdentity ?? this.#identity,
  ...(runtimeEvent?.idempotencyKey === undefined
    ? {}
    : { idempotencyKey: runtimeEvent.idempotencyKey }),
});
```

两个细节：

- `?? this.#identity`：不是 cron 事件触发的 turn（普通用户 turn）就用 Runner 自己的身份。
- 展开运算符那一段：`idempotencyKey` 是**可选字段**，没有就不放这个键，而不是放一个 `undefined`。`Object.freeze` 保证工具拿到的上下文不可篡改。

`createCronEvent` 做的映射是：

```text
job.identity  → event.contextIdentity   ← 创建者身份
event.eventId → event.idempotencyKey    ← 这次触发的唯一 id
```

用 `eventId` 当幂等键是自然的选择：同一个事件重投时 id 不变，不同 slot 的事件 id 不同。**「同一次工作意图」和「同一个幂等键」天然对应。**

---

## 触发时重新授权

创建一个 cron job，意味着「允许保存一条未来的工作意图」。它**不意味着**未来所有操作都被永久授权。

这条边界如果搞错，定时任务会变成一个绕过权限系统的后门。

### 完整顺序一步不跳

`CronEvent` 保存创建者 identity。event-only turn 执行时，Runner 用这个 identity 构造**新的** `ToolContext`。模型随后提出的每个工具调用仍按公共顺序处理：

```text
工具参数校验（Zod schema）
  ↓
PreToolUse Hook
  ↓
当前 PermissionPolicy 判决
  ↓
handler 执行
  ↓
PostToolUse Hook
  ↓
对应 tool result 回填
```

**没有任何一步因为「这是定时任务」而被跳过。** 如果当前策略拒绝，handler 调用次数就是零。

### 以哪一版权限为准

假设你周一创建了一个 job：「每天 9 点用 `shell` 跑一次部署脚本」。周三你收紧了权限策略，禁止 `shell` 执行部署命令。周四 9 点这个 job 触发了，会发生什么？

```text
❌ 按创建时的规则 → 部署照样执行，你周三的收紧毫无作用
✅ 按触发时的规则 → 被拒绝，handler 一次都没调用
```

本章选后者。理由很直接：**权限策略表达的是「现在允许什么」，不是「当初允许过什么」。** 如果定时任务能凭旧规则执行，那么收紧权限就永远无法生效——攻击者只要提前埋一个 job 就行。

所以：

```text
cron prompt 是「未来要重新交给 Agent 的输入」
cron prompt 不是「权限票据」
```

它不能绕过文件、Shell 或其他任何副作用工具的授权。**它只是一段文本，到期后被重新提交给 Agent。**

### 那为什么还要保存创建者 identity

既然要重新授权，为什么不干脆用当前交互者的身份？

因为**审计和权限判决都需要知道「这是谁的任务」**。考虑一个多用户场景：

```text
alice 创建了「每天 9 点清理她的临时目录」
bob 正在使用这个 workspace
9 点到了
```

如果用 bob 的身份执行，会有两个问题：一是审计日志把 alice 的任务记成 bob 干的；二是如果权限策略是按用户划分目录的，任务会去操作 bob 的目录。

用 alice 的身份执行，然后**按当前策略判断 alice 现在还有没有这个权限**——这才是正确的组合。**身份来自过去，权限来自现在。**

回看开头那条轨迹的最后两行，现在应该完全清楚了：

```text
工具看到的上下文 = [{"identity":"cron-owner","idempotencyKey":"...0501"}]
Runner 自身 identity = interactive-user
```

---

## 受管 scheduler 与关闭顺序

scheduler 不是一个野生的 `setInterval`。它是第 13 章 `JobSupervisor` 管的一个 worker：

```typescript
this.#worker = this.#supervisor.startManaged(
  async (signal) => await this.#runScheduler(signal),
  "cron-scheduler",
);
```

`startManaged` 把 supervisor 的取消信号转成 scheduler 自己的 `AbortSignal`。这样 scheduler 和后台 job **共享唯一 task owner**，不会出现「supervisor 说自己关干净了，但还有个定时器在跑」。

scheduler 循环本身很简单：醒来 → tick → 睡 `pollMilliseconds`（默认 1000 毫秒）→ 再醒。sleep 接收 signal，所以关闭时能立即中断，不用等满一秒。

### 可注入的时间边界

测试注入 `CronClock` 和 `CronSleeper`，不依赖真实墙钟或真实 sleep：

| 注入点 | 作用 |
| --- | --- |
| `CronClock` | 控制「现在几点」，可以瞬间跳到三天后 |
| `CronSleeper` | 控制 sleep 行为，测试不用真等一秒 |
| `BlockingSleeper` | 一个**永不返回**的 sleep，用来验证关闭能中断阻塞中的 worker |
| `idGenerator` / `eventIdGenerator` | 产生可预测 id，输出能和文档对照 |
| `atomicReplace` | 注入写失败，验证状态不漂移 |
| `outboxCapacity` | 调成 1，容量满的行为一步就能测到 |

这六个注入点不是为了业务需要，是为了**让测试和探测拿到确定输出**。整套 ch14 测试**不需要 API Key，不访问网络，不睡真实时间**——4 秒跑完。

`BlockingSleeper` 值得单说。它模拟「worker 卡在 sleep 里」，用来验证 `close()` 真的能通过 signal 把它叫醒。如果不注入这个，你只能测「正常情况下能关掉」，测不到「卡住的时候能不能关掉」——而后者才是关闭逻辑真正要解决的问题。

### 关闭顺序：反过来

组合根把资源按这个顺序交给 Runner：

```typescript
const resources = Object.freeze([
  backgroundSupervisor,
  cronRuntime,
]);
```

`AgentRunner.close()` **逆序**关闭，所以先执行 `cronRuntime.close()`，再关 `backgroundSupervisor`。

顺序不能颠倒。因为 cron scheduler 是 supervisor 管的 worker：

```text
✅ 先关 cron → scheduler 停了 → 再关 supervisor → 没有活跃 worker，干净退出
❌ 先关 supervisor → 它发现还有个 cron-scheduler 在跑 → 要么强行取消，要么超时报错
```

这是一条通用规律：**依赖别人的先关，被依赖的后关。** 和构造顺序正好相反。

`cronRuntime.close()` 内部做四件事，顺序同样重要：

```text
① 标记 #closed = true          ← 新的 tick 立即失败，不再进入
② abort scheduler + await worker ← 停掉循环
③ await #tickTail               ← 等已经进去的 tick 走完
④ releaseLeader()               ← 释放 leader 锁
```

第 ③ 步容易漏。`#tickTail` 是一个 Promise 链，串行化所有 tick。如果不等它，`close()` 返回时可能还有一个 tick 正在改快照——然后 leader 锁已经释放了，另一个 session 可能同时在写。

第 ① 步和第 ④ 步的配合也有讲究：标记 closed 在最前面，意味着**等锁的后续 tick 醒来后会看到 closed 状态并明确失败**（抛 `CronClosedError`），不会在 close 返回后重新获取 leader。

多个失败会被聚合成 `AggregateError` 一起抛出，而不是只报第一个。关闭路径上「报全部错误」比「快速失败」重要——你需要知道所有没关干净的地方。

CLI 构建失败时也按同样纪律清理：`Cron → supervisor → OpenAI model`。**关闭返回后不应再有活跃 worker。**

---

## 接进组合根

### 一个能力位

```typescript
const P13 = new CapabilitySet([...P12.capabilities, "background"]);
const P14 = new CapabilitySet([...P13.capabilities, "cron"]);
```

P14 = P13 + 一个 `cron` 能力位，一共 17 个能力位。实测：

```text
P12: background=false cron=false
P13: background=true cron=false
P14: background=true cron=true
P14 能力数 = 17
```

`P13.capabilities.has("cron") === false` 是一条测试断言。**新章节不能反向污染旧 profile**——这是整个系列一直在守的规矩。

### 四道配对检查

P14 的 `BuildDependencies` 要求显式提供 `cronRuntime`，并且有四道双向检查：

| 检查 | 报错信息 | 防的是什么 |
| --- | --- | --- |
| P14 缺 cronRuntime | `cronRuntime is required for chapter 14 or later` | 声明有能力却没运行时 |
| P13 传了 cronRuntime | `cronRuntime requires chapter 14 or later` | 构造了资源却没接入 |
| supervisor 不是同一个 | `cronRuntime must share the background supervisor and event inbox` | 两套外观相似但互不相通的运行时 |
| inbox 不是同一个 | 同上 | 事件发到了没人监听的队列 |

第三、四道最值得说。想象这个 bug：

```text
CLI 造了 supervisor A 和 inbox A，交给 Runner
CronRuntime 内部自己造了 supervisor B 和 inbox B
```

代码能跑，类型能过，什么都不报错。但 cron 事件全部发进 inbox B，而 Runner 在监听 inbox A——**定时任务永远不会触发，而且没有任何错误信息**。这类 bug 排查起来极其痛苦，所以组合根直接把它变成启动时的显式失败。

### 14 个工具，schedule_cron 排最后

```text
 1. shell           8. load_skill
 2. read_file       9. create_task
 3. write_file     10. get_task
 4. edit_file      11. list_tasks
 5. glob           12. claim_task
 6. todo_write     13. complete_task
 7. task           14. schedule_cron   ← 本章新增
```

测试断言的是**精确顺序**，不是「包含这些工具」。顺序稳定有两个好处：一是 prompt 缓存命中率高（工具列表是 system prompt 的一部分），二是新章节插队会立刻被测试发现。

P13 和 P14 的 `shell` schema **完全相同**。本章不反向改变上一章的能力——这也是一条测试断言。

### CLI 怎么接线

```typescript
// 一个共享的 EventInbox，两个消费者
const inbox = supervisor.eventInbox;

return new CronRuntime({
  store: new JsonCronStore(workspace),
  inbox,
  supervisor,
  clock: { now: () => new Date() },
});
```

然后在 `buildAgent` 之后绑定 wakeup 并启动：

```typescript
cronRuntime.bindWakeup(async () => {
  await runner.runEvents();
});
cronRuntime.start();
```

为什么 `bindWakeup` 必须在 `buildAgent` 之后？因为它需要 `runner`，而 `runner` 需要 `cronRuntime` 作为 `eventPump`。这是一个循环依赖，用「先构造，后绑定」打破：

```text
① 造 cronRuntime（还没有 wakeup）
② 造 runner（拿 cronRuntime 当 eventPump）
③ 把 runner.runEvents 绑给 cronRuntime
④ cronRuntime.start()
```

顺序错了会怎样？如果在 ② 之前就 `start()`，scheduler 可能在 runner 存在之前就 tick 到一个到期 job。那时候没有 wakeup，事件会留在 inbox 里等下一次 tick——**不会丢，但会晚一秒**。所以这个顺序是为了行为确定，不是为了避免崩溃。

### 两个入口

固定章节入口：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch14 -- --prompt "每天上海时间 9 点检查 CI，周期执行并持久化"
```

通用入口：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run agent-tutorial -- run --chapter 14 --prompt "每天上海时间 9 点检查 CI，周期执行并持久化"
```

两个入口绑定同一个 P14 Bootstrap，行为完全一致。模型调用 `schedule_cron` 后，durable 状态会写到当前 workspace 的 `.agent_tutorial/cron/state.json`。

---

## 运行第 14 章

下面的命令都在 PowerShell 里执行。跟着走一遍，你会亲眼看到 `state.json` 出现在磁盘上。

### 第 0 步：确认环境

```powershell
node --version
npm --version
```

Node 需要 ≥ 20.12。

### 第 1 步：进入代码目录

```powershell
Set-Location 'F:\笔记\Agent实操\code'
```

后面所有命令都在这个目录下跑。

### 第 2 步：安装依赖

```powershell
npm install
```

只需做一次。本章新增的运行时依赖是 `cron-parser@5.6.2`（锁定版本）。

### 第 3 步：先跑测试，确认基线是好的

```powershell
npm run test:ch14
```

预期输出：

```text
 Test Files  39 passed (39)
      Tests  318 passed (318)
   Duration  4.06s
```

**先确认基线，再去跑真实模型。** 否则出问题时分不清是环境问题还是代码问题。

顺便你会在 stderr 上看到一行：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这是正常的。** `ch14-entry.test.ts` 故意在不配置凭证的情况下调入口，断言它会退出码 `2` 并把缺失项一次性报全。看到这行说明那条测试正在生效。

### 第 4 步：配置模型凭证

```powershell
$env:OPENAI_API_KEY = "sk-..."
$env:OPENAI_BASE_URL = "https://your-endpoint/v1"
$env:OPENAI_MODEL = "gpt-4o-mini"
$env:OPENAI_FALLBACK_MODEL = "gpt-4o"
```

四个都要设。第 11 章那套 fallback 机制要求主模型和兜底模型都明确指定，不猜。

### 第 5 步：真跑

```powershell
npm run ch14 -- --prompt "每 2 分钟检查一次当前目录有哪些文件，周期执行并持久化"
```

这个 prompt 刻意选了 **2 分钟**而不是「每天 9 点」——你等不了到明天。它也刻意让任务本身很轻（列目录），这样你能专注看调度行为。

### 你应该看到什么

```text
1. 模型调用 schedule_cron，参数里有 cron、prompt、timezone、recurring、durable
2. 工具返回成功，带 job id 和 next_run_at_utc
3. 模型给一句确认回答
4. 进程保持运行（scheduler 在跑）
5. 大约 2 分钟后，一条 runtime_event 出现，Agent 自己开始干活
```

**关键观察点是第 5 步。** 你什么都没做，Agent 自己动了。这就是本章的全部意义。

如果等了 2 分钟什么都没发生，去看下面的排错表。

### 第 6 步：看磁盘上留下了什么

```powershell
Get-Content .agent_tutorial\cron\state.json | ConvertFrom-Json | ConvertTo-Json -Depth 5
```

你会看到和前面「长什么样」那节一样的结构：一个 `jobs` 数组、一个 `outbox` 数组、一个 `version: 1`。

看看目录里还有什么：

```powershell
Get-ChildItem .agent_tutorial\ -Force
Get-ChildItem .agent_tutorial\cron\ -Force
```

Agent 还在跑的时候，预期能看到：

```text
.agent_tutorial\
  cron\
    state.json        ← 单一权威快照
    leader\           ← leader 锁盯着的目标目录（建完就一直留着）
    leader.lock\      ← leader 锁本体（持有 lease 期间存在）
```

三个东西的生命周期完全不同，值得逐个说清楚：

| 名字 | 什么时候出现 | 什么时候消失 |
| --- | --- | --- |
| `state.json` | 第一个 durable job 落盘时 | 不会自己消失，它就是权威状态 |
| `leader\` | store 第一次准备路径时 `mkdir` 出来 | **不消失**，它只是锁要盯的目标 |
| `leader.lock\` | 抢到 leader lease 时 | `close()` 释放 lease 时 |
| `.agent_tutorial\.cron.lock` | 一次快照读改写的**开始** | 同一次读改写的**结束** |

上面那个树里**没有** `.cron.lock`，这不是漏写。它在 `.agent_tutorial\` 下而不是 `cron\` 下，而且持有时间只覆盖一次读改写、通常是毫秒级——**你几乎不可能用 `Get-ChildItem` 抓到它**。想亲眼看见它，得在写盘那一刻插一脚，实验 12 会用注入的 `atomicReplace` 做到这件事。

再说明一个容易看错的细节：`leader` 是锁的**目标路径**，`proper-lockfile` 真正创建的是紧挨着它的 `leader.lock`（也是个目录）。所以后面提到「leader 锁」时，两个名字指的是同一件事的两面。

如果进程被强杀，`leader.lock` 会残留。不用手工删——`stale` 设成了 30 秒，30 秒后下一个 session 就能接管。

### 第 7 步：验证 durable 真的有用

关掉 Agent（`Ctrl+C`），然后重新启动：

```powershell
npm run ch14 -- --prompt "现在有哪些定时任务在等着"
```

`state.json` 里的 job 会被恢复。注意这里有个体验上的限制：**本章没有 `list_cron_jobs` 工具**，所以模型没法真的查出来告诉你。你只能自己看文件。这个缺失是刻意的，后面「刻意没做什么」会讲。

真正能验证恢复的方式是：让 job 的下一次触发时间落在重启之后，然后等着看它是否触发。

### 常见报错排查

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| `cronRuntime is required for chapter 14 or later` | 用 P14 profile 但没传 cronRuntime | 检查是不是绕过 `cli.ts` 自己组装了 |
| `cronRuntime requires chapter 14 or later` | 给 P13 或更早传了 cronRuntime | 去掉那个参数 |
| `cronRuntime must share the background supervisor and event inbox` | supervisor 或 inbox 不是同一个实例 | 看 `createCronRuntime`，inbox 要用 `supervisor.eventInbox` |
| `Error [cron_expression_error]: ...exactly five fields` | 表达式段数不对 | 五段，不带秒 |
| `Error [cron_expression_error]: Unknown Cron timezone` | 时区名拼错 | 用 IANA 名，如 `Asia/Shanghai`，不是 `CST` 或 `+08:00` |
| `Error [cron_expression_error]: Invalid descending Cron range` | 写了 `8-2` 这样的反向范围 | 拆成两段：`8-31,1-2` |
| `Error [cron_storage_error]` | 快照损坏或版本不符 | 见下面「一个坏字段毒化整个快照」 |
| `Error [cron_closed]` | 关闭之后还在调 schedule/tick | 检查是不是在 `close()` 之后又用了 runtime |
| 等了很久没触发 | 时区算出来的 UTC 时刻还没到 | 用实验 2 的脚本核对 `next_run_at_utc` |
| 到期了但 Agent 没动 | 另一个 session 抢到了 leader | 关掉其他 session，或看实验 8 |
| 事件触发两次 | 正常不会发生 | 检查是不是有两个进程各自持有 leader（锁被判 stale） |
| Windows 上偶发 `EBUSY` / `EPERM` | 杀毒软件或索引服务占着文件 | 已内置 ≤100 次重试；持续出现就把工作目录排除在扫描外 |

### 一个坏字段毒化整个快照

这条单独说，因为表现很反直觉。

手工在 `state.json` 里加一个不认识的字段：

```powershell
$state = Get-Content .agent_tutorial\cron\state.json | ConvertFrom-Json
$state | Add-Member -NotePropertyName "enabled" -NotePropertyValue $true
$state | ConvertTo-Json -Depth 5 | Set-Content .agent_tutorial\cron\state.json -Encoding utf8
```

然后启动 Agent：

```text
Error [cron_storage_error]: ...
```

**一个多余的字段让整份快照不可用。** 因为 `requireExactKeys` 要求键集合精确匹配，多一个就失败。

这是 fail closed 的一贯做法。看起来严格得过分，但考虑替代方案：如果忽略未知字段，那么一个手工写错键名的 job（比如把 `next_run_at_utc` 打成 `next_run_at_utf`）会被当成「缺少必填字段」还是「多了个未知字段」？两种解释都可能，而且都不该静默通过。

**要么完整可信，要么整体不可用。** 清理办法是删掉那个字段，或者直接删掉整个 `state.json` 重新开始（会丢失已保存的 job）。

---

## 12 个动手小实验

前面所有结论，我都是用脚本跑出来的，不是推出来的。这一节把这些脚本交给你。

每个实验都很短，目的都是**让一条抽象规则变成你亲眼看到的一行输出**。建议按顺序做，后面几个会用到前面建立的直觉。

### 准备：先建一个探测脚本

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType File -Path scripts\probe-ch14.mts
```

脚本的公共头部（所有实验共用）：

```ts
// scripts/probe-ch14.mts
import { mkdtemp, rm, readFile } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { JsonCronStore } from "../chapters/ch14/src/adapters/cron-json.ts";
import { JsonBackgroundJobStore } from "../chapters/ch14/src/adapters/background-json.ts";
import { EventInbox } from "../chapters/ch14/src/core/events.ts";
import { AgentRunner } from "../chapters/ch14/src/core/loop.ts";
import { assistantMessage, toolCall } from "../chapters/ch14/src/core/messages.ts";
import { PermissionPolicy, PermissionRule } from "../chapters/ch14/src/core/permissions.ts";
import { ToolRegistry, toolSuccess } from "../chapters/ch14/src/core/tools.ts";
import { JobSupervisor } from "../chapters/ch14/src/features/background.ts";
import {
  CronRuntime,
  nextCronOccurrence,
  validateCronExpression,
} from "../chapters/ch14/src/features/cron.ts";

// 每个实验都在一个临时目录里跑，互不干扰
async function withTempDir<T>(fn: (dir: string) => Promise<T>): Promise<T> {
  const dir = await mkdtemp(join(tmpdir(), "ch14-probe-"));
  try {
    return await fn(dir);
  } finally {
    await rm(dir, { recursive: true, force: true });
  }
}

// 造出 ...0401、...0501 这样可预测的 id，方便和文档对照
function seqId(start = 0x401): () => string {
  let next = start;
  return () => {
    const tail = next.toString(16).padStart(12, "0");
    next += 1;
    return `00000000-0000-4000-8000-${tail}`;
  };
}

// 用命令行参数选跑哪个实验
const EXPERIMENT = process.argv[2] ?? "all";
function section(name: string): boolean {
  const hit = EXPERIMENT === "all" || EXPERIMENT === name;
  if (hit) console.log(`\n===== ${name} =====`);
  return hit;
}
```

`seqId` 是这一节的关键便利：真实运行用 `randomUUID`，每次输出都不一样，很难对照。把 `idGenerator` / `eventIdGenerator` 换成递增序列后，你看到的 id 会和文中一模一样。

> 这也是 `JsonCronStore` 把两个 id 生成器做成可注入参数的原因——不是业务需要，是为了**让测试和探测拿到确定输出**。

运行方式：

```powershell
npx tsx scripts\probe-ch14.mts expr      # 跑单个实验
npx tsx scripts\probe-ch14.mts all       # 全部跑一遍
```

每个实验开头那行 `if (section("xxx"))` 里的名字就是命令行参数，12 个实验依次是：`expr`、`tz`、`state`、`tick`、`misfire`、`durable`、`capacity`、`leader`、`trace`、`defer`、`tools`、`locks`。

### 实验 1：表达式的接受与拒绝边界

**要验证的**：五段限制、字段越界、反向范围各自报什么错。

```ts
if (section("expr")) {
  const cases: Array<[string, string, string]> = [
    ["* * * * *", "UTC", "2026-06-01T09:00:00Z"],
    ["0 9 * * *", "UTC", "2026-06-01T09:00:00Z"],
    ["*/15 9-10 * * 1,3,5", "UTC", "2026-06-01T09:00:00Z"],
    ["0 9 * * 1-5", "UTC", "2026-06-05T09:00:00Z"],
    ["0 9 1 * 1", "UTC", "2026-06-02T00:00:00Z"],
  ];
  for (const [expr, tz, after] of cases) {
    const next = nextCronOccurrence(expr, tz, new Date(after));
    console.log(`${expr.padEnd(22)} after ${after} -> ${next.toISOString()}`);
  }
  const bad = ["* * * *", "* * * * * *", "61 * * * *", "*/0 * * * *",
               "0 9 8-2 * *", "0 9 * 13 *", "0 9 * * 8"];
  for (const expr of bad) {
    try {
      validateCronExpression(expr);
      console.log(`${expr.padEnd(16)} -> 通过（不该出现）`);
    } catch (error) {
      console.log(`${expr.padEnd(16)} -> ${(error as Error).message}`);
    }
  }
}
```

实测输出：

```text
* * * * *              after 2026-06-01T09:00:00Z -> 2026-06-01T09:01:00.000Z
0 9 * * *              after 2026-06-01T09:00:00Z -> 2026-06-02T09:00:00.000Z
*/15 9-10 * * 1,3,5    after 2026-06-01T09:00:00Z -> 2026-06-01T09:15:00.000Z
0 9 * * 1-5            after 2026-06-05T09:00:00Z -> 2026-06-08T09:00:00.000Z
0 9 1 * 1              after 2026-06-02T00:00:00Z -> 2026-06-08T09:00:00.000Z
* * * *          -> Cron expression must contain exactly five fields
* * * * * *      -> Cron expression must contain exactly five fields
61 * * * *       -> Invalid Cron expression: 61 * * * *
*/0 * * * *      -> Invalid Cron expression: */0 * * * *
0 9 8-2 * *      -> Invalid descending Cron range
0 9 * 13 *       -> Invalid Cron expression: 0 9 * 13 *
0 9 * * 8        -> Invalid Cron expression: 0 9 * * 8
```

三处值得停一下：

- **第 2 行**：基准就是 `09:00:00`，结果是**第二天**。`next_run` 严格大于当前时刻，不会返回「就是现在」。
- **第 4 行**：基准 6 月 5 日是周五，`1-5` 是周一到周五，结果跳到 6 月 8 日（周一）——正确跳过了周末。
- **第 5 行**：这就是 DOM/DOW 的 OR。6 月 8 日是周一，所以它中了，不用等到 7 月 1 号。

### 实验 2：时区与 DST

**要验证的**：同一个表达式在不同时区得到不同 UTC instant；gap 推进、fold 保留两个时刻。

```ts
if (section("tz")) {
  const rows: Array<[string, string, string]> = [
    ["0 9 * * *", "UTC", "2026-06-01T00:30:00Z"],
    ["0 9 * * *", "Asia/Shanghai", "2026-06-01T00:30:00Z"],
    ["0 9 * * *", "Europe/London", "2026-06-01T00:30:00Z"],
    ["0 9 * * *", "America/New_York", "2026-06-01T00:30:00Z"],
  ];
  for (const [expr, tz, after] of rows) {
    console.log(`${tz.padEnd(18)} ${expr} -> ${nextCronOccurrence(expr, tz, new Date(after)).toISOString()}`);
  }
  console.log("-- DST gap（不存在的 02:30）--");
  console.log(`  ${nextCronOccurrence("30 2 * * *", "America/New_York", new Date("2026-03-07T08:00:00Z")).toISOString()}`);
  console.log("-- DST fold（重复的 01:30）--");
  const first = nextCronOccurrence("30 1 * * *", "America/New_York", new Date("2026-11-01T04:59:00Z"));
  const second = nextCronOccurrence("30 1 * * *", "America/New_York", first);
  console.log(`  第一次 -> ${first.toISOString()}`);
  console.log(`  第二次 -> ${second.toISOString()}`);
  try {
    nextCronOccurrence("0 9 * * *", "Mars/Olympus", new Date("2026-06-01T00:00:00Z"));
  } catch (error) {
    console.log(`  Mars/Olympus -> ${(error as Error).message}`);
  }
}
```

实测输出：

```text
UTC                0 9 * * * -> 2026-06-01T09:00:00.000Z
Asia/Shanghai      0 9 * * * -> 2026-06-01T01:00:00.000Z
Europe/London      0 9 * * * -> 2026-06-01T08:00:00.000Z
America/New_York   0 9 * * * -> 2026-06-01T13:00:00.000Z
-- DST gap（不存在的 02:30）--
  2026-03-08T07:00:00.000Z
-- DST fold（重复的 01:30）--
  第一次 -> 2026-11-01T05:30:00.000Z
  第二次 -> 2026-11-01T06:30:00.000Z
  Mars/Olympus -> Unknown Cron timezone: Mars/Olympus
```

**动手改一下**：把 `America/New_York` 换成你的本地时区，看 `0 9 * * *` 算出来是几点 UTC。如果你在上海，应该得到 `01:00:00Z`。这个练习能帮你建立「本地时间 ↔ UTC」的直觉。

### 实验 3：state.json 到底长什么样

**要验证的**：一次 tick 同时改了三个地方。

```ts
if (section("state")) {
  await withTempDir(async (root) => {
    const store = new JsonCronStore(root, {
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const job = await store.scheduleCron({
      cron: "0 9 * * *", prompt: "check CI", timezone: "UTC",
      recurring: true, durable: true, identity: "user",
      nowUtc: new Date("2026-06-01T12:00:30Z"),
    });
    const statePath = join(root, ".agent_tutorial", "cron", "state.json");
    console.log("-- 刚创建 --");
    console.log(await readFile(statePath, "utf8"));
    await store.tick(job.nextRunAtUtc);
    console.log("-- tick 之后 --");
    console.log(await readFile(statePath, "utf8"));
  });
}
```

输出就是前面「长什么样」那节贴的两段 JSON。**对着看那三处变化**：`next_run_at_utc` 推进、`last_slot_at_utc` 从 `null` 变成具体时刻、`outbox` 从空数组变成一个事件。

**动手改一下**：把 `recurring` 改成 `false`，再跑一次。你会看到 tick 之后 `jobs` 变成空数组，但 `outbox` 里那个事件还在——这就是 Job/Event 分离。

### 实验 4：one-shot 的完整生命周期

**要验证的**：定义被删、事件保留、重复 tick 不产生第二个事件、ack 只成功一次。

```ts
if (section("tick")) {
  await withTempDir(async (root) => {
    const store = new JsonCronStore(root, {
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const once = await store.scheduleCron({
      cron: "* * * * *", prompt: "one shot", timezone: "UTC",
      recurring: false, durable: true, identity: "user",
      nowUtc: new Date("2026-06-01T12:00:30Z"),
    });
    console.log(`one-shot 创建: next=${once.nextRunAtUtc.toISOString()}`);
    const created = await store.tick(once.nextRunAtUtc);
    console.log(`tick -> 产生 ${created.length} 个 event, slot=${created[0]?.slotAtUtc.toISOString()}`);
    console.log(`定义还在吗: ${(await store.listJobs()).length === 0 ? "已删除" : "还在"}`);
    console.log(`outbox: ${(await store.pendingEvents()).map((e) => e.eventId).join(",")}`);
    console.log(`再 tick 一次 -> ${(await store.tick(once.nextRunAtUtc)).length} 个 event`);
    console.log(`ack 第一次 -> ${await store.ackEvent(created[0]!.eventId)}`);
    console.log(`ack 第二次 -> ${await store.ackEvent(created[0]!.eventId)}`);
  });
}
```

实测输出：

```text
one-shot 创建: next=2026-06-01T12:01:00.000Z
tick -> 产生 1 个 event, slot=2026-06-01T12:01:00.000Z
定义还在吗: 已删除
outbox: 00000000-0000-4000-8000-000000000501
再 tick 一次 -> 0 个 event
ack 第一次 -> true
ack 第二次 -> false
```

最后两行是重点：`ackEvent` 返回布尔值，**第二次是 `false` 而不是抛错**。这个设计让重复 ack 变成一个可判断的结果——调用方能区分「我刚刚确认成功了」和「这条早就被别人确认过了」。

### 实验 5：停机三天只补一个

**要验证的**：misfire 策略是「补最早一个，然后追平」，不是「补齐全部」。

```ts
if (section("misfire")) {
  await withTempDir(async (root) => {
    const store = new JsonCronStore(root, {
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const job = await store.scheduleCron({
      cron: "* * * * *", prompt: "every minute", timezone: "UTC",
      recurring: true, durable: true, identity: "user",
      nowUtc: new Date("2026-06-01T12:00:30Z"),
    });
    console.log(`创建时 next = ${job.nextRunAtUtc.toISOString()}`);
    const later = new Date(job.nextRunAtUtc.valueOf() + 3 * 86_400_000);
    console.log(`三天后开机, now = ${later.toISOString()}`);
    const created = await store.tick(later);
    console.log(`补发了 ${created.length} 个 event, slot = ${created.map((e) => e.slotAtUtc.toISOString()).join(",")}`);
    const updated = await store.getJob(job.id);
    console.log(`lastSlotAtUtc = ${updated.lastSlotAtUtc?.toISOString()}`);
    console.log(`nextRunAtUtc  = ${updated.nextRunAtUtc.toISOString()}  (> now: ${updated.nextRunAtUtc > later})`);
  });
}
```

实测输出：

```text
创建时 next = 2026-06-01T12:01:00.000Z
三天后开机, now = 2026-06-04T12:01:00.000Z
补发了 1 个 event, slot = 2026-06-01T12:01:00.000Z
lastSlotAtUtc = 2026-06-01T12:01:00.000Z
nextRunAtUtc  = 2026-06-04T12:02:00.000Z  (> now: true)
```

**「每分钟」的 job 停了三天，只补 1 个事件。** 如果换成朴素实现，这里会是 4320 个。

而且补的是**最早那个**（`06-01T12:01`），不是最新那个。这样模型能从 `slot_at_utc` 和当前时间的差值看出「漏了三天」。

**动手改一下**：把 `3 * 86_400_000` 改成 `30 * 86_400_000`（三十天），看结果。仍然是 1 个事件——策略不随停机时长变化。

### 实验 6：durable vs session-only

**要验证的**：重建 store 后，durable 的还在，session-only 的干净消失。

```ts
if (section("durable")) {
  await withTempDir(async (root) => {
    const store = new JsonCronStore(root, {
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const now = new Date("2026-06-01T12:00:30Z");
    const durable = await store.scheduleCron({
      cron: "* * * * *", prompt: "durable", timezone: "UTC",
      recurring: false, durable: true, identity: "user", nowUtc: now,
    });
    await store.scheduleCron({
      cron: "* * * * *", prompt: "session", timezone: "UTC",
      recurring: false, durable: false, identity: "user", nowUtc: now,
    });
    await store.tick(durable.nextRunAtUtc);
    console.log(`同一实例 outbox = ${(await store.pendingEvents()).map((e) => `${e.eventId.slice(-4)}:${e.prompt}`).join(", ")}`);
    const rebuilt = new JsonCronStore(root);   // 模拟进程重启
    console.log(`重建后 jobs   = ${(await rebuilt.listJobs()).length} 个`);
    console.log(`重建后 outbox = ${(await rebuilt.pendingEvents()).map((e) => `${e.eventId.slice(-4)}:${e.prompt}`).join(", ")}`);
  });
}
```

实测输出：

```text
durable job = 00000000-0000-4000-8000-000000000401
session job = 00000000-0000-4000-8000-000000000402
同一实例 outbox = 0501:durable, 0502:session
重建后 jobs   = 0 个
重建后 outbox = 0501:durable
```

第三行两个事件都在，第五行只剩 durable 那个。**session-only 的消失是静默的、干净的**——没有报错，也没有留下半截状态。

`new JsonCronStore(root)` 就是「进程重启」的最小模拟：新实例的内存 Map 是空的，只能从 `state.json` 读到东西。

### 实验 7：outbox 满了怎么办

**要验证的**：容量满时，被挤掉的 job 的 `next_run_at_utc` **一动不动**。

```ts
if (section("capacity")) {
  await withTempDir(async (root) => {
    const store = new JsonCronStore(root, {
      outboxCapacity: 1,               // 故意调成 1
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const now = new Date("2026-06-01T12:00:30Z");
    const first = await store.scheduleCron({
      cron: "* * * * *", prompt: "first", timezone: "UTC",
      recurring: true, durable: true, identity: "user", nowUtc: now,
    });
    const second = await store.scheduleCron({
      cron: "* * * * *", prompt: "second", timezone: "UTC",
      recurring: true, durable: true, identity: "user", nowUtc: now,
    });
    const created = await store.tick(first.nextRunAtUtc);
    console.log(`容量 1, 两个 job 同时到期 -> 产生 ${created.length} 个 event (${created[0]?.prompt})`);
    console.log(`first  next: ${(await store.getJob(first.id)).nextRunAtUtc.toISOString()} (推进了)`);
    const s = await store.getJob(second.id);
    console.log(`second next: ${s.nextRunAtUtc.toISOString()} (原地不动: ${s.nextRunAtUtc.valueOf() === second.nextRunAtUtc.valueOf()})`);
    await store.ackEvent(created[0]!.eventId);
    const again = await store.tick(second.nextRunAtUtc);
    console.log(`ack 之后再 tick -> 产生 ${again.length} 个 event (${again[0]?.prompt})`);
  });
}
```

实测输出：

```text
容量 1, 两个 job 同时到期 -> 产生 1 个 event (first)
first  next: 2026-06-01T12:02:00.000Z (推进了)
second next: 2026-06-01T12:01:00.000Z (原地不动: true)
ack 之后再 tick -> 产生 1 个 event (second)
```

**`原地不动: true` 是这个实验的全部意义。** 容量满不会让工作静默消失，只会让它等下一轮。

对比一下错误实现会输出什么：

```text
second next: 2026-06-01T12:02:00.000Z    ← 推进了，但事件没产生
ack 之后再 tick -> 产生 0 个 event        ← 这次工作永久丢失
```

而且错误实现**看起来完全正常**——job 状态显示「已经处理到 12:02 了」。这类静默丢失是最难排查的一类 bug。

### 实验 8：两个 session 抢 leader

**要验证的**：只有一个 session 能为 durable job 产生事件；leader 换人时工作不丢。

```ts
if (section("leader")) {
  await withTempDir(async (root) => {
    const inboxA = new EventInbox();
    const supA = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox: inboxA });
    const inboxB = new EventInbox();
    const supB = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox: inboxB });
    const creator = new JsonCronStore(root, { idGenerator: seqId(0x401) });
    const job = await creator.scheduleCron({
      cron: "* * * * *", prompt: "durable job", timezone: "UTC",
      recurring: false, durable: true, identity: "user",
      nowUtc: new Date("2026-06-01T12:00:30Z"),
    });
    const clock = { now: () => job.nextRunAtUtc };
    const first = new CronRuntime({
      store: new JsonCronStore(root, { eventIdGenerator: seqId(0x501) }),
      inbox: inboxA, supervisor: supA, clock,
    });
    const second = new CronRuntime({
      store: new JsonCronStore(root, { eventIdGenerator: seqId(0x501) }),
      inbox: inboxB, supervisor: supB, clock,
    });
    await first.tick();
    await second.tick();
    console.log(`session A drain: ${first.drainEvents().map((e) => e.eventId.slice(-4)).join(",") || "(空)"}`);
    console.log(`session B drain: ${second.drainEvents().map((e) => e.eventId.slice(-4)).join(",") || "(空)"}`);
    await first.close();
    console.log("A 关闭, leader 释放");
    await second.tick();
    const recovered = second.drainEvents();
    console.log(`session B 再 tick: ${recovered.map((e) => e.eventId.slice(-4)).join(",") || "(空)"}`);
    await second.acknowledgeEvents(recovered);
    console.log(`ack 后 outbox: ${(await new JsonCronStore(root).pendingEvents()).length} 条`);
    await second.close();
    await supA.close();
    await supB.close();
  });
}
```

实测输出：

```text
session A drain: 0501
session B drain: (空)
A 关闭, leader 释放
session B 再 tick: 0501
ack 后 outbox: 0 条
```

四条结论一次看到：

1. **A 拿到 leader**，产生并收到事件。
2. **B 什么也没拿到**——同一个 slot 没有被投递两次。
3. A 关闭后 leader 释放，**B 接管**。
4. B 拿到的是**同一个事件 `0501`**——A 关闭前没 ack，工作意图还在 outbox 里。

第 4 条是整个持久化设计的兑现。如果 A 是被强杀的（而不是正常 close），30 秒后锁被判 stale，B 同样能接管。

**动手改一下**：把 `durable: true` 改成 `false`，再跑。实测结果是**三行全空**：

```text
session A drain: (空)
session B drain: (空)
session B 再 tick: (空)
```

一开始可能觉得意外，但这正是 session-only 的定义在起作用：那个 job 只活在 `creator` 这个 store 实例的内存里，而 `first` 和 `second` 是**另外两个实例**，它们从 `state.json` 里读不到它，也没有任何途径看到别人的内存。

**session-only 的隔离边界是 store 实例，不是进程。** 这个实验顺手证明了 session-only job 绝不会泄漏到 durable 快照里——连同进程的另一个实例都看不见它。

### 实验 9：完整的 Agent 轨迹

**要验证的**：事件以 `user` 消息进入 history；工具看到的 identity 是**创建者**，不是当前交互者。

这个实验就是开头那条轨迹的来源。它需要一个假模型和一个记录上下文的工具：

```ts
if (section("trace")) {
  await withTempDir(async (root) => {
    const inbox = new EventInbox();
    const supervisor = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox });
    const store = new JsonCronStore(root, {
      idGenerator: seqId(0x401), eventIdGenerator: seqId(0x501),
    });
    const job = await store.scheduleCron({
      cron: "* * * * *", prompt: "检查 CI 状态", timezone: "Asia/Shanghai",
      recurring: false, durable: true, identity: "cron-owner",
      nowUtc: new Date("2026-06-01T12:00:30Z"),
    });
    const runtime = new CronRuntime({
      store, inbox, supervisor, clock: { now: () => job.nextRunAtUtc },
    });

    // 一个只做一件事的工具：把它看到的上下文记下来
    const contexts: unknown[] = [];
    const { z } = await import("zod");
    const tools = new ToolRegistry();
    tools.register({
      name: "action", description: "Run the scheduled action.",
      inputSchema: z.object({ value: z.string() }).strict(), effect: "read",
      handler: async (input, context) => {
        contexts.push({ identity: context.identity, idempotencyKey: context.idempotencyKey });
        return toolSuccess(`CI: ${input.value}`);
      },
    });

    // 假模型：第一轮调工具，第二轮给结论
    let calls = 0;
    const model = {
      complete: async () => {
        calls += 1;
        if (calls === 1) {
          return {
            message: assistantMessage(null, [toolCall("cron-action", "action", '{"value":"green"}')]),
            finishReason: "tool_calls" as const,
          };
        }
        return { message: assistantMessage("CI 是 green，无需处理。"), finishReason: "stop" as const };
      },
    };

    const runner = new AgentRunner({
      model, tools, systemPrompt: "system", workspace: root,
      identity: "interactive-user",           // 注意：和 job 的 identity 不同
      permissionPolicy: new PermissionPolicy({
        rules: [new PermissionRule({ name: "allow-all", behavior: "allow", reason: "ok", matches: () => true })],
      }),
      eventPump: runtime,
      resources: [supervisor, runtime],
    });
    runtime.bindWakeup(async () => { await runner.runEvents(); });

    await runtime.tick();                     // 时间到了

    console.log(`模型请求次数 = ${calls}`);
    runner.history.forEach((message, index) => {
      const head = `${String(index + 1).padStart(2)}. ${message.role.padEnd(9)}`;
      if (message.role === "assistant") {
        const named = message.toolCalls.map((c) => `${c.name}(${c.arguments})#${c.id}`).join(", ");
        console.log(`${head} content=${message.content === null ? "(null)" : JSON.stringify(message.content)} toolCalls=[${named}]`);
      } else if (message.role === "tool") {
        console.log(`${head} toolCallId=${message.toolCallId} ${message.content}`);
      } else {
        console.log(`${head} ${message.content}`);
      }
    });
    console.log(`工具看到的上下文 = ${JSON.stringify(contexts)}`);
    console.log(`outbox 剩余 = ${(await store.pendingEvents()).length}`);
    await runner.close();
  });
}
```

实测输出（就是开头那段）：

```text
模型请求次数 = 2
 1. user      {"runtime_event":{"event_id":"...0501","job_id":"...0401","kind":"cron",
              "identity":"cron-owner","prompt":"检查 CI 状态","timezone":"Asia/Shanghai",
              "durable":true,"slot_at_utc":"2026-06-01T12:01:00.000Z"}}
 2. assistant content=(null) toolCalls=[action({"value":"green"})#cron-action]
 3. tool      toolCallId=cron-action CI: green
 4. assistant content="CI 是 green，无需处理。" toolCalls=[]
工具看到的上下文 = [{"identity":"cron-owner","idempotencyKey":"...0501"}]
outbox 剩余 = 0
```

注意 `runtime.tick()` 一句话触发了后面**全部**流程：产生事件 → 发布到 inbox → 调 wakeup → `runEvents()` → 进 history → ack → 跑两轮模型 → 工具执行。

**动手改一下**：把 `PermissionRule` 的 `behavior` 改成 `"deny"`。你会看到 `contexts` 是**空数组**——handler 一次都没被调用。这就是「触发时重新授权」那一节的实证。

### 实验 10：用户回合进行中到期

**要验证的**：cron 事件被延后，不污染用户对话，也不重复发布。

关键技巧是在工具 handler 里手工触发一次 tick，模拟「用户回合进行中定时任务到期」。下面只摘了核心几行，外层的 `if (section("defer")) { ... }` 和 runner 构造与实验 9 一样，照抄即可：

```ts
    tools.register({
      name: "probe", description: "Probe the context.",
      inputSchema: z.object({}).strict(), effect: "read",
      handler: async (_input, context) => {
        contexts.push({ identity: context.identity, idempotencyKey: context.idempotencyKey });
        await runtime.tick();          // ← 用户回合进行中，定时任务到期
        return toolSuccess("ok");
      },
    });
    // ... 省略 runner 构造（和实验 9 相同）
    await runner.run("帮我看一下");     // 一次普通用户回合
    console.log(`历史条数 = ${runner.history.length}`);
    console.log(`cron 事件是否已进历史 = ${runner.history.some((m) => m.content?.includes('"kind":"cron"'))}`);
    const after = await runner.runEvents();
    console.log(`显式 runEvents() -> ${after === undefined ? "undefined" : "有结果"}`);
    console.log(`现在进历史了吗 = ${runner.history.some((m) => m.content?.includes('"kind":"cron"'))}`);
    console.log(`再调一次 runEvents() -> ${(await runner.runEvents()) === undefined ? "undefined" : "有结果"}`);
```

实测输出：

```text
用户回合结束: 模型调用 2 次, 工具看到 [{"identity":"interactive-user"}]
历史条数 = 4
cron 事件是否已进历史 = false
显式 runEvents() -> 有结果, 模型调用累计 3 次
现在进历史了吗 = true
工具上下文 = [{"identity":"interactive-user"}]
再调一次 runEvents() -> undefined
```

四条结论：

1. **用户回合结束时 cron 事件没进历史**（`false`）——用户看到的对话是干净的。
2. **工具在用户回合里看到的 identity 是 `interactive-user`**，没被 cron 事件的身份污染。
3. `runEvents()` 之后事件才进历史，模型多跑一轮。
4. **再调一次返回 `undefined`**——被延后的事件没有重复发布。

第 4 条是最容易写错的地方。一个 naive 的延后实现会把事件既暂存在 Runner 里、又留在 inbox 里，结果处理两次。

### 实验 11：能力位

**要验证的**：新章节不反向污染旧 profile。

```ts
if (section("tools")) {
  const { P12, P13, P14 } = await import("../chapters/ch14/src/core/profiles.ts");
  for (const [name, set] of [["P12", P12], ["P13", P13], ["P14", P14]] as const) {
    console.log(`${name}: background=${set.capabilities.has("background")} cron=${set.capabilities.has("cron")}`);
  }
  console.log(`P14 能力数 = ${P14.capabilities.size}`);
}
```

实测输出：

```text
P12: background=false cron=false
P13: background=true cron=false
P14: background=true cron=true
P14 能力数 = 17
```

一眼能看出能力位是**严格累加**的。这个实验最短，但它守的规矩最重要：**加一章不能改变前面任何一章的行为。**

### 实验 12：抓住那把只活几毫秒的锁

**要验证的**：两把锁的生命周期长度差了几个数量级——前面「运行第 14 章」第 6 步里那五行输出，就是这个脚本跑出来的。

`.cron.lock` 只在一次快照读改写期间存在，用 `Get-ChildItem` 基本抓不到。想看见它，只能在写盘那一刻插一脚——而 `atomicReplace` 正好是个注入点：

```ts
if (section("locks")) {
  const { readdir, writeFile } = await import("node:fs/promises");
  await withTempDir(async (dir) => {
    const stateDir = join(dir, ".agent_tutorial");
    const cronDir = join(stateDir, "cron");
    const ls = async (d: string) => (await readdir(d)).sort().join(" | ");

    const store = new JsonCronStore(dir, {
      // 写盘前先把两个目录打印出来，此刻快照锁一定还被持有
      atomicReplace: async (path, content) => {
        console.log("写盘瞬间 .agent_tutorial/ ：", await ls(stateDir));
        console.log("写盘瞬间 cron/ ：          ", await ls(cronDir));
        await writeFile(path, content);
      },
    });

    await store.tryAcquireLeader();
    await store.scheduleCron({
      cron: "*/1 * * * *",
      prompt: "p",
      timezone: "UTC",
      recurring: true,
      durable: true,
      identity: "cron-owner",
      nowUtc: new Date("2026-06-01T12:00:00.000Z"),
    });

    console.log("写完后   .agent_tutorial/ ：", await ls(stateDir));
    console.log("写完后   cron/ ：          ", await ls(cronDir));
    await store.releaseLeader();
    console.log("释放 leader 后 cron/ ：     ", await ls(cronDir));
  });
}
```

实测输出：

```text
写盘瞬间 .agent_tutorial/ ： .cron.lock | cron
写盘瞬间 cron/ ：           leader | leader.lock
写完后   .agent_tutorial/ ： cron
写完后   cron/ ：           leader | leader.lock | state.json
释放 leader 后 cron/ ：      leader | state.json
```

对照着读这五行：

| 行 | 看什么 |
| --- | --- |
| 1 | `.cron.lock` 出现了——**只有在临界区里面才能看到它** |
| 2 | `leader.lock` 已经在了，但 `state.json` 还没写出来 |
| 3 | 同一次调用还没返回，`.cron.lock` 就已经不见了 |
| 4 | `state.json` 出现；`leader.lock` 依然在 |
| 5 | 只有显式 `releaseLeader()` 才让 `leader.lock` 消失 |

**一把是瞬时的临界区，一把是长期的身份。** 顺便注意 `leader` 这个空目录从头到尾都在——它只是锁要盯的目标路径，不是锁本身，所以不会被清理。

**动手改一下**：把 `await store.releaseLeader()` 那行注释掉，再跑一次。最后一行变成这样：

```text
释放 leader 后 cron/ ：      leader | leader.lock | state.json
```

`leader.lock` 没走。进程照样以退出码 0 结束——**没有任何报错提醒你漏了一步**。在临时目录里这没后果，因为整个目录随后就被删了；但在真实 workspace 里，这就是下一个 session 要等 30 秒 stale 判定才能接管的原因。这也是为什么 `CronRuntime.close()` 里那句 `releaseLeader()` 不能省：**析构顺序里漏一步，锁会活得比它保护的东西更久，而且是静默的。**

### 实验完成后清理

```powershell
Remove-Item scripts\probe-ch14.mts
```

所有实验都在系统临时目录里跑，不会在你的 workspace 里留下东西。

---

## 这章刻意没有做什么

一个能力清单容易让人以为「定时调度」已经做完了。其实 P14 只铺到了地基，有几件事是**明确决定不做**的。分清「没做」和「做不到」很重要，因为它决定了你在这套代码上还能安全地加什么。

| 没做的事 | 为什么不做 | 现在的替代 |
| --- | --- | --- |
| `list_cron_jobs` / `cancel_cron_job` | 每个新工具都要一整套 schema + 权限 + 测试 | 直接看 `state.json` |
| 抖动（jitter） | 需要额外的时间偏移策略和测试 | 固定 UTC slot，不打散 |
| 自动过期 | 「多久算过期」是产品决策 | job 永久有效直到删除 |
| QoS / 优先级 | 需要在事件通路上加分级 | 所有事件平等排队 |
| 操作系统级调度 | 超出 Agent 进程边界 | 进程停了就没人看表 |
| 文件监听 / Webhook | 事件源多样化是另一个主题 | 只有时间驱动 |
| 事件批量注入 | 会破坏身份隔离 | 每轮最多一个事件 |
| 补齐所有漏掉的 slot | 上下文会被历史事件吃光 | 只补最早一个，然后追平 |
| fold 去重 | 需要一层「本地时间语义」的判断 | UTC instant 是唯一真相，会执行两次 |
| P15 的 Teammate 和 Mailbox | 下一章的主题 | —— |

### 特别说一下 list 和 cancel

这是最容易觉得「顺手加上不就完了」的两个工具。为什么不加？

先看它们各自需要什么：

```text
list_cron_jobs   → 返回什么字段？要不要分页？要不要过滤 identity？
                   一个用户能看到别人的 job 吗？（权限问题）
cancel_cron_job  → 谁能取消？只有创建者吗？
                   取消一个正在 outbox 里等待的事件算不算取消成功？
                   取消 + 并发 tick 的竞态怎么办？
```

**每一个问号都是一次设计决策，都需要测试。** 尤其是 `cancel` 那三个问题：「取消」和「事件已经在 outbox 里」这两件事一撞，就变成了一个和「只发一次」同等复杂的仲裁问题。

本章要教的是**调度的正确性骨架**：表达式、时区、Job/Event 分离、原子快照、leader 仲裁、事件回灌、重新授权。加两个 CRUD 工具不会让这些道理更清楚，但会让代码量和测试量显著变大。

**它们不难，只是不属于这一章。**

### 「没做」不等于「设计没考虑」

看 `CronStore` 的接口，`getJob` 和 `listJobs` 已经存在了——实验里就在用它们。缺的只是**工具层的包装**：

```text
已有：store.listJobs()  ← 领域能力
缺的：list_cron_jobs 工具 ← schema、权限、错误码、测试
```

同样，`ackEvent` 返回布尔而不是抛错，正是为了让「取消一个 pending 事件」这类操作有可判断的结果。

**通路是通的，只是没有暴露成工具。** 这和「架构上根本做不到」是两码事。

### 特别说一下 fold 会执行两次

秋季回拨那天，「每天 01:30」的 job 会执行两次。这看起来像 bug，值得解释为什么不修。

要「修」它，你得引入一个新概念：「本地时间语义的同一次」。也就是说，状态里除了 UTC instant，还要记「这个本地时钟面已经用过了」。而一旦记了本地时间，你就重新引入了本章一开始极力避免的东西——**本地时间字符串作为状态**。

然后 gap 那边会立刻出问题：一个不存在的 `02:30` 被推进到 `03:00` 执行，那么「本地时钟面 02:30」到底算用过了没有？如果算，明年这一天的正常 `02:30` 会被跳过。

**一致地用 UTC instant，代价是一年一次的重复执行；引入本地语义，代价是一整类新的边界情况。** 前者可以由工具的幂等性化解（`idempotencyKey` 就在那里），后者只能靠更多代码和更多测试。这个交换在教学场景里很清楚。

---

## 与 Claude Code 的差异

P14 是教学实现，Claude Code 是产品。它们在这个能力上的差别，恰好能说明「教学版够用的地方」和「产品版必须额外做的地方」。

| 维度 | P14 | Claude Code |
| --- | --- | --- |
| 工具数量 | 1 个：`schedule_cron` | 3 个：`CronCreate` / `CronDelete` / `CronList` |
| 特性开关 | 无条件注册，只由 Profile 决定 | 编译时 + 远端标志 + 本地环境变量三重控制 |
| 存储 | `.agent_tutorial/cron/state.json`，job + outbox 单一快照 | `.claude/scheduled_tasks.json`，不保存未消费事件 |
| 锁 | `.cron.lock`（快照）+ `leader.lock`（选主） | `.scheduled_tasks.lock` 防同项目重复触发 |
| 时区 | 必填 IANA 名，UTC instant 持久化 | 按本地时区解释，模型不能显式传时区 |
| DST | gap 推进、fold 保留两个 instant，有测试 | 参考实现没有等价说明 |
| 防惊群 | 无抖动 | 重复任务加确定性抖动（≤ 周期 10%，上限 15 分钟） |
| 过期 | 无 | 重复任务 7 天自动过期（可配置，上限 30 天） |
| 任务上限 | 无 job 上限，只有 outbox 容量上限 | `MAX_JOBS = 50`，超限提示先取消一个 |
| 交付 | 进 `EventInbox`，空闲时逐条执行 | 入命令队列，带 `workload` 标签和更低 QoS |
| 调度器归属 | `JobSupervisor` 管的受管 worker | 每秒检查 + `chokidar` 监听状态文件变化 |

### 差异背后的两个原因

十一行差异，根子上是两件事。

**第一，产品要面对「用户会创建一堆 job」这个现实。**

```text
MAX_JOBS = 50    ← 有人会创建 500 个
7 天自动过期      ← 有人创建完就忘了
抖动             ← 一万个用户都设了「每天 9 点」，9:00:00 会打爆 API
```

这三样都不是架构问题，是**运营问题**。P14 是单人教学环境，遇不到这些。但值得注意的是：**它们都不需要改架构**。抖动是在算出 slot 之后加一个偏移，过期是多一个字段和一次检查，上限是创建时多一个判断。P14 的骨架能原样承载这三样。

**第二，产品要面对「用户会同时开着很多东西」这个现实。**

```text
chokidar 监听文件变化  ← 另一个 session 改了 job，我要立刻知道
workload 标签 + QoS   ← 定时任务不该和用户的交互请求抢 API 配额
```

第二条特别值得琢磨。定时任务在**优先级上天然低于用户请求**——用户正在等回答，定时任务晚五分钟没人在意。P14 没有做这个区分，所有事件平等排队。在单人环境下无所谓，但在共享 API 配额的产品里，这是必须做的。

### 时区那一条是反过来的

有意思的是，**P14 在时区上比 CC 做得更明确**：P14 要求显式传 IANA 时区并持久化 UTC instant，CC 按本地时区解释、模型不能传时区。

这不是说 P14 更好，而是两者面对的问题不同。CC 是本地 CLI，用户的机器时区就是用户的时区，猜对的概率极高；「让模型传时区」反而增加了模型出错的机会。P14 是教学实现，要把「时区必须显式」这条道理讲清楚，所以选了更啰嗦但更明确的做法。

如果 P14 这套代码要接到一个**服务端**场景（用户在上海，服务器在弗吉尼亚），显式时区就从「教学上的严谨」变成了「唯一正确的做法」。

---

## 从 ai-agent-book 学到什么：事件流与安全点

[`bojieli/ai-agent-book`](https://github.com/bojieli/ai-agent-book)（《深入理解 AI Agent：设计原理与工程实践》配套仓库）第 4 章有两个相关实验：实验 4-5 的 `agent-with-event-trigger` 用 `register -> trigger -> wake -> handle` 演示外部世界唤醒 Agent；实验 4-6 的 `async-agent`（代号 Flux）在这个事件队列上继续做紧急度分类、批量处理和状态检查点。

它们没有引入新的模型推理原语，而是把「什么时候让事件进入 Agent」变成框架层可控的工程边界——这正是本章要解决的同一个问题。

### 那两个实验的设计

实验 4-5 把触发源分成三类：一次性定时器、循环定时器、文件监听。三类统一成 `{event_type, content, metadata}` 结构化事件，由后台线程推入共享队列。它的离线 demo 不调 LLM，也能清楚看到五个阶段：定时器注册、触发、事件循环取事件、唤醒 Agent、模拟处理。

实验 4-6 更进一步，`classify_urgency()` 把事件分成三级：

```text
INTERRUPT  → 立即取消当前 turn 和所有后台任务
IMMEDIATE  → 立即回答，但不打断后台任务
DEFERRED   → 进入 pending 缓冲，等异步结果到达时一次性批量追加
```

书里明确点出一条关键原则：**事件只在每轮循环的边界被消费，不中途插入模型推理或工具执行。**

这条原则和本章的 `runEvents()` 共用运行锁是同一件事。

### P14 的对照与取舍

六个维度，每一条都是一次明确的取舍：

| 维度 | ai-agent-book | P14 | P14 这样选的原因 |
| --- | --- | --- | --- |
| 事件结构 | `event_type` + `content` + `metadata` | `eventId`、`jobId`、`identity`、`prompt`、`timezone`、`slotAtUtc` + `idempotencyKey` | 触发 turn 不靠裸字符串猜上下文 |
| 消费时机 | 只在循环边界消费 | `runEvents()` 与用户 turn 共用运行锁 | **同一条原则**，实现方式不同 |
| 批量 | 非紧急事件批量追加 | 每轮最多一个 | 不同 identity 不能共用一个 `ToolContext` |
| 紧急度 | 三级分类 | 无，统一排队到空闲 | 打断当前模型请求要改 Loop 核心时序 |
| 持久化 | trajectory + task progress 检查点 | job + pending outbox | 范围更窄，正好覆盖「重启后不丢工作」 |
| 事件源 | 定时器 + 文件监听 | 只有时间驱动 | 多事件源是独立主题 |

### 最值得注意的一条：为什么 P14 不做批量

Flux 批量追加非紧急事件是为了效率——一次 LLM 调用处理多条事件，省 token 省时间。这个优化在 Flux 的场景里是对的，因为它的事件都来自同一个用户。

P14 不能这么做，原因在实验 9 那条轨迹里：

```text
工具看到的上下文 = [{"identity":"cron-owner","idempotencyKey":"...0501"}]
```

一个 model turn 只有**一个** `ToolContext`，装一个 identity 和一个 idempotencyKey。如果批量注入两个不同创建者的事件：

```text
事件 A: identity=alice
事件 B: identity=bob
     ↓ 批量进同一个 turn
ToolContext.identity = ???
```

无论选哪个都错。选 alice，bob 的任务以 alice 的权限执行；选 bob，反过来。**身份隔离和批量效率在这里是直接冲突的，P14 选了身份隔离。**

顺带一个好处：逐条处理避免了「批量给模型五个任务，它只认真做最后一个」这个实际存在的问题。

### 另一条差异：checkpoint 的范围

Flux 保存完整 trajectory + task progress，恢复时把 running 任务标成 `suspended`，让上层决定重跑还是续跑。

P14 只保存 job 定义和 pending outbox。**没有 trajectory，没有进度。**

```text
Flux:  「这个任务做到 60% 了，你要不要接着做」
P14:   「这个任务还没开始做，重新做一次」
```

P14 的范围更窄，但它覆盖的正是定时任务真正需要的那一点：**时间到了，工作意图不能丢。** 一个「检查 CI 状态」的定时任务不需要续跑——重新查一次就好，而且重新查得到的还是更新的结果。

这里有个通用判断：**任务是否值得保存进度，取决于重做的代价。** 查 CI 状态重做代价接近零，`npm install` 重做代价是几分钟，数据迁移重做代价可能是灾难。P14 的定时任务都属于第一类，所以不保存进度是合理的。

---

## 配套代码索引

| 文件 | 行数 | 本章的角色 |
| --- | --- | --- |
| [features/cron.ts](code/chapters/ch14/src/features/cron.ts) | 684 | 全章核心。四种 typed 错误、`CronJob`/`CronEvent` 领域模型、`CronRuntime`、`schedule_cron` 工具、`nextCronOccurrence`、`validateCronExpression`、DST 调整 |
| [adapters/cron-json.ts](code/chapters/ch14/src/adapters/cron-json.ts) | 669 | `JsonCronStore`：单一原子快照、`tick()` 的一次性迁移、三层锁、leader 选举、fail-closed 严格解析 |
| [core/events.ts](code/chapters/ch14/src/core/events.ts) | 77 | `RuntimeEvent` 契约、`EventInbox` typed-only FIFO、`runtimeEventMessage` 把事件变成 user 消息（第 13 章引入，本章复用） |
| [core/loop.ts](code/chapters/ch14/src/core/loop.ts) | 677 | `RuntimeEventPump` 契约、`runEvents()` 事件专用回合、`#deferredRuntimeEvents` 延后机制、history-before-ack 与回滚、`ToolContext` 构造 |
| [features/background.ts](code/chapters/ch14/src/features/background.ts) | 632 | `JobSupervisor.startManaged()` 让 scheduler 成为受管 worker（第 13 章引入） |
| [bootstrap.ts](code/chapters/ch14/src/bootstrap.ts) | 330 | 组合根。四道配对检查、14 个工具的注册顺序、资源数组顺序 |
| [cli.ts](code/chapters/ch14/src/cli.ts) | 284 | 真实入口。共享 inbox 的接线、`bindWakeup` 的时机、失败时的清理顺序 |
| [core/profiles.ts](code/chapters/ch14/src/core/profiles.ts) | 294 | `P14 = P13 + "cron"` 的能力位定义 |
| [tests/ch14-cron.test.ts](code/chapters/ch14/tests/ch14-cron.test.ts) | 70 | 表达式、时区、DOM/DOW OR、七种拒绝路径、DST gap/fold |
| [tests/ch14-cron-store.test.ts](code/chapters/ch14/tests/ch14-cron-store.test.ts) | 391 | 11 个 store 行为：misfire、ack 幂等、三条路径的原子写失败、事件 id 冲突、双 store 竞争、版本不符、符号链接拒绝 |
| [tests/ch14-runtime.test.ts](code/chapters/ch14/tests/ch14-runtime.test.ts) | 481 | event-only turn、ack 失败回滚、leader 交接、用户回合延后、关闭顺序、阻塞 sleeper |
| [tests/ch14-bootstrap.test.ts](code/chapters/ch14/tests/ch14-bootstrap.test.ts) | 132 | 14 个工具的精确顺序、`P13` 不含 `cron`、两个配对失败信息 |
| [tests/ch14-entry.test.ts](code/chapters/ch14/tests/ch14-entry.test.ts) | 25 | 固定入口和配置缺失一次性报全 |

### 读代码的建议顺序

不要从 `cron.ts` 第 1 行往下读。按这个顺序，每一步都能独立读懂：

```text
① core/events.ts 全文（77 行）
   → 先建立「事件是什么」的概念，这是最小的一块
   → 如果读过第 13 章可以跳过

② cron.ts 的 validateCronExpression + nextCronOccurrence
   → 表达式和时区的全部逻辑，最容易验证的一段

③ cron.ts 的 CronJob / CronEvent 接口定义
   → 看清两个对象各有什么字段，为什么不能合并

④ cron-json.ts 的 tick()
   → 全章最重要的一个函数：一次原子迁移做了哪几件事

⑤ cron-json.ts 的 atomicReplace + parseState
   → 写入纪律和 fail-closed 解析

⑥ cron.ts 的 CronRuntime.tick() 和 close()
   → 四步发布流程，四步关闭顺序

⑦ loop.ts 的 #acceptNextRuntimeEvent
   → history-before-ack、回滚、延后机制

⑧ tests/ch14-cron-store.test.ts
   → 用 11 个测试反查你有没有漏掉的分支
```

前四步大约 250 行，看完就能理解这一章八成的内容。后四步是把剩下的细节补齐。

---

## 运行与验证

两个入口绑定同一个 P14 Bootstrap：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch14 -- --prompt "每天上海时间 9 点检查 CI，周期执行并持久化"
npm run agent-tutorial -- run --chapter 14 --prompt "每 2 分钟看一次当前目录，周期执行"
```

五条验证门：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch14
npm run typecheck
npm run lint
npm run format:check
npm run build
```

本章测试**不依赖真实 OpenAI、真实 sleep 或系统时间**。注入 `CronClock` 控制「现在几点」，注入 `CronSleeper` 免掉真实等待，注入 `BlockingSleeper` 验证关闭能中断阻塞中的 worker，注入 `atomicReplace` 制造写失败，注入 `outboxCapacity: 1` 让容量满的行为一步可测。**318 个测试 4 秒跑完，全程离线。**

五个测试文件的分工：

| 测试文件 | 回答的问题 |
| --- | --- |
| `ch14-cron.test.ts` | 表达式算得对不对？时区和 DST 对不对？坏输入拒绝了吗？ |
| `ch14-cron-store.test.ts` | 状态迁移对不对？写失败会不会漂移？两个 store 竞争会不会双发？ |
| `ch14-runtime.test.ts` | 事件回得对不对？ack 失败回滚了吗？关闭干净吗？ |
| `ch14-bootstrap.test.ts` | 接线对不对？能力位配错了会不会报错？工具顺序变了吗？ |
| `ch14-entry.test.ts` | 缺配置的时候，报错够不够全？ |

关键断言不是「对象存在」，而是可观察结果：

- 五段表达式、列表、范围、步进、星期和 DOM/DOW OR 得到确定 slot。
- 本地时区计算结果转换为预期 UTC instant，DST gap/fold 行为固定。
- recurring 同 slot 不重复，one-shot 删除定义但保留 outbox。
- 延迟三天只补一个最早 slot，`next_run_at_utc` 推进到 now 之后。
- durable 重建恢复，session-only 重建消失。
- outbox 满以及 schedule、tick、ack 持久化失败时，**旧文件字节**保持不变。
- 两个 store 竞争同一 slot 只提交一个 event；事件 id 冲突时先到的事件仍然 pending，后一个 job 的 `next_run_at_utc` 不变。
- Agent 忙时不 ack，空闲后 event-only turn 使用**保存的身份**并重新授权。
- ack 失败时零条 cron user 消息进入 history，模型请求次数为 0；重试后恰好一条。
- close 等待在途 tick，关闭后 scheduler 和 supervisor 都没有活跃 task，也不会重新获取 leader。
- `state.json` 的 `cron` 目录被换成符号链接/junction 时，在创建外部 `leader` 目录**之前**就被拒绝。

---

## 本章小结

### 三句话版本

第 14 章让 Agent 在没有人说话的时候也能开始干活。做法是：用成熟解析器和显式时区算出下一个 UTC slot，把「时间规则」和「一次到期的工作意图」拆成两个对象放进同一份原子快照，时间到了就产生一个持久事件、经由第 13 章那条 `EventInbox` 通路以 `user` 消息回到同一个 Agent Loop，并在触发时用创建者身份重新走一遍权限。所有「看起来更省事」的做法——本地时间当状态、Job 和 Event 合成一个、先 ack 再入历史、批量注入多个事件、补齐所有漏掉的 slot——都会在某个具体场景下把正确性弄丢。

### 一定要记住的七条

1. **slot 是应该执行的时刻，不是发现到期的时刻。** 一个 9:00 的任务因为忙了 8 秒才被发现，事件的 slot 仍然是 `09:00:00`。防重复触发靠比对 slot，靠发现时刻会失效。
2. **DOM 和 DOW 同时被约束时是 OR。** `0 9 1 * 1` 是「每月 1 号或每周一」，不是「每月第一个周一」。五段 cron 表达不了「同时满足两个条件」，别硬凑。
3. **本地日历算，UTC instant 存。** 这一条同时解决了 gap（推进到有效时刻）和 fold（保留两个不同 instant）。存本地时间字符串会在 fold 那天静默漏一次执行。
4. **Job 和 Event 必须是两个对象。** 定义的生命周期和工作意图的生命周期不一样。合成一个，one-shot 任务在「删定义」和「执行」之间崩溃时，无论怎么排顺序都会丢工作或重复工作。
5. **先入历史，再确认。** 顺序反了，崩在中间就永久丢工作且没有痕迹。按正确顺序最坏是重复执行一次，而重复由 `idempotencyKey` 兜底。**宁可重复，不可丢失。**
6. **容量满不能推进时间。** 被挤掉的 job 必须保持原来的 `next_run_at_utc` 原地等待。先推进再丢工作是一类看起来完全正常的静默 bug。
7. **身份来自过去，权限来自现在。** 定时任务以创建者身份执行，但按触发时的当前策略判决。cron prompt 是一段待重新提交的文本，不是权限票据。

### 一张图回顾整条链路

```text
                    ┌─────────────────────────────────────┐
                    │  schedule_cron（五个必填字段）       │
                    │  identity 从 ToolContext 观察        │
                    └──────────────┬──────────────────────┘
                                   ↓
              ┌────────────────────────────────────────────┐
              │  state.json（version + jobs + outbox）      │
              │  .cron.lock 保护读改写                      │
              └──────────────┬─────────────────────────────┘
                             ↓  scheduler 每秒 tick，leader.lock 选主
              ┌────────────────────────────────────────────┐
              │  一次原子迁移：造 event + 推进/删除 job      │
              └──────────────┬─────────────────────────────┘
                             ↓
              ┌────────────────────────────────────────────┐
              │  EventInbox（和第 13 章后台事件共用）        │
              └──────────────┬─────────────────────────────┘
                             ↓  Runner 空闲时，每轮最多一个
              ┌────────────────────────────────────────────┐
              │  进 history（role=user）→ ack → model turn   │
              │  ToolContext.identity = 创建者              │
              │  工具仍过 schema → Hook → 权限 → Hook        │
              └────────────────────────────────────────────┘
```

### 检查你是否真的读懂了

不看前文回答这八个问题：

1. 一个「每分钟」的 job 创建于 `12:00:30`，第一个 slot 是什么时候？为什么不是 `12:00:30`？
2. `0 9 1 * 1` 表示什么？说出一个它会触发但你可能没预期的日期。
3. 纽约「每天 02:30」的 job 在春季跳时那天几点执行？为什么不选「跳过那天」？
4. 一把快照锁不够，为什么还要 leader 锁？
5. one-shot 任务如果不做 Job/Event 分离，「先删定义再执行」和「先执行再删定义」各会出什么问题？
6. 停机三天后重启，「每分钟」的 job 补发几个事件？slot 是哪个时刻？
7. ack 失败时为什么必须把刚追加的 history 弹出来？不弹会怎样？
8. 为什么 cron 事件不能批量注入，而第 13 章的后台事件可以？

答不上来的，回去看对应小节：

| 问题 | 回去看 |
| --- | --- |
| 1 | 「三个『时间』要分清」 |
| 2 | 「DOM 和 DOW 是 OR，不是 AND」+ 实验 1 |
| 3 | 「DST gap：不存在的那一小时」+ 实验 2 |
| 4 | 「为什么一把锁不够」+ 实验 8 |
| 5 | 「为什么不能合成一个」 |
| 6 | 「停机三天：只补一个」+ 实验 5 |
| 7 | 「ack 失败要回滚」 |
| 8 | 「每次只取一个事件」+「为什么 P14 不做批量」 |

---

下一篇进入 Agent Teams。到这里 Agent 已经能被人触发、能后台执行、能自己看表，但它始终是**一个** Agent。当任务大到一个 Agent 装不下——需要有人写代码、有人审代码、有人跑测试——就得组队。P15 会讲持久队友和异步收件箱：队友怎么长期存在、消息怎么不丢、谁在等谁的时候整个团队不会死锁。事件边界的原则不变，变的是事件的另一端从「时间」换成了「另一个 Agent」。

