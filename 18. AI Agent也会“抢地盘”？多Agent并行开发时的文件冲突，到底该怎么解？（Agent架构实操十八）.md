上一章结束时，Alice 和 Bob 已经能从同一张 SQLite 任务板上自主找活，并用 claim token 证明"这项任务现在归我"。

看起来问题解决了。但只要让它们真的同时动手改代码，你会看到一个新的失败：

```text
Alice 认领了"重构认证模块" → 在主目录写 config.ts
Bob   认领了"重构登录页"   → 在主目录写 config.ts
                            ↓
                  后写的那一方，覆盖了先写的那一方
```

Task DAG 没错、lease 没错、消息协议没错。冲突发生在完全另一层：**两个执行者共享同一片物理文件空间。**

任务所有权（谁有权做这件事）和文件系统隔离（这次写入落在哪里）是两个不同的问题。上一章解决了前者，这一章解决后者。

这一章要落地的机制，一句话说清：

```text
每一项任务在动手之前，先由 Lead 为它开一个独立的 Git Worktree
（物理上是一个独立目录 + 一个独立分支，但共用同一个 .git 对象库）；
之后每一次工具调用之前，运行时都重新解析一遍"这次调用该在哪个目录执行"——
认领了任务的，落到该任务绑定的 Worktree；没认领的，留在主目录。
清理的时候不用 --force：只有"已完成 + 干净 + 确认并入了指定分支"才删，
证明不出来就转 needs_review，把现场留给人看。
```

第 18 章在第 17 章全部能力之上只增加一项 `worktree` capability。

![图片](https://mmbiz.qpic.cn/mmbiz_png/1z26fJxNuiaExpIBPJzibicWOB7vYtNzliaSpVn5rYOmN38pN6outCzcFvWRfcccAJ6kiakYElPj0Zxpia8faC5qSuWHGVux8UkzQ3kVrVCmjhaJg/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

---

## 本章导读

### 读完这一章，你能做到什么

- 说清为什么"给 `write_file` 加一把锁"解决不了并行开发的文件冲突，锁到底防住了什么、没防住什么；
- 说清 Project Task、Task Claim、Worktree Binding 为什么必须是三条独立状态线，把 worktree 塞进 Task 会坏在哪；
- 解释"先在 SQLite 写 `reserved`，再调 Git"这个顺序为什么不能反，跨系统操作为什么不能假装原子；
- 读懂"每次工具调用前重新解析工作目录"的实现位置，以及为什么不能用 `process.chdir()`；
- 说清 `remove_worktree` 那 8 步证明各自在防什么，为什么代码里没有 `--force` 和 `discard_changes`；
- 说清失效 claim 为什么必须显式失败，而不能"回落到主目录接着干"；
- 亲手跑通第 18 章，并用 11 个不需要 API key 的实验，逐条验证上面每个结论。

### 你需要先具备什么

| 前置 | 具体要求 | 不满足会怎样 |
| --- | --- | --- |
| 第 17 章的机制 | 知道任务板在 SQLite 里、claim token 是认领凭证、lease 是半开区间 | 本章的认领路由整节看不懂 |
| 第 15、16 章的机制 | 知道 Teammate 是常驻 worker、计划门禁会拦有副作用的工具 | 自动认领那节会缺一环 |
| Git 基本操作 | 会 `git branch`、`git commit`、`git merge`，知道"分支"是什么 | 清理证明那节需要多读两遍 |
| Node.js 22+ | 本文实测用的是 v25.7.0 | 依赖装不上，或 `tsx` 跑不起来 |
| Git 2.5+ | `git worktree` 是 2.5 引入的，实际用 2.20+ 更稳 | `git worktree add` 直接报未知命令 |
| Windows PowerShell | 本文所有命令都是 PowerShell 写法 | 命令要自己翻译成 bash |
| 模型凭证（可选） | 只有"真跑一次"那步需要 | 前 6 步和全部 11 个实验都不需要 |

**关于 `git worktree`：** 如果你之前没用过，只需要先记住一句话——它能让**同一个仓库**在磁盘上有**多个工作目录**，每个目录各自 checkout 一个分支，但它们**共用一个 `.git` 对象库**。不是 `git clone`，不占多份历史，切换也不需要 stash。本章实验 2 会带你亲手确认这一点。

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| **Git Worktree** | 同一仓库的第二个（第三个…）工作目录，各自 checkout 不同分支，共用一个 `.git` |
| **主工作目录** | 你 `git clone` 出来的那个目录，本章里也是 `integration_ref` 所在的地方 |
| **受管 Worktree** | 由本章运行时创建、路径和分支名都由代码派生、不允许调用方自定义的 Worktree |
| **binding（绑定）** | 一条"任务 ↔ Worktree"的持久化记录，存在 `worktree_bindings` 表里 |
| **integration ref（集成引用）** | 显式指定的目标分支，例如 `refs/heads/main`；既是建 Worktree 的基线，也是删除时的判据 |
| **baseline commit（基线提交）** | `integration_ref` 在创建那一刻解析出的不可变 commit ID |
| **branch tip（分支顶端）** | 该 Worktree 当前 `HEAD` 指向的 commit |
| **`reserved`** | binding 已在 SQLite 落库，但 Git 目录还没建成功——一个"意图记录" |
| **`active`** | Git 目录和分支都已回读确认，这条 binding 可以被认领和路由了 |
| **`kept`** | 任务完成了，但目录显式保留，等人来看 |
| **`needs_review`** | 清理过程中任何一步证明不出来，就落到这里，并且必须带上一条 `review_reason` |
| **`removed`** | 目录和分支都已安全删除；binding 行本身留在库里当审计记录 |
| **ToolContext（工具上下文）** | 一次工具调用的执行环境：workspace、identity、claimToken 等 |
| **`ToolContextProvider`** | 一个可插拔接口，能在每次工具调用前重算 ToolContext |
| **`executionScope`** | 每个回合（turn）新建的一个空冻结对象，用作"同一次回复"的身份标记 |
| **WeakMap** | 键是对象、且不阻止垃圾回收的 Map；本章用它把 `executionScope` 映射到 claim token |
| **append-only（只追加）** | 只能 INSERT，不能 UPDATE / DELETE；本章用数据库 trigger 强制 |
| **fail-closed（失败即拒绝）** | 判断不了就拒绝，而不是"放过去"；本章失效 claim 一律显式报错 |

### 建议的阅读路线

```text
[1] 先看一次完整隔离的轨迹      | 有画面感，知道最终跑出来是什么样
      ↓
[2] 为什么文件锁不是答案        | 本章存在的理由，一个锁能保证什么
      ↓
[3] 三条独立状态线              | 本章最重要的设计判断
      ↓
[4] 表结构与约束                | 数据长什么样，数据库挡住了什么
      ↓
[5] 创建：先 reserve 再调 Git    | 跨系统操作的顺序为什么不能反
      ↓
[6] 逐工具解析工作目录          | 本章最核心的实现，"地盘"是怎么落地的
      ↓
[7] 失效 claim 必须失败         | fail-closed 的具体边界
      ↓
[8] 清理必须先证明"可以删"       | 8 步证明，一条都不能少
      ↓
[9] 一步一步跑起来              | 从零跑起来，除了第 4、5 步都不需要 API key
      ↓
[10] 11 个动手实验              | 全部不需要 API key，逐条验证上面结论
```

如果你只有 10 分钟，读 `[1]`、`[6]`、`[8]` 三节，再跑实验 1 和实验 10。

---

## 先看一次完整隔离的轨迹

下面这段输出来自本章实验 1（脚本 `scripts/probe-ch18.mts`，稍后会带你一步步建起来）。两条任务、两个 Worktree，一个正常删除、一个转人工审查：

```text
===== trace =====
[1] Lead 建了两条任务：A=…1801 B=…1802
[2] binding alice: active branch=wt/alice path=.agent_tutorial/worktrees/alice
[3] binding bob:   active branch=wt/bob path=.agent_tutorial/worktrees/bob
[4] 任务状态没被创建 binding 推进：重构认证模块(pending) 重构登录页(pending)
[5] alice 认领 重构认证模块 token=…1811；bob 认领 重构登录页 token=…1812
[6] alice 解析到的 cwd: <root>\.agent_tutorial\worktrees\alice
[7] bob 解析到的 cwd:   <root>\.agent_tutorial\worktrees\bob
[8] alice 的 config.ts: "// alice 的版本\n"
[9] bob 的 config.ts:   "// bob 的版本\n"
[10] 主目录 config.ts:   "export const value = 0;\n"
[11] alice 的分支已并入 main，main 顶端 = b47d4a2
[12] bob 提交了但没有并入 main，wt/bob 顶端 = f038ae9
[13] 两条任务都 completed：重构认证模块(completed) 重构登录页(completed)
[14] remove alice → removed
[15] remove bob   → needs_review（worktree branch tip is not contained by the integration ref）
[16] 剩下的分支：main wt/bob
[17] Git 仍登记的 worktree 数：2
[18] 审计事件（按 sequence）：
     #1 alice reserve -> reserved
     #2 alice create -> active
     #3 bob reserve -> reserved
     #4 bob create -> active
     #5 alice remove -> removed
     #6 bob needs_review -> needs_review (worktree branch tip is not contained by the integration ref)
```

先别急着看代码，把这 18 行读一遍，本章的全部设计判断都在里面：

**`[4]` 建 Worktree 不推进任务状态。** binding 建好了，任务还是 `pending`。这不是遗漏，是刻意的——Lead 可以先把整张图和所有目录都备齐，再启动队友。真正把任务从 `pending` 推到 `in_progress` 的，仍然只有 claim。

**`[8]` `[9]` `[10]` 三个 `config.ts` 内容都不一样。** 同名文件、同时存在、互不覆盖。主目录那份甚至完全没动。这就是本章要的结果。

**`[14]` 和 `[15]` 是两种不同的结局。** Alice 的分支已经并入 `main`，删得掉；Bob 提交了但没并入，删不掉——**但不是报错，而是转成 `needs_review` 并写下原因**。目录和提交都还在。

**`[16]` `[17]` 印证了上一条。** `wt/bob` 分支还在，Git 登记的 worktree 数还是 2（主目录 + bob）。运行时没有替你做决定。

**`[18]` 每一次状态变化都留了一条不可修改的记录。** 六条事件，包括那条带原因的 `needs_review`。

接下来的每一节，都是在解释这 18 行里的某一行为什么必须是这样。

---

## 为什么文件锁不是答案

看到"两个 Agent 抢同一个文件"，第一反应往往是加锁。这个思路值得认真反驳一次，因为它错得很有道理。

先看锁能做到什么。实验 2 里我用一把真正的互斥锁包住写入，全程严格串行，没有任何并发：

```text
===== lock =====
  alice 拿到 config.ts 的锁
  alice 释放锁
  bob 拿到 config.ts 的锁
  bob 释放锁
共享目录最终内容: "// bob 的版本\n"
锁全程没有被并发持有，alice 的改动依然消失了：串行化只保证顺序，不保存版本。
```

锁工作得很完美。Alice 的改动照样没了。

原因很简单：**锁解决的是"同一时刻只有一个人写"，不解决"两个人各自的版本怎么分别保存"。** 它把并发覆盖变成了有序覆盖，最终仍然只有一份内容活下来。

```text
加锁之前：Alice 和 Bob 同时写 → 结果不确定，可能撕裂
加锁之后：Alice 先写，Bob 后写 → 结果确定，就是 Bob 那份

确定的错误结果，还是错误结果。
```

而且真实的并行开发里，冲突远不止"同一个文件被覆盖"这一种：

| 冲突类型 | 例子 | 加锁能防住吗 |
| --- | --- | --- |
| 同文件写入冲突 | 两人都改 `config.ts` | 能防住撕裂，防不住覆盖 |
| 跨文件语义冲突 | Alice 改了函数签名，Bob 还在按旧签名调用 | 完全防不住 |
| 中间态被读到 | Alice 改了一半，Bob 读到半成品 | 要靠更大粒度的锁，很快就退化成串行 |

要真正解决，需要的不是"排队写同一份"，而是**让两个候选改动分别存在，各自完整，之后由一个明确的流程决定哪一份进入主线**。

这正是 Git Worktree 提供的东西：

```text
repository-root/                    ← 主工作目录，integration ref 在这里
├── .git/                           ← 唯一的对象库，三个目录共用
├── .agent_tutorial/
│   ├── tasks.sqlite3               ← 第 17 章的任务板 + 本章的 binding
│   └── worktrees/
│       ├── alice/                  ← 独立目录，checkout 分支 wt/alice
│       │   └── config.ts           "// alice 的版本"
│       └── bob/                    ← 独立目录，checkout 分支 wt/bob
│           └── config.ts           "// bob 的版本"
└── config.ts                       "export const value = 0;"  ← 没被动过
```

三份 `config.ts` 同时存在，各自完整，各自有独立的提交历史。之后是 review 还是 merge，是人的决定，不是运行时的决定。

**注意"共用一个 `.git`"这件事。** 这是 Worktree 和 `git clone` 的关键区别：不复制历史、不占多份磁盘、Alice 的提交对象 Bob 立刻就能引用。实验 2 会用 `git rev-parse --git-common-dir` 把这一点验出来：

```text
alice worktree: "// alice 的版本\n"
bob   worktree: "// bob 的版本\n"
主目录        : "export const value = 0;\n"
两个 worktree 共享同一个对象库："<root>/.git"
```

---

## 三条独立状态线

明确了要用 Worktree，下一个问题是：这个"任务 ↔ 目录"的关系存哪儿？

最省事的写法看起来很有吸引力——在 `tasks` 表上加一列：

```sql
-- 看起来很自然，但是错的
ALTER TABLE tasks ADD COLUMN worktree TEXT;
```

这一列会把三件本来无关的事情搅在一起：

| 状态线 | 回答的问题 | 典型取值 | 谁来推进 |
| --- | --- | --- | --- |
| **Project Task** | 这份工作没开始、在做、还是做完了 | `pending` → `in_progress` → `completed` | `claim_task` / `complete_task` |
| **Task Claim** | 谁在租期内拥有执行权 | active / expired / cleared | 认领与租约到期 |
| **Worktree Binding** | 任务对应的隔离目录处于什么生命周期 | `reserved` / `active` / `kept` / `needs_review` / `removed` | `create_worktree` / `keep_worktree` / `remove_worktree` |

它们的**生命周期长度根本不一样**。这是判断"要不要拆表"最实用的一条标准：

```text
时间轴 ───────────────────────────────────────────────────────────▶

Task:      pending ──────────── in_progress ──────── completed ─────────────
Claim:                          ├─ 60 秒租约 ─┤        （清空）
Binding:   reserved → active ────────────────────────────────── kept/removed
           ↑                                                    ↑
           任务还没开始就已经存在                                任务完成之后还活着
```

Binding 比 Claim 长，甚至可能比 Task 的"进行中"阶段长很多——任务完成之后，那个目录可能还要留几天等人 review。塞进一张表意味着这三条线的每一次变化都要互相体谅，最后谁都说不清"现在到底什么状态"。

具体会坏在三个地方：

**一，Task 状态迁移会被污染。** `create_worktree` 要写这一列，就得 UPDATE `tasks` 表。那 UPDATE 要不要检查 `status = 'pending'`？要不要顺手把状态改成"准备中"？一旦开始纠结这个问题，Task 就不再只由 claim/complete 驱动了。

**二，`needs_review` 无处安放。** 一个已经 `completed` 的任务，它的目录处于"清理失败，等人看"的状态。这是**目录**的状态，不是**任务**的状态。塞进 Task 就只能加一个 `completed_but_worktree_needs_review`——状态机开始爆炸。

**三，审计对不上。** binding 有自己的完整历史（reserve → create → keep → …），Task 有自己的历史。混在一张表里，就没法回答"这个目录经历过什么"。

所以 P18 **完全没有改动 Task schema**。实验 3 会把两张表的字段列出来对比：

```text
tasks 表里没有 worktree 字段：sequence, id, subject, description, status, owner, claim_token, lease_expires_at_utc
binding 独立成表：task_id, name, branch, relative_path, integration_ref, baseline_commit, branch_tip, status, review_reason, created_at_utc, updated_at_utc
```

同一个实验也把"三条线各走各的"验了出来：

```text
===== state =====
createTask 之后          task=pending     owner=null   token=null   binding=（无 binding）
createWorktree 之后      task=pending     owner=null   token=null   binding=active
claimNext 之后           task=in_progress owner=alice  token=…1811  binding=active
completeTask 之后        task=completed   owner=alice  token=null   binding=active
keepWorktree 之后        task=completed   owner=alice  token=null   binding=kept
```

一行一行看：`createWorktree` 只动 binding；`claimNext` 只动 task 和 token；`completeTask` 只清 token；`keepWorktree` 只动 binding。**四次操作，没有任何一次同时改两条线。**

---

## Binding 的状态机

五个状态，四条合法迁移。全部定义在 [src/features/worktrees.ts](code/chapters/ch18/src/features/worktrees.ts)：

```text
                         ┌──────────┐
   create_worktree  ───▶ │ reserved │   SQLite 里有记录，Git 目录还没建成
   （第一步）             └────┬─────┘
                              │ git worktree add 成功，且回读确认了分支
                              ▼
                         ┌──────────┐
                         │  active  │   ← 只有这个状态能被认领、能路由 cwd
                         └────┬─────┘
                 ┌────────────┼────────────────┐
                 │            │                │
      keep_      │            │ remove_        │ 任何一步证明失败
      worktree   │            │ worktree       │
                 ▼            ▼                ▼
            ┌────────┐   ┌─────────┐   ┌──────────────┐
            │  kept  │   │ removed │   │ needs_review │
            └───┬────┘   └─────────┘   └──────┬───────┘
                │                             │
                └──── remove_worktree ────────┘
                      （证明通过才能到 removed）
```

三个设计点值得单独说：

**`reserved` 不是"创建中"这种临时状态，它是一条持久化的意图记录。** 如果 `git worktree add` 失败了，这条 `reserved` 会**留在数据库里**，不会被清掉。原因下一节讲。

**只有 `active` 能被认领。** `claimNextBound` 的 SQL 里明确写了 `WHERE status = 'active'`。`reserved` 的捡不到，`kept` 的捡不到。

**`kept` 和 `needs_review` 都是终态前的保留态，都还能进 `removed`。** 但进去的条件一样严——必须重新走完整套证明。

代码里的 `WorktreeStatus` 和 `WorktreeAction` 是两个不同的枚举，这一点容易看混：

```typescript
export const WorktreeStatus = Object.freeze({
  RESERVED: "reserved",
  ACTIVE: "active",
  KEPT: "kept",
  NEEDS_REVIEW: "needs_review",
  REMOVED: "removed",
} as const);

export const WorktreeAction = Object.freeze({
  RESERVE: "reserve",
  CREATE: "create",
  KEEP: "keep",
  NEEDS_REVIEW: "needs_review",
  REMOVE: "remove",
} as const);
```

`Status` 是"binding 现在在哪"，`Action` 是"这次审计事件记的是什么动作"。两者名字有重叠（`needs_review`）但含义不同：状态是名词，动作是动词。审计表里两列都存，这样"谁把它变成这样的"和"它现在是什么样"都能查。

---

## 表结构与约束

`worktree_bindings` 存当前快照，`worktree_events` 存不可变历史。DDL 在 [src/adapters/task-sqlite.ts](code/chapters/ch18/src/adapters/task-sqlite.ts)：

```sql
CREATE TABLE IF NOT EXISTS worktree_bindings(
  task_id TEXT PRIMARY KEY,
  name TEXT NOT NULL UNIQUE,
  branch TEXT NOT NULL UNIQUE,
  relative_path TEXT NOT NULL UNIQUE,
  integration_ref TEXT NOT NULL,
  baseline_commit TEXT NOT NULL,
  branch_tip TEXT,
  status TEXT NOT NULL CHECK(status IN ('reserved', 'active', 'kept', 'needs_review', 'removed')),
  review_reason TEXT,
  created_at_utc TEXT NOT NULL,
  updated_at_utc TEXT NOT NULL,
  CHECK(
    (status = 'needs_review' AND review_reason IS NOT NULL)
    OR (status != 'needs_review' AND review_reason IS NULL)
  ),
  FOREIGN KEY(task_id) REFERENCES tasks(id) ON DELETE RESTRICT
);
```

**四个 UNIQUE / PRIMARY KEY，各防一件事：**

| 约束 | 防住什么 |
| --- | --- |
| `task_id PRIMARY KEY` | 一个任务只能有一个 Worktree，不会出现"这任务到底在哪个目录做"的歧义 |
| `name UNIQUE` | 两个任务不能都叫 `alice` |
| `branch UNIQUE` | 两条 binding 不能指向同一个分支 |
| `relative_path UNIQUE` | 两条 binding 不能指向同一个目录 |

后三条其实是同一件事的三个投影——`name` 决定 `branch`（`wt/{name}`）也决定 `relative_path`（`.agent_tutorial/worktrees/{name}`）。都写上是因为**约束应该直接表达不变量，而不是依赖调用方一定会按规则派生**。

**那个双向 CHECK 是本章我最喜欢的一行。** 它把"`needs_review` 必须有原因"和"其他状态不能有原因"同时钉死：

```sql
CHECK(
  (status = 'needs_review' AND review_reason IS NOT NULL)
  OR (status != 'needs_review' AND review_reason IS NULL)
)
```

为什么两个方向都要管？

- 只管前半句 → 可能出现"`active` 但带着一条陈旧的 review_reason"，读的人不知道该不该信；
- 只管后半句 → 可能出现"`needs_review` 但没写原因"，人接手时完全不知道发生了什么。

实验 5 会直接手写 SQL 撞这条约束：

```text
CHECK 约束也挡住非法组合，试着手写一个 needs_review 但不给理由：
  → Error: CHECK constraint failed: (status = 'needs_review' AND review_reason IS NOT NULL)
          OR (status != 'needs_review' AND review_reason IS NULL)
```

审计表用自增 `sequence` 而不是时间戳排序，并且用两个 trigger 强制只追加：

```sql
CREATE TABLE IF NOT EXISTS worktree_events(
  sequence INTEGER PRIMARY KEY AUTOINCREMENT,
  action TEXT NOT NULL CHECK(action IN ('reserve', 'create', 'keep', 'needs_review', 'remove')),
  status TEXT NOT NULL CHECK(status IN ('reserved', 'active', 'kept', 'needs_review', 'removed')),
  task_id TEXT NOT NULL,
  name TEXT NOT NULL,
  branch TEXT NOT NULL,
  relative_path TEXT NOT NULL,
  integration_ref TEXT NOT NULL,
  baseline_commit TEXT NOT NULL,
  branch_tip TEXT,
  reason TEXT,
  created_at_utc TEXT NOT NULL,
  FOREIGN KEY(task_id) REFERENCES tasks(id) ON DELETE RESTRICT
);

CREATE TRIGGER IF NOT EXISTS worktree_events_reject_update
BEFORE UPDATE ON worktree_events
BEGIN
  SELECT RAISE(ABORT, 'worktree_events is append-only');
END;

CREATE TRIGGER IF NOT EXISTS worktree_events_reject_delete
BEFORE DELETE ON worktree_events
BEGIN
  SELECT RAISE(ABORT, 'worktree_events is append-only');
END;
```

**为什么用 trigger 而不是"在代码里不写 UPDATE 语句"？** 因为审计的价值来自"改不了"。代码层的自我约束只能防住自己的疏忽，防不住别人拿 `sqlite3` 命令行直接改。trigger 在数据库层拒绝，谁来都一样。

**为什么用 `sequence` 而不是靠 `created_at_utc` 排序？** 同一个事务里可能连写两条事件（reserve 和 create），时间戳完全相同。自增主键给出唯一确定的顺序。

实验 5 会验证 trigger 真的拦得住：

```text
UPDATE worktree_events SET action = 'remove' WHERE sequence = 1
  → Error: worktree_events is append-only
DELETE FROM worktree_events WHERE sequence = 1
  → Error: worktree_events is append-only
```

---

## 创建：为什么先 reserve 再调 Git

这一节讲的是本章最容易写错的地方，也是所有"跨系统操作"共同的难题。

`create_worktree` 要动两个系统：SQLite（写 binding）和 Git（建目录和分支）。它们之间**没有共同的事务**。你没法说"要么两边都成，要么两边都回滚"。

那顺序怎么排？只有两个选择：

```text
方案 A：先 Git，后 SQLite
  git worktree add 成功 → 进程崩溃 → 数据库里什么都没有
  结果：磁盘上多出一个孤儿目录和一个孤儿分支，没人知道它是干嘛的，也没人负责清

方案 B：先 SQLite，后 Git
  写入 reserved → git worktree add 失败 → 数据库里留着一条 reserved
  结果：状态明确写着"这里有个未完成的预留"，能查、能重试、能人工处理
```

代码选 B。核心是这个判断：**孤儿副作用比多余的状态记录危险得多。**

多余的状态记录是可见的、可查询的、可以决定怎么处理的。孤儿目录是不可见的——除了 `git worktree list`，没有任何地方记得它存在。

来看真实实现（[src/features/worktrees.ts](code/chapters/ch18/src/features/worktrees.ts) 的 `createWorktree`）：

```typescript
async createWorktree(input: {
  readonly taskId: string;
  readonly name: string;
  readonly integrationRef: string;
}): Promise<WorktreeBinding> {
  await this.validateRepository();
  const taskId = canonicalTaskId(input.taskId);
  const name = canonicalAgentName(input.name);
  const integrationRef = canonicalIntegrationRef(input.integrationRef);
  const task = await this.#store.getTask(taskId);
  if (task.status !== TaskStatus.PENDING) {
    throw new WorktreeStateError(
      `Task ${task.id} is ${task.status}; creating a worktree requires pending`,
    );
  }
  // ① 把 integration ref 钉成不可变 commit
  const baseline = await this.#resolveCommit(integrationRef, this.#workspaceRoot);
  const now = this.#now();
  // ② 先在 SQLite 落 reserved（binding + reserve 事件，同一事务）
  const binding = await this.#store.reserveWorktree(
    new WorktreeBinding({
      taskId,
      name,
      branch: `wt/${name}`,
      relativePath: `.agent_tutorial/worktrees/${name}`,
      integrationRef,
      baselineCommit: baseline,
      branchTip: null,
      status: WorktreeStatus.RESERVED,
      reviewReason: null,
      createdAtUtc: now,
      updatedAtUtc: now,
    }),
  );
  // ③ 再执行 Git 副作用
  const path = await this.#prepareNewWorktreePath(binding);
  const result = await this.#runGit(
    ["worktree", "add", "-b", binding.branch, path, binding.baselineCommit],
    this.#workspaceRoot,
  );
  if (result.returncode !== 0)
    throw new WorktreeGitError("Git could not create the reserved worktree");
  // ④ 回读证明：分支顶端 + 当前分支名
  const branchTip = await this.#resolveCommit("HEAD", path);
  const branch = await this.#runGit(["branch", "--show-current"], path);
  if (branch.returncode !== 0 || branch.stdout.trim() !== binding.branch) {
    throw new WorktreeGitError("Git created a worktree with an unexpected branch");
  }
  // ⑤ 证明通过，才迁移到 active
  return await this.#store.activateWorktree(binding.taskId, {
    branchTip,
    occurredAtUtc: this.#now(),
  });
}
```

五步，每一步都有理由：

**① `baseline` 是解析出来的 commit ID，不是分支名。** `#resolveCommit` 走的是 `git rev-parse --verify <ref>^{commit}`，拿到一个 40 位十六进制。之后 `git worktree add` 用的是这个 ID，不是 `refs/heads/main`。

为什么？因为分支会动。如果两条 binding 都写"基于 main"，而中间有人往 main 推了提交，两个 Worktree 的起点就不一样了——但数据库里记的是同一个字符串。钉成 commit ID 之后，`baseline_commit` 这一列就是**确定的事实**。

**② `reserveWorktree` 内部是一个事务，binding 和 reserve 事件一起提交。** 不会出现"预留了但没有审计"。

**③ `#prepareNewWorktreePath` 做了三件路径安全检查**：确认 `relative_path` 确实是运行时派生的那个值、创建父目录后重新 `realpath` 确认没逃出 workspace、确认目标路径**尚不存在**。

**④ 回读证明不是形式主义。** `git worktree add` 返回 0 不等于结果就是你要的。它可能因为各种边缘情况 checkout 到别的分支。所以再问一次 `git branch --show-current`，对不上就报错。

**⑤ 只有回读通过才 `activateWorktree`。** 这是从 `reserved` 到 `active` 的唯一入口。

实验 4 会注入一个"`worktree add` 一定失败"的 Git runner，看失败之后留下了什么：

```text
===== reserve =====
createWorktree 抛出：WorktreeGitError: Git could not create the reserved worktree
binding 状态仍是：reserved（branch_tip=null）
Task 状态仍是：  pending
审计事件：#1 reserve->reserved
自动认领会不会捡到它：undefined（reserved 不是 active）
Git 分支：main（没有留下半个 wt/alice）
```

五行结论，每一行都重要：

- binding 留在 `reserved`，`branch_tip` 是 null——诚实地记录"还没建成"；
- Task 仍是 `pending`——没被这次失败影响；
- 有一条 reserve 审计事件——留下了证据；
- **自动认领捡不到它**——`reserved` 不在 `claimNextBound` 的筛选范围里，所以这条失败的预留不会害到任何人；
- Git 里没有半个 `wt/alice`——失败之前就失败了，没留下孤儿。

最后一点值得强调：`reserved` 状态**不会**卡住系统。它就在那儿，等人来看，等人来重试。这比"自动清掉，假装什么都没发生"要好得多。

### `integration_ref` 为什么必须写全 `refs/...`

`create_worktree` 的 schema 里，`integration_ref` 是必填的，而且必须是完整的 `refs/...` 形式：

```typescript
const createWorktreeSchema = z.strictObject({
  task_id: z.string().uuid().toLowerCase(),
  name: z.string().trim().min(1),
  integration_ref: z.string().trim().min(1),
});
```

校验函数 `canonicalIntegrationRef` 把一长串危险输入挡在外面：

```typescript
export function canonicalIntegrationRef(value: string): string {
  if (
    typeof value !== "string" ||
    value !== value.trim() ||
    !value.startsWith("refs/") ||
    value.endsWith("/") ||
    value.endsWith(".") ||
    value.includes("//") ||
    value.includes("..") ||
    value.includes("@{") ||
    [...value].some((character) => {
      const codePoint = character.codePointAt(0);
      return codePoint === undefined || INVALID_REF_CHARACTERS.has(character) || codePoint < 32;
    })
  ) {
    throw new WorktreeStateError("integration_ref must be an explicit safe refs/... Git reference");
  }
  return value;
}
```

配合 `INVALID_REF_CHARACTERS`：

```typescript
const INVALID_REF_CHARACTERS = new Set([" ", "~", "^", ":", "?", "*", "[", "\\"]);
```

**为什么不接受 `main` 这种简写？** 因为 Git 的引用解析规则有优先级：`main` 可能命中 `refs/heads/main`，也可能命中 `refs/tags/main`，还可能在特定情况下命中别的。这个歧义在"决定要不要删掉一个目录"的场合是不能接受的。

**为什么禁 `@{`？** 因为 `main@{1}`、`HEAD@{yesterday}` 这类 reflog 语法解析出的 commit **会随时间变化**。你昨天算出的"已并入"，今天可能就不成立了。

**为什么禁 `..`？** `refs/heads/a..b` 在 Git 里是区间语法，不是单个引用。禁掉它同时也顺手挡住了路径穿越。

**为什么禁空格和 `~ ^ : ? * [ \`？** 这些都是 Git revision 语法里的操作符（`~3` 是"往前三个"、`^` 是"父提交"、`:` 是"路径分隔"），或者在 shell 里有特殊含义。虽然本章用 `execFile` 不经过 shell，但让引用保持"就是一个引用"是更干净的边界。

实验 11 会把这些输入全部跑一遍：

```text
canonicalIntegrationRef 的输入校验：
  "refs/heads/main"                  → refs/heads/main
  "refs/remotes/origin/main"         → refs/remotes/origin/main
  "main"                             → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  "HEAD"                             → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  "refs/heads/../../etc/passwd"      → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  "refs/heads/main@{1}"              → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  "refs/heads/feature branch"        → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  "refs/heads/main/"                 → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
  " refs/heads/main"                 → WorktreeStateError: integration_ref must be an explicit safe refs/... Git reference
```

### `name` 也不能随便起

`name` 走的是第 15 章就有的 `canonicalAgentName`：

```typescript
const AGENT_NAME = /^[a-z0-9]+(?:-[a-z0-9]+)*$/u;

export function canonicalAgentName(value: string): string {
  if (typeof value !== "string" || !AGENT_NAME.test(value) || isWindowsReservedComponent(value)) {
    throw new Error("Invalid or unsafe Agent name; expected a safe lowercase slug");
  }
  return value;
}
```

小写字母、数字、中间可以用单个连字符。**并且拒绝 Windows 保留名**（`CON`、`PRN`、`AUX`、`NUL`、`COM1`…）——在 Windows 上，一个叫 `con` 的目录是建不出来的，与其让 `git worktree add` 报一个看不懂的错，不如在入口就拒绝。

这也解释了为什么 `branch` 和 `relative_path` 不让调用方指定：

```typescript
branch: `wt/${name}`,
relativePath: `.agent_tutorial/worktrees/${name}`,
```

**名字校验过一次，路径和分支就都安全了。** 如果允许自定义路径，就要在另一个地方再校验一遍路径穿越——多一个入口就多一处可能漏。

### Git 命令为什么用 `execFile` + 数组

[src/adapters/git.ts](code/chapters/ch18/src/adapters/git.ts) 只有 74 行，但每一处都有理由：

```typescript
const result = await execFileAsync("git", ["--no-pager", ...argumentsValue], {
  // execFile 不经过 shell，数组元素直接作为 argv 传递，参数含空格或特殊字符也安全。
  cwd: workingDirectory,
  encoding: "utf8",
  timeout: this.#timeoutMs,
  windowsHide: true,
  maxBuffer: 8 * 1024 * 1024,
});
```

| 选择 | 理由 |
| --- | --- |
| `execFile` 而不是 `exec` | `exec` 会拼一条 shell 命令行，参数里的空格、`&&`、`;` 都可能改变语义。`execFile` 直接把数组当 argv |
| `--no-pager` | 没有它，某些命令（`branch`、`log`）会尝试调 `less`，在非交互环境下挂住 |
| `timeout: 30_000` | Git 也会卡（网络引用、文件锁）。30 秒是个够宽松但有上限的值 |
| `windowsHide: true` | Windows 上不弹黑窗口 |
| `maxBuffer: 8MB` | 默认 1MB，`git status` 在大仓库里能超 |

错误处理分了三类，这个分类很关键：

```typescript
if (details.killed === true || details.code === "ETIMEDOUT") {
  throw new GitExecutionError("Git command timed out", { cause: error });
}
if (typeof details.code === "number") {
  return Object.freeze({
    returncode: details.code,
    stdout: typeof details.stdout === "string" ? details.stdout : "",
    stderr: typeof details.stderr === "string" ? details.stderr : "",
  });
}
throw new GitExecutionError("Git command could not be started", { cause: error });
```

- **超时** → 抛异常（这是环境问题，不是业务结果）；
- **非零退出码** → 返回结构化结果（这是**正常的业务信号**，比如"这个分支没并入"）；
- **进程起不来** → 抛异常（Git 没装、PATH 不对）。

**"非零退出码不是异常"这个判断很重要。** `git merge-base --is-ancestor A B` 返回 1 表示"A 不是 B 的祖先"——这是一个**答案**，不是错误。如果把它变成异常，调用方就要用 try/catch 来做分支判断，代码立刻变丑。

领域层还有一层兜底，把 `GitExecutionError` 统一转成 `returncode: -1`：

```typescript
async #runGit(argumentsValue: readonly string[], cwd: string): Promise<GitCommandResult> {
  try {
    return await this.#git.run(argumentsValue, cwd);
  } catch (error) {
    if (error instanceof GitExecutionError) return { returncode: -1, stdout: "", stderr: "" };
    throw error;
  }
}
```

这样清理流程里"Git 超时"和"Git 说不行"走同一条路——都是"证明不出来"，都落 `needs_review`。**不需要为超时单独写一个分支。**

---

## 逐工具解析工作目录

这是本章的核心机制。前面所有的表和状态都是为了这一节服务的。

问题很具体：Alice 调用 `write_file("config.ts", ...)`，运行时怎么知道该写到 `.agent_tutorial/worktrees/alice/config.ts` 而不是主目录？

### 三种做不对的写法

**写法一：`process.chdir()`。**

```typescript
// 不能这么写
process.chdir(worktreePath);
await writeFile("config.ts", content);
```

`process.cwd()` 是**整个进程共享的一个全局变量**。本章的运行形态是：Lead、多个 Teammate、若干 Subagent 全部在同一个 Node 进程里并发跑。Alice 改了进程 cwd，Bob 的下一次写入就落到 Alice 的目录里去了。

```text
时刻 t1: alice 调 chdir(.../alice) → 进程 cwd = .../alice
时刻 t2: bob  调 chdir(.../bob)   → 进程 cwd = .../bob
时刻 t3: alice 的下一次 write_file → 写到了 .../bob ！
```

而且这是**时序相关**的 bug，重现困难，排查极痛苦。

**写法二：给 Runner 一个固定的 workspace。**

```typescript
// 也不行
const runner = new AgentRunner({ workspace: worktreePath, ... });
```

这解决了并发问题，但解决不了**同一次回复内认领**的场景。模型完全可能在一次回复里输出两个 tool call：

```text
assistant 回复：
  tool_call #1: claim_task(task_id=…1801)
  tool_call #2: write_file(path="config.ts", content="...")
```

`#1` 执行完，Alice 才拥有了那个 Worktree。但 Runner 的 workspace 是构造时定的，`#2` 还是会写主目录。

**写法三：让模型自己带路径。**

```text
系统提示词：认领任务后，请在所有文件路径前加上你的 worktree 前缀
```

这把隔离边界交给了模型的自觉。模型忘了、猜错了、或者被 prompt injection 骗了，隔离就没了。**安全边界不能靠模型配合。**

### 正确的形状：每次工具调用前重新解析

代码的做法是引入一个可插拔接口 `ToolContextProvider`（[src/core/loop.ts](code/chapters/ch18/src/core/loop.ts)）：

```typescript
export interface ToolContextProvider {
  readonly workspaceRoot: string;
  resolve(context: ToolContext): ToolContext | Promise<ToolContext>;
}
```

然后在 `#executeTool` 里，**每一次工具调用**都调它一次。位置极其关键：

```typescript
let prepared: PreparedToolCall;
try {
  prepared = tools.prepare(call);          // ① 参数 schema 校验
} catch {
  return execution(toolError("tool_preparation_error", "Tool preparation failed"));
}
if (prepared.error !== undefined) {
  return execution(prepared.error);
}

// ② 在参数 schema 校验之后、PreToolUse/权限/handler 之前解析可信工作区；
//    解析失败时工具 handler 保持零调用，直接返回配对错误。
try {
  context = await this.#resolveToolContext(context);
} catch {
  return execution(toolError("tool_context_error", "Tool context resolution failed"));
}

let preHook: HookResult;
try {
  preHook = await this.#hooks.runPreTool(prepared);   // ③ PreToolUse hook
  ...
```

顺序是 **schema 校验 → 解析 cwd → PreToolUse hook → 权限判定 → handler**。

为什么必须卡在这个位置？

```text
放在 schema 校验之前  ✗  参数还没验，可能解析一个畸形 task_id
放在 PreToolUse 之后  ✗  Hook 看到的 workspace 是错的，基于它的判断全部无效
放在权限判定之后      ✗  权限策略基于错误的路径做决定，可能放过越界写入
放在 handler 里        ✗  每个工具都要自己记得解析，漏一个就漏一个洞

放在 schema 之后、Hook 之前  ✓  参数可信了，且后面所有环节看到的都是最终 cwd
```

**"handler 保持零调用"这句话是本章的一条硬约束。** 解析失败时，工具函数一次都不会被执行。返回的是一条配对的错误消息（`tool_context_error`），历史里 tool_call 和 tool_result 仍然成对——模型看到"这次调用失败了"，而不是看到一个悬空的调用。

### Loop 不信任 provider

`ToolContextProvider` 是可插拔的。那如果有人注入一个恶意 provider，把 workspace 改成 `C:\Windows` 呢？

`#resolveToolContext` 在 provider 返回之后又验了一遍：

```typescript
async #resolveToolContext(context: ToolContext): Promise<ToolContext> {
  const provider = this.#toolContextProvider;
  if (provider === undefined) return context;
  // provider 负责把 claim 映射到受限 workspace；核心只验证身份不可变和路径仍在仓库根内。
  const resolved = await provider.resolve(context);
  if (typeof resolved !== "object" || resolved === null) {
    throw new TypeError("tool context provider must return ToolContext");
  }
  if (
    resolved.identity !== context.identity ||
    resolved.idempotencyKey !== context.idempotencyKey ||
    resolved.executionScope !== context.executionScope
  ) {
    throw new Error("tool context provider changed immutable execution identity");
  }
  const workspace = await realpath(resolved.workspace);
  if (!(await stat(workspace)).isDirectory() || !isWithin(this.#workspaceRoot, workspace)) {
    throw new Error("resolved tool workspace is outside the controlled workspace root");
  }
  // 再次冻结并写入解析后的真实路径，工具不能在调用期间自行改写执行目录。
  return Object.freeze({ ...resolved, workspace });
}
```

三道检查，职责划得很清楚：

| 检查 | 防什么 | 谁负责 |
| --- | --- | --- |
| `identity` / `idempotencyKey` / `executionScope` 不可变 | provider 冒充别人、篡改幂等键 | Loop |
| `realpath` + `isWithin` | 路径逃出仓库根（包括符号链接绕过） | Loop |
| claim 是否有效、binding 是否 active | 业务规则 | provider |

**Loop 只管"不能越界"，provider 只管"该去哪"。** 这个分工让 provider 可以被替换，但替换不能突破边界。最后再 `Object.freeze` 一次，工具执行期间也改不动。

### `executionScope`：怎么认出"同一次回复"

现在回到那个"同一次回复里先 claim 再 write"的场景。`claim_task` 执行完之后，`write_file` 怎么知道自己该去 Alice 的目录？

答案是每个回合新建一个空对象，用它的**引用身份**当标记（[src/core/loop.ts](code/chapters/ch18/src/core/loop.ts)）：

```typescript
// 每个 turn 创建独立 executionScope；provider 可用时注入，
// 使同一回复内连续 tool call 能共享 claim 关联，但不同 turn 不串用。
const toolContextScope = Object.freeze({});
const context: ToolContext = Object.freeze({
  workspace: this.#workspace,
  identity: runtimeEvent?.contextIdentity ?? this.#identity,
  ...(turnIdempotencyKey === undefined ? {} : { idempotencyKey: turnIdempotencyKey }),
  ...(claimToken === undefined ? {} : { claimToken }),
  ...(this.#toolContextProvider === undefined ? {} : { executionScope: toolContextScope }),
});
```

`Object.freeze({})` 就是一个空对象。它不携带任何数据，唯一的作用是"它是它自己"——两个空对象永远不相等。

`WorktreeRuntime` 拿它当 WeakMap 的键：

```typescript
// executionScope -> claimToken 映射，同一回复中连续的 tool call 共享此绑定。
readonly #scopeClaims = new WeakMap<object, string>();
```

`claim_task` 成功之后，登记一条：

```typescript
async claimTask(taskId: string, context: ToolContext): Promise<TaskClaim> {
  const binding = await this.#store.getWorktreeBinding(taskId);
  if (binding.status !== WorktreeStatus.ACTIVE) {
    throw new WorktreeStateError(`Task ${binding.taskId} does not have an active worktree`);
  }
  if (context.executionScope === undefined) {
    throw new WorktreeContextError("Task claim requires an execution scope");
  }
  const claim = await this.#store.claimTask(binding.taskId, context.identity);
  this.#scopeClaims.set(context.executionScope, claim.claimToken);
  return claim;
}
```

后面的每次 `resolve()` 就能查到它：

```typescript
#explicitContextToken(
  context: ToolContext,
): { readonly required: true; readonly value: string } | { readonly required: false } {
  // 显式 claimToken 优先级最高；executionScope 只作为同一回复内前序认领的短时关联。
  if (context.claimToken !== undefined) {
    try {
      return { required: true, value: canonicalClaimToken(context.claimToken) };
    } catch (error) {
      throw new WorktreeContextError("Execution claim token is invalid", { cause: error });
    }
  }
  if (context.executionScope !== undefined) {
    const token = this.#scopeClaims.get(context.executionScope);
    if (token !== undefined) return { required: true, value: token };
  }
  return { required: false };
}
```

返回类型是个**可辨识联合**：`required: true` 时必然带 `value`，`required: false` 时压根没有 `value` 这个字段。所以下一节 `resolve()` 里那句 `if (!token.required)` 之后，TypeScript 就知道剩下的分支里 `token.value` 一定是 string，不需要再判空。这比返回 `string | undefined` 然后到处 `if (token !== undefined)` 要干净——"有没有 claim"和"claim 是什么"是两个问题，类型上就分开。

**为什么用 WeakMap 而不是 Map？** 因为键是那个 scope 对象。回合结束、对象没人引用了，WeakMap 里的条目自动消失。用 Map 的话，每个回合都会永久留下一条记录——一个只增不减的内存泄漏。

**为什么显式 `claimToken` 优先级更高？** 因为跨回复的场景需要它。Teammate 自动认领时，claim token 是通过 `AgentRunner.run(prompt, { claimToken })` 传进来的，这时候还没有任何 `claim_task` 调用登记过 scope。显式 token 是"跨回复恢复凭据"，scope 是"同回复内的短时关联"，两者层次不同。

**完成任务时 scope 要清掉：**

```typescript
if (
  context.executionScope !== undefined &&
  this.#scopeClaims.get(context.executionScope) === normalizedToken
) {
  this.#scopeClaims.delete(context.executionScope);
}
```

否则同一次回复里 `complete_task` 之后的工具还会解析到那个已完成的租约。

实验 6 用一个脚本化模型，在一次回复里发出 `claim_task` + `write_file` 两个调用：

```text
===== routing =====
最终文本：done
两次工具调用解析出的 cwd：
  claim_task → <root>
  write_file → <root>\.agent_tutorial\worktrees\alice
worktree 里的 config.ts："// alice\n"
主目录 config.ts：      "export const value = 0;\n"
```

**`claim_task` 自己解析到的是主目录**（那时还没认领），**紧接着的 `write_file` 就解析到了 Worktree**。切换发生在同一次 assistant 回复的两个 tool call 之间。

---

## 失效 claim 必须失败，不能回落主目录

`resolve()` 的完整逻辑是这样的（[src/features/worktrees.ts](code/chapters/ch18/src/features/worktrees.ts)）：

```typescript
async resolve(context: ToolContext): Promise<ToolContext> {
  const token = this.#explicitContextToken(context);
  if (!token.required) {
    if (context.taskId !== undefined) {
      throw new WorktreeContextError("Explicit task context does not have an active claim");
    }
    return contextForWorkspace(context, this.#workspaceRoot);
  }
  let claim: TaskClaim | undefined;
  try {
    claim = await this.#store.lookupClaim(token.value);
  } catch (error) {
    if (error instanceof TaskError) {
      throw new WorktreeContextError("Execution claim is no longer active", { cause: error });
    }
    throw error;
  }
  if (claim === undefined) {
    throw new WorktreeContextError("Execution claim does not have a known active lease");
  }
  if (claim.task.owner !== context.identity) {
    throw new WorktreeContextError("Execution identity does not own the active Task claim");
  }
  if (context.taskId !== undefined && context.taskId !== claim.task.id) {
    throw new WorktreeContextError("Execution task does not match the active Task claim");
  }
  const binding = await this.#store.getWorktreeBinding(claim.task.id);
  if (binding.status !== WorktreeStatus.ACTIVE) {
    throw new WorktreeContextError("Active Task claim does not have an active worktree binding");
  }
  if (context.worktreeName !== undefined && context.worktreeName !== binding.name) {
    throw new WorktreeContextError("Execution worktree does not match the active Task binding");
  }
  const path = await this.#registeredWorktreePath(binding, true);
  return Object.freeze({
    ...context,
    workspace: path,
    taskId: claim.task.id,
    claimToken: claim.claimToken,
    worktreeName: binding.name,
  });
}
```

关键在开头那个分叉：

```text
有 claim（显式 token 或 scope 映射）
  → 必须通过全部 5 道校验，通过才给 Worktree 路径
  → 任何一道不通过：抛 WorktreeContextError，不给任何路径

没有 claim
  → 返回主 workspace（这是正常的：Lead 建任务、读文件，本来就该在主目录）
```

**"没 claim → 主目录"和"claim 无效 → 报错"是两件完全不同的事。** 前者是正常路径，后者是 fail-closed。

如果把后者也写成"回落主目录"，会发生什么？

```text
Alice 的租约刚过期（她自己不知道）
  → 她的下一次 write_file 解析失败
  → 如果回落主目录：她开始往主目录写代码，污染 integration ref
  → 而且没有任何报错，没人知道
```

这就是"静默降级"的典型危害：**它让一个可检测的失败变成一个不可检测的错误结果。**

五道校验各防一件事：

| 校验 | 防什么攻击/错误 |
| --- | --- |
| token 能查到、且仍是 active 租约 | 用过期的、伪造的、已完成的 token |
| `claim.task.owner === context.identity` | Bob 拿到 Alice 的 token 来用 |
| `context.taskId` 与 claim 的任务一致 | token 对了但声明的任务不对 |
| binding 必须是 `active` | 用一条 `reserved` / `kept` / `needs_review` 的 binding 路由 |
| `context.worktreeName` 与 binding 一致 | 声明的目录名和实际绑定不符 |

注意 `lookupClaim` 里那个"已知但失效"的处理：

```typescript
const known = queryRows(database, "SELECT task_id FROM task_claim_tokens WHERE token = ?", [
  normalizedToken,
])[0];
if (known === undefined) return undefined;      // 从来没生成过 → undefined
const now = this.#now();
releaseExpired(database, now);
const row = queryRows(
  database,
  "SELECT id, lease_expires_at_utc FROM tasks WHERE claim_token = ? AND status = 'in_progress'",
  [normalizedToken],
)[0];
if (row === undefined) {
  throw new TaskClaimError("claim token is known but no longer active");  // 生成过但失效 → 抛错
}
```

**"从来没有过"和"曾经有过但现在失效"要区分开。** 前者可能是随手编的字符串，后者说明系统里确实发生了什么（租约过期、任务完成）。第 17 章的 `task_claim_tokens` 历史表让这个区分成为可能。

实验 7 把九种情况全跑一遍：

```text
===== failclosed =====
正确 identity + token        → <root>\.agent_tutorial\worktrees\alice
错 identity                 → WorktreeContextError: Execution identity does not own the active Task claim
错 task_id                  → WorktreeContextError: Execution task does not match the active Task claim
没生成过的 token                → WorktreeContextError: Execution claim does not have a known active lease
格式错误的 token                → WorktreeContextError: Execution claim token is invalid
错 worktree 名               → WorktreeContextError: Execution worktree does not match the active Task binding
完全没有 claim                 → <root>
只给 task_id 不给 token        → WorktreeContextError: Explicit task context does not have an active claim
完成后再用旧 token               → WorktreeContextError: Execution claim is no longer active
```

**九行里只有两行返回了路径。** 第一行（一切正确）给 Worktree；第七行（完全没有 claim）给主目录。其余七行全部是明确的错误，错误消息各不相同——排查时能立刻知道是哪一环出了问题。

特别看第八行："只给 task_id 不给 token"。这是那个 `if (context.taskId !== undefined) throw` 分支。为什么不能放过去？因为"我要在任务 X 的上下文里执行"这个声明本身就意味着应该有 claim。没有 claim 却声明了任务，说明调用方的状态是混乱的——宁可失败。

---

## 手动认领和自动窃取必须走同一条路

上一章有两个认领入口：模型显式调 `claim_task`，以及 idle Teammate 自动 `claimNext`。本章给认领加了一条新约束（"必须有 active binding"），这条约束**不能只加在其中一个入口上**。

代码的做法是让 `WorktreeRuntime` 直接实现 `TaskClaimService` 接口：

```typescript
export class WorktreeRuntime implements TaskClaimService, ToolContextProvider {
```

三个方法都在它身上：

| 方法 | 谁调用 | 加了什么约束 |
| --- | --- | --- |
| `claimTask(taskId, context)` | 模型调 `claim_task` 工具 | binding 必须 `active`；必须有 `executionScope` |
| `claimNext(owner)` | Teammate idle loop | 走 `claimNextBound`，只扫 `active` binding |
| `completeTask(taskId, token, context)` | 模型调 `complete_task` 工具 | 先 `resolve()` 再核对，成功后清 scope |

`claimNext` 只有三行，但换掉了底层查询：

```typescript
async claimNext(owner: string): Promise<TaskClaim | undefined> {
  // 自动认领走 claimNextBound：只挑已有 active binding 的 ready task，
  // 避免 idle polling 绕过 Worktree 约束。
  return await this.#store.claimNextBound(owner);
}
```

`claimNextBound` 在第 17 章 `claimNext` 的基础上多了一层筛选（[src/adapters/task-sqlite.ts](code/chapters/ch18/src/adapters/task-sqlite.ts)）：

```typescript
async claimNextBound(owner: string): Promise<TaskClaim | undefined> {
  const normalizedOwner = normalizeOwner(owner);
  return await this.#transaction(true, (database) => {
    const now = this.#now();
    releaseExpired(database, now);
    const graph = loadGraph(database);
    const active = new Set(
      queryRows(database, "SELECT task_id FROM worktree_bindings WHERE status = 'active'").map(
        (row) => requiredString(row.task_id, "worktree task id"),
      ),
    );
    for (const id of graph.order) {
      const task = graph.tasks.get(id);
      if (task === undefined || !active.has(id) || task.status !== TaskStatus.PENDING) continue;
      if (
        task.blockedBy.some(
          (dependency) => graph.tasks.get(dependency)?.status !== TaskStatus.COMPLETED,
        )
      ) {
        continue;
      }
      return this.#claim(database, graph, task, normalizedOwner, now);
    }
    return undefined;
  });
}
```

注意 `!active.has(id)` 那个 `continue`：**没有 active binding 的任务被跳过，不是报错，是跳过。** 它就像不存在一样，扫描继续往后走。

这个选择有个直接后果：**一个没绑定 Worktree 的 ready 任务会永远留在 `pending`。** 这是刻意的。第 17 章的行为是"有活就干"，本章改成"有地盘才干"。想让它被认领，Lead 得先给它建 Worktree。

实验 8 验证了这一点：

```text
===== auto =====
任务板顺序：没有 worktree 的任务(pending) / 有 worktree 的任务(pending)
未绑定任务排在前面，但 claimNext 只会挑已绑定的那条：
  第一次 claimNext → 有 worktree 的任务
  未绑定任务状态   → pending
  第二次 claimNext（bob）→ undefined（没有第二个 active binding）

自动认领 prompt 长这样：
<auto-claimed-task>
{"claim_token":"00000000-0000-4000-8000-000000001811","lease_expires_at_utc":"2026-08-30T10:01:00.000Z","task":{"blocked_by":[],"description":"","id":"00000000-0000-4000-8000-000000001802","owner":"alice","status":"in_progress","subject":"有 worktree 的任务"}}
</auto-claimed-task>
```

**未绑定的任务排在更前面，但被跳过了**，而且它的状态没被动过。第二次认领返回 `undefined`——不是"没有 ready 任务"，而是"没有第二条 active binding"。

### 自动认领之后，claim token 怎么传下去

Teammate 拿到 claim 之后，把 token 同时当幂等键和路由凭据传给 Runner（[src/features/teammates.ts](code/chapters/ch18/src/features/teammates.ts)）：

```typescript
// claim 是 SQLite 的原子租约；claimToken 同时作为幂等键，失败重放不会重复执行同一任务。
await worker.runner.run(workStealing.renderClaimPrompt(claim), {
  idempotencyKey: claim.claimToken,
  claimToken: claim.claimToken,
  signal,
});
```

一个 token，三个作用：

```text
claim.claimToken
  ├─▶ idempotencyKey  ：重放不重复执行（第 17 章）
  ├─▶ claimToken      ：ToolContext 的显式凭据 → resolve() 路由到 Worktree（本章）
  └─▶ complete_task 的参数：完成任务时必须出示（第 17 章）
```

这个复用不是省事，而是本来就该是同一个东西：**"这次运行属于哪个任务租约"这个问题只有一个答案。**

### 组合根强制三者共享同一个对象

[src/bootstrap.ts](code/chapters/ch18/src/bootstrap.ts) 有一段看着啰嗦但很关键的校验：

```typescript
if (
  dependencies.worktreeRuntime !== undefined &&
  (dependencies.workStealingRuntime === undefined ||
    dependencies.workStealingRuntime.store !== dependencies.worktreeRuntime.store ||
    dependencies.workStealingRuntime.claimService !== dependencies.worktreeRuntime)
) {
  throw new Error(
    "worktreeRuntime must be the WorkStealingRuntime claim service and share its store",
  );
}
```

三个 `!==` 都是**引用相等**判断，不是内容比较。它要求：

```text
      ┌─────────────────────┐
      │  SqliteTaskStore    │  ← 只有一个实例
      └──────┬───────┬──────┘
             │       │
    ┌────────▼──┐  ┌─▼───────────────────┐
    │ Worktree  │  │ WorkStealingRuntime │
    │ Runtime   │◀─┤ .claimService       │  ← 必须指向同一个 WorktreeRuntime
    └───────────┘  └─────────────────────┘
```

**为什么用引用相等而不是"内容一样就行"？** 因为 `#scopeClaims` 那个 WeakMap 是**实例私有状态**。两个 `WorktreeRuntime` 实例即使配置完全相同，WeakMap 也是各自独立的——Alice 在实例 A 上登记的 scope，实例 B 查不到。这类 bug 会表现为"偶尔路由失败"，极难定位。所以在组合根就用一行 `!==` 直接拒绝。

`WorkStealingRuntime` 那边也有对称的检查（[src/features/work-stealing.ts](code/chapters/ch18/src/features/work-stealing.ts)）：

```typescript
// claimService.store === store 才允许，防止两处状态分叉
if (!isTaskClaimService(claimService) || claimService.store !== store) {
  throw new Error("claimService must use the leased Task store");
}
```

---

## 三个新工具，只给 Lead

Lead 多了三个工具，Teammate 和 Subagent 一个都没有。定义在 `worktreeToolDefinitions`：

| 工具 | 参数 | effect | 干什么 |
| --- | --- | --- | --- |
| `create_worktree` | `task_id`, `name`, `integration_ref` | `external` | 为一个 pending 任务建绑定的 Worktree |
| `keep_worktree` | `task_id` | `write` | 显式保留已完成任务的 Worktree |
| `remove_worktree` | `task_id` | `external` | 证明安全后删除 Worktree |

**`effect` 分类不是装饰。** 它决定这个工具会不会经过审批、会不会被计划门禁拦住（第 3、4、16 章的机制）。分类理由：

- `create_worktree` / `remove_worktree` 是 `external`：它们调 Git 子进程、改磁盘目录结构、改分支，影响范围超出单个文件；
- `keep_worktree` 是 `write`：它只改数据库里的一行状态，Git 侧只读了一下 `HEAD`。

**为什么 Teammate 拿不到这三个？** 因为 Worktree 的生命周期是 Lead 的编排职责。如果队友能自己建 Worktree，就会出现"我先给自己开个目录再干活"的自增长循环——和第 17 章不给队友 `create_task` 是同一个理由。如果队友能自己删，一个判断失误就可能删掉别人还没 review 的成果。

实验 9 把差量列出来。注意它搭的是**最小注册表**（文件工具 + 任务工具 + 本章三个），不是 P18 完整的 Lead 工具面——真实 Lead 还有 `todo_write`、`task`、Skill、Cron、`spawn_teammate`、`send_message`、Protocol 那一堆。这里只关心"本章加了什么、队友有没有"：

```text
===== tools =====
Lead：     shell, read_file, write_file, edit_file, glob, create_task, get_task, list_tasks, claim_task, complete_task, create_worktree, keep_worktree, remove_worktree
Teammate： shell, read_file, write_file, edit_file, glob, get_task, list_tasks, claim_task, complete_task

三个 Worktree 工具的 effect 分类：
  create_worktree  effect=external
  keep_worktree    effect=write
  remove_worktree  effect=external

Lead 有 create_task 吗：        true
Teammate 有 create_task 吗：    false
Teammate 有 create_worktree 吗：false
```

两侧的差量是四个：`create_task` 加三个 Worktree 工具。但队友照样有 `claim_task` / `complete_task`——**它能认领和完成，只是不能创建任务，也不能创建或销毁地盘。**

（真实的队友工具面比这里更窄：组合根用 `subset(["shell", "read_file", "write_file"])` 起步，再加 `send_message`、`submit_plan` 和四个任务工具。实验里用完整文件工具集是为了让对比只聚焦在任务和 Worktree 工具上。）

### 工具返回给模型的是什么

`encodeBinding` 把 binding 序列化成 snake_case JSON，键按字母序排列：

```typescript
function encodeBinding(binding: WorktreeBinding): string {
  // 工具输出使用 snake_case，日期固定为 UTC ISO 字符串。
  return JSON.stringify({
    baseline_commit: binding.baselineCommit,
    branch: binding.branchTip,
    branch_tip: binding.branchTip,
    created_at_utc: binding.createdAtUtc.toISOString(),
    integration_ref: binding.integrationRef,
    name: binding.name,
    relative_path: binding.relativePath,
    review_reason: binding.reviewReason,
    status: binding.status,
    task_id: binding.taskId,
    updated_at_utc: binding.updatedAtUtc.toISOString(),
  });
}
```

实验 9 会把模型真正看到的内容打出来：

```text
create_worktree 返回给模型的内容（isError=false）：
  {
    "baseline_commit": "554107b249b070468bd47f5e44a6a6ea53138dec",
    "branch": "wt/alice",
    "branch_tip": "554107b249b070468bd47f5e44a6a6ea53138dec",
    "created_at_utc": "2026-08-30T10:00:00.000Z",
    "integration_ref": "refs/heads/main",
    "name": "alice",
    "relative_path": ".agent_tutorial/worktrees/alice",
    "review_reason": null,
    "status": "active",
    "task_id": "00000000-0000-4000-8000-000000001801",
    "updated_at_utc": "2026-08-30T10:00:00.000Z"
  }
```

刚创建时 `branch_tip === baseline_commit`——分支还没有新提交，顶端就是基线。这是个有用的观察点：之后每次 `keep` / `remove`，`branch_tip` 都会更新成当时的真实 `HEAD`。

（你自己跑出来的 commit 哈希会和上面不一样：实验每次都新建一个临时仓库，初始提交的时间戳不同，哈希自然不同。ID 和时间戳是固定的，因为实验注入了确定性的生成器和时钟。）

**内部用 camelCase，wire format 用 snake_case，这条边界只在工具定义里跨越。** 领域对象和数据库列名各自保持自己的风格，转换只发生在一处。

同一个实验还验证了唯一约束的表现——错误是**结构化返回**，不是抛异常：

```text
同名再建一次 → isError=true errorCode=worktree_invalid_state
  Error [worktree_invalid_state]: Worktree task, name, branch, or path is already reserved
```

模型收到一条带 `errorCode` 的错误结果，可以据此决定换个名字重试。如果这里抛异常，整个回合就断了。

---

## 清理必须先证明"可以删"

这是本章代码量最大、也最值得细读的一段。核心立场一句话：

> **删除不是一个命令，是一串证明。证明不出来就不删。**

`remove_worktree` 依次走八步，任何一步失败都转 `needs_review`：

```text
        remove_worktree(task_id)
                 │
    ┌────────────▼─────────────┐
    │ ① 任务是 completed 吗？   │──否──▶ 抛 WorktreeStateError（连状态都不对，不是 review 的事）
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ② binding 状态允许吗？    │──否──▶ 抛 WorktreeStateError
    │   （active/kept/         │
    │     needs_review）        │
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ③ 受管路径真实存在，      │──否──▶ needs_review
    │   且没逃出 workspace？    │        "managed worktree path is unavailable or escapes the workspace"
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ④ Git 登记的 worktree     │──否──▶ needs_review
    │   和分支都对得上吗？      │        "managed path is not the registered worktree and branch"
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ⑤ git status 跑成功了吗？ │──否──▶ needs_review
    └────────────┬─────────────┘        "git status could not prove the worktree is clean"
    ┌────────────▼─────────────┐
    │ ⑥ 输出是空的吗？          │──否──▶ needs_review
    │   （真的没有未提交改动）  │        "worktree has uncommitted changes"
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ⑦ 两个 commit 都解析出    │──否──▶ needs_review
    │   来了吗？                │        "integration ref or branch tip could not be resolved"
    └────────────┬─────────────┘
    ┌────────────▼─────────────┐
    │ ⑧ 分支顶端是 integration  │──否──▶ needs_review
    │   ref 的祖先吗？          │        "worktree branch tip is not contained by the integration ref"
    └────────────┬─────────────┘
                 │ 八步全过
    ┌────────────▼──────────────────────────────────┐
    │ git switch --detach <tip>   在 worktree 里     │──失败──▶ needs_review
    │ git branch -d wt/<name>     在主目录（非 -D）  │──失败──▶ needs_review
    │ git worktree remove <path>  在主目录（非 force）│──失败──▶ needs_review
    └────────────┬──────────────────────────────────┘
                 ▼
             removed
```

逐条说为什么这一步不能省：

**③ 路径检查用 `realpath` 而不是 `existsSync`。** `#registeredWorktreePath` 先确认 `relative_path` 就是运行时派生的那个值，再 `realpath` 解析真实路径，再确认解析结果**等于**预期路径（防符号链接被替换），再确认仍在 workspace 内。

**④ 光有目录不算"受管"。** `#isRegisteredWorktree` 同时问 Git 三个问题：

```typescript
const [top, common, branch] = await Promise.all([
  this.#runGit(["rev-parse", "--show-toplevel"], path),
  this.#runGit(["rev-parse", "--git-common-dir"], path),
  this.#runGit(["branch", "--show-current"], path),
]);
```

- `--show-toplevel` 必须等于这个路径（不是嵌套在别的仓库里）；
- `--git-common-dir` 必须等于**主仓库**的 `.git`（不是另一个仓库的 worktree）；
- `--show-current` 必须等于 `wt/{name}`（分支没被切走）。

**为什么要问 `--git-common-dir`？** 想象有人在 `.agent_tutorial/worktrees/alice` 位置放了另一个仓库的 worktree，或者干脆 `git init` 了一个新仓库。目录存在、分支名也能对上，但它属于另一个对象库。删掉它就是删别人的东西。

**⑤ 和 ⑥ 是两件不同的事。** `git status` 返回非零（命令本身失败）和 `git status` 返回空（确实干净）不能混。前者是"我不知道"，后者是"我知道是干净的"。**"不知道"必须按"可能有改动"处理。**

```typescript
const status = await this.#runGit(["status", "--porcelain=v1", "--untracked-files=all"], path);
if (status.returncode !== 0) {
  return await this.#needsReview(binding, binding.branchTip, "git status could not prove the worktree is clean");
}
if (status.stdout.length > 0) {
  return await this.#needsReview(binding, binding.branchTip, "worktree has uncommitted changes");
}
```

注意 `--untracked-files=all`：**新建但没 add 的文件也算脏。** 默认的 `--untracked-files=normal` 对目录只报目录名，`all` 会展开到每个文件。既然要判断"这里有没有人的劳动成果"，就该用最严的那档。

**⑧ 用 `git merge-base --is-ancestor` 而不是别的。**

```typescript
const ancestry = await this.#runGit(
  ["merge-base", "--is-ancestor", branchTip, integrationTip],
  this.#workspaceRoot,
);
```

这个命令回答的是一个精确的问题：**"`branchTip` 这个提交，是不是已经包含在 `integrationTip` 的历史里？"** 返回 0 是"是"，1 是"不是"。

它同时覆盖了几种情况：普通 merge、fast-forward、rebase 之后（如果 rebase 的结果并入了）、squash merge（不覆盖——squash 会产生新 commit，原提交不是祖先，所以会落 `needs_review`，这是保守但正确的）。

**对比一下代码里刻意没用的做法：**

| 没用的做法 | 为什么不用 |
| --- | --- |
| `git branch --merged` | 输出要解析文本，且语义依赖当前 HEAD |
| 比较 `@{push}` / upstream | 依赖远程配置，本章不假设有远程 |
| 检查"有没有未推送提交" | "推送了"不等于"并入了"，这是两件事 |
| `git branch -D` | 强制删除，会丢掉未并入的提交 |
| `git worktree remove --force` | 会丢掉未提交的改动 |

最后三步的顺序也有讲究：

```text
① git switch --detach <tip>   为什么先 detach？
   因为一个被 worktree checkout 出去的分支，git branch -d 删不掉。
   detach 让 worktree 的 HEAD 直接指向 commit，分支就"自由"了。

② git branch -d wt/<name>     为什么在主目录执行？
   分支是仓库级的，在哪个 worktree 里执行都一样，但主目录是权威位置。
   用 -d 不用 -D：-d 自带"已并入才删"的检查，等于第二道保险。

③ git worktree remove <path>  为什么最后？
   目录删了就没法再在里面执行 Git 命令了。
```

实验 10 用一个 `RecordingGitRunner` 把真实调用序列录下来（这是真实执行的命令，不是我写的示意）：

```text
===== cleanup =====
remove_worktree 逐条证明用到的 Git 命令：
  git branch --show-current -> 0
  git rev-parse --show-toplevel -> 0
  git rev-parse --git-common-dir -> 0
  git status --porcelain=v1 --untracked-files=all -> 0
  git rev-parse --verify HEAD^{commit} -> 0
  git rev-parse --verify refs/heads/main^{commit} -> 0
  git merge-base --is-ancestor d504d7df927bd8d6368c5462581f3943b9ab37cc d504d7df927bd8d6368c5462581f3943b9ab37cc -> 0
  git switch --detach d504d7df927bd8d6368c5462581f3943b9ab37cc -> 0
  git branch -d wt/alice -> 0
  git worktree remove C:\Users\<你>\AppData\Local\Temp\probe-ch18-kTskdw\.agent_tutorial\worktrees\alice -> 0
最终 binding：status=removed branch_tip=d504d7d review_reason=null
branch_tip 与 alice 分支最后一次提交一致：true
git worktree list 里还剩：1 个
alice 分支还在吗：""
main 上还有 alice 的提交吗："export const value = 1;\r\n"
```

十条命令，**没有一条带 `--force` 或 `-D`**。

**前三条的顺序你跑出来可能不一样。** 因为 `#isRegisteredWorktree` 用 `Promise.all` 并行发这三条，`RecordingGitRunner` 按**完成顺序**记录。它们之间没有依赖关系，谁先回来都不影响结论——三个答案必须全对才算"是受管 Worktree"。从第四条 `git status` 开始就是严格串行的了，因为后面每一步都依赖前一步的结论。

那个 `--is-ancestor A A` 看起来奇怪——两个参数是同一个 commit。这是因为这次是 fast-forward merge，`main` 直接指向了 alice 的提交。一个 commit 是它自己的祖先，返回 0，判断成立。

最后一行 `"export const value = 1;\r\n"` 证明 Alice 的改动**留在了 main 上**——目录删了，成果没丢。

同一个实验还跑了三条失败路径：

```text
有未提交改动 → status=needs_review
  review_reason: worktree has uncommitted changes
  目录还在：true
  分支还在：true
  事件序列：reserve/reserved → create/active → needs_review/needs_review

没并入 main → status=needs_review
  review_reason: worktree branch tip is not contained by the integration ref
  目录还在：true
  分支还在：true
  事件序列：reserve/reserved → create/active → needs_review/needs_review

git status 自己报错 → status=needs_review
  review_reason: git status could not prove the worktree is clean
  目录还在：true
  分支还在：true
  事件序列：reserve/reserved → create/active → needs_review/needs_review
```

三条路径，**三个不同的原因，三次目录和分支都还在**。第三条特别值得注意：Git 命令本身失败了，运行时没有"重试一下"或"假设是干净的"，而是直接判定"我证明不了"。

### review_reason 不泄露 stderr

八条 review reason 全是**固定的英文短句**，没有一条拼接了 Git 的 stderr：

```typescript
"managed worktree path is unavailable or escapes the workspace"
"managed path is not the registered worktree and branch"
"git status could not prove the worktree is clean"
"worktree has uncommitted changes"
"integration ref or branch tip could not be resolved"
"worktree branch tip is not contained by the integration ref"
"git could not detach the managed worktree"
"git could not safely delete the integrated branch"
"git could not remove the detached worktree"
```

三个理由：

**一，stderr 可能含敏感信息。** 绝对路径、用户名、远程 URL（可能带 token）。这些会被写进数据库，还会返回给模型，进入上下文。

**二，固定字符串可以被程序判断。** 想写一个"统计各类失败原因"的脚本？固定枚举可以直接分组，拼了 stderr 的字符串就得写正则。

**三，Git 的错误文本会随版本变化。** 依赖它做判断的代码会在升级 Git 时莫名失效。

### `#needsReview` 不重复迁移

一个容易忽略的细节：

```typescript
async #needsReview(
  binding: WorktreeBinding,
  branchTip: string | null,
  reason: string,
): Promise<WorktreeBinding> {
  // 清理失败统一落 needs_review；已是 kept 或 needs_review 时保持保留态，避免虚构状态迁移。
  if (binding.status === WorktreeStatus.NEEDS_REVIEW || binding.status === WorktreeStatus.KEPT) {
    return binding;
  }
  return await this.#store.markWorktreeNeedsReview(binding.taskId, {
    branchTip,
    reason,
    occurredAtUtc: this.#now(),
  });
}
```

如果 binding 已经是 `kept` 或 `needs_review`，直接原样返回，**不写新事件**。

为什么？因为这两个状态都已经是"保留态"了。对一个 `kept` 的 binding 反复调用 `remove_worktree`（每次都失败），不应该在审计表里留下一串重复的 `needs_review` 事件。**审计记录的是状态变化，不是失败尝试的次数。**

而且 `kept` 是 Lead 主动做的决定（"我要留着这个"）。清理失败不应该把这个主动决定覆盖成"出问题了"。

### `keep_worktree` 比 remove 简单，但也要证明

```typescript
async keepWorktree(taskId: string): Promise<WorktreeBinding> {
  const binding = await this.#completedBinding(taskId, [WorktreeStatus.ACTIVE]);
  let branchTip: string;
  try {
    const path = await this.#registeredWorktreePath(binding, true);
    branchTip = await this.#resolveCommit("HEAD", path);
  } catch (error) {
    if (!(error instanceof WorktreeError)) throw error;
    return await this.#needsReview(
      binding,
      binding.branchTip,
      "managed worktree path or branch tip could not be resolved",
    );
  }
  return await this.#store.keepWorktree(binding.taskId, {
    branchTip,
    occurredAtUtc: this.#now(),
  });
}
```

`keep` 不删任何东西，为什么还要证明？因为它要**记录一个事实**：这个 Worktree 保留时的分支顶端是哪个 commit。

如果连 `HEAD` 都解析不出来（目录被人删了、Git 元数据坏了），那就没法记录这个事实。这时候写一个 `kept` 是在说谎——所以转 `needs_review`。

注意 `keep` 只接受 `active`（`#completedBinding(taskId, [WorktreeStatus.ACTIVE])`），而 `remove` 接受三种（`active` / `kept` / `needs_review`）。因为 `keep` 是"从工作态转保留态"，而 `remove` 是最终清理，任何保留态都可以尝试进入。

---

## 审计和状态迁移必须同事务

binding 的每一次状态变化，都和一条审计事件在同一个 SQLite 事务里提交。`#transitionWorktree` 是所有迁移的唯一入口：

```typescript
return await this.#transaction(true, (database) => {
  const graph = loadGraph(database);
  const task = getExisting(graph, normalized);
  // ① 任务状态必须符合这次迁移的前提
  if (task.status !== options.requiredTaskStatus) {
    throw new WorktreeStateError(
      `Task ${task.id} is ${task.status}; expected ${options.requiredTaskStatus}`,
    );
  }
  const current = loadWorktreeBinding(database, normalized);
  // ② binding 当前状态必须在允许集合里
  if (!options.expected.includes(current.status)) {
    throw new WorktreeStateError(
      `Worktree binding ${current.taskId} is ${current.status}; transition is not allowed`,
    );
  }
  const updated = new WorktreeBinding({ /* ... */ });
  // ③ 条件 UPDATE：WHERE 里带上旧状态
  const changed = database.run(
    "UPDATE worktree_bindings SET branch_tip = ?, status = ?, review_reason = ?, updated_at_utc = ? WHERE task_id = ? AND status = ?",
    [
      updated.branchTip,
      updated.status,
      updated.reviewReason,
      encodeDate(updated.updatedAtUtc),
      updated.taskId,
      current.status,
    ],
  );
  // ④ 必须恰好改了一行
  if (changed.getRowsModified() !== 1) {
    throw new WorktreeStateError("Worktree binding could not be atomically transitioned");
  }
  // ⑤ 同一事务内追加审计
  appendWorktreeEvent(database, options.action, updated);
  return updated;
});
```

**`WHERE task_id = ? AND status = ?` 加上 `getRowsModified() !== 1` 是第 17 章那套原子认领的同一手法。** 读到的旧状态和 UPDATE 时的旧状态必须一致，否则说明中间有人改过——放弃，不要基于过期读做写入。

每种迁移的前提条件都通过 `requiredTaskStatus` 声明：

| 迁移方法 | `expected`（binding 旧状态） | `requiredTaskStatus`（任务状态） |
| --- | --- | --- |
| `activateWorktree` | `reserved` | `pending` |
| `keepWorktree` | `active` | `completed` |
| `markWorktreeNeedsReview` | `active` | `completed` |
| `markWorktreeRemoved` | `active` / `kept` / `needs_review` | `completed` |

`activateWorktree` 要求任务仍是 `pending`——**创建 Worktree 的整个过程中，任务不能被别人认领掉。** 如果在 `git worktree add` 期间有人认领了这个任务，激活会失败，binding 留在 `reserved`。

`reserveWorktree` 也是同样的事务模式：

```typescript
// binding 写入和 reserve 审计事件放在同一个 BEGIN IMMEDIATE 事务中；
// 若事件写入失败，绑定状态一起回滚，不会出现"已预留但无审计"的中间态。
```

实验 5 会跑一次完整的 reserve → create → keep，看审计链：

```text
===== audit =====
worktree_events（append-only）：
  #1 reserve       status=reserved     branch_tip=null at=2026-08-30T10:00:00.000Z
  #2 create        status=active       branch_tip=365675c at=2026-08-30T10:00:00.000Z
  #3 keep          status=kept         branch_tip=365675c at=2026-08-30T10:00:00.000Z
```

三条事件的时间戳完全相同（测试用固定时钟），但 `sequence` 给出了确定顺序。`#1` 的 `branch_tip` 是 null——那时候分支还不存在，诚实地记 null，而不是先填个占位值。

---

## 非 Git 目录要在建任何状态之前失败

`validateRepository()` 是所有 Worktree 操作的前置门禁：

```typescript
async validateRepository(): Promise<void> {
  if (this.#repositoryValidated) return;
  let root: string;
  try {
    root = await realpath(this.#workspaceRoot);
    if (!(await stat(root)).isDirectory()) throw new Error("workspace is not a directory");
  } catch (error) {
    throw new WorktreeRepositoryError("workspace is not a Git repository", { cause: error });
  }
  const inside = await this.#runGit(["rev-parse", "--is-inside-work-tree"], root);
  if (inside.returncode !== 0 || inside.stdout.trim() !== "true") {
    throw new WorktreeRepositoryError("workspace is not a Git repository");
  }
  const top = await this.#runGit(["rev-parse", "--show-toplevel"], root);
  if (top.returncode !== 0)
    throw new WorktreeRepositoryError("workspace is not a Git repository");
  let repositoryRoot: string;
  try {
    repositoryRoot = await realpath(top.stdout.trim());
  } catch (error) {
    throw new WorktreeRepositoryError("Git repository root could not be resolved", { cause: error });
  }
  if (repositoryRoot !== root) {
    throw new WorktreeRepositoryError("workspace is not a Git repository root");
  }
  this.#repositoryValidated = true;
}
```

**注意最后那个 `repositoryRoot !== root` 判断：必须正好是仓库根，子目录不行。**

为什么这么严？因为 `relative_path` 是相对仓库根算的（`.agent_tutorial/worktrees/{name}`）。如果 workspace 是 `repo/src`，那 `.agent_tutorial` 就会建在 `repo/src/.agent_tutorial`，而 `git worktree add` 的路径解析、`--git-common-dir` 的比较、清理时的 `isWithin` 检查全都基于不同的坐标系。**一套逻辑必须只有一个原点。**

CLI 在**构造任何运行状态之前**就调它（[src/cli.ts](code/chapters/ch18/src/cli.ts)）：

```typescript
try {
  // 在创建任何运行状态前先验证 cwd 是 Git repository root，避免在错误目录留下任务库。
  if (worktreeRuntime !== undefined) await worktreeRuntime.validateRepository();
  runner = buildAgent(profile, { /* ... */ });
```

这个顺序是为了一个很实际的问题：**如果你在错误的目录下跑了本章的入口，不应该留下一个 `.agent_tutorial/tasks.sqlite3` 让你困惑。**

实验 11 验证这一点：

```text
===== guard =====
非 Git 目录 → WorktreeRepositoryError: workspace is not a Git repository root
createWorktree → WorktreeRepositoryError: workspace is not a Git repository root
worktree_bindings 行数：0
worktree_events 行数：  0
仓库子目录 → WorktreeRepositoryError: workspace is not a Git repository root
```

`createWorktree` 自己也调 `validateRepository()`（第一行就是），所以即使有人绕过 CLI 直接调 API 也拦得住。第三、四行确认了**没有留下任何 binding 或事件**。

`#repositoryValidated` 那个布尔标记是个小优化：验证通过后缓存，不用每次工具调用都 fork 两次 Git 进程。仓库根不会在运行中途变成非仓库。

---

## 两个运行入口

先在教程代码目录同步锁定依赖：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location $TutorialCode
npm ci
```

固定章节入口：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location 'C:\workspace\my-agent-project'
npm exec --prefix $TutorialCode -- tsx "$TutorialCode/chapters/ch18/src/chapters/ch18.ts" --prompt "创建两个任务并绑定独立 Worktree，再启动 alice 和 bob 分别认领"
```

通用入口：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location 'C:\workspace\my-agent-project'
npm exec --prefix $TutorialCode -- tsx "$TutorialCode/chapters/ch18/src/cli.ts" run --chapter 18 --prompt "创建两个任务并绑定独立 Worktree，再启动 alice 和 bob 分别认领"
```

`npm exec --prefix` 只从教程代码目录解析 `tsx`；Agent 的运行 cwd 仍是你自己那个 Git 仓库根目录。真实文件写入、Git 命令和 Worktree 生命周期工具继续经过既有 Hook、权限与审批边界。

固定入口 [src/chapters/ch18.ts](code/chapters/ch18/src/chapters/ch18.ts) 只有 5 行：

```typescript
import { runProfile } from "../cli.js";
import { P18 } from "../core/profiles.js";

// 固定入口复用通用 CLI，只固定 P18 profile，避免章节命令重复传 --chapter。
process.exitCode = await runProfile(P18, process.argv.slice(2));
```

两种入口共用 [src/cli.ts](code/chapters/ch18/src/cli.ts) 里的 `execute()`，装配顺序是固定的：

```typescript
// 单个 SqliteTaskStore 同时注入 Lead、Subagent 和 Teammate factory，避免多个数据库分叉。
const sqliteTaskStore = profile.capabilities.has("task_dag_sqlite")
  ? new SqliteTaskStore(workspace)
  : undefined;
// P18 的 WorktreeRuntime 同时充当 TaskClaimService 和 ToolContextProvider，
// 复用同一个 SQLite store，并把 Git 子进程收拢到 SubprocessGitRunner 适配层。
const worktreeRuntime =
  profile.capabilities.has("worktree") && sqliteTaskStore !== undefined
    ? new WorktreeRuntime({
        workspace,
        store: sqliteTaskStore,
        gitRunner: new SubprocessGitRunner(),
      })
    : undefined;
const workStealingRuntime =
  sqliteTaskStore === undefined
    ? undefined
    : new WorkStealingRuntime({
        store: sqliteTaskStore,
        ...(worktreeRuntime === undefined ? {} : { claimService: worktreeRuntime }),
      });
```

一个 store、一个 `WorktreeRuntime`、一个 `WorkStealingRuntime`，后两者都指向同一个 store，`WorkStealingRuntime` 的 claim service 就是那个 `WorktreeRuntime`。前面 bootstrap 那三个 `!==` 校验的正是这个结构。

profile 的差量只有一行（[src/core/profiles.ts](code/chapters/ch18/src/core/profiles.ts)）：

```typescript
// P18 在 SQLite 工作窃取之上加入受控 Git worktree 隔离。
export const P18: ChapterProfile = Object.freeze({
  chapter: 18,
  capabilities: new CapabilitySet([...P17.capabilities, "worktree"]),
});
```

---

## 一步一步跑起来

这一节假设你从零开始，不需要读过前面章节的运行说明。

### 第 0 步：确认工具版本

```powershell
git --version
node --version
npm --version
```

需要 Git 2.5 或更高（`git worktree` 从 2.5 开始有），Node 22 或更高。如果 `git --version` 报"不是内部或外部命令"，先装 Git for Windows。

### 第 1 步：装教程代码的依赖

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location $TutorialCode
npm ci
```

`npm ci` 严格按 `package-lock.json` 安装，不会偷偷升级版本。第一次跑会拉几十兆依赖，等它跑完。

### 第 2 步：准备一个真的 Git 仓库

本章的运行时**必须**在 Git 仓库根目录下跑。新建一个练手仓库：

```powershell
New-Item -ItemType Directory -Force 'C:\workspace\my-agent-project' | Out-Null
Set-Location 'C:\workspace\my-agent-project'
git init -b main
git config user.name  'Your Name'
git config user.email 'you@example.com'
Set-Content -Path '.gitignore' -Value '.agent_tutorial/' -Encoding utf8
Set-Content -Path 'config.ts' -Value 'export const value = 0;' -Encoding utf8
git add .
git commit -m 'initial'
```

`.gitignore` 那一行很重要：**受管 Worktree 建在 `.agent_tutorial/worktrees/`，不忽略它的话，Git 会把子 Worktree 的内容当成主目录的待提交文件。** 忽略之后，`git status` 在主目录看起来永远是干净的。

`config.ts` 是待会儿观察隔离效果的靶子文件。

### 第 3 步：确认仓库状态合格

```powershell
git rev-parse --is-inside-work-tree
git rev-parse --show-toplevel
git status --porcelain=v1
```

第一条要输出 `true`，第二条要输出 `C:/workspace/my-agent-project`（正斜杠是 Git 的风格），第三条要输出空。三条都对，`validateRepository()` 才会放行。

如果第三条有输出，先 commit 或 stash——虽然 `validateRepository()` 不检查主目录是否干净，但脏的主目录会让你分不清哪些改动是 Agent 写的。

### 第 4 步：配置模型

```powershell
$env:OPENAI_BASE_URL = 'https://your-endpoint/v1'
$env:OPENAI_API_KEY  = 'sk-...'
$env:OPENAI_MODEL    = 'your-model'
```

三个都缺会得到一条明确的报错：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**只想看 Worktree 机制、不想连模型？** 跳到第 6 步的实验脚本，11 个实验全都不需要模型（只有实验 6 和 8 用一个回放脚本冒充模型）。

### 第 5 步：跑一次

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location 'C:\workspace\my-agent-project'
npm exec --prefix $TutorialCode -- tsx "$TutorialCode/chapters/ch18/src/chapters/ch18.ts" --prompt "把 config.ts 里的 value 改成 1，先给任务建一个名叫 alice 的 worktree"
```

跑完之后，看看留下了什么：

```powershell
git worktree list
Get-ChildItem '.agent_tutorial\worktrees'
git branch --list 'wt/*'
```

三条命令分别给出 Git 视角、文件系统视角和分支视角。三者应该一致。

### 第 6 步：直接读数据库

Worktree 状态存在 `.agent_tutorial/tasks.sqlite3`。没装 sqlite3 命令行工具的话，用教程依赖里的 sql.js 读：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
npm exec --prefix $TutorialCode -- tsx -e @'
import initSqlJs from "sql.js";
import { readFile } from "node:fs/promises";
import { createRequire } from "node:module";
import { dirname, join } from "node:path";
const require = createRequire(process.env.TUTORIAL_CODE + "/x.js");
const SQL = await initSqlJs({ locateFile: (f) => join(dirname(require.resolve("sql.js")), f) });
const db = new SQL.Database(await readFile(".agent_tutorial/tasks.sqlite3"));
for (const t of ["worktree_bindings", "worktree_events"]) {
  console.log("--", t);
  const r = db.exec(`SELECT * FROM ${t}`)[0];
  if (r) console.table(r.values.map((v) => Object.fromEntries(r.columns.map((c, i) => [c, v[i]]))));
}
'@
```

（`$env:TUTORIAL_CODE` 需要先设成教程代码目录。这段只是查看用，跑不通也不影响后面的实验。）

### 第 7 步：清理

```powershell
Set-Location 'C:\workspace\my-agent-project'
git worktree list
```

如果还有受管 Worktree 残留，**先看里面有没有你想留的东西**，再按正常流程删：

```powershell
git worktree remove '.agent_tutorial\worktrees\alice'
git branch -d 'wt/alice'
```

用 `-d` 不用 `-D`。如果 `-d` 拒绝删除，说明这个分支还有没并入的提交——那正是你该去看一眼的信号。

---

## 11 个动手实验

下面这些实验都在临时目录里跑，**不碰你的任何真实仓库**，跑完自动清理。它们直接 import 第 18 章的源码，所以看到的行为就是真实行为。

### 先搭好探测脚本

在 `F:\笔记\Agent实操\code\scripts\probe-ch18.mts` 新建文件。先写公共部分：

```typescript
// scripts/probe-ch18.mts —— 第 18 章探测脚本（临时用，跑完可删）
import { execFile } from "node:child_process";
import { mkdir, mkdtemp, readFile, rm, writeFile } from "node:fs/promises";
import { createRequire } from "node:module";
import { tmpdir } from "node:os";
import { dirname, join } from "node:path";
import { promisify } from "node:util";

import initSqlJs from "sql.js";

import { SubprocessGitRunner } from "../chapters/ch18/src/adapters/git.js";
import { SqliteTaskStore } from "../chapters/ch18/src/adapters/task-sqlite.js";
import { TaskStatus } from "../chapters/ch18/src/features/tasks.js";
import { WorkStealingRuntime } from "../chapters/ch18/src/features/work-stealing.js";
import type { GitCommandResult, GitRunner } from "../chapters/ch18/src/features/worktrees.js";
import { WorktreeRuntime } from "../chapters/ch18/src/features/worktrees.js";

const require = createRequire(import.meta.url);
const execFileAsync = promisify(execFile);
const EXPERIMENT = process.argv[2] ?? "all";
const CLOCK_BASE = new Date("2026-08-30T10:00:00.000Z");

// 只跑命令行指定的那个实验；不给参数就全跑。
function section(name: string): boolean {
  const selected = EXPERIMENT === "all" || EXPERIMENT === name;
  if (selected) console.log(`\n===== ${name} =====`);
  return selected;
}

// 每个实验一个临时目录，结束一定删掉。
async function withTempDir<T>(operation: (root: string) => Promise<T>): Promise<T> {
  const root = await mkdtemp(join(tmpdir(), "probe-ch18-"));
  try {
    return await operation(root);
  } finally {
    await rm(root, { recursive: true, force: true });
  }
}

async function git(cwd: string, ...argumentsValue: readonly string[]): Promise<string> {
  const result = await execFileAsync("git", [...argumentsValue], {
    cwd,
    encoding: "utf8",
    windowsHide: true,
  });
  return result.stdout.trim();
}
```

再加一个"造一个干净仓库"的工具函数——**它和第 2 步手工做的事情完全一样**：

```typescript
// 建一个干净的 Git 仓库：main 分支 + 一个初始提交 + 忽略 .agent_tutorial/
async function gitRepository(root: string): Promise<void> {
  await git(root, "init", "-b", "main");
  await git(root, "config", "user.name", "Probe");
  await git(root, "config", "user.email", "probe@example.test");
  await writeFile(join(root, ".gitignore"), ".agent_tutorial/\n", "utf8");
  await writeFile(join(root, "config.ts"), "export const value = 0;\n", "utf8");
  await git(root, "add", ".");
  await git(root, "commit", "-m", "initial");
}
```

然后是确定性工具。ID 和时间都要可复现，不然输出每次都不一样，没法对照：

```typescript
// 生成 …1801 / …1802 这样递增的假 UUID，输出可复现。
function seqId(start: number): () => string {
  let next = start;
  return () => {
    const hex = next.toString(16).padStart(12, "0");
    next += 1;
    return `00000000-0000-4000-8000-${hex}`;
  };
}

// 固定时钟：每次读取推进 1 秒，时间戳可预期。
class Clock {
  #current = CLOCK_BASE.getTime();
  now(): Date {
    this.#current += 1_000;
    return new Date(this.#current);
  }
}

function errorLine(error: unknown): string {
  if (error instanceof Error) return `${error.constructor.name}: ${error.message}`;
  return String(error);
}

function short(id: string): string {
  return id.slice(-4);
}

// 用 sql.js 只读地打开 tasks.sqlite3，直接看原始表结构和行。
async function readSqlite(
  path: string,
): Promise<(sql: string) => readonly Record<string, unknown>[]> {
  const SQL = await initSqlJs({
    locateFile: (file: string) => join(dirname(require.resolve("sql.js")), file),
  });
  const database = new SQL.Database(new Uint8Array(await readFile(path)));
  return (sql: string) => {
    const statement = database.prepare(sql);
    const rows: Record<string, unknown>[] = [];
    while (statement.step()) rows.push(statement.getAsObject() as Record<string, unknown>);
    statement.free();
    return rows;
  };
}
```

最后是装配函数。**这是实验的核心：它和 `cli.ts` 里的真实装配一模一样**——一个 store，一个 `WorktreeRuntime`（同时充当 claim service），一个 `WorkStealingRuntime`：

```typescript
async function components(root: string, idBase = 0x1801) {
  await gitRepository(root);
  const clock = new Clock();
  const store = new SqliteTaskStore(root, {
    idGenerator: seqId(idBase),
    claimTokenGenerator: seqId(idBase + 0x10),
    clock,
    leaseDurationMs: 60_000,
  });
  const worktrees = new WorktreeRuntime({
    workspace: root,
    store,
    gitRunner: new SubprocessGitRunner(),
    clock: () => clock.now(),
  });
  await worktrees.validateRepository();
  const workStealing = new WorkStealingRuntime({ store, claimService: worktrees });
  return { clock, store, worktrees, workStealing };
}
```

再加两个"故意让某条 Git 命令失败"的假 runner。它们只拦一条命令，其他照常执行——这样失败路径的其他环节还是真实的：

```typescript
// 记录每一条 Git 命令，同时真正执行它，用来观察清理链路的确切调用顺序。
class RecordingGitRunner implements GitRunner {
  readonly calls: string[] = [];
  readonly #inner = new SubprocessGitRunner();

  async run(argumentsValue: readonly string[], cwd: string): Promise<GitCommandResult> {
    const result = await this.#inner.run(argumentsValue, cwd);
    this.calls.push(`git ${argumentsValue.join(" ")} -> ${result.returncode}`);
    return result;
  }
}

// 只让 `git worktree add` 失败，其他命令照常执行，用来观察 reserved 的保留证据。
class FailingAddGitRunner implements GitRunner {
  readonly #inner = new SubprocessGitRunner();

  async run(argumentsValue: readonly string[], cwd: string): Promise<GitCommandResult> {
    if (argumentsValue[0] === "worktree" && argumentsValue[1] === "add") {
      return { returncode: 128, stdout: "", stderr: "fatal: injected failure" };
    }
    return await this.#inner.run(argumentsValue, cwd);
  }
}

// 只让 `git status` 报错，用来观察 review reason 不泄露原始 stderr。
class FailingStatusGitRunner implements GitRunner {
  readonly #inner = new SubprocessGitRunner();

  async run(argumentsValue: readonly string[], cwd: string): Promise<GitCommandResult> {
    if (argumentsValue[0] === "status") {
      return { returncode: 128, stdout: "", stderr: "fatal: injected status failure" };
    }
    return await this.#inner.run(argumentsValue, cwd);
  }
}

// 按脚本回放模型输出，只关心 tool call 的顺序，不需要真实 OpenAI。
class ScriptedModel implements ModelClient {
  readonly requests: ModelRequest[] = [];
  readonly #replies: ModelReply[];

  constructor(replies: readonly ModelReply[]) {
    this.#replies = [...replies];
  }

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.requests.push(request);
    const reply = this.#replies.shift();
    if (reply === undefined) throw new Error("脚本模型收到了预期外的请求");
    return reply;
  }
}
```

实验 6 还要用到 `AgentRunner` 和文件工具，所以 import 里再补上几行：

```typescript
import { NodeWorkspaceFileSystem } from "../chapters/ch18/src/adapters/filesystem.js";
import { AgentRunner } from "../chapters/ch18/src/core/loop.js";
import { assistantMessage, toolCall } from "../chapters/ch18/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch18/src/core/model.js";
import type { ToolContext } from "../chapters/ch18/src/core/tools.js";
import { ToolRegistry } from "../chapters/ch18/src/core/tools.js";
import { createChapterTwoTools } from "../chapters/ch18/src/features/builtin-tools.js";
import {
  canonicalIntegrationRef,
  registerWorktreeTools,
} from "../chapters/ch18/src/features/worktrees.js";
import {
  leasedTaskToolDefinitions,
  registerTeammateLeasedTaskTools,
} from "../chapters/ch18/src/features/work-stealing.js";
```

有了这些，每个实验都是一段 `if (section("名字")) { ... }`。跑法统一：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location $TutorialCode
npm exec -- tsx scripts/probe-ch18.mts <实验名>
```

不带实验名就 11 个全跑。

### 实验 1：走一遍完整的隔离轨迹

文章开头那段 18 步轨迹就是这个实验的输出。核心部分：

```typescript
if (section("trace")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const a = await store.createTask({ subject: "重构认证模块" });
    const b = await store.createTask({ subject: "重构登录页" });

    const alice = await worktrees.createWorktree({
      taskId: a.id, name: "alice", integrationRef: "refs/heads/main",
    });
    const bob = await worktrees.createWorktree({
      taskId: b.id, name: "bob", integrationRef: "refs/heads/main",
    });

    const aliceClaim = await worktrees.claimNext("alice");
    const bobClaim = await worktrees.claimNext("bob");
    if (aliceClaim === undefined || bobClaim === undefined) throw new Error("两侧都应认领成功");

    // 用 resolve() 拿到各自该写的目录，然后往同名文件里写不同内容。
    const aliceContext = await worktrees.resolve(
      Object.freeze({ workspace: root, identity: "alice", claimToken: aliceClaim.claimToken }),
    );
    const bobContext = await worktrees.resolve(
      Object.freeze({ workspace: root, identity: "bob", claimToken: bobClaim.claimToken }),
    );
    await writeFile(join(aliceContext.workspace, "config.ts"), "// alice 的版本\n", "utf8");
    await writeFile(join(bobContext.workspace, "config.ts"), "// bob 的版本\n", "utf8");

    // alice 提交并并入 main；bob 只提交，不并入。
    await git(aliceContext.workspace, "add", "config.ts");
    await git(aliceContext.workspace, "commit", "-m", "alice: rework config");
    await git(root, "merge", "--ff-only", "wt/alice");
    await git(bobContext.workspace, "add", "config.ts");
    await git(bobContext.workspace, "commit", "-m", "bob: rework config");

    await store.completeTask(a.id, "alice", aliceClaim.claimToken);
    await store.completeTask(b.id, "bob", bobClaim.claimToken);

    // 同样的清理调用，两个不同结果。
    console.log(`remove alice → ${(await worktrees.removeWorktree(a.id)).status}`);
    const removedBob = await worktrees.removeWorktree(b.id);
    console.log(`remove bob   → ${removedBob.status}（${removedBob.reviewReason}）`);
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts trace
```

**要验证的：** 第 8-10 步三个 `config.ts` 内容互不相同；第 14 步 alice 被删掉、第 15 步 bob 落 `needs_review`；第 18 步的审计事件按 `sequence` 严格有序，bob 那条带 reason。

**顺手改个实验：** 把 `await git(root, "merge", "--ff-only", "wt/alice")` 那行注释掉再跑。现在两个都会落 `needs_review`——**清理逻辑对 alice 和 bob 没有偏心，差别只来自"有没有并入"这一个事实。**

### 实验 2：文件锁救不了这个场景

```typescript
if (section("lock")) {
  await withTempDir(async (root) => {
    // 第一组：两人共享同一个目录，写入用"锁"严格排队。
    const shared = join(root, "shared");
    await mkdir(shared, { recursive: true });
    let held = false;
    const withLock = async (owner: string, work: () => Promise<void>): Promise<void> => {
      if (held) throw new Error("锁被并发持有，这里不会发生");
      held = true;
      console.log(`  ${owner} 拿到 config.ts 的锁`);
      await work();
      held = false;
      console.log(`  ${owner} 释放锁`);
    };
    await withLock("alice", async () => {
      await writeFile(join(shared, "config.ts"), "// alice 的版本\n", "utf8");
    });
    await withLock("bob", async () => {
      await writeFile(join(shared, "config.ts"), "// bob 的版本\n", "utf8");
    });
    console.log(`共享目录最终内容: ${JSON.stringify(await readFile(join(shared, "config.ts"), "utf8"))}`);

    // 第二组：同一段写入，落在两个受管 Worktree 里。
    const repository = join(root, "repository");
    await mkdir(repository, { recursive: true });
    const { store, worktrees } = await components(repository);
    const a = await store.createTask({ subject: "alice 的活" });
    const b = await store.createTask({ subject: "bob 的活" });
    const aliceBinding = await worktrees.createWorktree({
      taskId: a.id, name: "alice", integrationRef: "refs/heads/main",
    });
    const bobBinding = await worktrees.createWorktree({
      taskId: b.id, name: "bob", integrationRef: "refs/heads/main",
    });
    await writeFile(join(repository, aliceBinding.relativePath, "config.ts"), "// alice 的版本\n", "utf8");
    await writeFile(join(repository, bobBinding.relativePath, "config.ts"), "// bob 的版本\n", "utf8");
    const commonDir = await git(join(repository, aliceBinding.relativePath), "rev-parse", "--git-common-dir");
    console.log(`两个 worktree 共享同一个对象库：${JSON.stringify(commonDir.replace(repository.replaceAll("\\", "/"), "<root>"))}`);
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts lock
```

**要验证的：** 那个 `if (held) throw` 一次都没触发（锁真的没被并发持有），但 alice 的内容依然没了。第二组三个文件三种内容，且 `--git-common-dir` 指回主仓库的 `.git`。

**顺手改个实验：** 把第一组的锁改成"排队等锁"而不是直接写——你会发现无论怎么排队，最后活下来的还是最后一个写的人。**串行化解决的是"写坏"，不是"写没"。**

### 实验 3：三条状态线各走各的

```typescript
if (section("state")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const task = await store.createTask({ subject: "三条状态线" });
    const line = async (label: string): Promise<void> => {
      const current = await store.getTask(task.id);
      let bindingText = "（无 binding）";
      try {
        bindingText = (await worktrees.store.getWorktreeBinding(task.id)).status;
      } catch {
        bindingText = "（无 binding）";
      }
      // claim token 只存在 tasks 表的列里，Task 领域对象上并没有这个字段。
      const query = await readSqlite(store.databasePath);
      const raw = query("SELECT claim_token FROM tasks")[0]?.claim_token;
      const token = typeof raw === "string" ? `…${short(raw)}` : "null";
      console.log(
        `${label.padEnd(22, " ")} task=${current.status.padEnd(11, " ")} owner=${String(current.owner).padEnd(6, " ")} token=${token.padEnd(6, " ")} binding=${bindingText}`,
      );
    };
    await line("createTask 之后");
    await worktrees.createWorktree({ taskId: task.id, name: "alice", integrationRef: "refs/heads/main" });
    await line("createWorktree 之后");
    const claim = await worktrees.claimNext("alice");
    if (claim === undefined) throw new Error("应认领成功");
    await line("claimNext 之后");
    await store.completeTask(task.id, "alice", claim.claimToken);
    await line("completeTask 之后");
    await worktrees.keepWorktree(task.id);
    await line("keepWorktree 之后");

    const query = await readSqlite(store.databasePath);
    console.log(`\ntasks 表里没有 worktree 字段：${query("SELECT * FROM tasks").map((row) => Object.keys(row).join(", "))}`);
    console.log(`binding 独立成表：${query("SELECT * FROM worktree_bindings").map((row) => Object.keys(row).join(", "))}`);
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts state
```

**要验证的：** `createWorktree 之后` 那一行，task 仍是 `pending`、owner 仍是 `null`——**建 Worktree 不推进任务。** 最后两行确认 `tasks` 表没有任何 worktree 列。

**顺手改个实验：** 把 `keepWorktree` 换成 `removeWorktree`，再打一行。你会看到 task 停在 `completed` 不动，只有 binding 从 `active` 走到 `removed`。**两条线的终点不一样。**

### 实验 4：Git 失败时 reserved 留下的证据

```typescript
if (section("reserve")) {
  await withTempDir(async (root) => {
    await gitRepository(root);
    const clock = new Clock();
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1841), claimTokenGenerator: seqId(0x1851), clock,
    });
    // 只有 `git worktree add` 会失败，其他 Git 命令都是真的。
    const worktrees = new WorktreeRuntime({
      workspace: root, store, gitRunner: new FailingAddGitRunner(), clock: () => clock.now(),
    });
    await worktrees.validateRepository();
    const task = await store.createTask({ subject: "git add 会失败" });
    try {
      await worktrees.createWorktree({
        taskId: task.id, name: "alice", integrationRef: "refs/heads/main",
      });
      console.log("不应该走到这里");
    } catch (error) {
      console.log(`createWorktree 抛出：${errorLine(error)}`);
    }
    const binding = await store.getWorktreeBinding(task.id);
    console.log(`binding 状态仍是：${binding.status}（branch_tip=${String(binding.branchTip)}）`);
    console.log(`Task 状态仍是：  ${(await store.getTask(task.id)).status}`);
    console.log(
      `审计事件：${(await store.listWorktreeEvents()).map((event) => `#${event.sequence} ${event.action}->${event.status}`).join(" ")}`,
    );
    console.log(`自动认领会不会捡到它：${String(await worktrees.claimNext("alice"))}（reserved 不是 active）`);
    const branches = await git(root, "branch", "--format=%(refname:short)");
    console.log(`Git 分支：${branches.split("\n").join(" ")}（没有留下半个 wt/alice）`);
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts reserve
```

**要验证的：** binding 停在 `reserved` 且 `branch_tip=null`；任务还是 `pending`；审计表里只有 `#1 reserve->reserved` 一条（没有 `create`）；`claimNext` 返回 `undefined`；`git branch` 里只有 `main`。**一个 `reserved` 记录 + 干净的 Git，比"没记录 + 一个孤儿分支"好得多。**

**顺手改个实验：** 把 `FailingAddGitRunner` 里拦截的命令换成 `rev-parse`，让基线解析就失败。这次连 binding 都不会有——因为 `reserveWorktree` 排在解析之后…… 你去看一眼代码顺序，验证一下自己的猜测对不对。

### 实验 5：审计表是 append-only

```typescript
if (section("audit")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const task = await store.createTask({ subject: "审计与状态同事务" });
    await worktrees.createWorktree({ taskId: task.id, name: "alice", integrationRef: "refs/heads/main" });
    const claim = await worktrees.claimNext("alice");
    if (claim === undefined) throw new Error("应认领成功");
    await store.completeTask(task.id, "alice", claim.claimToken);
    await worktrees.keepWorktree(task.id);

    const events = await store.listWorktreeEvents();
    console.log("worktree_events（append-only）：");
    for (const event of events) {
      console.log(
        `  #${event.sequence} ${event.action.padEnd(13, " ")} status=${event.status.padEnd(12, " ")} branch_tip=${event.branchTip === null ? "null" : event.branchTip.slice(0, 7)} at=${event.createdAtUtc.toISOString()}`,
      );
    }

    // 绕开运行时，直接对 SQLite 下手：两条 trigger 会挡住。
    const query = await readSqlite(store.databasePath);
    for (const sql of [
      "UPDATE worktree_events SET action = 'remove' WHERE sequence = 1",
      "DELETE FROM worktree_events WHERE sequence = 1",
    ]) {
      try {
        query(sql);
        console.log(`${sql} → 竟然成功了`);
      } catch (error) {
        console.log(`${sql}\n  → ${errorLine(error)}`);
      }
    }
    console.log(`\nCHECK 约束也挡住非法组合，试着手写一个 needs_review 但不给理由：`);
    try {
      query("UPDATE worktree_bindings SET status = 'needs_review', review_reason = NULL WHERE name = 'alice'");
      console.log("  → 竟然成功了");
    } catch (error) {
      console.log(`  → ${errorLine(error)}`);
    }
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts audit
```

**要验证的：** 三条事件 `reserve → create → keep`；`UPDATE` 和 `DELETE` 都被 trigger 拒绝，报的是 `worktree_events is append-only`；手写非法组合被 CHECK 约束拒绝。

**顺手改个实验：** 试试 `INSERT INTO worktree_events` 手工插一条假事件。**它会成功。** trigger 只挡 UPDATE 和 DELETE，不挡 INSERT——这是刻意的，append-only 的定义就是"只能追加"。想防伪造得靠别的手段（比如哈希链），那超出本章范围。

### 实验 6：同一条回复里 claim 再写文件

这个实验用 `ScriptedModel` 回放一次真实的两工具回复，看 `resolve()` 在每次调用前分别算出了什么：

```typescript
if (section("routing")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const task = await store.createTask({ subject: "同一回复里 claim 再写文件" });
    const binding = await worktrees.createWorktree({
      taskId: task.id, name: "alice", integrationRef: "refs/heads/main",
    });

    const tools = createChapterTwoTools(
      { run: async () => ({ output: "", exitCode: 0, timedOut: false, truncated: false }) },
      new NodeWorkspaceFileSystem(),
    );
    registerTeammateLeasedTaskTools(tools, store, worktrees);
    registerWorktreeTools(tools, worktrees);

    // 一条回复里两个 tool call：先 claim_task，再 write_file。
    const seen: string[] = [];
    const model = new ScriptedModel([
      {
        message: assistantMessage(null, [
          toolCall("c1", "claim_task", JSON.stringify({ task_id: task.id })),
          toolCall("w1", "write_file", JSON.stringify({ path: "config.ts", content: "// alice\n" })),
        ]),
        finishReason: "tool_calls",
      },
      { message: assistantMessage("done"), finishReason: "stop" },
    ]);
    const runner = new AgentRunner({
      model, tools, systemPrompt: "probe", workspace: root, identity: "alice",
      // 包一层，把每次解析出来的 cwd 记下来。
      toolContextProvider: {
        workspaceRoot: root,
        resolve: async (context: ToolContext) => {
          const resolved = await worktrees.resolve(context);
          seen.push(resolved.workspace.replace(root, "<root>"));
          return resolved;
        },
      },
    });

    const result = await runner.run("claim then write");
    console.log(`两次工具调用解析出的 cwd：\n  claim_task → ${seen[0]}\n  write_file → ${seen[1]}`);
    console.log(`worktree 里的 config.ts：${JSON.stringify(await readFile(join(root, binding.relativePath, "config.ts"), "utf8"))}`);
    console.log(`主目录 config.ts：      ${JSON.stringify(await readFile(join(root, "config.ts"), "utf8"))}`);
    await runner.close();
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts routing
```

**要验证的：** `claim_task` 解析到 `<root>`（那时还没有 claim），`write_file` 解析到 `<root>\.agent_tutorial\worktrees\alice`。文件真的落在 Worktree 里，主目录的 `config.ts` 还是 `export const value = 0;`。

**顺手改个实验：** 把两个 tool call 的顺序交换（先 `write_file` 再 `claim_task`）。写入会落在主目录——因为那一刻确实还没有 claim。**这不是 bug，这是"按调用时刻的真实状态解析"的必然结果。**

### 实验 7：九种 claim 情形，只有一种放行

```typescript
if (section("failclosed")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const bound = await store.createTask({ subject: "已绑定" });
    await worktrees.createWorktree({ taskId: bound.id, name: "alice", integrationRef: "refs/heads/main" });
    const claim = await worktrees.claimNext("alice");
    if (claim === undefined) throw new Error("应认领成功");

    const attempt = async (
      label: string,
      context: Partial<ToolContext> & { readonly identity: string },
    ): Promise<void> => {
      try {
        const resolved = await worktrees.resolve(
          Object.freeze({ workspace: root, ...context }) as ToolContext,
        );
        console.log(`${label.padEnd(26, " ")} → ${resolved.workspace.replace(root, "<root>")}`);
      } catch (error) {
        console.log(`${label.padEnd(26, " ")} → ${errorLine(error)}`);
      }
    };

    await attempt("正确 identity + token", { identity: "alice", claimToken: claim.claimToken });
    await attempt("错 identity", { identity: "bob", claimToken: claim.claimToken });
    await attempt("错 task_id", {
      identity: "alice", taskId: "00000000-0000-4000-8000-000000009999", claimToken: claim.claimToken,
    });
    await attempt("没生成过的 token", {
      identity: "alice", claimToken: "00000000-0000-4000-8000-000000009999",
    });
    await attempt("格式错误的 token", { identity: "alice", claimToken: "not-a-token" });
    await attempt("错 worktree 名", {
      identity: "alice", claimToken: claim.claimToken, worktreeName: "bob",
    });
    await attempt("完全没有 claim", { identity: "alice" });
    await attempt("只给 task_id 不给 token", { identity: "alice", taskId: bound.id });

    // 任务完成之后，旧 token 就不再是有效路由凭据了。
    await store.completeTask(bound.id, "alice", claim.claimToken);
    await attempt("完成后再用旧 token", { identity: "alice", claimToken: claim.claimToken });
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts failclosed
```

**要验证的：** 九行里只有第一行返回了 Worktree 路径，第七行（完全没有 claim）返回 `<root>`，其余七行全是错误。特别看"完成后再用旧 token"——完成之后 token 立刻失效。

**顺手改个实验：** 把 `attempt("错 identity", ...)` 里的 identity 从 `"bob"` 改成 `"Alice"`（大写 A）。它照样会失败，因为 owner 比对是精确字符串比较。**大小写不敏感的身份判断是一类经典漏洞，这里没有开这个后门。**

### 实验 8：自动认领只碰已绑定的任务

```typescript
if (section("auto")) {
  await withTempDir(async (root) => {
    const { store, worktrees, workStealing } = await components(root);
    // 未绑定的任务先建，所以它在任务板上排在前面。
    const unbound = await store.createTask({ subject: "没有 worktree 的任务" });
    const bound = await store.createTask({ subject: "有 worktree 的任务" });
    await worktrees.createWorktree({ taskId: bound.id, name: "alice", integrationRef: "refs/heads/main" });
    console.log(`任务板顺序：${(await store.listTasks()).map((task) => `${task.subject}(${task.status})`).join(" / ")}`);
    const first = await workStealing.claimNext("alice");
    console.log(`  第一次 claimNext → ${first === undefined ? "undefined" : first.task.subject}`);
    console.log(`  未绑定任务状态   → ${(await store.getTask(unbound.id)).status}`);
    const second = await workStealing.claimNext("bob");
    console.log(`  第二次 claimNext（bob）→ ${String(second)}（没有第二个 active binding）`);
    if (first !== undefined) {
      console.log(`\n自动认领 prompt 长这样：\n${workStealing.renderClaimPrompt(first)}`);
    }
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts auto
```

**要验证的：** 排在前面的未绑定任务被跳过，状态保持 `pending`；第二次认领返回 `undefined`。

**顺手改个实验：** 给 `unbound` 也建一个叫 `bob` 的 Worktree，再跑。两次认领都会成功，各拿一条。**"有地盘才干活"这条规则一旦满足，工作窃取就恢复成第 17 章的样子。**

### 实验 9：三侧工具面和 wire format

```typescript
if (section("tools")) {
  await withTempDir(async (root) => {
    const { store, worktrees } = await components(root);
    const names = (registry: ToolRegistry): string => registry.names.join(", ");

    const lead = createChapterTwoTools(
      { run: async () => ({ output: "", exitCode: 0, timedOut: false, truncated: false }) },
      new NodeWorkspaceFileSystem(),
    );
    for (const definition of leasedTaskToolDefinitions(store, worktrees)) lead.register(definition);
    registerWorktreeTools(lead, worktrees);
    console.log(`Lead：     ${names(lead)}`);

    const teammate = createChapterTwoTools(
      { run: async () => ({ output: "", exitCode: 0, timedOut: false, truncated: false }) },
      new NodeWorkspaceFileSystem(),
    );
    registerTeammateLeasedTaskTools(teammate, store, worktrees);
    console.log(`Teammate： ${names(teammate)}`);

    console.log(`\n三个 Worktree 工具的 effect 分类：`);
    for (const definition of worktrees.leadToolDefinitions) {
      console.log(`  ${definition.name.padEnd(16, " ")} effect=${definition.effect}`);
    }
    console.log(`\nLead 有 create_task 吗：        ${lead.names.includes("create_task")}`);
    console.log(`Teammate 有 create_task 吗：    ${teammate.names.includes("create_task")}`);
    console.log(`Teammate 有 create_worktree 吗：${teammate.names.includes("create_worktree")}`);

    // 模型真正看到的返回值：create_worktree 的 wire format。
    const task = await store.createTask({ subject: "看一眼工具返回的 JSON" });
    const [create] = worktrees.leadToolDefinitions;
    const result = await create.handler(
      { task_id: task.id, name: "alice", integration_ref: "refs/heads/main" },
      Object.freeze({ workspace: root, identity: "lead" }),
    );
    console.log(`\ncreate_worktree 返回给模型的内容（isError=${result.isError}）：`);
    console.log(JSON.stringify(JSON.parse(result.content), null, 2).replace(/^/gmu, "  "));

    // 同名再建一次：唯一约束在 SQLite 层挡住，工具返回结构化错误而不是抛异常。
    const duplicate = await store.createTask({ subject: "重名" });
    const conflict = await create.handler(
      { task_id: duplicate.id, name: "alice", integration_ref: "refs/heads/main" },
      Object.freeze({ workspace: root, identity: "lead" }),
    );
    console.log(`\n同名再建一次 → isError=${conflict.isError} errorCode=${String(conflict.errorCode)}`);
    console.log(`  ${conflict.content}`);
  });
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts tools
```

**要验证的：** 两侧差量正好是 `create_task` 加三个 Worktree 工具；三个 effect 分类和前面表格一致；JSON 里 `branch_tip === baseline_commit`；重名返回 `isError=true` 而不是抛出。

**顺手改个实验：** 把重名那次的 `name` 改成 `"bob"` 但 `task_id` 保持不变（还是第一个任务）。还是会失败——因为 `task_id` 上有 PRIMARY KEY。**四个唯一约束各挡一类冲突，换哪个都躲不过。**

### 实验 10：清理的十条命令和三条失败路径

这个实验前面已经贴过输出，代码结构是"一个成功场景 + 一个三场景循环"：

```typescript
if (section("cleanup")) {
  // 场景 1：worktree 里的提交已经并入 main，清理链路应当完整走完并留下 removed。
  await withTempDir(async (root) => {
    await gitRepository(root);
    const clock = new Clock();
    const store = new SqliteTaskStore(root, {
      idGenerator: seqId(0x1801), claimTokenGenerator: seqId(0x1811), clock, leaseDurationMs: 60_000,
    });
    const recorder = new RecordingGitRunner();
    const worktrees = new WorktreeRuntime({
      workspace: root, store, gitRunner: recorder, clock: () => clock.now(),
    });
    await worktrees.validateRepository();
    const task = await store.createTask({ subject: "已合并，可以删" });
    const binding = await worktrees.createWorktree({
      taskId: task.id, name: "alice", integrationRef: "refs/heads/main",
    });
    const claim = await worktrees.claimNext("alice");
    if (claim === undefined) throw new Error("应认领成功");
    const worktreePath = join(root, binding.relativePath);
    await writeFile(join(worktreePath, "config.ts"), "export const value = 1;\n", "utf8");
    await git(worktreePath, "commit", "-am", "alice: bump value");
    const aliceTip = await git(worktreePath, "rev-parse", "HEAD");
    await git(root, "merge", "--ff-only", binding.branch);
    await store.completeTask(task.id, "alice", claim.claimToken);

    // 只录清理阶段的命令，前面准备工作的不算。
    recorder.calls.length = 0;
    const removed = await worktrees.removeWorktree(task.id);
    for (const call of recorder.calls) console.log(`  ${call}`);
    console.log(`branch_tip 与 alice 分支最后一次提交一致：${removed.branchTip === aliceTip}`);
    console.log(`main 上还有 alice 的提交吗：${JSON.stringify(await readFile(join(root, "config.ts"), "utf8"))}`);
  });
```

三条失败路径用同一段代码跑，只改三个布尔开关：

```typescript
  // 场景 2：没合并 / 有脏改动 / git status 报错，三条都必须落在 needs_review。
  const scenarios = [
    { label: "有未提交改动", dirty: true, merge: true, failStatus: false },
    { label: "没并入 main", dirty: false, merge: false, failStatus: false },
    { label: "git status 自己报错", dirty: false, merge: true, failStatus: true },
  ] as const;
  for (const scenario of scenarios) {
    await withTempDir(async (root) => {
      // …… 同样的装配，只有 gitRunner 换成 FailingStatusGitRunner
      const worktrees = new WorktreeRuntime({
        workspace: root, store,
        gitRunner: scenario.failStatus ? new FailingStatusGitRunner() : new SubprocessGitRunner(),
        clock: () => clock.now(),
      });
      // …… 建任务、建 worktree、认领、写文件
      if (!scenario.dirty) await git(worktreePath, "commit", "-am", "alice: bump value");
      if (scenario.merge && !scenario.dirty) await git(root, "merge", "--ff-only", binding.branch);
      await store.completeTask(task.id, "alice", claim.claimToken);

      const result = await worktrees.removeWorktree(task.id);
      console.log(`\n${scenario.label} → status=${result.status}`);
      console.log(`  review_reason: ${String(result.reviewReason)}`);
      console.log(`  目录还在：${(await git(root, "worktree", "list", "--porcelain")).includes(binding.relativePath.replaceAll("\\", "/"))}`);
      console.log(`  分支还在：${(await git(root, "branch", "--list", binding.branch)).length > 0}`);
    });
  }
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts cleanup
```

**要验证的：** 成功路径的十条命令没有一条带 `--force` 或 `-D`；三条失败路径的 reason 各不相同，且目录和分支都还在。

**顺手改个实验：** 在场景 1 里把 `git merge --ff-only` 改成 `git merge --squash` 再 `git commit`。清理会落 `needs_review`——squash 产生的是新 commit，原提交不是 `main` 的祖先。**这是保守判断带来的假警报，你现在知道它为什么会出现，也知道该怎么处理（人工确认后手工清理）。**

### 实验 11：非 Git 目录和 ref 校验

```typescript
if (section("guard")) {
  // 非 Git 目录：状态创建之前就必须失败，且不留下 binding。
  await withTempDir(async (root) => {
    const plain = join(root, "not-a-repo");
    await mkdir(plain, { recursive: true });
    const store = new SqliteTaskStore(plain, { idGenerator: seqId(0x1801) });
    const worktrees = new WorktreeRuntime({
      workspace: plain, store, gitRunner: new SubprocessGitRunner(),
    });
    try {
      await worktrees.validateRepository();
      console.log("非 Git 目录 → 竟然通过了");
    } catch (error) {
      console.log(`非 Git 目录 → ${errorLine(error)}`);
    }
    const task = await store.createTask({ subject: "在非仓库里建 worktree" });
    try {
      await worktrees.createWorktree({
        taskId: task.id, name: "alice", integrationRef: "refs/heads/main",
      });
      console.log("createWorktree → 竟然通过了");
    } catch (error) {
      console.log(`createWorktree → ${errorLine(error)}`);
    }
    const query = await readSqlite(store.databasePath);
    console.log(`worktree_bindings 行数：${String(query("SELECT COUNT(*) AS n FROM worktree_bindings")[0]?.["n"])}`);
    console.log(`worktree_events 行数：  ${String(query("SELECT COUNT(*) AS n FROM worktree_events")[0]?.["n"])}`);

    // 子目录也不行：workspace 必须正好是仓库根。
    await gitRepository(root);
    const nested = join(root, "src");
    await mkdir(nested, { recursive: true });
    const nestedRuntime = new WorktreeRuntime({
      workspace: nested,
      store: new SqliteTaskStore(nested, { idGenerator: seqId(0x1801) }),
      gitRunner: new SubprocessGitRunner(),
    });
    try {
      await nestedRuntime.validateRepository();
      console.log(`仓库子目录 → 竟然通过了`);
    } catch (error) {
      console.log(`仓库子目录 → ${errorLine(error)}`);
    }
  });

  // 九个候选 ref，看哪些能过、哪些被拒。
  console.log(`\ncanonicalIntegrationRef 的输入校验：`);
  for (const candidate of [
    "refs/heads/main", "refs/remotes/origin/main", "main", "HEAD",
    "refs/heads/../../etc/passwd", "refs/heads/main@{1}", "refs/heads/feature branch",
    "refs/heads/main/", " refs/heads/main",
  ]) {
    try {
      console.log(`  ${JSON.stringify(candidate).padEnd(34, " ")} → ${canonicalIntegrationRef(candidate)}`);
    } catch (error) {
      console.log(`  ${JSON.stringify(candidate).padEnd(34, " ")} → ${errorLine(error)}`);
    }
  }
}
```

```powershell
npm exec -- tsx scripts/probe-ch18.mts guard
```

**要验证的：** 四个失败点都报 `WorktreeRepositoryError`；`worktree_bindings` 和 `worktree_events` 都是 0 行；九个 ref 里只有前两个通过。

**顺手改个实验：** 试试 `"refs/tags/v1.0"`。它会通过校验（前缀合法、字符合法），但如果这个 tag 不存在，`git rev-parse --verify` 会失败。**格式校验和存在性校验是两道独立的关，本章两道都设了。**

### 实验做完清理

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Remove-Item "$TutorialCode\scripts\probe-ch18.mts"
```

实验用的临时目录由 `withTempDir` 的 `finally` 负责删掉，不需要手工清理。如果某次实验中途被 Ctrl+C 打断，可能有残留：

```powershell
Get-ChildItem $env:TEMP -Directory -Filter 'probe-ch18-*'
```

看一眼确认没有你想留的东西，再删。

---

## 这章刻意没有做什么

列清楚，你读代码时就不会到处找这些东西。

**没有把 worktree 字段塞进 Task。** 没有 `tasks.worktree` 列，没有 JSON blob，没有兼容层。实验 3 的最后两行验证过。

**没有进程级 cwd 切换。** 没有 `process.chdir()`，没有线程局部的 `wt_ctx` 字典，没有让模型在 prompt 里手动 `cd`。工作目录只通过 `ToolContextProvider.resolve()` 逐次解析。

**没有"未绑定就回落主目录"。** 声明了 claim 但 claim 无效 → 报错。这是实验 7 的九行输出。唯一回落主目录的情况是**完全没有声明任何 claim**——那是 Lead 的正常状态。

**没有 `@{push}`、`git branch -D`、`git worktree remove --force`。** 一个都没有。实验 10 录下的十条命令可以逐条核对。

**没有自动 merge、cherry-pick、push 或冲突解决。** 运行时只做隔离和证明。"代码怎么合"是人的决定，不是运行时的决定。

**没有 Git 与 SQLite 的跨系统 exactly-once。** 这做不到，也没假装做到。做法是把顺序排好（先记录意图，再产生副作用），让每种失败都留下可恢复的证据。

**没有后台清扫任务。** 没有"超过 N 天自动删除"。删除只由 Lead 显式发起。一个跑了半天没人管的 Worktree 会一直留着——**这比"某天早上发现改动没了"好得多。**

**没有 lease 续租，也没有 heartbeat。** 这是第 17 章的边界，本章没有改。

**没有 MCP 动态工具发现。** 工具面在组合根里写死，Lead 比 Teammate 多哪些是启动时就定好的。这是第 19 章的内容。

**符号链接防御有代码，但测试覆盖有限。** `#registeredWorktreePath` 用 `realpath` 解析后比对预期路径，能挡住路径被符号链接替换的情况。但 Windows 上普通用户创建符号链接需要额外权限，所以测试里没有真实的符号链接场景。**这段防御代码是真的，但你不能说它被验证过。**

这些不做项是本章边界的一部分。第 18 章要守住的最小闭环只有四个词：**目录隔离、逐调用路由、失效即失败、证明式清理。**

---

## 与 Claude Code 的差异

分清"我们简化了"和"我们比它更严"这两类差异，对理解取舍很有帮助。

### 我们简化了的地方

**隔离靠 resolve，它靠 chdir。** Claude Code 的 `EnterWorktree` 用 `process.chdir()` 切换会话级进程目录；`AgentTool` 的 worktree isolation 用 `cwdOverridePath` 包住子 agent。我们不动进程 cwd，只在每次工具调用前解析。代价是每个工具 handler 都必须老实用 `context.workspace` 而不是 `process.cwd()`——这是一条约定，不是编译期保证。

**没有运行期锁定。** CC 会在 agent 运行期间执行 `git worktree lock`，防止别的进程把正在用的 Worktree 删掉。我们没做。多个 Agent 进程同时操作同一个仓库时，这是个真实的缺口。

**没有清扫周期。** CC 有 `cleanupPeriodDays`，会自动清理无改动或未推送提交的临时 worktree。我们只有显式删除。

**Worktree 和会话的关系更简单。** CC 用 `PersistedWorktreeSession` 关联 worktree 和 transcript，session 里不含 `taskId`。我们用一张 `worktree_bindings` 表通过 `task_id` 关联，关系更直接，但也更死——一个任务一个 Worktree，不能一个 Worktree 跑多个任务。

**基线来源更死。** CC 的 `git worktree add -B` 默认基于 `origin/<defaultBranch>`，会自动挑默认分支。我们要求调用方显式给 `integration_ref`，且必须是 `refs/heads/*` 或 `refs/remotes/*`。少了便利，多了确定性。

### 我们比教学版更严的地方

**清理没有 force 逃生门。** CC 的 remove 检查未提交改动，但允许 `discard_changes=true` 绕过。我们没有这个参数。八条证明全过才删，否则 `needs_review`。

**认领和 Worktree 绑在一起。** CC 教学版用 `wt_ctx["path"]` 字典切 cwd，真实 CC 没有 task-worktree 绑定的认领路由。我们让手动 `claim_task`、自动 `claim_next`、`complete_task` 共用同一个 `TaskClaimService`，认领时必须有 active binding。

**同一条回复内的 claim 能立即生效。** `executionScope` + WeakMap 让"claim 完马上写文件"落到正确目录（实验 6）。这不是 CC 的机制，是本章为了让"一条回复里做完一件事"可行而加的。

**审计是数据库强制的。** `worktree_events` 有 trigger 挡 UPDATE 和 DELETE，`needs_review` 和 `review_reason` 有双向 CHECK 约束。CC 的 worktree 状态写在 transcript 里，靠代码约定维护。

**基线是不可变 commit。** `git rev-parse --verify <ref>^{commit}` 先把 ref 解析成 40 位 hex 再建 Worktree，避免"ref 在两次读取之间被人改了"。

一句话：**CC 真实源码比我们完整，CC 教学版比我们松。我们挑的是"能在离线测试里被断言住的最小严格集合"。**

---

## 从 ai-agent-book 学到什么：文件系统隔离与证明式清理

ai-agent-book 第 10 章把共享文件系统的冲突拆成两类：**文件级写入冲突**（两个人改同一个文件）和**跨文件语义冲突**（各改各的文件，但组合起来不能工作）。它的结论是：乐观锁只能处理第一类；Coding Agent 并行改代码库时，主流做法是给每个 Agent 独立分支或 Worktree，把冲突推迟到合并点。

本章走的正是后一条路。不是给 `write_file` 加逐文件版本号，而是让 claim 绑定受管 Worktree——在"谁拥有任务"之外，再建立"这次工具写在哪里"的边界。

它的[实验 10-4](ai-agent-book/chapter10/parallel-web-research/README.md) 还展示了同构 worker 的收束方式：每个 worker 有独立 Playwright Chromium context；第一个 `target_found` 在 `asyncio.Lock` 下只结算一次，只广播一次 `terminate`；losing worker 在安全点取消、ack 并关闭资源。验收 manifest 审计上下文创建/关闭计数、ack 集合和实测耗时。

本章把"独立资源、单次结算、安全点清理、证据审计"这四条翻译到文件系统上：

| ai-agent-book 的设计 | 本章的做法 | 为什么这么选 |
| --- | --- | --- |
| 冲突分文件级写入和跨文件语义两类 | claim 不解决文件覆盖，active binding 把每个任务路由到独立 Worktree，语义冲突留给 merge/review | 两类冲突需要两种机制，混在一起会两头都不彻底 |
| 乐观锁只保护同一文件的版本一致性 | SQLite 锁只保护 claim 和状态迁移；文件隔离覆盖整个受管目录，不做逐文件版本号 | 逐文件版本号在"改了 40 个文件"的重构面前没有意义 |
| 每个 worker 独立浏览器 context | 每个 active claim 独立 Git Worktree + 逐调用解析的 cwd | 资源隔离比资源共享 + 加锁更容易证明正确 |
| 第一个成功在锁下 settle once | `BEGIN IMMEDIATE` + 条件 UPDATE + token 历史，一次 claim/完成只成功一次 | 沿用第 17 章已验证的手法，不另起一套 |
| terminate 后等安全点 ack 与资源关闭 | remove 前依次证明 completed / registered / clean / integrated，失败转 `needs_review` | "清理"和"确认可以清理"是两件事 |
| 一个网站失败不影响其他 worker | 一个 Worktree 脏或未集成不阻塞其他 binding，独立保留待审查 | 失败要局部化，不能让一个待审查拖住整条流水线 |
| manifest 审计 context 创建/关闭与实测耗时 | 真实临时 Git 仓库测试 + append-only 事件表 | 断言"发生了什么"比断言"没报错"有用 |
| 并行搜索需要级联终止 | 不做 terminate 广播，删除只由 Lead 显式发起 | 自动清扫误删未集成改动的代价太高 |

最后一行是本章和 ai-agent-book 唯一的方向性分歧。并行搜索里"找到答案就停掉其他人"是对的，因为其他 worker 的中间结果没有价值。但并行改代码里，每个 Agent 的中间结果都是劳动成果——**宁可留一堆待审查的目录，也不要自动删掉任何人的工作。**

---

## 前后版本的变化

| 维度 | 第 17 章 | 第 18 章 |
| --- | --- | --- |
| 任务所有权 | SQLite claim token + 半开 lease | 完整保留，没改 |
| 文件目录 | 所有执行者共享主 workspace | active claim 路由到受管 Worktree |
| Task schema | Task DAG + token 历史 | **不加** worktree 字段 |
| 新持久状态 | Task、dependency、token history | 加 `worktree_bindings` + append-only `worktree_events` |
| 自动认领 | 所有 ready Task | 只认领有 active binding 的 ready Task |
| 工具上下文 | 固定 workspace | 每次调用前重新解析可信 cwd |
| Lead 工具面 | 无 Worktree 管理工具 | 恰好多三个：`create_worktree` / `keep_worktree` / `remove_worktree` |
| 清理证明 | 不适用 | completed + registered + clean + explicitly integrated |

代码上的实际差量（`diff chapters/ch17/src chapters/ch18/src`）：

| 文件 | 变化 | 内容 |
| --- | --- | --- |
| `features/worktrees.ts` | 新增 989 行 | 本章全部核心逻辑 |
| `adapters/git.ts` | 新增 74 行 | Git 子进程适配层 |
| `chapters/ch18.ts` | 新增 5 行 | 固定入口 |
| `adapters/task-sqlite.ts` | 改 526 行 | 两张新表、DDL、trigger、五个迁移方法、`claimNextBound` |
| `core/loop.ts` | 改 129 行 | `ToolContextProvider`、`executionScope`、`#resolveToolContext` |
| `features/teammates.ts` | 改 109 行 | 把 `claimToken` 传进 Runner |
| `bootstrap.ts` | 改 57 行 | 三个引用相等校验 + 三处工具注册 |
| `cli.ts` | 改 46 行 | 装配 `WorktreeRuntime`、前置 `validateRepository()` |
| `core/tools.ts` | 改 39 行 | `ToolContext` 加四个可选字段 |
| `core/profiles.ts` | 改 31 行 | `worktree` capability + `P18` |
| `features/subagents.ts` | 改 27 行 | 透传 `toolContextProvider` 和 `claimToken` |

任务 DAG、Mailbox、typed Protocol、计划门控、权限、Hook、后台任务和恢复逻辑都没有另起一套实现。本章只是把"谁能执行任务"和"这次工具应该在哪里执行"接到同一个边界上。

其中 `features/subagents.ts` 那 27 行值得单独提一句：

```typescript
const result =
  context.claimToken === undefined
    ? await runner.run(input.description)
    : await runner.run(input.description, {
        idempotencyKey: context.claimToken,
        claimToken: context.claimToken,
      });
```

**Subagent 继承父级的 claim token。** 一个队友认领了任务，在 Worktree 里干活时委派了一个 Subagent——那个 Subagent 应该写在同一个 Worktree 里，不是主目录。这行代码就是保证这件事的。

---

## 配套代码索引

本章源码在 `F:\笔记\Agent实操\code\chapters\ch18\`。

### 本章新增

| 文件 | 行数 | 职责 |
| --- | --- | --- |
| [src/features/worktrees.ts](code/chapters/ch18/src/features/worktrees.ts) | 989 | 领域对象、状态枚举、schema 校验、`WorktreeRuntime`（claim service + context provider）、三个工具定义 |
| [src/adapters/git.ts](code/chapters/ch18/src/adapters/git.ts) | 74 | `SubprocessGitRunner`：`execFile` + argv 数组 + 超时 + 三分类错误 |
| [src/chapters/ch18.ts](code/chapters/ch18/src/chapters/ch18.ts) | 5 | 固定 P18 profile 的章节入口 |

### 本章改动

| 文件 | 行数 | 本章相关的部分 |
| --- | --- | --- |
| [src/adapters/task-sqlite.ts](code/chapters/ch18/src/adapters/task-sqlite.ts) | 1175 | `worktree_bindings` / `worktree_events` DDL 与 trigger（735-780）、`reserveWorktree`、四个迁移方法、`#transitionWorktree`、`claimNextBound`、`lookupClaim` |
| [src/core/loop.ts](code/chapters/ch18/src/core/loop.ts) | 789 | `ToolContextProvider` 接口、每轮 `executionScope`（~355）、`#executeTool` 顺序（528-570）、`#resolveToolContext`（621-644） |
| [src/core/tools.ts](code/chapters/ch18/src/core/tools.ts) | 269 | `ToolContext` 的 `taskId` / `claimToken` / `worktreeName` / `executionScope` 四个可选字段 |
| [src/features/work-stealing.ts](code/chapters/ch18/src/features/work-stealing.ts) | 417 | `claimService.store === store` 校验、`renderClaimPrompt`、Teammate 工具注册 |
| [src/features/teammates.ts](code/chapters/ch18/src/features/teammates.ts) | 871 | 自动认领后把 `claimToken` 同时当幂等键和路由凭据 |
| [src/features/subagents.ts](code/chapters/ch18/src/features/subagents.ts) | — | 透传 `toolContextProvider`，继承父级 `claimToken` |
| [src/bootstrap.ts](code/chapters/ch18/src/bootstrap.ts) | 468 | 三个引用相等校验（102-119）、Lead/Teammate/Subagent 三侧工具注册 |
| [src/cli.ts](code/chapters/ch18/src/cli.ts) | 357 | `execute()` 装配顺序（127-215）、状态创建前 `validateRepository()` |
| [src/core/profiles.ts](code/chapters/ch18/src/core/profiles.ts) | 326 | `worktree` capability、`P18` |

### 测试

| 文件 | 行数 | 覆盖什么 |
| --- | --- | --- |
| [tests/ch18-worktrees.test.ts](code/chapters/ch18/tests/ch18-worktrees.test.ts) | 481 | 真实临时 Git 仓库：隔离、reserve 保留、清理八证明、审计 append-only、非 Git 拒绝 |
| [tests/ch18-bootstrap.test.ts](code/chapters/ch18/tests/ch18-bootstrap.test.ts) | 391 | 三侧工具面、引用相等校验、provider 注入 |
| [tests/ch18-loop-workspace.test.ts](code/chapters/ch18/tests/ch18-loop-workspace.test.ts) | 218 | provider 解析顺序、逃逸变成 `tool_context_error`、`executionScope` 生命周期 |
| [tests/ch18-claim-routing.test.ts](code/chapters/ch18/tests/ch18-claim-routing.test.ts) | 203 | 同回复 claim→write、跨回复 token 恢复、失效 claim 失败 |
| [tests/ch18-entry.test.ts](code/chapters/ch18/tests/ch18-entry.test.ts) | 53 | 两个入口的参数与退出码 |

### 读代码的建议顺序

```text
1. src/features/worktrees.ts   先看枚举和领域对象（前 200 行左右）
        │                       —— 搞清 reserved/active/kept/needs_review/removed 五个状态
        ▼
2. 同文件的 createWorktree      五步流程，注意 reserve 在 Git 之前
        │
        ▼
3. 同文件的 resolve             五道检查，注意每道检查失败都是 throw
        │
        ▼
4. 同文件的 removeWorktree      八条证明，注意每条失败都转 needs_review
        │
        ▼
5. src/adapters/task-sqlite.ts  跳到 735 行看 DDL，再看 #transitionWorktree
        │                       —— 约束在数据库里，不在应用层
        ▼
6. src/core/loop.ts             528-570 的 #executeTool，看 resolve 排在哪
        │
        ▼
7. src/cli.ts + src/bootstrap.ts  最后看装配，理解为什么必须共享实例
```

**先读状态机，再读流程，最后读装配。** 反过来读会一直在问"这个状态是什么意思"。

---

## 运行与验证

在 `F:\笔记\Agent实操\code` 下依次跑五道门：

```powershell
$TutorialCode = 'F:\笔记\Agent实操\code'
Set-Location $TutorialCode
npm run test:ch18
npm run typecheck
npm run lint
npm run format:check
npm run build
```

`npm run test:ch18` 会跑第 1 到第 18 章的全部测试（不只是 ch18 的），因为要确认新增能力没有破坏前面的任何一章：

```text
Test Files  59 passed (59)
     Tests  394 passed (394)
  Duration  15.85s
```

输出里有三行 stderr 是**测试刻意触发的**，不是失败：

```text
运行失败: workspace is not a Git repository root
运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

只想看本章那 26 个测试：

```powershell
npm exec -- vitest run --root . chapters/ch18/tests/ch18-worktrees.test.ts chapters/ch18/tests/ch18-loop-workspace.test.ts chapters/ch18/tests/ch18-claim-routing.test.ts chapters/ch18/tests/ch18-bootstrap.test.ts chapters/ch18/tests/ch18-entry.test.ts
```

```text
Test Files  5 passed (5)
     Tests  26 passed (26)
```

这 26 个测试里，几个特别值得你打开看的：

| 测试名 | 它锁住了什么 |
| --- | --- |
| `isolates same-named files across two active worktrees` | 两个真实 Worktree 里的同名文件互不影响 |
| `keeps a reservation and pending task when Git worktree creation fails` | Git 失败时 `reserved` 和 `pending` 都留着 |
| `keeps dirty worktree for review and only removes an integrated clean worktree` | 脏的留、干净且已并入的才删 |
| `turns Git status failures and process errors into needs_review without stderr` | Git 报错不泄露 stderr |
| `marks a mismatched registered worktree root for review` | 目录存在但不是受管 Worktree → review |
| `rolls back a binding transition when audit insertion fails` | 审计写失败时状态迁移一起回滚 |
| `keeps worktree audit append-only for UPDATE` / `DELETE` | 两条 trigger 真的生效 |
| `rejects a non-Git workspace before creating runtime state` | 非 Git 目录在建状态前失败 |
| `same reply claim then write resolves the new workspace before the second tool` | 同回复 claim→write 路由正确 |
| `provider escape becomes a paired tool_context_error before the handler` | provider 越界 → 成对错误，handler 零调用 |
| `execution scope is shared within one run and replaced before the next run` | scope 的生命周期正好是一轮 |
| `keeps a kept binding as kept when removal fails` | `kept` 不会被清理失败覆盖成 `needs_review` |

跑完之后确认一下工作区是干净的：

```powershell
git status --porcelain=v1
```

如果实验脚本还在，删掉它（前面"实验做完清理"那一节）。

---

## 本章小结

### 三句话版本

1. **任务所有权和文件系统隔离是两个问题。** 第 17 章解决了前者（claim token + lease），本章解决后者（受管 Git Worktree）。加锁只能让覆盖变得有序，不能让覆盖消失。
2. **工作目录不是全局状态，是每次工具调用的解析结果。** `ToolContextProvider.resolve()` 在 schema 校验之后、Hook 和权限之前跑，同一个 Agent 的两次调用可以落在两个目录。
3. **删除不是命令，是证明。** 八条证明全过才删，任何一条不过就转 `needs_review`，把目录、分支和原因一起留下。

### 一定要记住的 9 条

1. Task、Claim、Binding 是三条独立状态线，生命周期不同，不能合表。
2. 创建先在 SQLite 记 `reserved`，再调 Git；Git 成功后再凭读回证据转 `active`。**孤儿副作用比多余状态记录严重得多。**
3. `integration_ref` 先 `rev-parse --verify <ref>^{commit}` 解析成不可变 40 位 hex，再建 Worktree。
4. 手动认领、自动窃取、完成任务共用同一个 `TaskClaimService`；组合根用引用相等强制这一点。
5. 每次工具调用重新解析可信上下文；resolve 排在 Hook 和权限**之前**，失败时 handler 零调用。
6. 失效 claim 必须失败，绝不静默回落主目录。只有"完全没声明 claim"才用主目录。
7. `remove` 只接受 clean、登记一致、且分支顶端已包含在指定 integration ref 里的 Worktree；没有 `--force`，没有 `-D`。
8. 清理失败一律 `needs_review` 并保留现场；已经是 `kept` 或 `needs_review` 的不重复迁移。
9. binding 迁移和审计事件在同一个 SQLite 事务里提交；trigger 挡住对审计表的 UPDATE 和 DELETE。

### 一张图回顾整条链路

```text
  Lead                    SQLite                      Git 文件系统
   │                        │                              │
   │ create_worktree ──────▶│                              │
   │                        │ ① canonical 校验             │
   │                        │ ② rev-parse ref ────────────▶│ 解析成 40 位 commit
   │                        │ ③ INSERT reserved            │
   │                        │    + reserve 事件（同事务）   │
   │                        │ ④ worktree add ─────────────▶│ 建目录 + wt/{name} 分支
   │                        │ ⑤ 读回 branch --show-current ◀│ 证明分支正确
   │                        │    UPDATE → active           │
   │                        │    + create 事件（同事务）    │
   │◀───── binding JSON ────│                              │
   │                        │                              │
  Teammate                  │                              │
   │ claimNext ────────────▶│ 只扫 active binding          │
   │◀──── claim + token ────│                              │
   │                        │                              │
   │ write_file             │                              │
   │  └─ resolve(ctx) ─────▶│ 查 claim → 查 binding        │
   │◀── worktree 路径 ──────│  （active 才放行）            │
   │  └─ handler ──────────────────────────────────────────▶│ 写进 .agent_tutorial/
   │                        │                              │   worktrees/{name}/
   │ complete_task ────────▶│ task → completed             │
   │                        │ binding 仍是 active           │
   │                        │                              │
  Lead                      │                              │
   │ remove_worktree ──────▶│ 八条证明 ───────────────────▶│ registered? clean?
   │                        │                              │ integrated?
   │                        │ 全过 → detach ──────────────▶│ switch --detach
   │                        │       branch -d ────────────▶│（非强制）
   │                        │       worktree remove ──────▶│（非强制）
   │                        │ UPDATE → removed             │
   │                        │ 任一条不过 → needs_review     │ 目录和分支都留着
   │◀───── binding JSON ────│  + review_reason             │
```

### 检查你是否真的读懂了

不看前面的内容，试着回答：

1. 为什么给 `write_file` 加文件锁解决不了 Alice 和 Bob 的问题？
2. `git worktree add` 失败之后，binding 处于什么状态？任务处于什么状态？自动认领会不会捡到它？
3. `resolve()` 为什么排在 PreToolUse Hook **之前**？如果排在之后会发生什么？
4. 一个 Agent 在同一条回复里先 `claim_task` 再 `write_file`，第二个调用怎么知道该写哪个目录？如果这两个调用分在两条回复里，靠什么？
5. `remove_worktree` 判断"已集成"用的是哪个 Git 命令？为什么不用 `git branch --merged`？
6. `git status` 返回非零和返回空字符串，为什么必须区别对待？
7. 一个 binding 已经是 `kept` 了，再调 `remove_worktree` 且失败，审计表会多几条记录？为什么？
8. 为什么 `validateRepository()` 要求 workspace 正好是仓库根，子目录不行？
9. 组合根为什么用 `!==` 做引用相等判断，而不是比较配置内容？

答不上来的，回去跑对应的实验——每个问题都有一个实验能直接给出答案。

---

到这里，Alice 和 Bob 不仅不会认领同一项任务，也不会在同一片文件空间里互相覆盖。任务所有权和文件写入位置成了两条各自可证明的状态线，失败时留下的是可恢复的证据，不是"改动没了"这种无从追查的结果。

下一章解决另一种静态边界：工具面到现在还是在组合根里写死的，启动之后不能变。**外部能力怎么在运行期通过 MCP 进入和退出当前工具池？** 这是第 19 章的内容。
