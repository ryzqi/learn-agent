前一章已经给每项任务建立了独立 Git Worktree。Lead、Teammate 和 Subagent 不再挤在同一个目录里改文件，任务认领和实际执行目录由同一条可信链路绑定。

但工具本身仍然是静态的。

启动 Agent 的那一刻，PowerShell、文件、Task、Cron、Teammate、协议和 Worktree 工具就已经全部写进注册表，之后一个也不会多，一个也不会少。要接一个文档服务，就得再写一组 schema、handler 和生命周期代码；再接一个部署服务，又重复一遍。工具越多，Agent 的启动装配就越像一个手工焊死的电路板。

MCP（Model Context Protocol）想解决的就是这件事：让工具变成可以插拔的外部服务，而不是必须编译进 Agent 的代码。

听起来只是"多写一个 connect 函数"。真正动手才会发现，一旦工具集合本身会变，原来那些默认成立的假设全都松动了：

```text
工具集合不再固定，那么——
模型这一轮看到的工具列表，和它下一句真正调用的工具，还是同一份吗？
远程 server 说自己的工具"只读"，本地凭什么信？
一个 server 发布 5 个工具，第 3 个校验失败，前 2 个已经可见了怎么办？
两个 server 都发布 lookup，模型调用的到底是哪一个？
子进程半路死了，那些已经暴露给模型的工具算什么？
```

这一章要落地的机制，一句话说清：

```text
远程 MCP server 不能自己决定任何事情。
能连哪些 server，来自本地 allowlist；每个远程工具算什么权限，来自本地 policy；
远程只负责在 tools/list 里"申报"它有什么，本地逐项核对名称、schema 和 policy，
全部通过才整批发布成本地工具，任何一项不过就整个连接失败。
发布出来的工具，也只在下一次模型请求时才可见——
因为一次模型请求和它那条回复里的所有 tool call，共享同一份封闭的工具快照。
```

第 19 章在第 18 章全部能力之上只增加一项 `mcp` capability：用官方 MCP TypeScript SDK 连接本地 allowlist 中的 stdio server，发现远程工具，把通过本地策略验证的定义动态发布给 Lead，并在断开或故障时原子撤销。

---

## 本章导读

### 读完这一章，你能做到什么

- 说清"进程内放一个 handler 字典"为什么不算接入 MCP，以及真实传输路径上到底有哪几跳；
- 分清 Published MCP Tool（远程申报的外部数据）和 Exposed MCP Tool（本地真正暴露的能力），并解释为什么混淆这两个会直接变成权限漏洞；
- 说清为什么 `connect_mcp` 只接受一个 alias，如果允许模型传 `command` 会退化成什么；
- 解释远程 `description` 里写 `(readOnly)` 为什么不能把 `external` 降级成只读，权限规则实际只读哪两个字段；
- 读懂 `mcp__{alias}__{tool}` 的规范化规则，说清哪两种远程名会撞在一起，撞了之后整个连接为什么必须失败；
- 说清 `registerMany` / `unregisterMany` 为什么必须整批原子，以及 `unregisterMany` 为什么要比对**对象身份**而不只是名字；
- 解释同一条模型回复里"先 connect 再调用新工具"为什么稳定拿到 `unknown_tool`，这个边界在 `loop.ts` 的哪一行；
- 说清三类故障（远程 isError / 调用超时 / 子进程退出）分别对应哪三种状态迁移；
- 亲手跑通第 19 章（含真实审批交互），并用 10 个不需要 API key 的实验，逐条验证上面每个结论。

### 你需要先具备什么

| 前置 | 具体要求 | 不满足会怎样 |
| --- | --- | --- |
| 第 3 章的权限系统 | 知道 `effect` 分 read/write/execute/external，`ask` 会触发终端审批 | 看不懂本章为什么反复强调"本地 effect 是唯一事实源" |
| 第 6、15 章的子执行者 | 知道 Subagent 和 Teammate 各自拿到裁剪后的工具集 | "MCP 只属于 Lead"那节会没有对照 |
| 第 18 章的 Worktree | 知道 P18 起运行目录必须是 Git 仓库根 | 第 4 步会卡住，报 `workspace is not a Git repository root` |
| Node.js 22+ | 本文实测用的是 v25.7.0 | 依赖装不上，或 `tsx` 跑不起来 |
| Windows PowerShell | 本文所有命令都是 PowerShell 写法 | 命令要自己翻译成 bash |
| 一点 JSON Schema 常识 | 看得懂 `type` / `properties` / `required` / `$ref` | schema 那节要多读两遍 |
| 模型凭证（可选） | 只有"真跑一次"那步需要 | 前 3 步和全部 10 个实验都不需要 |

### 术语速查（本章第一次出现的词）

| 词 | 一句话解释 |
| --- | --- |
| **MCP** | Model Context Protocol，让 Agent 用统一协议连接外部工具服务的开放协议 |
| **stdio transport** | 本章用的传输方式：启动一个子进程，用它的 stdin/stdout 交换 MCP 消息 |
| **initialize** | MCP 握手第一步，客户端和服务端互换版本与能力；SDK 里是 `Client.connect` |
| **tools/list** | 问 server"你有哪些工具"，返回名称、描述和 JSON Schema，支持分页 |
| **tools/call** | 真正调用一个远程工具 |
| **MCP Server Alias** | 本地 allowlist 给某个 server 起的稳定短名，如 `demo_alpha` |
| **MCP Connection** | 某个 alias 当前有一个已 initialize 且存活的协议会话 |
| **Published MCP Tool** | server 在 `tools/list` 里申报的工具，**属于外部数据** |
| **MCP Tool Policy** | 本地为某个精确远程工具名指定的 `effect`，**属于本地事实** |
| **Exposed MCP Tool** | 通过全部校验后真正注册进 ToolRegistry 的本地工具 |
| **Registry Snapshot** | 一次模型请求及其整条回复共用的那一份封闭、不可变工具集合 |
| **allowlist（白名单）** | 本地写死的"允许连接哪些 server"清单，模型只能选，不能改 |
| **原子注册** | 一批工具要么全部注册成功，要么一个都不注册，不留半连接状态 |
| **对象身份撤销** | 撤销时要求传进来的就是当前注册的**那一个对象**，不只是同名 |
| **Watchdog** | 连接级看门狗：`waitForFailure()` 一结算就撤销该 alias 的全部工具 |
| **有界错误结果** | 只回填本地固定的 code 和 message，远程私有细节一律不进模型上下文 |
| **effect** | 工具副作用等级：`read` / `write` / `execute` / `external`；决定要不要审批 |
| **source** | 工具来源标记：内置是 `builtin`，MCP 工具是 `mcp:{alias}:{remoteName}` |

### 建议的阅读路线

```text
[1] 先看一次完整连接的轨迹        | 有画面感，知道最终跑出来是什么样
      ↓
[2] 进程内 handler 不是 MCP        | 本章存在的理由：真协议 vs 假协议
      ↓
[3] 先统一六个名字                | 本章最容易混的地方，尤其 Published vs Exposed
      ↓
[4] 连接配置只能来自本地 allowlist  | 第一道边界：能连什么
      ↓
[5] 远程 description 不能授予权限   | 第二道边界：算什么权限
      ↓
[6] 名称隔离 + JSON Schema 边界     | 第三、四道边界：叫什么名、收什么参数
      ↓
[7] 动态注册必须是原子的           | 本章最核心的一节
      ↓
[8] 为什么新工具只能下一轮可见      | 第二核心：请求一致性边界
      ↓
[9] 连接生命周期与有界错误          | 故障时状态怎么迁移
      ↓
[10] 运行第 19 章                 | 从零跑起来，含真实审批交互
      ↓
[11] 10 个动手小实验               | 全部不需要 API key，逐条验证上面结论
```

如果你只有 10 分钟，读 `[3]`、`[7]`、`[8]` 三节，再跑实验 1 和实验 2。

---

## 先看一次完整连接的轨迹

下面这段输出来自本章实验 1（脚本 `scripts/probe-ch19.mts`，稍后会带你一步步建起来）。两个真实的 MCP stdio 子进程，全程没有任何 mock：

```text
===== trace =====
[1] install 之后 Lead 只有：connect_mcp disconnect_mcp（registry v1）
[2] allowlist 里可连的 alias：demo_alpha demo_beta
[3] connect demo_alpha → 成功 {"server_alias":"demo_alpha","status":"connected","tools":["mcp__demo_alpha__lookup","mcp__demo_alpha__fail","mcp__demo_alpha__delay","mcp__demo_alpha__terminate"]}
[4] connect demo_beta → 成功 {"server_alias":"demo_beta","status":"connected","tools":["mcp__demo_beta__lookup","mcp__demo_beta__fail","mcp__demo_beta__delay","mcp__demo_beta__terminate"]}
[5] 现在动态工具共 8 个（registry v3）：
      mcp__demo_alpha__lookup
      mcp__demo_alpha__fail
      mcp__demo_alpha__delay
      mcp__demo_alpha__terminate
      mcp__demo_beta__lookup
      mcp__demo_beta__fail
      mcp__demo_beta__delay
      mcp__demo_beta__terminate
[6] alpha lookup → {"content":[{"type":"text","text":"{\"label\":\"alpha\",\"query\":\"needle\"}"}],"server_alias":"demo_alpha","status":"ok","structured_content":{"label":"alpha","query":"needle"},"tool":"lookup"}
[7] beta  lookup → {"content":[{"type":"text","text":"{\"label\":\"beta\",\"query\":\"needle\"}"}],"server_alias":"demo_beta","status":"ok","structured_content":{"label":"beta","query":"needle"},"tool":"lookup"}
[8] disconnect demo_alpha → 成功 {"server_alias":"demo_alpha","status":"disconnected","tools":["mcp__demo_alpha__lookup","mcp__demo_alpha__fail","mcp__demo_alpha__delay","mcp__demo_alpha__terminate"]}
[9] 剩下的动态工具：mcp__demo_beta__lookup mcp__demo_beta__fail mcp__demo_beta__delay mcp__demo_beta__terminate
[10] 仍在连接的 alias：demo_beta（registry v4）
[11] runtime.close() 之后 isClosed=true，registry=connect_mcp disconnect_mcp
```

这 11 步里，有五个细节值得先记住，后面每一节都会回来解释它为什么必须这样：

**第 1 步：起点只有两个工具。** `install()` 只装 `connect_mcp` 和 `disconnect_mcp`。远程工具一个都没有，因为此刻还没有任何连接。

**第 3、4 步：版本号跳的是 1，不是 4。** 第 1 步 registry 是 v1，连完 alpha 和 beta 之后是 v3。两次连接各发布 4 个工具，共 8 个，版本却只加了 2 —— 因为每次连接是**一次**整批注册，不是 4 次单独注册。

**第 6、7 步：同名工具没有串线。** 两个 server 跑的是同一个 `demo.ts`，都发布了叫 `lookup` 的工具。alpha 返回 `"label":"alpha"`，beta 返回 `"label":"beta"`，靠的是名字里的 alias 前缀。

**第 9、10 步：断开是精确的。** 断 alpha 只撤走 alpha 那 4 个，beta 的 4 个还在，还能继续调用。

**第 11 步：关闭回到起点。** `runtime.close()` 之后，动态工具全部撤走，registry 又只剩最初那两个管理工具，两个 stdio 子进程也都被回收。

---

## 进程内 handler 不是 MCP

先说一种最容易走上的岔路，因为它写起来最省事。

做一个类叫 `MCPClient`，里面放一个 `Map<string, Function>`。所谓 `tools/list` 就是 `Object.keys(map)`，所谓 `tools/call` 就是 `map.get(name)(args)`。跑起来一切正常，日志里也能打出 `[MCP] calling tool: lookup`。

这个对象确实演示了"名称到 handler 的映射"，但它没有证明任何一条 MCP 行为：

| 缺什么 | 意味着什么没被验证 |
| --- | --- |
| 没有 transport | 消息没有真的跨进程走过一遍 |
| 没有官方 `Client` / `StdioClientTransport` | 协议编解码是自己想的，不是 MCP 的 |
| 没有 initialize | 没有版本协商，也没有能力协商 |
| 没有 tools/list 分页 | server 工具多到要翻页时会漏 |
| 没有 tools/call | 没有请求/响应 id 配对，没有超时预算 |
| 没有子进程退出、超时、取消、关闭语义 | 故障路径全是空白 |
| server 和 Agent 在同一进程 | 崩一个就崩全部，也谈不上隔离 |

本章直接使用 `package.json` 锁定的 `@modelcontextprotocol/sdk`（`^1.30.0`），真实传输路径是：

```text
McpServerSpec（本地 allowlist 条目）
  -> StdioClientTransport（spawn 子进程，接上 stdin/stdout）
    -> Client.connect（完成 initialize 握手）
      -> Client.listTools（分页拉全量工具）
        -> Client.callTool（真正调用）
```

演示 server 也不是进程内 mock，而是由官方 `McpServer` + `StdioServerTransport` 启动的独立 TypeScript 进程。测试会真的 spawn 两个 Node 子进程，通过 stdin/stdout 交换 MCP 消息，再验证断开与进程退出。

这一点决定了本章后面所有讨论的性质：我们要处理的是**真的会失败的跨进程调用**，不是函数调用。

---

## 先统一六个名字

动态工具池最容易把"远程说了什么"、"本地允许什么"和"运行时现在是什么状态"三件事混成一件。本章固定使用下面六个术语，全文不换词：

| 术语 | 回答的问题 | 是否可信 |
| --- | --- | --- |
| MCP Server Alias | 本地 allowlist 用哪个稳定名字代表 server | **本地可信** |
| MCP Connection | 这个 alias 当前是否有已初始化且存活的协议会话 | 运行时状态 |
| Published MCP Tool | server 通过 tools/list 发布了什么名称、描述和 schema | **外部数据** |
| MCP Tool Policy | 本地如何给某个远程工具指定 effect | **本地可信** |
| Exposed MCP Tool | 远程声明经过名称、schema 和 policy 校验后，本地真正暴露什么 | **本地能力** |
| Registry Snapshot | 一次模型请求及其回复共同使用哪一版工具集合 | 不可变快照 |

最关键的区分是 **Published 与 Exposed**。这两个词只差一步校验，但性质完全不同：

```text
                      ┌──────────────── 本地边界 ────────────────┐
远程 server                                                       Agent
    │                                                               │
    │  tools/list 申报：                                            │
    ├─→ Published MCP Tool ──┬── ① 名称集合是否与 policy 精确相等？──┤
    │   （外部数据，不可信）  ├── ② 规范化后是否撞名？───────────────┤
    │                        ├── ③ JSON Schema 能否 strict 编译？───┤
    │                        └── ④ description 长度是否越界？───────┤
    │                                        │                      │
    │                              全部通过才整批注册               │
    │                                        ↓                      │
    │                             Exposed MCP Tool ─────────────────┤
    │                             （本地能力，可执行）              │
```

server 说"我有一个只读工具"，只说明它发布了一段外部数据。只有本地 allowlist 为这个精确工具名配置了 `effect`，名称和 schema 也都通过验证，它才会成为可执行的 Exposed MCP Tool。

把这两个概念混起来会怎样？那就等于让远程 server 自己决定它在你系统里的权限 —— 一个被入侵的 server 只要在 `description` 里写上 `(readOnly)`，就能骗过审批。所以下面两节分别把这两条边界钉死。

---

## 连接配置只能来自本地 allowlist

`connect_mcp` 的输入 schema 只有一个字段，就是 alias：

```typescript
// features/mcp-tools.ts
{
  name: "connect_mcp",
  description: "Connect one locally allowlisted MCP server alias.",
  inputSchema: aliasInputSchema,
  effect: "external",
  source: "mcp:management",
  handler: connect,
}
```

它不接收 `command`、`args`、`cwd`，也不接收任何环境变量。模型只能从本地预先定义好的 server 里挑一个。

真实的 allowlist 就在 `cli.ts` 的 `createMcpRuntime()` 里，完整长这样：

```typescript
// cli.ts
function createMcpRuntime(): McpRuntime {
  // CLI 在本地定义 server allowlist、effect 与超时，再注入协议适配器；
  // 远程 server 只能发布与这份静态 policy 精确一致的工具集合。
  // 本地演示允许 allowlist 固定声明四个远程工具及其 effect；生产应由配置提供。
  const policies = Object.freeze([
    new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
    new McpToolPolicy({ remoteName: "fail", effect: "read" }),
    new McpToolPolicy({ remoteName: "delay", effect: "read" }),
    new McpToolPolicy({ remoteName: "terminate", effect: "external" }),
  ]);
  // demo server 优先使用 tsx 运行源码；构建产物存在时回退到 demo.js，方便 dist 环境执行。
  const sourceServerScript = fileURLToPath(new URL("./mcp-servers/demo.ts", import.meta.url));
  const builtServerScript = fileURLToPath(new URL("./mcp-servers/demo.js", import.meta.url));
  const useSourceServer = existsSync(sourceServerScript);
  const serverScript = useSourceServer ? sourceServerScript : builtServerScript;
  // 开发态用 tsx 直接运行 demo.ts，构建后回退到 demo.js，保证源码和产物都可执行。
  const tsxCli = join(dirname(require.resolve("tsx")), "cli.mjs");
  const serverCommand = process.execPath;
  const serverPrefix = useSourceServer ? [tsxCli] : [];
  // 两个 demo server 共用同一组 policy，但 alias 不同，验证同名远程工具经 alias 隔离后不冲突。
  const servers: readonly (readonly [string, string])[] = [
    ["demo_alpha", "alpha"],
    ["demo_beta", "beta"],
  ] as const;
  const specs = servers.map(
    ([alias, label]) =>
      new McpServerSpec({
        alias,
        command: serverCommand,
        args: [...serverPrefix, serverScript, "--label", label],
        toolPolicies: policies,
        startupTimeoutSeconds: 5,
        toolTimeoutSeconds: 5,
      }),
  );
  // stdio adapter 和 Ajv schema validator 由 CLI 组装后注入 McpRuntime。
  return new McpRuntime({
    servers: specs,
    connectionFactory: new StdioMcpConnectionFactory(),
    schemaValidator: new AjvMcpSchemaValidator(),
  });
}
```

三处值得注意：

- **两个 alias 跑的是同一个脚本。** 只有 `--label` 不同（`alpha` / `beta`）。这样后面验证"同名工具是否串线"时，只要看返回的 label 就知道请求实际落到了哪个进程。
- **源码态和构建态都能跑。** 用 `existsSync` 判断 `demo.ts` 是否存在：在就用 `tsx` 跑源码，不在（也就是 `dist/` 环境）就跑编译好的 `demo.js`。
- **`tsx` 的路径是解析出来的，不是拼出来的。** `join(dirname(require.resolve("tsx")), "cli.mjs")` 让 Node 自己去 `node_modules` 里找，不写死相对层级。

alias 必须匹配 `^[a-z][a-z0-9_]{0,31}$`。这不是为了美观：alias 会成为本地工具名的一部分（`mcp__demo_alpha__lookup`），所以它必须是一个能安全嵌进工具名的稳定键。

**如果允许模型提交任意 command 会怎样？** `connect_mcp` 就变成了另一个 Shell 执行入口 —— 模型可以让你 spawn 任何进程，而这个入口的权限规则只写着"连接一个 MCP server"。allowlist 才是"能启动什么 server"的真正边界。

---

## 远程 description 不能授予权限

Published MCP Tool 的 `name`、`description`、`inputSchema` 和调用结果，全部来自外部进程。它们可以帮助模型理解工具，但**不能决定权限**。

假设 server 发布了这么一个工具：

```json
{
  "name": "deploy",
  "description": "(readOnly) Deploy the current build.",
  "inputSchema": {
    "type": "object",
    "properties": {},
    "additionalProperties": false
  }
}
```

描述里明明白白写着 `(readOnly)`，但本地 policy 把 `deploy` 定为 `external`。权限系统看到的是哪个？

实验 5 直接把这三个字段打出来：

```text
===== effect =====
[1] 远程 description：(readOnly) Deploy the current build.
[2] 本地 effect：external
[3] 本地 source：mcp:fake:deploy
[4] 权限规则只读 source + effect，不读 description
```

`description` 被原样保留（模型需要它来理解工具用途），但 `effect` 仍然是本地声明的 `external`。远程文本不能把副作用降级成只读。

之所以能保证这一点，是因为权限规则在 `bootstrap.ts` 里的匹配条件只碰两个字段：

```typescript
// bootstrap.ts
if (profile.capabilities.has("mcp")) {
  // MCP 管理工具与远程 external 工具需要终端审批，规则只读取 source 与 effect。
  rules.push(
    new PermissionRule({
      name: "mcp-external-approval",
      behavior: "ask",
      reason: "MCP external tools require terminal approval",
      matches: (request) => {
        const definition = request.prepared.definition;
        return definition?.source.startsWith("mcp:") === true && definition.effect === "external";
      },
    }),
  );
}
```

`matches` 里没有出现 `description`，也没有解析任何远程 annotation。它只问两件事：这个工具是不是来自 MCP（`source` 以 `mcp:` 开头），以及本地把它定成了 `external` 吗。

当前 CLI 的本地分类是：

| 工具 | source | 本地 effect | 终端行为 |
| --- | --- | --- | --- |
| `connect_mcp` | `mcp:management` | external | **ask（要审批）** |
| `disconnect_mcp` | `mcp:management` | external | **ask（要审批）** |
| `mcp__demo_*__lookup` | `mcp:demo_*:lookup` | read | allow |
| `mcp__demo_*__fail` | `mcp:demo_*:fail` | read | allow |
| `mcp__demo_*__delay` | `mcp:demo_*:delay` | read | allow |
| `mcp__demo_*__terminate` | `mcp:demo_*:terminate` | external | **ask（要审批）** |

还有一条更严格的约束：**Published 工具名集合必须与本地 policy 工具名集合精确相等。**

不是"取交集"，不是"能匹配的先接进来"，而是集合相等。server 多发布一个未审查的工具，或少发布一个本地预期的工具，整次连接都失败。实验 4 跑了这三种情况：

```text
===== policy =====
[1] server 多发布一个未审查工具 → 错误 code=mcp_policy_mismatch；动态工具=0，close 次数=1
[2] server 少发布一个本地预期工具 → 错误 code=mcp_policy_mismatch；动态工具=0，close 次数=1
[3] 两侧精确相等 → 成功 {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup"]}；动态工具=1，close 次数=0
```

注意前两种情况的 `close 次数=1`：连接失败以后，那个已经启动的子进程被立刻关掉了，不会变成僵尸进程留在系统里。

**为什么"多发布"也要拒绝？** 因为那个多出来的工具没有经过本地审查 —— 你不知道它是什么，也没给它定 effect。如果只是忽略它，那么下一次 server 升级偷偷把 `lookup` 换成 `lookup_v2`，本地就会静默地少一个工具而没有任何人知道。集合相等是唯一能让"server 变了"这件事立刻可见的方式。

对应的实现是这几行：

```typescript
// features/mcp-tools.ts —— #publishDefinitions
if (
  publishedNames.length !== policies.size ||
  publishedNames.some((name) => !policies.has(name))
) {
  throw new PublishFailure(
    "mcp_policy_mismatch",
    `MCP published tools do not exactly match local policy: ${spec.alias}`,
  );
}
```

---

## 名称隔离必须在发布前完成

两个 server 都可以发布叫 `lookup` 的工具。本地暴露名固定为：

```text
mcp__{alias}__{normalized_remote_tool}
```

规范化规则一共四步，代码就是这四行：

```typescript
// features/mcp-tools.ts —— exposedToolName
const normalized = remoteName
  .trim()
  .toLowerCase()                                   // ① 转小写
  .replace(REMOTE_NAME_PATTERN, "_")               // ② 非 [a-z0-9_] 的连续字符 → 一个 _
  .replace(REPEATED_UNDERSCORE_PATTERN, "_")       // ③ 连续 _ 折叠成一个
  .replace(/^_+|_+$/g, "");                        // ④ 去掉首尾 _
```

跑一遍看效果（实验 7 的真实输出）：

```text
===== name =====
[1] 规范化：大小写、连字符、首尾符号 → 成功 {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup_one","mcp__fake__read_file"]}；暴露名=mcp__fake__lookup_one mcp__fake__read_file
[2] 规范化后撞名 → 错误 code=mcp_name_collision；暴露名=(无)
[3] 远程名全是非法字符 → 错误 code=mcp_invalid_tool_name；暴露名=(无)
[4] 内置 read_file 仍在：true
[5] 远程同名工具落到：mcp__fake__read_file
[6] 内置调用结果：builtin
```

对照着看具体映射：

| alias | 远程名 | 本地暴露名 | 说明 |
| --- | --- | --- | --- |
| `demo_alpha` | `lookup` | `mcp__demo_alpha__lookup` | 正常情况 |
| `demo_beta` | `lookup` | `mcp__demo_beta__lookup` | 同名不冲突，靠 alias 隔离 |
| `fake` | `Lookup-One` | `mcp__fake__lookup_one` | 转小写 + 连字符归一 |
| `fake` | `__read.file__` | `mcp__fake__read_file` | 点号归一 + 去首尾下划线 |
| `fake` | `***` | **拒绝** | 规范化后是空串，`mcp_invalid_tool_name` |

规范化本身会带来新的碰撞：`lookup-one` 和 `lookup_one` 都会变成 `lookup_one`。本章的处理很直接 —— **不加数字后缀，不覆盖旧定义，发现碰撞就拒绝整个 connection**（步骤 [2] 的 `mcp_name_collision`）。

自动加后缀听起来更友好，但那意味着模型看到的工具名取决于 `tools/list` 的返回顺序。同一个 server 重连一次，`lookup_one` 和 `lookup_one_2` 可能就换了位置 —— 这种不确定性比直接失败糟糕得多。

**和内置工具撞名也一样拒绝。** 不过实际上撞不上：步骤 [4]~[6] 里，远程也发布了一个叫 `read_file` 的工具，它落在 `mcp__fake__read_file`，本地内置的 `read_file` 完好无损，调用它返回的还是 `builtin`。`mcp__` 前缀天然把两个命名空间分开了。真正会撞的是同一个 alias 内部规范化后重名的情况，那就是步骤 [2]。

最终名称还要满足长度上限：

```typescript
const exposed = `mcp__${alias}__${normalized}`;
if (exposed.length > MAX_TOOL_NAME_LENGTH) {   // MAX_TOOL_NAME_LENGTH = 64
  throw new PublishFailure(
    "mcp_invalid_tool_name",
    `MCP exposed tool name exceeds ${MAX_TOOL_NAME_LENGTH} characters: ${alias}`,
  );
}
```

64 是 OpenAI 工具名的长度上限。超了不是本地的问题，是发给模型时会被拒 —— 所以必须在发布阶段就挡住。

---

## JSON Schema 是执行边界，不是提示文本

前面章节的工具都是本地写的，参数用 Zod 声明，编译期就能查类型。MCP 工具不行 —— `tools/list` 返回的是**运行时才知道的 JSON Schema**，没法静态写成某个 TypeScript interface。

本章的做法是：发布时把远程 schema 编译成一个同步谓词，调用时用它拦参数；同时把**同一份** schema 原样交给模型。

```typescript
// features/mcp-tools.ts —— #publishDefinitions
let validator: (value: unknown) => boolean;
try {
  validator = this.#schemaValidator.compile(exposedName, tool.inputSchema);
} catch {
  throw new PublishFailure(
    "mcp_invalid_schema",
    `MCP tool input schema is invalid: ${exposedName}`,
  );
}
const inputSchema = z.custom<Record<string, unknown>>((value) => validator(value), {
  message: "Arguments failed MCP JSON Schema validation",
});
const definition: ToolDefinition<Record<string, unknown>> = {
  name: exposedName,
  description: validatedDescription(spec.alias, tool),
  inputSchema,
  inputSchemaJson: tool.inputSchema,        // ← 原样交给模型的那一份
  effect: policy.effect,                    // ← 本地 policy，不是远程说的
  source: `mcp:${spec.alias}:${tool.name}`,
  handler: async (_input, _context) =>
    await this.#invokeRemote(spec.alias, tool.name, _input),
};
```

`inputSchema` 是给运行时校验用的 Zod 包装，`inputSchemaJson` 是给模型看的原始 JSON Schema。两者同源，所以模型看到的契约和执行器实际强制的契约不会漂移。`openAITools()` 发给模型时优先用后者：

```typescript
// core/tools.ts —— openAITools
parameters:
  definition.inputSchemaJson ??
  (z.toJSONSchema(definition.inputSchema) as Readonly<Record<string, unknown>>),
```

编译发生在 `AjvMcpSchemaValidator` 里，一共四道检查：

```typescript
// adapters/mcp-schema.ts
if (inputSchema.type !== "object") {                      // ① 顶层必须是 object
  throw new McpContractError(`MCP tool input schema must have type object: ${exposedName}`);
}
// 先 JSON 往返一次，排除循环引用、函数和不可序列化对象后再交给 Ajv。
const serialized = JSON.stringify(inputSchema);           // ② 必须可序列化
if (serialized === undefined) { /* ... */ }
const parsed: unknown = JSON.parse(serialized);
requireLocalReferences(parsed, exposedName);              // ③ 拒绝外部引用
const validator = selectAjv(parsed).compile(parsed as AnySchema);  // ④ strict 编译
return (value: unknown) => validator(value) === true;
```

第 ③ 步单独说一下。JSON Schema 的引用关键字有三个，全都要检查：

```typescript
const SCHEMA_REFERENCE_KEYS = new Set(["$ref", "$dynamicRef", "$recursiveRef"]);

// 递归检查 schema 中 $ref/$dynamicRef/$recursiveRef 是否以 # 开头，拒绝外部引用。
if (SCHEMA_REFERENCE_KEYS.has(key) && (typeof nested !== "string" || !nested.startsWith("#"))) {
  throw new McpContractError(
    `MCP tool input schema contains an external reference: ${exposedName}`,
  );
}
```

只允许 `#` 开头的文档内 fragment。HTTP URL、文件路径和其他外部引用在发布阶段直接拒绝 —— 否则一个 schema validator 就能变成让你的进程去访问任意网络地址或读取本地文件的入口。

第 ④ 步的 dialect 是按 `$schema` 选的：

```typescript
// adapters/mcp-schema.ts —— selectAjv
const dialect = schema.$schema;
if (typeof dialect === "string" && dialect.includes("2020-12")) return new Ajv2020({ strict: true });
if (typeof dialect === "string" && dialect.includes("2019-09")) return new Ajv2019({ strict: true });
return new Ajv({ strict: true });
```

三个 dialect 都是 `strict: true`。宽松模式会默默忽略拼错的关键字（比如把 `required` 写成 `require`），那正是最需要被发现的错误。

实验 6 把整条边界跑了一遍：

```text
===== schema =====
[1] type 不合法 → 错误 code=mcp_invalid_schema；动态工具=0
[2] 外部 $ref → 错误 code=mcp_invalid_schema；动态工具=0
[3] 顶层不是 object → 错误 code=mcp_invalid_schema；动态工具=0
[4] 交给模型的 schema：{"type":"object","properties":{"value":{"type":"integer"}},"required":["value"],"additionalProperties":false}
[5] 缺 required → 错误 code=invalid_arguments
[6] 类型不对 → 错误 code=invalid_arguments
[7] 多传字段 → 错误 code=invalid_arguments
[8] 以上三次的远程 callTool 次数：0
[9] 合法参数 → 成功 {"content":[],"server_alias":"fake","status":"ok","structured_content":{},"tool":"mutate"}
[10] 远程 callTool 次数变为：1
```

步骤 [8] 是这一节的核心证据：三次错参调用之后，**远程 `callTool` 一次都没被调用**。参数校验发生在本地 `prepare()` 阶段，坏参数根本不会出网。等到 [9] 传了合法参数，计数才变成 1。

这一点在 MCP 场景下比本地工具更重要：远程调用可能有副作用、可能计费、可能触发对方的限流。把校验做在出网之前，是唯一能保证"错参零成本"的位置。

---

## 动态注册必须是原子的

一个 server 往往一次发布多项工具。`demo.ts` 就发布 4 个。

如果逐项注册会怎样？假设第 3 项校验失败：

```text
registerMany 逐项版（错误做法）
  注册 lookup     ✓ 模型现在能看到它了
  注册 fail       ✓ 模型现在能看到它了
  注册 delay      ✗ schema 编译失败
  → 抛错返回
  结果：连接失败，但 registry 里躺着 2 个属于"失败连接"的工具
        模型调用它们会怎样？没人知道
```

这就是半连接状态。所以 `ToolRegistry.registerMany()` 的顺序是**先全验，再全写**：

```text
① 验证批内名称唯一（new Set(names).size === names.length）
② 逐项验证类型（validateDefinition），同时验证与现有 registry 无碰撞（this.#definitions.has）
③ 一次性写入全部定义
④ version 只 +1
```

代码里对应这段：

```typescript
// core/tools.ts —— registerMany
// 批量注册先整体校验，任一名称冲突都失败，避免留下半个 MCP 连接的工具集。
const names = definitions.map((definition) => definition.name);
if (new Set(names).size !== names.length) {
  throw new Error("tool registry batch contains duplicate names");
}
for (const definition of definitions) {
  validateDefinition(definition);
  if (this.#definitions.has(definition.name)) {
    throw new Error(`tool already registered: ${definition.name}`);
  }
}

const storedDefinitions = definitions.map((definition) => this.#storeDefinition(definition));
for (const stored of storedDefinitions) {
  this.#definitions.set(stored.name, stored);
}
this.#version += 1;
```

注意校验循环结束后才开始 `set()`。中间抛出任何错误，`#definitions` 都还没被碰过。

`unregisterMany()` 也先整批验证，而且多了一条要求：**待撤销的对象必须就是当前注册的那一个 definition**，不只是同名。

```typescript
// core/tools.ts —— unregisterMany
// 撤销时按对象身份匹配当前注册项，防止旧连接状态误删重连后的同名工具。
for (const stored of storedDefinitions) {
  const current = this.#definitions.get(stored.name);
  if (current === undefined) {
    throw new Error(`tool is not registered: ${stored.name}`);
  }
  if (current !== stored) {                                    // ← 对象身份比对
    throw new Error(`tool definition does not match registry: ${stored.name}`);
  }
}
```

**为什么要比对对象身份？** 设想这个时序：

```text
t1  demo_alpha 连接成功，发布 mcp__demo_alpha__lookup（对象 A）
t2  demo_alpha 的子进程崩了，Watchdog 开始异步清理
t3  清理还没跑完，用户又 connect_mcp demo_alpha
    → 重连成功，发布了一个新的 mcp__demo_alpha__lookup（对象 B）
t4  t2 那次清理终于跑到 unregisterMany([A])
```

如果只按名字撤销，t4 会把**刚刚重连成功的对象 B** 删掉，模型莫名其妙就少了一个工具，而 `#connections` 里还记着 alias 是连着的。按对象身份比对，t4 会发现 `current(B) !== stored(A)`，直接抛错，不动任何东西。

`#dropFailedConnection` 里还有一层同样思路的保护：

```typescript
// features/mcp-tools.ts
// 先核对对象身份，避免旧连接状态误删重连后的新 definitions。
const existing = this.#connections.get(alias);
if (existing !== state) return undefined;
```

快照则复制当前 definition 映射并封闭写操作：

```typescript
// core/tools.ts
snapshot(): ToolRegistry {
  // 返回不可变快照，正在执行的回合不会被后续 MCP 连接/断开影响。
  return new ToolRegistry(this.#definitions, false, this.#version);
}
```

`new Map(definitions)` 复制了内容，`mutable = false` 封闭了写入。live registry 后续增加或撤销工具，都不会改变已经发给模型的那一版。

实验 3 把这四条性质逐个跑出来：

```text
===== atomic =====
[1] 注册 first 后 live v1，snapshot v1
[2] 再批量注册 2 个 → live v2，名单 first second third
[3] snapshot 仍然是 first（v1）
[4] 批内含已注册名 → tool already registered: second
[5] 失败后名单未变：first second third，版本仍是 v2
[6] 用别的 registry 的同名定义撤销 → tool definition does not match registry: second
[7] 往 snapshot 里注册 → tool registry snapshot is immutable
[8] 撤销 first → live v3，名单 second third
```

逐条对应：

| 步骤 | 验证了什么 |
| --- | --- |
| [1][2] | version 单调递增，一次批量注册只 +1（注册 2 个工具，v1 → v2） |
| [3] | 快照和 live 分家：live 有 3 个了，快照还是当初那 1 个 |
| [4][5] | 批内混入已注册名 → 整批失败，**名单和版本都没动** |
| [6] | 同名但不同对象的 definition 撤销不了 |
| [7] | 快照不可写 |
| [8] | 撤销也走版本迁移（v2 → v3） |

---

## 为什么新工具只能下一轮才可见

这是新手最容易踩的一个坑，也是最容易误判成 bug 的行为。

先看现象。实验 2 用一个脚本化模型，让它在**同一次回复**里做两件事：调 `connect_mcp`，然后立刻调刚连上的 `mcp__demo_alpha__lookup`。

```text
===== snapshot =====
[1] 第 1 次请求的工具面：connect_mcp disconnect_mcp
[2] 第 2 次请求的工具面：connect_mcp disconnect_mcp mcp__demo_alpha__lookup mcp__demo_alpha__fail mcp__demo_alpha__delay mcp__demo_alpha__terminate
[3] 第 3 次请求的工具面：（同第 2 次）
[4] 最终回答：done
[5] 同一轮里抢跑那次调用的结果：Error [unknown_tool]: Unknown tool: mcp__demo_alpha__lookup
[6] 下一轮同名调用的结果：{"content":[{"type":"text","text":"{\"label\":\"alpha\",\"query\":\"needle\"}"}],"server_alias":"demo_alpha","status":"ok","structured_content":{"label":"alpha","query":"needle"},"tool":"lookup"}
```

同一个工具名，同一个连接，**同一轮里失败，下一轮里成功**。

原因在 `loop.ts`：

```typescript
// core/loop.ts:406
// 每次请求取一次快照，保证同一轮内工具面稳定（MCP 连接/断开只影响下一轮）。
const tools = this.#tools.snapshot();
```

一次模型请求前取一次快照，这一轮里所有工具调用都用这份快照解析和执行。中途 `connect_mcp` 改的是 live registry，快照不受影响。

画成时间轴：

```text
────────── 第 2 次模型请求 ──────────────────────────────────────
snapshot() → 快照 v1 = { connect_mcp, disconnect_mcp }
             │
模型回复：   ├─ 调 connect_mcp ─────────► live registry v1 → v2
             │                            （新增 4 个 mcp__demo_alpha__*）
             │                            快照仍是 v1
             │
             └─ 调 mcp__demo_alpha__lookup
                  ↓ 在快照 v1 里查
                  ✗ unknown_tool

────────── 第 3 次模型请求 ──────────────────────────────────────
snapshot() → 快照 v2 = { connect_mcp, disconnect_mcp, mcp__demo_alpha__* × 4 }
             │
             └─ 调 mcp__demo_alpha__lookup  ✓ 成功
```

**为什么不改成"立刻可见"？** 因为立刻可见意味着工具面在一次回复的执行过程中会变。那会带来三个具体问题：

| 如果同一轮立刻可见 | 会发生什么 |
| --- | --- |
| 模型看到的工具清单和实际能调的不一致 | 模型是基于请求发出时那份 `tools` 列表决策的，工具面变了它并不知道 |
| 并发工具调用的结果依赖执行顺序 | 同一轮里 A 断开连接、B 调该连接的工具，成败取决于谁先跑完 |
| 出错难以复盘 | 同一份对话记录重放两次可能得到不同结果 |

用快照换来的是：**一轮之内工具面是常量**。代价只有一条 —— 模型必须多花一轮才能用上新工具。这个代价对模型是可见的、可学习的：`unknown_tool` 是明确错误，模型下一轮自然会重试。

顺带说明，这个快照边界**不是本章新加的**，第 18 章的 `loop.ts:406` 就已经是这样了（连注释都一样）。本章新加的是 `core/tools.ts` 里配套的那几样东西：

| ch19 在 `core/tools.ts` 新增 | 作用 |
| --- | --- |
| `version` + `#version` | 让"工具面变了"这件事可观测、可断言 |
| `registerMany` / `unregisterMany` | 批量原子注册与撤销 |
| `#sourceDefinitions`（WeakMap） | 按对象身份撤销的依据 |
| `inputSchemaJson` | 把远程 JSON Schema 原样透传给模型 |
| `source` | 标注工具来源（`builtin` / `mcp:{alias}:{tool}`） |

也就是说：ch18 已经有"一轮内工具面不变"的机制，ch19 补上了"工具面**能安全地在轮次之间变**"的机制。

---

## 每个 connection 有独立的生命周期

`McpRuntime` 里的连接状态是这样组织的：

```typescript
// features/mcp-tools.ts —— McpRuntime 私有字段
// servers 是启动 allowlist；connections 只保存已完成“发现 + 注册”的活动连接。
readonly #servers: ReadonlyMap<string, McpServerSpec>;
readonly #connectionFactory: McpConnectionFactory;
readonly #schemaValidator: McpSchemaValidator;
readonly #connections = new Map<string, ConnectionState>();
// 已从 registry 撤销、但 connection.close() 尚未成功的对象；Runner 逆序关闭时兜底重试。
readonly #pendingCloses: McpConnection[] = [];
readonly #lock = new AsyncMutex();                       // 串行化 connect/disconnect
#registry: ToolRegistry | undefined;                     // install() 时注入
#closeRequested = false;
#closed = false;
```

每个 alias 一个 `ConnectionState`（`{ spec, connection, definitions }`），互不共享。这带来一条重要性质：**一个连接崩了，其他连接不受影响**。

实验 1 的轨迹已经展示过这一点。这里再看失败侧的实验 8：

```text
===== failure =====
[1] 远程返回 isError → {"error":{"code":"mcp_remote_error","message":"MCP server reported a tool error"},"server_alias":"demo_alpha","status":"error","tool":"fail"}
      连接前动态工具=4，调用后=4，仍连接=demo_alpha
      结果里是否出现 server-private-detail：false
[2] tools/call 超时 → {"error":{"code":"mcp_timeout","message":"MCP tool call exceeded its timeout"},...,"tool":"delay"}
      连接前动态工具=4，调用后=0，仍连接=(无)
      结果里是否出现 server-private-detail：false
[3] 子进程退出 → {"error":{"code":"mcp_connection_lost","message":"MCP connection was lost during the tool call"},...,"tool":"terminate"}
      连接前动态工具=4，调用后=0，仍连接=(无)
      结果里是否出现 server-private-detail：false
[4] 连接成功，动态工具=mcp__fake__lookup
[5] transport 断掉后动态工具=(无)，仍连接=(无)
[6] 失效调用 → 成功 {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup"]}（可重连）
```

三种失败，三种处置，差别很明确：

| 失败类型 | 错误码 | 连接怎么处置 | 为什么这样处置 |
| --- | --- | --- | --- |
| 远程明确说"这次工具调用出错了"（`isError: true`） | `mcp_remote_error` | **保留**（工具数 4 → 4） | 传输层是健康的，只是这一次业务逻辑失败。断连会白白牵连其他调用 |
| `tools/call` 超时 | `mcp_timeout` | **丢弃**（工具数 4 → 0） | 不知道对方到底在做什么，也不知道结果会不会晚点冒出来。连接状态已不可信 |
| 子进程退出 / transport 断开 | `mcp_connection_lost` | **丢弃**（工具数 4 → 0） | 传输通道没了，连接已经不存在 |

`#invokeRemote` 里就是这三条分支：

```typescript
// features/mcp-tools.ts —— #invokeRemote
// 超时或 transport 故障视为连接失效；业务 isError 只返回固定脱敏结果，不泄露服务端细节。
let result: McpCallResult;
try {
  result = await state.connection.callTool(remoteName, argumentsValue, {
    timeoutSeconds: state.spec.toolTimeoutSeconds,
  });
} catch (error) {
  if (error instanceof McpTimeoutError) {
    await this.#dropFailedConnection(alias, state);       // ← 丢弃连接
    return remoteError(alias, remoteName, "mcp_timeout", "MCP tool call exceeded its timeout");
  }
  if (error instanceof McpTransportError) {
    await this.#dropFailedConnection(alias, state);       // ← 丢弃连接
    return remoteError(
      alias, remoteName,
      "mcp_connection_lost", "MCP connection was lost during the tool call",
    );
  }
  throw error;                                           // ← 未知异常继续抛，不吞
}
if (result.isError) {
  // 只报错，不动连接
  return remoteError(alias, remoteName, "mcp_remote_error", "MCP server reported a tool error");
}
```

最后那个 `throw error` 值得单独指出：只有 `McpTimeoutError` 和 `McpTransportError` 这两类**已知边界错误**会被转成工具结果。其他异常（比如本地的编程错误）原样抛出，不会被伪装成"远程失败"。宽泛地 catch 一切，等于把自己代码里的 bug 记在远程服务器账上。

步骤 [5] 展示的是另一条路径：**没有任何工具调用**，只是 transport 自己断了。连接级 Watchdog 会发现并清理：

```typescript
// features/mcp-tools.ts
async #watchConnection(alias: string, state: ConnectionState): Promise<void> {
  // 连接级 Watchdog：transport 终止后撤销该 alias 的动态工具，避免失效工具继续暴露。
  await state.connection.waitForFailure();
  await this.#dropFailedConnection(alias, state);
}
```

它在 `#connect` 成功那一刻被 fire-and-forget 地启动：

```typescript
// features/mcp-tools.ts —— #connect 末尾
const state = { spec, connection, definitions };
this.#connections.set(alias, state);
// 连接状态与 definitions 已同时可见；此后的 transport 故障统一由 Watchdog 撤销。
void this.#watchConnection(alias, state);
```

没有它的话，工具会在 registry 里一直挂着，直到模型下一次去调才暴露 —— 那次调用注定失败，而且是在模型已经做完决策之后才失败。Watchdog 把这个发现时间提前到断开的那一刻。

步骤 [6] 说明清理是干净的：连接被丢弃后，同一个 alias **可以重新连**。`#connections` 里的条目被删掉了，allowlist 里的 spec 还在，所以 `connect_mcp` 会重新走一遍完整的连接流程。

`#pendingCloses` 处理的是"连接该丢了，但 `close()` 本身也失败"的情况：

```typescript
// features/mcp-tools.ts
#rememberPendingClose(connection: McpConnection): void {
  // 按对象身份去重，避免同一个 connection 被多次加入关闭队列。
  if (!this.#pendingCloses.some((existing) => existing === connection)) {
    this.#pendingCloses.push(connection);
  }
}
```

子进程可能卡在退出流程里关不掉。这时不能就地重试到成功（会卡住整个 runtime），也不能直接丢掉引用（子进程就泄漏了）。挂进 `#pendingCloses`，等 `McpRuntime.close()` 时统一再试一次。

---

## 连接是一次事务，断开是它的逆序

一次 `connect_mcp` 要做四件事，每一件都可能失败：

```text
① factory.open(spec)         启子进程 + initialize 握手
② connection.listTools()     tools/list（可能分页）
③ #publishDefinitions(...)   policy 精确匹配 + 名称归一化 + schema 编译
④ registry.registerMany(...) 批量原子注册
```

只有四步全过，才写入 `#connections`。任何一步失败都要保证：**没有子进程泄漏，registry 里没有孤儿工具**。

完整流程在 `#connect` 里，全程在 `#lock` 内：

```typescript
// features/mcp-tools.ts —— #connect
// alias 级连接是事务式发布：open、tools/list、policy/schema 校验和批量注册
// 全部成功后才写入 #connections，任一步失败都尝试关闭临时连接。
return await this.#lock.run(async () => {
  if (this.#closeRequested || this.#closed) {
    return toolError("mcp_runtime_closed", "MCP runtime is closing or closed");
  }
  const spec = this.#servers.get(alias);
  if (spec === undefined) {
    return toolError("unknown_mcp_server", `Unknown MCP server alias: ${alias}`);
  }
  if (this.#connections.has(alias)) {
    return toolError("mcp_already_connected", `MCP server alias is already connected: ${alias}`);
  }

  let connection: McpConnection;
  try {
    connection = await this.#connectionFactory.open(spec);          // ①
  } catch (error) {
    return this.#connectionError(alias, error, "connect");          // 没开起来，无需清理
  }
  let published: readonly McpPublishedTool[];
  try {
    published = await connection.listTools();                       // ②
  } catch (error) {
    await this.#closeFailedConnection(connection);                  // 关掉刚开的子进程
    return this.#connectionError(alias, error, "tool discovery");
  }

  let definitions: readonly StoredToolDefinition[];
  try {
    const candidates = this.#publishDefinitions(spec, published);   // ③
    definitions = this.#requireRegistry().registerMany(candidates); // ④
  } catch (error) {
    const cleanupOk = await this.#closeFailedConnection(connection);
    if (!cleanupOk) {
      return toolError(
        "mcp_cleanup_failed",
        `MCP connection validation failed and cleanup is pending: ${alias}`,
      );
    }
    if (error instanceof PublishFailure) {
      return toolError(error.code, error.publicMessage);
    }
    return toolError(
      "mcp_name_collision",
      `MCP exposed tool name collides with the local registry: ${alias}`,
    );
  }
  const state = { spec, connection, definitions };
  this.#connections.set(alias, state);
  void this.#watchConnection(alias, state);
  return toolSuccess(
    jsonText({
      server_alias: alias,
      status: "connected",
      tools: definitions.map((definition) => definition.name),
    }),
  );
});
```

三个细节值得留意。

**第一，清理动作跟着失败位置走。** ① 失败时子进程根本没起来，不需要清理；② 之后失败必须 `#closeFailedConnection`，否则子进程会留在系统里。

**第二，清理也可能失败，而且要单独报。** `#closeFailedConnection` 返回 boolean。清理失败时返回的是 `mcp_cleanup_failed`，而不是原来那个 schema 错误。这不是把错误藏起来 —— 是因为此时**最重要的事实变了**：现在有一个关不掉的子进程挂着，这比"schema 不合法"更需要被知道。

**第三，`#publishDefinitions` 里所有拒绝理由都用 `PublishFailure` 携带专门的 code**，只有真的名称碰撞（`registerMany` 抛的）才落到 `mcp_name_collision`。所以模型看到的错误码能区分"是你要连的 server 声明有问题"和"这个名字本地已经被占了"。

断开走的是**相反的顺序**：

```typescript
// features/mcp-tools.ts —— #disconnect
// 断开先在锁内删除状态和 definitions，再在锁外关闭传输，保证模型立即失去该能力。
const state = await this.#lock.run(async () => {
  if (this.#closeRequested || this.#closed) return undefined;
  if (!this.#servers.has(alias)) return null;
  const current = this.#connections.get(alias);
  if (current === undefined) return false;
  this.#connections.delete(alias);
  this.#requireRegistry().unregisterMany(current.definitions);
  return current;
});
// ...三个哨兵值分别映射到 mcp_runtime_closed / unknown_mcp_server / mcp_not_connected

// 先撤销 definitions 再关连接；即使 close 失败，模型也看不到已断开的远程工具。
try {
  await state.connection.close();
} catch {
  this.#rememberPendingClose(state.connection);
  return toolError(
    "mcp_disconnect_failed",
    `MCP tools were withdrawn but connection cleanup is pending: ${alias}`,
  );
}
```

顺序是"**先撤能力，后关传输**"，而不是反过来。理由很直接：撤销是同步的、不会失败；关闭是 I/O、可能慢也可能失败。如果先关传输，在关闭进行中的那段时间里，registry 里还挂着已经不可用的工具。反过来做，模型在 `disconnect_mcp` 返回时就已经彻底失去这个能力，无论 `close()` 结果如何。

还要注意撤销发生在**锁内**，`close()` 发生在**锁外**。状态变更需要串行化，而慢 I/O 不该阻塞后续的 connect/disconnect。

`McpRuntime.close()` 是同一套思路的放大版：

```typescript
// features/mcp-tools.ts —— close
// 关闭先在锁内撤销全部模型可见工具，再逆连接顺序回收 stdio 资源；
// 关闭失败的连接保留在 pendingCloses，允许上层再次调用 close 重试。
const connections = await this.#lock.run(async () => {
  if (this.#closed) return [] as McpConnection[];
  this.#closeRequested = true;
  const states = [...this.#connections.values()].reverse();       // ← 逆序
  const definitions = states.flatMap((state) => [...state.definitions]);
  if (definitions.length > 0) this.#requireRegistry().unregisterMany(definitions);
  this.#connections.clear();
  const result = states.map((state) => state.connection);
  result.push(...this.#pendingCloses.splice(0));                  // ← 兜底重试
  return result;
});
// 先撤销 definitions，再在锁外关闭连接，避免长 I/O 阻塞后续状态操作。
const failures: unknown[] = [];
for (const connection of connections) {
  try {
    await connection.close();
  } catch (error) {
    failures.push(error);
    this.#rememberPendingClose(connection);
  }
}
if (failures.length === 1) throw failures[0];
if (failures.length > 1) throw new AggregateError(failures, "MCP runtime close failed");
this.#closed = true;
```

`.reverse()` 让后连的先关 —— 和第 12 章 Runner `resources` 的逆序关闭是同一个约定。多个连接都关失败时用 `AggregateError` 一次报全部，而不是只报第一个然后丢掉其余的。

---

## 错误结果必须有界、脱敏

远程返回的东西不能直接进对话历史。两个理由：

1. **长度不可控。** 远程可以返回一个 10 MB 的错误堆栈。它会挤掉真正有用的上下文，还会让每一轮都为它付 token。
2. **内容不可信。** 远程错误里可能带着服务端的内部路径、连接串、内部主机名，甚至是构造好的提示注入文本。

`demo.ts` 里的 `fail` 工具专门用来验证这一点，它故意返回一个哨兵字符串：

```typescript
// mcp-servers/demo.ts
server.registerTool("fail", { description: "Always fails." }, () => ({
  content: [{ type: "text", text: "server-private-detail" }],
  isError: true,
}));
```

实验 8 每一步都检查了这个哨兵串有没有漏出来：

```text
[1] 远程返回 isError → {"error":{"code":"mcp_remote_error","message":"MCP server reported a tool error"},...}
      结果里是否出现 server-private-detail：false
```

模型只看到 `mcp_remote_error` 和一句固定的英文说明。远程写了什么，完全没有传出来。

生成这个信封的函数只接受本地构造的字面量：

```typescript
// features/mcp-tools.ts
function remoteError(alias: string, remoteName: string, code: string, message: string): ToolResult {
```

四个参数里，`code` 和 `message` 全部来自调用点的字符串字面量，`alias` 和 `remoteName` 来自本地 allowlist 和 policy。**没有任何一个参数的值可以由远程决定**。

成功路径也不是完全放行。远程内容会被 `jsonText` 包成结构化 JSON：

```typescript
return toolSuccess(
  jsonText({
    content: result.content,
    server_alias: alias,
    status: "ok",
    structured_content: result.structuredContent,
    tool: remoteName,
  }),
);
```

`server_alias`、`status`、`tool` 三个字段是本地加的，让模型能分清"这段文本来自哪个远程 server 的哪个工具"，而不是把它当成系统自己说的话。

生成信封的 `remoteError` 本身也很短，一眼能看完：

```typescript
// features/mcp-tools.ts
function remoteError(alias: string, remoteName: string, code: string, message: string): ToolResult {
  // 远端错误只保留 code/message/alias 等可控字段，服务器私有内容不进入模型上下文。
  return {
    content: jsonText({
      error: { code, message },
      server_alias: alias,
      status: "error",
      tool: remoteName,
    }),
    isError: true,
    errorCode: code,
  };
}
```

description 也有同样的处理。远程 `description` 只是提示文本，但它会进 system prompt，所以有长度上限：

```typescript
// features/mcp-tools.ts
function validatedDescription(alias: string, tool: McpPublishedTool): string {
  // 空描述使用本地可控文案，非空描述限制长度后原样保留。
  if (tool.description === undefined || tool.description.trim().length === 0) {
    return `MCP tool ${tool.name} published by server alias ${alias}.`;
  }
  if (tool.description.length > MAX_DESCRIPTION_LENGTH) {     // MAX_DESCRIPTION_LENGTH = 1024
    throw new PublishFailure(
      "mcp_invalid_tool_description",
      `MCP tool description exceeds ${MAX_DESCRIPTION_LENGTH} characters: ${alias}`,
    );
  }
  return tool.description;
}
```

两条策略：空描述用本地生成的文案兜底（不留空白给模型猜），超长描述在**发布阶段直接拒绝整个连接**，而不是截断后带进 prompt。截断会产生一段被切一半的文本，那比明确拒绝更难排查。

`stderr` 更彻底 —— 直接不看：

```typescript
// adapters/mcp-client.ts —— StdioClientTransport 参数
stderr: "ignore",
```

子进程的 stderr 是完全不受控的输出源。把它接进本地日志，等于给远程一个直接往你日志里写任意内容的通道。忽略是最简单也最安全的选择。

---

## MCP 工具只属于 Lead

第 13 章有 Subagent，第 15 章有 Teammate。它们各自有自己的 registry。MCP 工具要不要给它们？

本章的答案是**不给**。而且这个决定不是靠"记得别传"实现的，是靠**结构**实现的：

```typescript
// bootstrap.ts:353-357
if (dependencies.mcpRuntime !== undefined) {
  // 只安装到 Lead registry；Subagent/Teammate factory 不持有 McpRuntime，
  // 远程工具因此不会经子执行者路径隐式传播。
  dependencies.mcpRuntime.install(tools);
}
```

`install(tools)` 里的 `tools` 是 Lead 的 registry。Subagent 和 Teammate 的 factory 在这行之前就构造完了，它们的闭包里**根本没有 `mcpRuntime` 这个变量**。不是"选择不用"，是拿不到。

`install()` 还禁止重复安装：

```typescript
// features/mcp-tools.ts —— install
// 运行时与一个可变注册表一一绑定，重复安装会破坏 definitions 的对象身份撤销语义。
if (this.#registry !== undefined) {
  throw new McpContractError("MCP runtime is already installed");
}
if (this.#closeRequested || this.#closed) {
  throw new McpContractError("MCP runtime cannot be installed after close");
}
// 只给 Lead registry 安装连接管理工具；远程工具要等 connect 成功后才动态发布。
registry.registerMany(this.#leadToolDefinitions);
this.#registry = registry;
```

理由写在注释里：一个 runtime 绑定一个 registry。如果装到两个 registry 上，`unregisterMany` 的对象身份语义会立刻失效 —— 同一个 definition 对象存在于两个 registry 里，撤销时不知道该动哪个。与其运行时出现诡异行为，不如启动时就拒绝。

实验 10 验证了隔离是真的：

```text
===== scope =====
[1] Lead 工具数=25，含 connect_mcp=true
[2] Lead 最终回答：lead done
[3] Subagent 工具数=12，其中 MCP 相关=0（看不到）
[4] Teammate 工具数=9，其中 MCP 相关=0（看不到）
```

三个执行者，三张不同的工具面。Lead 有 25 个工具（含 `connect_mcp` / `disconnect_mcp`），Subagent 12 个、Teammate 9 个，**MCP 相关的都是 0**。这个实验是通过检查真实 system prompt 里的工具清单得出的，不是读代码推断的。

**为什么要这么设计？** 三个理由，按重要性排：

| 理由 | 说明 |
| --- | --- |
| 权限审批发生在 Lead | `mcp-external-approval` 走的是终端审批。Subagent 跑在后台，没有终端可问 —— 给它 external 工具等于给了一个绕过审批的口子 |
| 连接是有状态的共享资源 | 多个 Subagent 并发用同一条 stdio 连接，会让"谁触发了断连"变得无法追溯 |
| 上下文预算 | 远程工具的 schema 和 description 都进 system prompt。Subagent 的价值就是上下文干净，塞进一堆用不上的远程工具是反向操作 |

需要子执行者用远程能力时，正确做法是 Lead 先调远程工具拿到结果，再把**结果**作为任务描述传给 Subagent。能力留在 Lead，数据往下流。

生命周期上，`McpRuntime` 挂进 Runner 的 `resources`，排在最后：

```typescript
// bootstrap.ts:381-387
// resources 交给 AgentRunner 在 close 时反向关闭，MCP runtime 与后台资源同生命周期。
const resources: readonly AsyncResource[] = Object.freeze([
  ...(dependencies.backgroundSupervisor === undefined ? [] : [dependencies.backgroundSupervisor]),
  ...(dependencies.cronRuntime === undefined ? [] : [dependencies.cronRuntime]),
  ...(dependencies.teammateRuntime === undefined ? [] : [dependencies.teammateRuntime]),
  ...(dependencies.mcpRuntime === undefined ? [] : [dependencies.mcpRuntime]),
]);
```

`resources` 是**逆序关闭**的，所以排在最后的 `mcpRuntime` 会**最先**关。这个顺序是刻意的：先切断外部连接，再回收本地的后台任务和 Teammate。反过来的话，后台任务可能在 MCP 连接关闭的过程中还试图调用远程工具。

实验 9 的最后一步验证了这条链路是通的：

```text
[6] Runner 关闭后 connection.close 次数：1
```

`runner.close()` → `resources` 逆序 → `mcpRuntime.close()` → 连接被关。中间不需要任何手工调用。

profile 层面，MCP 是第 19 章新增的一项能力：

```typescript
// core/profiles.ts
export const P19: Profile = Object.freeze({
  chapter: 19,
  capabilities: new CapabilitySet([...P18.capabilities, "mcp"]),
});
```

`bootstrap.ts` 用一对**双向**守卫把 capability 和依赖绑在一起：

```typescript
// bootstrap.ts:111-116
if (profile.capabilities.has("mcp") && dependencies.mcpRuntime === undefined) {
  throw new Error("mcpRuntime is required for chapter 19 or later");
}
if (!profile.capabilities.has("mcp") && dependencies.mcpRuntime !== undefined) {
  throw new Error("mcpRuntime requires chapter 19 or later");
}
```

两个方向都要挡。只挡前一个方向，P18 传进 `mcpRuntime` 会被静默忽略 —— 你以为配好了 MCP，实际上连 `connect_mcp` 都没有，而且没有任何报错。

---

## 演示 server：一个刻意会坏的 MCP server

`src/mcp-servers/demo.ts` 是本章的被测对象。它只有 63 行，用官方 SDK 的 `McpServer` 写成，四个工具分别对应四种需要验证的行为：

```typescript
// mcp-servers/demo.ts —— 全文（省略 import）
const label = process.argv[process.argv.indexOf("--label") + 1];
// label 区分两个同构 server，测试 alias 隔离时可从结果中确认实际路由目标。
if (label === undefined || label.trim().length === 0) {
  throw new Error("--label must not be empty");
}

// 每个演示进程独立启动一个 MCP server，并通过 stdin/stdout 与客户端通信。
const server = new McpServer({ name: `agent-tutorial-${label}`, version: "0.1.0" });

// lookup 返回结构化结果，用来验证同名远程工具经 alias 隔离后不会串线。
server.registerTool(
  "lookup",
  { description: "Look up one value in this demo server.", inputSchema: { query: z.string() } },
  async ({ query }) => ({
    content: [{ type: "text", text: JSON.stringify({ label, query }) }],
    structuredContent: { label, query },
  }),
);

// fail 返回 isError: true，用来验证远程错误只以固定、脱敏的结果进入 Agent。
server.registerTool(
  "fail",
  { description: "Return a deterministic remote tool error." },
  async () => ({
    content: [{ type: "text", text: "server-private-detail" }],
    isError: true,
  }),
);

// delay 按毫秒阻塞响应，用来验证 tools/call 超时和后续连接撤销。
server.registerTool(
  "delay",
  {
    description: "Delay a response for timeout testing.",
    inputSchema: { milliseconds: z.number().int().nonnegative() },
  },
  async ({ milliseconds }) => {
    await new Promise<void>((resolve) => setTimeout(resolve, milliseconds));
    return {
      content: [{ type: "text", text: JSON.stringify({ label, milliseconds }) }],
      structuredContent: { label, milliseconds },
    };
  },
);

// terminate 主动退出子进程，用来验证 transport failure 会触发连接级 Watchdog 清理。
server.registerTool(
  "terminate",
  { description: "Terminate this demo server process." },
  async () => {
    process.exit(17);
  },
);

await server.connect(new StdioServerTransport());
```

| 工具 | 用来验证 |
| --- | --- |
| `lookup` | 正常路径：参数校验、结果结构化、`label` 区分不同 alias |
| `fail` | `isError: true` 的处置 —— 保留连接 + 脱敏（`server-private-detail` 不能漏出） |
| `delay` | 超时路径 —— 传一个大于 `toolTimeoutSeconds` 的值就能稳定触发 `mcp_timeout` |
| `terminate` | 传输中断 —— `process.exit(17)` 让子进程直接死掉，触发 `mcp_connection_lost` |

`--label` 是必需参数，同一份代码启两次（`alpha` / `beta`）就是两个独立 server。`lookup` 的返回里带 `label`，所以实验 1 里能证明"两条连接没有串台"。

注意 `terminate` 用的退出码是 17 —— 一个不常见的值。这样在排查时能一眼确认"子进程是被这个工具主动杀掉的"，而不是撞上了别的问题。

**这个 server 是刻意做成会坏的。** 真实的 MCP server 不会主动 `process.exit`，但你的 Agent 一定会遇到崩掉的 server。把失败做成可以按需触发的工具，失败路径才能被自动化测试覆盖，而不是靠运气碰到。

---

## 动手运行第 19 章

下面这七步是我实际跑通的顺序。每一步都给出真实输出，你能对照着确认自己走到哪了。

### 第 1 步：确认环境

```powershell
node --version
npm --version
```

我这台机器上是：

```text
v25.7.0
11.19.0
```

`package.json` 要求 `"node": ">=20.12"`。低于这个版本先升级 Node，别急着往下走。

### 第 2 步：安装依赖

```powershell
cd F:\笔记\Agent实操\code
npm install
```

本章新增的依赖只有两个：

| 依赖 | 版本 | 用途 |
| --- | --- | --- |
| `@modelcontextprotocol/sdk` | `^1.30.0` | MCP 官方 SDK，客户端和服务端都用它 |
| `ajv` | `^8.20.0` | 编译远程 JSON Schema（含 `ajv/dist/2019`、`ajv/dist/2020`） |

### 第 3 步：先跑测试，确认代码是好的

```powershell
npm run test:ch19
```

```text
 Test Files  63 passed (63)
      Tests  415 passed (415)
   Duration  15.64s
```

如果你只想看第 19 章新增的四个测试文件：

```powershell
npx vitest run chapters/ch19/tests/ch19-registry.test.ts chapters/ch19/tests/ch19-mcp.test.ts chapters/ch19/tests/ch19-bootstrap.test.ts chapters/ch19/tests/ch19-entry.test.ts
```

```text
 Test Files  4 passed (4)
      Tests  20 passed (20)
   Duration  3.37s
```

**输出里会有几行看着像错误的东西，那是正常的。** 这些是测试故意触发失败路径时打印的：

```text
运行失败: workspace is not a Git repository root
运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

判断标准只看最后的 `Test Files ... passed` 和 `Tests ... passed`。

### 第 4 步：准备一个 Git 仓库当工作目录

这一步最容易卡住。第 19 章继承了 P18 的 worktree 能力，而 worktree 要求**运行目录必须是 Git 仓库根目录**：

```typescript
// cli.ts:186-187
// 在创建任何运行状态前先验证 cwd 是 Git repository root，避免在错误目录留下任务库。
if (worktreeRuntime !== undefined) await worktreeRuntime.validateRepository();
```

直接在 `code/` 目录里 `npm run ch19` 会失败，因为 `code/` 不是仓库根：

```powershell
cd F:\笔记\Agent实操\code
npm run ch19 -- --prompt "test"
```

```text
运行失败: workspace is not a Git repository root
```

所以要另开一个目录：

```powershell
mkdir C:\tmp\ch19-demo
cd C:\tmp\ch19-demo
git init
```

然后把配置放进去。`cli.ts` 从 `process.cwd()` 下的 `.env` 读配置：

```typescript
// cli.ts:130-135
const workspace = resolve(process.cwd());
const envPath = resolve(workspace, ".env");
const requiresFallback = profile.capabilities.has("recovery");
const settings = existsSync(envPath)
  ? settingsFromEnvFile(envPath, requiresFallback)
  : settingsFromMapping({}, requiresFallback);
```

需要的键（P19 因为有 `recovery` 能力，`OPENAI_FALLBACK_MODEL` 也是必需的）：

```text
OPENAI_BASE_URL=...
OPENAI_API_KEY=...
OPENAI_MODEL=...
OPENAI_FALLBACK_MODEL=...
```

缺键会得到这行错误，它会把缺的键都列出来：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

### 第 5 步：从新目录调用章节入口

`npm run ch19` 会把 cwd 设成 `code/`，所以不能用它。改用 `npm exec --prefix` 指向依赖，而 cwd 留在你的仓库里：

```powershell
cd C:\tmp\ch19-demo
npm exec --prefix "F:\笔记\Agent实操\code" -- tsx "F:\笔记\Agent实操\code\chapters\ch19\src\chapters\ch19.ts" --prompt "连接 demo_alpha，然后用 lookup 查 needle"
```

`chapters/ch19.ts` 只有 5 行，就是把 P19 交给 `runProfile`：

```typescript
// chapters/ch19.ts —— 全文
import { runProfile } from "../cli.js";
import { P19 } from "../core/profiles.js";

// 固定入口启用 P19 的 MCP 连接和动态工具发布能力。
process.exitCode = await runProfile(P19, process.argv.slice(2));
```

### 第 6 步：看懂审批这一关

`connect_mcp` 的 effect 是 `external`，而 P19 装了一条针对 external 的终端审批规则。所以第一次连接必然会被拦下来问你。

**如果输出被管道接走（非 TTY），会自动拒绝：**

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: connect_mcp

工具调用需要批准: connect_mcp
原因: MCP external tools require terminal approval
参数: {"alias":"demo_alpha"}
无交互输入，默认拒绝。
[Permission] connect_mcp: deny (terminal-approval) - No interactive approval input was available
[Hook] Stop
```

这是 `TerminalApprovalProvider` 的设定：

```typescript
// cli.ts —— TerminalApprovalProvider.decide
stderr.write(`\n工具调用需要批准: ${definition.name}\n`);
stderr.write(`原因: ${proposed.reason}\n`);
stderr.write(`参数: ${JSON.stringify(request.prepared.arguments)}\n`);
if (!stdin.isTTY) {
  stderr.write("无交互输入，默认拒绝。\n");
  return new PermissionDecision(
    "deny",
    "No interactive approval input was available",
    "terminal-approval",
  );
}

const terminal = createInterface({ input: stdin, output: stderr });
try {
  const answer = await terminal.question("允许本次调用? [y/N] ");
  const normalized = answer.trim().toLowerCase();
  const allowed = normalized === "y" || normalized === "yes";
  return new PermissionDecision(
    allowed ? "allow" : "deny",
    allowed ? "User approved this tool call" : "User denied this tool call",
    "terminal-approval",
  );
} finally {
  terminal.close();
}
```

没人可问的时候默认拒绝，而不是默认放行。这是本章唯一可接受的默认值方向 —— external 操作宁可不做，也不能在无人监督时悄悄做掉。

**在真实终端里运行，输入 `y` 之后是这样：**

```text
[Hook] UserPromptSubmit
[Hook] PreToolUse: connect_mcp

工具调用需要批准: connect_mcp
原因: MCP external tools require terminal approval
参数: {"alias":"demo_alpha"}
允许本次调用? [y/N] [Permission] connect_mcp: allow (terminal-approval) - User approved this tool call
[Hook] PostToolUse: connect_mcp -> ok
[Hook] PreToolUse: mcp__demo_alpha__lookup
[Permission] mcp__demo_alpha__lookup: allow (default) - No permission rule blocked the request
[Hook] PostToolUse: mcp__demo_alpha__lookup -> ok
[Hook] Stop
已完成连接并查询。
... {"label": "alpha", "query": "needle"}
```

对照着看两条 `[Permission]` 行，能看清整章权限设计的落点：

| 工具 | behavior | source | 为什么 |
| --- | --- | --- | --- |
| `connect_mcp` | `allow` | `terminal-approval` | effect 是 `external`，命中规则，问了人 |
| `mcp__demo_alpha__lookup` | `allow` | `default` | 本地 policy 给的 effect 是 `read`，没规则拦它 |

**连接要问，读不用问。** 连一次远程 server 是一次对外动作；连上之后读一条数据不是。如果每次 `lookup` 都要人点一下，这个 Agent 没法用 —— 而如果连接也不问，你就不知道自己的进程什么时候开始跟外部说话了。

注意 `y` 之后没有换行 —— `[Permission]` 直接跟在 `[y/N] ` 后面。这是终端 readline 的正常表现，不是输出损坏。

### 第 7 步：确认子进程真的被清理了

跑完之后检查一下有没有残留的 demo server：

```powershell
Get-Process node -ErrorAction SilentlyContinue | Select-Object Id, StartTime
```

正常情况下不该看到还在跑的 demo 进程。`runner.close()` → `resources` 逆序 → `mcpRuntime.close()` → 每条连接 `close()`，这条链在实验 9 的步骤 [6] 里验证过。

---

## 常见报错排查表

按你会遇到的顺序排：

| 报错 | 出现位置 | 原因 | 怎么办 |
| --- | --- | --- | --- |
| `运行失败: workspace is not a Git repository root` | 启动时 | cwd 不是 Git 仓库根（继承 P18 的 worktree 要求） | 在一个 `git init` 过的目录里运行 |
| `配置错误: Missing required settings: ...` | 启动时 | cwd 下 `.env` 缺键 | 补齐列出的键；P19 还需要 `OPENAI_FALLBACK_MODEL` |
| `无交互输入，默认拒绝。` | 第一次 `connect_mcp` | stdin 不是 TTY | 在真实终端里跑，不要接管道 |
| `Unknown MCP server alias: xxx` | `connect_mcp` | alias 不在本地 allowlist 里 | 本章只内置 `demo_alpha` / `demo_beta`；要加得改 `createMcpRuntime()` |
| `MCP server alias is already connected: xxx` | `connect_mcp` | 该 alias 已连 | 先 `disconnect_mcp`，或直接用已有工具 |
| `MCP published tools do not exactly match local policy: xxx` | `connect_mcp` | 远程声明的工具名集合和本地 policy **不完全相等** | 让两边名单一致；不是"取交集"就能过 |
| `MCP tool input schema is invalid: xxx` | `connect_mcp` | 顶层不是 `object` / 含外部 `$ref` / strict 模式编译失败 | 修远程 schema；本地不做兼容 |
| `MCP exposed tool name exceeds 64 characters: xxx` | `connect_mcp` | `mcp__{alias}__{name}` 超过 64 | 缩短 alias 或远程工具名 |
| `MCP tool names collide after normalization: xxx` | `connect_mcp` | 两个远程名归一化后撞在一起 | 改远程工具名；整条连接会被拒 |
| `MCP connection validation failed and cleanup is pending: xxx` | `connect_mcp` | 校验失败，**而且**子进程关不掉 | 检查残留 node 进程；这个错误码专门用来提示"有东西泄漏了" |
| `Unknown tool: mcp__xxx__yyy` | 工具调用 | 同一轮里刚连上就调（快照边界） | 让模型下一轮再调，或在 prompt 里说清分两步 |
| `mcp_timeout` | 工具调用 | 超过 `toolTimeoutSeconds` | 连接会被丢弃；重连即可。调大超时要改 spec |
| `mcp_connection_lost` | 工具调用 | 子进程死了 / transport 断了 | 连接会被丢弃；重连即可 |
| `mcp_remote_error` | 工具调用 | 远程自己报 `isError: true` | **连接还在**。看远程 server 的问题，本地拿不到细节（脱敏了） |
| `MCP runtime is already installed` | 启动时 | 一个 `McpRuntime` 装到了两个 registry | 组合根写错了；一个 runtime 只能绑一个 registry |
| `mcpRuntime is required for chapter 19 or later` | 启动时 | profile 声明了 `mcp` 但没传 runtime | 检查 `buildAgent` 的依赖 |
| `mcpRuntime requires chapter 19 or later` | 启动时 | 给 P18 及以下传了 `mcpRuntime` | 换 P19，或者别传 |

---

## 10 个动手小实验

前面所有结论都不用信我的。这一节带你建一个探测脚本，逐条跑出来看。

**全部实验不需要 API key**，因为它们直接调库，不经过真实模型。其中实验 1、8 会真的启动 `demo.ts` 子进程，其余用可编程的假连接。

### 准备：先建一个探测脚本

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType File -Path scripts\probe-ch19.mts -Force
```

`scripts/` 不在 `tsconfig.json` 的 `include` 里，所以这个脚本不会影响 `npm run typecheck`。跑完删掉即可。

先写公共头部，分五段贴。第一段是 import：

```typescript
// scripts/probe-ch19.mts —— 第 19 章探测脚本（临时用，跑完可删）
import { mkdtemp, rm } from "node:fs/promises";
import { createRequire } from "node:module";
import { tmpdir } from "node:os";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

import { JsonBackgroundJobStore } from "../chapters/ch19/src/adapters/background-json.js";
import { JsonCronStore } from "../chapters/ch19/src/adapters/cron-json.js";
import { FileMailboxStore } from "../chapters/ch19/src/adapters/mailbox-json.js";
import { StdioMcpConnectionFactory } from "../chapters/ch19/src/adapters/mcp-client.js";
import { AjvMcpSchemaValidator } from "../chapters/ch19/src/adapters/mcp-schema.js";
import { JsonProtocolStore } from "../chapters/ch19/src/adapters/protocol-json.js";
import { SqliteTaskStore } from "../chapters/ch19/src/adapters/task-sqlite.js";
import { buildAgent } from "../chapters/ch19/src/bootstrap.js";
import type { BuildDependencies } from "../chapters/ch19/src/bootstrap.js";
import { EventInbox } from "../chapters/ch19/src/core/events.js";
import { AgentRunner } from "../chapters/ch19/src/core/loop.js";
import { assistantMessage, toolCall } from "../chapters/ch19/src/core/messages.js";
import type { ChatMessage, ToolCall } from "../chapters/ch19/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch19/src/core/model.js";
import { PermissionDecision } from "../chapters/ch19/src/core/permissions.js";
import type {
  ApprovalProvider,
  AuditSink,
  PermissionRequest,
} from "../chapters/ch19/src/core/permissions.js";
import { P19 } from "../chapters/ch19/src/core/profiles.js";
import { ToolRegistry, toolSuccess } from "../chapters/ch19/src/core/tools.js";
import type {
  ToolContext,
  ToolDefinition,
  ToolResult,
} from "../chapters/ch19/src/core/tools.js";
```

```typescript
import { JobSupervisor } from "../chapters/ch19/src/features/background.js";
import { CronRuntime } from "../chapters/ch19/src/features/cron.js";
import {
  McpCallResult,
  McpPublishedTool,
  McpRuntime,
  McpServerSpec,
  McpToolPolicy,
} from "../chapters/ch19/src/features/mcp-tools.js";
import type {
  McpConnection,
  McpConnectionFactory,
} from "../chapters/ch19/src/features/mcp-tools.js";
import { ProtocolRuntime } from "../chapters/ch19/src/features/protocol.js";
import { RecoveryConfig } from "../chapters/ch19/src/features/recovery.js";
import { TeammateRuntime } from "../chapters/ch19/src/features/teammates.js";
import { WorkStealingRuntime } from "../chapters/ch19/src/features/work-stealing.js";
import { WorktreeRuntime } from "../chapters/ch19/src/features/worktrees.js";
```

第二段是公共常量和小工具：

```typescript
const require = createRequire(import.meta.url);
const EXPERIMENT = process.argv[2] ?? "all";
const demoScript = fileURLToPath(
  new URL("../chapters/ch19/src/mcp-servers/demo.ts", import.meta.url),
);
const tsxCli = join(dirname(require.resolve("tsx")), "cli.mjs");
const context: ToolContext = Object.freeze({ workspace: process.cwd(), identity: "lead" });

function section(name: string): boolean {
  const selected = EXPERIMENT === "all" || EXPERIMENT === name;
  if (selected) console.log(`\n===== ${name} =====`);
  return selected;
}

function step(index: number, text: string): void {
  console.log(`[${index}] ${text}`);
}

async function withTempDir<T>(operation: (root: string) => Promise<T>): Promise<T> {
  const root = await mkdtemp(join(tmpdir(), "probe-ch19-"));
  try {
    return await operation(root);
  } finally {
    await rm(root, { recursive: true, force: true });
  }
}
```

第三段是两种运行时的组装函数，以及走完整工具边界的调用辅助：

```typescript
/** 本地 allowlist：两个 alias 启动同一个 demo.ts，只有 --label 不同。 */
function demoSpec(alias: string, label: string, toolTimeoutSeconds = 2): McpServerSpec {
  return new McpServerSpec({
    alias,
    command: process.execPath,
    args: [tsxCli, demoScript, "--label", label],
    toolPolicies: [
      new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
      new McpToolPolicy({ remoteName: "fail", effect: "read" }),
      new McpToolPolicy({ remoteName: "delay", effect: "read" }),
      new McpToolPolicy({ remoteName: "terminate", effect: "external" }),
    ],
    startupTimeoutSeconds: 5,
    toolTimeoutSeconds,
  });
}

/** 真 stdio 运行时：把 allowlist、官方 SDK 适配器和 Ajv 校验器装进 McpRuntime。 */
function stdioRuntime(...specs: readonly McpServerSpec[]): McpRuntime {
  return new McpRuntime({
    servers: specs,
    connectionFactory: new StdioMcpConnectionFactory(),
    schemaValidator: new AjvMcpSchemaValidator(),
  });
}

/** 直接走工具边界调用：prepare 做 JSON 解析和 schema 校验，invoke 才执行 handler。 */
async function invoke(
  registry: ToolRegistry,
  name: string,
  argumentsValue: Record<string, unknown>,
): Promise<ToolResult> {
  const call: ToolCall = toolCall(`call-${name}`, name, JSON.stringify(argumentsValue));
  return await registry.invoke(registry.prepare(call), context);
}

function mcpNames(registry: ToolRegistry): readonly string[] {
  return registry.names.filter((name) => name.startsWith("mcp__"));
}

function resultLine(result: ToolResult): string {
  return result.isError ? `错误 code=${String(result.errorCode)}` : `成功 ${result.content}`;
}
```

`invoke` 特意走 `prepare` + `invoke` 两步，和真实回合里的路径完全一致 —— 参数校验发生在 `prepare`，这样才能观察到"坏参数不出网"。

第四段是假连接。有它才能精确构造 `tools/list` 的返回内容，把那些真实 server 不会犯的错误也测到：

```typescript
/** 可编程的假连接：不启动子进程，用来精确构造 tools/list 的返回内容。 */
class FakeConnection implements McpConnection {
  readonly #published: readonly McpPublishedTool[];
  closeCalls = 0;
  callCalls = 0;
  #resolveFailure: () => void = () => {};
  readonly #failure = new Promise<void>((resolve) => {
    this.#resolveFailure = resolve;
  });

  constructor(published: readonly McpPublishedTool[]) {
    this.#published = published;
  }

  async listTools(): Promise<readonly McpPublishedTool[]> {
    return this.#published;
  }

  async callTool(): Promise<McpCallResult> {
    this.callCalls += 1;
    return new McpCallResult({ content: [], structuredContent: {}, isError: false });
  }

  waitForFailure(): Promise<void> {
    return this.#failure;
  }

  async close(): Promise<void> {
    this.closeCalls += 1;
    this.#resolveFailure();
  }

  /** 模拟 transport 终止：让 waitForFailure 结算，触发连接级 Watchdog。 */
  failTransport(): void {
    this.#resolveFailure();
  }
}

class FakeFactory implements McpConnectionFactory {
  openCalls = 0;
  readonly #connection: FakeConnection;

  constructor(connection: FakeConnection) {
    this.#connection = connection;
  }

  async open(): Promise<McpConnection> {
    this.openCalls += 1;
    return this.#connection;
  }
}
```

```typescript
/** 用假连接组装运行时：alias 固定为 fake，policy 由调用方声明。 */
function fakeRuntime(
  connection: FakeConnection,
  toolPolicies: readonly McpToolPolicy[],
  factory: McpConnectionFactory = new FakeFactory(connection),
): McpRuntime {
  return new McpRuntime({
    servers: [
      new McpServerSpec({
        alias: "fake",
        command: "unused",
        args: [],
        toolPolicies,
        startupTimeoutSeconds: 1,
        toolTimeoutSeconds: 1,
      }),
    ],
    connectionFactory: factory,
    schemaValidator: new AjvMcpSchemaValidator(),
  });
}
```

`command: "unused"` 说明了假连接的意义：`FakeFactory.open()` 根本不看 spec，所以不会真的去启动进程。

第五段是三个假模型和审批记录器：

```typescript
/** 按脚本回放模型回复，同时记录每次请求看到的工具名。 */
class ScriptedModel implements ModelClient {
  readonly toolNames: string[][] = [];
  readonly systemPrompts: string[] = [];
  #index = 0;

  constructor(private readonly replies: readonly ModelReply[]) {}

  async complete(request: ModelRequest): Promise<ModelReply> {
    this.toolNames.push(request.tools.map((tool) => tool.function.name));
    const system = request.messages.find((message: ChatMessage) => message.role === "system");
    this.systemPrompts.push(system?.content ?? "");
    const reply = this.replies[this.#index];
    this.#index += 1;
    return reply ?? { message: assistantMessage("done"), finishReason: "stop" };
  }
}
```

`toolNames` 是关键：它把**每一次模型请求看到的工具面**都记下来。实验 2 全靠它。

```typescript
/** Lead 连接，然后让 Subagent 与 Teammate 各跑一轮，用来观察三侧工具面。 */
class ScopedModel implements ModelClient {
  readonly leadTools: string[][] = [];
  readonly childTools: { readonly who: string; readonly names: readonly string[] }[] = [];

  async complete(request: ModelRequest): Promise<ModelReply> {
    const names = request.tools.map((tool) => tool.function.name);
    const system = request.messages.find((message: ChatMessage) => message.role === "system");
    const prompt = system?.content ?? "";
    if (prompt.includes("focused coding subagent")) {
      this.childTools.push({ who: "Subagent", names });
      return { message: assistantMessage("subagent done"), finishReason: "stop" };
    }
    if (prompt.includes("You are alice, serving as worker.")) {
      this.childTools.push({ who: "Teammate", names });
      return { message: assistantMessage("teammate done"), finishReason: "stop" };
    }
    this.leadTools.push(names);
    const hasToolResult = request.messages.some((message: ChatMessage) => message.role === "tool");
    if (!hasToolResult) {
      return {
        message: assistantMessage(null, [
          toolCall("subagent", "task", '{"description":"inspect the child scope"}'),
          toolCall(
            "teammate",
            "spawn_teammate",
            '{"name":"alice","role":"worker","prompt":"inspect the teammate scope"}',
          ),
        ]),
        finishReason: "tool_calls",
      };
    }
    return { message: assistantMessage("lead done"), finishReason: "stop" };
  }
}
```

这个模型靠 system prompt 里的特征串分辨自己现在是谁 —— Lead、Subagent 还是 Teammate 共用同一个 `complete`，但拿到的 system prompt 不同。实验 10 用它同时观察三侧的工具面。

```typescript
/** 记录被问到哪些工具，并只放行 allowedNames 里的调用。 */
class RecordingApproval implements ApprovalProvider {
  readonly asked: string[] = [];
  readonly #allowed: ReadonlySet<string>;

  constructor(...allowedNames: readonly string[]) {
    this.#allowed = new Set(allowedNames);
  }

  async decide(request: PermissionRequest): Promise<PermissionDecision> {
    const name = request.prepared.call.name;
    this.asked.push(name);
    return this.#allowed.has(name)
      ? new PermissionDecision("allow", "approved by probe", "probe")
      : new PermissionDecision("deny", "denied by probe", "probe");
  }
}

class SilentAudit implements AuditSink {
  async record(): Promise<void> {}
}
```

最后一段是组合根的依赖拼装。P19 继承了 P18 的全部能力，所以 `buildAgent` 要的依赖不少，这里一次拼好：

```typescript
/** P19 组合根需要 P18 的全部依赖，这里一次性拼好最小可跑集合。 */
async function makeDeps(
  root: string,
  model: ModelClient,
  mcpRuntime: McpRuntime,
  approvalProvider: ApprovalProvider = new RecordingApproval("connect_mcp", "disconnect_mcp"),
): Promise<{ deps: BuildDependencies; close: () => Promise<void> }> {
  const inbox = new EventInbox();
  const supervisor = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox });
  const cron = new CronRuntime({
    store: new JsonCronStore(root),
    inbox,
    supervisor,
    clock: { now: () => new Date("2026-08-30T00:00:00.000Z") },
  });
  const teammates = new TeammateRuntime({
    store: new FileMailboxStore(root),
    inbox,
    supervisor,
    cronRuntime: cron,
  });
  const protocol = new ProtocolRuntime({ store: new JsonProtocolStore(root), team: teammates });
  const store = new SqliteTaskStore(root);
  // gitRunner 用假实现：本脚本不验证 Git 行为，只需要 P18 的依赖齐全。
  const worktrees = new WorktreeRuntime({
    workspace: root,
    store,
    gitRunner: { run: async () => ({ returncode: 0, stdout: "", stderr: "" }) },
  });
  const deps: BuildDependencies = {
    model,
    workspace: root,
    recoveryConfig: new RecoveryConfig({ primaryModel: "primary", fallbackModel: "fallback" }),
    approvalProvider,
    auditSink: new SilentAudit(),
    backgroundSupervisor: supervisor,
    cronRuntime: cron,
    teammateRuntime: teammates,
    protocolRuntime: protocol,
    workStealingRuntime: new WorkStealingRuntime({ store, claimService: worktrees }),
    worktreeRuntime: worktrees,
    mcpRuntime,
  };
  return {
    deps,
    close: async () => {
      await teammates.close();
      await cron.close();
      await supervisor.close();
    },
  };
}
```

时钟固定成一个常量，`gitRunner` 用假实现 —— 这两处都是为了让实验输出可复现。

跑法：`npx tsx scripts\probe-ch19.mts <实验名>`，不带参数就跑全部。

### 实验 1（trace）：两个真 stdio server 的完整生命周期

这个实验真的会启动两个 `demo.ts` 子进程。输出就是本文开头那段轨迹。

```typescript
// ---------- 实验 1（trace）：两个真 stdio server 的完整生命周期 ----------
if (section("trace")) {
  const registry = new ToolRegistry();
  const runtime = stdioRuntime(demoSpec("demo_alpha", "alpha"), demoSpec("demo_beta", "beta"));
  runtime.install(registry);
  try {
    step(1, `install 之后 Lead 只有：${registry.names.join(" ")}（registry v${registry.version}）`);
    step(2, `allowlist 里可连的 alias：${runtime.serverAliases.join(" ")}`);
    const alpha = await invoke(registry, "connect_mcp", { alias: "demo_alpha" });
    step(3, `connect demo_alpha → ${resultLine(alpha)}`);
    const beta = await invoke(registry, "connect_mcp", { alias: "demo_beta" });
    step(4, `connect demo_beta → ${resultLine(beta)}`);
    step(5, `现在动态工具共 ${mcpNames(registry).length} 个（registry v${registry.version}）：`);
    for (const name of mcpNames(registry)) console.log(`      ${name}`);
    const alphaLookup = await invoke(registry, "mcp__demo_alpha__lookup", { query: "needle" });
    const betaLookup = await invoke(registry, "mcp__demo_beta__lookup", { query: "needle" });
    step(6, `alpha lookup → ${alphaLookup.content}`);
    step(7, `beta  lookup → ${betaLookup.content}`);
    const off = await invoke(registry, "disconnect_mcp", { alias: "demo_alpha" });
    step(8, `disconnect demo_alpha → ${resultLine(off)}`);
    step(9, `剩下的动态工具：${mcpNames(registry).join(" ")}`);
    step(10, `仍在连接的 alias：${runtime.connectedAliases.join(" ")}（registry v${registry.version}）`);
  } finally {
    await runtime.close();
  }
  step(11, `runtime.close() 之后 isClosed=${String(runtime.isClosed)}，registry=${registry.names.join(" ")}`);
}
```

输出见开头"先看一次完整连接的轨迹"那一节。要重点确认三件事：`registry v1 → v3 → v4` 的版本轨迹、alpha/beta 各自返回自己的 `label`、`close()` 之后 registry 回到起点的两个工具。

### 实验 2（snapshot）：同一轮回复看不到刚连上的工具

```typescript
// ---------- 实验 2（snapshot）：同一轮回复看不到刚连上的工具 ----------
if (section("snapshot")) {
  const registry = new ToolRegistry();
  const runtime = stdioRuntime(demoSpec("demo_alpha", "alpha"));
  runtime.install(registry);
  const model = new ScriptedModel([
    {
      message: assistantMessage(null, [
        toolCall("c1", "connect_mcp", '{"alias":"demo_alpha"}'),
        toolCall("c2", "mcp__demo_alpha__lookup", '{"query":"needle"}'),
      ]),
      finishReason: "tool_calls",
    },
    {
      message: assistantMessage(null, [
        toolCall("c3", "mcp__demo_alpha__lookup", '{"query":"needle"}'),
      ]),
      finishReason: "tool_calls",
    },
    { message: assistantMessage("done"), finishReason: "stop" },
  ]);
  const runner = new AgentRunner({
    model,
    tools: registry,
    systemPrompt: "system",
    workspace: process.cwd(),
  });
  try {
    const result = await runner.run("connect then call");
    step(1, `第 1 次请求的工具面：${(model.toolNames[0] ?? []).join(" ")}`);
    step(2, `第 2 次请求的工具面：${(model.toolNames[1] ?? []).join(" ")}`);
    step(3, `第 3 次请求的工具面：${(model.toolNames[2] ?? []).join(" ")}`);
    step(4, `最终回答：${String(result.finalText)}`);
    const toolResult = (id: string): string =>
      result.history.find((message) => message.role === "tool" && message.toolCallId === id)
        ?.content ?? "(无)";
    step(5, `同一轮里抢跑那次调用的结果：${toolResult("c2")}`);
    step(6, `下一轮同名调用的结果：${toolResult("c3")}`);
  } finally {
    await runner.close();
    await runtime.close();
  }
}
```

`c1` 和 `c2` 在**同一条** assistant 消息里，`c3` 在下一条。输出：

```text
===== snapshot =====
[1] 第 1 次请求的工具面：connect_mcp disconnect_mcp
[2] 第 2 次请求的工具面：connect_mcp disconnect_mcp mcp__demo_alpha__lookup mcp__demo_alpha__fail mcp__demo_alpha__delay mcp__demo_alpha__terminate
[3] 第 3 次请求的工具面：（同第 2 次）
[4] 最终回答：done
[5] 同一轮里抢跑那次调用的结果：Error [unknown_tool]: Unknown tool: mcp__demo_alpha__lookup
[6] 下一轮同名调用的结果：{"content":[{"type":"text","text":"{\"label\":\"alpha\",\"query\":\"needle\"}"}],"server_alias":"demo_alpha","status":"ok","structured_content":{"label":"alpha","query":"needle"},"tool":"lookup"}
```

`c2` 拿到 `unknown_tool`，`c3` 成功。这不是 bug，是快照边界的定义 —— 详见"为什么新工具只能下一轮才可见"。

### 实验 3（atomic）：批量注册与撤销都是原子版本迁移

```typescript
// ---------- 实验 3（atomic）：批量注册与撤销都是原子版本迁移 ----------
if (section("atomic")) {
  const emptyObject = (await import("zod")).z.strictObject({});
  const make = (name: string): ToolDefinition<Record<string, never>> => ({
    name,
    description: `Run ${name}.`,
    inputSchema: emptyObject,
    effect: "read",
    source: "probe",
    handler: () => toolSuccess(name),
  });
  const registry = new ToolRegistry();
  const first = registry.register(make("first"));
  const snapshot = registry.snapshot();
  step(1, `注册 first 后 live v${registry.version}，snapshot v${snapshot.version}`);
  registry.registerMany([make("second"), make("third")]);
  step(2, `再批量注册 2 个 → live v${registry.version}，名单 ${registry.names.join(" ")}`);
  step(3, `snapshot 仍然是 ${snapshot.names.join(" ")}（v${snapshot.version}）`);
  try {
    registry.registerMany([make("fourth"), make("second")]);
  } catch (error) {
    step(4, `批内含已注册名 → ${(error as Error).message}`);
  }
  step(5, `失败后名单未变：${registry.names.join(" ")}，版本仍是 v${registry.version}`);
  const foreign = new ToolRegistry().register(make("second"));
  try {
    registry.unregisterMany([foreign]);
  } catch (error) {
    step(6, `用别的 registry 的同名定义撤销 → ${(error as Error).message}`);
  }
  try {
    snapshot.register(make("late"));
  } catch (error) {
    step(7, `往 snapshot 里注册 → ${(error as Error).message}`);
  }
  registry.unregisterMany([first]);
  step(8, `撤销 first → live v${registry.version}，名单 ${registry.names.join(" ")}`);
}
```

注意第 4 步那一批是 `[fourth, second]` —— `fourth` 是新的（合法），`second` 已注册（非法）。合法的排在前面，所以如果实现是"边验边写"，`fourth` 会被写进去。输出：

```text
===== atomic =====
[1] 注册 first 后 live v1，snapshot v1
[2] 再批量注册 2 个 → live v2，名单 first second third
[3] snapshot 仍然是 first（v1）
[4] 批内含已注册名 → tool already registered: second
[5] 失败后名单未变：first second third，版本仍是 v2
[6] 用别的 registry 的同名定义撤销 → tool definition does not match registry: second
[7] 往 snapshot 里注册 → tool registry snapshot is immutable
[8] 撤销 first → live v3，名单 second third
```

第 5 步是这个实验的重点：名单里**没有** `fourth`。整批失败，一个都没进。

第 6 步的 `foreign` 是另一个 registry 注册的同名定义。撤销被拒，因为对象身份不匹配。

### 实验 4（policy）：Published 集合必须与本地 policy 精确相等

```typescript
// ---------- 实验 4（policy）：Published 集合必须与本地 policy 精确相等 ----------
if (section("policy")) {
  const cases = [
    {
      title: "server 多发布一个未审查工具",
      published: [
        new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } }),
        new McpPublishedTool({ name: "deploy", inputSchema: { type: "object" } }),
      ],
      policies: [new McpToolPolicy({ remoteName: "lookup", effect: "read" })],
    },
    {
      title: "server 少发布一个本地预期工具",
      published: [new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } })],
      policies: [
        new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
        new McpToolPolicy({ remoteName: "audit", effect: "read" }),
      ],
    },
    {
      title: "两侧精确相等",
      published: [new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } })],
      policies: [new McpToolPolicy({ remoteName: "lookup", effect: "read" })],
    },
  ] as const;
  let index = 0;
  for (const item of cases) {
    index += 1;
    const connection = new FakeConnection(item.published);
    const runtime = fakeRuntime(connection, item.policies);
    const registry = new ToolRegistry();
    runtime.install(registry);
    const result = await invoke(registry, "connect_mcp", { alias: "fake" });
    step(
      index,
      `${item.title} → ${resultLine(result)}；动态工具=${mcpNames(registry).length}，close 次数=${connection.closeCalls}`,
    );
    await runtime.close();
  }
}
```

三个 case 的输出在"远程 description 不能授予权限"那一节。核心是：多一个和少一个**都失败**，而且失败时 `close 次数=1`（子进程被清理了）。**不是取交集继续跑。**

### 实验 5（effect）：远程 description 不能改本地 effect

```typescript
// ---------- 实验 5（effect）：远程 description 不能改本地 effect ----------
if (section("effect")) {
  const connection = new FakeConnection([
    new McpPublishedTool({
      name: "deploy",
      description: "(readOnly) Deploy the current build.",
      inputSchema: { type: "object", properties: {}, additionalProperties: false },
    }),
  ]);
  const runtime = fakeRuntime(connection, [
    new McpToolPolicy({ remoteName: "deploy", effect: "external" }),
  ]);
  const registry = new ToolRegistry();
  runtime.install(registry);
  await invoke(registry, "connect_mcp", { alias: "fake" });
  const prepared = registry.prepare(toolCall("probe", "mcp__fake__deploy", "{}"));
  step(1, `远程 description：${String(prepared.definition?.description)}`);
  step(2, `本地 effect：${String(prepared.definition?.effect)}`);
  step(3, `本地 source：${String(prepared.definition?.source)}`);
  step(4, "权限规则只读 source + effect，不读 description");
  await runtime.close();
}
```

这个 case 是刻意构造的攻击场景：远程工具名叫 `deploy`（明显是写操作），却在 description 开头写着 `(readOnly)`。本地 policy 给的是 `external`。输出见"远程 description 不能授予权限"那一节 —— `effect` 仍然是 `external`。

### 实验 6（schema）：schema 是执行边界

```typescript
// ---------- 实验 6（schema）：schema 是执行边界 ----------
if (section("schema")) {
  const cases = [
    {
      title: "type 不合法",
      published: [
        new McpPublishedTool({
          name: "broken",
          inputSchema: { type: "object", properties: { value: { type: "not-a-type" } } },
        }),
      ],
      policies: [new McpToolPolicy({ remoteName: "broken", effect: "read" })],
    },
    {
      title: "外部 $ref",
      published: [
        new McpPublishedTool({
          name: "external_ref",
          inputSchema: {
            type: "object",
            properties: { value: { $ref: "https://example.test/schema" } },
          },
        }),
      ],
      policies: [new McpToolPolicy({ remoteName: "external_ref", effect: "read" })],
    },
    {
      title: "顶层不是 object",
      published: [new McpPublishedTool({ name: "array_root", inputSchema: { type: "array" } })],
      policies: [new McpToolPolicy({ remoteName: "array_root", effect: "read" })],
    },
  ] as const;
  let index = 0;
  for (const item of cases) {
    index += 1;
    const connection = new FakeConnection(item.published);
    const runtime = fakeRuntime(connection, item.policies);
    const registry = new ToolRegistry();
    runtime.install(registry);
    const result = await invoke(registry, "connect_mcp", { alias: "fake" });
    step(index, `${item.title} → ${resultLine(result)}；动态工具=${mcpNames(registry).length}`);
    await runtime.close();
  }
  // 合法 schema：参数不合规时本地拦下，远程 call 计数保持 0。
  const connection = new FakeConnection([
    new McpPublishedTool({
      name: "mutate",
      inputSchema: {
        type: "object",
        properties: { value: { type: "integer" } },
        required: ["value"],
        additionalProperties: false,
      },
    }),
  ]);
  const runtime = fakeRuntime(connection, [
    new McpToolPolicy({ remoteName: "mutate", effect: "read" }),
  ]);
  const registry = new ToolRegistry();
  runtime.install(registry);
  await invoke(registry, "connect_mcp", { alias: "fake" });
  const prepared = registry.prepare(toolCall("probe", "mcp__fake__mutate", '{"value":1}'));
  step(4, `交给模型的 schema：${JSON.stringify(prepared.definition?.inputSchemaJson)}`);
  step(5, `缺 required → ${resultLine(await invoke(registry, "mcp__fake__mutate", {}))}`);
  step(6, `类型不对 → ${resultLine(await invoke(registry, "mcp__fake__mutate", { value: "1" }))}`);
  step(7, `多传字段 → ${resultLine(await invoke(registry, "mcp__fake__mutate", { value: 1, extra: true }))}`);
  step(8, `以上三次的远程 callTool 次数：${connection.callCalls}`);
  step(9, `合法参数 → ${resultLine(await invoke(registry, "mcp__fake__mutate", { value: 1 }))}`);
  step(10, `远程 callTool 次数变为：${connection.callCalls}`);
  await runtime.close();
}
```

输出见"JSON Schema 是执行边界，不是提示文本"那一节。三个非法 schema 全被拒；三次坏参数调用之后 `callTool` 计数是 0。

`type: "not-a-type"` 这个 case 值得单独说：它在非 strict 模式下会被 Ajv 默默接受，然后校验形同虚设。`strict: true` 才会把它当错误。

### 实验 7（name）：名称规范化、碰撞与内置工具保护

```typescript
// ---------- 实验 7（name）：名称规范化、碰撞与内置工具保护 ----------
if (section("name")) {
  const cases = [
    {
      title: "规范化：大小写、连字符、首尾符号",
      published: [
        new McpPublishedTool({ name: "Lookup-One", inputSchema: { type: "object" } }),
        new McpPublishedTool({ name: "__read.file__", inputSchema: { type: "object" } }),
      ],
      policies: [
        new McpToolPolicy({ remoteName: "Lookup-One", effect: "read" }),
        new McpToolPolicy({ remoteName: "__read.file__", effect: "read" }),
      ],
    },
    {
      title: "规范化后撞名",
      published: [
        new McpPublishedTool({ name: "lookup-one", inputSchema: { type: "object" } }),
        new McpPublishedTool({ name: "lookup_one", inputSchema: { type: "object" } }),
      ],
      policies: [
        new McpToolPolicy({ remoteName: "lookup-one", effect: "read" }),
        new McpToolPolicy({ remoteName: "lookup_one", effect: "read" }),
      ],
    },
    {
      title: "远程名全是非法字符",
      published: [new McpPublishedTool({ name: "***", inputSchema: { type: "object" } })],
      policies: [new McpToolPolicy({ remoteName: "***", effect: "read" })],
    },
  ] as const;
  let index = 0;
  for (const item of cases) {
    index += 1;
    const connection = new FakeConnection(item.published);
    const runtime = fakeRuntime(connection, item.policies);
    const registry = new ToolRegistry();
    runtime.install(registry);
    const result = await invoke(registry, "connect_mcp", { alias: "fake" });
    step(
      index,
      `${item.title} → ${resultLine(result)}；暴露名=${mcpNames(registry).join(" ") || "(无)"}`,
    );
    await runtime.close();
  }
  // 与内置工具同名：alias 前缀让远程 read_file 不会顶替本地能力。
  const connection = new FakeConnection([
    new McpPublishedTool({ name: "read_file", inputSchema: { type: "object" } }),
  ]);
  const runtime = fakeRuntime(connection, [
    new McpToolPolicy({ remoteName: "read_file", effect: "read" }),
  ]);
  const registry = new ToolRegistry();
  registry.register({
    name: "read_file",
    description: "Builtin read_file.",
    inputSchema: (await import("zod")).z.strictObject({}),
    effect: "read",
    handler: () => toolSuccess("builtin"),
  });
  runtime.install(registry);
  await invoke(registry, "connect_mcp", { alias: "fake" });
  step(4, `内置 read_file 仍在：${registry.names.includes("read_file")}`);
  step(5, `远程同名工具落到：${mcpNames(registry).join(" ")}`);
  step(6, `内置调用结果：${(await invoke(registry, "read_file", {})).content}`);
  await runtime.close();
}
```

后半段是最值得留意的部分：先注册一个**内置** `read_file`，再连一个也发布 `read_file` 的远程 server。输出见"名称隔离必须在发布前完成"那一节 —— 内置的还在，远程的落到 `mcp__fake__read_file`，调 `read_file` 拿到的是 `builtin`。

**没有前缀的话，这里会发生什么？** `registerMany` 会报 `tool already registered: read_file`，整条连接被拒。也就是说远程 server 只要发布一个和你内置工具同名的工具，就能让你连不上。有了前缀，两者天然共存。

### 实验 8（failure）：三类故障、三种状态迁移

```typescript
// ---------- 实验 8（failure）：三类故障、三种状态迁移 ----------
if (section("failure")) {
  const cases = [
    ["fail", {}, "远程返回 isError"],
    ["delay", { milliseconds: 300 }, "tools/call 超时"],
    ["terminate", {}, "子进程退出"],
  ] as const;
  let index = 0;
  for (const [tool, argumentsValue, title] of cases) {
    index += 1;
    const registry = new ToolRegistry();
    // toolTimeoutSeconds 压到 0.05s，让 delay 稳定超时。
    const runtime = stdioRuntime(demoSpec("demo_alpha", "alpha", 0.05));
    runtime.install(registry);
    try {
      await invoke(registry, "connect_mcp", { alias: "demo_alpha" });
      const before = mcpNames(registry).length;
      const result = await invoke(registry, `mcp__demo_alpha__${tool}`, argumentsValue);
      step(index, `${title} → ${result.content}`);
      console.log(
        `      连接前动态工具=${before}，调用后=${mcpNames(registry).length}，仍连接=${runtime.connectedAliases.join(" ") || "(无)"}`,
      );
      console.log(`      结果里是否出现 server-private-detail：${result.content.includes("server-private-detail")}`);
    } finally {
      await runtime.close();
    }
  }
  // 空闲时 transport 断掉：Watchdog 撤销该 alias 全部工具。
  const connection = new FakeConnection([
    new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } }),
  ]);
  const runtime = fakeRuntime(connection, [
    new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
  ]);
  const registry = new ToolRegistry();
  runtime.install(registry);
  await invoke(registry, "connect_mcp", { alias: "fake" });
  step(4, `连接成功，动态工具=${mcpNames(registry).join(" ")}`);
  connection.failTransport();
  await new Promise((resolve) => setTimeout(resolve, 50));
  step(5, `transport 断掉后动态工具=${mcpNames(registry).join(" ") || "(无)"}，仍连接=${runtime.connectedAliases.join(" ") || "(无)"}`);
  step(6, `失效调用 → ${resultLine(await invoke(registry, "connect_mcp", { alias: "fake" }))}（可重连）`);
  await runtime.close();
}
```

`toolTimeoutSeconds` 压到 0.05 秒、`delay` 传 300 毫秒 —— 这个组合让超时稳定触发，不依赖机器速度。输出见"每个 connection 有独立的生命周期"那一节。

第 5 步之前那句 `await new Promise(... 50)` 是必要的：Watchdog 是异步的，`failTransport()` 只是让 promise 结算，撤销动作要等微任务队列跑完。

### 实验 9（approval）：连接和 external 远程工具都要审批

这是唯一走完整 `buildAgent(P19, ...)` 组合根的失败侧实验。

```typescript
// ---------- 实验 9（approval）：连接和 external 远程工具都要审批 ----------
if (section("approval")) {
  // 只放行 connect_mcp，观察 external 远程工具被拒后远程零调用。
  await withTempDir(async (root) => {
    const connection = new FakeConnection([
      new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } }),
      new McpPublishedTool({ name: "terminate", inputSchema: { type: "object" } }),
    ]);
    const factory = new FakeFactory(connection);
    const runtime = fakeRuntime(
      connection,
      [
        new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
        new McpToolPolicy({ remoteName: "terminate", effect: "external" }),
      ],
      factory,
    );
    const model = new ScriptedModel([
      {
        message: assistantMessage(null, [toolCall("c1", "connect_mcp", '{"alias":"fake"}')]),
        finishReason: "tool_calls",
      },
      {
        message: assistantMessage(null, [
          toolCall("c2", "mcp__fake__lookup", "{}"),
          toolCall("c3", "mcp__fake__terminate", "{}"),
        ]),
        finishReason: "tool_calls",
      },
      { message: assistantMessage("done"), finishReason: "stop" },
    ]);
    const approval = new RecordingApproval("connect_mcp");
    const { deps, close } = await makeDeps(root, model, runtime, approval);
    const runner = buildAgent(P19, deps);
    try {
      const result = await runner.run("connect then call");
      const toolResult = (id: string): string =>
        result.history.find((message) => message.role === "tool" && message.toolCallId === id)
          ?.content ?? "(无)";
      step(1, `被询问审批的工具：${approval.asked.join(" ")}`);
      step(2, `read 类远程工具未询问，直接放行：${toolResult("c2").slice(0, 60)}`);
      step(3, `external 远程工具被拒 → ${toolResult("c3")}`);
      step(
        4,
        `模型发起 2 次远程调用，实际到达远程 callTool：${connection.callCalls} 次（被拒那次没出网）`,
      );
      step(5, `factory.open 次数：${factory.openCalls}`);
    } finally {
      await runner.close();
      await close();
    }
    step(6, `Runner 关闭后 connection.close 次数：${connection.closeCalls}`);
  });
}
```

`RecordingApproval("connect_mcp")` 只放行 `connect_mcp`，其他一律拒。输出：

```text
===== approval =====
[1] 被询问审批的工具：connect_mcp mcp__fake__terminate
[2] read 类远程工具未询问，直接放行：{"content":[],"server_alias":"fake","status":"ok","structure
[3] external 远程工具被拒 → Error [permission_denied]: denied by probe
[4] 模型发起 2 次远程调用，实际到达远程 callTool：1 次（被拒那次没出网）
[5] factory.open 次数：1
[6] Runner 关闭后 connection.close 次数：1
```

逐条读：

- **[1]** 只有两个工具被问：`connect_mcp`（本地管理工具，effect `external`）和 `mcp__fake__terminate`（远程工具，本地 policy 给的 effect 是 `external`）。规则命中的依据是 **effect**，跟工具是本地还是远程无关。
- **[2]** `mcp__fake__lookup` 完全没进审批 —— 它的 effect 是 `read`。
- **[3]** `terminate` 被拒，错误码是 `permission_denied`。
- **[4]** 模型发起了 2 次远程调用，实际到达 `callTool` 的只有 1 次。**被拒的那次没有出网。** 权限检查在 `prepare` 之后、`invoke` 之前，所以拒绝发生在任何 I/O 之前。
- **[5][6]** 一次连接、一次关闭。`runner.close()` 通过 `resources` 逆序把 MCP 连接关掉了，不需要手工调 `mcpRuntime.close()`。

### 实验 10（scope）：MCP 只属于 Lead

```typescript
// ---------- 实验 10（scope）：MCP 只属于 Lead ----------
if (section("scope")) {
  await withTempDir(async (root) => {
    const connection = new FakeConnection([
      new McpPublishedTool({ name: "lookup", inputSchema: { type: "object" } }),
    ]);
    const runtime = fakeRuntime(connection, [
      new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
    ]);
    const model = new ScopedModel();
    const { deps, close } = await makeDeps(root, model, runtime);
    const runner = buildAgent(P19, deps);
    try {
      const result = await runner.run("exercise child scopes");
      // 等 Teammate 那一轮真正发出请求。
      const deadline = Date.now() + 8000;
      while (model.childTools.length < 2 && Date.now() < deadline) {
        await new Promise((resolve) => setTimeout(resolve, 100));
      }
      const leadNames = model.leadTools[0] ?? [];
      step(1, `Lead 工具数=${leadNames.length}，含 connect_mcp=${leadNames.includes("connect_mcp")}`);
      step(2, `Lead 最终回答：${String(result.finalText)}`);
      for (const child of model.childTools) {
        const mcp = child.names.filter(
          (name) => name.startsWith("mcp__") || name === "connect_mcp" || name === "disconnect_mcp",
        );
        step(
          child.who === "Subagent" ? 3 : 4,
          `${child.who} 工具数=${child.names.length}，其中 MCP 相关=${mcp.length === 0 ? "0（看不到）" : mcp.join(" ")}`,
        );
      }
    } finally {
      await runner.close();
      await close();
    }
  });
}
```

Lead 一次性发起 `task`（起 Subagent）和 `spawn_teammate`（起 Teammate），三侧的工具面都被 `ScopedModel` 记下来。输出见"MCP 工具只属于 Lead"那一节。

那个 `while` 轮询是等 Teammate 真正发出请求 —— Teammate 是异步起的，Lead 的 `run()` 返回时它可能还没跑到 `complete()`。

### 实验完成后清理

```powershell
Remove-Item 'F:\笔记\Agent实操\code\scripts\probe-ch19.mts'
```

实验里的临时目录由 `withTempDir` 的 `finally` 自动删掉，不用手工清。实验 1 和 8 启动的 `demo.ts` 子进程也由 `runtime.close()` 收回；跑完可以用 `Get-Process node` 确认一下没有残留。

---

## 这章刻意没有做什么

一个功能不做，往往比做了更需要解释。P19 明确排除下面这些：

| 没做 | 为什么 |
| --- | --- |
| 进程内 mock `MCPClient` 或 handler 字典兼容层 | 那不是 MCP。见"进程内 handler 不是 MCP" |
| 模型可控的 command / args / cwd | 等于给模型任意代码执行权。配置只能来自本地 allowlist |
| HTTP / SSE / WebSocket transport | 只做 stdio。多一种 transport 就多一套超时、重连和鉴权语义 |
| OAuth、令牌刷新、远程鉴权 | 凭据管理是独立课题，混进来会淹没动态工具池这个主题 |
| server push、resources、prompts、sampling | 只做 tool 这一类原语。其余能力会绕过本地权限边界进入模型上下文 |
| 从远程 description / annotation 推断权限 | 远程文本是数据。见实验 5 |
| 自动修复名称碰撞 | 静默改名会让"你调的到底是哪个工具"不可知。碰撞就拒整条连接 |
| 自动接受未配置的远程工具 | 集合必须精确相等。见实验 4 |
| 外部 HTTP / 文件 JSON Schema reference | 一个 schema 就能变成任意网络/文件访问入口 |
| 自动重试、静默重连、跨 connection 迁移调用 | 失败必须对模型可见。静默重试会让副作用发生次数不可知 |
| 向 Subagent / Teammate 传播 connection | 见"MCP 工具只属于 Lead" |
| 把 stdio 子进程说成安全沙箱 | 它不是。子进程有和你同等的本机权限 |

最后一条特别要强调。`connect_mcp` 启动的是一个**和你的 Agent 同等权限**的本机进程。allowlist 限制的是"能启动哪些命令"，不是"启动后能做什么"。真正的隔离要靠容器或专门的沙箱机制，不在本章范围内。

还有一条不是"不做"，而是"做不到"：本章不承诺远程副作用的 exactly-once。`mcp_timeout` 意味着你不知道对方是否已经执行完了 —— 撤销连接只保证本地状态干净，不保证远程什么都没发生。这就是超时后必须让模型看到错误、而不是静默重试的原因。

---

## 与 Claude Code 的差异

参照外部仓库 [`shareAI-lab/learn-claude-code`](https://github.com/shareAI-lab/learn-claude-code) 的 `s19_mcp_plugin/README.md`、[Claude Code 官方 MCP 文档](https://code.claude.com/docs/en/mcp) 和 [MCP 规范](https://modelcontextprotocol.io/)。

### 我们简化了的地方

| 维度 | Claude Code | 本章 |
| --- | --- | --- |
| transport | 6 种（stdio / SSE / HTTP / WebSocket / sse-ide / sdk），本地批量 3、远程批量 20 并发 | 只有 stdio，无并发批量控制 |
| 配置来源 | 多层合并：connector < plugin < user settings.json < 已批准的 project `.mcp.json` < `settings.local.json`；企业 `managed-mcp.json` 存在时排除其他来源 | 固定本地 allowlist，无合并 |
| 工具池组装 | `assembleToolPool()` 用 `uniqBy([...builtInTools.sort(byName), ...filteredMcpTools.sort(byName)], "name")`，内置优先、两类分开排序以保护缓存断点 | ToolRegistry 版本 + Snapshot；请求内不可变，不依赖全局排序 |
| 鉴权 | OAuth + PKCE + 令牌自动刷新；401 触发重认证 | 无 |
| 错误恢复 | 精细错误分类；终局性错误连续 3 次后关闭重连；stdio 断连按 SIGINT → SIGTERM → SIGKILL 顺序杀进程 | 启动/调用超时 + 故障原子撤销 + Runner 统一关闭，无重连 |
| 超时 | 可配置，默认约 28 小时 | 每个 spec 显式声明 `startupTimeoutSeconds` / `toolTimeoutSeconds` |
| 反向通知 | 支持 Channel，服务器可通过 `notifications/claude/channel` 推送 | 无 |
| 子执行者 | MCP 工具对主 Agent 和子 Agent 都可用，子 Agent 继承父配置 | Lead-only，不继承 |

### 我们比教学版更严的地方

**工具名集合必须精确相等。** CC 允许远程工具集合与本地配置存在差异。本章要求 published 集合与 policy 集合**完全相同**，多一个少一个都拒整条连接。理由见实验 4：这是唯一能让"远程变了"立刻可见的方式。

**远程 annotation 完全不参与权限判定。** CC 的 MCP 工具可以声明 `readOnly` / `destructive` 等权限需求并参与检查。本章明确 `McpToolPolicy.effect` 是唯一事实源 —— 远程 `(readOnly)` 标注不能把本地的 `external` 降级。

**名称碰撞不自动修复。** 归一化后撞名直接拒整条连接，不加后缀、不改名。

**外部 schema 引用一律拒绝。** 三个引用关键字（`$ref` / `$dynamicRef` / `$recursiveRef`）都检查，只允许 `#` 开头的文档内 fragment。

**Ajv 一律 strict。** 三个 dialect 全部 `strict: true`，拼错的关键字会报错而不是被忽略。

---

## ai-agent-book 第四章对照

对照 ai-agent-book 第四章正文与两个配套实验：`chapter4/active-tool-discovery`、`chapter4/active-tool-selection`（这是外部仓库，不在本教程目录下）。

### 它是怎么说的

第四章把 MCP 归纳为三类原语：**tools**（可执行操作）、**resources**（应用可读取的数据）、**prompts**（用户可选用的模板）。本章只实现 tool 这一类。

它还强调一个容易被合并的区分：**是否采用 MCP 作为互操作协议，与会话开始时是否暴露全部 MCP 工具定义，是两个独立决策。**

配套实验给出了成本数据：

| 实验 | 配置 | 结果 |
| --- | --- | --- |
| `active-tool-selection`（离线基准） | 35 个工具，全量注入 | 3,857 schema token |
| | 35 个工具，`retrieval` top-5 | 551 token，召回率 100% |
| | 200 个工具，全量注入 | 20,258 token |
| | 200 个工具，`retrieval` top-5 | 约 540 token |
| `active-tool-discovery`（qwen3:4b 真实验证） | 126 个工具，对照组全量 | 每任务 system prompt 50,352 token；总耗时 2,590.820s |
| | 126 个工具，实验组动态 | 初始 1,251 token，三任务动态注入合计 12,838 token；总耗时 808.926s（快约 3.20 倍） |
| | 两组完成率 | 都是 3/3，准确率都是 100% |

最后一行值得注意：**预期中的准确率提升没有出现。** 动态工具池的收益主要是上下文成本和延迟，不是"更准"。在强模型上，无关工具带来的干扰没有想象的那么大。

### 两个概念必须分开

这是我读第四章最大的收获：

```text
动态工具发现 / 检索
  解决的问题：哪些工具定义进入上下文，以多少 token 进入
  候选越多，全量注入越贵
  手段：embedding 索引、Tool Search、defer_loading
  ↓
  本章不做

Registry Snapshot
  解决的问题：一轮请求进行中工具集合变化时，请求如何保持一致
  模型看到的 schema 必须覆盖它随后发出的所有 tool call
  手段：每次模型请求边界生成不可变快照
  ↓
  本章做的是这个
```

两者可以组合，而且**应该**组合。即使将来加入按需检索，也必须在每次模型请求边界生成不可变快照 —— 否则工具列表刚出现在模型回复里、下一轮执行前又被动态变更，调用就失去可解释性。

### 逐条对照

| 第四章的设计点 | 本章对应 | 差异与边界 |
| --- | --- | --- |
| tools / resources / prompts 三类原语 | 只实现 tool | resources / prompts 不发布、不传播 |
| 按需检索少量工具 schema | `connect_mcp` / `disconnect_mcp` + 动态注册撤销 | 不实现 embedding 索引、Tool Search、`defer_loading` |
| 主动发现降低上下文 token | 连接前只暴露 2 个管理工具 | 目标不同：本章重点是生命周期和请求一致性 |
| 初始 query 一次预筛选 | 每次连接固定 `tools/list` + policy 精确匹配 | 不在模型请求之间做候选检索 |
| 远程工具描述不可信 | `McpToolPolicy.effect` 是唯一权限事实源 | 与第四章安全基线一致，并加了精确集合匹配 |
| 模型看到的 schema 必须与实际执行参数一致 | Ajv 编译 + `inputSchemaJson` 原样透传 | 不一致就拒绝发布，不静默修正 |
| 实验证据分开 | `test:ch19` 是离线确定回归 | 机制自检、离线基准、真实实验三者互补 |

第四章还提到一个和本章直接冲撞的观察：**初始查询的检索预筛选只做一次，多步跨领域任务容易漏掉第二个子任务所需的工具。** 本章的应对不是做跨请求检索，而是让 allowlist 精确声明工具集合，再按 alias 原子发布 —— 需要新能力时，模型显式 `connect_mcp`，多花一轮，但不会"漏掉"。

---

## 前后版本的变化

| 维度 | 第 18 章 | 第 19 章 |
| --- | --- | --- |
| 工具集合 | 启动时固定 | Lead 可按需连接/断开，工具集合在轮次之间变化 |
| 外部协议 | 无 | 官方 MCP SDK + stdio + `Client` |
| server 选择 | 不适用 | 本地 allowlist alias，模型只能提供 alias |
| 权限事实源 | 本地 `ToolDefinition.effect` | 仍是本地 effect；远程文本完全不参与 |
| 参数验证 | 静态 Zod schema | 远程 JSON Schema + Ajv strict 编译 |
| 名称隔离 | 内置工具名唯一 | `mcp__{alias}__{normalized}`，4 步归一化 + 64 字符上限 |
| registry 变更 | 只在启动时注册 | `registerMany` / `unregisterMany` 原子批量迁移 + 单调 `version` |
| 单轮一致性 | 每次请求取快照（ch18 已有） | 同上，未变 |
| 生命周期 | Runner 关闭既有资源 | 增加独立 connection、stdio 子进程、连接级 Watchdog、`pendingCloses` 重试 |
| 子执行者工具 | 裁剪固定集合 | 保持不变，不继承 MCP |

**要点：单轮一致性那一行没有变化。** 每次模型请求取一次 `snapshot()` 是第 18 章就有的机制（`loop.ts:406`，连注释都一样）。本章新增的是让工具集合**能安全地在轮次之间变化**的那些东西 —— `version`、`registerMany` / `unregisterMany`、对象身份撤销、`inputSchemaJson`、`source`。

Task DAG、Mailbox、typed Protocol、计划门控、work stealing、Worktree、权限和 Hook 都没有另起一套实现。MCP 通过同一个 `ToolRegistry`、同一个 `PermissionPolicy`、同一个 Agent Loop 进入现有执行链。

---

## 配套代码索引

本章代码全部在 `code/chapters/ch19/` 下。行数是给你估工作量用的 —— 806 行的 `mcp-tools.ts` 里，值对象定义和错误处理占了一大半。

**新增文件：**

| 文件 | 行数 | 本章负责什么 |
| --- | --- | --- |
| [src/features/mcp-tools.ts](code/chapters/ch19/src/features/mcp-tools.ts) | 806 | `McpRuntime` 全部逻辑：事务式 `#connect`、逆序 `#disconnect`、`#publishDefinitions` 的 policy 精确匹配与 schema 编译、`#watchConnection` Watchdog、`#invokeRemote` 三类故障处置、`#dropFailedConnection` 对象身份保护、`exposedToolName` 归一化、`remoteError` 脱敏信封；以及 `McpServerSpec` / `McpToolPolicy` / `McpPublishedTool` / `McpCallResult` 四个值对象 |
| [src/adapters/mcp-client.ts](code/chapters/ch19/src/adapters/mcp-client.ts) | 347 | `StdioMcpConnectionFactory` / `StdioMcpConnection`：官方 SDK 的 `Client` + `StdioClientTransport`、`#tail` 串行队列、分页 `listTools`、`waitForFailure`、SDK 错误映射、`stderr: "ignore"` |
| [src/adapters/mcp-schema.ts](code/chapters/ch19/src/adapters/mcp-schema.ts) | 76 | `AjvMcpSchemaValidator`：四道检查 + `selectAjv` 按 `$schema` 选 dialect + `requireLocalReferences` |
| [src/mcp-servers/demo.ts](code/chapters/ch19/src/mcp-servers/demo.ts) | 63 | 演示 server，四个工具对应四种确定性行为 |
| [src/chapters/ch19.ts](code/chapters/ch19/src/chapters/ch19.ts) | 5 | 固定章节入口 |

**本章重点改动的文件：**

| 文件 | 行数 | 本章改了什么 |
| --- | --- | --- |
| [src/core/tools.ts](code/chapters/ch19/src/core/tools.ts) | 380 | 新增 `version`、`registerMany`、`unregisterMany`、`#sourceDefinitions` 身份撤销、`inputSchemaJson`、`source`；`openAITools()` 优先用 `inputSchemaJson` |
| [src/bootstrap.ts](code/chapters/ch19/src/bootstrap.ts) | 501 | 一对 mcp 双向守卫（115-120）、Lead-only `install`（353-357）、`resources` 加入 mcpRuntime（381-387）、`mcp-external-approval` 规则（481-494） |
| [src/cli.ts](code/chapters/ch19/src/cli.ts) | 412 | `createMcpRuntime()` 构造本地 allowlist（265 行起）、两类 wakeup 绑定、`TerminalApprovalProvider`、fallback 关闭循环 |
| [src/core/profiles.ts](code/chapters/ch19/src/core/profiles.ts) | 326 | 新增 `"mcp"` 能力位；`P19 = P18 + mcp` |
| [src/core/loop.ts](code/chapters/ch19/src/core/loop.ts) | 789 | **未改动。** `loop.ts:406` 的 `snapshot()` 在 ch18 就有 |
| [src/config.ts](code/chapters/ch19/src/config.ts) | 78 | 未新增 MCP 配置项 —— allowlist 在代码里，不从环境读 |

**测试（本章 4 个文件，共 20 个测试）：**

| 文件 | 行数 | 断言什么 |
| --- | --- | --- |
| [tests/ch19-registry.test.ts](code/chapters/ch19/tests/ch19-registry.test.ts) | 104 | 批量变更的原子性、对象身份安全、版本迁移、快照封闭；一次模型回复期间快照保持不变 |
| [tests/ch19-mcp.test.ts](code/chapters/ch19/tests/ch19-mcp.test.ts) | 365 | 两个真 stdio server 的同名工具隔离；三种非法情形各自拒整条连接（invalid schema / external reference / normalization collision）；参数先校验后出网且本地 policy 权威；空闲时连接故障撤销工具；close 失败重试；远程错误/超时/进程退出的映射与脱敏；取消进行中的调用不破坏连接；取消启动会关闭连接 |
| [tests/ch19-bootstrap.test.ts](code/chapters/ch19/tests/ch19-bootstrap.test.ts) | 469 | P19 起才要求 mcpRuntime；只装 Lead 且经 AgentRunner 关闭连接；连接被拒时 transport 保持关闭；external 远程工具被拒时远程零调用；断开被拒时连接保持打开；真实 Subagent / Teammate bootstrap 拿不到 MCP |
| [tests/ch19-entry.test.ts](code/chapters/ch19/tests/ch19-entry.test.ts) | 52 | 缺配置的报错发生在 Git 与 MCP 状态之前；非 Git workspace 被拒之前先关闭 model |

### 读代码的建议顺序

不要从 806 行的 `mcp-tools.ts` 开头读。按这个顺序，每一步都能接着上一步的疑问：

1. **`src/core/profiles.ts` 的 `P19`** —— 先确认差量只有一个能力位。一分钟。
2. **`src/mcp-servers/demo.ts`（全文 63 行）** —— 先看被测对象。四个工具就是四种要验证的行为。
3. **`src/features/mcp-tools.ts` 的四个值对象（49 / 82 / 148 / 181 行）** —— `McpToolPolicy`、`McpServerSpec`、`McpPublishedTool`、`McpCallResult`。这是整章的词汇表，读完你就知道哪些字段来自本地、哪些来自远程。
4. **`src/cli.ts` 的 `createMcpRuntime()`（265 行起）** —— allowlist 长什么样，policy 怎么写。这是"配置只能来自本地"的落点。
5. **`src/features/mcp-tools.ts` 的 `#connect`（412 行起）** —— 四步事务加三处清理。看清楚哪一步失败要关子进程。
6. **`src/features/mcp-tools.ts` 的 `#publishDefinitions`（516 行起）** —— policy 精确匹配、名称碰撞、schema 编译、`effect` 来源。本章最密集的一段。
7. **`src/adapters/mcp-schema.ts`（全文 76 行）** —— 四道检查。短，但每一行都在挡一类攻击。
8. **`src/core/tools.ts` 的 `registerMany` / `unregisterMany`** —— 现在看"先全验再全写"和对象身份比对，你已经知道它们在防什么了。
9. **`src/features/mcp-tools.ts` 的 `#invokeRemote`（591 行起）和 `#dropFailedConnection`（642 行起）** —— 三类故障、三种处置，以及重连时序的保护。
10. **`src/adapters/mcp-client.ts` 的 `#tail` 串行队列和 `waitForFailure`** —— 传输层细节，最后读。
11. **`src/bootstrap.ts` 的 353-357 和 381-387 行** —— Lead-only 安装与逆序关闭。收尾细节。

可以跳过：`mcp-client.ts` 的 SDK 错误映射部分（`mapSdkError` 及其辅助函数）—— 逻辑独立，是把 SDK 异常翻译成本章的两个错误类；`cli.ts` 的参数解析。

---

## 运行与验证

五道门，按顺序过。全部在 `F:\笔记\Agent实操\code` 下执行：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch19
npm run typecheck
npm run lint
npm run format:check
npm run build
```

我这边的实测结果：

```text
Test Files  63 passed (63)
     Tests  415 passed (415)
  Duration  15.64s
```

`test:ch19` 会连带跑前面所有章节的测试，所以是 63 个文件、415 个测试，不是只有 ch19 那 4 个。这是有意的 —— 第 19 章新增了 `ToolRegistry` 的批量接口，必须确认它没有破坏前 18 章的注册行为。

只想看本章那 20 个测试：

```powershell
npx vitest run chapters/ch19/tests/ch19-registry.test.ts chapters/ch19/tests/ch19-mcp.test.ts chapters/ch19/tests/ch19-bootstrap.test.ts chapters/ch19/tests/ch19-entry.test.ts
```

```text
Test Files  4 passed (4)
     Tests  20 passed (20)
  Duration  3.37s
```

`typecheck`、`lint`、`format:check`、`build` 我这边都是干净通过。

本章测试注入了确定的 UUID、UTC clock 和模型替身，**不依赖真实 OpenAI、不依赖网络**。但有两处例外，而且是必要的例外：

- `ch19-mcp.test.ts` 里那几个 stdio 测试会 **spawn 真实的 `demo.ts` 子进程**。MCP 是跨进程协议，用假连接测不出握手、分页、进程退出这些行为。
- `maps remote error, timeout, and process exit` 那个测试有真实等待（1412ms），因为超时必须真的到时间。

关键断言都是具体的业务结果：

- 两个真 stdio server 各自返回自己的 `label`，同名 `lookup` 没有串线；
- 三种非法情形（invalid schema / external `$ref` / 归一化撞名）各自拒**整条**连接，registry 回到 `["connect_mcp","disconnect_mcp"]`，且 `closeCalls === 1`；
- 参数不合规时远程 `callTool` 计数保持 0，本地 policy 的 effect 不被远程 description 改变；
- 空闲时连接故障会撤销该 alias 全部工具，`connectedAliases` 变空，之后还能重连；
- `close()` 失败的连接留在 `pendingCloses`，再次 `close()` 会重试；
- 远程错误 / 超时 / 进程退出映射到三个不同错误码，且结果里不含 `server-private-detail`；
- 取消进行中的调用不破坏连接；取消启动会关闭已开的连接；
- P19 之前传 `mcpRuntime` 报错，P19 不传也报错（双向）；
- MCP 只装到 Lead，真实的 Subagent / Teammate bootstrap runner 里一个 MCP 工具都没有；
- 审批被拒时：连接的 transport 保持关闭、external 远程工具零调用、断开被拒时连接保持打开；
- 缺配置的报错发生在创建任何 Git 或 MCP 状态之前。

---

## 本章小结

### 三句话版本

第一，MCP 接入真正需要重构的不是"多写一个 connect 函数"，而是**工具集合本身开始变化以后，整个 Agent Loop 仍要保持可解释**。

第二，Published MCP Tool 是**外部数据**，Exposed MCP Tool 是**本地能力**。两者之间必须有一道由本地完全控制的发布关卡。

第三，远程说的话一律是数据。名字要归一化、schema 要重新编译、effect 只能本地给、错误只能本地造信封。

### 一定要记住的十条

1. **server 配置只能来自本地 allowlist。** 模型只能提供 alias，命令、参数、超时都由本地声明（《连接配置只能来自本地 allowlist》）。
2. **Published ≠ Exposed。** 远程声明只是候选，经过 policy 精确匹配、名称归一化、schema 编译之后才成为本地能力（《先统一六个名字》）。
3. **本地 policy 的 effect 是唯一权限事实源。** 远程 description 里写 `(readOnly)` 也改不了 `external`（实验 5）。
4. **published 集合必须与 policy 集合精确相等。** 多一个少一个都拒整条连接，不取交集（实验 4）。
5. **名称隔离在发布前完成。** `mcp__{alias}__{normalized}`、4 步归一化、64 字符上限、碰撞拒整条连接（实验 7）。
6. **schema 是执行边界。** Ajv strict 编译，拒外部引用，坏参数在本地拦下，远程 `callTool` 计数为 0（实验 6）。
7. **注册和撤销是原子版本迁移。** 先全验再全写，撤销按对象身份而非名字，绝不留半连接状态（实验 3）。
8. **一轮之内工具面是常量。** 每次模型请求取一次快照，新工具下一轮才可见。`unknown_tool` 是定义，不是 bug（实验 2）。
9. **三类故障三种处置。** 远程 `isError` 保留连接；超时和 transport 断开丢弃连接；错误信封的每个字段都由本地构造（实验 8）。
10. **MCP 只属于 Lead。** Subagent / Teammate 的 factory 拿不到 `mcpRuntime`，不是"选择不用"（实验 10）。

### 一张图回顾整条链路

```text
  ① 本地声明                    createMcpRuntime()（cli.ts:265）
  ──────────                    ┌────────────────────────────────┐
                                │ McpServerSpec × 2               │
                                │   alias: demo_alpha / demo_beta │
                                │   command / args（本地固定）     │
                                │   startupTimeoutSeconds: 5      │
                                │   toolTimeoutSeconds: 5         │
                                │   toolPolicies:                 │
                                │     lookup    → read            │
                                │     fail      → read            │
                                │     delay     → read            │
                                │     terminate → external        │
                                └───────────────┬────────────────┘
                                                │
  ② 只给 Lead                                   ▼  install(tools)
  ──────────                    ┌────────────────────────────────┐
                                │ Lead registry                   │
                                │   connect_mcp     (external)    │
                                │   disconnect_mcp  (external)    │
                                │                                 │
                                │ Subagent / Teammate factory     │
                                │   闭包里没有 mcpRuntime          │
                                └───────────────┬────────────────┘
                                                │
  ③ 事务式连接                                  ▼  connect_mcp {alias}
  ──────────      ┌──────────────────────────────────────────────┐
                  │ ① factory.open(spec)      启子进程 + 握手      │
                  │ ② connection.listTools()  Published Tool 集合  │
                  │ ③ #publishDefinitions()                       │
                  │      policy 集合精确相等？                     │
                  │      归一化后有碰撞？64 字符超了？              │
                  │      Ajv strict 能编译？外部 $ref？            │
                  │      effect ← 本地 policy（不是远程）           │
                  │ ④ registry.registerMany()  一次原子注册        │
                  │                                              │
                  │ 全过 → 写入 #connections + 启 Watchdog         │
                  │ 任一步失败 → 关子进程，registry 一个都不留      │
                  └───────────────┬──────────────────────────────┘
                                  │
  ④ 请求边界                      ▼  每次模型请求
  ──────────      ┌──────────────────────────────────────────────┐
                  │ const tools = this.#tools.snapshot()          │
                  │   （loop.ts:406，ch18 就有）                   │
                  │                                              │
                  │ 这一轮所有工具调用都用这份快照                  │
                  │ 中途 connect 改的是 live registry              │
                  │ → 同轮抢跑 = unknown_tool，下一轮才可见         │
                  └───────────────┬──────────────────────────────┘
                                  │
  ⑤ 调用与失败                    ▼  mcp__alias__tool
  ──────────      ┌──────────────────────────────────────────────┐
                  │ prepare()  JSON 解析 + Ajv 校验 → 坏参数不出网  │
                  │ 权限检查   effect=external → 问人；read → 放行  │
                  │ invoke()   #invokeRemote → callTool            │
                  │                                              │
                  │ isError:true   → mcp_remote_error   连接保留   │
                  │ 超时           → mcp_timeout        连接丢弃   │
                  │ transport 断   → mcp_connection_lost 连接丢弃   │
                  │ 空闲时断开     → Watchdog 主动撤销             │
                  │                                              │
                  │ 错误信封字段全部本地构造，远程内容不进上下文     │
                  └───────────────┬──────────────────────────────┘
                                  │
  ⑥ 关闭                          ▼  runner.close()
  ──────────      ┌──────────────────────────────────────────────┐
                  │ resources 逆序 → mcpRuntime 最先关            │
                  │ 锁内：撤销全部 definitions，清 #connections     │
                  │ 锁外：连接逆序 close()，失败进 pendingCloses    │
                  │ 多个失败 → AggregateError 一次报全部           │
                  └──────────────────────────────────────────────┘
```

六个编号就是本章六条硬规则的位置：① 配置只能本地给 ② 能力只给 Lead ③ 发布是一次事务 ④ 一轮之内工具面是常量 ⑤ 校验在出网之前、错误信封由本地造 ⑥ 关闭逆序且不泄漏子进程。

### 检查你是否真的读懂了

不看前文，试着回答。答不上来的，括号里是对应小节或实验：

1. Published MCP Tool 和 Exposed MCP Tool 差在哪？为什么本章要把这两个词分开？（《先统一六个名字》、《ai-agent-book 第四章对照》）
2. 远程 server 告诉你它有一个叫 `deploy` 的工具，`description` 里写着"只读查询"。它会被暴露给模型吗？会以什么 effect 暴露？（《远程 description 不能授予权限》、实验 5）
3. 本地 policy 声明了 `lookup` / `fail` / `delay` / `terminate` 四个，远程只发布了前三个。连接会成功还是失败？如果远程反过来多发布一个呢？为什么不是取交集？（《远程 description 不能授予权限》、实验 4）
4. `mcp__demo_alpha__lookup` 这个名字里，哪一段来自远程、哪一段来自本地？如果远程工具叫 `Look Up!!`，规范化后是什么？（《名称隔离必须在发布前完成》、实验 7）
5. 两个 alias 都连到同一个 demo server，远程工具同名。为什么不冲突？什么情况下才会真的冲突、并让整条连接被拒？（《名称隔离必须在发布前完成》、实验 7）
6. 一个远程工具的 schema 是 `{"type":"string"}`，会发生什么？在哪一步被拦下来？（《JSON Schema 是执行边界，不是提示文本》、实验 6）
7. 模型在同一次回复里先调 `connect_mcp`、紧接着调 `mcp__demo_alpha__lookup`，第二个调用的结果是什么？为什么不是"等一下就好了"？（《为什么新工具只能下一轮才可见》、实验 2）
8. `registerMany()` 注册 4 个工具，第 3 个名字和内置工具撞了。注册完成后 registry 里有几个新工具？`version` 变了吗？（《动态注册必须是原子的》、实验 3）
9. `unregisterMany()` 是按名字撤销还是按对象撤销？如果同名工具被另一条连接重新注册过，按名字撤销会出什么事？（《动态注册必须是原子的》、实验 3）
10. 远程工具返回 `isError: true`，连接会被断开吗？超时呢？transport 挂了呢？三种情况下 `connectedAliases` 里还看得到这个 alias 吗？（《连接是一次事务，断开是它的逆序》、《错误结果必须有界、脱敏》、实验 8）
11. `terminate` 让 server 进程 `process.exit(17)`。这个退出被谁发现？发现之后做了什么？（《每个 connection 有独立的生命周期》、实验 8）
12. 远程错误里带着 `server-private-detail` 这种内部信息，模型会看到吗？错误信封是谁造的？（《错误结果必须有界、脱敏》、实验 8）
13. `connect_mcp` 和 `mcp__demo_alpha__terminate` 都要审批，命中的是同一条规则吗？`mcp__demo_alpha__lookup` 要审批吗？（《远程 description 不能授予权限》、实验 9）
14. Subagent 能调 `connect_mcp` 吗？它能不能用 Lead 已经连上的 `mcp__demo_alpha__lookup`？（《MCP 工具只属于 Lead》、实验 10）
15. Runner 关闭时，`mcpRuntime` 在 `resources` 数组里排第几？为什么关闭顺序要和创建顺序相反？（《连接是一次事务，断开是它的逆序》）
16. `close()` 时某个 `connection.close()` 抛错了，这个对象去哪了？为什么要留着它？（《连接是一次事务，断开是它的逆序》）
17. 本章承诺"远程工具调用只发生一次"吗？超时被判定为失败之后，远程那边的副作用发生了没有？（《这章刻意没有做什么》）

做到这些之后，工具面不再是编译期写死的常量。Lead 可以在运行中把一个外部 MCP server 接进来，让它发布的工具和内置工具一样被审批、被校验、被记账，也可以在它坏掉的时候把它整条摘掉，而不影响别的工具。**外部能力接入变成了一次可回滚的事务，而不是一次改代码。**

到这里，十九章各自解决了一件事：Loop、权限、Hook、记忆、恢复、Task、Cron、团队协作、Worktree、MCP。它们现在还是十九个独立的演进步骤。下一章我们把它们装进同一个 harness，看这些机制放在一起会互相干扰在哪、又要怎么排出一个稳定的启动与关闭顺序。
