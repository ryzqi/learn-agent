前一章把工具从"编译进 Agent"变成了"运行时可插拔"：能连哪些 MCP server 来自本地 allowlist，远程工具算什么权限来自本地 policy，一批工具要么整批发布、要么一个都不发布。

到这里，十九章各自解决了一件事：Loop、权限、Hook、记忆、压缩、恢复、Task、Cron、团队协作、Worktree、MCP。

但它们现在还是十九个独立的演进步骤。

![图片](https://mmbiz.qpic.cn/sz_mmbiz_png/1z26fJxNuiaGka13EGOqz6T9u1QGbeaOMqFM5QaIAdFzuiaNYm8WgxKjpicrhJG3ICHWjkdEG7HyIGbGdqyYjZs0knaHv26ez0eQJT6o0mmOIU/640?from=appmsg&watermark=1&tp=webp&wxfrom=5&wx_lazy=1#imgIndex=0)

每章都有自己的入口、自己的测试、自己的"本章新增"。第 12 章的 Task DAG 和第 14 章的 Cron 从没在同一个进程里同时跑过；第 8 章的上下文压缩和第 11 章的失败恢复，各自测试里都默认"另一个不存在"；第 19 章刚做出来的 MCP 动态工具池，也没遇到过"队友正好卡在等审批"这种局面。

单独看每一章，它们都是对的。放在一起，麻烦不在功能，而在**顺序、共享和边界**：

```text
它们要抢同一个位置——
一次模型请求之前，压缩、动态 Prompt、事件消费、工具快照，谁先谁后？
后台任务完成了、Cron 到点了、队友发消息了，这三条进同一个收件箱还是三个？
Cron 拿到的 JobSupervisor，和后台工具拿到的是同一个对象吗？不是会怎样？
模型能看到"当前连了哪些 MCP server"吗？看到之后它能拿这个当授权理由吗？
进程要关了，MCP 子进程、队友、Cron、后台任务按什么顺序收？其中一个关不掉怎么办？
```

这一章要落地的机制，一句话说清：

```text
P20 不新增任何运行时。
它只声明一件事：前十九章的能力，必须在同一个组合根里同时成立。
所有能力共用一个 AgentRunner、一个 EventInbox、一份工具快照；
依赖有没有正确共享，在启动时用对象身份检查，而不是等运行到一半才出错；
资源按注册顺序的逆序关闭，任何一个关不掉都要汇总报出来，不许静默吞掉。
```

第 20 章在第 19 章全部能力之上只增加一项 `full_harness` capability。它是第 26 项能力，也是唯一一项"不带代码"的能力：没有新模块、没有新工具、没有新 handler。它断言的是**共存**。

---

## 本章导读

### 读完这一章，你能做到什么

- 说清 P20 为什么"没有新代码"也仍然算一章，`full_harness` 这项能力到底断言了什么；
- 读懂组合根那 7 条依赖门禁，并解释对象身份检查为什么必须放在启动时、而不是等运行时报错；
- 背出单一 `AgentRunner` 一个回合的执行顺序，并指出每一步在 `loop.ts` 的哪一行；
- 说清三条不变量：一次模型回复共用一份工具快照、一个 tool call 恰好对应一个 tool result、Prompt 和 Hook 都不是授权边界；
- 解释 `runtime_status` 为什么固定在提示词最后一段、为什么要参与缓存键、为什么不能当授权来源；
- 说清 Lead 那 23 个工具是怎么攒起来的，队友又被裁剪成了什么样；
- 说清三条状态线（SQLite 任务、协议状态机、Worktree 绑定）为什么互不代替；
- 解释后台完成、Cron 触发、队友消息如何汇入同一个 `EventInbox`，以及"至少一次投递 + eventId 去重 + ack"怎么对模型表现成恰好一次；
- 读懂恢复矩阵的四种失败各自怎么处理，以及为什么恢复过程一条都不进 canonical history；
- 自己找出 P19 到 P20 之间一处**真实的行为回退**，并用实验验证它确实回退了；
- 亲手跑通第 20 章，并用 10 个不需要 API key 的实验逐条验证上面每个结论。

### 你需要先具备什么

| 前置 | 具体要求 | 不满足会怎样 |
| --- | --- | --- |
| 第 3 章的权限系统 | 知道 `effect` 分 read/write/execute/external，`ask` 触发终端审批，`deny` 直接拒绝 | 看不懂"deny 强于 ask"那一节，也会误判审批到底问了几次 |
| 第 8、11 章的压缩与恢复 | 知道压缩会重写历史，恢复会重试模型请求 | 恢复矩阵那节没有对照，看不出"为什么恢复不进历史" |
| 第 12 到 18 章的状态机制 | 知道 Task DAG、后台任务、Cron、队友、协议、Worktree 各自存什么 | 三条状态线那节会分不清谁管谁 |
| 第 19 章的 MCP | 知道动态工具池、Registry Snapshot、原子注册 | 第 11 节和实验 4、实验 5 的前提不成立 |
| Node.js 22+ | 本文实测用的是 v25.7.0，仓库要求 `>=20.12` | 依赖装不上，或者 `tsx` 跑不起来 |
| Windows PowerShell | 本文所有命令都是 PowerShell 写法 | 命令要自己翻译成 bash |
| 模型凭证（可选） | 只有"真跑一次"那步需要 | 前 3 步和全部 10 个实验都不需要 |

### 术语速查（本章第一次出现，或本章含义变了的词）

| 词 | 一句话解释 |
| --- | --- |
| **Harness** | 把模型之外的一切装到一起的那层代码：工具、权限、上下文、状态、资源生命周期 |
| **full_harness** | P20 的标记能力，第 26 项。不带任何新代码，只断言前十九章能力同时生效 |
| **组合根（composition root）** | 全程序唯一负责"创建对象并把它们接起来"的地方，本章就是 `bootstrap.ts` |
| **对象身份（object identity）** | 两个变量是不是指向**同一个对象**（`===`），而不是"长得一样" |
| **依赖门禁** | `validateBuildDependencies()`：启动时逐条检查依赖有没有正确共享，不对就直接抛错 |
| **Registry Snapshot** | 一次模型请求及其整条回复共用的那一份不可变工具集合 |
| **canonical history** | 真正会喂给模型、也会被压缩和持久化的那条消息序列；恢复过程的中间态不在里面 |
| **runtime_status** | 动态 Prompt 固定的最后一段，只汇总"后台还有没有活""MCP 连了哪些 alias" |
| **EventInbox** | 后台完成、Cron 触发、队友消息共用的类型化收件箱 |
| **at-least-once + 去重** | 事件可能被投递多次，但用 `eventId` 去重后，对模型表现成恰好一次 |
| **ack** | 事件被成功消费后的确认。ack 之前不丢弃，ack 失败要回滚 |
| **逆序关闭** | 关闭顺序 = 注册顺序反过来。后注册的先关，先注册的最后关 |
| **AggregateError** | 多个资源同时关闭失败时，把所有失败汇总成一个错误抛出，不只报第一个 |
| **三条状态线** | SQLite 任务状态、协议状态机、Worktree 绑定；各自独立，互不代替 |
| **plan gate** | 第 16 章的权限规则：队友提交计划并获批之前，它的副作用工具被拦住 |

### 建议的阅读路线

```text
[1] 先看一次完整回合的轨迹      | 有画面感：23 个工具、连一个 MCP、三个调用各自什么结果
      ↓
[2] full_harness 是标记不是模块  | 本章存在的理由
      ↓
[3] 组合根的对象身份门禁        | 本章第一核心：错误的共享必须在启动时炸
      ↓
[4] 单一 AgentRunner 的执行顺序  | 本章第二核心：谁先谁后
      ↓
[5] 三条不变量                  | 快照 / 配对 / 谁才是授权边界
      ↓
[6] runtime_status 不是权限来源   | P20 唯一新增的模型可见输出
      ↓
[7] 23 个工具面与子执行者裁剪     | 谁能看到什么
      ↓
[8] 三条状态线 + 事件汇聚 + 恢复  | 状态与信号怎么并存
      ↓
[9] MCP 边界，以及一处真实回退    | 本章最需要你自己动手确认的一节
      ↓
[10] 资源注册与逆序关闭          | 进程怎么干净地结束
      ↓
[11] 运行第 20 章 + 10 个实验     | 全部离线，逐条验证上面结论
```

如果你只有 10 分钟，读 `[3]`、`[4]`、`[9]` 三节，再跑实验 2 和实验 4。

---

## 一、先看一次完整回合的轨迹

先不谈架构。看一次真实的 P20 回合里到底发生了什么。

下面这段输出来自本章实验 4（完整脚本在"10 个动手小实验"一节，全部离线、不需要 API key）。它构造了一个模型替身，让它在**同一条回复里**发出三个工具调用：连一个 MCP server、调用那个刚连上的远程工具、往工作区外面写文件。

```text
[1] 模型主请求次数：2
[2] 第 1 次请求的工具数：23
[3] 第 1 次请求里有 mcp__fake__lookup 吗：false
[4] 第 2 次请求的工具数：24
[5] 第 2 次请求里有 mcp__fake__lookup 吗：true
[6] 第 1 次请求的 runtime_status：{"mcp_connections":[],"pending_work":false}
[7] 第 2 次请求的 runtime_status：{"mcp_connections":["fake"],"pending_work":false}
[8] 同一条回复里三个工具调用各自的结果：
      probe-connect → {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup"]}
      probe-mcp     → Error [unknown_tool]: Unknown tool: mcp__fake__lookup
      probe-escape  → Error [permission_denied]: Writing outside the workspace is forbidden
[9] 远程 callTool 实际被调用次数：0
[10] 审批被问到的工具：[]（deny 强于 ask，越界写不进审批）
[11] 最终回答：probe done
[12] close() 之后 McpRuntime.isClosed=true
```

一次回合，六件事同时成立。逐条读：

**工具面是 23 个，不是"一大堆"。** 二十章攒下来的 Lead 工具刚好 23 个，从 `shell` 到 `disconnect_mcp`，这个数字是可以数出来的（实验 10 会一条条列出来）。

**`connect_mcp` 成功了，但新工具这一轮用不上。** 第 8 行里 `probe-connect` 明确返回 `"status":"connected"`，工具列表里也确实有 `mcp__fake__lookup`。可紧接着的 `probe-mcp` 调用同一个名字，拿到的是 `unknown_tool`。这不是 bug——第 2 行和第 4 行说明了原因：第 1 次请求带着 23 个工具，第 2 次才是 24 个。**一次模型回复里的所有 tool call，共用发起那次请求时的那一份工具快照。** 连接改变的是"活注册表"，不是这条回复手里的快照。

**远程 handler 一次都没被真的调用。** 第 9 行 `远程 callTool 实际被调用次数：0`。`unknown_tool` 是在快照上就地拒绝的，请求根本没走到传输层。

**越界写入被拒了，而且没有惊动审批。** 第 10 行是本章最容易看错的地方。P3 起就有一条 `confirm-file-write` 规则，`write_file` 本该走 `ask` 审批。但这次写的路径在工作区外面，工作区边界给的是 `deny`，而 `deny` 比 `ask` 强——最终决定是拒绝，审批提供方连问都没被问。所以"审批被问到的工具"是空数组。

**`runtime_status` 变了，但它只是观察结果。** 第 6、7 行：第 1 次请求时 `mcp_connections` 是空的，第 2 次变成了 `["fake"]`。模型能看到当前连了什么，但看到不等于被授权——第 8 行那个 `unknown_tool` 就是证据：状态栏里写着工具存在，调用依然被拒。

**三个工具调用，三条结果，一条不少。** 哪怕其中两个是错误，也必须各自回填一条与 `tool call id` 配对的结果。少一条、多一条、id 对不上，历史就非法（实验 6 会把六种错法逐一跑出来）。

**进程收干净了。** 第 12 行：`close()` 之后 MCP 运行时确实关掉了，子进程不会留在后台。

这一章剩下的篇幅，就是把上面每一条拆开讲清楚：它为什么必须这样，代码在哪一行，以及你怎么自己跑一遍确认。

---

## 二、full_harness 是一个标记，不是一个模块

### 2.1 先看它在代码里长什么样

打开 `code/chapters/ch20/src/core/profiles.ts`，能力清单是这样声明的（第 80 到 103 行）：

```typescript
// 每章只声明新增能力；后续章节通过累计 PROFILE_DELTAS 得到完整档案。
const PROFILE_DELTAS = [
  ["loop", "powershell"],
  ["tool_registry", "files"],
  ["policy"],
  ["hooks"],
  ["todo"],
  ["subagent"],
  ["skills"],
  ["artifacts", "compaction"],
  ["memory"],
  ["dynamic_prompt"],
  ["recovery"],
  ["task_dag_json"],
  ["background"],
  ["cron"],
  ["teammate", "mailbox"],
  ["protocol", "plan_gate"],
  ["task_dag_sqlite", "work_stealing"],
  ["worktree"],
  ["mcp"],
  // full_harness 是 P20 的标记能力：不新增具体运行时，只声明前十九章能力全部生效。
  ["full_harness"],
] as const satisfies readonly (readonly Capability[])[];
```

每一行是"这一章新增了什么"，不是"这一章有什么"。完整档案由累加得出（第 105 到 113 行）：

```typescript
// PROFILES 由增量能力数组推导，保证每章档案都是前一章的严格超集。
const PROFILES: readonly ChapterProfile[] = Object.freeze(
  PROFILE_DELTAS.map((_, index) =>
    Object.freeze({
      chapter: index + 1,
      capabilities: new CapabilitySet(PROFILE_DELTAS.slice(0, index + 1).flat()),
    }),
  ),
);
```

`slice(0, index + 1).flat()` 就是"把第 1 章到第 N 章的增量全部摊平"。这一行代码保证了一件事：**P(N) 一定是 P(N-1) 的严格超集**，不可能出现"第 15 章有、第 16 章没了"。

### 2.2 累加出来的数字

实验 1 直接把二十章的能力数打印出来：

```text
[1] 每章累计能力数：P01=2 P02=4 P03=5 P04=6 P05=7 P06=8 P07=9 P08=11 P09=12
    P10=13 P11=14 P12=15 P13=16 P14=17 P15=19 P16=21 P17=23 P18=24 P19=25 P20=26
[2] P20 相对 P19 新增：[full_harness]，移除：[]
[3] P20 是否仍持有 P19 的每一项能力：true
```

26 项能力，P20 只比 P19 多了 `full_harness` 一项，一项也没少。

这个"只多一项标记"是本章最容易被误解的地方，所以说清楚它**不是**什么：

| 它不是 | 为什么 |
| --- | --- |
| 不是一个新模块 | 没有 `full-harness.ts`。P20 相对 P19 新增的源文件只有入口 `src/chapters/ch20.ts`（5 行） |
| 不是一个新工具 | Lead 的工具面在 P19 和 P20 都是 23 个，`full_harness` 不注册任何工具 |
| 不是一个开关 | 没有 `if (capabilities.has("full_harness"))` 去启用某个功能分支 |
| 不是"终极框架" | 没有第二套 Loop、第二个 Runner、第二条历史 |

那它做什么？它是**装配契约的名字**。`bootstrap.ts` 会因为 profile 的章节号 ≥ 19 而要求 `mcpRuntime` 必须存在，会因为 `dynamic_prompt` 能力而装上动态提示词，会因为 profile 是 P20 而装上 `runtime_status` 状态栏。`full_harness` 这个标记的意义在于：**当它出现时，前面 25 项能力必须全都能在同一个进程里同时工作，且有测试证明。**

证明它的地方是 `code/chapters/ch20/tests/ch20-full-harness.test.ts`（1210 行，7 个测试）。这 7 个测试没有一个在测"新功能"，全部在测"旧功能放在一起会不会互相踩"。

### 2.3 档案必须是固定对象，不能自己拼

`profileForChapter()` 只认 1 到 20（第 137 到 143 行）：

```typescript
export function profileForChapter(chapter: number): ChapterProfile {
  // 只有 1 到 20 的固定章节可查询；越界立即失败，避免调用方构造未知档案。
  if (!Number.isInteger(chapter) || chapter < 1 || chapter > PROFILES.length) {
    throw new Error("chapter must be an integer from 1 to 20");
  }
  return profileAt(chapter);
}
```

实验 1 后半段验证了这道门：

```text
[4] profileForChapter(20) === P20：true
[5] profileForChapter(21) → chapter must be an integer from 1 to 20
[6] profileForChapter(19.5) → chapter must be an integer from 1 to 20
[7] 自造 {chapter:20, capabilities:...} 传给 buildAgent → profile must be a fixed chapter profile
```

第 7 行是关键。哪怕你手工造一个 `{chapter: 20, capabilities: <和 P20 一模一样的能力集>}`，`buildAgent` 也会拒绝。它的检查方式是**对象身份**：

```typescript
if (profileForChapter(profile.chapter) !== profile) {
  throw new Error("profile must be a fixed chapter profile");
}
```

`!==` 而不是逐项比对能力。为什么要这么严？因为一旦允许调用方自由拼装能力集合，"章节"就不再是契约了——有人可以拼出一个"有 `mcp` 但没有 `dynamic_prompt`"的组合，跑出一个从没被任何测试覆盖过的 Agent。固定对象把可能的组合从 2^26 收敛到 20 个，每一个都有对应的测试文件。

---

## 三、组合根：错误的共享必须在启动时就炸

这一节是本章第一个核心。

### 3.1 先说清"假完整实现"是什么样

想象一个装配错误：`CronRuntime` 拿到的是 `JobSupervisor` A，后台工具拿到的是 `JobSupervisor` B。两个 supervisor 都能正常工作，类型完全正确，TypeScript 一个错都不报，启动也不会失败。

然后你会看到这样的现象：

```text
Cron 到点了 → 提交给 supervisor A → 完成后事件进 A 的 EventInbox
模型下一轮请求前，Runner 从 B 的 EventInbox 里读事件 → 空的
于是 Cron 明明跑完了，模型永远不知道
```

功能都在。测试单独测 Cron 能过，单独测后台也能过。但放在一起就是坏的，而且坏得很安静——没有异常、没有报错，只是事件永远送不到。

`bootstrap.ts` 里那段注释把这个反面案例直接写出来了（第 331 到 332 行）：

```typescript
// 这里的引用身份检查是 Full Harness 的装配门禁：同一 store、EventInbox、Cron、队友和 MCP 资源
// 必须由同一组合根创建，避免"功能都存在但各自持有状态"的假完整实现。
```

"功能都存在但各自持有状态"，就是假完整实现。P20 对付它的办法不是写文档提醒，而是在启动时用 `!==` 逐条检查。

### 3.2 七条门禁，逐条跑一遍

实验 2 把 `validateBuildDependencies()` 的每一条都触发了一次：

```text
[1] 完整依赖 → (没有报错)
[2] 换掉 cronRuntime（另一个 supervisor/inbox）→ cronRuntime must share the background supervisor and event inbox
[3] 只换掉 protocolRuntime → protocolRuntime must share the teammate runtime and mailbox store
[4] 换掉 teammateRuntime + protocolRuntime（两者互相匹配）→ teammateRuntime must share the background supervisor, EventInbox, and CronRuntime
[5] 换掉 worktreeRuntime（不是 claimService）→ worktreeRuntime must be the WorkStealingRuntime claim service and share its store
[6] 删掉 mcpRuntime → mcpRuntime is required for chapter 19 or later
[7] 把 P20 依赖交给 P18 → mcpRuntime requires chapter 19 or later
```

第 4 行有个细节值得说：只替换 `teammateRuntime` 时，报出来的其实是 `protocolRuntime` 那条错——因为协议共享检查排在队友共享检查前面，`protocolRuntime` 引用的还是老队友，先撞上。要看到队友那条错，得把 `teammateRuntime` 和 `protocolRuntime` 作为**互相匹配的一对**同时换掉。这也说明门禁是有顺序的，第一条不满足就不会继续往下查。

### 3.3 三类检查，作用完全不同

把 `validateBuildDependencies()` 的 20 多个 `if` 归一下类，其实只有三种：

```text
第一类：能力要求依赖必须在（"is required for"）
    profile 有 mcp，但 mcpRuntime 是 undefined  →  炸
    典型错误串：mcpRuntime is required for chapter 19 or later

第二类：依赖要求能力必须够（"requires chapter N or later"）
    传了 mcpRuntime，但 profile 是 P18  →  炸
    典型错误串：mcpRuntime requires chapter 19 or later

第三类：依赖之间必须是同一个对象（"must share" / "must be"）
    传了 cronRuntime，但它的 supervisor 不是你传的那个  →  炸
    典型错误串：cronRuntime must share the background supervisor and event inbox
```

前两类是"数量对不对"，第三类才是本章真正关心的"身份对不对"。看代码就明白差别（第 398 到 405 行）：

```typescript
if (
  dependencies.cronRuntime !== undefined &&
  (dependencies.backgroundSupervisor === undefined ||
    dependencies.cronRuntime.supervisor !== dependencies.backgroundSupervisor ||
    dependencies.cronRuntime.eventInbox !== dependencies.backgroundSupervisor.eventInbox)
) {
  throw new Error("cronRuntime must share the background supervisor and event inbox");
}
```

`cronRuntime.supervisor !== dependencies.backgroundSupervisor`：不是比较类型，不是比较字段，是比较**这两个变量指的是不是同一个对象**。这一行就是 3.1 节那个 bug 的解药。


### 3.4 四组"必须同一个对象"的关系

P20 一共守着四组身份关系。画成图更清楚：

```text
                    ┌──────────────────────┐
                    │   JobSupervisor      │  ← 后台任务的主管
                    │   .eventInbox        │  ← 唯一的事件收件箱
                    └──────────┬───────────┘
                               │ 必须是同一个对象
              ┌────────────────┼────────────────┐
              │                │                │
      ┌───────▼──────┐  ┌──────▼───────┐        │
      │ CronRuntime  │  │ Teammate     │────────┘
      │ .supervisor  │  │ Runtime      │  .supervisor / .eventInbox
      │ .eventInbox  │  │ .cronRuntime │──┐
      └──────┬───────┘  │ .mailboxStore│  │ 必须是同一个 CronRuntime
             └──────────┴──────┬───────┘◄─┘
                               │ 必须是同一个对象
                       ┌───────▼────────────┐
                       │ ProtocolRuntime    │
                       │ .teamRuntime       │
                       │ .mailboxStore      │
                       └────────────────────┘

      ┌────────────────────┐        ┌─────────────────────┐
      │ WorkStealingRuntime│        │  WorktreeRuntime    │
      │ .store             │◄──────►│  .store             │  同一个 SQLite store
      │ .claimService      │◄───────│  （就是它本身）      │  claimService === worktreeRuntime
      └────────────────────┘        └─────────────────────┘
```

用表格再列一遍，方便对照报错：

| 这一组 | 检查什么 | 报错串 | 不检查会怎样 |
| --- | --- | --- | --- |
| Cron ↔ 后台 | `cronRuntime.supervisor` 和 `.eventInbox` 都必须是你传的那一个 | `cronRuntime must share the background supervisor and event inbox` | Cron 完成的事件进了另一个收件箱，模型永远收不到 |
| 队友 ↔ 后台/Cron | `teammateRuntime` 的 supervisor、eventInbox、cronRuntime 三个都必须一致 | `teammateRuntime must share the background supervisor, EventInbox, and CronRuntime` | 队友完成通知丢失，或者队友的定时任务不受主 Cron 管 |
| 协议 ↔ 队友 | `protocolRuntime.teamRuntime` 和 `.mailboxStore` 必须一致 | `protocolRuntime must share the teammate runtime and mailbox store` | 计划审批写在一个信箱，队友读的是另一个，plan gate 永远解不开 |
| Worktree ↔ 工作窃取 | `worktreeRuntime` 必须**就是** `workStealingRuntime.claimService`，且两者 `store` 相同 | `worktreeRuntime must be the WorkStealingRuntime claim service and share its store` | 任务认领和目录绑定分离，队友在错误的目录里改文件 |

第四组的写法特别一点，值得单独看（第 376 到 385 行）：

```typescript
if (
  dependencies.worktreeRuntime !== undefined &&
  (dependencies.workStealingRuntime === undefined ||
    dependencies.workStealingRuntime.store !== dependencies.worktreeRuntime.store ||
    dependencies.workStealingRuntime.claimService !== dependencies.worktreeRuntime)
) {
  throw new Error(
    "worktreeRuntime must be the WorkStealingRuntime claim service and share its store",
  );
}
```

注意最后一行条件：`claimService !== worktreeRuntime`。要求的不是"claimService 长得像 worktreeRuntime"，而是**它就是同一个对象**。这条对应第 18 章的结论：任务认领和实际执行目录必须由同一条链路绑定。如果 `claimService` 是另一个实现，就可能出现"认领成功但目录没建"或者"目录建了但认领记录在别处"。

### 3.5 为什么必须在启动时炸

这一点值得单独强调，因为它是一种可以迁移到你自己项目里的设计习惯。

```text
装配错误在启动时炸：
  报错时机 —— 进程刚起来，第一行业务代码还没跑
  报错内容 —— "cronRuntime must share the background supervisor and event inbox"
  排查成本 —— 读一遍这句话就知道改哪里
  影响面   —— 零。没有半装配的 Agent，没有写脏的状态

装配错误在运行时暴露：
  报错时机 —— 跑了 40 分钟，某次 Cron 完成之后
  报错内容 —— 没有报错。事件就是没到，日志干净得像一切正常
  排查成本 —— 要同时怀疑 Cron、EventInbox、Runner 的事件消费、去重逻辑
  影响面   —— 期间产生的任务状态、worktree、审批记录都已经落盘了
```

`validateBuildDependencies()` 是 `buildAgent()` 的第一句（第 95 行），在创建任何工具、任何运行时之前：

```typescript
export function buildAgent(profile: ChapterProfile, dependencies: BuildDependencies): AgentRunner {
  // Full Harness 的构造顺序固定为"先验证依赖，再注册工具，最后交给 Runner 托管资源"；
  // 这样任何失败都发生在明确的组合边界内，不会留下半装配的 Agent。
  validateBuildDependencies(profile, dependencies);
```

"不会留下半装配的 Agent"是这个顺序的全部理由。校验放在后面，就可能先起了 MCP 子进程、先建了 worktree，然后才发现依赖接错——那时候要清理的东西就多了。

---

## 四、一个 AgentRunner，一条固定的执行顺序

这一节是本章第二个核心。二十章的能力都挂在同一个 `AgentRunner` 上，谁在谁前面执行不是随意的。

### 4.1 完整顺序图

先看全图，再逐段解释。所有行号都指向 `code/chapters/ch20/src/core/loop.ts`。

```text
run(prompt)                                              L271
  │
  ├─ #ensureOpen()  已 close 就直接抛 "AgentRunner is closed"   L754
  ├─ 运行锁：已在跑就拒绝并发回合
  │
  └─ #runUserTurn(prompt)                                L332
       │
       ├─ [1] userMessage + UserPromptSubmit hook          L342-344
       │        ↑ 只能加内容，不能授权
       ├─ [2] turnLifecycle.beginTurn(prompt)              L348   ← 记忆选择
       ├─ [3] modelRequestExecutor.beginTurn()             L351   ← 恢复计数归零
       │
       └─ for turn = 1 .. maxTurns                         L366
            │
            ├─ [4] #acceptNextRuntimeEvent(false, false)    L369  ← 收后台事件
            │
            ├─ [5] #prepareModelRequest()                   L372 → L475
            │        ├─ validateToolPairing(history)             L479
            │        ├─ historyProcessor.prepare()  请求级压缩    L483
            │        ├─ validateToolPairing(requestHistory)      L491
            │        ├─ turnLifecycle.beforeModel()              L493
            │        ├─ toolRoundObserver.beforeModel()          L502
            │        ├─ systemMessage(#renderSystemPrompt())     L505  ← 动态 Prompt
            │        ├─ validateToolPairing(requestMessages)      L510
            │        └─ tools = this.#tools.snapshot()            L511  ← 快照就在这里
            │
            ├─ [6] model.complete() 或 RecoveryManager.complete()  L373-375
            │        ├─ finishReason === "length"       → IncompleteModelReplyError  L377
            │        └─ finishReason === "content_filter" → AgentRunError            L380
            │
            ├─ [7] history.push(assistant)                  L385
            │
            ├─ 没有 toolCalls？
            │    ├─ [8] Stop hook（最多一次 forceContinue） L390-396
            │    ├─ [9] #acceptNextRuntimeEvent(true, false) L397 ← 收尾前再等一次
            │    └─ [10] #complete(content, turn)            L402
            │
            └─ 有 toolCalls？逐个执行                        L409
                 │
                 ├─ [11] #executeTool(call, context, tools)  L417 → L532
                 │         ├─ tools.prepare(call)     未知工具 → unknown_tool
                 │         ├─ Zod 校验参数
                 │         ├─ #resolveToolContext()          L626  ← 可信工作区
                 │         ├─ PreToolUse hook                      ← 可拒绝，不可放宽
                 │         ├─ PermissionPolicy.evaluate()          ← 真正的授权边界
                 │         ├─ definition.invoke(args, ctx)
                 │         └─ PostToolUse hook
                 │
                 ├─ [12] #processToolResults()                L426
                 └─ [13] 逐个 history.push(toolMessage(result.content, call.id))  L433
                          ↑ 一个 call 恰好一条，id 必须对上
```

### 4.2 三个位置为什么必须是那个位置

整张图里，有三处顺序换了就会出错。

**第一处：`#acceptNextRuntimeEvent` 在 `#prepareModelRequest` 之前（L369 在 L372 之前）。**

事件消费必须早于请求组装。原因写在注释里：

```typescript
// 用户回合内只接收普通后台事件；Cron/mailbox 上下文事件会被延迟，避免打断当前用户会话。
// 每次模型请求前都先接收已完成的后台事件，避免事件只在首轮可见。
await this.#acceptNextRuntimeEvent(false, false);
```

"避免事件只在首轮可见"是关键。如果只在进入回合时收一次事件，那么一个跑了三轮工具的回合里，第二轮才完成的后台任务就要等到下一个用户输入才能被看到。放在循环里每轮都收，后台结果最迟在下一次模型请求就能进入上下文。

**第二处：`snapshot()` 是 `#prepareModelRequest` 的最后一步（L511）。**

```typescript
validateToolPairing(requestMessages);
const tools = this.#tools.snapshot();
return Object.freeze({
  request: Object.freeze({ messages: requestMessages, tools: tools.openAITools() }),
  tools,
});
```

同一个 `tools` 对象被用了两次：一次转成 `openAITools()` 发给模型，一次作为返回值交给 `#executeTool`。这就是第一节里"连上了却用不了"的完整解释——模型看到的工具列表和执行时校验的工具集合，**是同一个不可变对象**。中间哪怕连了十个 MCP server，这条回复也用不到。

**第三处：系统提示在每轮渲染，不在构建时固化（L505）。**

```typescript
// 系统提示在每轮组装请求前渲染，使工具列表和选中记忆等运行态变化下一轮生效。
systemMessage(this.#renderSystemPrompt()),
```

注意"下一轮生效"这个措辞。渲染发生在每轮请求前，所以上一轮的连接、上一轮选中的记忆、上一轮的后台状态，都会体现在这一轮的提示词里。但**同一轮之内**不会变——渲染和快照是同一次 `#prepareModelRequest` 里前后紧邻的两步。

### 4.3 `validateToolPairing` 被调了三次，不是冗余

`#prepareModelRequest` 里三次校验配对（L479、L491、L510），位置分别是：

| 时机 | 校验对象 | 挡住什么 |
| --- | --- | --- |
| 处理器之前 | `this.#history`（canonical history） | 上一轮自己写坏了历史 |
| 处理器之后 | `requestHistory`（压缩后的历史） | **压缩把一对 call/result 拆散了** |
| 组装之后 | `requestMessages`（含系统提示和指导消息） | 注入的指导消息插到了 call 和 result 之间 |

第二次最重要。压缩会重写历史，是本章所有机制里最有可能破坏配对的一个——它要丢掉旧消息，而"旧消息"里可能正好有一条 tool result，它对应的 tool call 因为在更前面被丢掉了。这一次校验就是压缩的验收关。


### 4.4 一个回合最多允许一次 Stop 续跑

`stopHookActive` 这个布尔量控制的是"Stop hook 能不能反复要求继续"（L365、L390 到 396）：

```typescript
const stopHook = await this.#hooks.runStop(this.#history, stopHookActive);
if (stopHook.forceContinue !== undefined) {
  // Stop Hook 可在模型无工具调用时强制注入继续提示，打破常规结束流程。
  this.#history.push(...stopHook.additionalContext, stopHook.forceContinue);
  stopHookActive = true;
  continue;
}
```

第一次进 Stop hook 时 `stopHookActive` 是 `false`，hook 可以返回 `forceContinue` 把回合拉回循环。续跑之后 `stopHookActive` 变成 `true`，再传给 `runStop`——实现方看到这个标记就不该再要求继续。这条约束防止的是一个具体的死循环：Stop hook 每次都说"再来一轮"，模型每次都说"我说完了"，回合永远跑到 `maxTurns` 才结束。

---

## 五、三条不变量

前面讲的是顺序，这一节讲的是"无论顺序怎么走都必须成立"的三件事。它们是 P20 用来判断"整合有没有做对"的标准。

### 5.1 不变量一：一次模型回复共用一份工具快照

`ToolRegistry.snapshot()` 的实现只有两行（`code/chapters/ch20/src/core/tools.ts`）：

```typescript
snapshot(): ToolRegistry {
  // 返回不可变快照，正在执行的回合不会被后续 MCP 连接/断开影响。
  return new ToolRegistry(this.#definitions, false, this.#version);
}
```

第二个参数 `false` 就是 `mutable`。快照拷了一份 definitions 的 Map，版本号沿用当时的值，然后把自己标记成不可变。

实验 5 把它的行为完整跑了一遍：

```text
[1] 活注册表 v1：[alpha]
[2] 快照 v1：[alpha]
[3] 活注册表新增后 v2：[alpha, beta]
[4] 同一个快照仍是 v1：[alpha]
[5] 在快照上 prepare("beta") → Error [unknown_tool]: Unknown tool: beta
[6] 试图往快照里注册 → tool registry snapshot is immutable
[7] 按名字（不是对象身份）撤销 → tool definition was not registered by this registry
[8] 按对象身份撤销 → (没有报错)
[9] 撤销后活注册表 v3：[beta]
```

第 3、4 行是核心：活注册表已经涨到 v2 并且有了 `beta`，快照还停在 v1 只有 `alpha`。第 5 行是这个设计对模型的直接后果——快照上调用 `beta` 得到 `unknown_tool`，跟第一节实验 4 里的 `mcp__fake__lookup` 完全一样。

第 7、8 行是另一层保护。撤销工具时比对的是**对象身份**，不是名字：

```typescript
// unregisterMany 用 #sourceDefinitions（WeakMap）确认这个定义确实由本注册表注册过
throw new Error("tool definition was not registered by this registry");
```

传一个"同名同形状但不是同一个对象"的定义进去，撤销会失败。这挡住的场景是：某个 MCP 连接断了要撤销它发布的 5 个工具，但期间另一个 server 已经把同名工具重新发布了——按名字撤会把新连接的工具误删，按对象身份撤就只会撤自己那一批。

### 5.2 不变量二：一个 tool call 恰好一条 tool result

规则实现在 `code/chapters/ch20/src/core/messages.ts`（第 117 到 149 行）：

```typescript
export function validateToolPairing(messages: readonly ChatMessage[]): void {
  const pending = new Set<string>();
  for (const message of messages) {
    if (pending.size > 0) {
      if (message.role !== "tool") {
        throw new MessageContractError(
          `missing tool results for ids: ${JSON.stringify([...pending].sort())}`,
        );
      }
      if (!pending.delete(message.toolCallId)) {
        throw new MessageContractError(`unexpected tool result id: ${message.toolCallId}`);
      }
      continue;
    }
    if (message.role === "tool") {
      throw new MessageContractError(`orphan tool result id: ${message.toolCallId}`);
    }
    if (message.role === "assistant" && message.toolCalls.length > 0) {
      for (const call of message.toolCalls) {
        pending.add(call.id);
      }
    }
  }
  if (pending.size > 0) {
    throw new MessageContractError(
      `missing tool results for ids: ${JSON.stringify([...pending].sort())}`,
    );
  }
}
```

读法很简单：`pending` 是"还欠着结果的 call id"。只要 `pending` 非空，下一条消息就**必须**是 tool，而且 id 必须在 `pending` 里。

实验 6 把六种错法全跑了一遍：

```text
[1] 调用后紧跟同 id 结果 → (没有报错)
[2] 调用后没有结果就收尾 → missing tool results for ids: ["call-1"]
[3] 调用后跟了别的 id → unexpected tool result id: call-2
[4] 调用后先插一条 user → missing tool results for ids: ["call-1"]
[5] 没有调用却有结果 → orphan tool result id: call-1
[6] 同一 id 回填两次 → orphan tool result id: call-1
```

第 4 行值得注意：往 call 和 result 之间插一条正常的 user 消息也不行。这正好解释了 4.3 节为什么组装完请求还要再校验一次——注入的指导消息如果插错位置，就是第 4 行这个错。

第 6 行的错误信息可能有点反直觉：同一个 id 回填两次，报的是 `orphan`（孤立结果）而不是"重复"。因为第一条结果已经把 `call-1` 从 `pending` 里删掉了，第二条进来时 `pending` 是空的，走的是"没有调用却有结果"那个分支。

Loop 侧保证这条不变量的地方在 L427 到 434：

```typescript
for (const [index, call] of assistant.toolCalls.entries()) {
  const result = processedResults[index];
  if (result === undefined) {
    throw new AgentRunError("Tool execution did not produce a paired result");
  }
  // 每个工具调用都必须回填一个 tool message，否则后续模型请求会破坏协议配对。
  this.#history.push(toolMessage(result.content, call.id));
}
```

按 `assistant.toolCalls` 的顺序遍历，逐个配对回填。哪怕某个工具被权限拒了、被 Hook 停了、参数校验没过，也要回填一条错误结果——不能跳过。第一节实验 4 里那三条结果（一个成功、两个错误）就是这条规则的体现。


### 5.3 不变量三：Prompt 和 Hook 都不是授权边界

这是三条里最容易被违反的一条，因为违反它的代码通常看起来很合理。

先明确三者各自能做什么：

| 机制 | 能做 | 不能做 |
| --- | --- | --- |
| 系统提示词 | 告诉模型"不要写工作区外的文件" | 阻止模型真的这么做 |
| PreToolUse hook | 拒绝一次调用、追加上下文 | 把系统的 `deny` 改成 `allow` |
| PermissionPolicy | 真正决定 allow / ask / deny | —— |

提示词那条不用多说：模型可以无视任何指令，所以提示词永远只是建议。

Hook 那条要看代码。`PermissionPolicy.evaluate()` 收集候选决定的顺序是（`permissions.ts` 第 185 到 195 行）：

```typescript
const workspaceBoundary = await this.#workspaceBoundaryDecision(request);
if (workspaceBoundary !== undefined) {
  candidates.push(workspaceBoundary);
}
const shellDefault = shellDefaultDecision(request);
if (shellDefault !== undefined) {
  candidates.push(shellDefault);
}
candidates.push(...request.recommendations, ...(await this.#evaluateRules(request)));

const proposed = strongestDecision(candidates);
```

Hook 的意见进的是 `request.recommendations`——它只是**候选之一**，跟工作区边界、shell 默认、显式规则平等地扔进同一个数组。最终由 `strongestDecision()` 裁决（第 302 到 314 行）：

```typescript
function strongestDecision(decisions: readonly PermissionDecision[]): PermissionDecision {
  for (const behavior of ["deny", "ask", "allow"] as const) {
    const decision = decisions.find((candidate) => candidate.behavior === behavior);
    if (decision !== undefined) {
      return decision;
    }
  }
  return new PermissionDecision(
    "passthrough",
    "No permission participant made a decision",
    "default",
  );
}
```

`["deny", "ask", "allow"]` 这个顺序就是强度排序。只要候选里有一个 `deny`，别的全都不算——**Hook 推荐 `allow` 不能覆盖系统的 `deny`**，因为 `deny` 先被找到。

### 5.4 "deny 强于 ask"的实际后果

`strongestDecision` 里 `deny` 排在 `ask` 前面，这带来一个具体现象，就是第一节里那行让人困惑的输出：

```text
[10] 审批被问到的工具：[]（deny 强于 ask，越界写不进审批）
```

完整推演一次"往工作区外面写文件"这个调用：

```text
工具：write_file，effect = "write"，path = <工作区外的路径>

候选 1：#workspaceBoundaryDecision
        effect === "write" ✓，path 是字符串 ✓
        isPathWithinWorkspace(...) → false
        → PermissionDecision("deny", "Writing outside the workspace is forbidden", "workspace-boundary")

候选 2：shellDefaultDecision
        effect !== "execute" → undefined，不参与

候选 3：request.recommendations（Hook 意见）
        本例为空

候选 4：#evaluateRules → confirm-file-write 规则
        工具名是 write_file ✓
        → PermissionDecision("ask", "File writes require explicit approval from chapter 3 onward", ...)

strongestDecision([deny, ask])
        找 "deny" → 命中候选 1
        → 最终决定 deny

proposed.behavior === "deny"，不是 "ask"
        → 不进 #resolveApproval
        → 审批提供方一次都没被调用
```

所以"审批被问到的工具"是空数组，不是因为规则没配，而是因为**更强的拒绝先生效了，根本没到需要问人的那一步**。

这个行为是对的：审批的意义是"让人来决定这个有风险的操作要不要做"，而工作区越界已经是系统硬边界，不需要也不应该给人一个批准它的机会。反过来想，如果 `ask` 强于 `deny`，那么任何硬边界都能被一次误点的批准绕过。

顺手记住另一条相关的：`shellDefaultDecision` 让所有 `effect === "execute"` 的工具默认走 `ask`（第 324 到 333 行）：

```typescript
if (definition.effect !== "execute") {
  return undefined;
}
return new PermissionDecision("ask", "Shell execution requires approval", "shell-default");
```

这是默认值，不需要任何规则配置。`shell` 工具因此天然需要审批。

### 5.5 还有一道更靠前的边界

在权限判断之前，`#executeTool` 还会解析一次工具的工作区（`loop.ts` L626，`#resolveToolContext`）。它的失败信息是：

```text
resolved tool workspace is outside the controlled workspace root
```

这道边界管的不是"能不能写这个路径"，而是"这个工具运行在哪个目录"。第 18 章的 worktree 会给不同任务分配不同目录，这一步确认解析出来的目录仍在受控根之下。它抛的是异常而不是返回决定，因为这属于装配错误级别的问题——工具的执行目录跑到受控范围之外，说明绑定链路本身坏了，不是一次调用该不该批准的问题。

一次工具调用因此有三道由外到内的门：

```text
[门 1] #resolveToolContext   工具运行的目录必须在受控根内     → 抛异常
[门 2] PermissionPolicy      这次操作允许吗（含工作区写边界）  → allow/ask/deny
[门 3] handler 自身          业务校验                        → ToolResult
```

Hook 挂在门 2 前后（PreToolUse / PostToolUse），能观察、能拒绝、能加上下文，但不在这三道门的判定链上。

---

## 六、runtime_status：P20 唯一新增的模型可见输出

`full_harness` 不带新运行时，但它确实让模型多看到一样东西：动态提示词最后多了一段 `## runtime_status`。

### 6.1 它长什么样

就是一行 JSON：

```text
## runtime_status
{"mcp_connections":["fake"],"pending_work":false}
```

两个字段，全部来自同步读取（`bootstrap.ts` 第 74 到 89 行）：

```typescript
// P20 把运行时状态渲染成动态 Prompt 的最后一段 runtime_status。
// 这里只汇总可同步读取的事实：后台是否仍有工作、MCP 当前连接哪些 alias。
// 状态栏不替代 EventInbox：事件仍在请求前消费，状态只帮助模型判断下一轮是否等待。
function fullHarnessRuntimeStatus(dependencies: BuildDependencies): JsonObject {
  // statusProvider 只读取当前快照，不触发连接、任务或事件副作用；动态 Prompt 的职责是观察而非调度。
  const pendingWork =
    dependencies.backgroundSupervisor?.hasPendingWork === true ||
    dependencies.cronRuntime?.hasPendingWork === true ||
    dependencies.teammateRuntime?.hasPendingWork === true;
  const mcpConnections =
    dependencies.mcpRuntime === undefined ? [] : dependencies.mcpRuntime.connectedAliases;
  return Object.freeze({
    mcp_connections: Object.freeze([...mcpConnections]),
    pending_work: pendingWork,
  });
}
```

三个注释各自否掉一种误用：

- "只汇总可同步读取的事实"——不发起任何异步调用。状态栏不能因为渲染提示词而去 ping 一次远程 server。
- "不替代 EventInbox"——`pending_work: true` 只是说"还有活在跑"，具体结果仍然要走事件通道进历史。
- "观察而非调度"——读 `connectedAliases` 不会顺手重连，读 `hasPendingWork` 不会顺手推进队列。

### 6.2 它只装给 P20

装配那段在第 272 到 289 行：

```typescript
const systemPromptProvider = dynamicPrompt
  ? new DynamicPromptProvider({
      renderer: new DynamicPromptRenderer(),
      identity: systemPrompt,
      tools,
      workspace: dependencies.workspace,
      context: Object.freeze({ chapter: profile.chapter, identity }),
      ...(profile.capabilities.has("full_harness")
        ? {
            // P20 的状态段参与 DynamicPromptRenderer 缓存键，因此连接或待处理工作变化会让下一次请求重渲染。
            // P20 使用 statusProvider，让 MCP 连接与后台状态在当前请求重新读取，并保持在提示尾部。
            statusProvider: () => fullHarnessRuntimeStatus(dependencies),
          }
        : {}),
      ...(skillRegistry === undefined ? {} : { skills: skillRegistry }),
      ...(memorySession === undefined ? {} : { memory: memorySession }),
    })
  : undefined;
```

`profile.capabilities.has("full_harness")` 是全代码库里唯一一处读这项能力的地方。P19 和之前的章节走的是 `{}` 分支，提示词里就没有 `runtime_status` 段——这保证了旧章节的提示词一个字节都没变，旧测试全部继续有效。

注意 `statusProvider` 是个**函数**，不是一个值。`DynamicPromptProvider.render()` 每次都重新调它：

```typescript
...(this.#statusProvider === undefined ? {} : { status: this.#statusProvider() }),
```

所以状态是每轮请求重新读的。传个非函数进去会在构造时就失败：

```typescript
if (options.statusProvider !== undefined && typeof options.statusProvider !== "function") {
  throw new TypeError("statusProvider must be a function");
}
```

### 6.3 固定在最后一段，且参与缓存键

这两件事是 P20 对 `prompting.ts` 的全部改动，加起来 26 行。

先看位置。`DynamicPromptRenderer.render()` 里，`runtime_status` 是最后被 push 进 sections 的：

```typescript
if (statusJson !== undefined) {
  // 运行态状态固定在最后，状态变化不会移动或重排前面的稳定段落。
  sections.push(`## runtime_status\n${statusJson}`);
}
```

段落顺序因此固定成：

```text
## identity        ← 身份 + context JSON
## tools           ← 当前工具名列表
## workspace       ← 解析后的绝对路径
## skills          ← 可选，有 Skill 才出现
## memory          ← 可选，有选中记忆才出现
## runtime_status  ← 可选，只有 P20 有；永远在最后
```

"只追加不插队"这个约束对模型缓存很实际：状态每轮都在变（后台任务起来了、MCP 连上了），如果它插在中间，前面所有稳定内容的位置都会移动。放在最后，变化只影响尾部。

再看缓存键：

```typescript
const cacheInput: Record<string, JsonValue> = {
  context, identity, memory: memoryBody, skills: skillCatalog, tools: [...tools], workspace,
};
if (statusJson !== undefined) {
  cacheInput.status = statusJson;
}
const key = stableJson(cacheInput);
```

状态**必须**参与缓存键。理由很直接：渲染器有缓存，命中就直接返回上次的字符串。如果 `status` 不在键里，那么状态从 `{"mcp_connections":[]}` 变成 `{"mcp_connections":["fake"]}` 时，键没变、缓存命中、返回的还是旧提示词——模型永远看不到状态更新。

实验 3 把这两点都验证了：

```text
[1] 不传 status 时的段落顺序：## identity ## tools ## workspace
[2] 传 status 后的段落顺序：## identity ## tools ## workspace ## runtime_status
[3] 最后一段内容：{"mcp_connections":[],"pending_work":false}
[4] 同样的 status 再渲染一次，cacheHits 0 → 1
[5] 只改 status 后 cacheHits 仍是 1，说明缓存被判定失效
[6] 新的最后一段：{"mcp_connections":["fake"],"pending_work":true}
[7] runtime_status 之前的正文是否完全没变：true
[8] statusProvider 传非函数 → statusProvider must be a function
```

第 4 行和第 5 行连起来读：同样的输入渲染两次，`cacheHits` 从 0 涨到 1（命中了）；只改 `status` 再渲染，`cacheHits` **停在** 1（没有再涨，说明这次没命中，重新渲染了）。第 7 行确认重渲染之后前面的正文一个字都没动。

### 6.4 为什么它不能当授权来源

这是本节最重要的一条，也是最容易在实际项目里搞错的一条。

`runtime_status` 告诉模型"当前连着 `fake` 这个 server"。模型很自然会推理：既然连着，那我就能调用它的工具。

第一节实验 4 的输出直接否掉了这个推理：

```text
[7] 第 2 次请求的 runtime_status：{"mcp_connections":["fake"],"pending_work":false}
[8] probe-mcp → Error [unknown_tool]: Unknown tool: mcp__fake__lookup
```

第 7 行说连着，第 8 行说不认识这个工具。矛盾吗？不矛盾——它们回答的是两个不同的问题：

```text
runtime_status 回答："现在这个时刻，活注册表里有哪些连接"
工具快照回答：  "你这条回复能调用哪些工具"

两者可以不一致，而且必然会在连接刚建立的那一轮不一致。
授权只看后者。
```

把这条推广成一句可迁移的设计原则：**任何送给模型看的状态信息都是"观察"，不是"许可"。** 模型看到什么、被允许做什么，是两条独立的通道。前者可以宽松（多告诉一些有助于它判断下一步），后者必须严格（只由权限系统和工具快照决定）。

---

## 七、23 个工具面：Lead 有什么，子执行者有什么

### 7.1 Lead 的完整工具面

实验 10 把它一条条打印出来：

```text
[1] Lead 工具面共 23 个：
       1. shell              8. load_skill          15. keep_worktree      22. connect_mcp
       2. read_file          9. create_task         16. remove_worktree    23. disconnect_mcp
       3. write_file        10. get_task            17. schedule_cron
       4. edit_file         11. list_tasks          18. spawn_teammate
       5. glob              12. claim_task          19. send_message
       6. todo_write        13. complete_task       20. request_shutdown
       7. task              14. create_worktree     21. review_plan
[2] Lead 有 request_shutdown / review_plan：true
[3] Lead 有 submit_plan：false
[4] Lead 有 connect_mcp / disconnect_mcp：true
```

按章节归组，能看出这 23 个是怎么攒起来的：

| 来源章节 | 工具 | 数量 |
| --- | --- | --- |
| P1 PowerShell | `shell` | 1 |
| P2 文件 | `read_file` `write_file` `edit_file` `glob` | 4 |
| P5 TODO | `todo_write` | 1 |
| P6 Subagent | `task` | 1 |
| P7 Skill | `load_skill` | 1 |
| P17 SQLite 任务（含租约） | `create_task` `get_task` `list_tasks` `claim_task` `complete_task` | 5 |
| P18 Worktree | `create_worktree` `keep_worktree` `remove_worktree` | 3 |
| P14 Cron | `schedule_cron` | 1 |
| P15 队友 | `spawn_teammate` `send_message` | 2 |
| P16 协议（Lead 侧） | `request_shutdown` `review_plan` | 2 |
| P19 MCP 管理 | `connect_mcp` `disconnect_mcp` | 2 |
| | **合计** | **23** |

第 3 行"Lead 没有 `submit_plan`"是有意的。协议运行时暴露的工具分成两组（`bootstrap.ts` 第 197 到 202 行）：

```typescript
if (dependencies.protocolRuntime !== undefined) {
  // Lead 获得 request_shutdown/review_plan；submit_plan 只注册给队友 factory。
  const [requestShutdownTool, reviewPlanTool] =
    dependencies.protocolRuntime.leadToolDefinitions;
  tools.register(requestShutdownTool);
  tools.register(reviewPlanTool);
```

Lead 是审批方（`review_plan`）和终止请求方（`request_shutdown`），队友是提交方（`submit_plan`）。角色不同，工具就不同——Lead 拿到 `submit_plan` 等于允许它给自己批计划。

### 7.2 队友被裁剪成什么样

队友的工具集是**显式列举**出来的，不是"Lead 的减去几个"（第 210 到 224 行）：

```typescript
teammateRuntime.configureRunnerFactory((name, role, sendDefinition) => {
  // 持续 Teammate 只获得受限文件/消息工具与协议/任务工具，不暴露 create_task。
  const teammateTools = standardTools.tools.subset(["shell", "read_file", "write_file"]);
  teammateTools.register(sendDefinition);
  if (dependencies.protocolRuntime !== undefined) {
    teammateTools.register(dependencies.protocolRuntime.submitPlanToolDefinition);
  }
  if (dependencies.workStealingRuntime !== undefined) {
    // 队友任务工具使用 worker 专用注册入口，与 Lead 的 create_task 边界保持分离。
    registerTeammateLeasedTaskTools(
      teammateTools,
      dependencies.workStealingRuntime.store,
      dependencies.workStealingRuntime.claimService,
    );
  }
```

实验 10 后两行验证了结果：

```text
[5] 队友被裁剪到的内置工具：[shell, read_file, write_file]，再加 send_message / submit_plan 与租约任务工具
[6] 队友能看到 connect_mcp 吗：false
```

`subset(["shell", "read_file", "write_file"])` 是白名单写法。这个选择很关键——如果写成"排除 `connect_mcp` 等等"的黑名单，那么以后任何新增的 Lead 工具都会自动漏给队友。白名单的默认值是"不给"，加工具时必须显式加。

`subset()` 的实现顺手做了拼写检查：

```typescript
subset(names: readonly string[]): ToolRegistry {
  // subset 只暴露显式声明的工具，队友运行时用它限制模型可调用的能力。
  const definitions = new Map<string, StoredToolDefinition>();
  for (const name of names) {
    const definition = this.#definitions.get(name);
    if (definition === undefined) throw new Error(`tool does not exist: ${name}`);
    definitions.set(name, definition);
  }
  return new ToolRegistry(definitions, true, this.#version);
}
```

名字写错就抛 `tool does not exist: xxx`，而不是安静地少给一个工具。

### 7.3 队友为什么不能碰 MCP

对照第 6 行的 `false`，三个理由：

```text
[1] MCP 连接是进程级资源
      连上一个 stdio server 会起一个子进程，归 AgentRunner 的资源生命周期管。
      队友是 Lead 的下级 Runner，它连的 server 谁负责关？

[2] allowlist 是本地信任决定
      "允许连哪些 server"属于 Lead 层的信任配置。
      让队友也能 connect，等于把这个决定下放给了一个权限更低的执行者。

[3] 工具面必须可预测
      队友的工具集是启动时确定的白名单。
      如果队友能 connect，它的工具面就会在运行中自己长出新工具——
      这跟"子执行者能力必须受限且可枚举"直接冲突。
```

补一条：队友的权限策略比 Lead 多一条规则（第 115 到 118 行）：

```typescript
const teammatePermissionPolicy =
  dependencies.protocolRuntime === undefined || basePermissionPolicy === undefined
    ? basePermissionPolicy
    : basePermissionPolicy.withRules([dependencies.protocolRuntime.planGateRule]);
```

`withRules` 是在基础策略之上**追加**，不是替换。所以队友身上同时有 Lead 的全部规则加上 plan gate——计划没获批之前，它的副作用工具会被拦。


---

## 八、三条状态线：谁也不能代替谁

到 P20，Agent 手里同时有三份持久状态。它们经常被误当成"同一件事的三个视角"，其实是三条独立的线。

```text
状态线 1：SQLite 任务                    状态线 2：协议状态机
  ┌─────────────────────────┐            ┌──────────────────────────┐
  │ 任务 id / 标题 / 依赖    │            │ 计划：draft → submitted   │
  │ status（DAG 推进）      │            │        → approved/rejected│
  │ owner（谁认领的）        │            │ 关停：requested → agreed  │
  │ claim token（认领凭证）  │            │                          │
  │ lease（租约到期时间）    │            │ 存储：JsonProtocolStore   │
  │ 存储：SqliteTaskStore   │            └──────────────────────────┘
  └─────────────────────────┘
                    状态线 3：Worktree 绑定
                      ┌──────────────────────────────┐
                      │ 任务 id ↔ Git worktree 目录   │
                      │ 分支名 / 保留还是删除          │
                      │ 存储：WorktreeRuntime        │
                      └──────────────────────────────┘
```

为什么不能合并？各举一个具体场景：

| 场景 | 只有任务状态会怎样 | 只有协议状态会怎样 | 只有 worktree 会怎样 |
| --- | --- | --- | --- |
| 队友提交计划等审批 | 任务还是 `claimed`，看不出它在等人 | 知道在等审批，但不知道等的是哪个任务 | 目录建好了，但没人知道该不该开工 |
| 任务完成但目录要留着复查 | 任务 `completed`，目录信息丢了 | 协议里没有这个概念 | 目录在，但不知道对应任务是否已完成 |
| Lead 请求关停，队友手上还有活 | 看到有 `claimed` 任务，不知道有人要关停 | 知道关停在协商，不知道还剩几个任务 | 只知道有几个目录还开着 |

三条线各自回答一个不同的问题：**任务进行到哪一步**（SQLite）、**参与者之间在协商什么**（协议）、**代码改在哪个目录**（worktree）。合并任意两条都会丢掉第三个问题的答案。

这也是 3.4 节第四组身份检查存在的理由：任务状态和 worktree 绑定虽然是两条线，但必须由同一条链路写入，否则会出现"认领了但没目录"或"有目录但没认领记录"。

---

## 九、后台、Cron、队友：三个来源，一个收件箱

### 9.1 为什么必须是一个收件箱

三种异步信号：

```text
后台任务跑完了          （P13 JobSupervisor）
Cron 到点触发了         （P14 CronRuntime）
队友发来一条消息         （P15 TeammateRuntime）
```

它们的共同点是：**都发生在模型回合之外，都需要在某个时刻进入模型上下文**。

如果各自一个通道，Runner 就要在每次请求前依次问三家"有没有新东西"，还要自己排优先级、自己去重、自己处理"三家同时有"的情况。更麻烦的是顺序——三个通道的事件按什么顺序进历史？两次运行会不会顺序不同？

一个 `EventInbox` 把这些问题一次解决：事件在入口就排好了序，Runner 只需要一个消费点。

`bootstrap.ts` 选事件泵的方式很直接（第 290 到 292 行）：

```typescript
const eventPump =
  dependencies.teammateRuntime ?? dependencies.cronRuntime ?? dependencies.backgroundSupervisor;
// 事件泵按队友 > cron > 后台的优先级选择，保证同一轮只有一个消费者。
```

`??` 链的意思是"取第一个存在的"。因为三者共享同一个 `EventInbox`（3.4 节的身份检查保证了这一点），所以从哪个对象上读都是同一个队列。"保证同一轮只有一个消费者"是这行代码的全部目的——两个消费者会互相抢事件。

### 9.2 消费一个事件的完整流程

代码在 `loop.ts` 第 693 到 751 行。逐段拆：

**第一步，延迟队列优先（L704 到 707）：**

```typescript
let events =
  this.#deferredRuntimeEvents.length > 0
    ? Object.freeze([this.#deferredRuntimeEvents.shift() as RuntimeEvent])
    : this.#eventPump.drainEvents(1);
```

上一轮被推迟的事件排在新事件前面，不会被无限饿死。

**第二步，该不该现在收（L716 到 725）：**

```typescript
if (event.contextIdentity !== undefined && !allowContextEvents) {
  this.#deferredRuntimeEvents.push(event);
  events = this.#eventPump.drainEvents(1);
  continue;
}
if (event.toPayload().kind === "mailbox" && !allowContextEvents) {
  this.#deferredRuntimeEvents.push(event);
  events = this.#eventPump.drainEvents(1);
  continue;
}
```

两类事件在用户回合里会被推迟：带 `contextIdentity` 的（属于别的身份的上下文事件）和 `mailbox` 类的（队友消息）。原因是 L367 那句注释：

```typescript
// 用户回合内只接收普通后台事件；Cron/mailbox 上下文事件会被延迟，避免打断当前用户会话。
```

用户正在对话，队友消息突然插进历史会打断当前的推理链。这类事件留给 `runEvents()` 处理——它是 Cron 和 mailbox wakeup 的专用入口，调用时 `allowContextEvents` 传 `true`（L318）。

**第三步，去重（L726 到 732）：**

```typescript
const isNewEvent = !this.#seenEventIds.has(event.eventId);
const retryingAcknowledgement = this.#pendingEventAcks.has(event.eventId);
if (!isNewEvent && !retryingAcknowledgement) {
  // 只有首次看到的普通事件写历史；重复事件只补 ack，避免后台终态重复注入。
  events = this.#eventPump.drainEvents(1);
  continue;
}
```

`#seenEventIds` 记住见过的 id。重复事件直接跳过，不写历史。这就是"至少一次投递"变成"对模型恰好一次"的地方。

**第四步，先写历史再 ack（L733 到 738）：**

```typescript
if (isNewEvent) {
  this.#history.push(runtimeEventMessage(event));
  this.#seenEventIds.add(event.eventId);
}
try {
  await this.#eventPump.acknowledgeEvents(events);
```

顺序是"先入历史，后 ack"。反过来会丢事件：ack 成功之后进程崩了，事件已经从队列删掉但没进历史，模型永远不知道。

**第五步，ack 失败要回滚（L739 到 746）：**

```typescript
} catch (error) {
  if (event.toPayload().kind !== "mailbox" && isNewEvent) {
    this.#history.pop();
    this.#seenEventIds.delete(event.eventId);
  }
  if (event.toPayload().kind === "mailbox") this.#pendingEventAcks.add(event.eventId);
  throw error;
}
```

普通事件回滚（把刚 push 的消息弹出、把 id 从 seen 里删掉），下次重新消费。mailbox 事件不回滚，而是记进 `#pendingEventAcks` 等待补 ack——因为队友消息已经进历史被模型看到了，撤回它会让上下文自相矛盾，只补 ack 更安全。

### 9.3 事件长什么样

`runtimeEventMessage(event)` 把事件包成一条**普通的 user 消息**，内容是 `{"runtime_event": ...}`。

它不是假的 tool result。这一点很重要：事件跟任何 tool call 都没有配对关系，如果伪装成 tool result 塞进历史，`validateToolPairing` 立刻会报 `orphan tool result id`。事件是"外部世界发生的事"，用 user 角色表达最自然。

实验 8 验证了整条链路：

```text
[1] 第 1 次 runEvents() → 收到 1 条 user 消息
[2] 第 2 次 runEvents()（同一个 eventId 再投一遍）→ undefined
[3] canonical history 里的 runtime_event 条数：1
[4] ack 记录：[evt-1]
```

同一个 `eventId` 投两次，第二次返回 `undefined`（没有可消费的新事件），历史里始终只有 1 条。

---

## 十、恢复矩阵：失败在模型边界内消化

### 10.1 四种失败，四种处理

`RecoveryManager` 只认四种模型边界错误，其余原样上抛（`features/recovery.ts` 第 299 到 340 行）：

| 失败 | 判断依据 | 处理动作 | 副作用 |
| --- | --- | --- | --- |
| 429 限流 | `ModelRateLimitError` | 按 `Retry-After` 或退避算法 sleep，重试 | `consecutive529` 归零 |
| 529 过载 | `ModelOverloadedError` | 计数 +1，达阈值切 fallback 模型，退避重试 | 达阈值后 `consecutive529` 归零 |
| 输出截断 | `finishReason === "length"` | 首次提升输出预算重试；再次则续写拼接 | 残缺回复被完全丢弃 |
| 输入过长 | `ModelPromptTooLongError` | 保留首条 system，对请求快照压缩一次后重试 | `hasAttemptedReactiveCompact = true` |
| 其他 | —— | `throw error` 原样上抛 | 无 |

429 和 529 的区别值得留意（第 301 到 317 行）：

```typescript
if (error instanceof ModelRateLimitError) {
  // 429 是限流信号，不累计到 529 fallback。
  state.consecutive529 = 0;
  transientFailures += 1;
  await this.#retryTransient(transientFailures, error.retryAfter, "rate limit", signal);
  continue;
}
if (error instanceof ModelOverloadedError) {
  // 529 连续达到阈值后切 fallback，下一次请求才显式携带新模型。
  state.consecutive529 += 1;
  if (state.consecutive529 >= this.#config.overloadFallbackThreshold) {
    state.currentModel = this.#config.fallbackModel;
    state.consecutive529 = 0;
  }
  transientFailures += 1;
  await this.#retryTransient(transientFailures, undefined, "overload", signal);
  continue;
}
```

429 说的是"你请求太快了"，等一会儿就好，换模型没用——所以它把 `consecutive529` 清零，不推动 fallback。529 说的是"这个模型现在扛不住"，连续几次之后换一个模型才有意义。两种都退避重试，但只有 529 会累计。

另外，529 走 `#retryTransient(..., undefined, ...)`——第二个参数是 `retryAfter`，传 `undefined` 意味着 529 不读服务端给的重试间隔，只用本地退避算法。

输出截断的两级处理（第 345 到 373 行）：

```typescript
if (!state.hasEscalated) {
  // 首次 length 提升输出预算并完全丢弃残缺回复，重试同一请求。
  state.currentMaxTokens = this.#config.escalatedMaxTokens;
  state.hasEscalated = true;
  continue;
}
```

"完全丢弃残缺回复"是第一级。第二级才是续写：把片段和续写提示追加到局部 `requestMessages`，最后 `mergeFragments` 合并。续写有两道限制：片段必须是纯文本（有 tool call 就抛 `RecoveryRetriesExhausted`），续写次数受 `maxContinuations` 约束。

### 10.2 实测一次完整恢复

实验 9 让模型替身连续抛出六种失败，然后看 Runner 怎么消化：

```text
[1] 模型侧真实失败序列：[429, 529, 529, 529, length, prompt-too-long]
[2] 主请求次数：7
[3] 每次请求用的模型：[primary, primary, primary, primary, fallback, fallback, fallback]
[4] 每次请求的 maxTokens：[8000, 8000, 8000, 8000, 8000, 64000, 64000]
[5] 最终回答：recovered
[6] canonical history 长度：2
[7] history 内容：user:验证恢复边界 | assistant:recovered
[8] history 里有 finish_reason=length 那条被丢弃的回复吗：false
```

对照着读，能把恢复矩阵的每一格都验证一遍：

```text
请求 1  primary  8000   → 429              退避重试，consecutive529 = 0
请求 2  primary  8000   → 529              consecutive529 = 1，未到阈值 3
请求 3  primary  8000   → 529              consecutive529 = 2，未到阈值
请求 4  primary  8000   → 529              consecutive529 = 3，达阈值 → 切 fallback
请求 5  fallback 8000   → length           首次 length → 预算 8000 提到 64000
请求 6  fallback 64000  → prompt-too-long  反应式压缩一次
请求 7  fallback 64000  → 正常回复 "recovered"
```

第 3 行的模型序列印证了"下一次请求才显式携带新模型"这句注释：第 4 次请求**仍然**用 primary（它就是那次触发阈值的请求），切换在第 5 次生效。同理第 4 行的 maxTokens：第 5 次请求还是 8000（它是触发 length 的那次），提升在第 6 次生效。

### 10.3 恢复过程一条都不进 canonical history

第 6、7、8 行是本节最重要的结论：**7 次模型请求、6 次失败，canonical history 里只有 2 条消息。**

```text
history 内容：user:验证恢复边界 | assistant:recovered
```

一条用户输入，一条最终回答。中间的退避、模型切换、预算提升、被丢弃的截断回复、压缩后的请求快照——全都不在里面。

实现上靠的是"局部变量"。看 `complete()` 的开头（第 279 到 283 行）：

```typescript
let requestMessages = Object.freeze([...request.messages]);
const fragments: string[] = [];
let promptTooLongRetries = 0;
let transientFailures = 0;
state.hasAttemptedReactiveCompact = false;
```

`requestMessages` 是 `request.messages` 的一份**拷贝**。后续所有重试都在改这个局部变量：

```typescript
requestMessages = Object.freeze([...leadingSystem, ...outcome.history]);   // 压缩
requestMessages = Object.freeze([...requestMessages, reply.message, userMessage(CONTINUATION_PROMPT)]);  // 续写
```

`AgentRunner` 那边的 `this.#history` 完全不知道这些事。它调用 `modelRequestExecutor.complete(request)`，拿回一个成功的 `ModelReply`，push 一条 assistant 消息，结束。

为什么必须这样？三个理由：

```text
[1] 压缩的输入必须干净
      如果六次失败的中间态都在历史里，下次压缩就要去总结"我们重试了六次"——
      这对任务本身毫无信息量，纯粹是噪音。

[2] 恢复是实现细节，不是对话内容
      模型不需要知道自己刚才被限流了。让它知道反而可能让它改变行为
      （比如"既然刚才超长了，我就少说点"）。

[3] 重放必须确定
      历史是可持久化、可重放的。如果它包含重试痕迹，那么同一次会话
      在不同网络条件下会产生不同的历史，重放结果就不确定了。
```

顺带说一句 `beginTurn()` 的作用（`loop.ts` L351）：

```typescript
this.#modelRequestExecutor?.beginTurn();
```

它在每个回合开始时重置恢复状态（第 250 行注释："状态在回合边界重置，避免 fallback、长度升级或重试计数泄漏到下一次请求"）。上一个回合切到 fallback 模型、提升了输出预算，不应该影响下一个回合的起点。

---

## 十一、MCP 边界，以及一处真实的行为回退

### 11.1 P20 继承的 MCP 边界

第 19 章建立的四道边界，P20 一条没改：

```text
[1] 能连谁 —— 本地 allowlist
      connect_mcp 只接受一个 alias 字符串，不接受 command/args。
      CLI 里写死两个：demo_alpha、demo_beta。

[2] 算什么权限 —— 本地 policy
      远程 description 里写什么都不影响 effect。
      CLI 里写死四条：lookup/fail/delay 是 read，terminate 是 external。

[3] 叫什么名 —— 本地规范化
      mcp__{alias}__{tool}。两个 server 都发布 lookup 也不会撞。

[4] 收什么参数 —— Ajv 校验远程 schema
      schema 深克隆并冻结，编译成同步谓词后才发布。
```

`cli.ts` 里的 policy 声明就是第二道边界的实体（第 302 到 308 行）：

```typescript
// 本地演示允许 allowlist 固定声明四个远程工具及其 effect；生产应由配置提供。
const policies = Object.freeze([
  new McpToolPolicy({ remoteName: "lookup", effect: "read" }),
  new McpToolPolicy({ remoteName: "fail", effect: "read" }),
  new McpToolPolicy({ remoteName: "delay", effect: "read" }),
  new McpToolPolicy({ remoteName: "terminate", effect: "external" }),
]);
```

`terminate` 被标成 `external`，其余三个是 `read`。注意"生产应由配置提供"这句注释——demo 写死是为了教学，真实项目应该从配置文件读。

管理工具本身也带 `effect` 和 `source`（`features/mcp-tools.ts` 第 296 到 325 行）：

```typescript
this.#leadToolDefinitions = Object.freeze([
  { name: "connect_mcp", description: "Connect one locally allowlisted MCP server alias.",
    inputSchema: aliasInputSchema, effect: "external", source: "mcp:management", handler: connect },
  { name: "disconnect_mcp", description: "Disconnect one MCP server alias and withdraw its tools.",
    inputSchema: aliasInputSchema, effect: "external", source: "mcp:management", handler: disconnect },
]);
```

两个都是 `effect: "external"`、`source: "mcp:management"`。记住这两个字段，下一节要用。

### 11.2 P19 有一条审批规则，P20 没有了

这是本章唯一一处需要你自己动手确认的地方，因为它是一处**真实的行为回退**，不是设计意图。

打开两个文件对比 `permissionPolicyForProfile()`：

`code/chapters/ch19/src/bootstrap.ts` 构造的是一个可变数组，然后按能力追加第二条规则：

```typescript
const rules: PermissionRule[] = [
  new PermissionRule({
    name: "confirm-file-write",
    behavior: "ask",
    reason: "File writes require explicit approval from chapter 3 onward",
    matches: (request) => {
      const name = request.prepared.definition?.name;
      return name === "write_file" || name === "edit_file";
    },
  }),
];
if (profile.capabilities.has("mcp")) {
  // MCP 管理工具与远程 external 工具需要终端审批，规则只读取 source 与 effect。
  rules.push(
    new PermissionRule({
      name: "mcp-external-approval",
      behavior: "ask",
      reason: "MCP external tools require terminal approval",
      matches: (request) => {
        const definition = request.prepared.definition;
        return definition?.source.startsWith("mcp:") === true && definition.effect === "external";
      },
    }),
  );
}
```

`code/chapters/ch20/src/bootstrap.ts` 只有一条规则，`rules` 直接写成内联字面量：

```typescript
return new PermissionPolicy({
  rules: [
    new PermissionRule({
      name: "confirm-file-write",
      behavior: "ask",
      reason: "File writes require explicit approval from chapter 3 onward",
      matches: (request) => {
        const name = request.prepared.definition?.name;
        return name === "write_file" || name === "edit_file";
      },
    }),
  ],
  approval: dependencies.approvalProvider,
  audit: dependencies.auditSink,
  writeBoundary: fileSystem,
});
```

`mcp-external-approval` 整条不见了。

### 11.3 三条独立证据

一个差异可能是我看错了，三条互相独立的证据才能定性。

**证据一：代码差异**（上面已列）。ch20 的 `permissionPolicyForProfile` 里没有任何读 `source.startsWith("mcp:")` 的规则。

**证据二：测试被删了。** ch20 继承了 ch19 的 bootstrap 测试文件（`tests/ch19-bootstrap.test.ts`），但少了三个测试：

```text
ch19/tests/ch19-bootstrap.test.ts  469 行，MCP 相关 6 个测试
ch20/tests/ch19-bootstrap.test.ts  286 行，MCP 相关 3 个测试

ch20 里没有的三个：
  - "asks before connecting MCP and keeps the transport closed when denied"
  - "asks before invoking an MCP external tool and keeps the remote handler zero-call when denied"
  - "asks before disconnecting MCP and keeps the connection open when denied"

连带删掉的辅助类：ExternalToolModel、DisconnectToolModel、SelectableApproval，
以及 Connection.callCalls / Factory.openCalls 这两个调用计数器。
```

三个被删的测试，测的正好就是那条被删的规则。

**证据三：运行时行为。** 第一节实验 4 的输出：

```text
[8] probe-connect → {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup"]}
[10] 审批被问到的工具：[]（deny 强于 ask，越界写不进审批）
```

`connect_mcp` 成功执行了，审批提供方一次都没被问。在 P19 里，这次调用会先弹出终端审批。

### 11.4 影响范围与结论

先说清哪些**没有**受影响：

| 仍然有效的边界 | 为什么不受影响 |
| --- | --- |
| allowlist | 在 `McpRuntime` 构造时固定，跟权限规则无关 |
| 本地 policy 决定 effect | 在 `mcp-tools.ts` 里实现，跟权限规则无关 |
| 名称隔离与 schema 校验 | 同上 |
| Lead-only | `install()` 只对 Lead registry 调用，跟权限规则无关 |
| 工作区写边界 | `#workspaceBoundaryDecision` 是硬编码的，不是规则 |
| shell 默认审批 | `shellDefaultDecision` 是硬编码的，不是规则 |

受影响的只有一件事：**`effect: "external"` 的 MCP 工具不再触发终端审批。** 具体是 `connect_mcp`、`disconnect_mcp`，以及 policy 里标成 `external` 的远程工具（demo 中的 `terminate`）。

按本章的三条不变量来判断，这确实是个缺陷：`cli.ts` 仍然把 `terminate` 声明为 `external`，说明"这个操作有副作用、需要人确认"的意图还在；但 P20 的策略里已经没有把这个意图转成审批的规则了。声明和执行脱节。

补回来的改法很直接——把 ch19 那段 `if (profile.capabilities.has("mcp"))` 原样搬回 ch20 的 `permissionPolicyForProfile`，再把三个被删的测试搬回来。本章不做这个改动，因为本章的任务是讲清 P20 的现状；但你在读代码时看到 `terminate` 是 `external` 却不弹审批，那不是你理解错了。

你可以自己两条命令确认一遍。先跑 ch19 那个被删掉的测试，它在 ch19 里是通过的：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npx vitest run chapters/ch19/tests/ch19-bootstrap.test.ts -t "asks before connecting MCP"
```

```text
 Test Files  1 passed (1)
      Tests  1 passed | 5 skipped (6)
```

再在 ch20 的同名文件里找这个测试，一条都搜不到：

```powershell
Select-String -Path 'chapters\ch20\tests\ch19-bootstrap.test.ts' -Pattern 'asks before'
```

没有输出。两个文件里 `test(` 的数量也能直接数出来：ch19 是 6 个（469 行），ch20 是 3 个（286 行）。

### 11.5 顺便记住这个排查方法

这一节的价值不只在于这条具体的规则。它演示了一个可复用的排查套路：

```text
[1] 从"声明"出发
      cli.ts 说 terminate 是 external → 意图是"需要审批"

[2] 找"执行"
      谁把 external 变成审批？→ 搜权限规则 → 没找到

[3] 找"历史"
      上一章有吗？→ diff bootstrap.ts → ch19 有，ch20 没有

[4] 找"测试"
      这个行为原来有测试吗？→ diff 测试文件 → 三个测试被删了

[5] 跑一遍
      实测审批次数 → 0 次 → 确认
```

"声明和执行分离"是这类缺陷的共同形状。只要一个系统里存在"用数据表达意图"的地方（这里是 `effect` 字段），就要能指出"把意图变成行为"的那段代码在哪。指不出来，意图就是注释。

---

## 十二、资源注册与逆序关闭

### 12.1 注册顺序

`bootstrap.ts` 第 293 到 299 行：

```typescript
// 事件泵按队友 > cron > 后台的优先级选择，保证同一轮只有一个消费者。
// Full Harness 把所有异步运行时交给 AgentRunner 统一管理，关闭时按注册顺序逆序释放。
const resources: readonly AsyncResource[] = Object.freeze([
  ...(dependencies.backgroundSupervisor === undefined ? [] : [dependencies.backgroundSupervisor]),
  ...(dependencies.cronRuntime === undefined ? [] : [dependencies.cronRuntime]),
  ...(dependencies.teammateRuntime === undefined ? [] : [dependencies.teammateRuntime]),
  ...(dependencies.mcpRuntime === undefined ? [] : [dependencies.mcpRuntime]),
]);
```

顺序是 `JobSupervisor → CronRuntime → TeammateRuntime → McpRuntime`。这个顺序按依赖方向排：后面的依赖前面的。Cron 要往 supervisor 提交任务，队友要用 supervisor 和 Cron，MCP 最独立所以排最后。

### 12.2 关闭走反向

`loop.ts` 第 662 到 690 行，整个 `close()`：

```typescript
// 重复关闭是幂等操作；正在关闭时拒绝并发调用，防止资源释放顺序错乱。
async close(): Promise<void> {
  if (this.#closed) {
    return;
  }
  if (this.#closing) {
    throw new AgentRunError("AgentRunner close is already in progress");
  }
  this.#closing = true;
  const failures: unknown[] = [];
  try {
    for (const resource of [...this.#resources].reverse()) {
      try {
        await resource.close();
      } catch (error) {
        failures.push(error);
      }
    }
  } finally {
    this.#closing = false;
  }
  if (failures.length === 1) {
    throw failures[0];
  }
  if (failures.length > 1) {
    throw new AggregateError(failures, "AgentRunner close failed");
  }
  this.#closed = true;
}
```

29 行代码，五个决定：

**`[...this.#resources].reverse()`——逆序，而且先拷贝。**

`reverse()` 会原地修改数组，所以必须先 `[...]` 拷一份，否则 `#resources` 本身就被改掉了。逆序的理由跟注册顺序对应：MCP 最先关（它不被任何人依赖），supervisor 最后关（大家都可能还在用它）。

反过来会怎样？如果先关 supervisor，那么正在关闭的队友运行时可能还要往它提交清理任务，得到的是"supervisor 已关闭"的错误。

**每个资源的 `close()` 单独 try/catch——失败不中断。**

一个资源关不掉，剩下的还得继续关。这是资源清理的通用原则：不能因为第一个失败就泄漏后面所有的。

**一个失败原样抛，多个失败用 `AggregateError`。**

```typescript
if (failures.length === 1) {
  throw failures[0];
}
if (failures.length > 1) {
  throw new AggregateError(failures, "AgentRunner close failed");
}
```

单个失败保留原始错误类型和堆栈，方便定位。多个失败必须汇总——只抛第一个会让排查的人以为只坏了一个，修完继续坏。

**`#closed = true` 只在全部成功后才设。**

它在最后一行，前面任何 `throw` 都会跳过它。这让关闭失败后可以再次调用 `close()` 重试。MCP 运行时里有个 `pendingCloses` 集合专门配合这件事：已从注册表撤销、但 `connection.close()` 还没成功的连接留在里面，等下一次 `close()` 重试。

**`#closing` 挡并发。**

同时调两次 `close()`，第二次会抛 `AgentRunner close is already in progress`。它跟 `#closed` 的区别是：`#closed` 表示"关完了，再调是空操作"，`#closing` 表示"正在关，不许插队"。两个状态都会让 `#ensureOpen()` 拒绝新回合（L754）：

```typescript
#ensureOpen(): void {
  if (this.#closed || this.#closing) {
    throw new AgentRunError("AgentRunner is closed");
  }
}
```

### 12.3 实测

实验 7 用一个记录关闭轨迹的假资源验证了全部行为：

```text
[1] 注册顺序 supervisor → cron → teammate → mcp，关闭顺序：mcp → teammate → cron → supervisor
[2] 再 close() 一次，关闭轨迹仍是 4 条：true
[3] 关闭后还能 run() 吗 → AgentRunner is closed
[4] 两个资源关闭失败 → AggregateError: AgentRunner close failed
[5] 聚合里的每一条：[teammate close failed | supervisor close failed]
[6] 失败也走完了全部资源：mcp → teammate → cron → supervisor
```

第 2 行验证幂等：第二次 `close()` 没有再关一遍，轨迹还是 4 条。第 6 行验证"失败不中断"：两个资源关闭失败（teammate 和 supervisor），但 4 个资源都被尝试过了。

第 5 行的顺序值得看一眼：`[teammate close failed | supervisor close failed]`。teammate 排在前面，因为逆序关闭时它比 supervisor 先被处理。`AggregateError` 里的顺序就是失败发生的顺序。

### 12.4 谁负责调 close()

CLI 侧在 `runProfile()` 的 `finally` 里调（`cli.ts`）。入口文件只有 5 行，不碰任何清理逻辑：

```typescript
import { runProfile } from "../cli.js";
import { P20 } from "../core/profiles.js";

// 固定入口运行 P20 Full Harness，并由 CLI 负责结束后的资源释放与错误码返回。
process.exitCode = await runProfile(P20, process.argv.slice(2));
```

这也是二十章共用同一套运行时的好处：任何一章修好了关闭逻辑，所有引入该能力的章节一起受益，不需要在 20 个入口里各改一遍。

---

## 动手运行第 20 章

下面六步是我实际跑通的顺序。每一步都给真实输出，你能对照确认自己走到哪了。

### 第 1 步：确认环境

```powershell
node --version
npm --version
```

我这台机器上是：

```text
v25.7.0
11.19.0
```

`package.json` 要求 `"node": ">=20.12"`。低于这个版本先升级，别急着往下走。

### 第 2 步：安装依赖

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm install
```

第 20 章**没有新增任何依赖**。这本身就是"P20 不引入新运行时"的一个侧面证据。整个仓库的依赖是：

| 依赖 | 版本 | 哪一章引入 |
| --- | --- | --- |
| `openai` | `7.0.0` | P1 |
| `zod` | `4.4.3` | P2 |
| `yaml` | `2.9.0` | P7 |
| `dotenv` | `17.2.3` | P1 |
| `proper-lockfile` | `4.1.2` | P12 |
| `cron-parser` | `5.6.2` | P14 |
| `sql.js` | `1.14.1` | P17 |
| `@modelcontextprotocol/sdk` | `^1.30.0` | P19 |
| `ajv` | `^8.20.0` | P19 |

### 第 3 步：跑测试，确认代码是好的

```powershell
npm run test:ch20
```

```text
 Test Files  65 passed (65)
      Tests  428 passed (428)
   Duration  18.11s
```

`npm test` 跑的是同一条命令（`vitest run chapters/ch20/tests`），因为 P20 就是仓库的默认验收目标。

这 65 个文件不是"第 20 章的 65 个测试"，而是**累计的全套测试**——`chapters/ch20/tests/` 目录里同时放着从 ch01 到 ch20 的所有测试文件。第 20 章自己新增的只有两个：

```powershell
npx vitest run chapters/ch20/tests/ch20-full-harness.test.ts chapters/ch20/tests/ch20-entry.test.ts
```

```text
 Test Files  2 passed (2)
      Tests  11 passed (11)
```

11 个测试 = `ch20-full-harness.test.ts` 的 7 个跨功能场景 + `ch20-entry.test.ts` 的 4 个档案测试。

**输出里有几行看着像错误的东西，那是正常的。** 这些是测试故意触发失败路径时打印的：

```text
运行失败: workspace is not a Git repository root
运行失败: workspace is not a Git repository root
运行失败: workspace is not a Git repository root
运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

判断标准只看最后的 `Test Files ... passed` 和 `Tests ... passed`。

### 第 4 步：准备一个 Git 仓库当工作目录

这一步最容易卡住。P20 继承了 P18 的 worktree 能力，而 worktree 要求**运行目录必须是 Git 仓库根目录**（`cli.ts` 第 163 到 164 行）：

```typescript
// 在创建任何运行状态前先验证 cwd 是 Git repository root，避免在错误目录留下任务库。
if (worktreeRuntime !== undefined) await worktreeRuntime.validateRepository();
```

直接在 `code/` 里跑会失败，因为 `code/` 不是仓库根：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run ch20 -- --prompt "test"
```

```text
运行失败: workspace is not a Git repository root
```

所以另开一个目录：

```powershell
New-Item -ItemType Directory -Path 'C:\tmp\ch20-demo' -Force
Set-Location 'C:\tmp\ch20-demo'
git init
```

然后把配置放进去。`cli.ts` 从 `process.cwd()` 下的 `.env` 读（第 213 到 217 行）：

```typescript
const envPath = resolve(workspace, ".env");
const requiresFallback = profile.capabilities.has("recovery");
const settings = existsSync(envPath)
  ? settingsFromEnvFile(envPath, requiresFallback)
  : settingsFromMapping({}, requiresFallback);
```

P20 有 `recovery` 能力，所以 `OPENAI_FALLBACK_MODEL` 也是必需的。四个键：

```text
OPENAI_BASE_URL=...
OPENAI_API_KEY=...
OPENAI_MODEL=...
OPENAI_FALLBACK_MODEL=...
```

缺键会把缺的都列出来：

```text
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

注意 `.env` 里是真实密钥，别提交进 Git。`C:\tmp\ch20-demo` 是临时目录，跑完删掉就行。

### 第 5 步：从新目录调用章节入口

`npm run ch20` 会把 cwd 设成 `code/`，所以不能用它。改用 `npm exec --prefix` 指向依赖，cwd 留在你的仓库里：

```powershell
Set-Location 'C:\tmp\ch20-demo'
npm exec --prefix 'F:\笔记\Agent实操\code' -- tsx 'F:\笔记\Agent实操\code\chapters\ch20\src\chapters\ch20.ts' --prompt "先连接 demo_alpha，然后在下一步用 lookup 查 needle"
```

prompt 里那句"在下一步"是有必要的。5.1 节讲过快照边界：同一条回复里连接完立刻调用，一定拿 `unknown_tool`。说清分两步，模型才会先 connect、等结果、下一轮再调。

参数只认 `--chapter` 和 `--prompt`，其他一律报错（第 110 行注释）：

```typescript
// 只接受 --chapter/--prompt；固定章节入口会自动补上 chapter，其他参数立即失败。
```

固定入口会自动补 chapter，所以你只需要给 `--prompt`。

### 第 6 步：确认子进程真的被清理了

跑完检查有没有残留的 demo server：

```powershell
Get-Process node -ErrorAction SilentlyContinue | Select-Object Id, StartTime
```

正常情况下不该看到还在跑的 demo 进程。这条链是 `runProfile` 的 finally → `runner.close()` → `resources` 逆序 → `mcpRuntime.close()` → 每条连接 `close()`，在实验 7 里逐步验证过。

`cli.ts` 里还有一条兜底路径值得看（第 188 到 198 行）：

```typescript
} else {
  // buildAgent 失败时没有统一 resources，需要按创建顺序兜底关闭。
  for (const resource of [mcpRuntime, teammateRuntime, cronRuntime, backgroundSupervisor]) {
    if (resource === undefined) continue;
    try {
      await resource.close();
    } catch (error) {
      failures.push(error);
    }
  }
}
```

`buildAgent` 失败时（比如 3.2 节那七种依赖错误），Runner 还没建起来，`resources` 也就不存在。这时 CLI 自己按逆序关一遍已经创建的运行时。注意这个数组的顺序 `[mcpRuntime, teammateRuntime, cronRuntime, backgroundSupervisor]` 就是 12.1 节注册顺序的反向，跟 `close()` 的逻辑保持一致。

---

## 常见报错排查表

按你会遇到的顺序排。

### 启动阶段：装配错误

这一组全部来自 `validateBuildDependencies()`，报错时进程刚起来，什么都没写。

| 报错 | 原因 | 怎么办 |
| --- | --- | --- |
| `profile must be a fixed chapter profile` | 传了自造的 profile 对象 | 用 `P20` 或 `profileForChapter(20)`，不要手工拼能力集 |
| `chapter must be an integer from 1 to 20` | `profileForChapter()` 传了越界值或小数 | 只有 1 到 20 是有效章节 |
| `cronRuntime must share the background supervisor and event inbox` | Cron 的 supervisor / inbox 不是你传的那个对象 | 一个 `JobSupervisor` 建一次，`CronRuntime` 用它构造 |
| `teammateRuntime must share the background supervisor, EventInbox, and CronRuntime` | 队友的三个依赖里至少一个不是同一对象 | 队友运行时必须用同一个 supervisor 和同一个 CronRuntime 构造 |
| `protocolRuntime must share the teammate runtime and mailbox store` | 协议运行时引用了另一个队友运行时或另一个信箱 | 先建 `TeammateRuntime`，再用它建 `ProtocolRuntime` |
| `worktreeRuntime must be the WorkStealingRuntime claim service and share its store` | worktree 不是工作窃取的 claimService，或两者 store 不同 | 把 `WorktreeRuntime` 本身作为 `claimService` 传给 `WorkStealingRuntime` |
| `mcpRuntime is required for chapter 19 or later` | profile 有 `mcp` 能力但没传 runtime | 补 `mcpRuntime` |
| `mcpRuntime requires chapter 19 or later` | 给 P18 及以下传了 `mcpRuntime` | 换成 P19/P20，或者别传 |
| `recoveryConfig is required for chapter 11 or later` | P11 起必须给恢复配置 | 补 `recoveryConfig` |
| `recovery capability requires compaction` | 有 `recovery` 但没有 `compaction` | 这是能力组合错误，用固定 profile 就不会出现 |
| `approvalProvider is required for chapter 3 or later` | P3 起权限策略必须有审批提供方 | 补 `approvalProvider` |
| `auditSink is required for chapter 3 or later` | 同上，审计也是必需的 | 补 `auditSink` |
| `MCP runtime is already installed` | 一个 `McpRuntime` 装到了两个 registry | 一个 runtime 只能绑一个 registry |
| `tool does not exist: xxx` | `subset()` 里的工具名写错了 | 对照 7.1 节的 23 个名字检查拼写 |

### 启动阶段：环境错误

| 报错 | 原因 | 怎么办 |
| --- | --- | --- |
| `运行失败: workspace is not a Git repository root` | cwd 不是 Git 仓库根（P18 worktree 要求） | 在 `git init` 过的目录里运行 |
| `配置错误: Missing required settings: ...` | cwd 下 `.env` 缺键 | 补齐列出的键；P20 还需要 `OPENAI_FALLBACK_MODEL` |
| `Both --chapter and --prompt are required` | 直接调 `cli.ts` 而没给参数 | 用 `chapters/ch20.ts` 入口，它会自动补 chapter |
| `--prompt must not be empty` | `--prompt` 后面跟了空字符串 | 给个真实的 prompt |

### 运行阶段：工具与权限

| 报错 | 原因 | 怎么办 |
| --- | --- | --- |
| `Error [unknown_tool]: Unknown tool: mcp__xxx__yyy` | 同一轮里刚连上就调（快照边界） | prompt 里说清分两步，让模型下一轮再调 |
| `Error [permission_denied]: Writing outside the workspace is forbidden` | 写路径在工作区外 | 这是硬边界，不能审批放行；改成工作区内的路径 |
| `tool registry snapshot is immutable` | 往快照里注册工具 | 快照只读；要改就改活注册表 |
| `tool definition was not registered by this registry` | 撤销时传的不是当初注册的那个对象 | 撤销要用对象身份，保留原始定义引用 |
| `resolved tool workspace is outside the controlled workspace root` | 工具解析出的执行目录跑到受控根外 | 检查 worktree 绑定链路，这属于装配错误级别 |
| `Approval was not explicitly granted: ...` | 审批提供方没明确给 allow/deny | 无交互环境下默认拒绝，这是有意的 |
| `mcp_timeout` | 超过 `toolTimeoutSeconds` | 连接会被丢弃，重连即可；调超时要改 spec |
| `mcp_connection_lost` | 子进程死了或 transport 断了 | 连接会被丢弃，重连即可 |
| `mcp_remote_error` | 远程自己报 `isError: true` | **连接还在**，看远程 server 的问题；本地拿不到细节（脱敏了） |

### 运行阶段：历史与消息

| 报错 | 原因 | 怎么办 |
| --- | --- | --- |
| `missing tool results for ids: ["xxx"]` | 有 tool call 没回填结果，或中间插了别的消息 | 每个 call 必须紧跟同 id 的 result |
| `unexpected tool result id: xxx` | 回填的 id 不在待回填集合里 | 检查 id 是否写错 |
| `orphan tool result id: xxx` | 没有对应 call 的结果，或同一 id 回填了两次 | 检查是否重复回填 |
| `Tool execution did not produce a paired result` | 结果处理器返回的数量和 call 数不一致 | 处理器必须一进一出 |
| `Model stopped without final text or tool calls` | 模型既没给文本也没给工具调用 | 模型侧问题；检查请求是否合法 |
| `Model output reached the token limit` | `finishReason === "length"` 且没有恢复管理器 | P11 起装了 `RecoveryManager` 就会自动处理 |
| `Model response was blocked by the content filter` | 内容过滤器拦了 | 改 prompt |
| `Agent exceeded maxTurns=N` | 回合数超限 | 调大 `maxTurns`，或检查是否陷入工具循环 |

### 关闭阶段

| 报错 | 原因 | 怎么办 |
| --- | --- | --- |
| `AgentRunner is closed` | 已关闭或正在关闭时还想 `run()` | 关闭后不能复用，重建一个 |
| `AgentRunner close is already in progress` | 并发调了两次 `close()` | 串行调用；等第一次 await 完 |
| `AggregateError: AgentRunner close failed` | 两个以上资源关闭失败 | 看 `error.errors` 数组里的每一条 |
| `AggregateError: CLI execution or cleanup failed` | 运行和清理都失败了 | 同上，逐条看 |
| `MCP connection validation failed and cleanup is pending: xxx` | 校验失败**而且**子进程关不掉 | 检查残留 node 进程；这个错误码专门提示"有东西泄漏了" |

---

## 10 个动手小实验

前面每一节引用的输出，全部来自同一个脚本。这一节带你把它建起来，一条一条跑。

三件事先说清：

- **不需要 API key。** 所有模型都是替身类，直接返回预设回复或抛预设错误。
- **不需要 Git 仓库。** 用到工作区的实验都在系统临时目录里建，跑完自动删。
- **不启动任何子进程。** 第 19 章的实验会真的 `spawn` 一个 demo server；这里用 `FakeConnection` 替代，因为本章关心的是"工具面怎么变"，不是"stdio 怎么通"。

### 准备：建一个探测脚本

```powershell
Set-Location 'F:\笔记\Agent实操\code'
New-Item -ItemType File -Path scripts\probe-ch20.mts -Force
```

`scripts/` 不在 `tsconfig.json` 的 `include` 里，所以这个文件不会影响 `npm run typecheck`。跑完删掉即可。

脚本分六段贴。第 1 段是 import，全部指向 `chapters/ch20/`——这一长串本身就说明了"一个组合根要拿到多少东西"：

```typescript
// scripts/probe-ch20.mts —— 第 20 章探测脚本（临时用，跑完可删）
import { mkdtemp, rm } from "node:fs/promises";
import { tmpdir } from "node:os";
import { join } from "node:path";

import { z } from "zod";

import { JsonBackgroundJobStore } from "../chapters/ch20/src/adapters/background-json.js";
import { JsonCronStore } from "../chapters/ch20/src/adapters/cron-json.js";
import { SubprocessGitRunner } from "../chapters/ch20/src/adapters/git.js";
import { FileMailboxStore } from "../chapters/ch20/src/adapters/mailbox-json.js";
import { AjvMcpSchemaValidator } from "../chapters/ch20/src/adapters/mcp-schema.js";
import { JsonProtocolStore } from "../chapters/ch20/src/adapters/protocol-json.js";
import { SqliteTaskStore } from "../chapters/ch20/src/adapters/task-sqlite.js";
import { buildAgent } from "../chapters/ch20/src/bootstrap.js";
import type { BuildDependencies } from "../chapters/ch20/src/bootstrap.js";
import { EventInbox } from "../chapters/ch20/src/core/events.js";
import type { RuntimeEvent } from "../chapters/ch20/src/core/events.js";
import { HookRegistry } from "../chapters/ch20/src/core/hooks.js";
import { AgentRunner } from "../chapters/ch20/src/core/loop.js";
import type { AsyncResource } from "../chapters/ch20/src/core/loop.js";
```

接着是 import 的后半段：

```typescript
import {
  assistantMessage,
  toolCall,
  toolMessage,
  userMessage,
  validateToolPairing,
} from "../chapters/ch20/src/core/messages.js";
import type { ChatMessage } from "../chapters/ch20/src/core/messages.js";
import type { ModelClient, ModelReply, ModelRequest } from "../chapters/ch20/src/core/model.js";
import {
  ModelOverloadedError,
  ModelPromptTooLongError,
  ModelRateLimitError,
} from "../chapters/ch20/src/core/model.js";
import { PermissionDecision } from "../chapters/ch20/src/core/permissions.js";
import type {
  ApprovalProvider,
  AuditSink,
  PermissionRequest,
} from "../chapters/ch20/src/core/permissions.js";
import { P19, P20, profileForChapter } from "../chapters/ch20/src/core/profiles.js";
import type { Capability, ChapterProfile } from "../chapters/ch20/src/core/profiles.js";
import { ToolRegistry, toolSuccess } from "../chapters/ch20/src/core/tools.js";
import type { ToolResult } from "../chapters/ch20/src/core/tools.js";
import { JobSupervisor } from "../chapters/ch20/src/features/background.js";
import { CronRuntime } from "../chapters/ch20/src/features/cron.js";
import {
  DynamicPromptProvider,
  DynamicPromptRenderer,
} from "../chapters/ch20/src/features/prompting.js";
import {
  McpCallResult,
  McpPublishedTool,
  McpRuntime,
  McpServerSpec,
  McpToolPolicy,
} from "../chapters/ch20/src/features/mcp-tools.js";
import type {
  McpConnection,
  McpConnectionFactory,
} from "../chapters/ch20/src/features/mcp-tools.js";
import { ProtocolRuntime } from "../chapters/ch20/src/features/protocol.js";
import { RecoveryConfig } from "../chapters/ch20/src/features/recovery.js";
import { TeammateRuntime } from "../chapters/ch20/src/features/teammates.js";
import { WorkStealingRuntime } from "../chapters/ch20/src/features/work-stealing.js";
import type { WorkStealingSleeper } from "../chapters/ch20/src/features/work-stealing.js";
import { WorktreeRuntime } from "../chapters/ch20/src/features/worktrees.js";
```

第 2 段是五个小工具。`section()` 让你能只跑其中一个实验；`reason()` 是本章最常用的一个——**它把"抛了什么错"变成可打印的字符串**，因为本章一半的结论都是"应该在这里炸，并且炸出这句话"：

```typescript
const EXPERIMENT = process.argv[2] ?? "all";

function section(name: string): boolean {
  const selected = EXPERIMENT === "all" || EXPERIMENT === name;
  if (selected) console.log(`\n===== ${name} =====`);
  return selected;
}

function step(index: number, text: string): void {
  console.log(`[${index}] ${text}`);
}

async function withTempDir<T>(operation: (root: string) => Promise<T>): Promise<T> {
  const root = await mkdtemp(join(tmpdir(), "probe-ch20-"));
  try {
    return await operation(root);
  } finally {
    await rm(root, { recursive: true, force: true });
  }
}

// 只捕获错误消息，实验里要打印的就是这句话本身。
async function reason(operation: () => unknown): Promise<string> {
  try {
    await operation();
    return "(没有报错)";
  } catch (error) {
    return error instanceof Error ? error.message : String(error);
  }
}

function statusLine(prompt: string): string {
  return /^## runtime_status\n(.+)$/mu.exec(prompt)?.[1] ?? "(没有 runtime_status)";
}
```

`statusLine()` 那个正则值得多看一眼：它把 `## runtime_status` 下面那一行整个抓出来。之所以能这么写，正是因为 6.3 节讲的"这一段永远在最后、格式固定"。如果段落顺序会变，这个正则就不成立了。

第 3 段是四个替身。`AllowApproval` 是个"永远同意"的审批提供方，**但它会把被问到的工具名记下来**——实验 4 全靠这个数组来证明"越界写根本没走到审批"：

```typescript
class AllowApproval implements ApprovalProvider {
  readonly asked: string[] = [];

  async decide(request: PermissionRequest): Promise<PermissionDecision> {
    this.asked.push(request.prepared.definition?.name ?? "(unknown)");
    return new PermissionDecision("allow", "probe approval", "probe");
  }
}

class NoopAudit implements AuditSink {
  readonly records: string[] = [];

  async record(request: PermissionRequest, decision: PermissionDecision): Promise<void> {
    this.records.push(`${request.prepared.definition?.name ?? "?"} -> ${decision.behavior}`);
  }
}

class ImmediateSleeper implements WorkStealingSleeper {
  async sleep(_seconds: number, _wakeup: AbortSignal): Promise<void> {}
}

class ProbeClock {
  #value = new Date("2026-08-01T00:00:00.000Z");

  now(): Date {
    return new Date(this.#value);
  }
}
```

`ImmediateSleeper` 和 `ProbeClock` 是为了让实验**不真的等**：工作窃取的轮询睡眠直接返回，Cron 的时钟停在一个固定时刻。这两个替身是 P17 和 P14 留下的接缝——注入睡眠和注入时钟都是为了可测试性，不是为了功能。

第 4 段是假 MCP 连接。它只申报一个 `lookup` 工具，`callTool` 会给调用次数 +1——实验 4 用这个计数器证明"抢跑的那次调用根本没出网"：

```typescript
// 假 MCP 连接：不启动子进程，只申报一个 lookup 工具，便于离线观察动态工具边界。
class FakeConnection implements McpConnection {
  closeCalls = 0;
  callCalls = 0;
  #resolveFailure: (() => void) | undefined;
  readonly #failure: Promise<void>;

  constructor() {
    this.#failure = new Promise<void>((resolve) => {
      this.#resolveFailure = resolve;
    });
  }

  async listTools(): Promise<readonly McpPublishedTool[]> {
    return [
      new McpPublishedTool({
        name: "lookup",
        description: "Look up local fixture data.",
        inputSchema: {
          type: "object",
          properties: { query: { type: "string" } },
          required: ["query"],
        },
      }),
    ];
  }

  async callTool(): Promise<McpCallResult> {
    this.callCalls += 1;
    return new McpCallResult({ content: [], structuredContent: {}, isError: false });
  }

  async waitForFailure(): Promise<void> {
    await this.#failure;
  }

  async close(): Promise<void> {
    this.closeCalls += 1;
    this.#resolveFailure?.();
    this.#resolveFailure = undefined;
  }
}
```

`waitForFailure()` 那个 Promise 是配合 P19 的 Watchdog 的：真实连接靠它感知子进程死亡，这里让它在 `close()` 时兑现，`McpRuntime` 关闭时 Watchdog 才不会挂住。

紧跟着是工厂和一个 `McpRuntime` 构造函数：

```typescript
class FakeFactory implements McpConnectionFactory {
  constructor(readonly connection: FakeConnection) {}

  async open(): Promise<McpConnection> {
    return this.connection;
  }
}

function createRuntime(connection: FakeConnection): McpRuntime {
  return new McpRuntime({
    servers: [
      new McpServerSpec({
        alias: "fake",
        command: "unused",
        args: [],
        toolPolicies: [new McpToolPolicy({ remoteName: "lookup", effect: "read" })],
        startupTimeoutSeconds: 1,
        toolTimeoutSeconds: 1,
      }),
    ],
    connectionFactory: new FakeFactory(connection),
    schemaValidator: new AjvMcpSchemaValidator(),
  });
}
```

注意 `command: "unused"`：工厂被替换掉了，命令字符串不会被执行。但 **`toolPolicies` 必须准确**——本地 policy 声明 `lookup` 是 `read`，跟远程申报的名字必须精确相等，否则连接会被拒（第 19 章的规则，P20 一字未改）。这里的 `effect: "read"` 也解释了实验 4 里 `mcp__fake__lookup` 为什么不需要审批。

`schemaValidator` 用的是**真的** `AjvMcpSchemaValidator`，不是替身。远程 schema 校验是执行边界，替换掉它实验就没意义了。

第 5 段是整章最重要的一段：`createHarness()`。它就是第 3 节讲的组合根，浓缩成一个函数。**一个 `EventInbox` 穿过三个运行时，一个 `SqliteTaskStore` 穿过两个运行时**——这两条共享关系就是实验 2 要逐条破坏的东西：

```typescript
interface Harness {
  readonly dependencies: BuildDependencies;
  readonly supervisor: JobSupervisor;
  readonly cron: CronRuntime;
  readonly teammates: TeammateRuntime;
  readonly protocol: ProtocolRuntime;
  readonly worktrees: WorktreeRuntime;
  readonly workStealing: WorkStealingRuntime;
  readonly inbox: EventInbox;
  readonly approval: AllowApproval;
}

// 与 tests/ch20-full-harness.test.ts 的 createDependencies 同构：一个 EventInbox 贯穿三个运行时。
function createHarness(root: string, model: ModelClient, mcpRuntime: McpRuntime): Harness {
  const inbox = new EventInbox();
  const supervisor = new JobSupervisor({ store: new JsonBackgroundJobStore(root), inbox });
  const cron = new CronRuntime({
    store: new JsonCronStore(root),
    inbox,
    supervisor,
    clock: new ProbeClock(),
  });
  const teammates = new TeammateRuntime({
    store: new FileMailboxStore(root),
    inbox,
    supervisor,
    cronRuntime: cron,
  });
  const protocol = new ProtocolRuntime({ store: new JsonProtocolStore(root), team: teammates });
  const taskStore = new SqliteTaskStore(root);
  const worktrees = new WorktreeRuntime({
    workspace: root,
    store: taskStore,
    gitRunner: new SubprocessGitRunner(),
  });
  const workStealing = new WorkStealingRuntime({
    store: taskStore,
    claimService: worktrees,
    sleeper: new ImmediateSleeper(),
    maxIdlePolls: 1,
  });
  const approval = new AllowApproval();
```

函数后半段把它们拼成 `BuildDependencies`：

```typescript
  return {
    dependencies: {
      model,
      workspace: root,
      hooks: new HookRegistry(),
      approvalProvider: approval,
      auditSink: new NoopAudit(),
      recoveryConfig: new RecoveryConfig({ primaryModel: "primary", fallbackModel: "fallback" }),
      backgroundSupervisor: supervisor,
      cronRuntime: cron,
      teammateRuntime: teammates,
      protocolRuntime: protocol,
      workStealingRuntime: workStealing,
      worktreeRuntime: worktrees,
      mcpRuntime,
    },
    supervisor,
    cron,
    teammates,
    protocol,
    worktrees,
    workStealing,
    inbox,
    approval,
  };
}
```

对着 3.4 节的四组关系读这段代码，你会发现构造顺序**不是随便排的**：

```text
inbox ──────────┐
                ├─→ supervisor ──┐
                │                ├─→ cron ──┐
                └────────────────┘          ├─→ teammates ──→ protocol
                                            │
taskStore ──→ worktrees ──(claimService)──→ workStealing
```

每个箭头都是"后者的构造参数里必须是前者那个对象"。谁先谁后不能颠倒，因为下游要拿上游的实例。**这张图就是组合根的全部含义**——不是"配置对象"，而是"对象图"。

第 6 段是两个探测模型和两个取值助手：

```typescript
function systemPromptOf(request: ModelRequest): string {
  const message = request.messages.find((candidate) => candidate.role === "system");
  return message === undefined ? "" : message.content;
}

function toolNamesOf(request: ModelRequest): readonly string[] {
  return request.tools.map((tool) => tool.function.name);
}

// 主回合模型：第一条回复里同时 connect_mcp 和调用刚连上的工具，用来观察快照边界。
class HarnessProbeModel implements ModelClient {
  readonly requests: ModelRequest[] = [];
  #started = false;

  async complete(request: ModelRequest): Promise<ModelReply> {
    validateToolPairing(request.messages);
    this.requests.push(request);
    if (request.tools.length === 0) {
      return { message: assistantMessage("[]"), finishReason: "stop" };
    }
    if (!this.#started) {
      this.#started = true;
      return {
        message: assistantMessage(null, [
          toolCall("probe-connect", "connect_mcp", '{"alias":"fake"}'),
          toolCall("probe-mcp", "mcp__fake__lookup", '{"query":"needle"}'),
          toolCall(
            "probe-escape",
            "write_file",
            JSON.stringify({ path: "../outside.txt", content: "must not be written" }),
          ),
        ]),
        finishReason: "tool_calls",
      };
    }
    return { message: assistantMessage("probe done"), finishReason: "stop" };
  }
}
```

这个模型里有三处刻意的设计，值得逐个说：

**第一行 `validateToolPairing(request.messages)`。** 模型自己校验收到的历史合法。这是 5.2 节那条不变量的"外部观察者"版本：任何一次请求的消息序列如果配对不上，实验会当场炸，而不是悄悄跑完给你个好看的结果。

**`if (request.tools.length === 0)` 返回 `"[]"`。** 工具数为 0 的请求不是主回合，是压缩或记忆挑选之类的辅助请求（它们不带工具）。返回一个合法的空 JSON 数组，免得辅助流程解析失败。后面统计"主请求次数"时，也是用 `tools.length > 0` 把这些筛掉的。

**三个工具调用写在同一条回复里。** 顺序是刻意的：先 `connect_mcp`（会成功），紧跟 `mcp__fake__lookup`（会撞快照边界），最后 `write_file` 越界（会撞工作区边界）。一条回复同时打三面墙。

第二个模型专门用来触发恢复矩阵。它按固定顺序抛出六种失败，最后才成功：

```typescript
// 恢复实验模型：按顺序抛 429 / 529×3 / length / prompt-too-long，最后成功。
class RecoveryProbeModel implements ModelClient {
  readonly requests: ModelRequest[] = [];
  readonly failures: string[] = [];
  #attempts = 0;

  async complete(request: ModelRequest): Promise<ModelReply> {
    validateToolPairing(request.messages);
    this.requests.push(request);
    if (request.tools.length === 0) {
      return {
        message: assistantMessage(
          JSON.stringify({
            current_goal: "验证恢复矩阵",
            key_findings: ["prompt-too-long 触发了一次压缩"],
            files_read_or_changed: [],
            remaining_work: [],
            user_constraints: [],
          }),
        ),
        finishReason: "stop",
      };
    }
    const attempt = this.#attempts;
    this.#attempts += 1;
    if (attempt === 0) {
      this.failures.push("429");
      throw new ModelRateLimitError("rate limited", { retryAfter: "0" });
    }
    if (attempt < 4) {
      this.failures.push("529");
      throw new ModelOverloadedError("overloaded");
    }
    if (attempt === 4) {
      this.failures.push("length");
      return { message: assistantMessage("discarded"), finishReason: "length" };
    }
    if (attempt === 5) {
      this.failures.push("prompt-too-long");
      throw new ModelPromptTooLongError("prompt too long");
    }
    return { message: assistantMessage("recovered"), finishReason: "stop" };
  }
}
```

这里 `tools.length === 0` 的分支返回的是一个**压缩摘要结构**（`current_goal` / `key_findings` 那五个字段）。因为 `ModelPromptTooLongError` 会触发一次真实压缩，压缩本身要调模型；不给它一个合法结构，压缩会解析失败，你就看不到最后那次成功了。

`retryAfter: "0"` 让 429 的等待归零，实验才能秒完。真实场景里这个值来自响应头。

公共头部到此结束。下面十段可以只贴你想跑的那几个。

### 实验 1（profile）：累计能力与固定档案身份

```typescript
// ---------- 实验 1（profile）：累计能力模型与固定档案身份 ----------
if (section("profile")) {
  const counts: string[] = [];
  for (let chapter = 1; chapter <= 20; chapter += 1) {
    counts.push(`P${String(chapter).padStart(2, "0")}=${profileForChapter(chapter).capabilities.size}`);
  }
  step(1, `每章累计能力数：${counts.join(" ")}`);
  const p19 = new Set<Capability>(P19.capabilities);
  const added = [...P20.capabilities].filter((capability) => !p19.has(capability));
  const removed = [...P19.capabilities].filter((capability) => !P20.capabilities.has(capability));
  step(2, `P20 相对 P19 新增：[${added.join(", ")}]，移除：[${removed.join(", ")}]`);
  step(3, `P20 是否仍持有 P19 的每一项能力：${String(removed.length === 0)}`);
  step(4, `profileForChapter(20) === P20：${String(profileForChapter(20) === P20)}`);
  step(5, `profileForChapter(21) → ${await reason(() => profileForChapter(21))}`);
  step(6, `profileForChapter(19.5) → ${await reason(() => profileForChapter(19.5))}`);
  // 自造同形对象即使能力集完全一致，也过不了组合根的身份门禁。
  const lookalike: ChapterProfile = { chapter: 20, capabilities: new Set(P20.capabilities) };
  const model: ModelClient = {
    async complete() {
      return { message: assistantMessage("unused"), finishReason: "stop" as const };
    },
  };
  step(
    7,
    `自造 {chapter:20, capabilities:...} 传给 buildAgent → ${await reason(() =>
      buildAgent(lookalike, { model, workspace: process.cwd() }),
    )}`,
  );
}
```

跑：

```powershell
npx tsx scripts\probe-ch20.mts profile
```

```text
===== profile =====
[1] 每章累计能力数：P01=2 P02=4 P03=5 P04=6 P05=7 P06=8 P07=9 P08=11 P09=12 P10=13 P11=14 P12=15 P13=16 P14=17 P15=19 P16=21 P17=23 P18=24 P19=25 P20=26
[2] P20 相对 P19 新增：[full_harness]，移除：[]
[3] P20 是否仍持有 P19 的每一项能力：true
[4] profileForChapter(20) === P20：true
[5] profileForChapter(21) → chapter must be an integer from 1 to 20
[6] profileForChapter(19.5) → chapter must be an integer from 1 to 20
[7] 自造 {chapter:20, capabilities:...} 传给 buildAgent → profile must be a fixed chapter profile
```

第 1 行那串数字是二十章的完整轨迹。有几处台阶比别处高：

| 跨章 | 增量 | 加了什么 |
| --- | --- | --- |
| P07 → P08 | +2 | 压缩需要两个能力位一起上（`compaction` 与配套的历史处理） |
| P14 → P15 | +2 | 队友机制同时带来信箱和事件注入 |
| P15 → P16 | +2 | 协议带来计划状态机和关停握手 |
| P16 → P17 | +2 | 工作窃取带来租约和轮询 |
| P19 → P20 | +1 | 只加 `full_harness` 一个标记 |

第 2、3 行合起来是本章的地基：**只增不减**。这不是"我们尽量兼容"，而是 `PROFILE_DELTAS.slice(0, index + 1).flat()` 这个实现方式的必然结果——切片只会越切越长。

第 5、6 行看着琐碎，其实挡的是真实错误：`profileForChapter(chapter + 1)` 这种写法在循环里很常见，越界时你希望它当场炸，而不是返回 `undefined` 让后面的 `.capabilities` 报 `TypeError`。小数那条挡的是 `parseFloat` 之类的输入。

第 7 行是这个实验的重点。那个 `lookalike` 对象**能力集和 P20 完全一样**，`chapter` 也是 20，但 `buildAgent` 拒绝了它。理由在 2.3 节：`profileForChapter(profile.chapter) !== profile` 用的是引用比较。这道门看着苛刻，作用是把"可能的能力组合"从 2²⁶ 收缩到 20 种——每一种都有测试覆盖。

### 实验 2（wiring）：七条装配门禁

这个实验建**两套**完整依赖（`harness` 和 `other`），然后把 `other` 的零件一个个换进 `harness`，看组合根认不认。

```typescript
// ---------- 实验 2（wiring）：组合根按对象身份校验共享依赖 ----------
if (section("wiring")) {
  await withTempDir(async (root) => {
    const model = new HarnessProbeModel();
    const connection = new FakeConnection();
    const mcpRuntime = createRuntime(connection);
    const harness = createHarness(root, model, mcpRuntime);
    const other = createHarness(root, model, createRuntime(new FakeConnection()));

    step(1, `完整依赖 → ${await reason(() => buildAgent(P20, harness.dependencies))}`);
    step(
      2,
      `换掉 cronRuntime（另一个 supervisor/inbox）→ ${await reason(() =>
        buildAgent(P20, { ...harness.dependencies, cronRuntime: other.cron }),
      )}`,
    );
```

```typescript
    step(
      3,
      `只换掉 protocolRuntime → ${await reason(() =>
        buildAgent(P20, { ...harness.dependencies, protocolRuntime: other.protocol }),
      )}`,
    );
    step(
      4,
      `换掉 teammateRuntime + protocolRuntime（两者互相匹配）→ ${await reason(() =>
        buildAgent(P20, {
          ...harness.dependencies,
          teammateRuntime: other.teammates,
          protocolRuntime: other.protocol,
        }),
      )}`,
    );
    step(
      5,
      `换掉 worktreeRuntime（不是 claimService）→ ${await reason(() =>
        buildAgent(P20, { ...harness.dependencies, worktreeRuntime: other.worktrees }),
      )}`,
    );
    const { mcpRuntime: _dropped, ...withoutMcp } = harness.dependencies;
    step(6, `删掉 mcpRuntime → ${await reason(() => buildAgent(P20, withoutMcp))}`);
    step(
      7,
      `把 P20 依赖交给 P18 → ${await reason(() =>
        buildAgent(profileForChapter(18), harness.dependencies),
      )}`,
    );
  });
}
```

```powershell
npx tsx scripts\probe-ch20.mts wiring
```

```text
===== wiring =====
[1] 完整依赖 → (没有报错)
[2] 换掉 cronRuntime（另一个 supervisor/inbox）→ cronRuntime must share the background supervisor and event inbox
[3] 只换掉 protocolRuntime → protocolRuntime must share the teammate runtime and mailbox store
[4] 换掉 teammateRuntime + protocolRuntime（两者互相匹配）→ teammateRuntime must share the background supervisor, EventInbox, and CronRuntime
[5] 换掉 worktreeRuntime（不是 claimService）→ worktreeRuntime must be the WorkStealingRuntime claim service and share its store
[6] 删掉 mcpRuntime → mcpRuntime is required for chapter 19 or later
[7] 把 P20 依赖交给 P18 → mcpRuntime requires chapter 19 or later
```

第 1 行是基线：完整依赖不报错。有这一行，后面六行的"炸"才有意义——否则你不知道是装配错了还是环境坏了。

第 4 行最值得琢磨。它换掉了**两个**运行时，而且这两个是互相匹配的（`other.protocol` 就是用 `other.teammates` 建的）。第 3 组"Protocol ↔ Teammate"的检查因此通过了。但它还是炸了，炸在第 2 组：`other.teammates` 用的是 `other` 的 supervisor 和 cron，跟 `harness` 的不是同一个。

这说明四组检查不是"任选一组过就行"，而是**全部都要过**。你没法通过"整体换一套"来绕过身份校验，除非你把整棵对象图都换掉（那就是 `other.dependencies` 了，它自己是自洽的）。

第 6、7 行是一对反向门禁，很容易被忽略：

```text
P20 + 没有 mcpRuntime  → 炸（少了）
P18 + 有  mcpRuntime  → 炸（多了）
```

只有"少了"这一条的话，你可以给 P18 传一堆 P20 的东西，它会默默忽略。那意味着**档案不再决定行为**——你以为在跑 P18，实际上组合根手里握着 MCP 运行时。双向检查才能让"档案 = 行为"这个等式成立。

顺便说，第 5 行那个 `claimService` 的检查是四组里最不直观的一条。它要求 `worktreeRuntime` 这个对象**本身就是** `WorkStealingRuntime` 的 `claimService`。为什么不能是两个对象？因为租约任务的 claim 会绑定 worktree，claim token 存在 `SqliteTaskStore` 里。如果领取任务的是 A、创建 worktree 的是 B，任务表里的 token 和 worktree 的绑定就会指向两套状态——3.5 节讲过这种错误的代价：它要等到任务真的跑起来才暴露。

### 实验 3（status）：runtime_status 的位置与缓存

```typescript
// ---------- 实验 3（status）：runtime_status 固定在最后一段，且参与缓存键 ----------
if (section("status")) {
  const renderer = new DynamicPromptRenderer();
  const registry = new ToolRegistry();
  registry.register({
    name: "read_file",
    description: "read a file",
    inputSchema: z.object({ path: z.string() }),
    effect: "read",
    handler: () => toolSuccess("ok"),
  });
  const base = {
    identity: "lead",
    tools: registry,
    workspace: process.cwd(),
    context: { chapter: 20 },
  } as const;
```

```typescript
  const withoutStatus = renderer.render(base);
  step(1, `不传 status 时的段落顺序：${withoutStatus.match(/^## .+$/gmu)?.join(" ") ?? "(无)"}`);
  const first = renderer.render({
    ...base,
    status: { mcp_connections: [], pending_work: false },
  });
  step(2, `传 status 后的段落顺序：${first.match(/^## .+$/gmu)?.join(" ") ?? "(无)"}`);
  step(3, `最后一段内容：${statusLine(first)}`);
  const hitsBefore = renderer.cacheHits;
  renderer.render({ ...base, status: { mcp_connections: [], pending_work: false } });
  step(4, `同样的 status 再渲染一次，cacheHits ${hitsBefore} → ${renderer.cacheHits}`);
  const second = renderer.render({
    ...base,
    status: { mcp_connections: ["fake"], pending_work: true },
  });
  step(5, `只改 status 后 cacheHits 仍是 ${renderer.cacheHits}，说明缓存被判定失效`);
  step(6, `新的最后一段：${statusLine(second)}`);
  const before = first.slice(0, first.indexOf("## runtime_status"));
  const after = second.slice(0, second.indexOf("## runtime_status"));
  step(7, `runtime_status 之前的正文是否完全没变：${String(before === after)}`);
  step(
    8,
    `statusProvider 传非函数 → ${await reason(
      () =>
        new DynamicPromptProvider({
          renderer,
          ...base,
          statusProvider: {} as unknown as () => never,
        }),
    )}`,
  );
}
```

```powershell
npx tsx scripts\probe-ch20.mts status
```

```text
===== status =====
[1] 不传 status 时的段落顺序：## identity ## tools ## workspace
[2] 传 status 后的段落顺序：## identity ## tools ## workspace ## runtime_status
[3] 最后一段内容：{"mcp_connections":[],"pending_work":false}
[4] 同样的 status 再渲染一次，cacheHits 0 → 1
[5] 只改 status 后 cacheHits 仍是 1，说明缓存被判定失效
[6] 新的最后一段：{"mcp_connections":["fake"],"pending_work":true}
[7] runtime_status 之前的正文是否完全没变：true
[8] statusProvider 传非函数 → statusProvider must be a function
```

第 1、2 行是同一件事的两面：**不传 `status` 就没有这一段**，传了就追加在末尾。这解释了为什么 P1 到 P19 的提示词不会因为 P20 的存在而变形——它们根本不传这个字段。

第 4、5 行是这个实验的技术核心，但输出容易看反，所以把两次调用摆在一起看：

```text
调用 1  status = {[], false}   → 未命中，渲染一次，cacheHits: 0
调用 2  status = {[], false}   → 命中！             cacheHits: 0 → 1
调用 3  status = {["fake"], true} → 未命中，重新渲染   cacheHits: 1（不变）
```

`cacheHits` 只在**命中**时自增。调用 2 命中说明缓存确实在工作（同样的输入不重复拼字符串）；调用 3 之后计数没动，说明它**没有**命中——`status` 变了，缓存键就变了。

这正是 6.3 节要的效果：`status` 是模型可见的输入，所以它必须参与缓存键。如果不参与，第 3 次调用会命中缓存，模型看到的会是**上一轮的连接列表**——一个陈旧的、看起来完全正常的谎。这类 bug 极难发现，因为提示词是合法的、格式是对的，只是内容过期了。

第 7 行是配套的另一半：`status` 变了，但它**前面的正文一个字节都没变**。这两条合起来才是完整的性质——变化被限制在最后一段之内。对提示词缓存友好的形状就是这样：前缀稳定，变化都在尾部。

第 8 行是构造期的类型门禁。`statusProvider` 必须是函数，不能是对象。这条很短，防的是一类很常见的手误：`statusProvider: { mcp_connections: [] }`（直接传了值）。传值意味着状态在构造时就冻住了，永远不会更新；报错比默默返回陈旧状态好得多。

### 实验 4（harness）：一次回合里四条边界同时生效

这是全章最关键的一个实验，第一节的那段输出就是它。

```typescript
// ---------- 实验 4（harness）：一次真实回合里四条边界同时生效 ----------
if (section("harness")) {
  await withTempDir(async (root) => {
    const model = new HarnessProbeModel();
    const connection = new FakeConnection();
    const mcpRuntime = createRuntime(connection);
    const harness = createHarness(root, model, mcpRuntime);
    const runner = buildAgent(P20, harness.dependencies);
    try {
      const result = await runner.run("连上 fake 然后查一次");
      const main = model.requests.filter((request) => request.tools.length > 0);
      const firstRequest = main[0];
      const secondRequest = main[1];
      if (firstRequest === undefined || secondRequest === undefined) {
        throw new Error("probe expected two main requests");
      }
```

```typescript
      step(1, `模型主请求次数：${main.length}`);
      step(2, `第 1 次请求的工具数：${toolNamesOf(firstRequest).length}`);
      step(
        3,
        `第 1 次请求里有 mcp__fake__lookup 吗：${String(
          toolNamesOf(firstRequest).includes("mcp__fake__lookup"),
        )}`,
      );
      step(4, `第 2 次请求的工具数：${toolNamesOf(secondRequest).length}`);
      step(
        5,
        `第 2 次请求里有 mcp__fake__lookup 吗：${String(
          toolNamesOf(secondRequest).includes("mcp__fake__lookup"),
        )}`,
      );
      step(6, `第 1 次请求的 runtime_status：${statusLine(systemPromptOf(firstRequest))}`);
      step(7, `第 2 次请求的 runtime_status：${statusLine(systemPromptOf(secondRequest))}`);
      step(8, "同一条回复里三个工具调用各自的结果：");
      for (const message of result.history) {
        if (message.role !== "tool") continue;
        console.log(`      ${message.toolCallId} → ${message.content.split("\n")[0] ?? ""}`);
      }
      step(9, `远程 callTool 实际被调用次数：${connection.callCalls}`);
      step(10, `审批被问到的工具：[${harness.approval.asked.join(", ")}]（deny 强于 ask，越界写不进审批）`);
      step(11, `最终回答：${result.finalText}`);
    } finally {
      await runner.close();
      step(12, `close() 之后 McpRuntime.isClosed=${String(mcpRuntime.isClosed)}`);
    }
  });
}
```

```powershell
npx tsx scripts\probe-ch20.mts harness
```

```text
===== harness =====
[1] 模型主请求次数：2
[2] 第 1 次请求的工具数：23
[3] 第 1 次请求里有 mcp__fake__lookup 吗：false
[4] 第 2 次请求的工具数：24
[5] 第 2 次请求里有 mcp__fake__lookup 吗：true
[6] 第 1 次请求的 runtime_status：{"mcp_connections":[],"pending_work":false}
[7] 第 2 次请求的 runtime_status：{"mcp_connections":["fake"],"pending_work":false}
[8] 同一条回复里三个工具调用各自的结果：
      probe-connect → {"server_alias":"fake","status":"connected","tools":["mcp__fake__lookup"]}
      probe-mcp → Error [unknown_tool]: Unknown tool: mcp__fake__lookup
      probe-escape → Error [permission_denied]: Writing outside the workspace is forbidden
[9] 远程 callTool 实际被调用次数：0
[10] 审批被问到的工具：[]（deny 强于 ask，越界写不进审批）
[11] 最终回答：probe done
[12] close() 之后 McpRuntime.isClosed=true
```

12 行输出对应四条边界，一条一条对：

**边界一：工具快照（第 2 到 5 行）。** 23 → 24 这个变化只发生在两次请求**之间**。第 1 次请求发出时快照已经定型，`connect_mcp` 在这一轮里成功注册了工具，但注册进的是**活注册表**，跟本轮的快照无关。所以第 3 行是 `false`，第 5 行是 `true`。

**边界二：工作区（第 8 行第三条）。** `../outside.txt` 被 `permission_denied` 拦下。注意这不是"审批拒了"，是**根本没进审批**——第 10 行的空数组就是证据。5.4 节讲过原因：`deny` 强于 `ask`。

**边界三：配对（第 8 行整体）。** 三个 `tool call`，三条结果。一个成功、两个失败，但**都有**。如果失败的那两个被跳过，下一次请求就会带着不完整的历史，`validateToolPairing` 会在模型侧（还记得 `HarnessProbeModel` 第一行吗）当场炸。

**边界四：关闭（第 12 行）。** `close()` 一路走到 `McpRuntime`，`isClosed` 变成 `true`。

第 9 行单独说。远程 `callTool` 被调用了 **0 次**，尽管模型明确要求调用 `mcp__fake__lookup`。这条很重要：抢跑的调用**没有变成一次真实的远程请求**。它在 `snapshot.prepare()` 那一步就被判定成 `unknown_tool` 了，连参数校验都没走到，更别说出网。

这就是"边界在正确的层"的意思——如果快照检查放在网络调用**之后**，模型的抢跑会变成一次真实的副作用，然后你再告诉它"哦这个工具其实还不存在"。那时候撤销已经来不及了。

第 6、7 行是顺带的收获：`runtime_status` 在第 1 次请求里是空连接，第 2 次变成 `["fake"]`。它诚实地反映了上一轮的结果，而且**它反映的是活注册表的状态，不是快照的状态**。这里有个微妙但重要的错位：

```text
第 2 次请求：
  runtime_status 说   mcp_connections: ["fake"]    ← 活状态
  tools 列表里也有     mcp__fake__lookup           ← 本轮快照
  两者一致 ✓

如果模型在第 1 次请求里就能看到 status（假设我们把 status 放在响应之后刷新）：
  runtime_status 会说  mcp_connections: ["fake"]
  但 tools 列表里没有  mcp__fake__lookup
  两者矛盾 ✗ → 模型会去调一个"状态说有、工具面说没有"的工具
```

P20 的做法是：`status` 和 `tools` 在**同一次** `#prepareModelRequest` 里取值（`loop.ts:505` 渲染提示词，`loop.ts:511` 取快照，隔了六行）。同一时刻的两个快照，不会互相矛盾。

### 实验 5（snapshot）：快照是不可变副本

实验 4 是在完整 Agent 里看快照边界，这个实验直接对着 `ToolRegistry` 看，把机制单独剥出来。

```typescript
// ---------- 实验 5（snapshot）：快照是不可变副本，不跟随活注册表变化 ----------
if (section("snapshot")) {
  const registry = new ToolRegistry();
  const definition = {
    name: "alpha",
    description: "first tool",
    inputSchema: z.object({}),
    effect: "read" as const,
    handler: () => toolSuccess("alpha"),
  };
  registry.register(definition);
  const snapshot = registry.snapshot();
  step(1, `活注册表 v${registry.version}：[${registry.names.join(", ")}]`);
  step(2, `快照 v${snapshot.version}：[${snapshot.names.join(", ")}]`);
  registry.register({
    name: "beta",
    description: "second tool",
    inputSchema: z.object({}),
    effect: "read" as const,
    handler: () => toolSuccess("beta"),
  });
  step(3, `活注册表新增后 v${registry.version}：[${registry.names.join(", ")}]`);
  step(4, `同一个快照仍是 v${snapshot.version}：[${snapshot.names.join(", ")}]`);
  step(
    5,
    `在快照上 prepare("beta") → ${
      snapshot.prepare(toolCall("x", "beta", "{}")).error?.content ?? "(成功)"
    }`,
  );
  step(
    6,
    `试图往快照里注册 → ${await reason(() =>
      snapshot.register({
        name: "gamma",
        description: "third",
        inputSchema: z.object({}),
        effect: "read" as const,
        handler: () => toolSuccess("gamma"),
      }),
    )}`,
  );
  step(
    7,
    `按名字（不是对象身份）撤销 → ${await reason(() =>
      registry.unregisterMany([{ ...definition }]),
    )}`,
  );
  step(8, `按对象身份撤销 → ${await reason(() => registry.unregisterMany([definition]))}`);
  step(9, `撤销后活注册表 v${registry.version}：[${registry.names.join(", ")}]`);
}
```

```powershell
npx tsx scripts\probe-ch20.mts snapshot
```

```text
===== snapshot =====
[1] 活注册表 v1：[alpha]
[2] 快照 v1：[alpha]
[3] 活注册表新增后 v2：[alpha, beta]
[4] 同一个快照仍是 v1：[alpha]
[5] 在快照上 prepare("beta") → Error [unknown_tool]: Unknown tool: beta
[6] 试图往快照里注册 → tool registry snapshot is immutable
[7] 按名字（不是对象身份）撤销 → tool definition was not registered by this registry
[8] 按对象身份撤销 → (没有报错)
[9] 撤销后活注册表 v3：[beta]
```

第 3、4 行是核心对比：活注册表已经 v2、有两个工具；同一个快照还是 v1、只有一个。快照不是"引用"，是**副本**。

第 5 行把这件事翻译成模型能感知的形式：在快照上调用一个活注册表里已经存在的工具，得到 `unknown_tool`。这跟实验 4 里 `mcp__fake__lookup` 的报错**一模一样**——因为就是同一个机制。

第 6 行防的是另一个方向：拿到快照的代码不能反过来改工具面。`#executeTool` 拿的就是这份快照，如果它可写，一个工具 handler 就能在执行中途给自己加个新工具。

第 7、8 行是对比着看的一组。同一个定义，一个是展开副本 `{ ...definition }`，一个是原对象。副本的每个字段都相等，但撤销失败了——**撤销认对象身份，不认名字**。

为什么要这么严？想一个真实场景：

```text
时刻 1  连接 A 注册了 mcp__x__query
时刻 2  连接 A 掉线，Watchdog 准备撤销
时刻 3  连接 A 重连成功，注册了新的 mcp__x__query（新对象）
时刻 4  时刻 2 那个撤销终于执行了
```

如果按名字撤销，时刻 4 会把**新连接刚注册的工具**删掉，而模型手里的快照还显示它存在。按对象身份撤销就没这个问题：时刻 2 持有的是旧对象，新对象不匹配，撤销自然失败——而失败在这里正是想要的行为。

第 9 行的 `v3` 也值得一眼：撤销**也会**推进版本号。版本号记的是"工具面变过几次"，不是"有几个工具"。

### 实验 6（pairing）：六种错误配对

这个实验不建 Agent，直接把 `validateToolPairing` 拎出来喂六种历史。

```typescript
// ---------- 实验 6（pairing）：一个 tool call 必须且只能有一个同 id 的结果 ----------
if (section("pairing")) {
  const call = toolCall("call-1", "read_file", '{"path":"a.txt"}');
  const cases: readonly (readonly [string, readonly ChatMessage[]])[] = [
    ["调用后紧跟同 id 结果", [assistantMessage(null, [call]), toolMessage("ok", "call-1")]],
    ["调用后没有结果就收尾", [assistantMessage(null, [call])]],
    ["调用后跟了别的 id", [assistantMessage(null, [call]), toolMessage("ok", "call-2")]],
    ["调用后先插一条 user", [assistantMessage(null, [call]), userMessage("插话"), toolMessage("ok", "call-1")]],
    ["没有调用却有结果", [toolMessage("ok", "call-1")]],
    [
      "同一 id 回填两次",
      [assistantMessage(null, [call]), toolMessage("ok", "call-1"), toolMessage("ok", "call-1")],
    ],
  ];
  let index = 1;
  for (const [label, history] of cases) {
    step(index, `${label} → ${await reason(() => validateToolPairing(history))}`);
    index += 1;
  }
}
```

```powershell
npx tsx scripts\probe-ch20.mts pairing
```

```text
===== pairing =====
[1] 调用后紧跟同 id 结果 → (没有报错)
[2] 调用后没有结果就收尾 → missing tool results for ids: ["call-1"]
[3] 调用后跟了别的 id → unexpected tool result id: call-2
[4] 调用后先插一条 user → missing tool results for ids: ["call-1"]
[5] 没有调用却有结果 → orphan tool result id: call-1
[6] 同一 id 回填两次 → orphan tool result id: call-1
```

六种情况，四种不同的报错。挨个说：

| 行 | 错在哪 | 报错 | 真实成因 |
| --- | --- | --- | --- |
| 2 | 少了结果 | `missing tool results` | 工具执行抛异常被吞掉，跳过了回填 |
| 3 | id 不对 | `unexpected tool result id` | 结果处理器搞错了配对关系 |
| 4 | 中间插了消息 | `missing tool results` | 用户输入或事件在工具结果之前被插进去 |
| 5 | 多了结果 | `orphan tool result id` | 压缩把 assistant 消息裁掉了，留下孤儿结果 |
| 6 | 重复回填 | `orphan tool result id` | 重试逻辑回填了两次 |

第 3、4 行的对比最有意思。两种情况**都是**"结果没在正确位置"，但报错不同：

```text
第 3 行  [assistant(call-1)] [tool(call-2)]
         → 遇到 call-2 时，待回填集合里只有 call-1
         → unexpected：这个 id 我没在等

第 4 行  [assistant(call-1)] [user] [tool(call-1)]
         → 遇到 user 时，待回填集合里还有 call-1 没清空
         → missing：该回填的还没回填，你却换了话题
```

区别在于**检查在哪一步触发**。第 4 行的关键是：`user` 消息一出现，校验器就认为"工具轮结束了"，此时集合非空即为非法。所以后面那条 `tool(call-1)` 根本没被检查到。

这条规则对 P20 有直接影响：9.2 节讲的"事件在用户回合期间要延后"，根本原因就在这里。如果一条 `runtime_event` 用户消息在工具结果之前被插进历史，历史立刻非法。事件必须等一整轮工具都回填完才能进。

第 5、6 行报同一个错，因为在校验器眼里它们是同一件事：**遇到一条结果，但它的 id 不在待回填集合里**。第 5 行是从来没进过集合，第 6 行是已经被第一条结果消费掉了。

### 实验 7（close）：逆序、聚合、幂等

```typescript
// ---------- 实验 7（close）：逆序关闭、失败聚合、幂等与并发拒绝 ----------
if (section("close")) {
  const trace: string[] = [];
  class TracedResource implements AsyncResource {
    constructor(
      readonly name: string,
      readonly fails = false,
    ) {}

    async close(): Promise<void> {
      trace.push(this.name);
      if (this.fails) throw new Error(`${this.name} close failed`);
    }
  }
```

```typescript
  const model: ModelClient = {
    async complete() {
      return { message: assistantMessage("ok"), finishReason: "stop" as const };
    },
  };
  const okRunner = new AgentRunner({
    model,
    tools: new ToolRegistry(),
    systemPrompt: "system",
    workspace: process.cwd(),
    resources: [
      new TracedResource("supervisor"),
      new TracedResource("cron"),
      new TracedResource("teammate"),
      new TracedResource("mcp"),
    ],
  });
  await okRunner.close();
  step(1, `注册顺序 supervisor → cron → teammate → mcp，关闭顺序：${trace.join(" → ")}`);
  await okRunner.close();
  step(2, `再 close() 一次，关闭轨迹仍是 ${trace.length} 条：${String(trace.length === 4)}`);
  step(3, `关闭后还能 run() 吗 → ${await reason(() => okRunner.run("再来一次"))}`);

  trace.length = 0;
  const failRunner = new AgentRunner({
    model,
    tools: new ToolRegistry(),
    systemPrompt: "system",
    workspace: process.cwd(),
    resources: [
      new TracedResource("supervisor", true),
      new TracedResource("cron"),
      new TracedResource("teammate", true),
      new TracedResource("mcp"),
    ],
  });
  try {
    await failRunner.close();
  } catch (error) {
    const aggregate = error as AggregateError;
    step(4, `两个资源关闭失败 → ${aggregate.name}: ${aggregate.message}`);
    step(
      5,
      `聚合里的每一条：[${aggregate.errors.map((item: unknown) => (item as Error).message).join(" | ")}]`,
    );
  }
  step(6, `失败也走完了全部资源：${trace.join(" → ")}`);
}
```

```powershell
npx tsx scripts\probe-ch20.mts close
```

```text
===== close =====
[1] 注册顺序 supervisor → cron → teammate → mcp，关闭顺序：mcp → teammate → cron → supervisor
[2] 再 close() 一次，关闭轨迹仍是 4 条：true
[3] 关闭后还能 run() 吗 → AgentRunner is closed
[4] 两个资源关闭失败 → AggregateError: AgentRunner close failed
[5] 聚合里的每一条：[teammate close failed | supervisor close failed]
[6] 失败也走完了全部资源：mcp → teammate → cron → supervisor
```

第 1 行是逆序。注册时 `supervisor` 在最前，关闭时它在最后——因为上游要等下游全部停了才能关。

第 2 行是幂等。第二次 `close()` 没有再走一遍资源，轨迹还是 4 条。这条很实用：`try/finally` 里调 `close()`、外层错误处理里又调一次，是很常见的写法。不幂等的话，第二次会对已关闭的连接再调 `close()`，行为不可预测。

第 3 行是关闭后的封闭性。已关闭的 Runner 不能复用，直接报 `AgentRunner is closed`。为什么不让它自动重开？因为 `resources` 里的东西已经死了——子进程杀了、连接断了、文件句柄释放了。"重开"实际上是"重建"，那应该由调用方显式做。

第 4、5、6 行是失败处理，三行连起来才完整：

```text
四个资源，其中 supervisor 和 teammate 会抛错

close() 的行为：
  mcp        → 成功
  teammate   → 抛错，记下来，继续
  cron       → 成功
  supervisor → 抛错，记下来
  → 两个失败 → AggregateError

trace = mcp → teammate → cron → supervisor    ← 一个都没跳过
errors = [teammate 的错, supervisor 的错]      ← 按发生顺序
```

**关键是第 6 行：失败没有中断循环。** 如果第一个抛错就 `throw` 出去，后面三个资源永远不会被关——子进程留着、文件锁不释放。所以每个资源单独 `try/catch`，全部走完再决定怎么报。

`errors` 数组的顺序是 `teammate` 在前、`supervisor` 在后，跟**关闭顺序**一致，不是注册顺序。看聚合错误时按这个顺序读，就能知道哪一层先出问题。

一个细节：**只有一个失败时不会包成 `AggregateError`**，而是原样抛出那个错误。因为包起来会多一层，让调用方为了看一条错误还得先展开 `.errors`。这属于"错误处理不要制造新的噪音"。

### 实验 8（events）：同一事件只进历史一次

这个实验手写一个事件泵，往队列里塞**两个 `eventId` 相同**的事件，然后看历史里进了几条。

```typescript
// ---------- 实验 8（events）：同一事件重复投递只进历史一次 ----------
if (section("events")) {
  class ProbeEvent implements RuntimeEvent {
    constructor(readonly eventId: string) {}

    toPayload(): Readonly<Record<string, unknown>> {
      return Object.freeze({ kind: "background_job", prompt: `事件 ${this.eventId} 完成` });
    }
  }

  const acked: string[] = [];
  const queue: RuntimeEvent[] = [new ProbeEvent("evt-1"), new ProbeEvent("evt-1")];
  const pump = {
    get hasPendingWork(): boolean {
      return queue.length > 0;
    },
    drainEvents(limit = queue.length): readonly RuntimeEvent[] {
      return Object.freeze(queue.splice(0, limit));
    },
    async waitForEvents(limit = queue.length): Promise<readonly RuntimeEvent[]> {
      return this.drainEvents(limit);
    },
    acknowledgeEvents(events: readonly RuntimeEvent[]): void {
      for (const event of events) acked.push(event.eventId);
    },
  };
```

这个 `pump` 是刻意做成"会重复投递"的：两个 `ProbeEvent` 是**不同的对象**，但 `eventId` 都是 `"evt-1"`。真实世界里这对应 at-least-once 投递——ack 丢了、进程重启、网络重试，都会让同一个事件到达两次。

```typescript
  const model: ModelClient = {
    async complete(request: ModelRequest) {
      return {
        message: assistantMessage(`收到 ${request.messages.filter((m) => m.role === "user").length} 条 user 消息`),
        finishReason: "stop" as const,
      };
    },
  };
  const runner = new AgentRunner({
    model,
    tools: new ToolRegistry(),
    systemPrompt: "system",
    workspace: process.cwd(),
    eventPump: pump,
  });
```

```typescript
  try {
    const first = await runner.runEvents();
    step(1, `第 1 次 runEvents() → ${first?.finalText ?? "(没有事件)"}`);
    const second = await runner.runEvents();
    step(2, `第 2 次 runEvents()（同一个 eventId 再投一遍）→ ${second?.finalText ?? "undefined"}`);
    const events = runner.history.filter(
      (message) => message.role === "user" && message.content.includes("runtime_event"),
    );
    step(3, `canonical history 里的 runtime_event 条数：${events.length}`);
    step(4, `ack 记录：[${acked.join(", ")}]`);
  } finally {
    await runner.close();
  }
}
```

```powershell
npx tsx scripts\probe-ch20.mts events
```

```text
===== events =====
[1] 第 1 次 runEvents() → 收到 1 条 user 消息
[2] 第 2 次 runEvents()（同一个 eventId 再投一遍）→ undefined
[3] canonical history 里的 runtime_event 条数：1
[4] ack 记录：[evt-1]
```

四行输出对应一条完整的去重语义：

**第 1 行：** 第一个事件被接受，包成一条 user 消息进历史，模型看到了 1 条。

**第 2 行：** 第二个事件（同 id）被丢弃，`runEvents()` 返回 `undefined`——**连模型都没调**。这个返回值很重要：`undefined` 表示"这一轮什么都没发生"，调用方（比如 CLI 的事件循环）据此决定要不要继续等。

**第 3 行：** 历史里只有 1 条。这是最终要保护的性质。

**第 4 行：** 但 `evt-1` 被 ack 了**一次**。注意不是零次——第一个事件成功进历史之后才 ack。

第 4 行值得多想一层。为什么 ack 记录里只有一条，而不是两条？因为第二次投递的事件被去重丢弃了，走的不是"接受 → 进历史 → ack"这条路。它在进历史之前就被拦下，所以不 ack。

这看起来像是"漏 ack 了"，其实不是问题：`eventId` 相同意味着它就是同一个事件，第一次的 ack 已经代表它被处理过了。真实实现里，事件源那边看到的是"这个 id 已经 ack 过"，不会无限重投。

**顺序也是刻意的：历史先写，ack 后发。** 反过来的话（先 ack 再写历史），如果写历史失败，事件就永远丢了——事件源认为已经交付，Agent 那边什么都没有。现在这个顺序最坏的情况是"写进去了但没 ack 上"，下一次会再投一遍，然后被 `eventId` 去重拦掉。**至少一次投递 + 幂等消费 = 恰好一次效果**，这是分布式系统里最常用的一对组合。

### 实验 9（recovery）：六次失败，两条消息

```typescript
// ---------- 实验 9（recovery）：恢复尝试不进 canonical history ----------
if (section("recovery")) {
  await withTempDir(async (root) => {
    const model = new RecoveryProbeModel();
    const connection = new FakeConnection();
    const harness = createHarness(root, model, createRuntime(connection));
    const runner = buildAgent(P20, {
      ...harness.dependencies,
      recoveryConfig: new RecoveryConfig({
        primaryModel: "primary",
        fallbackModel: "fallback",
        baseDelaySeconds: 0.001,
        maxDelaySeconds: 0.001,
        jitterRatio: 0,
      }),
    });
    try {
      const result = await runner.run("验证恢复边界");
      const main = model.requests.filter((request) => request.tools.length > 0);
      step(1, `模型侧真实失败序列：[${model.failures.join(", ")}]`);
      step(2, `主请求次数：${main.length}`);
      step(3, `每次请求用的模型：[${main.map((request) => request.model).join(", ")}]`);
      step(4, `每次请求的 maxTokens：[${main.map((request) => request.maxTokens).join(", ")}]`);
      step(5, `最终回答：${result.finalText}`);
      step(6, `canonical history 长度：${result.history.length}`);
      step(
        7,
        `history 内容：${result.history
          .map((message) => `${message.role}:${message.content ?? "(null)"}`)
          .join(" | ")}`,
      );
      step(
        8,
        `history 里有 finish_reason=length 那条被丢弃的回复吗：${String(
          result.history.some((message) => message.content?.includes("discarded") === true),
        )}`,
      );
    } finally {
      await runner.close();
    }
  });
}
```

那三个 `0.001` 和 `jitterRatio: 0` 是为了实验能秒跑完。生产配置里退避是真的要等的，抖动也是必要的——否则一批 Agent 会在同一毫秒重试，把过载变成雪崩。

```powershell
npx tsx scripts\probe-ch20.mts recovery
```

```text
===== recovery =====
[1] 模型侧真实失败序列：[429, 529, 529, 529, length, prompt-too-long]
[2] 主请求次数：7
[3] 每次请求用的模型：[primary, primary, primary, primary, fallback, fallback, fallback]
[4] 每次请求的 maxTokens：[8000, 8000, 8000, 8000, 8000, 64000, 64000]
[5] 最终回答：recovered
[6] canonical history 长度：2
[7] history 内容：user:验证恢复边界 | assistant:recovered
[8] history 里有 finish_reason=length 那条被丢弃的回复吗：false
```

七次请求，六次失败，最后成功。把第 1、3、4 行三个数组竖着对齐，整条恢复轨迹就清楚了：

```text
        失败          模型        maxTokens   RecoveryManager 做了什么
第 1 次  429          primary     8000        按 Retry-After 等待，重试
第 2 次  529          primary     8000        计数 1/3，退避重试
第 3 次  529          primary     8000        计数 2/3，退避重试
第 4 次  529          primary     8000        计数 3/3 → 达到阈值，切 fallback
第 5 次  length       fallback    8000        上限 8000 → 64000，丢弃片段重来
第 6 次  prompt-too-long fallback 64000       触发一次压缩，重来
第 7 次  （成功）      fallback    64000       返回 recovered
```

几个容易读错的地方：

**模型切换发生在第 5 次请求，不是第 4 次。** 第 4 次请求**用的还是 primary**，它失败之后才切。`overloadFallbackThreshold = 3` 数的是"连续 529 的次数"，数到 3 才动作。

**`maxTokens` 提升也发生在下一次请求。** 第 5 次请求用的还是 8000，它返回 `length` 之后，第 6 次才是 64000。

**529 不看 `Retry-After`，429 看。** 这两个状态码语义不同：429 是"你太快了，等这么久再来"，服务端明确告诉你等多久；529 是"我这边扛不住了"，等多久都是猜，所以用指数退避，而且要考虑换个模型。

第 6、7、8 行是这个实验真正要证明的东西。**六次失败，canonical history 里一条痕迹都没有**：只有一条 user 和一条 assistant。

第 8 行专门去找那条 `finishReason === "length"` 的回复（内容是 `"discarded"`）——找不到。它被丢弃了，没进历史。

为什么必须这样？如果恢复过程进历史，会发生两件坏事：

```text
坏事一：模型看到自己的失败
  history 里有一条被截断的残缺回复
  → 模型下一轮会试图"接着"那条残缺内容写
  → 但它已经被丢弃了，没有上下文
  → 输出错乱

坏事二：历史被污染，压缩跟着倒霉
  6 次失败 = 6 条垃圾消息
  → 压缩要处理它们，还可能把它们当成"关键发现"总结进摘要
  → 一次网络抖动被写进了 Agent 的长期记忆
```

实现上靠的是 10.3 节讲的那件事：`#prepareModelRequest` 返回的 `requestMessages` 是一个**局部的冻结副本**，恢复管理器改的是这个副本。`this.#history` 从头到尾只在两个地方被 `push`——收到用户输入时，和收到**成功的**模型回复时。

### 实验 10（surface）：23 个工具，一个不多

```typescript
// ---------- 实验 10（surface）：Lead 工具面与子执行者裁剪 ----------
if (section("surface")) {
  await withTempDir(async (root) => {
    const model = new HarnessProbeModel();
    const connection = new FakeConnection();
    const mcpRuntime = createRuntime(connection);
    const harness = createHarness(root, model, mcpRuntime);
    const runner = buildAgent(P20, harness.dependencies);
    try {
      await runner.run("列一下工具");
      const main = model.requests.filter((request) => request.tools.length > 0);
      const first = main[0];
      if (first === undefined) throw new Error("probe expected one main request");
      const names = toolNamesOf(first);
      step(1, `Lead 工具面共 ${names.length} 个：`);
      for (const [index, name] of names.entries()) {
        console.log(`      ${String(index + 1).padStart(2, " ")}. ${name}`);
      }
      step(2, `Lead 有 request_shutdown / review_plan：${String(
        names.includes("request_shutdown") && names.includes("review_plan"),
      )}`);
      step(3, `Lead 有 submit_plan：${String(names.includes("submit_plan"))}`);
      step(4, `Lead 有 connect_mcp / disconnect_mcp：${String(
        names.includes("connect_mcp") && names.includes("disconnect_mcp"),
      )}`);
      const teammateSurface = ["shell", "read_file", "write_file"];
      step(5, `队友被裁剪到的内置工具：[${teammateSurface.join(", ")}]，再加 send_message / submit_plan 与租约任务工具`);
      step(6, `队友能看到 connect_mcp 吗：${String(teammateSurface.includes("connect_mcp"))}`);
    } finally {
      await runner.close();
    }
  });
}
```

第 5、6 行的 `teammateSurface` 是**照抄** `bootstrap.ts` 第 212 行 `subset()` 的参数，不是运行时读出来的——队友要真的 spawn 出来才会发请求。想自己确认这个数组只有这一处，搜一下就知道：

```powershell
Select-String -Path 'chapters\ch20\src\bootstrap.ts' -Pattern 'subset\('
```

整个组合根里只有一行命中。Subagent 走的是另一条路（`createStandardTools(profile, ..., false)`，最后那个 `false` 就是"不是 Lead"），7.3 节讲过这两条路为什么都拿不到 MCP。

```powershell
npx tsx scripts\probe-ch20.mts surface
```

```text
===== surface =====
[1] Lead 工具面共 23 个：
       1. shell
       2. read_file
       3. write_file
       4. edit_file
       5. glob
       6. todo_write
       7. task
       8. load_skill
       9. create_task
      10. get_task
      11. list_tasks
      12. claim_task
      13. complete_task
      14. create_worktree
      15. keep_worktree
      16. remove_worktree
      17. schedule_cron
      18. spawn_teammate
      19. send_message
      20. request_shutdown
      21. review_plan
      22. connect_mcp
      23. disconnect_mcp
[2] Lead 有 request_shutdown / review_plan：true
[3] Lead 有 submit_plan：false
[4] Lead 有 connect_mcp / disconnect_mcp：true
[5] 队友被裁剪到的内置工具：[shell, read_file, write_file]，再加 send_message / submit_plan 与租约任务工具
[6] 队友能看到 connect_mcp 吗：false
```

这个列表的**顺序**不是字母序，是注册顺序，也就是能力被引入的章节顺序。1 到 8 是 P1 到 P7 的基础工具，9 到 13 是 P12 的任务系统，14 到 16 是 P18 的 worktree，17 是 P14 的 Cron，18 到 19 是 P15 的队友，20 到 21 是 P16 的协议，22 到 23 是 P19 的 MCP。

第 3 行是这个实验最容易被忽略的一条：**Lead 没有 `submit_plan`**。计划是队友提交给 Lead 审的，Lead 自己不提交计划。这三个协议工具的分配是：

```text
submit_plan      → 只给队友     （我要做这些，请批）
review_plan      → 只给 Lead    （我批了 / 我打回）
request_shutdown → 只给 Lead    （你可以收工了吗）
```

如果 Lead 也有 `submit_plan`，它就能给自己提交计划再自己批准——一个形式上完整、实质上空转的流程。这类"角色工具不对称"的设计，是多 Agent 系统里防止自我授权的常规手法。

### 跑全部十个

```powershell
npx tsx scripts\probe-ch20.mts
```

不带参数就是 `all`，十段依次跑，一共 119 行输出。整个过程在我这台机器上约 1.7 秒，因为没有真实网络、没有子进程、退避时间全被调到 1 毫秒。

### 实验完成后清理

```powershell
Remove-Item 'F:\笔记\Agent实操\code\scripts\probe-ch20.mts'
```

实验里的临时目录由 `withTempDir` 的 `finally` 自动删。没有子进程要清——这一章的实验全部在进程内跑完。

---

## 这章刻意没有做什么

一个整合章最容易犯的错，是趁着"收尾"顺手加一堆东西。P20 明确排除下面这些：

| 没做 | 为什么 |
| --- | --- |
| 第二个 Loop、第二个 Runner、第二条历史 | `full_harness` 是标记不是模块。一个 Agent 就该只有一条执行路径 |
| 运行时能力开关 | 档案是编译期常量，`profileForChapter()` 只认 20 个固定对象。能改的组合就是能出错的组合 |
| 让 `runtime_status` 参与授权 | 它是给模型看的观测量。6.4 节：可观测 ≠ 可授权 |
| 用 `runtime_status` 替代 `EventInbox` | 它只能说"有没有待办"，说不出"发生了什么"。事件必须进历史，状态栏不进 |
| 补回 `mcp-external-approval` 规则 | 本章任务是讲清 P20 的现状。11.4 节给了完整改法和三条证据，改不改是你的决定 |
| 跨进程 / 跨机器的 Harness | 全部在一个 Node 进程内。队友是同进程的 Runner，不是独立进程 |
| 把子进程说成沙箱 | `shell` 和 stdio MCP server 都是**和 Agent 同等权限**的本机进程。真隔离要靠容器 |
| 关闭超时与强杀 | `close()` 会老实等每个资源。一个卡死的资源会挂住整个关闭，没有超时兜底 |
| 事件去重表的持久化 | `#seenEventIds` 是 Runner 实例的字段，进程重启就空了 |
| 新的模型能力 | 没有 function calling 之外的新协议，没有流式，没有多模态 |

有两条要展开说，因为它们是真实的边界，不是"以后再做"。

**关闭没有超时。** 看一眼 12.2 节那段代码就知道：`for` 循环里 `await resource.close()`，没有 `Promise.race`，没有定时器。如果某个 MCP server 的子进程卡在 `close()` 里不返回，整个进程就停在那儿了。生产系统里通常会加"等 N 秒还没关就 SIGKILL"，本章没做——因为强杀的语义（要不要等清理、要不要保状态）依赖具体资源类型，做成通用逻辑会变成一堆开关。

**事件去重表不跨进程。** `#seenEventIds` 只在一个 Runner 实例的生命周期内有效。那进程重启后会不会把已处理的事件再跑一遍？不会，但**靠的不是这张表**，而是各个 store 里的 ack 状态：

```text
一个 cron 事件的完整生命周期

  cron.tick()          → 事件写入 cronStore.pendingEvents()   [持久]
  runEvents()          → 事件进历史
  acknowledgeEvents()  → 从 pendingEvents() 删除              [持久]

  进程重启
  → pendingEvents() 里已经没有它了
  → 事件泵读不到，自然不会重放
```

`ch20-full-harness.test.ts` 的第 6 个测试专门验证这条：跑掉一个 cron 事件、留一个未消费的，然后 `close()` 掉整个 Runner、用同一个工作目录**重建**一套依赖，确认 `pendingEvents()` 里只剩那个没消费的。`#seenEventIds` 在这个场景里根本没派上用场——它防的是同一进程内的重复投递（实验 8 那种），持久去重是 store 的责任。

**这两层分工要记住：内存表防进程内重复，持久 ack 防跨重启重放。** 少任何一层都会漏。

---

## 与 Claude Code 的差异

前十九章每章都对照过一次局部差异。这一章对照的是**整体形状**：一个真实产品的 harness 和一个教学 harness 差在哪。

### 我们简化了的地方

| 维度 | Claude Code | 本章 |
| --- | --- | --- |
| 能力组合 | 运行时可配置：settings 文件、CLI 参数、插件、企业策略多层合并 | 20 个固定档案，编译期常量，不可组合 |
| 进程模型 | 主进程 + 多个子进程（MCP server、shell、IDE 扩展），有进程管理和重启 | 一个 Node 进程，队友和 Subagent 都是同进程的 Runner |
| 状态栏 | 丰富的状态行：token 用量、成本、模型名、当前分支、上下文占比 | `runtime_status` 只有两个字段：`mcp_connections` 和 `pending_work` |
| 关闭 | 分阶段关闭 + 超时 + 强杀，孤儿进程有回收机制 | 逆序 `close()`，无超时，无强杀 |
| 事件来源 | 后台任务、Cron、队友、IDE、远程会话、MCP 通知 | 后台任务、Cron、队友三种 |
| 权限交互 | 可视化审批面板、`/permissions` 管理、durable 授权、模式切换（plan/acceptEdits/bypass） | 终端 y/n 审批，无持久授权，无模式 |
| 恢复 | 分类更细的重试策略，多 provider fallback，请求级预算控制 | 429 / 529 / length / prompt-too-long 四类，单条 fallback 链 |
| 上下文 | 自动压缩 + 手动 `/compact` + 微压缩 + 缓存断点管理 | 请求级压缩 + 一次反应式压缩 |

### 我们比产品更严的地方

这些不是"教学版凑合一下"，而是刻意选了更硬的约束——因为教学代码要能被读懂，而不只是能跑。

**档案身份用引用比较。** `profileForChapter(profile.chapter) !== profile` 就报错。真实产品必须支持用户自定义配置，不可能这么做。但对教学代码，这道门把"可能的能力组合"从 2²⁶ 收缩到 20 种，每一种都有测试。

**依赖共享用对象身份校验。** 四组"必须是同一个对象"的检查（3.4 节）在启动时就跑。产品通常靠依赖注入容器保证单例，出问题时报错信息是容器的内部错误。这里的报错直接说"cronRuntime must share the background supervisor and event inbox"——它告诉你**该怎么改**。

**恢复过程一条都不进历史。** 严格到连被截断的模型回复都丢弃（实验 9 第 8 行）。产品里为了调试往往会保留一些痕迹。

**关闭失败不吞。** 两个以上失败聚合成 `AggregateError` 抛出，一个失败原样抛出。**关闭失败也是失败**，不能因为"反正要退出了"就当没发生——泄漏的子进程和文件锁会影响下一次启动。

**`runtime_status` 只有两个字段。** 这是刻意的最小化。每加一个字段都要回答两个问题：它是同步可得的吗？它会不会被误当成授权依据？6.4 节讲过 `pending_work` 只报布尔不报内容的原因。

---

## 从 ch19 到 ch20，代码到底改了什么

这一节把 diff 摊开。之所以值得单列，是因为它能证明一件事：**"完整"不等于"多"**。

先看文件层面。ch20 相对 ch19 只**新增了三个文件**：

| 新增文件 | 行数 | 作用 |
| --- | --- | --- |
| `src/chapters/ch20.ts` | 5 | 固定章节入口 |
| `tests/ch20-full-harness.test.ts` | 1210 | 7 个跨功能场景 |
| `tests/ch20-entry.test.ts` | 65 | 4 个入口与档案测试 |

**没有新增任何 `src/features/` 或 `src/adapters/` 文件。** 一个新增的运行时都没有。

再看修改的文件。13 个文件有 diff，但性质完全不同，分三类：

**第一类：只改了注释（5 个文件）。**

```text
src/features/mcp-tools.ts    806 → 756 行
src/adapters/mcp-client.ts   347 → 335 行
src/adapters/mcp-schema.ts    76 →  73 行
src/core/tools.ts            380 → 379 行
src/mcp-servers/demo.ts       63 →  61 行
```

这五个文件把注释去掉之后**逐字节相同**。你可以自己验一遍：

```powershell
Set-Location 'F:\笔记\Agent实操\code\chapters'
$a = Get-Content ch19\src\features\mcp-tools.ts | Where-Object { $_ -notmatch '^\s*//' -and $_.Trim() -ne '' }
$b = Get-Content ch20\src\features\mcp-tools.ts | Where-Object { $_ -notmatch '^\s*//' -and $_.Trim() -ne '' }
Compare-Object $a $b
```

没有输出。MCP 那 756 行核心逻辑，P20 一个字符都没动——它继承的是**已经验证过的**边界，不是重新实现一遍。

**第二类：P20 真正的新增（2 个文件）。**

| 文件 | 行数 | 改了什么 |
| --- | --- | --- |
| `src/features/prompting.ts` | 252 → 274 | 加 `status?: JsonObject` 字段、`DynamicPromptStatusProvider` 类型、缓存键纳入 `status`、末尾追加 `## runtime_status` 段落、构造期校验 `statusProvider` 必须是函数 |
| `src/bootstrap.ts` | 493 → 517 | 加 `fullHarnessRuntimeStatus()`（12 行）、`full_harness` 时装 `statusProvider`（5 行）；另外把装配校验抽成独立函数 |

**22 行 + 24 行。这就是 P20 全部的新增功能代码。**

**第三类：结构重整，行为不变（3 个文件）。**

| 文件 | 行数 | 重整了什么 |
| --- | --- | --- |
| `src/core/profiles.ts` | 326 → **152** | 20 个手写的冻结对象 → `PROFILE_DELTAS` 增量数组 + 推导 |
| `src/core/loop.ts` | 781 → 789 | 把回合里那段"组装请求"的内联代码抽成 `#prepareModelRequest()` |
| `src/cli.ts` | 403 → 447 | 抽出 `createLiveRuntime()` 和 `LiveRuntime` 接口 |

`profiles.ts` 那个 **−174 行**是本章最漂亮的一处改动。ch19 里那 326 行由两段样板组成。

第一段：**前 11 个档案手写完整能力集**。P11 长这样：

```typescript
export const P11: ChapterProfile = Object.freeze({
  chapter: 11,
  capabilities: new CapabilitySet([
    "loop", "powershell", "tool_registry", "files", "policy", "hooks", "todo",
    "subagent", "skills", "artifacts", "compaction", "memory", "dynamic_prompt",
    "recovery",
  ]),
});
```

14 个字符串手写一遍。P10 里前 13 个再写一遍，P09 里前 12 个再写一遍……P12 之后才改用 spread：

```typescript
export const P19: ChapterProfile = Object.freeze({
  chapter: 19,
  capabilities: new CapabilitySet([...P18.capabilities, "mcp"]),
});
```

**同一个文件里两种写法并存**——前 11 个手抄，后 8 个继承。手抄的那 11 个里漏掉一个能力，代码照样编译通过，"P(N) ⊃ P(N-1)"只是一个约定。

第二段：**`profileForChapter` 是一条 19 分支的 if 链**（264 到 326 行，整整 63 行）：

```typescript
export function profileForChapter(chapter: number): ChapterProfile {
  if (chapter === 1) { return P01; }
  if (chapter === 2) { return P02; }
  // ……一路到 19
  if (!Number.isInteger(chapter) || chapter < 1 || chapter > 20) {
    throw new Error("chapter must be an integer from 1 to 20");
  }
  throw new Error(`Chapter ${chapter} has not been migrated to TypeScript yet`);
}
```

加一章就要改两处：写一个新档案，再补一条 if。而且注意最后那行 —— 边界写的是 `chapter > 20`，但 if 链只到 19，所以 `profileForChapter(20)` 在 ch19 里会掉进 `has not been migrated` 那个分支。**两个数字必须手工保持一致，这就是样板代码的典型代价。**

ch20 把两段一起消掉了（2.1 节讲过 `slice(0, index + 1).flat()`）：能力集从 `PROFILE_DELTAS` 推导，if 链换成一次数组下标 + `PROFILES.length` 边界检查。少写 174 行只是副产品，真正的收获是**两处不一致都不可能再发生**。

`loop.ts` 那 +8 行是纯提取。ch19 的回合循环里，从 `validateToolPairing(this.#history)`（369 行）到 `tools: tools.openAITools()`（409 行）那 41 行是**内联在 `for` 循环体里**的；ch20 把它们搬进 `#prepareModelRequest()`，循环里只剩一行调用：

```typescript
// #prepareModelRequest 负责请求级压缩、动态提示注入和 tool round 观察器指导；
// turnGuidance/observerGuidance 只影响本次模型请求，不写入 canonical history。
const { request, tools } = await this.#prepareModelRequest();
```

这个提取对本章有直接价值：4.1 节那张顺序图之所以能画得清楚，就是因为"组装请求"现在是一个有名字的步骤。**`tools` 和 `request` 由同一个方法一起返回**，这个签名本身就在说"它们必须来自同一时刻"——实验 4 里工具数（第 2、4 行）和 `runtime_status`（第 6、7 行）必须同步变化，被函数签名钉住了。

### 测试的变化

| 文件 | 行数 | 测试数 | 变化 |
| --- | --- | --- | --- |
| `tests/ch20-full-harness.test.ts` | 1210 | 7 | 新增 |
| `tests/ch20-entry.test.ts` | 65 | 4 | 新增 |
| `tests/prompting.test.ts` | 324 → 369 | 8 | 加了 `runtime_status` 的位置、缓存键、`statusProvider` 校验 |
| `tests/profiles.test.ts` | 147 → 160 | 2 | 改成验证增量模型与固定身份 |
| `tests/ch19-bootstrap.test.ts` | 469 → **286** | 6 → 3 | **删了三个 MCP 审批测试**（11.3 节） |

前四行是正常的演进。最后一行是本章唯一一处**回退**——测试和它保护的规则一起消失了。这也是 11.2 到 11.5 节存在的理由：一个整合章最该做的事，就是把这种"上一章有、这一章没了"的东西找出来。

---

## 配套代码索引

本章代码全部在 `code/chapters/ch20/` 下。整章 63 个源文件共 18573 行，67 个测试文件共 15541 行 —— 但**其中属于"第 20 章"的只有下面这几个表格里的内容**，剩下的都是前十九章一路攒下来的。

**新增文件（3 个）：**

| 文件 | 行数 | 本章负责什么 |
| --- | --- | --- |
| [src/chapters/ch20.ts](code/chapters/ch20/src/chapters/ch20.ts) | 5 | 固定章节入口，和前十九章一模一样的五行 |
| [tests/ch20-full-harness.test.ts](code/chapters/ch20/tests/ch20-full-harness.test.ts) | 1210 | 7 个整合测试。前 735 行是共享的装配辅助，测试体从 736 行开始 |
| [tests/ch20-entry.test.ts](code/chapters/ch20/tests/ch20-entry.test.ts) | 65 | 4 个入口测试：能力累计、固定入口的缺配置时机、统一入口解析第 20 章、非 Git workspace 前先关 model |

**本章重点改动的文件（5 个）：**

| 文件 | 行数 | 本章改了什么 |
| --- | --- | --- |
| [src/features/prompting.ts](code/chapters/ch20/src/features/prompting.ts) | 252 → 274 | `status?: JsonObject`（31）、`DynamicPromptStatusProvider`（35）、缓存键纳入 `status`（87）、`## runtime_status` 追加到末尾（111）、`statusProvider` 必须是函数（142-143）、每次渲染调用一次 provider（164） |
| [src/bootstrap.ts](code/chapters/ch20/src/bootstrap.ts) | 493 → 517 | `fullHarnessRuntimeStatus()`（77-90）、`full_harness` 时装 `statusProvider`（282-283）、装配校验抽成 `validateBuildDependencies()`（330）、`usesJsonTaskStore()`（441） |
| [src/core/profiles.ts](code/chapters/ch20/src/core/profiles.ts) | 326 → 152 | `PROFILE_DELTAS`（81-103）、推导 `PROFILES`（106-113）、`profileForChapter` 边界（137-143）；`"full_harness"` 能力位 |
| [src/core/loop.ts](code/chapters/ch20/src/core/loop.ts) | 781 → 789 | 抽出 `#prepareModelRequest()`（475-532），回合循环里只剩一行调用（372） |
| [src/cli.ts](code/chapters/ch20/src/cli.ts) | 403 → 447 | 抽出 `LiveRuntime` 接口（99）和 `createLiveRuntime()`（211） |

**本章要读但没改的文件**（P20 的行为几乎全在这些里，它们从上一次出现时就没动过）：

| 文件 | 行数 | 本章为什么要看它 |
| --- | --- | --- |
| [src/core/loop.ts](code/chapters/ch20/src/core/loop.ts) `#prepareModelRequest` | 475-532 | 4.1 节那张顺序图的原文。505 渲染提示、511 拍快照 |
| [src/core/loop.ts](code/chapters/ch20/src/core/loop.ts) `close` | 663-690 | 逆序关闭 + 失败聚合。12 节全部依据 |
| [src/core/messages.ts](code/chapters/ch20/src/core/messages.ts) `validateToolPairing` | 117-149 | 不变量二的唯一实现。实验 6 的六种输入都打在这 33 行上 |
| [src/core/tools.ts](code/chapters/ch20/src/core/tools.ts) `snapshot` / `version` | 379 行文件 | 不变量一。实验 5 全部依据 |
| [src/core/permissions.ts](code/chapters/ch20/src/core/permissions.ts) `#workspaceBoundaryDecision` / `strongestDecision` | 255 / 302 | "deny 强于 ask"。实验 4 第 10 行为什么是空数组 |
| [src/core/events.ts](code/chapters/ch20/src/core/events.ts) `EventInbox` | 32 起 | 三个来源一个收件箱。9 节与实验 8 |

### 读代码的建议顺序

本章没有新运行时可读，**要读的是"连接处"**。按这个顺序走，每一步都在回答上一步留下的问题：

1. **`src/core/profiles.ts` 全文（152 行）** —— 从 `PROFILE_DELTAS` 开始。这是整章唯一的"新概念"，而且它把 20 个档案压成了一个数组。十分钟读透，后面全部受益。
2. **`src/bootstrap.ts` 的 77-90 行** —— `fullHarnessRuntimeStatus()`，12 行。P20 的全部新功能就在这里。注意它只读同步 getter。
3. **`src/bootstrap.ts` 的 282-283 行** —— `full_harness` 唯一被读取的地方。两行。
4. **`src/features/prompting.ts` 的 96-113 行** —— `## runtime_status` 为什么必然在最后。看 `sections` 数组的初值和三个 `push` 的先后。
5. **`src/features/prompting.ts` 的 77-89 行** —— 缓存键。为什么改 status 就必然 miss。
6. **`src/core/loop.ts` 的 475-532 行** —— `#prepareModelRequest()` 全文。读的时候数一下 505 和 511 之间隔了几行。
7. **`src/core/messages.ts` 的 `validateToolPairing`** —— 33 行代码，五种拒绝。配着实验 6 的六行输出读（第 1 行是合法的对照组）。
8. **`src/core/permissions.ts` 的 302-313 行** —— `strongestDecision` 的 `["deny","ask","allow"]` 顺序。一个 for 循环决定了实验 4 第 10 行。
9. **`src/core/loop.ts` 的 663-690 行** —— `close()`。`[...].reverse()`、per-resource try/catch、`#closed` 的赋值位置。
10. **`tests/ch20-full-harness.test.ts` 的 1035-1135 行** —— 第 6 个测试。跨重启去重靠的是 store 里的 ack，不是内存表。这是本章最容易想错的一处。
11. **`src/bootstrap.ts` 的 330-438 行** —— `validateBuildDependencies()`。开头就是那句"profile must be a fixed chapter profile"，末尾是四组对象身份检查。配着实验 2 的七行输出读，一行对一条。

可以跳过：`src/cli.ts` 的参数解析和 `createLiveRuntime()` 内部（它只是把 ch19 的内联代码搬了个地方）；`tests/ch20-full-harness.test.ts` 的前 735 行装配辅助 —— 除非你要自己写新测试。

---

## 运行与验证

五道门，按顺序过。全部在 `F:\笔记\Agent实操\code` 下执行：

```powershell
Set-Location 'F:\笔记\Agent实操\code'
npm run test:ch20
npm run typecheck
npm run lint
npm run format:check
npm run build
```

我这边的实测结果：

```text
Test Files  65 passed (65)
     Tests  428 passed (428)
  Duration  18.65s
```

`test:ch20` 会连带跑前面所有章节的测试，所以是 65 个文件、428 个测试，不是只有 ch20 那 2 个文件。**对整合章来说这是最重要的一次运行** —— 第 20 章改了 `profiles.ts`（20 个档案的构造方式）、`prompting.ts`（每一章都在用的动态提示）和 `loop.ts`（每一章都在用的回合循环）。这三个文件动一下，前十九章任何一处行为变化都会在这 428 个测试里露出来。

跑的过程中你会在输出里看到这几行：

```text
运行失败: workspace is not a Git repository root
运行失败: CLI execution or cleanup failed
配置错误: Missing required settings: OPENAI_BASE_URL, OPENAI_API_KEY, OPENAI_MODEL
```

**这些不是报错，是被断言的正确行为。** 从第 11 章起每章都有一个入口测试，专门验证"配置缺失或 workspace 非法时必须以这种方式失败"，所以这几行是多个章节的入口测试叠加出来的。判断成败只看最后的 `Tests 428 passed`。

单跑 `ch20-entry.test.ts` 你只会看到一行：

```powershell
npx vitest run chapters/ch20/tests/ch20-entry.test.ts
```

```text
运行失败: workspace is not a Git repository root

 Test Files  1 passed (1)
      Tests  4 passed (4)
```

只有一行的原因是：那 4 个测试里，两个"缺配置"测试用 `vi.spyOn(process.stderr, "write")` 把输出接走了并断言内容含 `OPENAI_FALLBACK_MODEL`，所以你看不到；只有最后那个"非 Git workspace"测试的 `运行失败` 是真的打到终端上的。

只想看本章那 11 个测试：

```powershell
npx vitest run chapters/ch20/tests/ch20-full-harness.test.ts chapters/ch20/tests/ch20-entry.test.ts
```

```text
Test Files  2 passed (2)
     Tests  11 passed (11)
  Duration  4.84s
```

`typecheck`、`lint`、`format:check`、`build` 我这边都是干净通过 —— `biome lint` 和 `biome format` 各检查 1367 个文件，`No fixes applied`。

本章测试注入了确定的 UUID、UTC clock 和模型替身，**不依赖真实 OpenAI、不依赖网络**。7 个整合测试的关键断言都是具体的业务结果，不是"没抛错"：

- **测试 1**（`combines dynamic context, MCP policy, pairing, and resource ownership`）：一次回合里，动态提示的 `runtime_status` 反映了当前 MCP 连接，同一条回复里三个工具调用各拿到自己 id 的结果，越界写在到达审批之前就被 deny，`close()` 之后 `McpRuntime.isClosed === true`。
- **测试 2**（`persists a large MCP result while retaining TODO and DAG state`）：一个超大 MCP 结果落成 artifact（`.agent_tutorial/artifacts` 下恰好 1 个文件，内容含完整原文），同时 TODO 两条的 `status` 和 DAG 里 `taskB.blockedBy === [TASK_A]` 一条都没丢，远程 `callTool` 只被调了 1 次。
- **测试 3**（`delivers background and cron completions as typed events exactly once`）：后台完成和 Cron 到点各投递一次，`runtime_event` 在 canonical history 里各出现一条。
- **测试 4**（`keeps recovery attempts outside canonical history and compacts prompt-too-long once`）：六种模型侧失败全部恢复之后，canonical history 长度仍然是 2，`finishReason === "length"` 那条残片不在里面，`prompt-too-long` 只触发了一次压缩。
- **测试 5**（`forces one Stop continuation and then accepts the final model answer`）：Stop hook 第一次返回 `forceContinue` 续一轮，第二次看到 `stopHookActive === true` 就放行。assistant 文本恰好是 `["intermediate", "final"]`，`stopCalls === 2` —— 强制续轮不是无限循环。
- **测试 6**（`rebuilds task, memory, cron outbox, and worktree state without replaying an acked event`）：**跨进程重启**后，task / memory / cron outbox / worktree 四份状态全部从磁盘重建，而已经 ack 过的事件没有重放。注意它靠的是 store 里的 ack 记录，不是内存去重表（9.2 节讲过这个区别）。
- **测试 7**（`gates teammate effects before approval and routes the approved claim into its worktree`）：队友在审批前想写的 `before-approval.txt` 读起来是 `ENOENT`（副作用真的没发生），审批通过后写出的 `worker.txt` 只存在于 `binding.relativePath` 下，仓库根目录下的 `worker.txt` 同样是 `ENOENT` —— claim 被路由进了队友自己的 worktree，不是 Lead 的工作目录。

如果你也跑了那十个动手小实验，`probe-ch20.mts` 那 119 行输出是对上面这些断言的另一种确认方式：测试告诉你"结果对不对"，探针告诉你"过程长什么样"。

---

## 本章小结

### 三句话版本

第一，把十九章的机制装进一个 Agent，需要的不是新功能，而是**一条固定的顺序和几条不能违反的不变量**。P20 只新增了 46 行功能代码。

第二，**错误的装配必须在启动时就炸，不能等到运行时**。`buildAgent` 的七条门禁全部在创建任何状态之前完成，因为一个"功能都在但各自持有状态"的 Agent 比一个启动失败的 Agent 危险得多。

第三，**Prompt 和 Hook 都不是授权边界**。`runtime_status` 告诉模型"现在有什么"，权限管道决定"能做什么"，这两件事在代码里就不该有交集。

### 一定要记住的十条

1. **能力是累加出来的，不是抄出来的。** `PROFILE_DELTAS` 只声明每章新增，`slice(0, index + 1).flat()` 推导完整档案 ——"P(N) ⊃ P(N-1)"从此是实现的必然结果（2.1、2.2、实验 1）。
2. **档案必须是固定对象。** `profileForChapter(profile.chapter) !== profile` 就拒绝启动。26 个能力位理论上有 2²⁶ 种组合，只有 20 种被测过（2.3、实验 1）。
3. **共享关系用对象身份检查，不用字段比对。** 四组"必须同一个对象"的关系（Cron↔background、Teammate↔background/Cron、Protocol↔Teammate、Worktree↔WorkStealing），错一个都启动不了（3.4、实验 2）。
4. **一次模型回复共用一份工具快照。** `#prepareModelRequest()` 里 505 行渲染提示、511 行拍快照，只隔六行，所以状态和工具面不可能互相矛盾。同轮抢跑新工具必然是 `unknown_tool`（4.1、5.1、实验 4、实验 5）。
5. **一个 tool call 恰好一条 tool result。** 五种偏差全部拒绝：缺结果、id 不匹配、中间插消息、孤儿结果、重复回填。`validateToolPairing` 在一次请求里被调三次，输入各不相同（4.3、5.2、实验 6）。
6. **deny 强于 ask。** `strongestDecision` 按 `["deny","ask","allow"]` 找第一个命中，所以越界写根本到不了审批环节 —— 实验 4 第 10 行的空数组就是这个意思（5.4、5.5、实验 4）。
7. **`runtime_status` 只读同步 getter，永远在最后一段，且参与缓存键。** 它是观察窗口，不是调度器，也不是授权来源。状态一变缓存必然 miss，但前面的稳定段落一个字都不动（6.1-6.4、实验 3）。
8. **三个来源，一个收件箱。** 后台、Cron、队友都通过 `EventInbox` 进历史。去重是两层：内存 `#seenEventIds` 防进程内重投，store 里的 ack 防跨重启重放 —— 别把这两层记混（9.1-9.3、实验 8、测试 6）。
9. **恢复过程一条都不进 canonical history。** 429 / 529 / `length` / `prompt-too-long` 全部在模型边界内消化。六次真实失败之后 history 长度仍然是 2，因为 `requestMessages` 是本地冻结副本（10.1-10.3、实验 9）。
10. **逆序关闭，不吞异常，不半关。** `[...this.#resources].reverse()`，每个资源独立 try/catch，一个失败原样抛、多个失败聚合成 `AggregateError`，`#closed = true` 只在全部成功时才置位（12.1-12.4、实验 7）。

### 一张图回顾整条链路

```text
  ① 装配（启动时，创建任何状态之前）    buildAgent(P20, dependencies)
  ────────────────────────────────    ┌──────────────────────────────────┐
                                      │ validateBuildDependencies()       │
                                      │  a. 档案身份                       │
                                      │     profileForChapter(20) === P20？│
                                      │  b. 能力门禁 × 每项依赖             │
                                      │     有依赖没能力 → 炸               │
                                      │     有能力没依赖 → 炸（双向）        │
                                      │  c. 对象身份 × 四组共享关系          │
                                      │     Cron    ↔ background/inbox    │
                                      │     Teammate↔ background/Cron     │
                                      │     Protocol↔ Teammate            │
                                      │     Worktree↔ WorkStealing        │
                                      │                                   │
                                      │ 全过 → 装 23 个工具 + statusProvider│
                                      │ 任一条不过 → throw，零副作用         │
                                      └────────────────┬─────────────────┘
                                                       │
  ② 一个回合（run / runEvents）                        ▼
  ────────────────────────────    ┌──────────────────────────────────────┐
                                  │ #prepareModelRequest()  loop.ts:475   │
                                  │   479  validateToolPairing(#history)  │
                                  │   483  historyProcessor.prepare()     │
                                  │   491  validateToolPairing(request..) │
                                  │   493  turnLifecycle.beforeModel()    │
                                  │   502  toolRoundObserver.beforeModel()│
                                  │   505  渲染系统提示 ← runtime_status   │
                                  │   511  tools = #tools.snapshot()  ←┐  │
                                  │   510  validateToolPairing(msgs)   │  │
                                  │                                   │  │
                                  │   505 和 511 隔六行：状态与工具面 ──┘  │
                                  │   必然来自同一时刻                    │
                                  └────────────────┬─────────────────────┘
                                                   │
  ③ 模型边界（失败在这里消化）                      ▼  model.complete()
  ────────────────────────────    ┌──────────────────────────────────────┐
                                  │ 429  ModelRateLimitError             │
                                  │        → 按 Retry-After 等待重试      │
                                  │ 529  ModelOverloadedError            │
                                  │        → 累计 3 次切 fallback 模型     │
                                  │ finishReason === "length"            │
                                  │        → 8000 抬到 64000，残片丢弃    │
                                  │ ModelPromptTooLongError              │
                                  │        → 反应式压缩一次               │
                                  │                                      │
                                  │ 全部作用在 requestMessages（本地冻结） │
                                  │ → canonical history 一条都不多        │
                                  └────────────────┬─────────────────────┘
                                                   │
  ④ 工具执行（每个调用逐条过）                      ▼  tool_calls[]
  ────────────────────────────    ┌──────────────────────────────────────┐
                                  │ snapshot.prepare(name, args)          │
                                  │   不在快照里 → unknown_tool（定义）    │
                                  │ ↓                                    │
                                  │ 权限管道 strongestDecision            │
                                  │   ["deny", "ask", "allow"] 取第一个    │
                                  │   ① workspace 边界   → deny 就到此为止 │
                                  │   ② shell 默认                        │
                                  │   ③ request.recommendations           │
                                  │   ④ #evaluateRules                    │
                                  │   ⑤ 只有 ask 才问人                   │
                                  │ ↓                                    │
                                  │ invoke() → 结果按 id 一对一回填        │
                                  │   超大结果 → artifact，留 path         │
                                  └────────────────┬─────────────────────┘
                                                   │
  ⑤ 事件（三个来源，一个收件箱）                    ▼  runEvents()
  ────────────────────────────    ┌──────────────────────────────────────┐
                                  │ background ┐                          │
                                  │ Cron       ├→ EventInbox → user 消息   │
                                  │ teammate   ┘                          │
                                  │                                      │
                                  │ 去重两层：                             │
                                  │   #seenEventIds  防进程内重投          │
                                  │   store 里的 ack  防跨重启重放         │
                                  └────────────────┬─────────────────────┘
                                                   │
  ⑥ 关闭（逆序，不吞，不半关）                      ▼  runner.close()
  ────────────────────────────    ┌──────────────────────────────────────┐
                                  │ [...#resources].reverse()             │
                                  │   mcp → teammate → cron → supervisor │
                                  │                                      │
                                  │ 每个资源独立 try/catch，失败继续走完    │
                                  │   1 个失败 → 原样抛                   │
                                  │  >1 个失败 → AggregateError           │
                                  │                                      │
                                  │ #closed = true 只在全部成功时置位      │
                                  │ 之后 run() → AgentRunner is closed    │
                                  └──────────────────────────────────────┘
```

六个编号就是本章六件事的位置：① 错误的共享在启动时炸 ② 状态与工具面同时取样 ③ 失败在模型边界内消化 ④ deny 强于 ask，越界到不了审批 ⑤ 三个来源一个收件箱、两层去重 ⑥ 逆序关闭且不半关。

### 检查你是否真的读懂了

不看前文，试着回答。答不上来的，括号里是对应小节或实验：

1. `full_harness` 这个能力位在整个代码库里有几处读取它？读到它之后装了什么？（2.1、6.2）
2. P20 有 26 个能力位。为什么 `buildAgent` 不允许你自己拼一个 `{chapter: 20, capabilities: ...}` 传进去，哪怕能力集完全一样？（2.3、实验 1）
3. ch19 的 `profiles.ts` 是 326 行，ch20 是 152 行，能力还多了一个。少掉的 174 行由哪两段样板组成？改成增量推导之后，什么错误从"能编译通过"变成了"不可能发生"？（从 ch19 到 ch20）
4. `cronRuntime` 的 supervisor 和 `backgroundSupervisor` 是两个不同对象，但字段值完全一样。启动会成功吗？为什么检查的是身份不是值？（3.4、实验 2）
5. 只把 `protocolRuntime` 换成另一个（`teammateRuntime` 不动），报的是哪一条错？如果两个一起换、而且互相匹配呢？（3.4、实验 2）
6. 一次模型请求里，系统提示在第几行渲染，工具快照在第几行拍？为什么这两个数字之间的距离很重要？（4.1、4.2）
7. `validateToolPairing` 在 `#prepareModelRequest()` 里被调了三次。三次的输入分别是什么？为什么不能只调一次？（4.3）
8. 模型在同一条回复里先调 `connect_mcp`、紧接着调 `mcp__fake__lookup`，第二个的结果是什么？下一轮呢？工具数从几变成几？（5.1、实验 4、实验 5）
9. 在一个快照上调 `register()` 会怎样？在活注册表上按名字撤销一个 MCP 工具会怎样？为什么撤销要按对象身份？（5.1、实验 5）
10. 模型想写 workspace 外的文件，`external` 效果、审批规则都配好了。审批提供者会被问到吗？为什么？（5.4、5.5、实验 4）
11. `runtime_status` 里有 `mcp_connections: ["fake"]`。模型能据此认为自己有权调用 `mcp__fake__lookup` 吗？谁才是权限事实源？（6.4、实验 3）
12. 只改 `status`、其他输入全不变，再渲染一次，`cacheHits` 会加一吗？提示词里 `runtime_status` 之前的正文会变吗？（6.3、实验 3）
13. Lead 有 23 个工具，队友有几个内置工具？队友能看到 `connect_mcp` 吗？Lead 能看到 `submit_plan` 吗？（7.1-7.3、实验 10）
14. 同一个 `eventId` 投递两次，`runEvents()` 第二次返回什么？canonical history 里那条 `runtime_event` 有几条？（9.2、实验 8）
15. 进程重启之后，靠什么保证已经处理过的事件不被重放？内存里的 `#seenEventIds` 在这里帮得上忙吗？（9.2、实验 8、测试 6）
16. 模型侧连续抛出 429、529×3、`length`、`prompt-too-long` 六种失败。主请求发了几次？最终 canonical history 有几条消息？`length` 那条残片在里面吗？（10.1-10.3、实验 9）
17. 529 连续几次才切 fallback？切了之后 `maxTokens` 是多少、什么时候变的？（10.1、实验 9）
18. 注册顺序是 supervisor → cron → teammate → mcp，关闭顺序是什么？为什么必须反过来？（12.1、12.2、实验 7）
19. `close()` 时两个资源都抛错，第三个资源还会被关吗？调用方收到的是哪个异常？如果只有一个失败呢？（12.2、实验 7）
20. `close()` 失败之后 `#closed` 是 `true` 还是 `false`？再调一次 `close()` 会发生什么？再调 `run()` 呢？（12.2、实验 7）
21. ch19 有一条 `mcp-external-approval` 规则，ch20 没有了。`connect_mcp` 现在还会被审批吗？你会用哪两条命令确认这件事？（11.2-11.5）

### 二十章走完了

这一章没有教新东西。它做的是另一件事：**把前十九章的机制放进同一个进程，然后确认它们不打架**。

十九章各自解决一件事的时候，每件事都可以单独讲清楚。装到一起，问题就变了 —— 动态提示要不要看到 MCP 连接状态？后台任务完成的通知从哪进历史？某个资源关不掉的时候，剩下的资源还关不关？这些问题在单章里根本不存在。

P20 的答案里没有新组件，只有三样东西：

- **一条固定的执行顺序**，所以每个机制都知道自己在什么时候被调用；
- **三条不变量**，所以机制之间的交互有明确的合法/非法边界；
- **一组启动时门禁**，所以错误的装配活不到运行时。

46 行新代码，加上 `profiles.ts` 删掉的 174 行样板 —— 整合章在源码上的净成果是负数。这不是巧合。**一个设计对了的架构，把零件装到一起的时候不需要新写胶水**；需要大量胶水的时候，说明零件之间的边界当初就没划对。

如果你从第 1 章跟到这里，现在手里的东西是：一个能调工具、能读写文件、能被权限管住、能被 Hook 介入、有 TODO、能派子 Agent、能加载 Skill、能落 artifact、能压缩上下文、能记事、提示词是动态的、模型抖动能自己恢复、有任务 DAG、能跑后台、能定时、能带队友、能走协议审批、任务能被抢占、能开 worktree、能接 MCP 的 Agent。二十章，一步一个能力，每一步都有测试守着。

它不是 Claude Code —— 我们简化掉的地方在《与 Claude Code 的差异》里列得很清楚。但它是一个**你完整读过、每一行都知道为什么在那里**的 Agent。这比用一个更强但看不见内部的框架，能让你走得更远。
